IF a AND b OR c THEN work; END IF;
IF a THEN IF b THEN nested_work; END IF; END IF;
value := '' || id;
DECLARE BEGIN work; END;
IF first_condition THEN first_work; ELSIF other_condition THEN other_work; ELSIF first_condition THEN final_work; END IF;
IF value IN (a, fn(b, c), a) THEN in_work; END IF;
BEGIN NULL; END;
PROCEDURE implicit_mode(value NUMBER) IS BEGIN work; END;
FUNCTION output_mode(value OUT NUMBER) RETURN NUMBER IS BEGIN RETURN 1; END;
IF account.id = account.id THEN identical_work; END IF;
IF done THEN EXIT; END IF;
IF valid THEN RETURN TRUE; ELSE RETURN FALSE; END IF;
IF branch_a THEN same_work; ELSIF branch_b THEN same_work; END IF;
IF (x = 1) AND (x = 1) THEN condition_work; END IF;
SELECT name FROM users WHERE ROWNUM <= 5 ORDER BY created DESC;
IF terminal THEN RETURN value; ELSE value := NULL; END IF;
BEGIN work; NULL; END;
IF ((left_value = right_value)) THEN parenthesis_work; END IF;
DECLARE called_value VARCHAR2(10) := lookup(5); BEGIN work; END;
DECLARE null_value VARCHAR2(10) := NULL; BEGIN work; END;
