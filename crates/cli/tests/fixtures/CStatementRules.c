void run(int x, int y) {
    if (x) work(); else work();
    while (x) work();
    if (x) {} else if (y) {}
    switch (x) {}
    switch (x) { default: break; }
    ;
    if (x);
    while (x) { break; }
    while (7) {}
}
