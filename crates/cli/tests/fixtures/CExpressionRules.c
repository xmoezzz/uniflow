int *pointer = 0;
int read(void);
void check(void) {
    int a, b, value = 0;
    return value = read();
    value = a++ + 1;
    sizeof(value++);
    value = (a, b);
    if ((value = read())) use(value);
    value = a ? b : value;
    value = a ? b + value : 0;
    RegOpenKeyExW(key, 0, 0, KEY_ALL_ACCESS, &out);
}
