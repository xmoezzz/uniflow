<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0302000010090003" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">CWE</Category>
				<Category type="SubClassChin">资源管理</Category>
				<Category type="DetailClassChin">格式化缺陷</Category>
			</Categories>
			<BugServerity>3</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行存在格式化缺陷。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行存在格式化缺陷。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[格式化对象是非线程安全的，java.text.Format中的`parse()`和`format()`方法包含一个可导致用户看到其他用户数据的race condition。  
  
**例如**：下面代码片段中，定义了一个成员变量（静态的日期格式对象）。  
```java
public class DateFormat extends Thread{  
    private static SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd");  
    private String name;  
    private String dateStr;  
    public DateFormat(String name, String dateStr) {  
        this.name = name;  
        this.dateStr = dateStr;  
    }  
    @Override  
    public void run() {  
        try {  
            Date date = sdf.parse(dateStr);  
            System.out.println("线程"+name+"运行日期:"+date);  
        } catch (ParseException e) {  
            e.printStackTrace();  
        }  
    }  
    public static void main(String[] args) {  
        ExecutorService executorService = Executors.newFixedThreadPool(3);  
        executorService.execute(new DateFormat("A", "2017-06-10"));  
        executorService.execute(new DateFormat("B", "2016-06-06"));  
        executorService.shutdown();  
    }  
}
```  
如上代码中输出会有两种情况，一种情况是报错，还有一种情况是两个线程输出一致。出现这种情况的原因是因为SimpleDateFormat类内部有一个Calendar对象引用,它用来储存和这个SimpleDateFormat相关的日期信息。这样就会导致一个问题,如果SimpleDateFormat是static的, 那么多个thread之间就会共享SimpleDateFormat, 同时也是共享Calendar引用。在高并发的情况下，容易出现幻读成员变量的现象。
]]></Description>
			<Advice><![CDATA[有如下四种解决方法  
1. 将格式化对象定义成局部变量，但是每调用一次方法意味创建一个格式化对象，浪费内存。  
2. 方法加同步锁synchronized，在同一时刻，只有一个线程可以执行类中的某个方法。这样性能较差，每次都要等待锁释放后其他线程才能进入。  
3. 使用第三方库joda-time，由第三方考虑线程不安全的问题。  
4. 使用ThreadLocal：每个线程拥有自己的格式化对象。
]]></Advice>
			<References>
				<Reference type="CWE">488:Exposure of Data Element to Wrong Session</Reference>
				<Reference type="CWE">362:Concurrent Execution using Shared Resource with Improper Synchronization ('Race Condition')</Reference>
				<Reference type="OWASP TOP 10">2007:A6 Information Leakage and Improper Error Handling</Reference>
				<Reference type="CWE/SANS TOP 25">2010:Insecure Interaction - CWE ID 362</Reference>
				<Reference type="CWE/SANS TOP 25">2009:Insecure Interaction - CWE ID 362</Reference>
				<Reference type="ISO/IEC TR 24772">2013:New Vulnerabilities Concurrent Data Access [CGX]</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>yes</Default>
			<WhiteList>not</WhiteList>
			<Security>not</Security>



			<Tag>concurrency,bad_practice</Tag>
			<DomainType>bug</DomainType>
		</BugInfo>
		