<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0302000010020021" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">CWE</Category>
				<Category type="SubClassChin">代码注入</Category>
				<Category type="DetailClassChin">有风险的SQL查询：Persistence</Category>
			</Categories>
			<BugServerity>1</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行进行了有风险的Persistence查询。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行进行了有风险的Persistence查询。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[SQL注入是一种数据库攻击手段。攻击者通过向应用程序提交恶意代码来改变原SQL语句的含义，进而执行任意SQL命令，达到入侵数据库乃至操作系统的目的。使用Persistence执行由用户输入构建的动态SQL指令，会让攻击者有机会篡改指令的含义或者执行任意的SQL命令。系统中有一些方法存在着一定风险，当方法所需的数据来源于不可信赖的数据源时，可能会导致SQL注入。  
  
**例如**：下面代码片段中，动态构造并执行了一个SQL查询来认证用户。  
```java
public void doPrivilegedAction(String username, char[] password) throws SQLException {   
    EntityManagerFactory emf = Persistence.createEntityManagerFactory("myJPA");  
    EntityManager em = emf.createEntityManager();  
    String pwd = hashPassword(password);   
    String sqlString = "SELECT * FROM db_user WHERE username = '" + username + "' AND password = '" + pwd + "'";   
    Query createQuery = em.createQuery(sqlString);  
    //...  
    Object singleResult = createQuery.getSingleResult();  
    if(object == null){  
        //...  
    }  
    //...  
}
```  
如果攻击者能够替代`username`和`password`中的任意字符串，它们可以使用下面的关于`username`的字符串进行SQL注入。  
`validuser' OR '1'='1`   
当其注入到命令时，命令就会变成：  
`SELECT * FROM db_user WHERE username='validuser' OR '1'='1' AND password=''`   
同样，攻击者可以为password提供如下字符串。  
`' OR '1'='1`   
当其注入到命令时，命令就会变成：  
`SELECT * FROM db_user WHERE username='' AND password='' OR '1'='1'`
]]></Description>
			<Advice><![CDATA[造成SQL注入攻击的根本原因在于攻击者可以改变SQL查询的上下文，使程序员原本要作为数据解析的数值，被篡改为命令了。防止SQL注入：JDO可以在构造SQL指令时需要加入约束条件，比如通过创建一份合法字符串列表，使其对应于可能要加入到SQL指令中的不同元素，来避免SQL注入攻击。另外，由于SQL注入只对SQL语句的编译过程有破坏作用，因此可以使用预编译语句集提前编译好SQL语句，待执行阶段才把参数传入。
**例如**：下面是存在SQL注入漏洞的代码片段。
```java
EntityMangerFactory factory = Persistence.createEntityMangerFactory("unit");
EntityManager manager = factory.createEntityManager();
TypedQuery<UserEntity> query = manager.createQuery(String.format("select * from Users where name = %s", username), UserEnetity.class);
UserEntity res = query.getSingleResult();
```
针对上面存在SQL注入漏洞的代码的修复方案如下。
```java
TypedQuery<UserEntity> query = manager.createQuery("select * from Users where name = usernamePara", UserEntity.class).setParameter("usernameParam", username);
UserEntity res = query.getSingleResult();
```
]]></Advice>
			<References>
				<Reference type="CWE">89:Improper Neutralization of Special Elements used in an SQL Command ('SQL Injection')</Reference>
				<Reference type="CERT">IDS00-J:Prevent SQL injection</Reference>
				<Reference type="OWASP TOP 10">2017:A1 Injection</Reference>
				<Reference type="OWASP TOP 10">2013:A1 Injection</Reference>
				<Reference type="OWASP TOP 10">2010:A1 Injection</Reference>
				<Reference type="OWASP TOP 10">2007:A2 Injection Flaws</Reference>
				<Reference type="OWASP TOP 10">2004:A6 Injection Flaws</Reference>
				<Reference type="CWE/SANS TOP 25">2019:CWE ID 89</Reference>
				<Reference type="CWE/SANS TOP 25">2011:Insecure Interaction - CWE ID 89</Reference>
				<Reference type="CWE/SANS TOP 25">2010:Insecure Interaction - CWE ID 89</Reference>
				<Reference type="CWE/SANS TOP 25">2009:Insecure Interaction - CWE ID 89</Reference>
				<Reference type="ISO/IEC TR 24772">2013:Application Vulnerabilities Injection [RST]</Reference>
				<Reference type="GB/T 34944-2017 Java">数据处理:SQL注入</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>yes</Default>
			<WhiteList>not</WhiteList>
			<Security>yes</Security>



			<Tag>security</Tag>
			<DomainType>security</DomainType>
		</BugInfo>
		