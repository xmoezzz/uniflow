<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0302000010130035" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">CWE</Category>
				<Category type="SubClassChin">资源管理</Category>
				<Category type="DetailClassChin">资源未删除：临时文件</Category>
			</Categories>
			<BugServerity>1</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行临时文件没有释放。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行临时文件没有释放。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[程序创建或分配临时文件句柄后，没有进行删除，这将会降低系统性能，攻击者还可能会通过耗尽资源池的方式发起拒绝服务攻击。  
  
**例如**：下面代码片段中，创建了一个临时文件句柄tempFile，生成临时文件并得不到有效删除。  
```java
...  
File tempFile = File.createTempFile("temp", ".txt");  
...
```
]]></Description>
			<Advice><![CDATA[程序中在使用创建或分配临时文件句柄后，应该释放文件句柄资源。  
  
**例如**：下面代码片段中，使用完之前创建的文件句柄tempFile后，在finally代码块中进行了释放。  
```java
File tempFile = null;  
try {  
    ...  
    tempFile = File.createTempFile("temp", ".txt");  
    ...  
} catch (IOException e) {  
    e.printStackTrace();  
}finally{  
    if(tempFile!=null&&tempFile.exists()){  
        try {  
            tempFile.delete();  
        } catch (IOException e) {  
            e.printStackTrace();  
        }  
    }  
}
```
]]></Advice>
			<References>
				<Reference type="CWE">459:Incomplete Cleanup</Reference>
				<Reference type="CWE">404:Improper Resource Shutdown or Release</Reference>
				<Reference type="CERT">FIO04-J:Close resources when they are no longer needed</Reference>
				<Reference type="OWASP TOP 10">2004:A9 Application Denial of Service</Reference>
				<Reference type="CWE/SANS TOP 25">2009:Risky Resource Management - CWE ID 404</Reference>
				<Reference type="SJ/T 11683-2017">6.6.1 资源泄漏</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>not</Default>
			<WhiteList>not</WhiteList>
			<Security>not</Security>



			<Tag>bad_practice</Tag>
			<DomainType>bug</DomainType>
		</BugInfo>
		