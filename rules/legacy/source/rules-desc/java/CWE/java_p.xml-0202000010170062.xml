<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0202000010170062" language="Java"> 
      <Type>guide</Type>  
      <Categories> 
        <Category type="ClassChin">CWE</Category>  
        <Category type="SubClassChin">7.2 内存管理</Category>  
        <Category type="DetailClassChin">A.4.2.2 避免拒绝服务攻击</Category> 
      </Categories>  
      <BugServerity>3</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[规范说明：拒绝服务是攻击者通过极度消耗应用资源，以致程序崩溃或其他合法用户无法进行使用的一种攻击方式。  
对于避免拒绝服务攻击的情况，示例1给出了不规范用法（java语言）示例。示例2给出了规范用法（java语言）示例。  
**示例1**：   
```java
FileInputStream fileInputStream = new FileInputStream(file);  
fileInputStream.read(bytes, 0, length);  
...
```  
上述代码片段中，攻击者控制了输入流的长度length，当文件足够大和length足够长时，会导致DOS攻击。拒绝服务攻击是一种滥用资源性的攻击。从程序源代码角度讲，对涉及到系统资源的外部数据应该进行严格校验，防止无限制的输入。  
**示例2**：  
```java
static final int MAX = 0x3200000; // 50MB
// ...
// write the files to the disk, but only if file is not insanely big
if (file.length > MAX) {
    throw new IllegalStateException("File is huge.");
}
FileOutputStream fos = new FileOutputStream(file_name);
bop = new BufferedOutputStream(fos, SIZE);
while ((count = fileInputStream.read(bytes)) != -1) {
    bop.write(bytes, 0, count);
}
```  
上述代码片段中，对文件大小进行检验，并不设置读取长度。
]]></Description>  
      <Advice><![CDATA[]]></Advice>  
      <References> 
        <Reference type="CWE">730:OWASP Top Ten 2004 Category A9 - Denial of Service</Reference>  
        <Reference type="OWASP TOP 10">2004:A9 Application Denial of Service</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>yes</Default>  
      <WhiteList>yes</WhiteList>  
      <Security>yes</Security>  
      <BigClassChin>A.4.2 内存管理</BigClassChin>  
       
    </BugInfo>  
    