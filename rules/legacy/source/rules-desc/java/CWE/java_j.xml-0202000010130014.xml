<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0202000010130014" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">CWE</Category>
				<Category type="SubClassChin">代码质量</Category>
				<Category type="DetailClassChin">冗余的初始化</Category>
			</Categories>
			<BugServerity>1</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行进行了冗余的初始化。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行进行了冗余的初始化。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[程序未使用变量的初始值，变量初始化后，被重新赋值或者转向作用域之外。  
  
**例如**：下列程序声明一个变量`total`并赋值，后续并未使用所赋的值，再次赋值。  
```java
...  
double total = getTotal();  
total = getOtherTotal();  
...
```
]]></Description>
			<Advice><![CDATA[为了使代码易于理解和维护，删除不必要的赋值。
]]></Advice>
			<References>
				<Reference type="CWE">398:Indicator of Poor Code Quality</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>yes</Default>
			<WhiteList>not</WhiteList>
			<Security>not</Security>



			<Tag>bad_practice</Tag>
			<DomainType>quality</DomainType>
		</BugInfo>
		