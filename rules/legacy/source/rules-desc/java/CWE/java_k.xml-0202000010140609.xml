<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0202000010140609" language="Java"> 
      <Type>cert</Type>  
      <Categories> 
        <Category type="ClassChin">CWE</Category>  
        <Category type="SubClassChin">Rule 05. 面向对象 (OBJ)</Category>  
        <Category type="DetailClassChin">OBJ09-J：比较类而不是类名</Category> 
      </Categories>  
      <BugServerity>1</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[在JVM中，如果两个类可以被同一个类装载器装载，并且拥有相同的全名，那么这两个类被认为是相同的类（并且具有相同的类型）。  
有时候需要检测一个指定的对象是否是特定的类型，或者两个对象的类型是否相同，例如：实现`equals()`接口的时候。如果这个比较不能正确完成，代码会认为这两个对象是同样的类，但它们实际上却不相同。  
如果一个受保护的资源的访问是授权的，并且该授权是基于类名的比较，如果攻击者提供一个完全与目标类全名相同的恶意类，那么攻击者就能访问到受保护的资源。  
风险评估：仅仅使用类的名称来比较类，会允许恶意类绕过安全检查并且获得受保护的资源。  
**不规范的代码样例1**：  
```java
// Determine whether object auth has required/expected class object  
if (auth.getClass().getName().equals("com.application.auth.DefaultAuthenticationHandler")) {  
  // . . .访问受保护资源的代码  
}
```
]]></Description>  
      <Advice><![CDATA[比较两个类是否相同的时候，应该比较两个类对象。  
**规范的代码样例1**：  
```java
// Determine whether object auth has required/expected class name  
if (auth.getClass() == this.getClass().getClassLoader().loadClass("com.application.auth.DefaultAuthenticationHandler")) {  
   // . . .访问受保护资源的代码  
}
```
]]></Advice>  
      <References> 
        <Reference type="CWE">486:Comparison of Classes by Name</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>yes</Default>  
      <WhiteList>yes</WhiteList>  
      <Security>not</Security>  
       
    </BugInfo>  
    