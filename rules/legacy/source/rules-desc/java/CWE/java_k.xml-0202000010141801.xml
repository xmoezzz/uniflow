<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0202000010141801" language="Java"> 
      <Type>cert</Type>  
      <Categories> 
        <Category type="ClassChin">CWE</Category>  
        <Category type="SubClassChin">Rule 49. 其他 (MSC)</Category>  
        <Category type="DetailClassChin">MSC01-J：不要使用空的无限循环</Category> 
      </Categories>  
      <BugServerity>1</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[一个空的无限循环会消耗大量的CPU计算资源而不做任何事情。优化编译器和即时系统允许去除这样的循环。  
**不规范的代码样例1**：  
代码实现了一个闲置任务，这个任务会连续的执行一个循环，却在这个循环中不执行任何指令。一个优化编译器或者即时系统会去掉这个样例中的while循环。  
```java
public int nop() {  
    while (true) {}  
}
```
]]></Description>  
      <Advice><![CDATA[**规范的代码样例1**：  
通过在while循环中调用`Thread.sleep()`方法来避免毫无意义的无限循环。这个循环体包含一些有语义的操作，并且不会被优化掉。  
```java
public final int DURATION = 10000; // in milliseconds

public void nop() throws InterruptedException {
    while (true) {
        // Useful operations
        Thread.sleep(DURATION);
    }
}
```  
**规范的代码样例2**：  
通过在while循环中调用`Thread.yield()`方法，它会使得这个方法中运行的线程被其他线程延迟。  
```java
public void nop() {
    while (true) {
        Thread.yield();
    }
}
```
]]></Advice>  
      <References> 
        <Reference type="CWE">835:Loop with Unreachable Exit Condition ('Infinite Loop')</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>yes</Default>  
      <WhiteList>not</WhiteList>  
      <Security>not</Security>  
       
    </BugInfo>  
    