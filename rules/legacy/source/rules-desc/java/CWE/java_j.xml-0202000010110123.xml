<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0202000010110123" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">CWE</Category>
				<Category type="SubClassChin">代码质量</Category>
				<Category type="DetailClassChin">调用AccessController特权代码的方法的访问标识符为Public</Category>
			</Categories>
			<BugServerity>1</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行的+'`'+${funcName}+'`'+方法的访问标识符为Public且调用AccessController特权代码。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行所在的方法的访问标识符为Public且调用AccessController特权代码。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[如果方法带有public访问标识符，则表明任意外部代码都可以调用该方法，如果代码会动态进入系统的库或在环境中共享代码，则用于执行特权（privileged）操作的公共方法可能非常危险（**例如**：代码注入、添加危险文件、上传文件等）。  
  
**例如**：在下列代码中，`doPrivilegedOpenFile()`声明为public，并且执行了一个权限操作。  
```java
public static void doPrivilegedOpenFile(final String filePath) {  
    final MyPrivilegedAction mpAction  = new MyPrivilegedAction(filePath);  
    FileInputStream fis = null;  
    ...  
    fis = (FileInputStream)AccessController.doPrivileged(mpAction);  
    ...  
}
```
]]></Description>
			<Advice><![CDATA[将包含特权`(privileged)`代码的方法设置为private，并且确保调用这些方法过程中的安全性，比如验证输入、凭证检查等，在无需特权调用的地方不使用特权调用。  
  
**例如**：以下代码中`doPrivilegedOpenFile()`定义为private。
```java  
private static void doPrivilegedOpenFile(final String filePath) {  
    final MyPrivilegedAction mpAction = new MyPrivilegedAction(filePath);  
    FileInputStream fis = null;  
    ...  
    fis = (FileInputStream)AccessController.doPrivileged(mpAction);  
    ...  
}
```
]]></Advice>
			<References>
				<Reference type="CWE">265:Privilege / Sandbox Issues</Reference>
				<Reference type="OWASP MOBILE TOP 10">2014:M5 Poor Authorization and Authentication</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>yes</Default>
			<WhiteList>not</WhiteList>
			<Security>yes</Security>



			<Tag>security,bad_practice</Tag>
			<DomainType>security</DomainType>
		</BugInfo>
		