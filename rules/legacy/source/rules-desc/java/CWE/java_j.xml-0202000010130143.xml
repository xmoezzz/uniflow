<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0202000010130143" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">CWE</Category>
				<Category type="SubClassChin">代码质量</Category>
				<Category type="DetailClassChin">不安全的可变对象声明</Category>
			</Categories>
			<BugServerity>1</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行声明的对象可变。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行声明的对象可变。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[在Java Applet程序中，程序声明了一个final的public static可变对象，虽然对象被声明为final，保证了对象对象本身只分配一次，但是由于对象是公共的，但是仍然可以改变对象中的数据。  
  
**例如**：下面代码片段中，程序错误的声明了一个final的public static 的date。
  
```java
public final class urlTool extends Applet {  
    public final static Date date;  
    ...  
  
}
```  
date虽然被声明为final，但是仍然可以通过`setTime()`改变其内部数据。
]]></Description>
			<Advice><![CDATA[检查程序逻辑，判断是否需要final的public static可变对象。
]]></Advice>
			<References>
				<Reference type="CWE">607:Public Static Final Field References Mutable Object</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>not</Default>
			<WhiteList>not</WhiteList>
			<Security>yes</Security>



			<Tag>security</Tag>
			<DomainType>security</DomainType>
		</BugInfo>
		