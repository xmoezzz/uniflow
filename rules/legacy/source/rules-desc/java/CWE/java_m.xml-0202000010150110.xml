<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0202000010150110" language="Java"> 
      <Type>std</Type>  
      <Categories> 
        <Category type="ClassChin">CWE</Category>  
        <Category type="SubClassChin">API误用</Category>  
        <Category type="DetailClassChin">【强制】不要调用notify()</Category> 
      </Categories>  
      <BugServerity>1</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[程序调用`notify()`方法无法指定将唤醒哪个线程。  
  
**例如**：下面代码片段中，线程执行步骤由`time`字段决定，一个线程执行完操作后，将`time`的值加一，然后通知负责下一个步骤的线程。  
```java
public final class ProcessStep implements Runnable {  
    private static final Object lock = new Object();  
    private static int time = 0;  
    private final int step; // Perform operations when field time  
    // reaches this value  
    public ProcessStep(int step) {  
        this.step = step;  
    }  
    @Override public void run() {  
        try {  
            synchronized (lock) {  
                while (time != step) {  
                    lock.wait();  
                }  
                // Perform operations  
                time++;  
                lock.notify();  
            }  
        } catch (InterruptedException ie) {  
            Thread.currentThread().interrupt(); // Reset interrupted status  
        }  
    }  
    public static void main(String[] args) {  
        for (int i = 4; i >= 0; i--) {  
            new Thread(new ProcessStep(i)).start();  
        }  
    }  
}
```  
由于每个线程有不同的wait条件，假如存在满足条件的线程，但是因为`notify()`方法只能唤醒一个线程，若被唤醒的线程不满足条件，则必须继续等待，程序有可能因此将会发生死锁。
]]></Description>  
      <Advice><![CDATA[使用`notifyAll()`方法代替`notify()`方法。  
  
**例如**：下列代码片段中，调用`notifyAll()`方法代替了`notify()`方法，将会唤醒所有线程，满足条件的线程将会执行它的任务，不满足条件的线程将会继续等待。  
```java
public final class ProcessStep implements Runnable {  
    private static final Object lock = new Object();  
    private static int time = 0;  
    private final int step; // Perform operations when field time  
                            // reaches this value  
    public ProcessStep(int step) {  
        this.step = step;  
    }  
    @Override public void run() {  
        try {  
            synchronized (lock) {  
                while (time != step) {  
                    lock.wait();  
                }  
                // Perform operations  
                time++;  
                lock.notifyAll(); // Use notifyAll() instead of notify()  
            }  
        } catch (InterruptedException ie) {  
            Thread.currentThread().interrupt(); // Reset interrupted status  
        }  
    }  
}
```
]]></Advice>  
      <References> 
        <Reference type="CWE">373:DEPRECATED: State Synchronization Error</Reference>  
        <Reference type="CERT">THI02-J:Notify all waiting threads rather than a single thread</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>yes</Default>  
      <WhiteList>not</WhiteList>  
      <Security>not</Security>  
      <EnClassChin>Code Quality</EnClassChin>  
        
        
        
        
        
      <Tag>bad_practice</Tag> 
    </BugInfo>  
    