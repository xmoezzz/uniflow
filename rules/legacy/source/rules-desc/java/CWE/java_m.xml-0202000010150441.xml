<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0202000010150441" language="Java"> 
      <Type>std</Type>  
      <Categories> 
        <Category type="ClassChin">CWE</Category>  
        <Category type="SubClassChin">并发规范</Category>  
        <Category type="DetailClassChin">【推荐】不要使用static修饰线程不安全的字段</Category> 
      </Categories>  
      <BugServerity>2</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[Java库中不是所有类都是线程安全的，如Calendar，XPath，或者SchemaFactory都是线程不安全的， 多线程运行时它们可能出现数据问题或异常。  
  
**例如**：下面代码示例中定义了static的Calendar。 
```java 
public class DateUtils{  
    private static Calendar calendar = Calendar.getInstance();   
    ...  
}
```
]]></Description>  
      <Advice><![CDATA[可以将其置为非静态成员变量，或在方法内使用时再声明，或者使用ThreadLocal从而使每个线程拥有自己的对象。  
  
**例如**：下面代码示例中定义了非static的Calendar。 
```java 
public class DateUtils{  
    private Calendar calendar = Calendar.getInstance();   
    ...  
}
```
]]></Advice>  
      <References> 
        <Reference type="CWE">398:Indicator of Poor Code Quality</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>yes</Default>  
      <WhiteList>not</WhiteList>  
      <Security>not</Security>  
      <EnClassChin/>  
        
        
        
        
        
      <Tag>concurrency,bad_practice</Tag> 
    </BugInfo>  
    