<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0202000010140208" language="Java"> 
      <Type>cert</Type>  
      <Categories> 
        <Category type="ClassChin">CWE</Category>  
        <Category type="SubClassChin">Rule 00. 输入验证和数据处理 (IDS)</Category>  
        <Category type="DetailClassChin">IDS08-J：净化传递给正则表达式的非受信数据</Category> 
      </Categories>  
      <BugServerity>3</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[非受信的输入应该在使用前净化，从而防止发生正则表达式注入。当用户必须指定正则表达式作为输入时，必须注意需要保证初始的正则表达式没有被无限制修改。在用户输入字符串提交给正则解析之前，进行白名单字符处理（比如字母和数字）是一个很好的输入净化策略。开发人员必须仅仅提供最有限的正则表达式功能给用户，从而减少被误用的可能。  
正则表达式注入示例  
假设一个系统日志文件包含一系列系统过程的输出信息。其中一些过程会产生公开的信息，而另一些会产生被标识为`“private”`的敏感信息。以下是这个日志文件的**例子**：  
```
10:47:03 private[423] Successful logout name: usr1 ssn: 111223333    
10:47:04 public[48964] Failed to resolve network service    
10:47:04 public[1] (public.message[49367])
Exited with exit code: 255    
10:47:43 private[423] Successful login name: usr2 ssn: 444556666    
10:48:08 public[48964] Backup failed with error: 19   
```
用户希望搜索日志文件以寻找感兴趣的信息，然而又必须防止读取私有数据。程序可能会采用以下的方式，它允许用户将搜索文本作为下面所示正则表达式的一部分：  
`(.*? +public\[\d+\] +.*<SEARCHTEXT>.*) `  
然而，攻击者可以用任何字符串替代\<SEARCHTEXT\>，这样他就可以通过下面的文本实现正则表达式注入：  
`.*)|(.* `  
注入后的正则表达式为：  
`(.*? +public\[\d+\] +.*.*)|(.*.*)`   
这个正则表达式会匹配日志文件中的所有信息，包括那些私有的信息。  
**不规范的代码样例1**：  
```java
import java.io.FileInputStream;  
import java.io.IOException;  
import java.nio.CharBuffer;  
import java.nio.MappedByteBuffer;  
import java.nio.channels.FileChannel;  
import java.nio.charset.Charset;  
import java.nio.charset.CharsetDecoder;  
import java.util.regex.Matcher;  
import java.util.regex.Pattern;  
   
public class LogSearch {  
    public static void FindLogEntry(String search) {  
        // Construct regex dynamically from user string  
        String regex = "(.*? +public\\[\\d+\\] +.*" + search + ".*)";  
        Pattern searchPattern = Pattern.compile(regex);  
        try (FileInputStream fis = new FileInputStream("log.txt")) {  
            FileChannel channel = fis.getChannel();  
            // Get the file's size and map it into memory  
            long size = channel.size();  
            final MappedByteBuffer mappedBuffer = channel.map(  
                    FileChannel.MapMode.READ_ONLY, 0, size);  
            Charset charset = Charset.forName("ISO-8859-15");  
            final CharsetDecoder decoder = charset.newDecoder();  
            // Read file into char buffer  
            CharBuffer log = decoder.decode(mappedBuffer);  
            Matcher logMatcher = searchPattern.matcher(log);  
            while (logMatcher.find()) {  
                String match = logMatcher.group();  
                if (!match.isEmpty()) {  
                    System.out.println(match);  
                }  
            }  
        } catch (IOException ex) {  
            System.err.println("thrown exception: " + ex.toString());  
            Throwable[] suppressed = ex.getSuppressed();  
            for (int i = 0; i < suppressed.length; i++) {  
                System.err.println("suppressed exception: "  
                        + suppressed[i].toString());  
            }  
        }  
        return;  
    }
}
```  
这个不规范的代码使用非受信的用户输入查找日志文件，会导致正则表达式注入。
]]></Description>  
      <Advice><![CDATA[**规范的代码样例1（白名单）**：  
```java
public static void FindLogEntry(String search) {
    // Sanitize search string
    StringBuilder sb = new StringBuilder(search.length());
    for (int i = 0; i < search.length(); ++i) {
        char ch = search.charAt(i);
        if (Character.isLetterOrDigit(ch) || ch == ' ' || ch == '\'') {
            sb.append(ch);
        }
    }
    search = sb.toString();

    // Construct regex dynamically from user string
    String regex = "(.*? +public\\[\\d+\\] +.*" + search + ".*)";
    // ...
}
```  
这个符合规则的方案过滤搜索字符串中的非字母数字的字符（除了空格和单引号外），这种方案可以阻止正则表达式注入。  
**规范的代码样例2（`Pattern.quote()`）**：
```java  
public static void FindLogEntry(String search) {
    // Sanitize search string
    StringBuilder sb = new StringBuilder(search.length());
    for (int i = 0; i < search.length(); ++i) {
        char ch = search.charAt(i);
        if (Character.isLetterOrDigit(ch) || ch == ' ' || ch == '\'') {
            sb.append(ch);
        }
    }
    search = sb.toString();

    // Construct regex dynamically from user string
    String regex = "(.*? +public\\[\\d+\\] +.*" + search + ".*)";
    // ...
}
```  
这个符合规则的方案使用`Pattern.quote()`来避免恶意字符串。`Matcher.quoteReplacement()` 方法可以被用来在正则表达式中过滤字符串。  
**规范的代码样例3**：  
另一个减少这种漏洞的方法是，在匹配前过滤敏感信息。这样的方案要求每一次日志文件定期更新时都要进行过滤，从而会产生额外的复杂性和性能损失。如果日志格式变化了，但类的设计没有反映这些变化，那么敏感数据依然可能被暴露。
]]></Advice>  
      <References> 
        <Reference type="CWE">730:OWASP Top Ten 2004 Category A9 - Denial of Service</Reference>  
        <Reference type="CWE">185:Incorrect Regular Expression</Reference>  
        <Reference type="OWASP TOP 10">2004:A9 Application Denial of Service</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>yes</Default>  
      <WhiteList>yes</WhiteList>  
      <Security>yes</Security>  
       
    </BugInfo>  
    