<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0202000010130127" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">CWE</Category>
				<Category type="SubClassChin">代码质量</Category>
				<Category type="DetailClassChin">错误的赋值操作符</Category>
			</Categories>
			<BugServerity>1</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行的赋值操作符不正确。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行的赋值操作符不正确。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[程序意图比较但是使用了错误的赋值操作符。  
  
**例如**：以下代码中`isValid = true`错误的使用了赋值操作符，if语句块中代码将会肯定执行。  
```java
public void checkValid(boolean isValid) {  
    if (isValid = true) {  
        System.out.println("Performing processing");  
        doSomethingImportant();  
    }else{  
        System.out.println("Not Valid, do not perform processing");  
        return;  
    }  
}
```
]]></Description>
			<Advice><![CDATA[检查程序逻辑，使用正确的比较操作符或者直接使用布尔值。  
  
**例1**：下面代码片段中，程序使用正确的比较操作符进行比较。  
```java
public void checkValid(boolean isValid) {  
    if (isValid == true) {  
        System.out.println("Performing processing");  
        doSomethingImportant();  
    }else{  
        System.out.println("Not Valid, do not perform processing");  
        return;  
    }  
}
```  
**例2**：下面代码片段中，程序直接使用布尔值判断。  
```java
public void checkValid(boolean isValid) {  
    if (isValid) {  
        System.out.println("Performing processing");  
        doSomethingImportant();  
    }else{  
        System.out.println("Not Valid, do not perform processing");  
        return;  
    }  
}
```
]]></Advice>
			<References>
				<Reference type="CWE">481:Assigning instead of Comparing</Reference>
				<Reference type="CWE">398:Indicator of Poor Code Quality</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>yes</Default>
			<WhiteList>not</WhiteList>
			<Security>not</Security>



			<Tag>correctness</Tag>
			<DomainType>bug</DomainType>
		</BugInfo>
		