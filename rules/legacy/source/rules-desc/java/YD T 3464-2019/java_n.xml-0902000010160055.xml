<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0902000010160055" language="Java"> 
      <Type>YD/T 3464-2019</Type>  
      <Categories> 
        <Category type="ClassChin">YD/T 3464-2019</Category>  
        <Category type="SubClassChin">6.2 面对对象程序安全</Category>  
        <Category type="DetailClassChin">C.6.6 确保构造函数不会调用可覆写的方法</Category> 
      </Categories>  
      <BugServerity>2</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[1) 规定   
构造函数只能调用 final或 private的方法，即不可覆写的方法。   
2) 原理   
和 C++不同，在创建类的新实例时，Java编程语言没有为方法派生指定变更的规则。如果在初始化的对象的子类中覆写了被调用的方法，则将会使用这些覆写方法，即使新实例还没有完全初始化。   
在创建对象时调用可覆写的方法可能造成使用未初始化的数据，进而导致执行异常或是预期外的结果。在构造函数中调用可覆写的方法也会在对象创建完成前泄漏 this 引用，这使得其他线程可能访问到未初始化或不一致的数据。因此，构造函数只能调用 final或 private 的方法。   
3) 示例   
    这个不符合规则的代码示例中，方法`doLogic()`使用了未初始化的数据。   
    ```java
    class Subclass extends Superclass {
        private String color = null;
    
        public Subclass() {
            super();
            color = "Red";
        }
    
        public void doLogic() {
            System.out.println("This is subclass! The color is :" + color); // . . .
            public class Overridable {
                public static void main(String[] args) {
                    Superclass be = new Superclass();
                    // Prints "This is superclass!"
                    Superclass sc = new Subclass();
                    // Prints "This is subclass! The color is :null"
                }
            }
        }
    }
    ```
       
    对应的符合规定的解决方案是：将方法`doLogic()`声明为 final，从而确保`doLogic()`不会被覆写。   
    ```java
    class Superclass {
        public Superclass() {
            doLogic();
        }
    
        public final void doLogic() {
            System.out.println("This is superclass!");
        }
    }
    ```
]]></Description>  
      <Advice><![CDATA[]]></Advice>  
      <References> 
        <Reference type="CERT">MET05-J:Ensure that constructors do not call overridable methods</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>not</Default>  
      <WhiteList>not</WhiteList>  
      <Security>not</Security>  
      <BigClassChin>C.6 方法</BigClassChin>  
       
    </BugInfo>  
    