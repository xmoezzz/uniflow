<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0502000010170005" language="Java"> 
      <Type>guide</Type>  
      <Categories> 
        <Category type="ClassChin">ISO/IEC TR 24772</Category>  
        <Category type="SubClassChin">5.1 数据清洗</Category>  
        <Category type="DetailClassChin">A.2.1.1.6 预防XML注入</Category> 
      </Categories>  
      <BugServerity>5</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[规范说明：如果用户有能力使用结构化XML文档作为输入，那么他能够通过在数据字段中插入XML标签来重写这个XML文档的内容。XML解析器会将这些标签按照正常标签进行解析。下面是一段在线商店的XML代码，主要用于查询后台数据库。  
```xml
<item>
	<description>Widget</description>
	<price>500.0</price>
	<quantity>1</quantity>
</item>
```
恶意用户可以在<quantity>元素中输入以下字符串：1</quantity><price>1.0</price><quantity>1  
会生成以下的XML文档：  
```xml
<item>
	<description>Widget</description>
	<price>500.0</price>
	<quantity>1</quantity><price>1.0</price><quantity>1</quantity>
</item>  
```
通过使用简单的API解析器（`org.xml.sax and javax.xml.parsers.SAXParser`）可以解析该XML文件，如果解析XML的代码获取的是最后一个元素`<price>`的值，那么商品价格就被设置为1.0。  
对于预防XML注入的情况，示例1给出了不规范用法（java语言）示例。示例2给出了规范用法（java语言）示例。  
**示例1**：  
```java
public class OnlineStore {
    private static void createXMLStreamBad(final BufferedOutputStream outStream, final String quantity)
            throws IOException {
        String xmlString = "<item>\n<description>Widget</description>\n" + "<price>500</price>\n" + "<quantity>"
                + quantity + "</quantity></item>";
        outStream.write(xmlString.getBytes());
        outStream.flush();
    }
}
```  
上面的代码样例中，一个方法简单的使用了字符串拼接来创建一个XML查询，然后将其发送到服务器。在这时就有可能出现XML注入问题，因为这个方法没有进行任何输入验证。  
当XML可能已经载入还未处理的输入数据时，一般情况下使用XML模板或者DTD验证XML。如果还没有创建这样的XML字符串，那么应在创建XML之前处理输入，这种方式性能较高。  
**示例2（输入验证）**：  
```java
public class OnlineStore {
    private static void createXMLStream(final BufferedOutputStream outStream, final String quantity)
            throws IOException, NumberFormatException {
        // Write XML string only if quantity is an unsigned integer (count).
        int count = Integer.parseUnsignedInt(quantity);
        String xmlString = "<item>\n<description>Widget</description>\n" + "<price>500</price>\n" + "<quantity>"
                + count + "</quantity></item>";
        outStream.write(xmlString.getBytes());
        outStream.flush();
    }
}
```  
代码的解决方案是验证`quantity`是一个无符号整数。
]]></Description>  
      <Advice><![CDATA[]]></Advice>  
      <References> 
        <Reference type="CWE">112:Missing XML Validation</Reference>  
        <Reference type="CWE">91:XML Injection (aka Blind XPath Injection)</Reference>  
        <Reference type="CERT">IDS16-J:Prevent XML Injection</Reference>  
        <Reference type="CERT">IDS16-J:Prevent XML Injection,IDS17-J:Prevent XML External Entity Attacks</Reference>  
        <Reference type="OWASP TOP 10">2017:A1 Injection</Reference>  
        <Reference type="OWASP TOP 10">2013:A1 Injection</Reference>  
        <Reference type="OWASP TOP 10">2010:A1 Injection</Reference>  
        <Reference type="OWASP TOP 10">2007:A2 Injection Flaws</Reference>  
        <Reference type="OWASP TOP 10">2004:A6 Injection Flaws</Reference>  
        <Reference type="OWASP TOP 10">2004:A1 Unvalidated Input</Reference>  
        <Reference type="OWASP MOBILE TOP 10">2014:M7 Client Side Injection</Reference>  
        <Reference type="ISO/IEC TR 24772">2013:Application Vulnerabilities Injection [RST]</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>yes</Default>  
      <WhiteList>yes</WhiteList>  
      <Security>yes</Security>  
      <BigClassChin>A.2.1 数据清洗</BigClassChin>  
       
    </BugInfo>  
    