<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0102000010170030" language="Java"> 
      <Type>guide</Type>  
      <Categories> 
        <Category type="ClassChin">CERT</Category>  
        <Category type="SubClassChin">6.1 面向对象程序安全</Category>  
        <Category type="DetailClassChin">A.3.1.4 构造方法中不要调用可覆写的方法</Category> 
      </Categories>  
      <BugServerity>1</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[规范说明：在创建对象时调用可覆写的方法可能造成未初始化的数据，进而导致执行异常或是预期外的结果。在构造函数中调用可覆写的方法同时会在对象创建完成前泄露this引用，这使得其它线程可访问到未初始化或不一致的数据。  
对于构造方法中不要调用可覆写的方法的情况，示例1给出了不规范用法（java语言）示例。示例2给出了规范用法（java语言）示例。  
**示例1**：  
```java
class SuperClass {  
    public SuperClass() {  
        doLogic();  
    }  
    public void doLogic() {  
        System.out.println("This is superclass!");  
    }  
}  
class SubClass extends SuperClass {  
    private String color = null;  
    public SubClass() {  
        super();  
        color = "Red";  
    }  
    public void doLogic() {  
        System.out.println("This is subclass! The color is :" + color);  
            // ...  
    }  
}     
public class Overridable {  
    public static void main(String[] args) {  
        SuperClass bc = new SuperClass();  
        // Prints "This is superclass!"  
        SuperClass sc = new SubClass();  
        // Prints "This is subclass! The color is :null"  
    }  
}
```  
这个不符合规则的代码示例中，方法`doLogic()`使用了未初始化的数据。  
方法`doLogic()`是在基类的构造函数中调用的。当直接创建基类的实例时，将调用基类的方法`doLogic()`，从而正确的执行。然而，当子类发起基类的创建时，却调用了子类的`doLogic()`方法，这种情况下，因为子类的构造函数还没有完成执行，所有`color`的数值还是null。  
构造函数只能调用final或private的方法。  
**示例2**：  
```java
class SuperClass {  
public SuperClass () {  
    doLogic();  
}  
public final void  doLogic() {  
    System.out.println("This is superclass!");  
}  
}
```  
这个符合规范的方案将`doLogic()`声明为final的，从而确保了`doLogic()`方法不会被覆写。
]]></Description>  
      <Advice><![CDATA[]]></Advice>  
      <References> 
        <Reference type="CERT">MET05-J:Ensure that constructors do not call overridable methods</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>not</Default>  
      <WhiteList>not</WhiteList>  
      <Security>not</Security>  
      <BigClassChin>A.3.1 面向对象程序安全</BigClassChin>  
       
    </BugInfo>  
    