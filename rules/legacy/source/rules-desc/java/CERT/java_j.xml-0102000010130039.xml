<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0102000010130039" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">CERT</Category>
				<Category type="SubClassChin">代码质量</Category>
				<Category type="DetailClassChin">浮点数的字符串比较</Category>
			</Categories>
			<BugServerity>1</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行进行了浮点数的字符串比较。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行进行了浮点数的字符串比较。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[在浮点值与 String对象进行比较的过程中，必须先将浮点变量转换为String对象，常用的方法是利用`Float.toString()`和`Double.toString()`等方法，但是这些方法的执行有时会导致浮点数发生精度误差，例如浮点数0.001经转换后得到的字符串是“0.0010”；并且转换也可能得到`NaN`、`Infinity`或`-Infinity`，或者可能包含指数字段等情形。  
  
**例如**：以下示例将浮点值与 String 进行了比较，其中if表达式`s.equals("0.0002")`的返回值是false。  
```java
...  
int num = 2;  
...  
String resultString = Double.valueOf(num/10000.0).toString();  
if (s.equals("0.0002")){  
    //do something  
    ...  
}  
...
```
]]></Description>
			<Advice><![CDATA[将待比较的浮点类型字符串转换成BigDecimal类型，这样可以避免精度损失，然后以BigDecimal类型来作比较就可以得到预期的结果。  
  
**例如**：下面代码片段中将需要比较的浮点数的字符串，用BigDecimal构造方法转换成BigDecimal类型，然后再调用BigDecimal类型的`compareTo`方法来比较。  
```java
int num = 1;  
double d = Double.valueOf(num / 1000.0);  
BigDecimal data = new BigDecimal(d.toString());  
if (data.compareTo(new BigDecimal("0.001")) == 0) {  
// ...  
}
```
]]></Advice>
			<References>
				<Reference type="CWE">398:Common Weakness Enumeration</Reference>
				<Reference type="CERT">NUM11-J:Do not compare or inspect the string representation of floating-point values</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>yes</Default>
			<WhiteList>yes</WhiteList>
			<Security>not</Security>



			<Tag>correctness,bad_practice</Tag>
			<DomainType>bug</DomainType>
			<FormalizationData version="0.1">
				<CodeFragments xmlns="">
					<CodeFragment id="1" name="1.从外部数据源返回触发浮点数的字符串比较漏洞的不可信数据"/>
					<CodeFragment id="2" name="2.过滤函数"/>
					<CodeFragment id="3" name="3.传递函数"/>
					<CodeFragment id="4" name="4.中断函数"/>
					<CodeFragment id="5" name="5.存在浮点数的字符串比较漏洞的爆发函数"/>
					<CodeFragment id="6" name="6.不存在浮点数的字符串比较漏洞的其他函数"/>
					<CodeFragment id="7" name="7.触发浮点数的字符串比较漏洞"/>
					<CodeFragment id="8" name="8.程序执行正常"/>
					<CodeFragment id="9" name="9.数据验证净化"/>
					<CodeFragment id="10" name="10.数据传递中断"/>
				</CodeFragments>
				<Relationships xmlns="">
					<Relationship fromId="1" toId="2" name="不可信数据传给过滤函数"/>
					<Relationship fromId="1" toId="3" name="不可信数据传给传递函数"/>
					<Relationship fromId="1" toId="4" name="不可信数据传给中断函数"/>
					<Relationship fromId="1" toId="5" name="不可信数据直接传给爆发函数"/>
					<Relationship fromId="1" toId="6" name="不可信数据直接传给其他函数"/>
					<Relationship fromId="2" toId="9" name="执行后数据状态"/>
					<Relationship fromId="3" toId="5" name="不可信数据经过传递函数传给爆发函数"/>
					<Relationship fromId="3" toId="6" name="不可信数据经过传递函数传给其他函数"/>
					<Relationship fromId="4" toId="10" name="执行后数据状态"/>
					<Relationship fromId="5" toId="7" name="函数执行后"/>
					<Relationship fromId="6" toId="8" name="函数执行后"/>
				</Relationships>
			</FormalizationData>
		</BugInfo>
		