<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0102000010160021" language="Java"> 
      <Type>YD/T 3464-2019</Type>  
      <Categories> 
        <Category type="ClassChin">CERT</Category>  
        <Category type="SubClassChin">6.2 面对对象程序安全</Category>  
        <Category type="DetailClassChin">C.3.4 不要使用相等操作符比较两个基础数据类型的值</Category> 
      </Categories>  
      <BugServerity>3</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[1) 规定   
不要使用相等操作符比较两个基础数据类型的值。   
2) 原理   
    不能直接使用`==`或`!=`操作符比较两个基础数据类型的值，这些操作符比较的是对象引用而不是对象的值。   
    自动封装基础数据类型的数值需要使用对应的封装器。在自动封装时，这些基础结构数据被内存化：   
    如果封装的 p 的数值是 true、false、一个字节、一个在\u0000～\u007f 范围的 char 数据，或者是一个位于-128～127 的 int 或 short 数据，对 p 的任意两次封装的结果是 rl 和 r2，那么 rl==r2。   
    
    | 基础数据类型 | 封装类型 | 完全内存化|
    | :------: | :------: | :------: |
    | boolean. byte | Boolean. Byte |是|
    | char. short. int | Char. Short. Integer |否|
    使用`==`和`!=`操作符比较两个完全内存化的封装过的基础数据类型的值是允许的。   
    使用`==`和`!=`操作符比较那些没有完全内存化的封装过的基础结构数据的值，只在这些值在规定的范围内是允许的。   
    在其他情况下，使用`==`和`!=`操作比较那些封装过的基础数据类型的数值是不允许的。   
3) 示例   
    1.这个不符合规则的代码示例：定义的`compare()`方法接收两个封装过的基础数据类型的参数，使用==操作符比较这两个封装过的基础数据类型。这实际上是对封装对象的引用进行比较，而不是对对象的值进行比较。   
    ```java
    static Comparator<Integer> cmp=new Comparator<Integer> () {   
        public int compare(Integer i,Integer j) {   
      return i <j ? -1 : ( i== j ? 0 : 1 );   
        }   
    };
    ```  
    对应的符合规则的代码示例：使用比较操作符`<`、`>`、`<=`或`>=`比较两个封装过的基础数据类型，这些操作符会自动封装基础数据类型数据。   
    ```java
    public int compare(Integer i,Integer j) {   
        return i <j ? -1 : ( i > j ? 0 : 1 );   
    } 
    ```
4) 特例   
在特殊情况下，如果程序只在单一实现中运行，依赖于该特定实现的内存化数值范围，使用相等操作符比较两个基础数据类型的值是允许的。
]]></Description>  
      <Advice><![CDATA[]]></Advice>  
      <References> 
        <Reference type="CWE">754:Improper Check for Unusual or Exceptional Conditions</Reference>  
        <Reference type="CWE">398:Indicator of Poor Code Quality</Reference>  
        <Reference type="CERT">EXP03-J:Do not use the equality operators when comparing values of boxed primitives</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>yes</Default>  
      <WhiteList>not</WhiteList>  
      <Security>not</Security>  
      <BigClassChin>C.3 表达式</BigClassChin>  
       
    </BugInfo>  
    