<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0102000010150309" language="Java"> 
      <Type>std</Type>  
      <Categories> 
        <Category type="ClassChin">CERT</Category>  
        <Category type="SubClassChin">并发规范</Category>  
        <Category type="DetailClassChin">【强制】不要基于非final对象进行同步</Category> 
      </Categories>  
      <BugServerity>1</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[非final对象意味着此对象可修改，基于可能被修改的对象进行同步，可能会导致死锁和不确定的行为。  
  
**例如**：下面代码片段中锁定了一个非final的对象。  
```java
public Object lock = new Object();  
public void doSomething() {  
    synchronized (lock) {  
        // ...  
    }  
}
```  
当程序中创建Object对象，并将此对象赋给`lock`，这将导致这个新的Object对象控制锁对象。
]]></Description>  
      <Advice><![CDATA[程序中应使用private、final的Object对象作为锁对象。  
  
**例如**：以下代码使用了private、final的Object对象作为锁对象。  
```java
private final Object lock = new Object();  
public void doSomething() {  
    synchronized (lock) {  
        // ...  
    }  
}
```
]]></Advice>  
      <References> 
        <Reference type="CWE">413:Improper Resource Locking</Reference>  
        <Reference type="CWE">412:Unrestricted Externally Accessible Lock</Reference>  
        <Reference type="CERT">LCK00-J:Use private final lock objects to synchronize classes that may interact with untrusted code</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>yes</Default>  
      <WhiteList>not</WhiteList>  
      <Security>not</Security>  
      <EnClassChin/>  
        
        
        
        
        
      <Tag>concurrency,bad_practice</Tag> 
    </BugInfo>  
    