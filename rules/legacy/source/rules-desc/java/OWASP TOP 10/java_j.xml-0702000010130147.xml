<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0702000010130147" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">OWASP TOP 10</Category>
				<Category type="SubClassChin">代码质量</Category>
				<Category type="DetailClassChin">未经身份验证的服务：Redis</Category>
			</Categories>
			<BugServerity>2</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行的Redis服务未进行身份验证。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行的Redis服务未进行身份验证。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[当程序初始化Redis客户端时没有设置任何凭据，这个不需要凭据的 Redis服务器可能会被攻击者作为目标，破坏其中存储的数据，并通过攻击Redis服务器来侵入内部网络。  
  
**例如**：下列代码使用没有设置凭据的Redis客户端。  
```java
...  
JedisConnectionFactory jedisConnectionFactory = new JedisConnectionFactory();  
...
```
]]></Description>
			<Advice><![CDATA[Redis服务器应对身份进行验证才能为其提供服务，防止攻击者入侵。  
  
**例如**：以下代码使用有设置凭据的Redis池，再获取Jedis实例进行操作。  
```java
...  
JedisPoolConfig config = new JedisPoolConfig();  
config.setMaxTotal(MAX_ACTIVE);  
config.setMaxIdle(MAX_IDLE);  
config.setMaxWaitMillis(MAX_WAIT);  
config.setTestOnBorrow(TEST_ON_BORROW);  
JedisPool jedisPool = new JedisPool(config, ADDR, PORT, TIMEOUT, AUTH);  
Jedis jedis = jedisPool.getResource();  
...
```
]]></Advice>
			<References>
				<Reference type="CWE">259:Use of Hard-coded Password</Reference>
				<Reference type="OWASP TOP 10">2017:A3 Sensitive Data Exposure</Reference>
				<Reference type="OWASP TOP 10">2013:A6 Sensitive Data Exposure</Reference>
				<Reference type="OWASP TOP 10">2010:A7 Insecure Cryptographic Storage</Reference>
				<Reference type="OWASP TOP 10">2007:A8 Insecure Cryptographic Storage</Reference>
				<Reference type="OWASP TOP 10">2004:A8 Insecure Storage</Reference>
				<Reference type="OWASP MOBILE TOP 10">2014:M2 Insecure Data Storage</Reference>
				<Reference type="CWE/SANS TOP 25">2009:Porous Defenses - CWE ID 259</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>yes</Default>
			<WhiteList>not</WhiteList>
			<Security>yes</Security>



			<Tag>security</Tag>
			<DomainType>security</DomainType>
		</BugInfo>
		