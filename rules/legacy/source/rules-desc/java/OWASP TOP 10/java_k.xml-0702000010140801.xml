<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0702000010140801" language="Java"> 
      <Type>cert</Type>  
      <Categories> 
        <Category type="ClassChin">OWASP TOP 10</Category>  
        <Category type="SubClassChin">Rule 07. 异常行为 (ERR)</Category>  
        <Category type="DetailClassChin">ERR01-J：不能允许异常泄露敏感信息</Category> 
      </Categories>  
      <BugServerity>1</BugServerity>  
      <DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行违反+'`'+${categoryName}+'`'+规范。" th:remove="tag"></p>]]></DynamicDescription>
      <Description><![CDATA[在异常传递的过程中，如果不对敏感信息进行过滤，会导致信息泄露，这将有助于攻击者实现对系统的攻击。  
**不规范的代码样例1（异常信息和类型引起的泄露）**：   
```java
class ExceptionExample {
    public static void main(String[] args) throws FileNotFoundException {
        // Linux stores a user's home directory path in
        // the environment variable $HOME, Windows in %APPDATA%
        FileInputStream fis = new FileInputStream(System.getenv("APPDATA") + args[0]);
    }
}
```  
在这个不合规范的代码示例中：如果要求的文件不存在，将会抛出FileNotFoundException异常，通过不断的传递虚构的路径给程序，攻击者可以重建内部文件系统结构。  
**不规范的代码样例2（封装和重新抛出敏感信息）**：   
```java
try {
    FileInputStream fis = new FileInputStream(System.getenv("APPDATA") + args[0]);
} catch (FileNotFoundException e) {
    // Log the exception
    throw new IOException("Unable to retrieve file", e);
}
```  
在这个不合规范的代码示例将异常记入日志，并封装了一个更普通的异常抛出，但这仍然有助于攻击者发现文件系统布局的信息。  
**不规范的代码样例3（异常过滤）**：  
```java
class SecurityIOException extends IOException {/* ... */};

try {
    FileInputStream fis = new FileInputStream(System.getenv("APPDATA") + args[0]);
} catch (FileNotFoundException e) {
    // Log the exception
    throw new SecurityIOException();
}
```  
在这个不合规范的代码示例将异常记入日志，抛出一个自定义的异常，并没有将FileNotFoundException异常封装在其中。虽然这个异常不能直接泄露有用的异常，但是它还是对无法读取的文件进行了提示。具体来说，这个程序对不存在的路径和存在的路径所作出的反应是不一样的，这使得攻击者仍然能从程序行为推导出文件系统的敏感信息。没能限制用户的输入让系统容易遭受穷举攻击，攻击者通过不停查询可能的文件名，可以发现有效的文件名。造成程序返回异常的文件名表明了该文件不存在，而未返回异常则暴漏了已有的文件。
]]></Description>  
      <Advice><![CDATA[**规范的代码样例1（安全策略）**：  
```java
class ExceptionExample {
    public static void main(String[] args) {

        File file = null;
        try {
            file = new File(System.getenv("APPDATA") + args[0]).getCanonicalFile();
            if (!file.getPath().startsWith("c:\\homepath")) {
                System.out.println("Invalid file");
                return;
            }
        } catch (IOException x) {
            System.out.println("Invalid file");
            return;
        }

        try {
            FileInputStream fis = new FileInputStream(file);
        } catch (FileNotFoundException x) {
            System.out.println("Invalid file");
            return;
        }
    }
}
```  
这个符合规范的方案实现了一个安全策略，用户只能打开`c:\homepath`中的文件，并且不允许用户发现这个目录之外的文件。当文件不能打开或不在正确的目录内时，这个方案返回一个简洁的错误信息。`c:\homepath`目录外文件的任何信息都被屏蔽了。  
这个方案还使用了`File.getCanonicalFile()`方法对文件进行标准化，简化了之后的对路径名进行比较的过程（参见规则IDS02-J）。  
**规范的代码样例2（输入限制）**：  
```java
class ExceptionExample {
    public static void main(String[] args) {
        FileInputStream fis = null;
        try {
            switch (Integer.valueOf(args[0])) {
            case 1:
                fis = new FileInputStream("c:\\homepath\\file1");
                break;
            case 2:
                fis = new FileInputStream("c:\\homepath\\file2");
                break;
            // ...
            default:
                System.out.println("Invalid option");
                break;
            }
        } catch (Throwable t) {
            MyExceptionReporter.report(t); // Sanitize
        }
    }
}
```  
这个符合规范的方案操作策略是用户只能打开 `c:\homepath\file1` 和 `c:\homepath\file2`。
]]></Advice>  
      <References> 
        <Reference type="CWE">600:Uncaught Exception in Servlet</Reference>  
        <Reference type="CWE">431:Missing Handler</Reference>  
        <Reference type="CWE">248:Uncaught Exception</Reference>  
        <Reference type="CWE">209:Information Exposure Through an Error Message</Reference>  
        <Reference type="OWASP TOP 10">2007:A6 Information Leakage and Improper Error Handling</Reference>  
        <Reference type="CWE/SANS TOP 25">2010:Insecure Interaction - CWE ID 209</Reference>  
        <Reference type="CWE/SANS TOP 25">2009:Insecure Interaction - CWE ID 209</Reference>  
        <Reference type="GB/T 34944-2017 Java">数据处理:信息通过错误消息泄露</Reference> 
      </References>  
      <Obsolete>not</Obsolete>  
      <Default>yes</Default>  
      <WhiteList>not</WhiteList>  
      <Security>yes</Security>  
       
    </BugInfo>  
    