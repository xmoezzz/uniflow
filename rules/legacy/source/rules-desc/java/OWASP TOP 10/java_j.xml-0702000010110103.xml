<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0702000010110103" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">OWASP TOP 10</Category>
				<Category type="SubClassChin">代码质量</Category>
				<Category type="DetailClassChin">外部存储Android数据</Category>
			</Categories>
			<BugServerity>1</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行将Android数据存储到外部文件中。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行将Android数据存储到外部文件中。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[应用程序数据存储到外部（如SD卡）文件中，将使其能够被其它app读取、修改。如果存储的数据中包含敏感信息，将造成敏感信息泄露。攻击者还可通过修改程序所依赖的外部文件将恶意数据注入程序。  
  
**例如**：下面代码片段中，将敏感信息存储到外部文件中。  
```java
private String filename = "myfile"  
private String string = "sensitive data such as credit card number"  
FileOutputStream fos = null;  
try {  
    file file = new File(getExternalFilesDir(TARGET_TYPE), filename);  
    fos = new FileOutputStream(file, false);  
    fos.write(string.getBytes());  
} catch (FileNotFoundException e) {  
    // handle FileNotFoundException  
} catch (IOException e) {  
    // handle IOException  
} finally {  
    if (fos != null) {  
        try {  
            fos.close();  
        } catch (IOException e) {  
            ...  
        }  
    }  
}
```
]]></Description>
			<Advice><![CDATA[将数据存储到应用程序的特定位置（如应用程序的内部目录下），并设置相应的文件操作模式，或者存储到应用程序的SQLite数据库中，避免其它恶意app进行访问。  
  
**例如**：下面代码片段中调用`openFileOutput()`方法，并设置文件操作模式为`MODE_PRIVATE`，防止其他app进行访问。  
```java
private String filename = "myfile"  
private String string = "sensitive data such as credit card number"  
FileOutputStream fos = null;  
try {  
    fos = openFileOutput(filename, Context.MODE_PRIVATE);  
    fos.write(string.getBytes());  
    fos.close();  
} catch (FileNotFoundException e) {  
    // handle FileNotFoundException  
} catch (IOException e) {  
    // handle IOException  
} finaly {  
    if (fos != null) {  
        try {  
            fos.close();  
        } catch (IOException e) {  
            ...  
        }  
    }  
}
```
]]></Advice>
			<References>
				<Reference type="CERT">DRD00-J:Do not store sensitive information on external storage (SD card) unless encrypted first</Reference>
				<Reference type="OWASP TOP 10">2017:A3 Sensitive Data Exposure</Reference>
				<Reference type="OWASP TOP 10">2013:A6 Sensitive Data Exposure</Reference>
				<Reference type="OWASP MOBILE TOP 10">2014:M2 Insecure Data Storage</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>yes</Default>
			<WhiteList>not</WhiteList>
			<Security>yes</Security>



			<Tag>security</Tag>
			<DomainType>security</DomainType>
		</BugInfo>
		