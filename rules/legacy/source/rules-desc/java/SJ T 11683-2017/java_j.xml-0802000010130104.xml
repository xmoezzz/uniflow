<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0802000010130104" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">SJ/T 11683-2017</Category>
				<Category type="SubClassChin">资源管理</Category>
				<Category type="DetailClassChin">资源未释放：WakeLock</Category>
			</Categories>
			<BugServerity>2</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行WakeLock没有释放。" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行WakeLock未释放。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[使用wakelock通常是为了保证在某段代码中，CPU不会休眠。运行完这段代码之后，wakelock得不到释放，意味着CPU将持续运行，手机耗电量将会增大，待机时间变短。  
  
**例如**：以下代码片段中，程序获取了WakeLock锁而没有释放。  
```java
PowerManager powerManager = (PowerManager) getSystemService(POWER_SERVICE);  
WakeLock wakeLock = powerManager.newWakeLock(PowerManager.PARTIAL_WAKE_LOCK,"MyWakelockTag");  
wakeLock.acquire();  
...
```
]]></Description>
			<Advice><![CDATA[wakelock可以通过`acquire(long timeout)`获取，意味着系统在timeout时间后会释放锁。而更好的办法是在使用完wakelock后通过`release()`方法，这样可以及时释放掉wakelock。  
  
**例如**：以下代码片段中，程序获取了WakeLock锁，在代码运行完之后使用`release()`方法将其释放。  
```java
PowerManager powerManager = (PowerManager) getSystemService(POWER_SERVICE);  
WakeLock wakeLock = null;  
try{  
    wakeLock = powerManager.newWakeLock(PowerManager.PARTIAL_WAKE_LOCK,"MyWakelockTag");  
    wakeLock.acquire();  
    ...  
}catch(Exception e){  
    ...  
}finally{  
    if(wakeLock != null){  
        wakeLock.release();  
    }  
    ...  
}
```
]]></Advice>
			<References>
				<Reference type="SJ/T 11683-2017">6.6.1 资源泄漏</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>yes</Default>
			<WhiteList>not</WhiteList>
			<Security>not</Security>



			<Tag>bad_practice</Tag>
			<DomainType>bug</DomainType>
			<FormalizationData version="0.1">
				<CodeFragments xmlns="">
					<CodeFragment id="1" name="1.持有WakeLock资源"/>
					<CodeFragment id="2" name="2.持有WakeLock资源的关联资源"/>
					<CodeFragment id="3" name="3.WakeLock资源被资源池持有"/>
					<CodeFragment id="4" name="4.WakeLock资源被返回函数外"/>
					<CodeFragment id="5" name="5.WakeLock资源已关闭"/>
					<CodeFragment id="6" name="6.WakeLock资源的关联资源已关闭"/>
					<CodeFragment id="7" name="7.WakeLock资源未关闭"/>
					<CodeFragment id="8" name="8.WakeLock资源的关联资源未关闭"/>
					<CodeFragment id="9" name="9.触发WakeLock资源未释放"/>
					<CodeFragment id="10" name="10.程序执行正常"/>
				</CodeFragments>
				<Relationships xmlns="">
					<Relationship fromId="1" toId="2" name="申请关联资源"/>
					<Relationship fromId="1" toId="3" name="资源被放入资源池中"/>
					<Relationship fromId="1" toId="4" name="资源通过函数返回值返回"/>
					<Relationship fromId="1" toId="5" name="关闭资源"/>
					<Relationship fromId="1" toId="7" name="作用域结束"/>
					<Relationship fromId="2" toId="6" name="关闭关联资源"/>
					<Relationship fromId="2" toId="8" name="作用域结束"/>
					<Relationship fromId="3" toId="10" name="作用域结束"/>
					<Relationship fromId="4" toId="10" name="作用域结束"/>
					<Relationship fromId="5" toId="10" name="作用域结束"/>
					<Relationship fromId="6" toId="10" name="作用域结束"/>
					<Relationship fromId="7" toId="9" name="执行"/>
					<Relationship fromId="8" toId="9" name="执行"/>
				</Relationships>
			</FormalizationData>
		</BugInfo>
		