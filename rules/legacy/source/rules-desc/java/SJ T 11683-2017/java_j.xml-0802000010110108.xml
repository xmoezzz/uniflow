<?xml version='1.0' encoding='utf-8'?>
<BugInfo xmlns="http://www.codesafe.cn/schema/rules" id="0802000010110108" language="Java">
			<Type>bug</Type>
			<Categories>
				<Category type="ClassChin">SJ/T 11683-2017</Category>
				<Category type="SubClassChin">资源管理</Category>
				<Category type="DetailClassChin">释放后使用：Media</Category>
			</Categories>
			<BugServerity>2</BugServerity>
			<DynamicDescription><![CDATA[<p th:if="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的+'`'+${funcName}+'`'+方法中的第+'`'+${line}+'`'+行Media在释放后使用" th:remove="tag"></p>
<p th:unless="${funcNameisEmpty}" th:text="在+'`'+${fileName}+'`'+的第+'`'+${line}+'`'+行Media在释放后使用。" th:remove="tag"></p>]]></DynamicDescription>
			<Description><![CDATA[程序释放了Media对象后，在未重新获取Media对象的情况下，试图再使用该对象，将会抛出异常。如果未捕获该异常，可能会导致程序崩溃。  
  
**例如**：下面代码片段的功能是切换Media播放，第一次触发PauseButtonListener监听器，Media会停止，并释放MediaPlayer资源，当再一次触发PauseButtonListener监听器时，程序将调用之前释放的MediaPlayer对象，导致出现程序异常。  
```java
public class ReuseMediaPlayerActivity extends Activity {  
    private MediaPlayer play;  
    ...  
    private class PauseButtonListener implements OnClickListener {  
        public void onClick(View view) {  
            if (toggle) {  
                play.pause();  
                play.release();  
            }else {  
                play.start();  
            }  
            toggle= !toggle;  
        }  
    }  
    ...  
}
```
]]></Description>
			<Advice><![CDATA[检查程序逻辑，确保程序未调用释放后的Media对象。
]]></Advice>
			<References>
				<Reference type="CWE/SANS TOP 25">2019:CWE ID 416</Reference>
				<Reference type="CWE">416:Use After Free</Reference>
				<Reference type="OWASP TOP 10">2004:A9 Application Denial of Service</Reference>
				<Reference type="SJ/T 11683-2017">6.6.2 使用已释放的资源</Reference>
			</References>
			<Obsolete>not</Obsolete>
			<Default>yes</Default>
			<WhiteList>not</WhiteList>
			<Security>not</Security>



			<Tag>correctness,exception</Tag>
			<DomainType>bug</DomainType>
		</BugInfo>
		