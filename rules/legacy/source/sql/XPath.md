# XPath表达式定义自定义规则漏洞

此规则允许使用XPath表达式定义自定义规则。

## 问题创建基于XPath表达式的返回值

根据XPath表达式的返回值，问题会被创建：

* 如果XPath表达式返回一个单一或列表中的AST节点，则为每个节点创建一条带有给定消息的行问题
* 如果XPath表达式返回布尔值，那么仅在布尔值为真时，才会创建具有给定消息的文件问题
* 其他任何情况，不创建任何问题