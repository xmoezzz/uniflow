BugInfos:
  BugInfo:
  - Advice: "**规范的使用样例1**：  \n```java\npublic final class Container implements Runnable\
      \ {\n  private final Vector<Integer> vector = new Vector<Integer>(1000);\n\n\
      \  public Vector<Integer> getVector() {\n    return vector;\n  }\n\n  @Override\n\
      \  public synchronized void run() {\n    Random number = new Random(123L);\n\
      \    int i = vector.capacity();\n    while (!Thread.interrupted() && i > 0)\
      \ {\n      vector.add(number.nextInt(100));\n      i--;\n    }\n  }\n\n  public\
      \ static void main(String[] args) throws InterruptedException {\n    Container\
      \ c = new Container();\n    Thread thread = new Thread(c);\n    thread.start();\n\
      \    Thread.sleep(5000);\n    thread.interrupt();\n  }\n}\n```\n调用`Thread.interrupt()`方法设置了一个内部的中断状态标志。线程可以通过`Thread.interrupt()`方法来查询这个标志。这个方法会在当前线程被中断时返回true，并会清除中断状态标志。\n\
      线程可以使用中断来执行除取消和关闭之外的任务。 因此，只有在事先知道其中断策略时，才应中断线程。 不这样做可能会导致中断请求失败。"
    Advice_En: "**Compliant Solution (Interruptible)**\n\nIn this compliant solution,\
      \ the `Thread.interrupt()` method is called from `main()` to terminate the thread.\
      \ Invoking `Thread.interrupt()` sets an internal interrupt status flag. The\
      \ thread polls that flag using the `Thread.interrupted()` method, which both\
      \ returns true if the current thread has been interrupted and clears the interrupt\
      \ status flag.\n\n```java\npublic final class Container implements Runnable\
      \ {\n  private final Vector<Integer> vector = new Vector<Integer>(1000);\n\n\
      \  public Vector<Integer> getVector() {\n    return vector;\n  }\n\n  @Override\n\
      \  public synchronized void run() {\n    Random number = new Random(123L);\n\
      \    int i = vector.capacity();\n    while (!Thread.interrupted() && i > 0)\
      \ {\n      vector.add(number.nextInt(100));\n      i--;\n    }\n  }\n\n  public\
      \ static void main(String[] args) throws InterruptedException {\n    Container\
      \ c = new Container();\n    Thread thread = new Thread(c);\n    thread.start();\n\
      \    Thread.sleep(5000);\n    thread.interrupt();\n  }\n}\n```\n\nA thread may\
      \ use interruption for performing tasks other than cancellation and shutdown.\
      \ Consequently, a thread should be interrupted only when its interruption policy\
      \ is known in advance. Failure to do so can result in failed interruption requests."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 10. 线程API (THI)
      - type: DetailClassChin
        value: THI05-J：确保可以终止受阻线程
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 10. Thread APIs (THI)
      - type: DetailClassChin
        value: THI05-J:Do not use Thread.stop() to terminate threads
    Default: true
    Description: "当线程被允许正常退出时，它们会保留类不变量。当程序员认为任务已完成、请求已被取消或程序或 Java 虚拟机 (JVM) 必须迅速关闭时，他们通常会尝试突然终止线程。\n\
      引入了某些线程 API 以促进线程暂停、恢复和终止，但后来由于固有的设计缺陷而被弃用。例如，Thread.stop() 方法会导致线程立即抛出 ThreadDeath\
      \ 异常，这通常会停止线程。 MET02-J 中提供了有关已弃用方法的更多信息。不要使用已弃用或过时的类或方法。\n调用 Thread.stop() 会导致释放线程已获取的所有锁，当这些对象处于不一致状态时，可能会暴露受这些锁保护的对象。线程可能会捕获\
      \ ThreadDeath 异常并使用 finally 块来尝试修复不一致的对象。但是，这样做需要仔细检查所有同步方法和块，因为线程执行期间的任何时候都可能引发\
      \ ThreadDeath 异常。此外，必须保护代码免受在执行 catch 或 finally 块时可能发生的 ThreadDeath 异常 [Sun\
      \ 1999]。因此，程序不能调用 Thread.stop()。\n从安全策略文件中删除 java.lang.RuntimePermission stopThread\
      \ 权限可防止使用 Thread.stop() 方法停止线程。尽管这种方法保证程序不能使用 Thread.stop() 方法，但还是强烈建议不要这样做。使用\
      \ Thread.stop() 方法的现有可信、定制开发的代码可能取决于系统执行此操作的能力。此外，系统可能无法正确处理产生的安全异常。此外，第三方库也可能依赖于\
      \ Thread.stop() 方法的使用。\n请参阅 ERR09-J.不要让不受信任的代码终止 JVM，以获取有关在 JVM 突然关闭时防止数据损坏的信息。\n\
      **不规范的代码样例1**：  \n```java\npublic final class Container implements Runnable\
      \ {\n  private final Vector<Integer> vector = new Vector<Integer>(1000);\n\n\
      \  public Vector<Integer> getVector() {\n    return vector;\n  }\n\n  @Override\n\
      \  public synchronized void run() {\n    Random number = new Random(123L);\n\
      \    int i = vector.capacity();\n    while (i > 0) {\n      vector.add(number.nextInt(100));\n\
      \      i--;\n    }\n  }\n\n  public static void main(String[] args) throws InterruptedException\
      \ {\n    Thread thread = new Thread(new Container());\n    thread.start();\n\
      \    Thread.sleep(5000);\n    thread.stop();\n  }\n}\n```\nVector类是线程安全的，所以多个线程对共享实例进行的操作时不会让其处于一个不一致的状态的。vector实例是使用自身的隐含锁来防止在实例状态暂时不一致时其他线程对它的访问。然而，`Thread.stop()`方法会造成线程停止正在进行的操作并抛出一个ThreadDeath异常。所有获得的锁则会被释放。如果线程是在加入一个新的整数至vector时被停止，就有可能导致访问处于不一致状态的vector。"
    Description_En: "Threads preserve class invariants when they are allowed to exit\
      \ normally. Programmers often attempt to terminate threads abruptly when they\
      \ believe the task is complete, the request has been canceled, or the program\
      \ or Java Virtual Machine (JVM) must shut down expeditiously.\n\nCertain thread\
      \ APIs were introduced to facilitate thread suspension, resumption, and termination\
      \ but were later deprecated because of inherent design weaknesses. For example,\
      \ the `Thread.stop()` method causes the thread to immediately throw a `ThreadDeath`\
      \ exception, which usually stops the thread. More information about deprecated\
      \ methods is available in MET02-J. Do not use deprecated or obsolete classes\
      \ or methods.\n\nInvoking `Thread.stop()` results in the release of all locks\
      \ a thread has acquired, potentially exposing the objects protected by those\
      \ locks when those objects are in an inconsistent state. The thread might catch\
      \ the `ThreadDeath` exception and use a `finally` block in an attempt to repair\
      \ the inconsistent object or objects. However, doing so requires careful inspection\
      \ of all synchronized methods and blocks because a `ThreadDeath` exception can\
      \ be thrown at any point during the thread's execution. Furthermore, code must\
      \ be protected from `ThreadDeath` exceptions that might occur while executing\
      \ `catch` or `finally` blocks [Sun 1999]. Consequently, programs must not invoke\
      \ `Thread.stop()`.\n\nRemoving the `java.lang.RuntimePermission stopThread`\
      \ permission from the security policy file prevents threads from being stopped\
      \ using the `Thread.stop()` method. Although this approach guarantees that the\
      \ program cannot use the `Thread.stop()` method, it is nevertheless strongly\
      \ discouraged. Existing trusted, custom-developed code that uses the `Thread.stop()`\
      \ method presumably depends on the ability of the system to perform this action.\
      \ Furthermore, the system might fail to correctly handle the resulting security\
      \ exception. Additionally, third-party libraries may also depend on use of the\
      \ `Thread.stop()` method.\n\nRefer to ERR09-J. Do not allow untrusted code to\
      \ terminate the JVM for information on preventing data corruption when the JVM\
      \ is abruptly shut down.\n\n**Noncompliant Code Example (Deprecated `Thread.stop()`)**\n\
      \nThis noncompliant code example shows a thread that fills a vector with pseudorandom\
      \ numbers. The thread is forcefully stopped after a given amount of time.\n\n\
      ```java\npublic final class Container implements Runnable {\n  private final\
      \ Vector<Integer> vector = new Vector<Integer>(1000);\n\n  public Vector<Integer>\
      \ getVector() {\n    return vector;\n  }\n\n  @Override\n  public synchronized\
      \ void run() {\n    Random number = new Random(123L);\n    int i = vector.capacity();\n\
      \    while (i > 0) {\n      vector.add(number.nextInt(100));\n      i--;\n \
      \   }\n  }\n\n  public static void main(String[] args) throws InterruptedException\
      \ {\n    Thread thread = new Thread(new Container());\n    thread.start();\n\
      \    Thread.sleep(5000);\n    thread.stop();\n  }\n}\n```\n\nBecause the `Vector`\
      \ class is [thread-safe], operations performed by multiple threads on its shared\
      \ instance are expected to leave it in a consistent state. For instance, the\
      \ `Vector.size()` method always returns the correct number of elements in the\
      \ vector, even after concurrent changes to the vector, because the vector instance\
      \ uses its own intrinsic lock to prevent other threads from accessing it while\
      \ its state is temporarily inconsistent.\n\nHowever, the `Thread.stop()` method\
      \ causes the thread to stop what it is doing and throw a `ThreadDeath` exception.\
      \ All acquired locks are subsequently released [API 2014]. If the thread were\
      \ in the process of adding a new integer to the vector when it was stopped,\
      \ the vector would become accessible while it is in an inconsistent state. For\
      \ example, this could result in `Vector.size()` returning an incorrect element\
      \ count because the element count is incremented after adding the element."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141105'
    language: Java
  - Advice: '`Thread.stop`的许多使用都应由只修改某些变量以指示目标线程应该停止运行的代码来取代。目标线程应定期检查该变量，并且如果该变量指示它要停止运行，则从其运行方法依次返回。如果目标线程等待很长时间（例如基于一个条件变量），则应使用`interrupt`方法来中断该等待。'
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 危险函数
      - type: DetailClassChin
        value: 【强制】不要使用Thread.stop()终止线程
    Categories_En:
      Category: []
    Default: true
    Description: 用`Thread.stop`来终止线程将释放它已经锁定的所有监视器（作为沿堆栈向上传播的未检查ThreadDeath异常的一个自然后果）。如果以前受这些监视器保护的任何对象都处于一种不一致的状态，则损坏的对象将对其他线程可见，这有可能导致任意的行为。
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - (THI05-J)不要使用Thread.stop()来终止线程
    ENSubClassChin: 线程API(THI)
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: THI05-J:Do not use Thread.stop() to terminate threads
    Security: not
    Tag: concurrency,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150603'
    language: Java
  - Advice: "对应符合规则的解决方案是使用一个 volatile 标志来请求线程终止。`shutdown()`访问方法是用来设置这个标志成 true 的。线程的\
      \ `run()`方法查询 done 标记，并在标记被设置后终止线程。   \n    ```java\n    public final class\
      \ Container implements Runnable {\n        private final Vector<Integer> vector\
      \ = new Vector<Integer>(1000);\n        private volatile boolean done = false;\n\
      \    \n        public Vector<Integer> getVector() {\n            return vector;\n\
      \        }\n    \n        public void shutdown() {\n            done = true;\n\
      \        }\n    \n        @Override\n        public synchronized void run()\
      \ {\n            Random number = new Random(123L);\n            int i = vector.capacity();\n\
      \            while (!done && i > 0) {\n                vector.add(number.nextInt(100));\n\
      \                i--;\n            }\n        }\n    \n        public static\
      \ void main(String[] args) throws InterruptedException {\n            Container\
      \ container = new Container();\n            Thread thread = new Thread(container);\n\
      \            thread.start();\n            Thread.sleep(5000);\n            container.shutdown();\n\
      \        }\n    }\n    ```   \n       \n    另一个符合规则的解决方案（可中断的）中，`main()`调用 `Thread.interrupt()`方法来终止线程。调用`Thread.interrupt()`设置了一个内部的中断状态标志。线程可以通过\
      \ `Thread.interrupted()`方法来查询这个标记。该方法会在当前线程被中断时返回 true,并会清除中断状态标志。   \n    ```java\n\
      \    public final class Container implements Runnable {\n        private final\
      \ Vector<Integer> vector = new Vector<Integer>(1000);\n    \n        public\
      \ Vector<Integer> getVector() {\n            return vector;\n        }\n   \
      \ \n        @Override\n        public synchronized void run() {\n          \
      \  Random number = new Random(123L);\n            int i = vector.capacity();\n\
      \            while (!Thread.interrupted() && i > 0) {\n                vector.add(number.nextInt(100));\n\
      \                i--;\n            }\n        }\n    \n        public static\
      \ void main(String[] args) throws InterruptedException {\n            Container\
      \ c = new Container();\n            Thread thread = new Thread(c);\n       \
      \     thread.start();\n            Thread.sleep(5000);\n            thread.interrupt();\n\
      \        }\n    }\n    ```  \n    一个线程可以使用中断来完成取消和关闭之外的操作。因此，一个线程应该只能在它的中断策略已知的情况下被中断。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.10.6 不要使用Thread.stop()来终止线程
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序不能调用`Thread.stop()`。   \n2) 原理   \n线程在可以正常退出时会维持类的不变性。但是，程序员在认为任务结束、要求被取消或程序必须快速关闭\
      \ Java 虚拟机时，经常会非正常地终止线程。   某些线程 API 最初是用来帮助线程的暂停、恢复和终止的，但是因设计上先天缺陷而废弃了。例如，`Thread.stop()`方法会导致线程立即抛出一个\
      \ ThreadDeath 异常，这通常会停止线程。调用 `Thread.stop()`会造成一个线程释放它获得的所有的锁，这有可能会暴露这些锁保护的对象，而这些对象正处于一个不一致的状态中。线程可以捕捉\
      \ ThreadDeath 异常，并使用 finally 程序段来尝试修复处于不一致状态的对象。可是，因为 ThreadDeath 可以在线程执行的任何时候被抛出，所以需要小心地检查所有同步的方法和程序段。而且，必须确保代码不受在\
      \ catch 或 finally 程序段执行时抛出的ThreadDeath 异常的影响。因此，程序不能调用 `Thread.stop()`。   \n\
      需注意的是，将 stopThread 权限从 `java.lang.RuntimePermission`中去除可以避免使用 `Thread.stop()`方法来停止线程。自行开发的可信任的使用\
      \ `Thread.stop()`的代码可能会依赖于系统的能力来执行这样的操作。此外，第三方开发的库也有可能依赖于 `Thread.stop()`方法的使用。\
      \   \n3) 示例   \n    不符合规则的代码示例（废弃的 `Thread.stop()`)，一个线程将伪随机数值写入一个vector中。在经过指定一段时间后，这个线程将被强迫停止。\
      \  \n    ```java\n    public final class Container implements Runnable {\n \
      \       private final Vector<Integer> vector = new Vector<Integer>(1000);\n\
      \    \n        public Vector<Integer> getVector() {\n            return vector;\n\
      \        }\n    \n        @Override\n        public synchronized void run()\
      \ {\n            Random number = new Random(123L);\n            int i = vector.capacity();\n\
      \            while (i > 0) {\n                vector.add(number.nextInt(100));\n\
      \                i--;\n            }\n        }\n    \n        public static\
      \ void main(String[] args) throws InterruptedException {\n            Thread\
      \ thread = new Thread(new Container());\n            thread.start();\n     \
      \       Thread.sleep(5000);\n            thread.stop();\n        }\n    }\n\
      \    ```  \n    因为 Vector 类是线程安全的，所以多个线程对共享实例进行的操作是不会让其处于一个不一致的状态的。比如，`Vector.size()`方法总是可以返回\
      \ vector 中元素的正确数目，即使在对 vector 进行并发操作之后。这是因为 vector 实例是使用自身的隐含锁来防止在实例状态暂时不一致时其他线程对实例的访问。\
      \   \n    然而，`Thread.stop()`方法会造成线程停止正在进行的操作并抛出一个 ThreadDeath 异常。所有获得的锁则会被释放。如果线程是在加入一个新整数至\
      \ vector 时被停止，就有可能 访问处于不一致状态的 vector。例如，因为元素数目是在添加一个元素后增加的，所以这可能导致 `Vector.size()`返回错误的元素数目。\
      \   \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: THI05-J:Do not use Thread.stop() to terminate threads
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160096
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\npublic final class Container\
      \ implements Runnable {\n    private final Vector<Integer> vector = new Vector<Integer>(1000);\n\
      \n    public Vector<Integer> getVector() {\n        return vector;\n    }\n\n\
      \    @Override\n    public synchronized void run() {\n        Random number\
      \ = new Random(123L);\n        int i = vector.capacity();\n        while (i\
      \ > 0) {\n            vector.add(number.nextInt(100));\n            i--;\n \
      \       }\n    }\n\n    public static void main(String[] args) throws InterruptedException\
      \ {\n        Thread thread = new Thread(new Container());\n        thread.start();\n\
      \        Thread.sleep(5000);\n        thread.interrupt();\n    }\n}\n```\n调用`Thread.interrupt()`方法设置了一个内部的中断状态标志。线程可以通过`Thread.interrupt()`方法来查询这个标志。这个方法会在当前线程被中断时返回true，并会清除中断状态标志。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.12 确保可以终止受阻线程
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明： `Thread.stop()`方法会导致线程抛出一个ThreadDeath异常，通常会停止线程。调用`Thread.stop()`会使得一个线程释放它获得的所有锁，这有可能暴露这些锁保护的对象，而这些对象正处于一个不一致的状态中。线程可以捕获ThreadDeath异常，并使用finally来尝试修复处于不一致的状态对象。\
      \  \n对于确保可以终止受阻线程的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\npublic final\
      \ class Container implements Runnable {\n    private final Vector<Integer> vector\
      \ = new Vector<Integer>(1000);\n\n    public Vector<Integer> getVector() {\n\
      \        return vector;\n    }\n\n    @Override\n    public synchronized void\
      \ run() {\n        Random number = new Random(123L);\n        int i = vector.capacity();\n\
      \        while (i > 0) {\n            vector.add(number.nextInt(100));\n   \
      \         i--;\n        }\n    }\n\n    public static void main(String[] args)\
      \ throws InterruptedException {\n        Thread thread = new Thread(new Container());\n\
      \        thread.start();\n        Thread.sleep(5000);\n        thread.stop();\n\
      \    }\n}\n```\nVector类是线程安全的，所以多个线程对共享实例进行的操作时不会让其处于一个不一致的状态的。vector实例是使用自身的隐含锁来防止在实例状态暂时不一致时其他线程对它的访问。然而，`Thread.stop()`方法会造成线程停止正在进行的操作并抛出一个ThreadDeath异常。所有获得的锁则会被释放。如果线程是在加入一个新的整数至vector时被停止，就有可能导致访问处于不一致状态的vector。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: THI05-J:Do not use Thread.stop() to terminate threads
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170044'
    language: Java
  - Advice: "**规范的代码样例1**：  \n```java\npublic final class CaseInsensitiveString {\n\
      \  private String s;\n\n  public CaseInsensitiveString(String s) {\n    if (s\
      \ == null) {\n      throw new NullPointerException();\n    }\n    this.s = s;\n\
      \  }\n\n  public boolean equals(Object o) {\n    return o instanceof CaseInsensitiveString\
      \ &&\n        ((CaseInsensitiveString)o).s.equalsIgnoreCase(s);\n  }\n\n  public\
      \ int hashCode() {/* ... */}\n\n  public static void main(String[] args) {\n\
      \    CaseInsensitiveString cis = new CaseInsensitiveString(\"Java\");\n    String\
      \ s = \"java\";\n    System.out.println(cis.equals(s)); // Returns false now\n\
      \    System.out.println(s.equals(cis)); // Returns false now\n  }\n}\n```\n\
      在这个符合规范的方案中，方法`CaseInsensitiveString.equals` 简化成只对`CaseInsensitiveString`类的实例进行操作，从而保持了对称性。\
      \  \n**规范的代码样例2**：\n```java\nclass XCard {\n  private String type;\n  private\
      \ Card card; // Composition\n\n  public XCard(int number, String type) {\n \
      \   card = new Card(number);\n    this.type = type;\n  }\n\n  public Card viewCard()\
      \ {\n    return card;\n  }\n\n  public boolean equals(Object o) {\n    if (!(o\
      \ instanceof XCard)) {\n      return false;\n    }\n\n    XCard cp = (XCard)o;\n\
      \    return cp.card.equals(card) && cp.type.equals(type);\n  }\n\n  public int\
      \ hashCode() {/* ... */}\n\n  public static void main(String[] args) {\n   \
      \ XCard p1 = new XCard(1, \"type1\");\n    Card p2 = new Card(1);\n    XCard\
      \ p3 = new XCard(1, \"type2\");\n    XCard p4 = new XCard(1, \"type1\");\n \
      \   System.out.println(p1.equals(p2)); // Prints false\n    System.out.println(p2.equals(p3));\
      \ // Prints false\n    System.out.println(p1.equals(p3)); // Prints false\n\
      \    System.out.println(p1.equals(p4)); // Prints true\n  }\n}\n```\n这个符合规范的方案使用合成而不是继承，在XCard类中增加了一个private的成员card，并提供了一个public的`viewCard()`方法。\
      \  \n**规范的代码样例3（字符串）**：  \n```java\npublic class Filter {\n    public static\
      \ void main(String[] args) throws MalformedURLException {\n        final URL\
      \ allowed = new URL(\"http://mailwebsite.com\");\n        if (!allowed.toString().equals(new\
      \ URL(args[0]).toString())) {\n            throw new SecurityException(\"Access\
      \ Denied\");\n        }\n        // Else proceed\n    }\n}\n```  \n\n上面符合规范的方案比较了两个URL的字符串表达式，从而避免了使用`URL.equals()`的隐患。\
      \   \n**规范的代码样例4（URI.equals()）**： \n```java \npublic class Filter {\n    public\
      \ static void main(String[] args) throws MalformedURLException, URISyntaxException\
      \ {\n        final URI allowed = new URI(\"http://mailwebsite.com\");\n    \
      \    if (!allowed.equals(new URI(args[0]))) {\n            throw new SecurityException(\"\
      Access Denied\");\n        }\n        // Else proceed\n    }\n}\n```  \n统一资源定位符（URI）包含了一个字符串来定位资源:这是一个比URL范围更广的概念。`java.net.URI`类提供了基于字符串的`equals()`和`hashCode()`方法用来满足`Object.equals()`和`Object.hashCode()`方法大体上的合约；它们不会调用主机名解析而且不会受网络连接的影响。URI还提供URL所没有处理标准化的方法。并且，`URL.toURI()`和`URI.toURL()`方法可以方便地在这两个类之间进行转换。程序应该尽可能使用URI而不是URL。根据Java\
      \ API[API 2006]URI类的文档的描述：  \n>一个URI可以是绝对的或是相对的。一个URI字符串是根据一般的语法而不会考虑用指定的纲要来进行解析的。不会有主机的查找，并且不会创建基于纲要的流句柄。\
      \  \n\n这个符合规范的方案使用一个URI而不是URL的对象。因为进行的比较会失败，所以这个过滤器恰当地拦截`http://mailwebsite.com`有着不同字符的网站。\
      \  \n**规范的代码样例4（java.security.Key）**：\n```java  \nprivate static boolean keysEqual(Key\
      \ key1, Key key2) {\n    if (key1.equals(key2)) {\n        return true;\n  \
      \  }\n\n    if (Arrays.equals(key1.getEncoded(), key2.getEncoded())) {\n   \
      \     return true;\n    }\n\n    // More code for different types of keys here.\n\
      \    // For example, the following code can check if\n    // an RSAPrivateKey\
      \ and an RSAPrivateCrtKey are equal:\n    if ((key1 instanceof RSAPrivateKey)\
      \ && (key2 instanceof RSAPrivateKey)) {\n\n        if ((((RSAKey) key1).getModulus().equals(((RSAKey)\
      \ key2).getModulus())) && (((RSAPrivateKey) key1)\n                .getPrivateExponent().equals(((RSAPrivateKey)\
      \ key2).getPrivateExponent()))) {\n            return true;\n        }\n   \
      \ }\n    return false;\n}\n```  \n这个符合规范的方案使用`equals()`方法作为最初的测试，然后比较编码后的密钥来完成独立于密钥提供者的操作。例如：这个方案能够判断一个RSAPrivateKey\
      \ 和 RSAPrivateCrtKey是否代表等同的私钥[Sun 2006]。  \n**特例**  \nMET08-EX0：当没有比较不兼容的类型时，可以不遵守这条规则的要求。一些Java平台库中的类会添加成员来扩展可实例化的类。例如：`java.sql.Timestamp`扩展了`java.util.Date`，并加入了一个nanoseconds成员。Timestamp\
      \ 的`equals()`实现违反了对称性，如果Timestamp和Date对象用于同一集合中或混杂在一起时，该方法可能导致奇怪的行为[Bloch 2008]。"
    Advice_En: "**Compliant Solution**\n\nIn this compliant solution, the `CaseInsensitiveString.equals()`\
      \ method is simplified to operate only on instances of the `CaseInsensitiveString`\
      \ class, consequently preserving symmetry:\n\n```java\npublic final class CaseInsensitiveString\
      \ {\n  private String s;\n\n  public CaseInsensitiveString(String s) {\n   \
      \ if (s == null) {\n      throw new NullPointerException();\n    }\n    this.s\
      \ = s;\n  }\n\n  public boolean equals(Object o) {\n    return o instanceof\
      \ CaseInsensitiveString &&\n        ((CaseInsensitiveString)o).s.equalsIgnoreCase(s);\n\
      \  }\n\n  public int hashCode() {/* ... */}\n\n  public static void main(String[]\
      \ args) {\n    CaseInsensitiveString cis = new CaseInsensitiveString(\"Java\"\
      );\n    String s = \"java\";\n    System.out.println(cis.equals(s)); // Returns\
      \ false now\n    System.out.println(s.equals(cis)); // Returns false now\n \
      \ }\n}\n```\n**Compliant Solution (Delegation)**\n\nUnfortunately, in this case\
      \ it is impossible to extend the `Card` class by adding a value or field in\
      \ the subclass while preserving the Java `equals()` contract. This problem is\
      \ not specific to the Card class but applies to any class hierarchy that can\
      \ consider equal instances of distinct subclasses of some superclass. For such\
      \ cases, use composition rather than inheritance to achieve the desired effect\
      \ [Bloch 2008], [Liskov 1994], [Cline, C++ Super-FAQ]. It is fundamentally impossible\
      \ to have a class that both allows arbitrary subclass extensions and permits\
      \ an `equals()` method that is reflexive, symmetric, and transitive, as is required\
      \ by `Object.equals()`. In the interests of consistency and security, we forgo\
      \ arbitrary subclass extensions, and assume that {{Card.equals()}} may impose\
      \ certain restrictions on its subclasses.\n\nThis compliant solution adopts\
      \ this approach by adding a private `card` field to the `XCard` class and providing\
      \ a public `viewCard()` method.\n\n```java\nclass XCard {\n  private String\
      \ type;\n  private Card card; // Composition\n\n  public XCard(int number, String\
      \ type) {\n    card = new Card(number);\n    this.type = type;\n  }\n\n  public\
      \ Card viewCard() {\n    return card;\n  }\n\n  public boolean equals(Object\
      \ o) {\n    if (!(o instanceof XCard)) {\n      return false;\n    }\n\n   \
      \ XCard cp = (XCard)o;\n    return cp.card.equals(card) && cp.type.equals(type);\n\
      \  }\n\n  public int hashCode() {/* ... */}\n\n  public static void main(String[]\
      \ args) {\n    XCard p1 = new XCard(1, \"type1\");\n    Card p2 = new Card(1);\n\
      \    XCard p3 = new XCard(1, \"type2\");\n    XCard p4 = new XCard(1, \"type1\"\
      );\n    System.out.println(p1.equals(p2)); // Prints false\n    System.out.println(p2.equals(p3));\
      \ // Prints false\n    System.out.println(p1.equals(p3)); // Prints false\n\
      \    System.out.println(p1.equals(p4)); // Prints true\n  }\n}\n```\n\n**Compliant\
      \ Solution (Strings)**\n\nThis compliant solution compares the string representations\
      \ of two URLs, thereby avoiding the pitfalls of `URL.equals()`:\n\n```java\n\
      public class Filter {\n  public static void main(String[] args) throws MalformedURLException\
      \ {\n    final URL allowed = new URL(\"http://mailwebsite.com\");\n    if (!allowed.toString().equals(new\
      \ URL(args[0]).toString())) {\n      throw new SecurityException(\"Access Denied\"\
      );\n    }\n    // Else proceed\n  }\n}\n```\n\nThis solution still has problems.\
      \ Two URLs with different string representation can still refer to the same\
      \ resource. However, the solution fails safely in this case because the `equals()`\
      \ contract is preserved, and the system will never allow a malicious URL to\
      \ be accepted by mistake.\n\n**Compliant Solution (`URI.equals()`)**\n\nA Uniform\
      \ Resource Identifier (URI) contains a string of characters used to identify\
      \ a resource; this is a more general concept than an URL. The `java.net.URI`\
      \ class provides string-based `equals()` and `hashCode()` methods that satisfy\
      \ the general contracts for `Object.equals()` and `Object.hashCode()`; they\
      \ do not invoke hostname resolution and are unaffected by network connectivity.\
      \ `URI` also provides methods for normalization and canonicalization that `URL`\
      \ lacks. Finally, the `URL.toURI()` and `URI.toURL()` methods provide easy conversion\
      \ between the two classes. Programs should use URIs instead of URLs whenever\
      \ possible. According to the Java API Class `URI` documentation [API 2014]:\n\
      \n> A `URI` may be either absolute or relative. A `URI` string is parsed according\
      \ to the generic syntax without regard to the scheme, if any, that it specifies.\
      \ No lookup of the host, if any, is performed, and no scheme-dependent stream\
      \ handler is constructed.\n\nThis compliant solution uses a `URI` object instead\
      \ of a `URL`. The filter appropriately blocks the website when presented with\
      \ any string other than `http://mailwebsite.com` because the comparison fails.\n\
      \n```java\npublic class Filter {\n  public static void main(String[] args)\n\
      \                     throws MalformedURLException, URISyntaxException {\n \
      \   final URI allowed = new URI(\"http://mailwebsite.com\");\n    if (!allowed.equals(new\
      \ URI(args[0]))) {\n      throw new SecurityException(\"Access Denied\");\n\
      \    }\n    // Else proceed\n  }\n}\n```\n\nAdditionally, the `URI` class performs\
      \ normalization (removing extraneous path segments such as \"`..`\") and relativization\
      \ of paths [API 2014], [Darwin 2004].\n**Compliant Solution (`java.security.Key`)**\n\
      \nThis compliant solution uses the `equals()` method as a first test and then\
      \ compares the encoded version of the keys to facilitate provider-independent\
      \ behavior. For example, this code can determine whether a `RSAPrivateKey` and\
      \ `RSAPrivateCrtKey` represent equivalent private keys [Sun 2006].\n\n```java\n\
      private static boolean keysEqual(Key key1, Key key2) {\n  if (key1.equals(key2))\
      \ {\n    return true;\n  }\n\n  if (Arrays.equals(key1.getEncoded(), key2.getEncoded()))\
      \ {\n    return true;\n  }\n\n  // More code for different types of keys here.\n\
      \  // For example, the following code can check if\n  // an RSAPrivateKey and\
      \ an RSAPrivateCrtKey are equal:\n  if ((key1 instanceof RSAPrivateKey) &&\n\
      \      (key2 instanceof RSAPrivateKey)) {\n\n    if ((((RSAKey)key1).getModulus().equals(\n\
      \         ((RSAKey)key2).getModulus())) &&\n       (((RSAPrivateKey) key1).getPrivateExponent().equals(\n\
      \        ((RSAPrivateKey) key2).getPrivateExponent()))) {\n      return true;\n\
      \    }\n  }\n  return false;\n}\n```\n\n**Exceptions**\n\nMET08-J-EX0: Requirements\
      \ of this rule may be violated provided that the incompatible types are never\
      \ compared. There are classes in the Java platform libraries (and elsewhere)\
      \ that extend an instantiable class by adding a value component. For example,\
      \ `java.sql.Timestamp` extends `java.util.Date` and adds a nanoseconds field.\
      \ The `equals()` implementation for `Timestamp` violates symmetry and can cause\
      \ erratic behavior when `Timestamp` and `Date` objects are used in the same\
      \ collection or are otherwise intermixed [Bloch 2008]."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 06. 方法 (MET)
      - type: DetailClassChin
        value: MET08-J：确保比较等同的对象能得到相等的结果
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 06. Methods (MET)
      - type: DetailClassChin
        value: MET08-J:Preserve the equality contract when overriding the equals()
          method
    Default: true
    Description: "通过组合或继承可以创建用于封装已有类的一个新类并加入一个或多个字段。当一个类用这种方式来扩展另一个类时，子类等同性的概念可能包括也可能不包括它的新字段。也就是说，当比较两个子类对象是否相等时，子类等同性的概念可能包括也可能不包括它的新字段。也就是说，当比较两个子类对象是否等同时，有时它们各自的字段必须一样，有时却不需要。根据子类等同的概念，子类可能会覆写`equals`。而且，这个方法必须符合Java语言规范指定的`equals()`的常规合约[JSL\
      \ 2005]。  \n一个对象是同时以它的标识（内存中的位置）和它的状态（实际的数据）作为特征的。操作符`==`只是比较两个对象的标识（核对引用是否指向相同的对象）；在`java.lang.Object`中定义的`euqals()`方法可以被覆写并用来比较对象的状态。如果一个类定义了`equals()`方法，则意味着这个方法会比较状态。当一个类没有自定义的`equals()`方法（在该类中没有定义也没有从基类继承而来），则使用从Object继承的默认的`Object.euqals()`的实现只是比较对象的引用，这可能会产生预期外的结果。\
      \  \n`equals()`方法适用于对象，而不适用于基础数据类型。  \n枚举的类型有一组固定的不同的数值，这些数值有可能用` == `比较而不用`equals()`\
      \ 方法。要注意的是，枚举类型提供的`equals()`方法的内部实现使用了` == `操作符，这是默认的，并且不能被覆写。一般来说，如果子类同时继承了`equals()`的实现并且不要求额外的功能的话，不需要覆写`equals()`方法。\
      \  \nJava语言规范指定的`equals()`通常合约用法有5点要求：  \n1）方法是自反的：对于任何引用x,`x.equals(x)`必须返回true。\
      \  \n2）方法是对称的：对于任何引用x和y，`x.equals(y)`必须返回true,当且仅当`y.equals(x)`返回true。  \n3）方法是可传递的：对于任何引用x、y、和z，如果`x.equals(y)`返回true并且`y.equals(z)`返回true,则`x.equals(z)`返回true。\
      \  \n4）方法是一致的：只要用在`equals()`中比较的对象信息没有发生变化，对于任何引用x和y，多次调用`x.equals(y)`总是返回true或总是返回false。\
      \  \n5）对于任何非空的引用x,`x.equals(null)`必须返回false。  \n注意，在覆写`equals()`方法时不要违反这些要求。\
      \  \n**不规范的代码样例1（对称性）**：\n这个不符合规范的代码示例定义了一个 CaseInsensitiveString 类，类中包含了一个String,并覆写了`equals()`方法。CaseInsensitiveString类知道普通的字符串，但String类对大小写不敏感的字符串没有任何概念。因此，方法`CaseInsensitiveString.equals()`不应该尝试与String类的对象进行互操作。\n\
      ```java\npublic final class CaseInsensitiveString {\n  private String s;\n\n\
      \  public CaseInsensitiveString(String s) {\n    if (s == null) {\n      throw\
      \ new NullPointerException();\n    }\n    this.s = s;\n  }\n\n  // This method\
      \ violates symmetry\n  public boolean equals(Object o) {\n    if (o instanceof\
      \ CaseInsensitiveString) {\n      return s.equalsIgnoreCase(((CaseInsensitiveString)o).s);\n\
      \    }\n\n    if (o instanceof String) {\n      return s.equalsIgnoreCase((String)o);\n\
      \    }\n    return false;\n  }\n\n  // Comply with MET09-J\n  public int hashCode()\
      \ {/* ... */}\n\n  public static void main(String[] args) {\n    CaseInsensitiveString\
      \ cis = new CaseInsensitiveString(\"Java\");\n    String s = \"java\";\n   \
      \ System.out.println(cis.equals(s)); // Returns true\n    System.out.println(s.equals(cis));\
      \ // Returns false\n  }\n}\n```\n通过对 `String` 对象进行操作，`CaseInsensitiveString.equals()`\
      \ 方法违反了第二个合约要求（对称性）。 由于不对称，给定一个 `String` 对象 `s` 和一个 `CaseInsensitiveString`\
      \ 对象 `cis` 仅大小写不同，`cis.equals(s))` 返回 true，而 `s.equals(cis)` 返回 false。\n**不规范的代码样例2（可传递性）**：\n\
      这个不规范的代码例子定义了一个`XCard'类，它继承了`Card'类。\n```java\npublic final class CaseInsensitiveString\
      \ {\n  private String s;\n\n  public CaseInsensitiveString(String s) {\n   \
      \ if (s == null) {\n      throw new NullPointerException();\n    }\n    this.s\
      \ = s;\n  }\n\n  // This method violates symmetry\n  public boolean equals(Object\
      \ o) {\n    if (o instanceof CaseInsensitiveString) {\n      return s.equalsIgnoreCase(((CaseInsensitiveString)o).s);\n\
      \    }\n\n    if (o instanceof String) {\n      return s.equalsIgnoreCase((String)o);\n\
      \    }\n    return false;\n  }\n\n  // Comply with MET09-J\n  public int hashCode()\
      \ {/* ... */}\n\n  public static void main(String[] args) {\n    CaseInsensitiveString\
      \ cis = new CaseInsensitiveString(\"Java\");\n    String s = \"java\";\n   \
      \ System.out.println(cis.equals(s)); // Returns true\n    System.out.println(s.equals(cis));\
      \ // Returns false\n  }\n}\n```\n通过对`String`对象的操作，`CaseInsensitiveString.equals()`方法违反了第二个契约要求（对称性）。由于不对称性，给定一个`String`对象`s`和一个`CaseInsensitiveString`对象`cis`只有大小写不同，`cis.equals(s))`返回真，而`s.equals(cis)`返回假。\n\
      **不规范的代码样例3（一致性）**：\n```java\npublic class Filter {\n  public static void main(String[]\
      \ args) throws MalformedURLException {\n    final URL allowed = new URL(\"http://mailwebsite.com\"\
      );\n    if (!allowed.equals(new URL(args[0]))) {\n      throw new SecurityException(\"\
      Access Denied\");\n    }\n    // Else proceed\n  }\n}\n```\n统一资源定位器（URL）指定了资源的地址和访问资源的方法。根据Java\
      \ API中 URL 类的文档[API 2006]的描述：  \n如果两个URL对象有相同的协议、引用、等价的主机、主机端口、相同的文件及文件片段，则这两个URL是相等的。\
      \  \n在以下的情况中，认为两个主机是等同的：当能够将两个主机的名字解析成相同的IP地址时；或者当其中的一个主机名字不能解析时，两个主机名在不考虑大小写时是相同的；或两个主机名都等于null。\
      \  \n对于HTTP的虚拟主机来说，为`equals()`方法定义的行为是不一致的。  \n虚拟主机允许一个网络服务器可以在一台电脑上处理多个网站，并有时会共享相同的IP地址。遗憾的是，在设计URL类的时候并没有考虑到这种技术。因此，当两个完全不同的URL被解析成相同的IP地址时，URL类认为它们是相等的。\
      \  \n另一个对URL对象使用`equals()`方法带来的风险是该方法在有网络连接和没有网络连接时使用的逻辑是不同的。当连接到Internet时，`equals()`方法遵循Java\
      \ API中描述的步骤，当没有网络连接时，该方法对两个URL进行字符串比较。从而，`URL.equals()`方法违反了对equals()一致性的要求。\
      \  \n设想一个应用允许组织内的一名员工可以访问`http://mailwebsite.com`来获得外部邮件服务。这个应用被设计成暂时的防火墙来拒绝其它网站的访问。然而，如果一个非法网站`http://illegitimatewebsite.com`与合法网站共享一台主机而有着相同的IP地址，则一个有经验的或恶意的用户仍然能够访问这个禁止的网站。更糟糕的是，一个攻击者可以尝试注册多个网站（为了钓鱼攻击的目的），直至一个网站在同一主机上成功注册，而使得防火墙失效。\
      \  \n**不规范的代码样例4（java.security.Key）**：\n```java\npublic class Filter {\n  public\
      \ static void main(String[] args) throws MalformedURLException {\n    final\
      \ URL allowed = new URL(\"http://mailwebsite.com\");\n    if (!allowed.toString().equals(new\
      \ URL(args[0]).toString())) {\n      throw new SecurityException(\"Access Denied\"\
      );\n    }\n    // Else proceed\n  }\n}\n```\n`java.lang.Object.equals()`方法默认是不能比较组合对象的，如密钥。大多数Key类没有提供`equals`方法来覆写Object中的默认实现。在这种情况下，组合对象的元素必须单独进行比较来保证正确性。\
      \  \n这个不符合规则的代码示例使用方法`equals()`比较两个密钥，即使两个实例逻辑上代表同一个密钥时，这一比较还是可能会返回false。"
    Description_En: "Composition or inheritance may be used to create a new class\
      \ that both encapsulates an existing class and adds one or more fields. When\
      \ one class extends another in this way, the concept of equality for the subclass\
      \ may or may not involve its new fields. That is, when comparing two subclass\
      \ objects for equality, sometimes their respective fields must also be equal,\
      \ and other times they need not be equal. Depending on the concept of equality\
      \ for the subclass, the subclass might override `equals()`. Furthermore, this\
      \ method must follow the general contract for `equals()`, as specified by *The\
      \ Java Language Specification* (JLS) [JLS 2015].\n\nAn object is characterized\
      \ both by its identity (location in memory) and by its state (actual data).\
      \ The `==` operator compares only the identities of two objects (to check whether\
      \ the references refer to the same object); the `equals()` method defined in\
      \ `java.lang.Object` can be overridden to compare the state as well. When a\
      \ class defines an `equals()` method, it implies that the method compares state.\
      \ When the class lacks a customized `equals()` method (either locally declared\
      \ or inherited from a parent class), it uses the default `Object.equals()` implementation\
      \ inherited from `Object`. The default `Object.equals()` implementation compares\
      \ only the references and may produce unexpected results.\n\nThe `equals()`\
      \ method applies only to objects, not to primitives.\n\nEnumerated types have\
      \ a fixed set of distinct values that may be compared using `==` rather than\
      \ the `equals()` method. Note that enumerated types provide an `equals()` implementation\
      \ that uses `==` internally; this default cannot be overridden. More generally,\
      \ subclasses that both inherit an implementation of `equals()` from a superclass\
      \ and lack a requirement for additional functionality need not override the\
      \ `equals()` method.\n\nThe general usage contract for `equals()`, as specified\
      \ by the JLS, establishes five requirements:\n\n1. It is reflexive: For any\
      \ reference value `x`, `x.equals(x)` must return true.\n2. It is symmetric:\
      \ For any reference values `x` and `y`, `x.equals(y)` must return true if and\
      \ only if `y.equals(x)` returns true.\n3. It is transitive: For any reference\
      \ values `x`, `y`, and `z`, if `x.equals(y)` returns true and `y.equals(z)`\
      \ returns true, then `x.equals(z)` must return true.\n4. It is consistent: For\
      \ any reference values `x` and `y`, multiple invocations of `x.equals(y)` consistently\
      \ return true or consistently return false, provided no information used in\
      \ `equals()` comparisons on the object is modified.\n5. For any non-null reference\
      \ value `x`, `x.equals(null)` must return false.\n\nNever violate any of these\
      \ requirements when overriding the `equals()` method.\n\n**Noncompliant Code\
      \ Example (Symmetry)**\n\nThis noncompliant code example defines a `CaseInsensitiveString`\
      \ class that includes a `String` and overrides the `equals()` method. The `CaseInsensitiveString`\
      \ class knows about ordinary strings, but the `String` class has no knowledge\
      \ of case-insensitive strings. Consequently, the `CaseInsensitiveString.equals()`\
      \ method should not attempt to interoperate with objects of the `String` class.\n\
      \n```java\npublic final class CaseInsensitiveString {\n  private String s;\n\
      \n  public CaseInsensitiveString(String s) {\n    if (s == null) {\n      throw\
      \ new NullPointerException();\n    }\n    this.s = s;\n  }\n\n  // This method\
      \ violates symmetry\n  public boolean equals(Object o) {\n    if (o instanceof\
      \ CaseInsensitiveString) {\n      return s.equalsIgnoreCase(((CaseInsensitiveString)o).s);\n\
      \    }\n\n    if (o instanceof String) {\n      return s.equalsIgnoreCase((String)o);\n\
      \    }\n    return false;\n  }\n\n  // Comply with MET09-J\n  public int hashCode()\
      \ {/* ... */}\n\n  public static void main(String[] args) {\n    CaseInsensitiveString\
      \ cis = new CaseInsensitiveString(\"Java\");\n    String s = \"java\";\n   \
      \ System.out.println(cis.equals(s)); // Returns true\n    System.out.println(s.equals(cis));\
      \ // Returns false\n  }\n}\n```\n\nBy operating on `String` objects, the `CaseInsensitiveString.equals()`\
      \ method violates the second contract requirement (symmetry). Because of the\
      \ asymmetry, given a `String` object `s` and a `CaseInsensitiveString` object\
      \ `cis` that differ only in case, `cis.equals(s))` returns true, while `s.equals(cis)`\
      \ returns false.\n\n**Noncompliant Code Example (Transitivity)**\n\nThis noncompliant\
      \ code example defines an `XCard` class that extends the `Card` class.\n\n```java\n\
      public class Card {\n  private final int number;\n\n  public Card(int number)\
      \ {\n    this.number = number;\n  }\n\n  public boolean equals(Object o) {\n\
      \    if (!(o instanceof Card)) {\n      return false;\n    }\n\n    Card c =\
      \ (Card)o;\n    return c.number == number;\n  }\n\n  public int hashCode() {/*\
      \ ... */}\n\n}\n\nclass XCard extends Card {\n  private String type;\n  public\
      \ XCard(int number, String type) {\n    super(number);\n    this.type = type;\n\
      \  }\n\n  public boolean equals(Object o) {\n    if (!(o instanceof Card)) {\n\
      \      return false;\n    }\n\n    // Normal Card, do not compare type\n   \
      \ if (!(o instanceof XCard)) {\n      return o.equals(this);\n    }\n\n    //\
      \ It is an XCard, compare type as well\n    XCard xc = (XCard)o;\n    return\
      \ super.equals(o) && xc.type == type;\n  }\n\n  public int hashCode() {/* ...\
      \ */}\n\n  public static void main(String[] args) {\n    XCard p1 = new XCard(1,\
      \ \"type1\");\n    Card p2 = new Card(1);\n    XCard p3 = new XCard(1, \"type2\"\
      );\n    System.out.println(p1.equals(p2)); // Returns true\n    System.out.println(p2.equals(p3));\
      \ // Returns true\n    System.out.println(p1.equals(p3)); // Returns false\n\
      \                                       // violating transitivity\n  }\n}\n\
      ```\n\nIn the noncompliant code example, `p1` and `p2` compare equal and `p2`\
      \ and `p3` compare equal, but `p1` and `p3` compare unequal, violating the transitivity\
      \ requirement. The problem is that the `Card` class has no knowledge of the\
      \ `XCard` class and consequently cannot determine that `p2` and `p3` have different\
      \ values for the field `type`.\n\n\n**Noncompliant Code Example (Consistency)**\n\
      \nA uniform resource locator (URL) specifies both the location of a resource\
      \ and a method to access it. According to the Java API documentation for Class\
      \ URL [API 2014]:\n\n> Two URL objects are equal if they have the same protocol,\
      \ reference equivalent hosts, have the same port number on the host, and the\
      \ same file and fragment of the file.\n>\n> Two hosts are considered equivalent\
      \ if both host names can be resolved into the same IP addresses; else if either\
      \ host name can't be resolved, the host names must be equal without regard to\
      \ case; or both host names equal to null.\n\nThe defined behavior for the `equals()`\
      \ method is known to be inconsistent with *virtual hosting* in HTTP.\n\nVirtual\
      \ hosting allows a web server to host multiple websites on the same computer,\
      \ sometimes sharing the same IP address. Unfortunately, this technique was unanticipated\
      \ when the `URL` class was designed. Consequently, when two completely different\
      \ URLs resolve to the same IP address, the `URL` class considers them to be\
      \ equal.\n\nAnother risk associated with the `equals()` method for `URL` objects\
      \ is that the logic it uses when connected to the Internet differs from that\
      \ used when disconnected. When connected to the Internet, the `equals()` method\
      \ follows the steps described in the Java API; when disconnected, it performs\
      \ a string compare on the two URLs. Consequently, the `URL.equals()` method\
      \ violates the consistency requirement for `equals()`.\n\nConsider an application\
      \ that allows an organization's employees to access an external mail service\
      \ via `http://mailwebsite.com`. The application is designed to deny access to\
      \ other websites by behaving as a makeshift firewall. However, a crafty or malicious\
      \ user could nevertheless access an illegitimate website `http://illegitimatewebsite.com`\
      \ if it were hosted on the same computer as the legitimate website and consequently\
      \ shared the same IP address. Even worse, if the legitimate website were hosted\
      \ on a server in a commercial pool of servers, an attacker could register multiple\
      \ websites in the pool (for phishing purposes) until one was registered on the\
      \ same computer as the legitimate website, consequently defeating the firewall.\n\
      \n```java\npublic class Filter {\n  public static void main(String[] args) throws\
      \ MalformedURLException {\n    final URL allowed = new URL(\"http://mailwebsite.com\"\
      );\n    if (!allowed.equals(new URL(args[0]))) {\n      throw new SecurityException(\"\
      Access Denied\");\n    }\n    // Else proceed\n  }\n}\n```\n\n**Noncompliant\
      \ Code Example (`java.security.Key`)**\n\nThe method `java.lang.Object.equals()`\
      \ by default is unable to compare composite objects such as cryptographic keys.\
      \ Most `Key` classes lack an `equals()` implementation that would override `Object`'s\
      \ default implementation. In such cases, the components of the composite object\
      \ must be compared individually to ensure correctness.\n\nThis noncompliant\
      \ code example compares two keys using the `equals()` method. The comparison\
      \ may return false even when the key instances represent the same logical key.\n\
      \n```java\nprivate static boolean keysEqual(Key key1, Key key2) {\n  if (key1.equals(key2))\
      \ {\n    return true;\n  }\n  return false;\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference:
      - type: CWE
        value: 697:Insufficient Comparison
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140708
    language: Java
  - Advice: "**规范的代码样例1**：使用`lock()`和`unlock()`  \n```java\nprivate final Lock lock\
      \ = new ReentrantLock();\n\npublic void doSomething() {\n  lock.lock();\n  try\
      \ {\n    // ...\n  } finally {\n    lock.unlock();\n  }\n}\n```\n如果不需要 `java.util.concurrent`\
      \ 包的动态锁定实用程序的高级功能，最好使用其他并发原语，例如同步和原子类。"
    Advice_En: "**Compliant Solution (`lock()` and `unlock()`)**\n\nThis compliant\
      \ solution uses the `lock()` and `unlock()` methods provided by the `Lock` interface.\n\
      \n```java\nprivate final Lock lock = new ReentrantLock();\n\npublic void doSomething()\
      \ {\n  lock.lock();\n  try {\n    // ...\n  } finally {\n    lock.unlock();\n\
      \  }\n}\n```\n\nIn the absence of a requirement for the advanced functionality\
      \ of the `java.util.concurrent` package's dynamic-locking utilities, it is better\
      \ to use other concurrency primitives such as synchronization and atomic classes."
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 09. 锁 (LCK)
      - type: DetailClassChin
        value: LCK03-J：不要基于高层并发对象的内置锁来实现同步
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 09. Locking (LCK)
      - type: DetailClassChin
        value: LCK03-J:Do not synchronize on the intrinsic locks of high-level concurrency
          objects
    Default: true
    Description: "那些实现其中一个或者都实现了在`java.util.concurrent.locks`包中Lock和Condition接口的类，被认为是高层的并发对象。对这样的对象使用内置锁是有问题的，尽管看起来代码的功能是正确的。因此，与这些对象交互的那些程序应该仅仅使用由接口提供的锁，而不允许使用内置锁。\n\
      因此，与这些对象交互的程序必须只使用接口提供的高级锁定工具； 禁止使用内部锁。 当代码从内部锁定重构为 `java.util.concurrent` 动态锁定实用程序时，通常会出现此问题。\n\
      不规范的代码样**例1**：\n这个不兼容的代码示例中的 `doSomething()` 方法在 `ReentrantLock` 实例的内在锁上同步，而不是在\
      \ `ReentrantLock` 封装的可重入互斥 `Lock` 上同步。\n```java\nprivate final Lock lock = new\
      \ ReentrantLock();\n\npublic void doSomething() {\n  synchronized(lock) {\n\
      \    // ...\n  }\n}\n```"
    Description_En: "Instances of classes that implement either or both of the `Lock`\
      \ and `Condition` interfaces of the `java.util.concurrent.locks` package are\
      \ known as high-level concurrency objects. Using the intrinsic locks of such\
      \ objects is a questionable practice even in cases where the code may appear\
      \ to function correctly. Code that uses the intrinsic lock of a `Lock` object\
      \ is likely to interact with code that uses the `Lock` interface. These two\
      \ components will believe they are protecting data with the same lock, while\
      \ they are, in fact, using two distinct locks. As such, the `Lock` will fail\
      \ to protect any data.\n\nConsequently, programs that interact with such objects\
      \ must use only the high-level locking facilities provided by the interfaces;\
      \ use of the intrinsic locks is prohibited. This problem generally arises when\
      \ code is refactored from intrinsic locking to the `java.util.concurrent` dynamic-locking\
      \ utilities.\n\n**Noncompliant Code Example (`ReentrantLock`)**\n\nThe `doSomething()`\
      \ method in this noncompliant code example synchronizes on the intrinsic lock\
      \ of an instance of `ReentrantLock` rather than on the reentrant mutual exclusion\
      \ `Lock` encapsulated by `ReentrantLock`.\n\n```java\nprivate final Lock lock\
      \ = new ReentrantLock();\n\npublic void doSomething() {\n  synchronized(lock)\
      \ {\n    // ...\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141003'
    language: Java
  - Advice: "对应的符合规则的解决方案使用`lock()`和`unlock()`方法，这些方法由 Lock 接口提供。   \n    ```java\n\
      \    private final Lock lock = new ReentrantLock();\n    \n    public void doSomething()\
      \ {\n        lock.lock();\n        try {\n            // do something\n    \
      \    } finally {\n            lock.unlock();\n        }\n    }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.9.4 不要基于高层并发对象的内置锁来实现同步
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n与高层的并发对象交互的程序应该仅仅使用由接口提供的锁，而不允许使用内置锁。   \n2) 原理   \n那些实现其中一个或者都实现了在\
      \ `java.util.concurrent.locks` 包中 Lock 和 Condition 接口的类，被认为是高层的并发对象。对这样的对象使用内置锁会带来安全问题，尽管看起来代码的功能是正确的。因此，与这些对象交互的那些程序应该仅仅使用由接口提供的锁，而不允许使用内置锁。这个问题通常在代码重构时发生，比如说从使用内置锁重构成使用\
      \ `java.util.concurrent` 的动态锁。   \n3) 示例   \n    如下不符合规则的代码示例（重入锁），该示例的 `doSomething()`方法中，基于\
      \ ReentrantLock 的内置锁实现同步而不是基于被 ReentrantLock 封装的可重入的互斥锁实现同步。   \n    ```java\n\
      \    private final Lock lock = new ReentrantLock();\n    \n    public void doSomething()\
      \ {\n        synchronized (lock) {\n            // . . .\n        }\n    }\n\
      \    ``` \n        \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160082
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：使用lock()和unlock()  \n```java\nprivate\
      \ final Lock lock = new ReentrantLock();  \npublic void doSomething() {  \n\
      \    lock.lock()  \n    try{  \n        // do something  \n    }finally{  \n\
      \        lock.unlock()  \n    }  \n}\n```  \n这个代码中，基于被ReentrantLock封装的可重入的互斥锁实现同步。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.5 不要基于高层并发对象的内置锁来实现同步
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：那些实现其中一个或者都实现了在`java.util.concurrent.locks`包中Lock和Condition接口的类，被认为是高层的并发对象。对这样的对象使用内置锁是有问题的，尽管看起来代码的功能是正确的。因此，与这些对象交互的那些程序应该仅仅使用由接口提供的锁，而不允许使用内置锁。\
      \  \n对于不要基于高层并发对象的内置锁来实现同步的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\n\
      private final Lock lock = new ReentrantLock();  \npublic void doSomething()\
      \ {  \n    synchronized (lock) {  \n        // . . .  \n    }  \n}\n```  \n\
      这个代码中，基于ReentrantLock的内置锁实现同步，对这样的对象使用内置锁是有问题的。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170037'
    language: Java
  - Advice: "**规范的代码样例1**：  \n```java\npublic void doGet(HttpServletRequest request,\
      \ HttpServletResponse response) throws IOException, ServletException {\n\n \
      \   try {\n        // Do work that doesn't require the output writer\n    }\
      \ catch (IOException x) {\n        response.sendError(HttpServletResponse.SC_INTERNAL_SERVER_ERROR);\n\
      \    }\n\n    PrintWriter out = response.getWriter();\n    try {\n        out.println(\"\
      <html>\");\n\n        // ... All work\n\n    } catch (IOException ex) {\n  \
      \      out.println(ex.getMessage());\n    } finally {\n        out.flush();\n\
      \    }\n}\n```\n`sendError()`方法应该只在输出流或writer创建之前使用，因为它覆盖了任何输出。一旦创建了输出流或writer，错误应该与有效的HTML一起输出。这个合规的代码使用了两种策略，确保流在最后的子句中只刷新一次。"
    Advice_En: "**Compliant Solution**\n\nThe `sendError()` method should be used\
      \ only before an output stream or writer has been created because it overwrites\
      \ any output. Once the output stream or writer has been created, errors should\
      \ be output alongside valid HTML. This compliant solution uses both strategies,\
      \ ensuring that the stream is flushed only once, in the `finally` clause.\n\n\
      ```java\npublic void doGet(HttpServletRequest request, HttpServletResponse response)\n\
      \  throws IOException, ServletException {\n\n  try {\n    // Do work that doesn't\
      \ require the output writer\n  } catch (IOException x) {\n    response.sendError(HttpServletResponse.SC_INTERNAL_SERVER_ERROR);\n\
      \  }\n\n  PrintWriter out = response.getWriter();\n  try {\n    out.println(\"\
      <html>\");\n\n    // ... All work\n\n  } catch (IOException ex) {\n    out.println(ex.getMessage());\n\
      \  } finally {\n    out.flush();\n  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO15-J：当一个servlet输出流提交后不要重置输出流
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO15-J:Do not reset a servlet's output stream after committing it
    Default: true
    Description: "当web servlet接收到来自客户端的请求时，它必须生成适当的响应。Java的HttpServlet提供HttpServletResponse对象捕捉到一个合适的回应。这种响应可以使用`getOutputStream()`提供的输出流或`getWriter()`方法返回的writer。\
      \  \n那么响应被提交指的是如果它的状态代码和HTML消息头已经被发送[J2EE API 2013]。在作出响应之后，会添加更多的数据到响应中，但是某些行为变得不可能。**例如**：不可能改变字符编码，因为编码包含在HTML头中。其中一些非法操作将会产生非法的异常，而另一些则没有效果。这些非法行为包括：\
      \  \n* 重新设置流或重新提交到流\n* 刷新流或者writer的缓冲区\n* 调用`getWriter()` 或 `getOutputStream()`\n\
      * 重定向一个HttpServletResponse到另外一个server\n* 修改流的编码、内容类型或缓冲区大小\n\n**不规范的代码样例1**：\
      \  \n```java\npublic void doGet(HttpServletRequest request, HttpServletResponse\
      \ response) throws IOException, ServletException {\n\n    PrintWriter out =\
      \ response.getWriter();\n    try {\n        out.println(\"<html>\");\n\n   \
      \     // ... Write some response text\n\n        out.flush(); // Commits the\
      \ stream\n\n        // ... More work\n\n    } catch (IOException x) {\n    \
      \    response.sendError(HttpServletResponse.SC_INTERNAL_SERVER_ERROR);\n   \
      \ }\n}\n```\n上面不合规范的代码中，如果在刷新流之后出现IOException，则在catch子句执行时将提交流。因此，`sendError()`方法将抛出IllegalStateException。\
      \  \n**不规范的代码样例2**：  \n```java\npublic void doGet(HttpServletRequest request,\
      \ HttpServletResponse response) throws IOException, ServletException {\n\n \
      \   PrintWriter out = response.getWriter();\n    try {\n        out.println(\"\
      <html>\");\n\n        // ... Write some response text\n\n        out.flush();\
      \ // Commits the stream\n\n        // ... More work\n\n    } catch (IOException\
      \ x) {\n        out.println(x.getMessage());\n        out.flush();\n    }\n\
      }\n```\n上面不合规范的代码中，如果刷新流之后出现IOException,在catch语句中流将刷新。"
    Description_En: "When a web servlet receives a request from a client, it must\
      \ produce some suitable response. Java's `HttpServlet` provides the `HttpServletResponse`\
      \ object to capture a suitable response. This response can be built using an\
      \ output stream provided by `getOutputStream()` or a writer provided by `getWriter()`.\n\
      A response is said to be committed if its status code and HTML headers have\
      \ been sent [J2EE API 2013]. After a response is committed, further data may\
      \ be added to the response, but certain behaviors become impossible. For example,\
      \ it is impossible to change the character encoding, because the encoding is\
      \ included in the HTML header. Some of these illegal operations will yield a\
      \ IllegalStateException, while others will have no effect. These illegal behaviors\
      \ include the following:\n\n* Resetting the stream or recommitting to the stream\n\
      * Flushing the stream's or writer's buffer\n* Invoking either `getWriter()`\
      \ or `getOutputStream()`\n* Redirecting an `HttpServletResponse` to another\
      \ server\n* Modifying the stream's character encoding, content type, or buffer\
      \ size\n\n**Noncompliant Code Example**\n\nThis noncompliant code example illustrates\
      \ a servlet that indicates if an internal error occurs by using the `HttpServletResponse.sendError()`\
      \ method to indicate an internal server error.\n\n```java\npublic void doGet(HttpServletRequest\
      \ request, HttpServletResponse response)\n  throws IOException, ServletException\
      \ {\n\n  PrintWriter out = response.getWriter();\n  try {\n    out.println(\"\
      <html>\");\n\n    // ... Write some response text\n\n    out.flush();  // Commits\
      \ the stream\n\n    // ... More work\n\n  } catch (IOException x) {\n    response.sendError(HttpServletResponse.SC_INTERNAL_SERVER_ERROR);\n\
      \  }\n}\n```\n\nIf an `IOException` occurs after flushing the stream, the stream\
      \ will be committed when the `catch` clause executes. Consequently, the `sendError()`\
      \ operation will throw an `IllegalStateException`.\n**Noncompliant Code Example**\n\
      \nThis noncompliant code example illustrates a servlet that indicates if an\
      \ internal error occurs by printing an error message to the output stream and\
      \ flushing it:\n\n```java\npublic void doGet(HttpServletRequest request, HttpServletResponse\
      \ response)\n  throws IOException, ServletException {\n\n  PrintWriter out =\
      \ response.getWriter();\n  try {\n    out.println(\"<html>\");\n\n    // ...\
      \ Write some response text\n\n    out.flush();  // Commits the stream\n\n  \
      \  // ... More work\n\n  } catch (IOException x) {\n    out.println(x.getMessage());\n\
      \    out.flush();\n  }\n}\n```\n\nIf an `IOException` occurs after flushing\
      \ the stream, the stream will be reflushed in the `catch` clause."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141415'
    language: Java
  - Advice: "**规范的代码样例1**\n这个解决方案删除了对可重写方法的调用。当移除这种调用不可行时，请声明该方法为私有或最终方法。\n```java\n\
      private void readObject(final ObjectInputStream stream)\n                  \
      \      throws IOException, ClassNotFoundException {\n  stream.defaultReadObject();\n\
      }\n```\n**特例**\nSER09-J-EX0: `readObject()`方法可以调用`defaultReadObject()`和`readFields()`类中的可重写方法`java.io.ObjectInputStream`\
      \ [SCG 2009]。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution removes the call\
      \ to the overridable method. When removing such calls is infeasible, declare\
      \ the method private or final.\n\n```java\nprivate void readObject(final ObjectInputStream\
      \ stream)\n                        throws IOException, ClassNotFoundException\
      \ {\n  stream.defaultReadObject();\n}\n```\n\n**Exceptions**\n\nSER09-J-EX0:\
      \ The `readObject()` method may invoke the overridable methods `defaultReadObject()`\
      \ and `readFields()` in class `java.io.ObjectInputStream` [SCG 2009]."
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 14. 序列化 (SER)
      - type: DetailClassChin
        value: SER09-J：不要从readObject()方法中调用可复用的方法
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 14. Serialization (SER)
      - type: DetailClassChin
        value: SER09-J:Do not invoke overridable methods from the readObject() method
    Default: true
    Description: "`readObject()`方法不能调用任何可重写的方法。从`readObject()`方法中调用可重写方法，可以让重写方法在对象完全初始化之前访问其状态。这种过早的访问是可能的，因为在反序列化中，`readObject`扮演着对象构造器的角色，因此在`readObject`退出之前，对象的初始化还没有完成（参见[MET06-J.不要在clone()中调用可重写方法]）。\n\
      **不规范的代码样例1**\n这个不符合规定的代码例子从`readObject()`方法中调用了一个可重写的方法。\n```java\nprivate\
      \ void readObject(final ObjectInputStream stream)\n                        throws\
      \ IOException, ClassNotFoundException {\n  overridableMethod();\n  stream.defaultReadObject();\n\
      }\n\npublic void overridableMethod() {\n  // ...\n}\n```"
    Description_En: "The `readObject()` method must not call any overridable methods.\
      \ Invoking overridable methods from the `readObject()` method can provide the\
      \ overriding method with access to the object's state before it is fully initialized.\
      \ This premature access is possible because, in deserialization, `readObject`\
      \ plays the role of object constructor and therefore object initialization is\
      \ not complete until `readObject` exits (see also [MET06-J. Do not invoke overridable\
      \ methods in clone()]).\n\n**Noncompliant Code Example**\n\nThis noncompliant\
      \ code example invokes an overridable method from the `readObject()` method:\n\
      \n```java\nprivate void readObject(final ObjectInputStream stream)\n       \
      \                 throws IOException, ClassNotFoundException {\n  overridableMethod();\n\
      \  stream.defaultReadObject();\n}\n\npublic void overridableMethod() {\n  //\
      \ ...\n}\n```"
    DomainType: ''
    DynamicDescription: ''
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141506'
    language: Java
  - Advice: "构造函数只能调用private或者final方法。\n**规范的代码样例1**：  \n```java\nclass SuperClass\
      \ {\n    public SuperClass() {\n        doLogic();\n    }\n\n    public final\
      \ void doLogic() {\n        System.out.println(\"This is superclass!\");\n \
      \   }\n}\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution declares the `doLogic()`\
      \ method as final so that it cannot be overridden:\n\n```java\nclass SuperClass\
      \ {\n  public SuperClass() {\n    doLogic();\n  }\n\n  public final void doLogic()\
      \ {\n    System.out.println(\"This is superclass!\");\n  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 06. 方法 (MET)
      - type: DetailClassChin
        value: MET05-J：确保构造函数不会调用可覆写的方法
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 06. Methods (MET)
      - type: DetailClassChin
        value: MET05-J:Ensure that constructors do not call overridable methods
    Default: not
    Description: "根据Java语言规范中12.5节“创建类的新实例”的描述：  \n> 在创建类的新实例时：Java编程语言没有为方法派生指定变更的规则。如果在初始化的对象的子类中覆写了被调用的方法，则将会使用这些覆写的方法，即使在新实例还没完全初始化。\n\
      \n在创建对象时调用可覆写的方法可能造成使用未初始化的数据，进而导致执行异常或是预期外的结果。在构造函数中调用可覆写的方法也会在对象创建完成前泄露this引用，这会使得其他线程可能访问到未初始化或不一致的数据。\
      \  \n**不规范的代码样例1**：\n这个不合规的代码示例导致 `doLogic()` 方法使用了未初始化的数据：\n```java\nclass\
      \ SuperClass {\n  public SuperClass () {\n    doLogic();\n  }\n\n  public void\
      \ doLogic() {\n    System.out.println(\"This is superclass!\");\n  }\n}\n\n\
      class SubClass extends SuperClass {\n  private String color = \"red\";\n\n \
      \ public void doLogic() {\n    System.out.println(\"This is subclass! The color\
      \ is :\" + color);\n    // ...\n  }\n}\n\npublic class Overridable {\n  public\
      \ static void main(String[] args) {\n    SuperClass bc = new SuperClass();\n\
      \    // Prints \"This is superclass!\"\n    SuperClass sc = new SubClass();\n\
      \    // Prints \"This is subclass! The color is :null\"\n  }\n}\n```\n方法`doLogic()`使用了未初始化的数据。\
      \ 方法`doLogic()`是在基类的构造方法中调用的。当直接创建基类的实例时：将调用基类中的方法`doLogic()`，从而正确的完成执行。然而，当子类发起基类的创建时，却调用了子类的`doLogic()`方法，在这种情况下，因为子类的构造方法还没有完成执行，所以`color`的数值还是null。"
    Description_En: "According to The Java Language Specification, §12.5, \"Creation\
      \ of New Class Instances\" [JLS 2015]:\n\n> Unlike C++, the Java programming\
      \ language does not specify altered rules for method dispatch during the creation\
      \ of a new class instance. If methods are invoked that are overridden in subclasses\
      \ in the object being initialized, then these overriding methods are used, even\
      \ before the new object is completely initialized.\n\nInvocation of an overridable\
      \ method during object construction may result in the use of uninitialized data,\
      \ leading to runtime exceptions or to unanticipated outcomes. Calling overridable\
      \ methods from constructors can also leak the this reference before object construction\
      \ is complete, potentially exposing uninitialized or inconsistent data to other\
      \ threads (see TSM01-J. Do not let the this reference escape during object construction\
      \ for additional information). As a result, a class's constructor must invoke\
      \ (directly or indirectly) only methods in that class that are static, final\
      \ or private.\n\n**Noncompliant Code Example**\n\nThis noncompliant code example\
      \ results in the use of uninitialized data by the `doLogic()` method:\n\n```java\n\
      class SuperClass {\n  public SuperClass () {\n    doLogic();\n  }\n\n  public\
      \ void doLogic() {\n    System.out.println(\"This is superclass!\");\n  }\n\
      }\n\nclass SubClass extends SuperClass {\n  private String color = \"red\";\n\
      \n  public void doLogic() {\n    System.out.println(\"This is subclass! The\
      \ color is :\" + color);\n    // ...\n  }\n}\n\npublic class Overridable {\n\
      \  public static void main(String[] args) {\n    SuperClass bc = new SuperClass();\n\
      \    // Prints \"This is superclass!\"\n    SuperClass sc = new SubClass();\n\
      \    // Prints \"This is subclass! The color is :null\"\n  }\n}\n```\n\nThe\
      \ `doLogic()` method is invoked from the superclass's constructor. When the\
      \ superclass is constructed directly, the `doLogic()` method in the superclass\
      \ is invoked and executes successfully. However, when the subclass initiates\
      \ the superclass's construction, the subclass's `doLogic()` method is invoked\
      \ instead. In this case, the value of `color` is still `null` because the subclass's\
      \ constructor has not yet concluded."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140705'
    language: Java
  - Advice: "对应的符合规定的解决方案是：将方法`doLogic()`声明为 final，从而确保`doLogic()`不会被覆写。   \n    ```java\n\
      \    class Superclass {\n        public Superclass() {\n            doLogic();\n\
      \        }\n    \n        public final void doLogic() {\n            System.out.println(\"\
      This is superclass!\");\n        }\n    }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.6.6 确保构造函数不会调用可覆写的方法
    Categories_En:
      Category: []
    Default: not
    Description: "1) 规定   \n构造函数只能调用 final或 private的方法，即不可覆写的方法。   \n2) 原理   \n和 C++不同，在创建类的新实例时，Java编程语言没有为方法派生指定变更的规则。如果在初始化的对象的子类中覆写了被调用的方法，则将会使用这些覆写方法，即使新实例还没有完全初始化。\
      \   \n在创建对象时调用可覆写的方法可能造成使用未初始化的数据，进而导致执行异常或是预期外的结果。在构造函数中调用可覆写的方法也会在对象创建完成前泄漏\
      \ this 引用，这使得其他线程可能访问到未初始化或不一致的数据。因此，构造函数只能调用 final或 private 的方法。   \n3) 示例\
      \   \n    这个不符合规则的代码示例中，方法`doLogic()`使用了未初始化的数据。   \n    ```java\n    class\
      \ Subclass extends Superclass {\n        private String color = null;\n    \n\
      \        public Subclass() {\n            super();\n            color = \"Red\"\
      ;\n        }\n    \n        public void doLogic() {\n            System.out.println(\"\
      This is subclass! The color is :\" + color); // . . .\n            public class\
      \ Overridable {\n                public static void main(String[] args) {\n\
      \                    Superclass be = new Superclass();\n                   \
      \ // Prints \"This is superclass!\"\n                    Superclass sc = new\
      \ Subclass();\n                    // Prints \"This is subclass! The color is\
      \ :null\"\n                }\n            }\n        }\n    }\n    ```\n   \
      \    \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: MET05-J:Ensure that constructors do not call overridable methods
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160055
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nclass SuperClass {  \n\
      public SuperClass () {  \n    doLogic();  \n}  \npublic final void  doLogic()\
      \ {  \n    System.out.println(\"This is superclass!\");  \n}  \n}\n```  \n这个符合规范的方案将`doLogic()`声明为final的，从而确保了`doLogic()`方法不会被覆写。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.1 面向对象程序安全
      - type: DetailClassChin
        value: A.3.1.4 构造方法中不要调用可覆写的方法
    Categories_En:
      Category: []
    Default: not
    Description: "规范说明：在创建对象时调用可覆写的方法可能造成未初始化的数据，进而导致执行异常或是预期外的结果。在构造函数中调用可覆写的方法同时会在对象创建完成前泄露this引用，这使得其它线程可访问到未初始化或不一致的数据。\
      \  \n对于构造方法中不要调用可覆写的方法的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nclass\
      \ SuperClass {  \n    public SuperClass() {  \n        doLogic();  \n    } \
      \ \n    public void doLogic() {  \n        System.out.println(\"This is superclass!\"\
      );  \n    }  \n}  \nclass SubClass extends SuperClass {  \n    private String\
      \ color = null;  \n    public SubClass() {  \n        super();  \n        color\
      \ = \"Red\";  \n    }  \n    public void doLogic() {  \n        System.out.println(\"\
      This is subclass! The color is :\" + color);  \n            // ...  \n    }\
      \  \n}     \npublic class Overridable {  \n    public static void main(String[]\
      \ args) {  \n        SuperClass bc = new SuperClass();  \n        // Prints\
      \ \"This is superclass!\"  \n        SuperClass sc = new SubClass();  \n   \
      \     // Prints \"This is subclass! The color is :null\"  \n    }  \n}\n```\
      \  \n这个不符合规则的代码示例中，方法`doLogic()`使用了未初始化的数据。  \n方法`doLogic()`是在基类的构造函数中调用的。当直接创建基类的实例时，将调用基类的方法`doLogic()`，从而正确的执行。然而，当子类发起基类的创建时，却调用了子类的`doLogic()`方法，这种情况下，因为子类的构造函数还没有完成执行，所有`color`的数值还是null。\
      \  \n构造函数只能调用final或private的方法。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: MET05-J:Ensure that constructors do not call overridable methods
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170030'
    language: Java
  - Advice: "**符合规范的代码样例1**\n当一个超类定义了一个`finalize()`方法时，要确保将可以立即被垃圾回收的对象与必须依赖finalizer的对象解耦。这个解决方案可以确保一旦对象变得不可及，\"\
      缓冲区 \"就可以被回收。\n```java\nclass MyFrame {\n  private JFrame frame;\n  private\
      \ byte[] buffer = new byte[16 * 1024 * 1024]; // Now decoupled\n}\n```\n**符合规范的代码样例2**\n\
      Joshua Bloch [Bloch 2008]建议明确地实现一个`stop()`方法，使其在类的生命周期结束后仍处于不可用的状态。类中的一个私有字段可以提示该类是否不可用。所有的类方法在对该类进行操作之前必须检查这个字段。这类似于OBJ11-J中讨论的\
      \ \"初始化标志 \"的解决方案。要警惕让构造函数抛出异常。一如既往，调用终止逻辑的好地方是在`finally`块中。\n**特例**\nMET12-J-EX0：在处理本机代码时可能会使用终结器，因为垃圾收集器无法回收用另一种语言编写的代码使用的内存，而且对象的生命周期通常是未知的。\
      \ 同样，本机进程不得执行任何需要立即释放资源的关键作业。\n\n任何覆盖 `finalize()` 的子类也必须显式调用其超类的方法。 `finalize`\
      \ 没有自动*链接*。 正确的处理方法如下：\n```java\nprotected void finalize() throws Throwable\
      \ {\n  try {\n    //...\n  } finally {\n    super.finalize();\n  }\n}\n```\n\
      一个代价更大的解决方案是声明一个匿名类，以保证 `finalize()` 方法为父类运行。 此解决方案适用于公共非最终类。 “如果子类覆盖 `finalize()`\
      \ 并且没有显式调用 `super.finalize`，则终结器守护程序强制调用 `super.finalize`” [JLS 2015]。\n```java\n\
      public class Foo {\n  // The finalizeGuardian object finalizes the outer Foo\
      \ object\n  private final Object finalizerGuardian = new Object() {\n    protected\
      \ void finalize() throws Throwable {\n      // Finalize outer Foo object\n \
      \   }\n  };\n  //...\n}\n```\n在处理本地代码时，排序问题可能是危险的。例如，如果对象`A`引用对象`B`（直接或反射地），而后者先被最终化，`A`的最终化器可能最终取消引用悬空的本地指针。为了给终结者施加一个明确的顺序，要确保`B`在`A`的终结者结束之前仍然可以到达。这可以通过在一些全局状态变量中添加对`B`的引用，并在`A`的终结者执行时删除它来实现。另一个方法是使用`java.lang.ref`引用。\n\
      \nMET12-J-EX1: 类可以使用空的finalizer来防止finalizer攻击，如OBJ11-J中规定.要警惕让构造函数抛出异常。"
    Advice_En: "**Compliant Solution (Superclass's finalizer)**\n\nWhen a superclass\
      \ defines a `finalize()` method, make sure to decouple the objects that can\
      \ be immediately garbage collected from those that must depend on the finalizer.\
      \ This compliant solution ensures that the `buffer` can be reclaimed as soon\
      \ as the object becomes unreachable.\n\n```java\nclass MyFrame {\n  private\
      \ JFrame frame;\n  private byte[] buffer = new byte[16 * 1024 * 1024]; // Now\
      \ decoupled\n}\n```\n**Compliant Solution**\n\nJoshua Bloch [Bloch 2008] suggests\
      \ implementing a `stop()` method explicitly such that it leaves the class in\
      \ an unusable state beyond its lifetime. A private field within the class can\
      \ signal whether the class is unusable. All the class methods must check this\
      \ field prior to operating on the class. This is akin to the \"initialized flag\"\
      –compliant solution discussed in OBJ11-J. Be wary of letting constructors throw\
      \ exceptions. As always, a good place to call the termination logic is in the\
      \ `finally` block.\n\n**Exceptions**\n\nMET12-J-EX0: Finalizers may be used\
      \ when working with native code because the garbage collector cannot reclaim\
      \ memory used by code written in another language and because the lifetime of\
      \ the object is often unknown. Again, the native process must not perform any\
      \ critical jobs that require immediate resource deallocation.\n\nAny subclass\
      \ that overrides `finalize()` must explicitly invoke the method for its superclass\
      \ as well. There is no automatic *chaining* with `finalize`. The correct way\
      \ to handle it is as follows:\n\n```java\nprotected void finalize() throws Throwable\
      \ {\n  try {\n    //...\n  } finally {\n    super.finalize();\n  }\n}\n```\n\
      \nA more expensive solution is to declare an anonymous class so that the `finalize()`\
      \ method is guaranteed to run for the superclass. This solution is applicable\
      \ to public nonfinal classes. \"The finalizer guardian forces `super.finalize`\
      \ to be called if a subclass overrides `finalize()` and does not explicitly\
      \ call `super.finalize`\" [JLS 2015].\n\n```java\npublic class Foo {\n  // The\
      \ finalizeGuardian object finalizes the outer Foo object\n  private final Object\
      \ finalizerGuardian = new Object() {\n    protected void finalize() throws Throwable\
      \ {\n      // Finalize outer Foo object\n    }\n  };\n  //...\n}\n```\n\nThe\
      \ ordering problem can be dangerous when dealing with native code. For example,\
      \ if object `A` references object `B` (either directly or reflectively) and\
      \ the latter gets finalized first, `A`'s finalizer may end up dereferencing\
      \ dangling native pointers. To impose an explicit ordering on finalizers, make\
      \ sure that `B` remains reachable until `A`'s finalizer has concluded. This\
      \ can be achieved by adding a reference to `B` in some global state variable\
      \ and removing it when `A`'s finalizer executes. An alternative is to use the\
      \ `java.lang.ref` references.\n\nMET12-J-EX1: A class may use an empty final\
      \ finalizer to prevent a finalizer attack, as specified in OBJ11-J. Be wary\
      \ of letting constructors throw exceptions."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 06. 方法 (MET)
      - type: DetailClassChin
        value: MET12-J：不要使用析构函数
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 06. Methods (MET)
      - type: DetailClassChin
        value: MET12-J:Do not use finalizers
    Default: true
    Description: "显式的调用`finalize()`并不能直接释放需要释放的资源。尽管Java语言规范中允许外部代码调用对象的`finalize()`方法，但这其实并不是一个好办法。直接调用\
      \ `finalize()`意味着要不止一次地调用`finalize()`方法： 第一次将会直接调用，而最后一次调用会在对象回收之后执行。  \n析构函数有太多问题，所以限制它使用在特殊情况中：\
      \  \n1）`finalize()`的执行没有固定的时间，这是由JVM所决定的。一个对象在不可达到后，`finalize()`的执行可能会滞后任意长时间。唯一的保证是在对应对象不可达（垃圾回收过程的第一阶段）或在回收对应对象所占资源（垃圾回收过程的第二阶段）后的某个时候，`finalize()`方法会执行。\
      \  \n2）在程序停止运行前，JVM可能不会去调用孤立对象的`finalize()`。因此，尝试从`finalize()`方法中更新持久状态会失败，而不会有警告。同样，Java并没有保证`finalize()`方法一定会在进程结束时执行。类似于`System.gc()、System.runFinalization()、System.runFinalizersOnExit()、Runtime.runFinalizersOnExit()`这些方法要么是缺少这样的保证，要么是因为缺乏安全性和存在死锁而废弃。\
      \  \n3）Java语言规范12.6.2节“finalize的调用是无序的”：\n>Java语言没有强制调用finalize()方法的次序。终结器可能以不同的次序甚至并发调用finalize()方法，一个可能的结果就是运行慢的finalize()方法会延迟执行队列栈的其他finalize()方法的执行。\
      \  \n\n4）在终结过程中抛出的异常如没有捕捉到则会被忽略。  \n5）造成内存泄漏的错误会导致对象被误解为是可达到的，从而JVM不会调用这些对象的`finalize()`方法。\
      \  \n6）程序员可能会在`finalize()`方法中无意的重新使用对象引用。在这种情况下，垃圾回收器必须再次判断是否可以释放对象，并且，因为已经执行了`finalize()`方法，所以垃圾回收不会再次调用它。\
      \  \n7）垃圾回收器是依赖于内存的可用性与使用情况，而与其他特定资源的缺乏无关。因此，当有足够内存时，一种不足的资源也有可能会被耗尽。  \n8）普遍认为析构函数为垃圾回收提供帮助，而事实上却恰恰相反，析构函数的执行增加了垃圾回收的时间，并带来了空间上的开销。由于析构函数延长了许多对象的生命周期，这干扰了新一代的垃圾回收器的操作。\
      \  \n9）析构函数的使用会带来同步化的问题，即使在余下的程序是单线程的情况下。垃圾回收器会从所选择的一个或多个线程中调用`finalize()`方法，当必须使用析构函数时，避免同步访问任何需要清理的数据结构。\
      \  \n10）在析构函数中使用锁和其他基于同步的机制会导致死锁或饥饿。存在这种问题的可能性是因为不能保证或控制析构函数调用次序和析构函数运行的线程。\
      \  \n**不规范的代码样例1**：\n使用终结器的父类对其扩展类施加额外的约束。 考虑 JDK 1.5 及更早版本的示例。 以下不合规的代码示例分配了一个\
      \ 16 MB 的缓冲区，用于支持 Swing `JFrame` 对象。 尽管 `JFrame` API 缺少 `finalize()` 方法，`JFrame`\
      \ 扩展了 `AWT.Frame`，后者确实有 `finalize()` 方法。 当 `MyFrame` 对象变得不可访问时，垃圾收集器无法回收字节缓冲区的存储空间，因为继承的\
      \ `finalize()` 方法中的代码可能会引用它。 因此，字节缓冲区必须*至少*持续到类 `MyFrame` 的继承的`finalize()` 方法完成其执行，并且在接下来的垃圾收集周期之前不能回收。\n\
      ```java\nclass MyFrame extends JFrame {\n  private byte[] buffer = new byte[16\
      \ * 1024 * 1024];\n  // Persists for at least two GC cycles\n}\n```\n\n**不规范的代码样例2**：\n\
      这个不合规的代码示例使用 `System.runFinalizersOnExit()` 方法来模拟垃圾收集运行。请注意，由于线程安全问题，不推荐使用此方法。\n\
      根据Java API [API 2014] 类`System`、`runFinalizersOnExit()` 方法文档，\n> 在退出时启用或禁用终结；这样做指定在\
      \ Java 运行时退出之前运行具有尚未自动调用的终结器的所有对象的终结器。默认情况下，退出时的最终确定是禁用的。\n\n类 `SubClass` 覆盖了\
      \ `protected` `finalize()` 方法并执行清理活动。随后，它调用 `super.finalize()` 以确保其超类也已最终确定。毫无戒心的\
      \ `BaseClass` 调用了 `doLogic()` 方法，该方法恰好在 `SubClass` 中被覆盖。这将恢复对“子类”的引用，这不仅可以防止它被垃圾收集，还可以防止它调用其终结器来关闭可能已由被调用方法分配的新资源。详见\
      \ MET05-J。确保构造函数不调用可覆盖的方法，如果子类的终结器终止了关键资源，则从超类调用其方法可能会导致观察到处于不一致状态的对象。在某些情况下，这可能会导致“NullPointerException”。\n\
      ```java\nclass BaseClass {\n  protected void finalize() throws Throwable {\n\
      \    System.out.println(\"Superclass finalize!\");\n    doLogic();\n  }\n\n\
      \  public void doLogic() throws Throwable {\n    System.out.println(\"This is\
      \ super-class!\");\n  }\n}\n\nclass SubClass extends BaseClass {\n  private\
      \ Date d; // Mutable instance field\n\n  protected SubClass() {\n    d = new\
      \ Date();\n  }\n\n  protected void finalize() throws Throwable {\n    System.out.println(\"\
      Subclass finalize!\");\n    try {\n      //  Cleanup resources\n      d = null;\n\
      \    } finally {\n      super.finalize();  // Call BaseClass's finalizer\n \
      \   }\n  }\n\n  public void doLogic() throws Throwable {\n    // Any resource\
      \ allocations made here will persist\n\n    // Inconsistent object state\n \
      \   System.out.println(\n        \"This is sub-class! The date object is: \"\
      \ + d);\n    // 'd' is already null\n  }\n}\n\npublic class BadUse {\n  public\
      \ static void main(String[] args) {\n    try {\n      BaseClass bc = new SubClass();\n\
      \      // Artificially simulate finalization (do not do this)\n      System.runFinalizersOnExit(true);\n\
      \    } catch (Throwable t) {\n      // Handle error\n    }\n  }\n}\n```\n该代码输出：\n\
      ```\nSubclass finalize!\nSuperclass finalize!\nThis is sub-class! The date object\
      \ is: null\n```"
    Description_En: "The garbage collector invokes object finalizer methods after\
      \ it determines that the object is unreachable but before it reclaims the object's\
      \ storage. Execution of the finalizer provides an opportunity to release resources\
      \ such as open streams, files, and network connections that might not otherwise\
      \ be released automatically through the normal action of the garbage collector.\n\
      \nA sufficient number of problems are associated with finalizers to restrict\
      \ their use to exceptional conditions:\n\n* There is no fixed time at which\
      \ finalizers must be executed because time of execution depends on the Java\
      \ Virtual Machine (JVM). The only guarantee is that any finalizer method that\
      \ executes will do so sometime after the associated object has become unreachable\
      \ (detected during the first cycle of garbage collection) and sometime before\
      \ the garbage collector reclaims the associated object's storage (during the\
      \ garbage collector's second cycle). Execution of an object's finalizer may\
      \ be delayed for an *arbitrarily* long time after the object becomes unreachable.\
      \ Consequently, invoking time-critical functionality such as closing file handles\
      \ in an object's `finalize()` method is problematic.\n* The JVM may terminate\
      \ without invoking the finalizer on some or all unreachable objects. Consequently,\
      \ attempts to update critical persistent state from finalizer methods can fail\
      \ without warning. Similarly, Java lacks any guarantee that finalizers will\
      \ execute on process termination. Methods such as `System.gc()`, `System.runFinalization()`,\
      \ `System.runFinalizersOnExit()`, and `Runtime.runFinalizersOnExit()` either\
      \ lack such guarantees or have been deprecated because of lack of safety and\
      \ potential for deadlock.\n* According to *The Java Language Specification*\
      \ (JLS), §12.6, &#34;Finalization of Class Instances&#34; [JLS 2015]:\n\n  >\
      \ The Java programming language imposes no ordering on `finalize()` method calls.\
      \ Finalizers [of different objects] may be called in any order, or even concurrently.\n\
      \n  One consequence is that slow-running finalizers can delay execution of other\
      \ finalizers in the queue. Further, the lack of guaranteed ordering can lead\
      \ to substantial difficulty in maintaining desired program invariants.\n* Uncaught\
      \ exceptions thrown during finalization are ignored. When an exception thrown\
      \ in a finalizer propagates beyond the `finalize()` method, the process itself\
      \ immediately stops and consequently fails to accomplish its sole purpose. This\
      \ termination of the finalization process may or may not prevent all subsequent\
      \ finalization from executing. The JLS fails to define this behavior, leaving\
      \ it to the individual implementations.\n* Coding errors that result in memory\
      \ leaks can cause objects to incorrectly remain reachable; consequently, their\
      \ finalizers are never invoked.\n* A programmer can unintentionally resurrect\
      \ an object's reference in the `finalize()` method. When this occurs, the garbage\
      \ collector must determine yet again whether the object is free to be deallocated.\
      \ Further, because the `finalize()` method has executed once, the garbage collector\
      \ cannot invoke it a second time.\n* Garbage collection usually depends on memory\
      \ availability and usage rather than on the scarcity of some other particular\
      \ resource. Consequently, when memory is readily available, a scarce resource\
      \ may be exhausted in spite of the presence of a finalizer that could release\
      \ the scarce resource if it were executed. See FIO04-J. Release resources when\
      \ they are no longer needed and TPS00-J. Use thread pools to enable graceful\
      \ degradation of service during traffic bursts for more details on handling\
      \ scarce resources correctly.\n* It is a common myth that finalizers aid garbage\
      \ collection. On the contrary, they increase garbage-collection time and introduce\
      \ space overheads. Finalizers interfere with the operation of modern generational\
      \ garbage collectors by extending the lifetimes of many objects. Incorrectly\
      \ programmed finalizers could also attempt to finalize reachable objects, which\
      \ is always counterproductive and can violate program invariants.\n* Use of\
      \ finalizers can introduce synchronization issues even when the remainder of\
      \ the program is single-threaded. The `finalize()` methods are invoked by the\
      \ garbage collector from one or more threads of its choice; these threads are\
      \ typically distinct from the `main()` thread, although this property is not\
      \ guaranteed. When a finalizer is necessary, any required cleanup data structures\
      \ must be protected from concurrent access. See the JavaOne presentation by\
      \ Hans J. Boehm [Boehm 2005] for additional information.\n* Use of locks or\
      \ other synchronization-based mechanisms within a finalizer can cause deadlock\
      \ or starvation. This possibility arises because neither the invocation order\
      \ nor the specific executing thread or threads for finalizers can be guaranteed\
      \ or controlled.\n\nObject finalizers have also been deprecated since Java 9.\
      \ See MET02-J. Do not use deprecated or obsolete classes or methods for more\
      \ information.\n\nBecause of these problems, finalizers must not be used in\
      \ new classes.\n\n**Noncompliant Code Example (Superclass's finalizer)**\n\n\
      Superclasses that use finalizers impose additional constraints on their extending\
      \ classes. Consider an example from JDK 1.5 and earlier. The following noncompliant\
      \ code example allocates a 16 MB buffer used to back a Swing `JFrame` object.\
      \ Although the `JFrame` APIs lack `finalize()` methods, `JFrame` extends `AWT.Frame`,\
      \ which does have a `finalize()` method. When a `MyFrame` object becomes unreachable,\
      \ the garbage collector cannot reclaim the storage for the byte buffer because\
      \ code in the inherited `finalize()` method might refer to it. Consequently,\
      \ the byte buffer must persist *at least* until the inherited `finalize()` method\
      \ for class `MyFrame` completes its execution and cannot be reclaimed until\
      \ the following garbage-collection cycle.\n\n```java\nclass MyFrame extends\
      \ JFrame {\n  private byte[] buffer = new byte[16 * 1024 * 1024];\n  // Persists\
      \ for at least two GC cycles\n}\n```\n\n**Noncompliant Code Example (`System.runFinalizersOnExit()`)**\n\
      \nThis noncompliant code example uses the `System.runFinalizersOnExit()` method\
      \ to simulate a garbage-collection run. Note that this method is deprecated\
      \ because of thread-safety issues.\n\nAccording to the Java API [API 2014] class\
      \ `System`, `runFinalizersOnExit()` method documentation,\n\n> Enable or disable\
      \ finalization on exit; doing so specifies that the finalizers of all objects\
      \ that have finalizers that have not yet been automatically invoked are to be\
      \ run before the Java runtime exits. By default, finalization on exit is disabled.\n\
      \nThe class `SubClass` overrides the `protected` `finalize()` method and performs\
      \ cleanup activities. Subsequently, it calls `super.finalize()` to make sure\
      \ its superclass is also finalized. The unsuspecting `BaseClass` calls the `doLogic()`\
      \ method, which happens to be overridden in the `SubClass`. This resurrects\
      \ a reference to `SubClass` that not only prevents it from being garbage-collected\
      \ but also prevents it from calling its finalizer to close new resources that\
      \ may have been allocated by the called method. As detailed in MET05-J. Ensure\
      \ that constructors do not call overridable methods, if the subclass's finalizer\
      \ has terminated key resources, invoking its methods from the superclass might\
      \ result in the observation of an object in an inconsistent state. In some cases,\
      \ this can result in `NullPointerException`.\n\n```java\nclass BaseClass {\n\
      \  protected void finalize() throws Throwable {\n    System.out.println(\"Superclass\
      \ finalize!\");\n    doLogic();\n  }\n\n  public void doLogic() throws Throwable\
      \ {\n    System.out.println(\"This is super-class!\");\n  }\n}\n\nclass SubClass\
      \ extends BaseClass {\n  private Date d; // Mutable instance field\n\n  protected\
      \ SubClass() {\n    d = new Date();\n  }\n\n  protected void finalize() throws\
      \ Throwable {\n    System.out.println(\"Subclass finalize!\");\n    try {\n\
      \      //  Cleanup resources\n      d = null;\n    } finally {\n      super.finalize();\
      \  // Call BaseClass's finalizer\n    }\n  }\n\n  public void doLogic() throws\
      \ Throwable {\n    // Any resource allocations made here will persist\n\n  \
      \  // Inconsistent object state\n    System.out.println(\n        \"This is\
      \ sub-class! The date object is: \" + d);\n    // 'd' is already null\n  }\n\
      }\n\npublic class BadUse {\n  public static void main(String[] args) {\n   \
      \ try {\n      BaseClass bc = new SubClass();\n      // Artificially simulate\
      \ finalization (do not do this)\n      System.runFinalizersOnExit(true);\n \
      \   } catch (Throwable t) {\n      // Handle error\n    }\n  }\n}\n```\n\nThis\
      \ code outputs:\n\n```java\nSubclass finalize!\nSuperclass finalize!\nThis is\
      \ sub-class! The date object is: null\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 586:Explicit Call to Finalize()
      - type: CWE
        value: 568:finalize() Method Without super.finalize()
      - type: SJ/T 11683-2017
        value: 6.6.18 调用finalize方法
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140712'
    language: Java
  - Advice: 方法`finalize()`只能在对象回收时由JVM进行调用。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【强制】不要调用finalize()
    Categories_En:
      Category: []
    Default: true
    Description: "`finalize()`方法是回收分配给对象的内存之前调用垃圾收集器线程的基本语句，该方法用于释放对象占用的存储空间，应由JVM进行调用。\
      \  \n不要调用`finalize()`方法，因为：  \n1. `finalize()`方法并不能保证执行。  \n2. 任何有`finalize()`方法抛出的异常都会被GC线程忽略而且不会被进一步传播，事实上也不会在日志文件上记录下来。\
      \  \n3. `finalize()`方法在性能上将会增加负担。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Explicit Call to finalize()
    ENSubClassChin: Poor Style
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 586:Explicit Call to Finalize()
      - type: CERT
        value: MET12-J:Do not use ﬁnalizers
      - type: SJ/T 11683-2017
        value: 6.6.18 调用finalize方法
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150107'
    language: Java
  - Advice: "因为析构函数有太多的问题，所以限制它使用在某些特殊的情况中。   \n同时，必须避免在新的类中使用析构函数。   \n3) 示例   \n\
      \    当一个基类定义了`fmalize()`方法时，要确保可以分离立即回收的对象和依赖于析构函数的对象。这个符合规定的方案确保在对象不可达到时可以马上回收缓冲区。\
      \   \n    ```java\n    class MyFrame {   \n        private JFrame frame;   \n\
      \        private byte[] buffer = new byte[16 * 1024 * 1024]; // now decoupled\
      \   \n    }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.6.13 不要使用析构函数
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n避免不恰当地使用析构函数，防止可回收对象的重新引用。   \n2) 原理   \n垃圾回收器在认为对象是不可达到的时候，在回收对象所占用空间之前会调用对象的终结方法。析构函数的执行提供了一种方式来释放资源，例如流、文件和网络连接。这些资源可能并不会由于垃圾回收器的执行而自动释放。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 586:Explicit Call to Finalize()
      - type: CWE
        value: 568:finalize() Method Without super.finalize()
      - type: CERT
        value: MET12-J:Do not use ﬁnalizers
      - type: SJ/T 11683-2017
        value: 6.6.18 调用finalize方法
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160062
    language: Java
  - Advice: "在比较数据时，如果可能与locale设置相关，则应指定相应的locale。\n**规范的代码样例1**：\n\n```java\npublic\
      \ static void processTag(String tag) {\n    if (tag.toUpperCase(Locale.ENGLISH).equals(\"\
      SCRIPT\")) {\n        return;\n    }\n    // Process tag\n}\n```\n\n代码中将locale设置为英文，从而避免意外的情况。\n\
      **规范的代码样例2**：\n\n```java\npublic static void processTag(String tag) {\n    Locale.setDefault(Locale.ENGLISH);\n\
      \n    if (tag.toUpperCase().equals(\"SCRIPT\")) {\n        return;\n    }\n\
      \    // Process tag\n}\n```\n\n代码中在程序进行字符串比较前，将默认的locale设置为英文。\n**规范的代码样例3**：\n\
      \n```java\npublic static void processTag(String tag) {\n    if (tag.equalsIgnoreCase(\"\
      SCRIPT\")) {\n        return;\n    }\n    // Process tag\n}\n```\n\n这个规范的代码通过不区分大小写的比较绕过locale。`String.equalsIgnoreCase()`方法创建临时规范形式的字符串,它执行适当的比较而不依赖当前locale环境\
      \ [Schindler 12]。\n**规范的代码样例4**：\n\n```java\npublic static void main(String[]\
      \ args) throws IOException {\n    Charset encoding = Charset.forName(\"UTF8\"\
      );\n    BufferedReader reader = new BufferedReader(new InputStreamReader(new\
      \ FileInputStream(inputFile), encoding));\n    PrintWriter writer = new PrintWriter(new\
      \ OutputStreamWriter(new FileOutputStream(outputFile), encoding));\n\n    int\
      \ line = 0;\n\n    /* Rest of code unchanged */\n```\n\n在这个规范的代码中，输入和输出文件都使用UTF8显式地编码。不管默认的编码是什么，这个程序都是正确的。\n\
      **规范的代码样例5**：\n\n```java\nString myString = DateFormat.getDateInstance(DateFormat.MEDIUM,\
      \ Locale.US).format(rightNow.getTime());\n/* Rest of code unchanged */\n```\n\
      \n这个规范的代码规定日期以一种英语格式打印，不管当前的语言环境。\n**规范的代码样例6**：\n\n```java\nif (rightNow.get(Calendar.MONTH)\
      \ == Calendar.JUNE) {\n/* Rest of code unchanged */\n```\n\n这个规范的代码检查日期的月份属性，而不格式化它。尽管日期表示因文化而异，但\
      \ Calendar 日期的内容却不一样。因此，该代码在任何地区都可以工作。"
    Advice_En: "**Compliant Solution (Explicit Locale)**\n\nThis compliant solution\
      \ explicitly sets the locale to English to avoid unexpected results:\n\n```java\n\
      public static void processTag(String tag) {\n  if (tag.toUpperCase(Locale.ENGLISH).equals(\"\
      SCRIPT\")) {\n    return;\n  }\n  // Process tag\n}\n```\n\nSpecifying `Locale.ROOT`\
      \ is a suitable alternative when an English-specific locale would not be appropriate.\n\
      \n**Compliant Solution (Default Locale)**\n\nThis compliant solution sets the\
      \ default locale to English before performing string comparisons:\n\n```java\n\
      public static void processTag(String tag) {\n  Locale.setDefault(Locale.ENGLISH);\n\
      \n  if (tag.toUpperCase().equals(\"SCRIPT\")) {\n    return;\n  }\n  // Process\
      \ tag\n}\n```\n\n**Compliant Solution (`String.equalsIgnoreCase()`)**\n\nThis\
      \ compliant solution bypasses locales entirely by performing a case-insensitive\
      \ match. The `String.equalsIgnoreCase()` method creates temporary canonical\
      \ forms of both strings, which may render them unreadable, but it performs proper\
      \ comparison without making them dependent on the current locale [Schindler\
      \ 12].\n\n```java\npublic static void processTag(String tag) {\n  if (tag.equalsIgnoreCase(\"\
      SCRIPT\")) {\n    return;\n  }\n  // Process tag\n}\n```\n\nThis solution is\
      \ compliant because `equalIgnoreCase()` compares two strings, one of which is\
      \ plain ASCII, and therefore its behavior is well-understood, even if the other\
      \ string is not plain ASCII. Calling `equalIgnoreCase()` where both strings\
      \ may not be ASCII is not recommended, simply because `equalIgnoreCase()` may\
      \ not behave as expected by the developer.\n\n**Compliant Solution (`Charset`)**\n\
      \nIn this compliant solution, both the input and output files are explicitly\
      \ encoded using UTF8. This program behaves correctly regardless of the default\
      \ encoding.\n\n```java\npublic static void main(String[] args) throws IOException\
      \ {\n  Charset encoding = Charset.forName(\"UTF8\");\n  BufferedReader reader\
      \ = new BufferedReader(new InputStreamReader(new FileInputStream(inputFile),\
      \ encoding));\n  PrintWriter writer = new PrintWriter(new OutputStreamWriter(new\
      \ FileOutputStream(outputFile), encoding));\n\n  int line = 0;\n\n  /* Rest\
      \ of code unchanged */\n```\nCompliant Solution (Explicit Locale)\nThis compliant\
      \ solution forces the date to be printed in an English format, regardless of\
      \ the current locale:\n\n```java\nString myString = DateFormat.getDateInstance(DateFormat.MEDIUM,\
      \ Locale.US).format(rightNow.getTime());\n/* Rest of code unchanged */\n```\n\
      \n**Compliant Solution (Bypass Locale)**\n\nThis compliant solution checks the\
      \ date's `MONTH` attribute without formatting it. Although date representations\
      \ vary by culture, the contents of a `Calendar` date do not. Consequently, this\
      \ code works in any locale.\n\n```java\nif (rightNow.get(Calendar.MONTH) ==\
      \ Calendar.JUNE) {\n/* Rest of code unchanged */\n```"
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 04. 字符和字符串 (STR)
      - type: DetailClassChin
        value: STR02-J：当比较local相关的数据时，指定恰当的local
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 04. Characters and Strings (STR)
      - type: DetailClassChin
        value: STR02-J:Specify an appropriate locale when comparing locale-dependent
          data
    Default: not
    Description: "当locale没有明确指定的时候，使用locale相关的方法处理与local相关的数据会产生意想不到的结果。编程语言标识符、协议关键字以及HTML标签通常会指定Locale.ENGLISH作为一个特定的locale。在不同的locale环境中运行程序可能会导致意外的程序行为，甚至允许攻击者绕过输入过滤器。由于这些原因，在比较数据时，如果可能与locale方法相关，则应指定相应的locale。\
      \  \n  \n例如下面程序代码：  \n```java\npublic class Example {\n  public static void\
      \ main(String[] args) {\n    System.out.println(\"Title\".toUpperCase());\n\
      \  }\n}\n```\n在英文locale环境下： \n```\n% java Example\nTITLE\n%\n```\n然而，大多数语言使用拉丁字母的i的大写形式是I,但土耳其语言环境是个**例外**：有一个点的i大写形式也有一个点\
      \ (İ)， 没有点的ı大写形式没有点 (I)。在土耳其locale（API 2006）会产生下面预期之外的结果：  \n```\n% java -Duser.language=tr\
      \ Example\nTİTLE\n%\n```\n许多程序只使用依赖于locale方法来输出信息，如果locale相关的数据，程序没有显示设置locale,则可以安全地依赖于默认的locale设置。\n\
      \n**不规范的代码样例1（`toUpperCase()`）**：\n许多网络应用程序，例如论坛或博客软件，输入 `HTML` 然后显示。显示不受信任的\
      \ `HTML `可能会使 Web 应用程序遭受跨站脚本 (XSS) 或 `HTML `注入攻击。因此，在将 `HTML `发送到 Web 浏览器之前对其进行净化至关重要。\n\
      \n净化的一个常见步骤是识别可能包含恶意内容的标签。 `<SCRIPT>` 标记通常包含由客户端浏览器执行的 `JavaScript `代码。因此，`HTML\
      \ `输入通常会针对 `<SCRIPT>` 标签进行过滤。然而，识别 `<SCRIPT>` 标签并不像看起来那么简单。\n\n在 `HTML `中，标签不区分大小写，因此可以使用大写、小写或任何大小写混合来指定。这个不规范的代码示例使用依赖于语言环境的\
      \ `String.toUpperCase()` 方法将 `HTML `标记转换为大写，以检查它以进行进一步处理。代码必须忽略 `<SCRIPT>` 标记，因为它们指示要丢弃的代码。英语语言环境会将\
      \ `\"script\"` 转换为 `\"SCRIPT\"`，而土耳其语语言环境会将 `<span>\"script\"</span>` 转换为 `\"\
      SCRİPT\"`，检查将无法检测到`<SCRIPT>` 标签。\n\n```java\npublic static void processTag(String\
      \ tag) {\n  if (tag.toUpperCase().equals(\"SCRIPT\")) {\n    return;\n  }\n\
      \  // Process tag\n}\n```\n**不规范的代码样例2（`FileReader`）**：\nJava提供用于处理输入和输出的类，这些类可以基于字节或字符。字节I/O系列源自InputStream和OutputStream接口，与语言环境或字符编码无关。然而，字符I/O系列源于读写器，它们必须将字节序列转换为字符串并返回，因此它们依赖于指定的字符编码来进行转换。此编码由文件指示。编码系统属性，它是当前区域设置的一部分。因此，使用一种编码（如UTF-8）编码的文件不能由使用不同编码（如UTF-16）的字符输入方法读取。\n\
      读取字符数据的程序（无论是直接使用读取器还是间接使用某种方法，例如从字节数组构造字符串）必须知道数据的来源。如果数据的编码是固定的（例如，如果数据来自程序附带的文件资源），则该编码必须由程序指定。未能指定编码会使攻击者更改编码，迫使程序使用错误的编码读取数据。\n\
      这种风险不适用于读取已知数据的程序，这些数据采用运行该程序的平台指定的编码。例如，如果程序必须打开用户提供的文件，那么依赖默认编码是合理的，因为它将被正确设置。\n\
      这个不一致的代码示例读取自己的源代码并打印出来，在每行前面加上行号。如果程序使用参数-Dfile运行。encoding=UTF16当其源文件存储为UTF8时，程序将在输出文件中保存垃圾。\n\
      ```java\nimport java.io.*;\n\npublic class PrintMyself {\n  private static String\
      \ inputFile = \"PrintMyself.java\";\n  private static String outputFile = \"\
      PrintMyself.txt\";\n\n  public static void main(String[] args) throws IOException\
      \ {\n    BufferedReader reader = new BufferedReader(new FileReader(inputFile));\n\
      \    PrintWriter writer = new PrintWriter(new FileWriter(outputFile));\n   \
      \ int line = 0;\n    while (reader.ready()) {\n      line++;\n      writer.println(line\
      \ + \": \" + reader.readLine());\n    }\n    reader.close();\n    writer.close();\n\
      \  }\n}\n```\n**不规范的代码样例3（`Date`）**：\n日和年的概念是普遍存在的，但日期的表示方式因文化而异，因此特定于地区。此不合规代码示例检查当前日期，并根据月份是否为6月打印两条消息中的一条：\n\
      ```java\nimport java.util.Date;\nimport java.text.DateFormat;\nimport java.util.Locale;\n\
      \n// ...\n\npublic static void isJune(Date date) {\n  String myString = DateFormat.getDateInstance().format(date);\n\
      \  System.out.println(\"The date is \" + myString);\n  if (myString.startsWith(\"\
      Jun \")) {\n    System.out.println(\"Enjoy June!\");\n  } else {\n    System.out.println(\"\
      It's not June.\");\n  }\n}\n```\n该程序在美国语言环境的平台上的表现与预期一样:\n```\nThe date is Jun\
      \ 20, 2014\nEnjoy June!\n```\n但是在其他地区却失败了。例如：德国地区的输出(指定-Duser.language = de)\
      \  \n```\nThe date is 20.06.2014\nIt's not June.\n```"
    Description_En: "Using locale-dependent methods on locale-dependent data can produce\
      \ unexpected results when the locale is unspecified. Programming language identifiers,\
      \ protocol keys, and HTML tags are often specified in a particular locale, usually\
      \ `Locale.ENGLISH`. Running a program in a different locale may result in unexpected\
      \ program behavior or even allow an attacker to bypass input filters. For these\
      \ reasons, any program that inspects data generated by a locale-dependent function\
      \ must specify the locale used to generate that data.\n\nFor example, the following\
      \ program:\n\n```java\npublic class Example {\n  public static void main(String[]\
      \ args) {\n    System.out.println(\"Title\".toUpperCase());\n  }\n}\n```\n\n\
      behaves as expected in an English locale:\n\n```java\n% java Example\nTITLE\n\
      %\n```\n\nHowever, most languages that use the Latin alphabet associate the\
      \ letter `I` as the uppercase version of `i`. But Turkish is an exception: it\
      \ has a dotted `i` whose uppercase version is also dotted (`İ`) and an undotted\
      \ `ı` whose uppercase version is undotted (`I`). Changing capitalization on\
      \ most strings in the Turkish locale [API 2006] may produce unexpected results:\n\
      \n```java\n% java -Duser.language=tr Example\nTİTLE\n%\n```\n\nMany programs\
      \ only use locale-dependent methods for outputting information, such as dates\
      \ provided that the locale-dependent data is not inspected by the program, and\
      \ it may safely rely on the default locale.\n\n**Noncompliant Code Example (`toUpperCase()`)**\n\
      \nMany web apps, such as forum or blogging software, input HTML and then display\
      \ it. Displaying untrusted HTML can subject a web app to cross-site scripting\
      \ (XSS) or HTML injection vulnerabilities. Therefore, it is vital that HTML\
      \ be sanitized before sending it to a web browser.\n\nOne common step in sanitization\
      \ is identifying tags that may contain malicious content. The `<SCRIPT>` tag\
      \ typically contains JavaScript code that is executed by a client's browser.\
      \ Consequently, HTML input is commonly filtered for `<SCRIPT>` tags. However,\
      \ identifying `<SCRIPT>` tags is not as simple as it appears.\n\nIn HTML, tags\
      \ are case-insensitive and consequently can be specified using uppercase, lowercase,\
      \ or any mixture of cases. This noncompliant code example uses the locale-dependent\
      \ `String.toUpperCase()` method to convert an HTML tag to uppercase to check\
      \ it for further processing. The code must ignore `<SCRIPT>` tags, as they indicate\
      \ code that is to be discarded. Whereas the English locale would convert `\"\
      script\"` to `\"SCRIPT\"`, the Turkish locale will convert `<span>\"script\"\
      </span>` to `\"SCRİPT\"`, and the check will fail to detect the `<SCRIPT>` tag.\n\
      \n```java\npublic static void processTag(String tag) {\n  if (tag.toUpperCase().equals(\"\
      SCRIPT\")) {\n    return;\n  }\n  // Process tag\n}\n```\n\n**Noncompliant Code\
      \ Example (`FileReader`)**\n\nJava provides classes for handling input and output,\
      \ which can be based on either bytes or characters. The byte I/O families derive\
      \ from the `InputStream` and `OutputStream` interfaces and are independent of\
      \ locale or character encoding. However, the character I/O families derive from\
      \ `Reader` and `Writer`, and they must convert byte sequences into strings and\
      \ back, so they rely on a specified character encoding to do their conversion.\
      \ This encoding is indicated by the `file.encoding` system property, which is\
      \ part of the current locale. Consequently, a file encoded with one encoding,\
      \ such as UTF-8, must not be read by a character input method using a different\
      \ encoding, such as UTF-16.\n\nPrograms that read character data (whether directly\
      \ using a `Reader` or indirectly using some method such as constructing a `String`\
      \ from a byte array) must be aware of the source of the data. If the encoding\
      \ of the data is fixed (such as if the data comes from a file resource that\
      \ is shipped with the program), then that encoding must be specified by the\
      \ program. Failure to specify the coding enables an attacker to change the encoding\
      \ to force the program to read the data using the wrong encoding.\n\nThis risk\
      \ does not apply to programs that read data known to be in the encoding specified\
      \ by the platform running the program. For example, if the program must open\
      \ a file provided by the user, it is reasonable to rely on the default encoding,\
      \ expecting that it will be set correctly.\n\nThis noncompliant code example\
      \ reads its own source code and prints it out, prepending each line with a line\
      \ number. If the program is run with the argument `-Dfile.encoding=UTF16` while\
      \ its source file is stored as `UTF8`, the program will save garbage in the\
      \ output file.\n\n```java\nimport java.io.*;\n\npublic class PrintMyself {\n\
      \  private static String inputFile = \"PrintMyself.java\";\n  private static\
      \ String outputFile = \"PrintMyself.txt\";\n\n  public static void main(String[]\
      \ args) throws IOException {\n    BufferedReader reader = new BufferedReader(new\
      \ FileReader(inputFile));\n    PrintWriter writer = new PrintWriter(new FileWriter(outputFile));\n\
      \    int line = 0;\n    while (reader.ready()) {\n      line++;\n      writer.println(line\
      \ + \": \" + reader.readLine());\n    }\n    reader.close();\n    writer.close();\n\
      \  }\n}\n```\n\n**Noncompliant Code Example (`Date`)**\n\nThe concepts of days\
      \ and years are universal, but the way in which dates are represented varies\
      \ across cultures and are therefore specific to locales. This noncompliant code\
      \ example examines the current date and prints one of two messages, depending\
      \ on whether or not the month is June:\n\n```java\nimport java.util.Date;\n\
      import java.text.DateFormat;\nimport java.util.Locale;\n\n// ...\n\npublic static\
      \ void isJune(Date date) {\n  String myString = DateFormat.getDateInstance().format(date);\n\
      \  System.out.println(\"The date is \" + myString);\n  if (myString.startsWith(\"\
      Jun \")) {\n    System.out.println(\"Enjoy June!\");\n  } else {\n    System.out.println(\"\
      It's not June.\");\n  }\n}\n```\n\nThis program behaves as expected on platforms\
      \ with a US locale:\n\n```java\nThe date is Jun 20, 2014\nEnjoy June!\n```\n\
      \nbut fails on other locales. For example, the output for a German locale (specified\
      \ by `-Duser.language=de`) is\n\n```java\nThe date is 20.06.2014\nIt's not June.\n\
      ```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 474:Use of Function with Inconsistent Implementations
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010142002'
    language: Java
  - Advice: "对应的符合规则的方案显式地将 locale设置为英文，从而避免产生意想不到的问题。   \n    `\"title\".toUpperCase(Locale.ENGLISH);`\
      \   \n    这条规则同样适用于 `String.equalsIgnoreCase()`方法。   \n    另一个符合规则的方案在对字符串数据进行处理之前，把默认的\
      \ locale设置为 English。\n    ```java\n    Locale.setDefault(Locale.ENGLISH);  \
      \ \n    \"title\".toUpperCase();\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 5.2 数据净化
      - type: DetailClassChin
        value: C.1.10 如果没有指定适当的locale，不要使用locale相关方法处理与locale相关的数据
    Categories_En:
      Category: []
    Default: not
    Description: "1) 规定   \n任何一个程序调用 locale 相关方法时，如果使用非受信数据，必须显式指明使用这些方法的locale。 \
      \  \n2) 原理   \n当 locale 没有明确指定的时候，使用 locale相关的方法处理与 locale相关的数据会产生意想不到的后果。编程语言的标示符、协议关键字以及\
      \ HTML标签通常会指定 Locale.ENGLISH作为一个特定的locale。当改变默认的 locale的时候，很有可能使数据绕过检查，从而改变\
      \ locale相关方法的行为。比如，当把一个字符串转换为大写字符时，可认为它是合法的，然而，当把它接着转换回小写字符时，可能就会产生黑名单字符串。 \
      \  \n3) 示例   \n    这个不符合规则的代码示例中使用了 locale 相关的 `String.toUpperCase()` 方法来将 HTML\
      \ 标签转换为大写字符。在英文 locale 中，会将`“title”` 转换为`“TITLE”`，在土耳其 locale 中，会将`“title”`\
      \ 转换为`“T?TLE”`，其中的`“?”`是拉丁文字母`“I”`，它在字符上是有一个点的。   \n    `\"title\".toUpperCase();`\
      \   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 474:Use of Function with Inconsistent Implementations
      - type: CERT
        value: STR02-J:Specify an appropriate locale when comparing locale-dependent
          data
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160010
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。   \n**示例2**：  \n```java\npublic static void processTag(String\
      \ tag) {\n    if (tag.toUpperCase(Locale.ENGLISH).equals(\"SCRIPT\")) {\n  \
      \      return;\n    }\n    // Process tag\n}\n```  \n代码中将locale设置为英文，从而避免意外的情况。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.1 数据清洗
      - type: DetailClassChin
        value: A.2.1.1.8 当比较local相关的数据时，指定恰当的local
    Categories_En:
      Category: []
    Default: not
    Description: "规范说明：当locale没有明确指定的时候，使用locale相关的方法处理与local相关的数据会产生意想不到的结果。编程语言标识符、协议关键字以及HTML标签通常会指定Locale.ENGLISH作为一个特定的locale。在不同的locale环境中运行程序可能会导致意外的程序行为，甚至允许攻击者绕过输入过滤器。由于这些原因，在比较数据时，如果可能与locale方法相关，则应指定相应的locale。\
      \  \n  \n例如下面程序代码：  \n```java\npublic class Example {  \n    public static void\
      \ main(String[] args) {  \n        System.out.println(\"Title\".toUpperCase());\
      \  \n    }  \n}\n```  \n在英文locale环境下：  \n`TITLE`  \n然而，大多数语言使用的拉丁字母i的大写形式是I,但土耳其语言环境是个例外：有一个点的i的大写形式也有一个点\
      \ `(İ)`， 没有点的ı大写形式没有点 `(I)`。在土耳其locale（API 2006）会产生下面预期之外的结果：  \n`TİTLE`   \n\
      许多程序只使用依赖于locale方法来输出信息，如果locale相关的数据，程序没有显示设置locale,则可以安全地依赖于默认的locale设置。 \
      \ \n对于指定恰当的local的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：      \n```java\npublic\
      \ static void processTag(String tag) {\n    if (tag.toUpperCase().equals(\"\
      SCRIPT\")) {\n        return;\n    }\n    // Process tag\n}\n```  \n在英语locale中\
      \ `\"script\"` 大写转换成 `\"SCRIPT\"`，而在土耳其locale环境中， 将`\"script\"`大写转换成`\"SCRİPT\"\
      `。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 474:Use of Function with Inconsistent Implementations
      - type: CERT
        value: STR02-J:Specify an appropriate locale when comparing locale-dependent
          data
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170007'
    language: Java
  - Advice: "使用字符串表达式来代替`new String()`或`new String(String expression)`。  \n  \n**例如**：下面代码示例中，直接使用字符串来为String\
      \ 赋值。  \n```java\nString string= \"string\";\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要创建String对象
    Categories_En:
      Category: []
    Default: true
    Description: "程序中采用`new String()`或`new String(String expression)`创建对象，该方法产生的额外对象将占用更多空间、降低性能。\
      \  \n  \n**例如**：下面代码示例中，使用了`new String(String expression)`来创建对象。 \n```java\n\
      String string= new String(\"string\");\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: SJ/T 11683-2017
        value: 6.6.20 不必要的对象创建
    Security: not
    Tag: performance,memory,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150420'
    language: Java
  - Advice: "**规范的代码样例1**：  \n```java\npublic class CountBoxes implements Runnable\
      \ {\n    private static int counter;\n    // . . .\n    private static final\
      \ Object lock = new Object();\n\n    public void run() {\n        synchronized\
      \ (lock) {\n            counter++;\n            // . . .\n        }\n    }\n\
      \    // . . .\n}\n```  \n代码通过静态对象实现的锁来保证递增操作的原子性。"
    Advice_En: "**Compliant Solution (Static Lock Object)**\n\nThis compliant solution\
      \ ensures the atomicity of the increment operation by locking on a static object:\n\
      \n```java\npublic class CountBoxes implements Runnable {\n  private static int\
      \ counter;\n  // ...\n  private static final Object lock = new Object();\n\n\
      \  public void run() {\n    synchronized (lock) {\n      counter++;\n      //\
      \ ...\n    }\n  }\n  // ...\n}\n```\nIt is unnecessary to declare the counter\
      \ variable volatile when using synchronization."
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 09. 锁 (LCK)
      - type: DetailClassChin
        value: LCK06-J：不要使用实例锁来保护共享静态数据
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 09. Locking (LCK)
      - type: DetailClassChin
        value: LCK06-J:Do not use an instance lock to protect shared static data
    Default: true
    Description: "程序不应该使用实例锁来保护共享静态数据，原因在于实例锁在两个或者多个实例的情况下是无效的。因此，如果不使用一个静态的锁对象，会导致共享状态在并发进入时是不被保护的。与非受信代码交换的类的锁对象必须是private\
      \ final的。  \n**不规范的代码样例1**：  \n```java\npublic final class CountBoxes implements\
      \ Runnable {\n    private static volatile int counter;\n    // . . .\n    private\
      \ final Object lock = new Object();\n\n    @Override\n    public void run()\
      \ {\n        synchronized (lock) {\n            counter++;\n            // .\
      \ . .\n        }\n    }\n\n    public static void main(String[] args) {\n  \
      \      for (int i = 0; i < 2; i++) {\n            new Thread(new CountBoxes()).start();\n\
      \        }\n    }\n}\n```  \n代码通过一个非静态的锁对象来保护静态`counter`字段的进入。当启动两个Runnable任务时，它们创建了两个锁对象实例，并且分别基于每一个实例进行锁定。然而，这个代码不能防止线程得到一个不一致的`counter`的数值，这是因为`volatile`字段的递增操作不是原子性的。\
      \  \n**不规范的代码样例2**：  \n```java\npublic final class CountBoxes implements Runnable\
      \ {\n    private static volatile int counter;\n\n    // . . .\n    public synchronized\
      \ void run() {\n        counter++;\n        // . . .\n    }\n    // . . .\n\
      }\n```  \n代码使用了内置锁，这个内置锁是与每一个类实例相关联的，而不是与类本身关联的内置锁。因此，使用不同的Runnable实例创建出来的线程，可能会观察到不一致的`counter`数值。"
    Description_En: "Programs must not use instance locks to protect static shared\
      \ data because instance locks are ineffective when two or more instances of\
      \ the class are created. Consequently, failure to use a static lock object leaves\
      \ the shared state unprotected against concurrent access. Lock objects for classes\
      \ that can interact with untrusted code must also be private and final, as shown\
      \ in LCK00-J. Use private final lock objects to synchronize classes that may\
      \ interact with untrusted code.\n**Noncompliant Code Example (Nonstatic Lock\
      \ Object for Static Data)**\n\nThis noncompliant code example attempts to guard\
      \ access to the static `counter` field using a nonstatic lock object. When two\
      \ `Runnable` tasks are started, they create two instances of the lock object\
      \ and lock on each instance separately.\n\n```java\npublic final class CountBoxes\
      \ implements Runnable {\n  private static volatile int counter;\n  // ...\n\
      \  private final Object lock = new Object();\n\n  @Override public void run()\
      \ {\n    synchronized (lock) {\n      counter++;\n      // ...\n    }\n  }\n\
      \n  public static void main(String[] args) {\n    for (int i = 0; i < 2; i++)\
      \ {\n    new Thread(new CountBoxes()).start();\n    }\n  }\n}\n```\nThis example\
      \ fails to prevent either thread from observing an inconsistent value of counter\
      \ because the increment operation on volatile fields fails to be atomic in the\
      \ absence of proper synchronization (see VNA02-J. Ensure that compound operations\
      \ on shared variables are atomic for more information).\n**Noncompliant Code\
      \ Example (Method Synchronization for Static Data)**\n\nThis noncompliant code\
      \ example uses method synchronization to protect access to a static class `counter`\
      \ field:\n\n```java\npublic final class CountBoxes implements Runnable {\n \
      \ private static volatile int counter;\n  // ...\n\n  public synchronized void\
      \ run() {\n    counter++;\n    // ...\n  }\n  // ...\n}\n```\n\nIn this case,\
      \ the method synchronization uses the intrinsic lock associated with each instance\
      \ of the class rather than the intrinsic lock associated with the class itself.\
      \ Consequently, threads constructed using different `Runnable` instances may\
      \ observe inconsistent values of `counter`."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 667:Improper Locking
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141006'
    language: Java
  - Advice: "对应的符合规则的解决方案是：通过基于静态对象实现的锁来保证递增操作的原子性。   \n    ```java\n    public class\
      \ CountBoxes implements Runnable {\n        private static int counter;\n  \
      \      // . . .\n        private static final Object lock = new Object();\n\
      \    \n        public void run() {\n            synchronized (lock) {\n    \
      \            counter++;\n                // . . .\n            }\n        }\n\
      \    ```  \n    当使用同步的时候，没有必要声明`counter`变量是 volatile 的。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.9.7 不要使用一个实例锁来保护共享静态数据
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序不应使用实例锁来保护静态共享数据   \n2) 原理   \n程序不应使用实例锁来保护静态共享数据，原因在于实例锁在两个或者多个实例的情况下是无效的。因此，如果不使用一个静态的锁对象，会导致共享状态在并发进入时是不被保护的。与非受信代码交互的类的锁对象必须同样是私有和\
      \ final 的。   \n3) 示例   \n    1、不符合规则的代码示例（静态数据的非静态锁对象），该示例想要通过使用一个非静态的锁对象来保护静态`counter`字段的进入。当启动两个\
      \ Runnable 任务时，它们创建了两个锁对象的实例，并且分别基于每一个实例进行锁定。   \n    ```java\n    public final\
      \ class CountBoxes implements Runnable {\n        private static volatile int\
      \ counter;\n        // . . .\n        private final Object lock = new Object();\n\
      \    \n        @Override\n        public void run() {\n            synchronized\
      \ (lock) {\n                counter++;\n                // . . .\n         \
      \   }\n        }\n    \n        public static void main(String[] args) {\n \
      \           for (int i = 0; i < 2; i++) {\n                new Thread(new CountBoxes()).start();\n\
      \            }\n    ```   \n    代码不能防止线程得到一个不一致的`counter`的数值，因为 volatile 字段的递增操作不是原子性的。\
      \   \n       \n    2、不符合规则的代码示例（基于静态数据的方法同步），该示例使用方法同步来保护静态类字段`counter`的访问。\
      \   \n    ```java\n    public final class CountBoxes implements Runnable {\n\
      \        private static volatile int counter;\n    \n        // . . .\n    \
      \    public synchronized void run() {\n            counter++;\n            //\
      \ . . .\n        }\n        // . . .\n    }\n    ```  \n    本示例中，方法同步使用了内置锁，这个内置锁是与每一个类实例相关联的，而不是与\
      \ 类本身相关联的内置锁。因此，使用不同的 Runnable 实例创建出来的线程，可能会观察到不一致的`counter`数值。   \n       \n\
      \    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 667:Improper Locking
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160085
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\npublic class CountBoxes\
      \ implements Runnable {  \n    private static int counter;\n    // . . .\n \
      \   private static final Object lock = new Object();\n\n    public void run()\
      \ {\n        synchronized (lock) {\n            counter++;\n            // .\
      \ . .\n        }\n    }\n    // . . .\n}\n```  \n代码通过静态对象实现的锁来保证递增操作的原子性。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.7 不要使用实例锁来保护共享静态数据
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：程序不应该使用实例锁来保护共享静态数据，原因在于实例锁在两个或者多个实例的情况下是无效的。因此，如果不使用一个静态的锁对象，会导致共享状态在并发进入时是不被保护的。\
      \  \n对于不要使用实例锁来保护共享静态数据的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\npublic\
      \ final class CountBoxes implements Runnable {  \n    private static volatile\
      \ int counter;\n    // . . .\n    private final Object lock = new Object();\n\
      \n    @Override\n    public void run() {\n        synchronized (lock) {\n  \
      \          counter++;\n            // . . .\n        }\n    }\n\n    public\
      \ static void main(String[] args) {\n        for (int i = 0; i < 2; i++) {\n\
      \            new Thread(new CountBoxes()).start();\n        }\n    } \n}\n```\
      \  \n代码通过一个非静态的锁对象来保护静态`counter`字段的进入。当启动两个Runnable任务时，它们创建了两个锁对象实例，并且分别基于每一个实例进行锁定。然而，这个代码不能防止线程得到一个不一致的`counter`的数值，这是因为volatile字段的递增操作不是原子性的。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 667:Improper Locking
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: 0302000010170039
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nstatic final int MAX =\
      \ 0x3200000; // 50MB\n// ...\n// write the files to the disk, but only if file\
      \ is not insanely big\nif (file.length > MAX) {\n    throw new IllegalStateException(\"\
      File is huge.\");\n}\nFileOutputStream fos = new FileOutputStream(file_name);\n\
      bop = new BufferedOutputStream(fos, SIZE);\nwhile ((count = fileInputStream.read(bytes))\
      \ != -1) {\n    bop.write(bytes, 0, count);\n}\n```  \n上述代码片段中，对文件大小进行检验，并不设置读取长度。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 7.2 内存管理
      - type: DetailClassChin
        value: A.4.2.2 避免拒绝服务攻击
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：拒绝服务是攻击者通过极度消耗应用资源，以致程序崩溃或其他合法用户无法进行使用的一种攻击方式。  \n对于避免拒绝服务攻击的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**：   \n```java\nFileInputStream fileInputStream = new FileInputStream(file);\
      \  \nfileInputStream.read(bytes, 0, length);  \n...\n```  \n上述代码片段中，攻击者控制了输入流的长度length，当文件足够大和length足够长时，会导致DOS攻击。拒绝服务攻击是一种滥用资源性的攻击。从程序源代码角度讲，对涉及到系统资源的外部数据应该进行严格校验，防止无限制的输入。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 730:OWASP Top Ten 2004 Category A9 - Denial of Service
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170062'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n```java\nSecretKeySpec secretKeySpec\
      \ = new SecretKeySpec(key.getBytes(\"UTF-8\"), \"AES\");  \naesCipher.init(Cipher.DECRYPT_MODE,\
      \ secretKeySpec);  \nString decryptedPassword = new String(aesCipher.doFinal(password.getBytes(\"\
      UTF-8\")), \"UTF-8\");  \ndBConnection = DriverManager.getConnection(url, name,\
      \ decryptedPassword);\n```  \n上述代码片段中，获取connection时使用已经加密的密码。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.3 访问控制
      - type: DetailClassChin
        value: A.2.3.2.4 避免使用明文密码
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：程序中使用了来自文件或者网络的未进行加密的明文密码。  \n对于避免使用明文密码的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**：   \n```java\nString password = request.getParamter(\"password\"\
      );  \ndBConnection = DriverManager.getConnection(url, name, password);\n```\
      \  \n上述代码片段中，使用来自网络的明文密码用于获取connection。  \n明文密码会降低系统安全性，应对程序中使用的密码值进行加密。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 256:Plaintext Storage of a Password
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Insufficiently Protected Credentials
          [XYM]
      - type: GB/T 34944-2017 Java
        value: 安全功能:明文存储口令
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170024'
    language: Java
  - Advice: "**规范的代码样例1**：\n```java\npublic final void readSensitiveFile() {\n   \
      \ try {\n        SecurityManager sm = System.getSecurityManager();\n       \
      \ if (sm != null) { // Check for permission to read file\n            sm.checkRead(\"\
      /temp/tempFile\");\n        }\n        // Access the file\n    } catch (SecurityException\
      \ se) {\n        // Log exception\n    }\n}\n```  \n这个符合规范的方案将`readSensitiveFile()`声明为final的，从而避免了覆写。\
      \  \n**规范的代码样例2**：\n```java\nprivate void readSensitiveFile() {\n    try {\n\
      \        SecurityManager sm = System.getSecurityManager();\n        if (sm !=\
      \ null) { // Check for permission to read file\n            sm.checkRead(\"\
      /temp/tempFile\");\n        }\n        // Access the file\n    } catch (SecurityException\
      \ se) {\n        // Log exception\n    }\n}\n```  \n这个符合规范的方案将`readSensitiveFile()`声明为private的，从而避免了覆写。\
      \  \n**特例**  \nMET03-EX0:声明为final的类不受这条规则的限制，这是因为它们的成员方法是不能被覆写的。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution prevents overriding\
      \ of the `readSensitiveFile()` method by declaring it final:\n\n```java\npublic\
      \ final void readSensitiveFile() {\n  try {\n    SecurityManager sm = System.getSecurityManager();\n\
      \    if (sm != null) {  // Check for permission to read file\n      sm.checkRead(\"\
      /temp/tempFile\");\n    }\n    // Access the file\n  } catch (SecurityException\
      \ se) {\n    // Log exception\n  }\n}\n```\n\n**Compliant Solution**\n\nThis\
      \ compliant solution prevents overriding of the `readSensitiveFile()` method\
      \ by declaring it private:\n\n```java\nprivate void readSensitiveFile() {\n\
      \  try {\n    SecurityManager sm = System.getSecurityManager();\n    if (sm\
      \ != null) {  // Check for permission to read file\n      sm.checkRead(\"/temp/tempFile\"\
      );\n    }\n    // Access the file\n  } catch (SecurityException se) {\n    //\
      \ Log exception\n  }\n}\n```\n\n**Exceptions**\n\nMET03-J-EX0: Classes that\
      \ are declared final are exempt from this rule because their member methods\
      \ cannot be overridden."
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 06. 方法 (MET)
      - type: DetailClassChin
        value: MET03-J：进行安全检测的方法必须声明为private或final
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 06. Methods (MET)
      - type: DetailClassChin
        value: MET03-J:Methods that perform a security check must be declared private
          or final
    Default: true
    Description: "恶意子类可以覆写进行安全检测的Nonfinal类的成员方法，并忽略这些检测。从而使安全检测不起作用。因此，这些方法必须声明为private或final来避免覆写。\
      \  \n**不规范的代码样例1**：  \n```java\npublic void readSensitiveFile() {\n  try {\n\
      \    SecurityManager sm = System.getSecurityManager();\n    if (sm != null)\
      \ {  // Check for permission to read file\n      sm.checkRead(\"/temp/tempFile\"\
      );\n    }\n    // Access the file\n  } catch (SecurityException se) {\n    //\
      \ Log exception\n  }\n}\n```\n这个不符合规范的代码示例允许子类覆写方法`readSensitiveFile()`，并省略了所需的安全检测。"
    Description_En: "Nonfinal member methods that perform security checks can be compromised\
      \ when a malicious subclass overrides the methods and omits the checks. Consequently,\
      \ such methods must be declared private or final to prevent overriding.\n\n\
      **Noncompliant Code Example**\n\nThis noncompliant code example allows a subclass\
      \ to override the `readSensitiveFile()` method and omit the required security\
      \ check:\n\n```java\npublic void readSensitiveFile() {\n  try {\n    SecurityManager\
      \ sm = System.getSecurityManager();\n    if (sm != null) {  // Check for permission\
      \ to read file\n      sm.checkRead(\"/temp/tempFile\");\n    }\n    // Access\
      \ the file\n  } catch (SecurityException se) {\n    // Log exception\n  }\n\
      }\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 358:Improperly Implemented Security Check for Standard
    Security: true
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140703'
    language: Java
  - Advice: "对应的符合规定的解决方案 1：将 `readSensitiveFile()`声明为 final，从而避免覆写。\n    ```java\n\
      \    public final void readSensitiveFile() {\n        try {\n            SecurityManager\
      \ sm = System.getSecurityManager();\n            if (sm != null) { // Check\
      \ for permission to read file\n                sm.checkRead(\"/temp/tempFile\"\
      );\n            }\n            // Access the file\n        } catch (SecurityException\
      \ se) {\n            // Log exception\n        }\n    }\n    ```      \n   \
      \ 对应的符合规定的解决方案 2：将 `readSensitiveFile()`声明为 private:，从而避免覆写。\n    ```java\n\
      \    private void readSensitiveFile() {\n        try {\n            SecurityManager\
      \ sm = System.getSecurityManager();\n            if (sm != null) { // Check\
      \ for permission to read file\n                sm.checkRead(\"/temp/tempFile\"\
      );\n            }\n        }\n        // Access the file }\n        catch (SecurityException\
      \ se) {\n            // Log exception\n        }\n    }\n    ```   \n4) 特例 \
      \  \n声明为 final 的类不受这条规则的限制，因为它们的成员方法不能够被覆写。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.6.4 进行安全检测的方法必须声明为private或final
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n安全检测方法必须声明为 private或 final，以避免恶意的子类覆写 nonfinal类的成员方法。\
      \   \n2) 原理   \n恶意的子类可以覆写进行安全检测的 nonfinal 类的成员方法，并忽略这些检测，从而使安全检测不起作用。因此，这些方法必须声明为\
      \ private或 final来避免覆写。   \n3) 示例   \n    这个不符合规则的代码示例：允许子类覆写方法 `readSensitiveFile()`，并省略了所需的安全检测。\n\
      \    ```java\n    public void readSensitiveFile() {\n        try {\n       \
      \     SecurityManager sm = System.getSecurityManager();\n            if (sm\
      \ != null) { // Check for permission to read file\n                sm.checkRead(\"\
      /temp/tempFile\"); // Access the file\n            }\n        } catch (SecurityException\
      \ se) { // Log exception\n        }\n    }\n    ```    \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 358:Improperly Implemented Security Check for Standard
      - type: CERT
        value: MET03-J:Methods that perform a security check must be declared private
          or final
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160053
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\npublic final void readSensitiveFile()\
      \ {\n    try {\n        SecurityManager sm = System.getSecurityManager();\n\
      \        if (sm != null) { // Check for permission to read file\n          \
      \  sm.checkRead(\"/temp/tempFile\");\n        }\n        // Access the file\n\
      \    } catch (SecurityException se) {\n        // Log exception\n    }\n}\n\
      ```\n这个符合规范的方案将`readSensitiveFile()`声明为final的，从而避免了覆写。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.1 面向对象程序安全
      - type: DetailClassChin
        value: A.3.1.5 进行安全检测的方法必须声明为private或final
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：恶意子类可以覆写进行安全检测的Nonfinal类的成员方法，并忽略这些检测。从而使安全检测不起作用。因此，这些方法必须声明为private或final来避免覆写。\
      \  \n对于进行安全检测的方法必须声明为private或final的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n\
      ```java\npublic void readSensitiveFile() {\n    try {\n        SecurityManager\
      \ sm = System.getSecurityManager();\n        if (sm != null) { // Check for\
      \ permission to read file\n            sm.checkRead(\"/temp/tempFile\");\n \
      \       }\n        // Access the file\n    } catch (SecurityException se) {\n\
      \        // Log exception\n    }\n}\n```  \n这个不符合规范的代码示例允许子类覆写方法`readSensitiveFile()`，并省略了所需的安全检测。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 358:Improperly Implemented Security Check for Standard
      - type: CERT
        value: MET03-J:Methods that perform a security check must be declared private
          or final
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170031'
    language: Java
  - Advice: "**规范的代码样例1**：  \n采用try-catch-finally语法释放创建的文件流。 \n```java \ntry {\n \
      \   final FileInputStream stream = new FileInputStream(fileName);\n    try {\n\
      \        final BufferedReader bufRead = new BufferedReader(new InputStreamReader(stream));\n\
      \        String line;\n        while ((line = bufRead.readLine()) != null) {\n\
      \            sendLine(line);\n        }\n    } finally {\n        if (stream\
      \ != null) {\n            try {\n                stream.close();\n         \
      \   } catch (IOException e) {\n                // forward to handler\n     \
      \       }\n        }\n    }\n} catch (IOException e) {\n    // forward to handler\n\
      }\n```  \n采用Java 7 try-with-resource 语法在try语句结束后，不论其包括的代码是正常执行完毕还是发生异常，都会自动调用资源的Close方法。\
      \  \n```java\ntry (FileInputStream stream = new FileInputStream(fileName);\n\
      \        BufferedReader bufRead = new BufferedReader(new InputStreamReader(stream)))\
      \ {\n    String line;\n    while ((line = bufRead.readLine()) != null) {\n \
      \       sendLine(line);\n    }\n} catch (IOException e) {\n    // forward to\
      \ handler\n}\n```  \n**规范的代码代码样例2**：  \n```java\nStatement stmt = null;\nResultSet\
      \ rs = null;\nConnection conn = getConnection();\ntry {\n  stmt = conn.createStatement();\n\
      \  rs = stmt.executeQuery(sqlQuery);\n  processResults(rs);\n} catch (SQLException\
      \ e) {\n  // Forward to handler\n} finally {\n  try {\n    if (rs != null) {rs.close();}\n\
      \  } catch (SQLException e) {\n    // Forward to handler\n  } finally {\n  \
      \  try {\n      if (stmt != null) {stmt.close();}\n    } catch (SQLException\
      \ e) {\n      // Forward to handler\n    } finally {\n      try {\n        if\
      \ (conn != null) {conn.close();}\n      } catch (SQLException e) {\n       \
      \ // Forward to handler\n      }\n    }\n  }\n}\n```    \n代码捕获了调用`rs.close()`或者`stmt.close()`时可能会抛出的所有异常，保证后续的关闭其他资源的代码可以正常执行。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution releases all acquired\
      \ resources, regardless of any exceptions that might occur. Even though dereferencing\
      \ `bufRead` might result in an exception, the `FileInputStream` object is closed\
      \ as required.\n\n```java\ntry {\n  final FileInputStream stream = new FileInputStream(fileName);\n\
      \  try {\n    final BufferedReader bufRead =\n        new BufferedReader(new\
      \ InputStreamReader(stream));\n\n    String line;\n    while ((line = bufRead.readLine())\
      \ != null) {\n      sendLine(line);\n    }\n  } finally {\n    if (stream !=\
      \ null) {\n      try {\n        stream.close();\n      } catch (IOException\
      \ e) {\n        // Forward to handler\n      }\n    }\n  }\n} catch (IOException\
      \ e) {\n  // Forward to handler\n}\n```\n\n**Compliant Solution (`try`-with-resources)**\n\
      \nThis compliant solution uses the `try`-with-resources statement, introduced\
      \ in Java SE 7, to release all acquired resources regardless of any exceptions\
      \ that might occur:\n\n```java\ntry (FileInputStream stream = new FileInputStream(fileName);\n\
      \     BufferedReader bufRead =\n         new BufferedReader(new InputStreamReader(stream)))\
      \ {\n\n  String line;\n  while ((line = bufRead.readLine()) != null) {\n   \
      \ sendLine(line);\n  }\n} catch (IOException e) {\n  // Forward to handler\n\
      }\n```\n\nThe `try`-with-resources construct sends any `IOException` to the\
      \ `catch` clause, where it is forwarded to an exception handler. Exceptions\
      \ generated during the allocation of resources (that is, the creation of the\
      \ `FileInputStream` or `BufferedReader`), as well as any `IOException` thrown\
      \ during execution of the `while` loop and any `IOException` generated by closing\
      \ `bufRead` or `stream`, are included.\n**Compliant Solution**\n\nThis compliant\
      \ solution ensures that resources are released as required:\n\n```java\nStatement\
      \ stmt = null;\nResultSet rs = null;\nConnection conn = getConnection();\ntry\
      \ {\n  stmt = conn.createStatement();\n  rs = stmt.executeQuery(sqlQuery);\n\
      \  processResults(rs);\n} catch (SQLException e) {\n  // Forward to handler\n\
      } finally {\n  try {\n    if (rs != null) {rs.close();}\n  } catch (SQLException\
      \ e) {\n    // Forward to handler\n  } finally {\n    try {\n      if (stmt\
      \ != null) {stmt.close();}\n    } catch (SQLException e) {\n      // Forward\
      \ to handler\n    } finally {\n      try {\n        if (conn != null) {conn.close();}\n\
      \      } catch (SQLException e) {\n        // Forward to handler\n      }\n\
      \    }\n  }\n}\n```"
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO04-J：在不需要时关闭资源
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO04-J:Release resources when they are no longer needed
    Default: true
    Description: "Java的垃圾回收机制可以回收那些没有引用而且没有被释放的内存。然而，垃圾回收不能释放那些非内存资源，例如打开的文件描述和数据库连接。因此，如果不能释放这样的资源会导致资源耗尽攻击。另外，在等待`finalize()`方法释放像Lock或者Semaphone对象的资源时，程序会经历资源枯竭，这种情况是因为Java不能保证什么时候执行`finalize()`方法，而只能保证它在程序结束前某个时刻执行。最后，输出流会引用缓存对象，直至输出流关闭为止，垃圾回收才会收集缓存对象，所以，输出流在使用后应该及时的关闭。\
      \  \n依赖`finalize()`方法释放系统资源，或者对使用哪部分程序来负责释放资源有疑问的话，一个程序就会发生资源泄漏。在一个忙碌的系统中，攻击者利用调用`finalize()`方法之前的延时引入Dos攻击。\
      \  \n程序员需要在非终结器方法中释放除原始内存之外的资源。  \n风险评估：违反该条准则可能会导致资源耗尽。    \n**不规范的代码样例1**：\
      \  \n```java\npublic int processFile(String fileName) throws IOException, FileNotFoundException\
      \ {\n    FileInputStream stream = new FileInputStream(fileName);\n    BufferedReader\
      \ bufRead = new BufferedReader(new InputStreamReader(stream));\n    String line;\n\
      \    while ((line = bufRead.readLine()) != null) {\n        sendLine(line);\n\
      \    }\n    return 1;\n}\n```  \n代码打开并使用了一个文件输入流，但没有的显式的关闭这个文件流。  \n**不规范的代码样例2**：\
      \  \n```java\npublic void getResults(String sqlQuery) {\n    try {\n       \
      \ Connection conn = getConnection();\n        Statement stmt = conn.createStatement();\n\
      \        ResultSet rs = stmt.executeQuery(sqlQuery);\n        processResults(rs);\n\
      \        stmt.close();\n        conn.close();\n    } catch (SQLException e)\
      \ {\n        /* forward to handler */ \n    }\n}\n```  \n代码打开并使用了一个数据库连接，使用完后，没有显式的关闭这个连接。这种情况下，资源池耗尽问题会更严重。\
      \  \n**不规范的代码样例3**：  \n```java\nStatement stmt = null;\nResultSet rs = null;\n\
      Connection conn = getConnection();\ntry {\n  stmt = conn.createStatement();\n\
      \  rs = stmt.executeQuery(sqlQuery);\n  processResults(rs);\n} catch(SQLException\
      \ e) {\n  // Forward to handler\n} finally {\n  rs.close();\n  stmt.close();\n\
      \  conn.close();\n}\n```  \n代码通过finally来释放数据库连接资源，然而，调用`rs.close()、stmt.close()`时会抛出NullPointerException，导致在这两个方法调用后的代码不能执行。"
    Description_En: "The Java garbage collector is called to free unreferenced but\
      \ as-yet unreleased memory. However, the garbage collector cannot free nonmemory\
      \ resources such as open file descriptors and database connections. Consequently,\
      \ failing to release such resources can lead to resource exhaustion attacks.In\
      \ addition, programs can experience resource starvation while waiting for a\
      \ finalizer to release resources such as Lock or Semaphore objects. This can\
      \ occur because Java lacks any temporal guarantee of when finalizers execute\
      \ other than \"sometime before program termination.\" Finally, output streams\
      \ may cache object references; such cached objects are not garbage-collected\
      \ until after the output stream is closed. Consequently, output streams should\
      \ be closed promptly after use.\n\nA program may leak resources when it relies\
      \ on finalizers to release system resources or when there is confusion over\
      \ which part of the program is responsible for releasing system resources. In\
      \ a busy system, the delay before the finalizer is called for an object provides\
      \ a window of vulnerability during which an attacker could induce a denial-of-service\
      \ (DoS) attack. Consequently, resources other than raw memory must be explicitly\
      \ freed in nonfinalizer methods because of the unsuitability of using finalizers.\
      \ See MET12-J. Do not use finalizers for additional reasons to avoid the use\
      \ of finalizers.\n\nNote that on Windows systems, attempts to delete open files\
      \ fail silently (see FIO03-J. Remove temporary files before termination for\
      \ more information).\n\n**Noncompliant Code Example (File Handle)**\n\nThis\
      \ noncompliant code example opens a file and uses it but fails to explicitly\
      \ close the file:\n\n```java\npublic int processFile(String fileName)\n    \
      \                   throws IOException, FileNotFoundException {\n  FileInputStream\
      \ stream = new FileInputStream(fileName);\n  BufferedReader bufRead =\n    \
      \  new BufferedReader(new InputStreamReader(stream));\n  String line;\n  while\
      \ ((line = bufRead.readLine()) != null) {\n    sendLine(line);\n  }\n  return\
      \ 1;\n}\n```\n\n**Noncompliant Code Example (SQL Connection)**\n\nThe problem\
      \ of resource pool exhaustion is exacerbated in the case of database connections.\
      \ Many database servers allow only a fixed number of connections, depending\
      \ on configuration and licensing. Consequently, failure to release database\
      \ connections can result in rapid exhaustion of available connections. This\
      \ noncompliant code example fails to close the connection when an error occurs\
      \ during execution of the SQL statement or during processing of the results:\n\
      \n```java\npublic void getResults(String sqlQuery) {\n  try {\n    Connection\
      \ conn = getConnection();\n    Statement stmt = conn.createStatement();\n  \
      \  ResultSet rs = stmt.executeQuery(sqlQuery);\n    processResults(rs);\n  \
      \  stmt.close(); conn.close();\n  } catch (SQLException e) { /* Forward to handler\
      \ */ }\n}\n```\n\n**Noncompliant Code Example**\n\nThis noncompliant code example\
      \ attempts to address exhaustion of database connections by adding cleanup code\
      \ in a `finally` block. However, `rs`, `stmt`, or `conn` could be `null`, causing\
      \ the code in the `finally` block to throw a `NullPointerException`.\n\n```java\n\
      Statement stmt = null;\nResultSet rs = null;\nConnection conn = getConnection();\n\
      try {\n  stmt = conn.createStatement();\n  rs = stmt.executeQuery(sqlQuery);\n\
      \  processResults(rs);\n} catch(SQLException e) {\n  // Forward to handler\n\
      } finally {\n  rs.close();\n  stmt.close(); conn.close();\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 775:Missing Release of File Descriptor or Handle after Effective Lifetime
      - type: CWE
        value: 772:Missing Release of Resource after Effective Lifetime
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 404:Improper Resource Shutdown or Release
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 772
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 404
      - type: SJ/T 11683-2017
        value: 6.6.1 资源泄漏
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141404'
    language: Java
  - Advice: "对应的符合规则的解决方案是：释放所有获取的资源，而不管是否会抛出任何异常。尽管对 bufRead的解引用会导致异常，但 FilelnputStream\
      \ 对象还是按照需要关闭。   \n    ```java\n    try {   \n        final FilelnputStream stream\
      \ = new FilelnputStream(fileName);   \n        try {   \n            final BufferedReader\
      \ bufRead =new BufferedReader(new InputStreamReader(stream));    \n        \
      \    String line;   \n            while ((line = bufRead.readLine()) != null)\
      \ {   \n                sendLine(line);   \n            }   \n            }\
      \ finally {   \n                if (stream != null) {    \n                \
      \    try {   \n                        stream.close();   \n                \
      \    } catch (IOException e) {   \n                        // forward to handler\
      \   \n                    }   \n                }   \n            }   \n   \
      \     } catch (IOException e) {    \n            // forward to handler   \n\
      \        }\n    ```   \n      \n    **例 2** 在使用数据库连接的情况下，资源池耗尽的问题会更严重。许多数据库服务器仅允许限量连接，这取决于配置和许可证的情况。不能释放数据库连接会导致可用连接快速耗尽。在执行\
      \ SQL 语句或者在处理相关的结果时，这个不符合规则代码示例没有关闭连接。   \n    ```java\n    public void getResults(String\
      \ sqlQuery) {    \n        try {   \n            Connection conn = getConnection();\
      \   \n            Statement stmt = conn.createStatement();   \n            ResultSet\
      \ rs = stmt.executeQuery(sqlQuery);    \n            processResults(rs);   \
      \ \n            stmt.close();   \n            conn.close ();   \n        } catch\
      \ (SQLException e) { /* forward to handler */ \n        }   \n    }\n    ```\
      \   \n    对应的符合规则的解决方案是：使用 try-with-resource，这个方法是在 Java SE 7 中引入的，用来保证资源按照要求释放。\
      \   \n    ```java\n    try (Connection conn = getConnection();Statement stmt\
      \ = conn.createStatement();   \n        ResultSet rs = stmt.executeQuery(sqlQuery)\
      \ ) {   \n        processResults(rs);   \n    } catch (SQLException e) {   \n\
      \      // forward to handler   \n    }\n    ```   \n      \n    **例 3**这个不符合规则的代码示例想要通过在\
      \ finally代码块中加入清理代码来解决数据库连接造成的资源耗尽问题。然而，`rs`、`stmt`、或者`conn`可以是 null,从而在 finally\
      \ 代码块中抛出 NullPointerException   \n    异常。   \n    ```java\n    Statement stmt\
      \ = null;   \n    ResultSet rs = null;   \n    Connection conn = getConnection();\
      \   \n    try {   \n          stmt = conn.createStatement();    \n         \
      \ rs = stmt.executeQuery(sqlQuery);   \n          processResults(rs);   \n \
      \   } catch(SQLException e) {   \n        // forward to handler    \n    } finally\
      \ {    \n          rs.close ();    \n          stmt.close ();    \n        \
      \  conn.close ();   \n    }\n    ```   \n    对应的符合规则的解决方案是：保证资源按照要求释放。   \n\
      \    ```java\n    Statement stmt = null;   \n    ResultSet rs = null;   \n \
      \   Connection conn = getConnection();    \n    try {   \n          stmt = conn.createStatement();\
      \   \n          rs = stmt.executeQuery(sqlQuery);    \n          processResults(rs);\
      \   \n    } catch (SQLException e) {   \n        // forward to handler    \n\
      \    } finally {    \n        try {   \n            if (rs != null) {\n    \
      \            rs.close();\n            }   \n        } catch (SQLException e)\
      \ {   \n            // forward to handler    \n        } finally {    \n   \
      \         try {   \n            if (stmt != null) {\n                stmt.close();\n\
      \            }   \n            } catch (SQLException e) {   \n             \
      \   // forward to handler    \n            } finally {    \n               \
      \ try {   \n                    if (conn != null) {\n                      \
      \  conn.close();\n                    }    \n                } catch (SQLException\
      \ e) {   \n                // forward to handler   \n                }   \n\
      \            }   \n        }   \n    }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 7.2 资源管理
      - type: DetailClassChin
        value: C.13.5 在不需要时关闭资源
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n输出流在使用后应当及时关闭。   \n2) 原理   \nJava 调用垃圾回收器来释放没有引用且没有被释放的内存。然而，垃圾回收器不能释放那些非内存资源，例如打开的文件描述符和数据库连接。因此，如果不能释放这样的资源会导致资源耗尽攻击。\
      \   \n3) 示例   \n  \n    **例 1** 这个不符合规则的代码示例打开并使用一个文件，但没有显示关闭这个文件。   \n    ```java\n\
      \    public int processFile(String fileName)    \n                  throws IOException,\
      \ FileNotFoundException {    \n        FilelnputStream stream = new FilelnputStream(fileName);\
      \   \n        BufferedReader bufRead =new BufferedReader(new InputStreamReader(stream));\
      \   \n        String line;   \n        while ((line = bufRead.readLine() )!=\
      \ null) {   \n            sendLine(line);   \n        }   \n        return 1;\
      \   \n    }\n    ```   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 775:Missing Release of File Descriptor or Handle after Effective Lifetime
      - type: CWE
        value: 772:Missing Release of Resource after Effective Lifetime
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 404:Improper Resource Shutdown or Release
      - type: CERT
        value: FIO04-J:Close resources when they are no longer needed
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 772
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 404
      - type: SJ/T 11683-2017
        value: 6.6.1 资源泄漏
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160110
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\npublic void getZipContents(String\
      \ fileName) {\n    ZipFile zFile = null;\n    try {\n        zFile = new ZipFile(fileName);\n\
      \        ...\n    } catch (IOException e) {\n        e.printStackTrace();\n\
      \    } finally {\n        if (zFile != null) {\n            try {\n        \
      \        zFile.close();\n            } catch (IOException e) {\n           \
      \     e.printStackTrace();\n            }\n        }\n    }\n}\n```  \n上述代码片段中，使用完之前创建的文件句柄`zFile`后，在finally代码块中进行了释放。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 7.4 文件管理
      - type: DetailClassChin
        value: A.4.4.2 需确保文件得到释放
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：程序创建或分配文件句柄后，不进行合理释放，将会降低系统性能，攻击者可能会通过耗尽资源池的方式发起拒绝服务攻击。  \n\
      对于需确保文件得到释放的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：   \n```java\npublic void getZipContents(String\
      \ fileName){  \n    ZipFile zFile = null;  \n    try{  \n        zFile = new\
      \ ZipFile(fileName);  \n        ...  \n    } catch (IOException e) {  \n   \
      \     e.printStackTrace();  \n    }  \n}\n```  \n上述代码片段中，创建了一个文件句柄`zFile`，但未进行合理释放。程序不应依赖于`finalize()`回收文件句柄资源，应在finally代码块中手动释放文件句柄资源。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 775:Missing Release of File Descriptor or Handle after Effective Lifetime
      - type: CWE
        value: 772:Missing Release of Resource after Effective Lifetime
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 404:Improper Resource Shutdown or Release
      - type: CERT
        value: FIO04-J:Close resources when they are no longer needed
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 772
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 404
      - type: SJ/T 11683-2017
        value: 6.6.1 资源泄漏
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170065'
    language: Java
  - Advice: "**规范的代码样例1**\n这个解决方案从不受信任的用户输入中获取文件名，对其进行规范化，然后根据良性路径名列表对其进行验证。 仅当验证成功时才对指定文件进行操作，即仅当该文件是\
      \ `/img/java` 中的 `file1.txt` 或 `file2.txt` 两个有效文件之一。\n```java\nFile file = new\
      \ File(\"/img/\" + args[0]);\nif (!isInSecureDir(file)) {\n  throw new IllegalArgumentException();\n\
      }\nString canonicalPath = file.getCanonicalPath();\nif (!canonicalPath.equals(\"\
      /img/java/file1.txt\") &&\n    !canonicalPath.equals(\"/img/java/file2.txt\"\
      )) {\n   // Invalid file; handle error\n}\n\nFileInputStream fis = new FileInputStream(f);\n\
      ```\n**规范的代码样例2**\n处理此问题的一种综合方法是授予应用程序仅对预期目录中存在的文件（本示例中为“/img”目录）进行操作的权限。\n\
      ```java\n// All files in /img/java can be read\ngrant codeBase \"file:/home/programpath/\"\
      \ {\n  permission java.io.FilePermission \"/img/java\", \"read\";\n};\n```"
    Advice_En: "**Compliant Solution (`getCanonicalPath()`)**\n\nThis compliant solution\
      \ obtains the file name from the untrusted user input, canonicalizes it, and\
      \ then validates it against a list of benign path names. It operates on the\
      \ specified file only when validation succeeds, that is, only if the file is\
      \ one of the two valid files `file1.txt` or `file2.txt` in `/img/java`.\n\n\
      ```java\nFile file = new File(\"/img/\" + args[0]);\nif (!isInSecureDir(file))\
      \ {\n  throw new IllegalArgumentException();\n}\nString canonicalPath = file.getCanonicalPath();\n\
      if (!canonicalPath.equals(\"/img/java/file1.txt\") &&\n    !canonicalPath.equals(\"\
      /img/java/file2.txt\")) {\n   // Invalid file; handle error\n}\n\nFileInputStream\
      \ fis = new FileInputStream(f);\n```\n\n**Compliant Solution (Security Manager)**\n\
      A comprehensive way to handle this issue is to grant the application the permissions\
      \ to operate only on files present within the intended directory—the `/img`\
      \ directory in this example. This compliant solution specifies the absolute\
      \ path of the program in its security policy file and grants `java.io.FilePermission`\
      \ with target `/img/java` and the read action.\nThis solution requires that\
      \ the `/img` directory is a secure directory, as described in FIO00-J. Do not\
      \ operate on files in shared directories\n```java\n// All files in /img/java\
      \ can be read\ngrant codeBase \"file:/home/programpath/\" {\n  permission java.io.FilePermission\
      \ \"/img/java\", \"read\";\n};\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO16-J：在验证路径名之前将其规范化
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO16-J:Canonicalize path names before validating them
    Default: true
    Description: "根据 java.io.File 类的 Java API [API 2006]：\n> 路径名，无论是抽象的还是字符串形式的，都可以是\
      \ *absolute* 或 *relative*。绝对路径名是完整的，因为不需要其他信息来定位它表示的文件。相反，相对路径名必须根据从其他路径名获取的信息来解释。\n\
      \n绝对或相对路径名可能包含文件链接，例如符号（软）链接、硬链接、快捷方式、阴影、别名和连接。在执行任何文件验证操作之前，必须完全解析这些文件链接。例如，名为“trace”的符号链接的最终目标可能是路径名“/home/system/trace”。路径名还可能包含使验证变得困难的特殊文件名：\n\
      \n1.“`.`”指的是目录本身。\n2. 在一个目录中，特殊的文件名“`..`”是指该目录的父目录。\n\n除了这些特定问题之外，各种特定于操作系统和特定于文件系统的命名约定使验证变得困难。\n\
      规范化文件名可以更轻松地验证路径名。多个路径名可以引用单个目录或文件。此外，路径名的文本表示可能很少或根本没有关于它所指的目录或文件的信息。因此，所有路径名必须在验证之前完全解析或规范化。\n\
      例如，当试图限制用户访问特定目录中的文件或以其他方式根据文件名或路径名的名称做出安全决定时，可能需要进行验证。通常，攻击者可以通过利用目录遍历或路径等效漏洞绕过这些限制。目录遍历漏洞允许\
      \ I/O 操作转义指定的操作目录。当攻击者为资源提供不同但等效的名称以绕过安全检查时，就会出现路径等效漏洞。\n规范化在程序获取规范路径名和打开文件之间包含一个固有的竞争窗口。在验证规范路径名时，文件系统可能已被修改，规范路径名可能不再引用原始有效文件。幸运的是，这种竞争状况可以很容易地得到缓解。规范路径名可用于确定引用的文件名是否在安全目录中（有关详细信息，请参阅\
      \ FIO00-J。请勿对共享目录中的文件进行操作）。如果引用的文件位于安全目录中，则根据定义，攻击者无法篡改它，也无法利用竞争条件。\n该建议是 IDS01-J\
      \ 的一个特定实例。在验证字符串之前对其进行规范化。\n**不规范代码样例1**\n这个不合规的代码示例允许用户指定要打开的图像文件的路径。 通过将`/img/`添加到目录中，此代码强制执行仅应打开此目录中的文件的策略。\
      \ 该程序还使用 FIO00-J 中定义的 `isInSecureDir()` 方法。 不要对共享目录中的文件进行操作。\n但是，用户仍然可以通过输入包含“../”序列的参数来指定预期目录之外的文件。\
      \ 攻击者还可以在 `/img` 目录中创建一个链接，该链接指向该目录之外的目录或文件。 链接的路径名可能看起来驻留在 `/img` 目录中并因此通过验证，但实际上该操作将在链接的最终目标上执行，该目标可以驻留在预期目录之外。\n\
      ```java\nFile file = new File(\"/img/\" + args[0]);\nif (!isInSecureDir(file))\
      \ {\n  throw new IllegalArgumentException();\n}\nFileOutputStream fis = new\
      \ FileOutputStream(file);\n// ...\n```\n**不规范的代码样例2**\n这个不合规的代码示例试图通过使用 Java\
      \ 2 中引入的 File.getCanonicalPath() 方法来缓解该问题，该方法完全解析参数并构造规范化路径。 特殊文件名，如点点 (`..`)\
      \ 也被删除，以便在执行验证之前将输入简化为规范化形式。 当存在 `validate()` 方法时，攻击者无法使用 `../` 序列突破指定目录。 例如，路径\
      \ `/img/../etc/passwd` 解析为 `/etc/passwd`。 `getCanonicalPath()` 方法在小程序中使用时会引发安全异常，因为它会泄露有关主机的太多信息。\
      \ `getCanonicalFile()` 方法的行为类似于 `getCanonicalPath()`，但返回一个新的 `File` 对象而不是 `String`。\n\
      不幸的是，规范化是在验证之后执行的，这使得验证无效。\n```java\nFile file = new File(\"/img/\" + args[0]);\n\
      if (!isInSecureDir(file)) {\n  throw new IllegalArgumentException();\n}\nString\
      \ canonicalPath = file.getCanonicalPath();\nFileOutputStream fis = new FileOutputStream(canonicalPath);\n\
      // ...\n```"
    Description_En: "According to the Java API [API 2006] for class java.io.File:\n\
      > A pathname, whether abstract or in string form, may be either *absolute* or\
      \ *relative*. An absolute pathname is complete in that no other information\
      \ is required to locate the file that it denotes. A relative pathname, in contrast,\
      \ must be interpreted in terms of information taken from some other pathname.\n\
      \nAbsolute or relative path names may contain file links such as symbolic (soft)\
      \ links, hard links, shortcuts, shadows, aliases, and junctions. These file\
      \ links must be fully resolved before any file validation operations are performed.\
      \ For example, the final target of a symbolic link called `trace` might be the\
      \ path name `/home/system/trace`. Path names may also contain special file names\
      \ that make validation difficult:\n\n1. \"`.`\" refers to the directory itself.\n\
      2. Inside a directory, the special file name \"`..`\" refers to the directory's\
      \ parent directory.\n\nIn addition to these specific issues, a wide variety\
      \ of operating system–specific and file system–specific naming conventions make\
      \ validation difficult.\nCanonicalizing file names makes it easier to validate\
      \ a path name. More than one path name can refer to a single directory or file.\
      \ Further, the textual representation of a path name may yield little or no\
      \ information regarding the directory or file to which it refers. Consequently,\
      \ all path names must be fully resolved or canonicalized before validation.\n\
      \nValidation may be necessary, for example, when attempting to restrict user\
      \ access to files within a particular directory or to otherwise make security\
      \ decisions based on the name of a file name or path name. Frequently, these\
      \ restrictions can be circumvented by an attacker by exploiting a directory\
      \ traversal or path equivalence vulnerability. A directory traversal vulnerability\
      \ allows an I/O operation to escape a specified operating directory. A path\
      \ equivalence vulnerability occurs when an attacker provides a different but\
      \ equivalent name for a resource to bypass security checks.\n\nCanonicalization\
      \ contains an inherent race window between the time the program obtains the\
      \ canonical path name and the time it opens the file. While the canonical path\
      \ name is being validated, the file system may have been modified and the canonical\
      \ path name may no longer reference the original valid file. Fortunately, this\
      \ race condition can be easily mitigated. The canonical path name can be used\
      \ to determine whether the referenced file name is in a secure directory (see\
      \ FIO00-J. Do not operate on files in shared directories for more information).\
      \ If the referenced file is in a secure directory, then, by definition, an attacker\
      \ cannot tamper with it and cannot exploit the race condition.\n\nThis recommendation\
      \ is a specific instance of IDS01-J. Normalize strings before validating them.\n\
      \n**Noncompliant Code Example**\n\nThis noncompliant code example allows the\
      \ user to specify the path of an image file to open. By prepending `/img/` to\
      \ the directory, this code enforces a policy that only files in this directory\
      \ should be opened. The program also uses the `isInSecureDir()` method defined\
      \ in FIO00-J. Do not operate on files in shared directories.\nHowever, the user\
      \ can still specify a file outside the intended directory by entering an argument\
      \ that contains `../` sequences.  An attacker can also create a link in the\
      \ `/img` directory that refers to a directory or file outside of that directory.\
      \ The path name of the link might appear to reside in the `/img` directory and\
      \ consequently pass validation, but the operation will actually be performed\
      \ on the final target of the link, which can reside outside the intended directory.\n\
      \n```java\nFile file = new File(\"/img/\" + args[0]);\nif (!isInSecureDir(file))\
      \ {\n  throw new IllegalArgumentException();\n}\nFileOutputStream fis = new\
      \ FileOutputStream(file);\n// ...\n```\n\n**Noncompliant Code Example (`getCanonicalPath()`)**\n\
      \nThis noncompliant code example attempts to mitigate the issue by using the\
      \ `File.getCanonicalPath()` method, introduced in Java 2, which fully resolves\
      \ the argument and constructs a canonicalized path. Special file names such\
      \ as dot dot (`..`) are also removed so that the input is reduced to a canonicalized\
      \ form before validation is carried out. An attacker cannot use `../` sequences\
      \ to break out of the specified directory when the `validate()` method is present.\
      \ For example, the path `/img/../etc/passwd` resolves to `/etc/passwd`. The\
      \ `getCanonicalPath()` method throws a security exception when used in applets\
      \ because it reveals too much information about the host machine. The `getCanonicalFile()`\
      \ method behaves like `getCanonicalPath()` but returns a new `File` object instead\
      \ of a `String`.\n\nUnfortunately, the canonicalization is performed after the\
      \ validation, which renders the validation ineffective.\n\n```java\nFile file\
      \ = new File(\"/img/\" + args[0]);\nif (!isInSecureDir(file)) {\n  throw new\
      \ IllegalArgumentException();\n}\nString canonicalPath = file.getCanonicalPath();\n\
      FileOutputStream fis = new FileOutputStream(canonicalPath);\n// ...\n```"
    DomainType: ''
    DynamicDescription: ''
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141416'
    language: Java
  - Advice: 防止路径遍历的最佳方法是创建一份合法资源名的列表，并且规定用户只能选择其中的文件名。
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 7.4 文件管理
      - type: DetailClassChin
        value: A.4.4.5 避免路径遍历
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：应用程序对用户可控制的输入未经合理校验，就传送给一个文件API。攻击者可能会使用一些特殊的字符（如`“..”`和`“/”`）摆脱受保护的限制，访问一些受保护的文件或目录。\
      \  \n对于避免路径遍历的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nString path\
      \ = getInputPath();  \nif (path.startsWith(\"/safe_dir/\")){  \n    File f =\
      \ new File(path);  \n    f.delete();  \n}\n```  \n上述代码片段通过验证输入路径是否以`“/safe_dir/”`为开头，来判断是否进行创建、删除操作。\
      \  \n攻击者可能提供类似下面的输入：  \n`/safe_dir/../important.dat`  \n程序假定路径是有效的，因为它是以`“/safe_dir/”`开头的，但是`“../”`将导致程序删除`“important.dat”`文件的父目录。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 73:External Control of File Name or Path
      - type: CWE
        value: 36:Absolute Path Traversal
      - type: CWE
        value: 23:Relative Path Traversal
      - type: CWE
        value: 22:Improper Limitation of a Pathname to a Restricted Directory ('Path
          Traversal')
      - type: CERT
        value: IDS02-J:Canonicalize path names before validating them
      - type: OWASP TOP 10
        value: 2017:A5 Broken Access Control
      - type: OWASP TOP 10
        value: 2013:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2010:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2007:A4 Insecure Direct Object Reference
      - type: OWASP TOP 10
        value: 2004:A1 Unvalidated Input
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2011:Risky Resource Management - CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2010:Risky Resource Management - CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 73
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Path Traversal [EWR]
      - type: GB/T 34944-2017 Java
        value: 数据处理:相对路径遍历
      - type: GB/T 34944-2017 Java
        value: 数据处理:绝对路径遍历
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: 0302000010170068
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nclass ExceptionExample\
      \ {\n    public static void main(String[] args) {\n\n        File file = null;\n\
      \        try {\n            file = new File(System.getenv(\"APPDATA\") + args[0]).getCanonicalFile();\n\
      \            if (!file.getPath().startsWith(\"c:\\\\homepath\")) {\n       \
      \         System.out.println(\"Invalid file\");\n                return;\n \
      \           }\n        } catch (IOException x) {\n            System.out.println(\"\
      Invalid file\");\n            return;\n        }\n\n        try {\n        \
      \    FileInputStream fis = new FileInputStream(file);\n        } catch (FileNotFoundException\
      \ x) {\n            System.out.println(\"Invalid file\");\n            return;\n\
      \        }\n    }\n}\n```  \n这个符合规范的方案实现了一个安全策略，用户只能打开`c:\\homepath`中的文件。当文件不能打开或不在正确的目录内时，这个方案将会返回一个简洁的错误信息。`c:\\\
      homepath`目录外文件的任何信息都被屏蔽了。  \n这个方案还使用了`File.getCanonicalFile()`方法对文件进行标准化，简化了之后的对路径名进行比较的过程。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.4 异常处理安全
      - type: DetailClassChin
        value: A.3.4.3 不能允许异常泄露敏感信息
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：在异常传递的过程中，如果不对敏感信息进行过滤，会导致信息泄露，这将有助于攻击者实现对系统的攻击。  \n对于不能允许异常泄露敏感信息的情况，示例1、给出了不规范用法（java语言）示例。\
      \  \n**示例1**：   \n```java\nclass ExceptionExample {\n    public static void\
      \ main(String[] args) throws FileNotFoundException {\n        // Linux stores\
      \ a user's home directory path in\n        // the environment variable $HOME,\
      \ Windows in %APPDATA%\n        FileInputStream fis = new FileInputStream(System.getenv(\"\
      APPDATA\") + args[0]);\n    }\n}\n```  \n在这个不合规范的代码示例中，如果要求的文件不存在，将会抛出FileNotFoundException异常，通过不断的传递虚构的路径给程序，攻击者可以重建内部文件系统结构。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 600:Uncaught Exception in Servlet
      - type: CWE
        value: 431:Missing Handler
      - type: CWE
        value: 248:Uncaught Exception
      - type: CWE
        value: 209:Information Exposure Through an Error Message
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 209
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 209
      - type: GB/T 34944-2017 Java
        value: 数据处理:信息通过错误消息泄露
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170055'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nclass ExceptionExample\
      \ {\n    public static void main(String[] args) {\n\n        File file = null;\n\
      \        try {\n            file = new File(System.getenv(\"APPDATA\") + args[0]).getCanonicalFile();\n\
      \            if (!file.getPath().startsWith(\"c:\\\\homepath\")) {\n       \
      \         System.out.println(\"Invalid file\");\n                return;\n \
      \           }\n        } catch (IOException x) {\n            System.out.println(\"\
      Invalid file\");\n            return;\n        }\n\n        try {\n        \
      \    FileInputStream fis = new FileInputStream(file);\n        } catch (FileNotFoundException\
      \ x) {\n            System.out.println(\"Invalid file\");\n            return;\n\
      \        }\n    }\n}\n```  \n这个符合规范的方案实现了一个安全策略，用户只能打开`c:\\homepath`中的文件。当文件不能打开或不在正确的目录内时，这个方案将会返回一个简洁的错误信息。`c:\\\
      homepath`目录外文件的任何信息都被屏蔽了。  \n这个方案还使用了`File.getCanonicalFile()`方法对文件进行标准化，简化了之后的对路径名进行比较的过程。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: GB/T 34944-2017
      - type: SubClassChin
        value: 6.4 异常处理安全
      - type: DetailClassChin
        value: A.3.4.3 不能允许异常泄露敏感信息
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：在异常传递的过程中，如果不对敏感信息进行过滤，会导致信息泄露，这将有助于攻击者实现对系统的攻击。  \n对于不能允许异常泄露敏感信息的情况，示例1、给出了不规范用法（java语言）示例。\
      \  \n**示例1**：   \n```java\nclass ExceptionExample {\n    public static void\
      \ main(String[] args) throws FileNotFoundException {\n        // Linux stores\
      \ a user's home directory path in\n        // the environment variable $HOME,\
      \ Windows in %APPDATA%\n        FileInputStream fis = new FileInputStream(System.getenv(\"\
      APPDATA\") + args[0]);\n    }\n}\n```  \n在这个不合规范的代码示例中，如果要求的文件不存在，将会抛出FileNotFoundException异常，通过不断的传递虚构的路径给程序，攻击者可以重建内部文件系统结构。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 600:Uncaught Exception in Servlet
      - type: CWE
        value: 431:Missing Handler
      - type: CWE
        value: 248:Uncaught Exception
      - type: CWE
        value: 209:Information Exposure Through an Error Message
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 209
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 209
      - type: GB/T 34944-2017 Java
        value: 数据处理:信息通过错误消息泄露
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0402000010170055'
    language: Java
  - Advice: "`clone()`方法只能调用private或者final方法。\n**规范的代码样例1**：  \n将`CloneExample.doSomething()`和`CloneExample.deepCopy()`声明为final。\
      \  \n```java\nclass CloneExample implements Cloneable {  \n    final void doSomething()\
      \ {  \n    // . . .  \n    }  \n    final HttpCookie[] deepCopy() {  \n    //\
      \ . . .  \n    }  \n    // . . .  \n}\n```\n**例外情况**\nMET06-J-EX0：允许通过 `super.<em>method</em>(...)`\
      \ 调用父类的方法，因为此类调用不会动态分派给子类定义的方法。 事实上，调用 `super.clone()` 是预期的行为。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution declares both the\
      \ `doSomething()` and the `deepCopy()` methods final, preventing overriding\
      \ of these methods:\n\n```java\nclass CloneExample implements Cloneable {\n\
      \  final void doSomething() {\n    // ...\n  }\n  final HttpCookie[] deepCopy()\
      \ {\n    // ...\n  }\n\n  // ...\n}\n```\n\nAlternative solutions that prevent\
      \ invocation of overridden methods include declaring these methods private or\
      \ final or declaring the class containing these methods final.\n\n**Exceptions**\n\
      \nMET06-J-EX0: It is permitted to call a superclass's method via `super.<em>method</em>(...)`,\
      \ since such calls will not be dynamically dispatched to methods defined by\
      \ a subclass. In fact, calling `super.clone()` is expected behavior."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 06. 方法 (MET)
      - type: DetailClassChin
        value: MET06-J：不要在clone()方法中调用可覆写的方法
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 06. Methods (MET)
      - type: DetailClassChin
        value: MET06-J:Do not invoke overridable methods in clone()
    Default: not
    Description: "在`clone()`方法中调用可覆写的方法是不安全的。首先，一个恶意的子类可以对方法进行覆写并改变`clone()`方法的行为。其次，子类可以监视（甚至修改）被克隆对象创建完成之前的某一特定初始化的状态。无论哪一种情况，子类可能使克隆过程、被克隆的对象或者两者处于一个不合逻辑的状态。\
      \  \n该规则与 MET05-J.确保构造函数不调用可覆盖的方法 密切相关。\n**不规范的代码样例1**：\n样例中有2个类：CloneExample和Sub。CloneExample类调用了可被覆写的方法`doSomething()`，可被覆写的方法`CloneExample.doSomething()`设置了cookies的值，覆写的方法`Sub.doSomething()`设置了cookies的域名值。基于多态的原因，`Sub.doSomething()`方法在运行时执行了2次。第一次是`CloneExample.clone()`，另一次是在`Sub.clone()`。因此，`cookies`的值并没有被初始化，而域名值却初始化了2次。\n\
      此外，子类改变了克隆过程并不符合逻辑的复制。因为，`CloneExample.deepCopy()`方法是在调用`Sub.doSomething()`方法之后执行的，并且`Sub.doSomething()`错误的修改了对象。\n\
      ```java\nclass CloneExample implements Cloneable {\n  HttpCookie[] cookies;\n\
      \n  CloneExample(HttpCookie[] c) {\n    cookies = c;\n  }\n\n  public Object\
      \ clone() throws CloneNotSupportedException {\n    final CloneExample clone\
      \ = (CloneExample) super.clone();\n    clone.doSomething(); // Invokes overridable\
      \ method\n    clone.cookies = clone.deepCopy();\n    return clone;\n  }\n\n\
      \  void doSomething() { // Overridable\n    for (int i = 0; i < cookies.length;\
      \ i++) {\n      cookies[i].setValue(\"\" + i);\n    }\n  }\n\n  HttpCookie[]\
      \ deepCopy() {\n    if (cookies == null) {\n      throw new NullPointerException();\n\
      \    }\n\n    // Deep copy\n    HttpCookie[] cookiesCopy = new HttpCookie[cookies.length];\n\
      \n    for (int i = 0; i < cookies.length; i++) {\n      // Manually create a\
      \ copy of each element in array\n      cookiesCopy[i] = (HttpCookie) cookies[i].clone();\n\
      \    }\n    return cookiesCopy;\n  }\n}\n\nclass Sub extends CloneExample {\n\
      \  Sub(HttpCookie[] c) {\n    super(c);\n  }\n\n  public Object clone() throws\
      \ CloneNotSupportedException {\n    final Sub clone = (Sub) super.clone();\n\
      \    clone.doSomething();\n    return clone;\n  }\n\n  void doSomething() {\
      \ // Erroneously executed\n    for (int i = 0;i < cookies.length; i++) {\n \
      \     cookies[i].setDomain(i + \".foo.com\");\n    }\n  }\n\n  public static\
      \ void main(String[] args)\n      throws CloneNotSupportedException {\n    HttpCookie[]\
      \ hc = new HttpCookie[20];\n    for (int i = 0 ; i < hc.length; i++){\n    \
      \  hc[i] = new HttpCookie(\"cookie\" + i,\"\" + i);\n    }\n    CloneExample\
      \ bc = new Sub(hc);\n    bc.clone();\n  }\n}\n```\n当在对象的浅拷贝上调用可覆盖的方法时，原始对象也会被修改。"
    Description_En: "Calling overridable methods from the `clone()` method is insecure.\
      \ First, a malicious subclass could override the method and affect the behavior\
      \ of the `clone()` method. Second, a trusted subclass could observe (and potentially\
      \ modify) the cloned object in a partially initialized state before its construction\
      \ has concluded. In either case, the subclass could leave the clone, the object\
      \ being cloned, or both in an inconsistent state. Consequently, `clone()` methods\
      \ may invoke only methods that are final or private.\n\nThis rule is closely\
      \ related to MET05-J. Ensure that constructors do not call overridable methods.\n\
      \n**Noncompliant Code Example**\n\nThis noncompliant code example shows two\
      \ classes, `CloneExample` and `Sub`. The class `CloneExample` calls an overridable\
      \ method `doSomething()`. The overridden method sets the value of the cookies;\
      \ the overriding method sets the values of the domain names. The `doSomething()`\
      \ method of the subclass `Sub` is erroneously executed twice at runtime because\
      \ of polymorphism. The first invocation comes from `CloneExample.clone()`, and\
      \ the other comes from `Sub.clone()`. Consequently, the values of the cookies\
      \ are never initialized, whereas the domains are initialized twice.\n\nFurthermore,\
      \ the subclass not only sees the clone in an inconsistent state but also modifies\
      \ the clone in a manner that creates inconsistent copies. This is because the\
      \ `deepCopy()` method occurs after the call to the `doSomething()` method, and\
      \ the overriding `doSomething()` implementation erroneously modifies the object.\n\
      \n```java\nclass CloneExample implements Cloneable {\n  HttpCookie[] cookies;\n\
      \n  CloneExample(HttpCookie[] c) {\n    cookies = c;\n  }\n\n  public Object\
      \ clone() throws CloneNotSupportedException {\n    final CloneExample clone\
      \ = (CloneExample) super.clone();\n    clone.doSomething(); // Invokes overridable\
      \ method\n    clone.cookies = clone.deepCopy();\n    return clone;\n  }\n\n\
      \  void doSomething() { // Overridable\n    for (int i = 0; i < cookies.length;\
      \ i++) {\n      cookies[i].setValue(\"\" + i);\n    }\n  }\n\n  HttpCookie[]\
      \ deepCopy() {\n    if (cookies == null) {\n      throw new NullPointerException();\n\
      \    }\n\n    // Deep copy\n    HttpCookie[] cookiesCopy = new HttpCookie[cookies.length];\n\
      \n    for (int i = 0; i < cookies.length; i++) {\n      // Manually create a\
      \ copy of each element in array\n      cookiesCopy[i] = (HttpCookie) cookies[i].clone();\n\
      \    }\n    return cookiesCopy;\n  }\n}\n\nclass Sub extends CloneExample {\n\
      \  Sub(HttpCookie[] c) {\n    super(c);\n  }\n\n  public Object clone() throws\
      \ CloneNotSupportedException {\n    final Sub clone = (Sub) super.clone();\n\
      \    clone.doSomething();\n    return clone;\n  }\n\n  void doSomething() {\
      \ // Erroneously executed\n    for (int i = 0;i < cookies.length; i++) {\n \
      \     cookies[i].setDomain(i + \".foo.com\");\n    }\n  }\n\n  public static\
      \ void main(String[] args)\n      throws CloneNotSupportedException {\n    HttpCookie[]\
      \ hc = new HttpCookie[20];\n    for (int i = 0 ; i < hc.length; i++){\n    \
      \  hc[i] = new HttpCookie(\"cookie\" + i,\"\" + i);\n    }\n    CloneExample\
      \ bc = new Sub(hc);\n    bc.clone();\n  }\n}\n```\n\nWhen an overridable method\
      \ is invoked on a shallow copy of the object, the original object is also modified."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140706'
    language: Java
  - Advice: "对应的符合规定的解决方案是：将 `doSomething()`和 `deepCopy()`方法声明为 final，从而避免对这些方法的覆写。\
      \   \n    ```java\n    class CloneExample implements Cloneable {  \n       \
      \ final void doSomething() {  \n        // . . .  \n        }  \n        final\
      \ HttpCookie[] deepCopy() {  \n        // . . .  \n        }  \n        // .\
      \ . .  \n    }\n    ```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.6.7 不要在clone()中调用可覆写的方法
    Categories_En:
      Category: []
    Default: not
    Description: "1) 规定   \nclone()方法只能调用 final或 private的方法，即不可覆写的方法。   \n2) 原理  \
      \ \n在`clone()`方法中调用可覆写的方法是不安全的。首先，一个恶意的子类可以对方法进行覆写并改变`clone()`方法的行为。其次，一个可信任的子类可以观察（并有可能修改）被克隆对象在创建完成之前的某一特定初始化的状态。无论哪一种情况，子类可能是克隆过程、被克隆的物体或两者处于一个不合逻辑的状态。\
      \   \n3) 示例   \n    这个不符合规则的代码示例中，有两个类：CloneExample 和 SubCloneExample 类调用可覆写的方法\
      \ `doSomething()`。被覆写的方法设置了 cookies 的值，覆写的方法则设置了域名的值。基于多态性的原因，子类 Sub 中 `doSomething()`方法在运行时被错误地执行了两次。第一次调用是发生在`CloneExample.clone()`,另一次是在\
      \ `Sub.clone()`。因此，cookies 的值并没有被初始化，而域名却初始化了两次。   \n    ```java\n    class\
      \ CloneExample implements Cloneable {\n        HttpCookie[] cookies;\n    \n\
      \        CloneExample(HttpCookie[] c) {\n            cookies = c;\n        }\n\
      \    \n        public Object clone() throws CloneNotSupportedException {\n \
      \           final CloneExample clone = (CloneExample) super.clone();\n     \
      \       clone.doSomething(); // Invokes overridable method\n            clone.cookies\
      \ = clone.deepCopy();\n            return clone;\n        }\n    \n        void\
      \ doSomething() { // Overridable\n            for (int i = 0; i < cookies.length;\
      \ i++) {\n                cookies[i].setValue(\"\" + i);\n            }\n  \
      \      }\n    \n        HttpCookie[] deepCopy() {\n            if (cookies ==\
      \ null) {\n                throw new NullPointerException();\n            }\n\
      \            // deep copy\n            HttpCookie[] cookiesCopy = new HttpCookie[cookies.length];\n\
      \            for (int i = 0; i < cookies.length; i++) {\n                //\
      \ Manually create a copy of each element in array\n                cookiesCopy[i]\
      \ = (HttpCookie) cookies[i].clone();\n            }\n            return cookiesCopy;\n\
      \        }\n    }\n    \n    class Sub extends CloneExample {\n        Sub(HttpCookie[]\
      \ c) {\n            super(c);\n        }\n    \n        public Object clone()\
      \ throws CloneNotSupportedException {\n            final Sub clone = (Sub) super.clone();\n\
      \            clone.doSomething();\n            return clone;\n        }\n  \
      \  \n        void doSomething() { // Erroneously executed\n            for (int\
      \ i = 0; i < cookies.length; i++) {\n                cookies[i].setDomain(i\
      \ + \".foo.com\");\n            }\n        }\n    \n        public static void\
      \ main(String[] args) throws CloneNotSupportedException {\n            HttpCookie[]\
      \ hc = new HttpCookie[20];\n            for (int i = 0; i < hc.length; i++)\
      \ {\n                hc[i] = new HttpCookie(\"cookie\" + i, \"\" + i);\n   \
      \         }\n            CloneExample bc = new Sub(hc);\n            bc.clone();\n\
      \        }\n    }\n    ```   \n      \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: MET06-J:Do not invoke overridable methods in clone()
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160056
    language: Java
  - Advice: "**规范的代码样例1（安全策略）**：  \n```java\nclass ExceptionExample {\n  public static\
      \ void main(String[] args) {\n\n    File file = null;\n    try {\n      file\
      \ = new File(System.getenv(\"APPDATA\") +\n             args[0]).getCanonicalFile();\n\
      \      if (!file.getPath().startsWith(\"c:\\\\homepath\")) {\n        System.out.println(\"\
      Invalid file\");\n        return;\n      }\n    } catch (IOException x) {\n\
      \      System.out.println(\"Invalid file\");\n      return;\n    }\n\n    try\
      \ {\n      FileInputStream fis = new FileInputStream(file);\n    } catch (FileNotFoundException\
      \ x) {\n      System.out.println(\"Invalid file\");\n      return;\n    }\n\
      \  }\n}\n```\n这个符合规范的方案实现了一个安全策略，用户只能打开`c:\\homepath`中的文件，并且不允许用户发现这个目录之外的文件。当文件不能打开或不在正确的目录内时，这个方案返回一个简洁的错误信息。`c:\\\
      homepath`目录外文件的任何信息都被屏蔽了。\n这个方案还使用了`File.getCanonicalFile()`方法对文件进行标准化，简化了之后的对路径名进行比较的过程（参见规则IDS02-J）。\
      \  \n**规范的代码样例2（输入限制）**：  \n```java\nclass ExceptionExample {\n  public static\
      \ void main(String[] args) {\n    FileInputStream fis = null;\n    try {\n \
      \     switch(Integer.valueOf(args[0])) {\n        case 1:\n          fis = new\
      \ FileInputStream(\"c:\\\\homepath\\\\file1\");\n          break;\n        case\
      \ 2:\n          fis = new FileInputStream(\"c:\\\\homepath\\\\file2\");\n  \
      \        break;\n        //...\n        default:\n          System.out.println(\"\
      Invalid option\");\n          break;\n      }\n    } catch (Throwable t) {\n\
      \      MyExceptionReporter.report(t); // Sanitize\n    }\n  }\n}\n```\n这个符合规范的方案操作策略是用户只能打开\
      \ `c:\\homepath\\file1` 和 `c:\\homepath\\file2`。"
    Advice_En: "**Compliant Solution (Security Policy)**\n\nThis compliant solution\
      \ implements the policy that only files that live in `c:\\homepath` may be opened\
      \ by the user and that the user is not allowed to discover anything about files\
      \ outside this directory. The solution issues a terse error message when the\
      \ file cannot be opened or the file does not live in the proper directory. Any\
      \ information about files outside `c:\\homepath` is concealed.\n\nThe compliant\
      \ solution also uses the `File.getCanonicalFile()` method to canonicalize the\
      \ file to simplify subsequent path name comparisons (see FIO16-J. Canonicalize\
      \ path names before validating them for more information).\n\n```java\nclass\
      \ ExceptionExample {\n  public static void main(String[] args) {\n\n    File\
      \ file = null;\n    try {\n      file = new File(System.getenv(\"APPDATA\")\
      \ +\n             args[0]).getCanonicalFile();\n      if (!file.getPath().startsWith(\"\
      c:\\\\homepath\")) {\n        System.out.println(\"Invalid file\");\n      \
      \  return;\n      }\n    } catch (IOException x) {\n      System.out.println(\"\
      Invalid file\");\n      return;\n    }\n\n    try {\n      FileInputStream fis\
      \ = new FileInputStream(file);\n    } catch (FileNotFoundException x) {\n  \
      \    System.out.println(\"Invalid file\");\n      return;\n    }\n  }\n}\n```\n\
      \n**Compliant Solution (Restricted Input)**\n\nThis compliant solution operates\
      \ under the policy that only `c:\\homepath\\file1` and `c:\\homepath\\file2`\
      \ are permitted to be opened by the user. It also catches `Throwable`, as permitted\
      \ by exception ERR08-J-EX2** **(see ERR08-J. Do not catch NullPointerException\
      \ or any of its ancestors). It uses the `MyExceptionReporter` class described\
      \ in ERR00-J. Do not suppress or ignore checked exceptions, which filters sensitive\
      \ information from any resulting exceptions.\n\n```java\nclass ExceptionExample\
      \ {\n  public static void main(String[] args) {\n    FileInputStream fis = null;\n\
      \    try {\n      switch(Integer.valueOf(args[0])) {\n        case 1:\n    \
      \      fis = new FileInputStream(\"c:\\\\homepath\\\\file1\");\n          break;\n\
      \        case 2:\n          fis = new FileInputStream(\"c:\\\\homepath\\\\file2\"\
      );\n          break;\n        //...\n        default:\n          System.out.println(\"\
      Invalid option\");\n          break;\n      }\n    } catch (Throwable t) {\n\
      \      MyExceptionReporter.report(t); // Sanitize\n    }\n  }\n}\n```\n\nCompliant\
      \ solutions must ensure that security exceptions such as `java.security.AccessControlException`\
      \ and `java.lang.SecurityException` continue to be logged and sanitized appropriately\
      \ (see ERR02-J. Prevent exceptions while logging data for additional information).\
      \ The `MyExceptionReporter` class from ERR00-J. Do not suppress or ignore checked\
      \ exceptions demonstrates an acceptable approach for this logging and sanitization.\n\
      \nFor scalability, the `switch` statement should be replaced with some sort\
      \ of mapping from integers to valid file names or at least an enum type representing\
      \ valid files."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 07. 异常行为 (ERR)
      - type: DetailClassChin
        value: ERR01-J：不能允许异常泄露敏感信息
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 07. Exceptional Behavior (ERR)
      - type: DetailClassChin
        value: ERR01-J:Do not allow exceptions to expose sensitive information
    Default: true
    Description: "在传播异常时未能过滤敏感信息，往往会导致信息泄露，从而帮助攻击者开发进一步的漏洞。攻击者可能会精心设计输入参数以暴露应用程序的内部结构和机制。异常信息文本和异常的类型都会泄露信息。例如，\"\
      FileNotFoundException \"消息揭示了关于文件系统布局的信息，而异常类型则揭示了所请求的文件的缺失。\n这个规则适用于服务器端的应用程序，也适用于客户端。攻击者不仅可以从易受攻击的网络服务器中收集敏感信息，还可以从使用易受攻击的网络浏览器的受害者那里收集敏感信息。2004年，Schönefeld发现了一个针对Opera\
      \ v7.54网页浏览器的[漏洞]，攻击者可以使用小程序中的`sun.security.krb5.Credentials`类作为神谕，\"从抛出的`java.security.AccessControlException`所提供的信息中获取当前登录用户的名称并解析其主目录\"\
      \ [Schönefeld 2004]\n所有的异常都会透露出一些信息，这些信息可以帮助攻击者对系统进行拒绝服务的努力。因此，程序必须过滤异常信息和可以跨越信任边界传播的异常类型。下表列出了几种有问题的异常。\n\
      \n| Exception Name | Description of Information Leak or Threat |\n| - | - |\n\
      | `java.io.FileNotFoundException` | Underlying file system structure, user name\
      \ enumeration |\n| `java.sql.SQLException` | Database structure, user name enumeration\
      \ |\n| `java.net.BindException` | Enumeration of open ports when untrusted client\
      \ can choose server port |\n| `java.util.ConcurrentModificationException` |\
      \ May provide information about thread-unsafe code |\n| `javax.naming.InsufficientResourcesException`\
      \ | Insufficient server resources (may aid DoS) |\n| `java.util.MissingResourceException`\
      \ | Resource enumeration |\n| `java.util.jar.JarException` | Underlying file\
      \ system structure |\n| `java.security.acl.NotOwnerException` | Owner enumeration\
      \ |\n| `java.lang.OutOfMemoryError` | DoS |\n| `java.lang.StackOverflowError`\
      \ | DoS |\n\n打印堆栈跟踪也会导致无意中把进程的结构和状态的信息泄露给攻击者。当一个在控制台中运行的Java程序由于未捕获的异常而终止时，异常的信息和堆栈跟踪会显示在控制台中；堆栈跟踪本身可能包含关于程序内部结构的敏感信息。因此，任何可能在未受信任的用户可访问的控制台中运行的程序都不能因为未捕获的异常而终止。\n\
      **不规范的代码样例1（异常信息和类型引起的泄露）**：\n在这个不合规范的代码示例中：如果要求的文件不存在，将会抛出FileNotFoundException异常，通过不断的传递虚构的路径给程序，攻击者可以重建内部文件系统结构。\n\
      ```java\nclass ExceptionExample {\n  public static void main(String[] args)\
      \ throws FileNotFoundException {\n    // Linux stores a user's home directory\
      \ path in\n    // the environment variable $HOME, Windows in %APPDATA%\n   \
      \ FileInputStream fis =\n        new FileInputStream(System.getenv(\"APPDATA\"\
      ) + args[0]);\n  }\n}\n```\n当请求的文件不存在时，`FileInputStream`构造函数抛出一个`FileNotFoundException`，允许攻击者通过反复向程序传递虚构的路径名来重建底层文件系统。\n\
      **不规范的代码样例2（封装和重新抛出敏感信息）**：\n在这个不合规范的代码示例将异常记入日志，并封装了一个更普通的异常抛出，但这仍然有助于攻击者发现文件系统布局的信息。\n\
      ```java\ntry {\n  FileInputStream fis =\n      new FileInputStream(System.getenv(\"\
      APPDATA\") + args[0]);\n} catch (FileNotFoundException e) {\n  // Log the exception\n\
      \  throw new IOException(\"Unable to retrieve file\", e);\n}\n```\n即使用户无法访问记录的异常，原始异常仍然可以提供信息，攻击者可以使用它来发现有关文件系统布局的敏感信息。\n\
      注意这个例子也违反了[FIO04-J. 在不再需要资源时释放资源]，因为它无法在“finally”块中关闭输入流。 为简洁起见，后续代码示例也省略了这个“finally”块。\n\
      **不规范的代码样例3（异常过滤）**：\n在这个不合规范的代码示例将异常记入日志，抛出一个自定义的异常，并没有将FileNotFoundException异常封装在其中。虽然这个异常不能直接泄露有用的异常，但是它还是对无法读取的文件进行了提示。具体来说，这个程序对不存在的路径和存在的路径所作出的反应是不一样的，这使得攻击者仍然能从程序行为推导出文件系统的敏感信息。没能限制用户的输入让系统容易遭受穷举攻击，攻击者通过不停查询可能的文件名，可以发现有效的文件名。造成程序返回异常的文件名表明了该文件不存在，而未返回异常则暴漏了已有的文件。\n\
      ```java\nclass SecurityIOException extends IOException {/* ... */};\n\ntry {\n\
      \  FileInputStream fis =\n      new FileInputStream(System.getenv(\"APPDATA\"\
      ) + args[0]);\n} catch (FileNotFoundException e) {\n  // Log the exception\n\
      \  throw new SecurityIOException();\n}\n```"
    Description_En: "Failure to filter sensitive information when propagating exceptions\
      \ often results in information leaks that can assist an attacker's efforts to\
      \ develop further exploits. An attacker may craft input arguments to expose\
      \ internal structures and mechanisms of the application. Both the exception\
      \ message text and the type of an exception can leak information. For example,\
      \ the `FileNotFoundException` message reveals information about the file system\
      \ layout, and the exception type reveals the absence of the requested file.\n\
      \nThis rule applies to server-side applications as well as to clients. Attackers\
      \ can glean sensitive information not only from vulnerable web servers but also\
      \ from victims who use vulnerable web browsers. In 2004, Schönefeld discovered\
      \ an exploit for the Opera v7.54 web browser in which an attacker could use\
      \ the `sun.security.krb5.Credentials` class in an applet as an oracle to \"\
      retrieve the name of the currently logged in user and parse his home directory\
      \ from the information which is provided by the thrown `java.security.AccessControlException`\"\
      \ [Schönefeld 2004].\n\nAll exceptions reveal information that can assist an\
      \ attacker's efforts to carry out a denial of service (DoS) against the system.\
      \ Consequently, programs must filter both exception messages and exception types\
      \ that can propagate across trust boundaries. The following table lists several\
      \ problematic exceptions.\n\n| Exception Name | Description of Information Leak\
      \ or Threat |\n| - | - |\n| `java.io.FileNotFoundException` | Underlying file\
      \ system structure, user name enumeration |\n| `java.sql.SQLException` | Database\
      \ structure, user name enumeration |\n| `java.net.BindException` | Enumeration\
      \ of open ports when untrusted client can choose server port |\n| `java.util.ConcurrentModificationException`\
      \ | May provide information about thread-unsafe code |\n| `javax.naming.InsufficientResourcesException`\
      \ | Insufficient server resources (may aid DoS) |\n| `java.util.MissingResourceException`\
      \ | Resource enumeration |\n| `java.util.jar.JarException` | Underlying file\
      \ system structure |\n| `java.security.acl.NotOwnerException` | Owner enumeration\
      \ |\n| `java.lang.OutOfMemoryError` | DoS |\n| `java.lang.StackOverflowError`\
      \ | DoS |\n\nPrinting the stack trace can also result in unintentionally leaking\
      \ information about the structure and state of the process to an attacker. When\
      \ a Java program that is run within a console terminates because of an uncaught\
      \ exception, the exception's message and stack trace are displayed on the console;\
      \ the stack trace may itself contain sensitive information about the program's\
      \ internal structure. Consequently, any program that may be run on a console\
      \ accessible to an untrusted user must never abort due to an uncaught exception.\n\
      \n**Noncompliant Code Example (Leaks from Exception Message and Type)**\n\n\
      In this noncompliant code example, the program must read a file supplied by\
      \ the user, but the contents and layout of the file system are sensitive. The\
      \ program accepts a file name as an input argument but fails to prevent any\
      \ resulting exceptions from being presented to the user.\n\n```java\nclass ExceptionExample\
      \ {\n  public static void main(String[] args) throws FileNotFoundException {\n\
      \    // Linux stores a user's home directory path in\n    // the environment\
      \ variable $HOME, Windows in %APPDATA%\n    FileInputStream fis =\n        new\
      \ FileInputStream(System.getenv(\"APPDATA\") + args[0]);\n  }\n}\n```\n\nWhen\
      \ a requested file is absent, the `FileInputStream` constructor throws a `FileNotFoundException`,\
      \ allowing an attacker to reconstruct the underlying file system by repeatedly\
      \ passing fictitious path names to the program.\n\n**Noncompliant Code Example\
      \ (Wrapping and Rethrowing Sensitive Exception)**\n\nThis noncompliant code\
      \ example logs the exception and then wraps it in a more general exception before\
      \ rethrowing it:\n\n```java\ntry {\n  FileInputStream fis =\n      new FileInputStream(System.getenv(\"\
      APPDATA\") + args[0]);\n} catch (FileNotFoundException e) {\n  // Log the exception\n\
      \  throw new IOException(\"Unable to retrieve file\", e);\n}\n```\n\nEven when\
      \ the logged exception is not accessible to the user, the original exception\
      \ is still informative and can be used by an attacker to discover sensitive\
      \ information about the file system layout.\nNote that this example also violates\
      \ FIO04-J. Release resources when they are no longer needed, as it fails to\
      \ close the input stream in a `finally` block. Subsequent code examples also\
      \ omit this `finally` block for brevity.\n\n**Noncompliant Code Example (Sanitized\
      \ Exception)**\n\nThis noncompliant code example logs the exception and throws\
      \ a custom exception that does not wrap the `FileNotFoundException`:\n\n```java\n\
      class SecurityIOException extends IOException {/* ... */};\n\ntry {\n  FileInputStream\
      \ fis =\n      new FileInputStream(System.getenv(\"APPDATA\") + args[0]);\n\
      } catch (FileNotFoundException e) {\n  // Log the exception\n  throw new SecurityIOException();\n\
      }\n```\n\nAlthough this exception is less likely than the previous noncompliant\
      \ code examples to leak useful information, it still reveals that the specified\
      \ file cannot be read. More specifically, the program reacts differently to\
      \ nonexistent file paths than it does to valid ones, and an attacker can still\
      \ infer sensitive information about the file system from this program's behavior.\
      \ Failure to restrict user input leaves the system vulnerable to a brute-force\
      \ attack in which the attacker discovers valid file names by issuing queries\
      \ that collectively cover the space of possible file names. File names that\
      \ cause the program to return the sanitized exception indicate nonexistent files,\
      \ whereas file names that do not return exceptions reveal existing files."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 600:Uncaught Exception in Servlet
      - type: CWE
        value: 431:Missing Handler
      - type: CWE
        value: 248:Uncaught Exception
      - type: CWE
        value: 209:Information Exposure Through an Error Message
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 209
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 209
      - type: GB/T 34944-2017 Java
        value: 数据处理:信息通过错误消息泄露
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: true
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140801
    language: Java
  - Advice: "\n3) 示例   \n  \n**例 1** 如下不符合规则的代码示例中，程序需要读取一个用户提供的文件，但是文件系统的内容和布局是敏感的。这个程序接受一个文件名作为参数，却没能防止潜在的异常会将敏感信息展现给用户。\
      \   \n```java\nclass ExceptionExample {   \n  public static void main(String[]\
      \ args) throws FileNotFoundException {   \n    // Linux stores a user's home\
      \ directory path in   \n    // the environment variable $HOME, Windows in %APPDATA%\
      \   \n    FilelnputStream fis =  new FilelnputStream(System.getenv(\"APPDATA\"\
      ) + args[0]);   \n  }   \n}\n```   \n\n**例 2** 如下不符合规则的代码示例将异常记入日志，并在重新抛出前将其封装在一个更普通的异常中。\
      \   \n```java\ntry {   \n    FileInputStream fis =  new FilelnputStream(System.getenv(\"\
      APPDATA\") + args[0]；   \n} catch (FileNotFoundException e) {   \n    // Log\
      \ the exception   \n    throw new IOException(\"Unable to retrieve file\", e);\
      \   \n}\n```   \n\n**例 3** 如下不符合规则的代码示例将异常记入日志，抛出一个自定义的异常，并没有将FileNotFoundException\
      \ 异常封装在其中。   \n```java\nclass SecuritylOException extends IOException {/* ...\
      \ */};    \ntry {   \n    FilelnputStream fis =   new FilelnputStream(System.getenv(\"\
      APPDATA\") + args[0]);   \n} catch (FileNotFoundException e) {   \n    // Log\
      \ the exception   \n    throw new SecurityIOException();   \n}\n```   \n   \n\
      对应于上述三个不符合规则的方案可以采用下述符合规则的方案，该方案实现了一个安全策略，用户只能打开 `c:\\homepath` 中的文件，并且不允许用户发现这个目录之外的文件。当文件不能打开或不在正确的目录内时，返回一个简洁的错误信息。`c:\\\
      homepath` 目录外文件的任何信息都被屏蔽了。   \n```java\nclass ExceptionExample {   \n    public\
      \ static void main(String[] args) {   \n        File file = null;    \n    \
      \    try {   \n            file = new File(System.getenv(\"APPDATA\") + args[0])\
      \ .getCanonicalFile ();    \n            if (!file.getPath().startsWith(\"c:Whomepath\"\
      )) {   \n                System.out.printIn(\"Invalid file\"); return;   \n\
      \            }   \n        } catch (IOException x) {   \n            System.out.printIn(\"\
      Invalid file\");    \n            return;   \n        }   \n        try {  \
      \ \n            FilelnputStream fis = new FilelnputStream(file);    \n     \
      \   } catch (FileNotFoundException x) {   \n            System.out.printIn(\"\
      Invalid file\"); return;   \n        }   \n    }   \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.4 错误处理安全
      - type: DetailClassChin
        value: C.7.2 不能允许异常泄露敏感信息
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n在异常传递的过程中，必须对敏感信息进行过滤，防止敏感信息泄露。   \n2) 原理   \n在异常传递的过程中，如果不对敏感信息进行过滤，常会导致信息泄露，这将有助于攻击者实现对系统的攻击。一个攻击者可以通过精心设计的输入参数来得到应用程序的内部数据结构或机制。异常的文本和异常的类型都可以泄漏信息。\
      \   \n这条规则同时适用于服务器端和客户端应用。攻击者不单只可以从易受攻击的网络服务器收集敏感信息，也可以从使用这些网络服务器的用户网络浏览器那里得到敏感信息。\
      \   \n所有异常都能提供信息，这会帮助攻击者完成对系统的拒绝服务（Dos）攻击。因此，程序员必须过滤能传递出可信任边界的异常信息和异常类型。   "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 600:Uncaught Exception in Servlet
      - type: CWE
        value: 431:Missing Handler
      - type: CWE
        value: 248:Uncaught Exception
      - type: CWE
        value: 209:Information Exposure Through an Error Message
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 209
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 209
      - type: GB/T 34944-2017 Java
        value: 数据处理:信息通过错误消息泄露
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160064
    language: Java
  - Advice: "检查程序逻辑，如果需要获取数组的hash值，可以通过遍历数组中每个对象综合获取，或者使用Java工具类Arrays中的`hashCode`方法获取。\
      \  \n  \n**例1**：采用循环的方式获取其中的值形成字符串。  \n```java\nString[] params = getParams();\
      \  \nint hashcode = 0;  \nif (params == null)  \n    hashcode = 0;  \nhashcode=\
      \ 1;  \nfor (String param: params )  \n    hashcode= 31 * hashcode+ (param==\
      \ null ? 0 : param.hashCode());\n```  \n**例2**：直接使用Java工具类Arrays中的`toString`方法转化数组为字符串。\
      \  \n```java\nString[] params = getParams();  \nint hashcode = Arrays.hashCode(strings);\n\
      ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【强制】不要在数组上调用hashCode()
    Categories_En:
      Category: []
    Default: true
    Description: 数组类中并没有对`hashCode`方法进行重写，在数组上调用`hashCode()`默认调用的是Object中的`hashCode`方法，返回的是内存地址哈希码。
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150116'
    language: Java
  - Advice: "**规范的代码样例1**：  \n```java\nprivate static final SomeType [] SOMETHINGS\
      \ = { ... };  \npublic static final SomeType [] somethings() {  \n    return\
      \ SOMETHINGS.clone();  \n}\n```  \n在上面规范的代码中，数组的值不能被客户端修改。  \n**规范的代码样例2**：\
      \  \n```java\nprivate static final SomeType [] THE_THINGS = { ... };  \npublic\
      \ static final List<SomeType> SOMETHINGS =  \n    Collections.unmodifiableList(Arrays.asList(THE_THINGS));\
      \ \n```\n在上面规范的代码中，数组的值和公有的List都不能被客户端修改。"
    Advice_En: "**Compliant Solution (clone)**\n\nContinuing with the assumption that\
      \ `SomeType` is immutable, one approach is to have a private array and a public\
      \ method that returns a copy of the array:\n\n```java\nprivate static final\
      \ SomeType [] SOMETHINGS = { ... };\npublic static final SomeType [] somethings()\
      \ {\n  return SOMETHINGS.clone();\n}\n```\n\nNow, the original array cannot\
      \ be modified by any client.  If `SomeType` were mutable, this approach would\
      \ not be effective because the array clone references the same `SomeType` objects\
      \ as the `SOMETHINGS` array. If the client modified the clone `SomeType` objects\
      \ directly, the `SomeType` objects referenced by the `SOMETHINGS` array would\
      \ also change.\n\n**Compliant Solution (Unmodifiable List)**\n\nContinuing with\
      \ the assumption that `SomeType` is immutable, an alternative approach is to\
      \ have a private array from which a public immutable list is constructed:\n\n\
      ```java\nprivate static final SomeType [] THE_THINGS = { ... };\npublic static\
      \ final List<SomeType> SOMETHINGS =\n  Collections.unmodifiableList(Arrays.asList(THE_THINGS));\n\
      ```\n\nNow, neither the original array values nor the public list can be modified\
      \ by a client. If `SomeType` were mutable, this would not be effective because\
      \ the list references the same `SomeType` objects as the `SOMETHINGS` array.\
      \ The `unmodifiabileList` prevents the list from being modified, not the elements\
      \ in the list. If the client modified the list's `SomeType` objects directly,\
      \ the `SomeType` objects referenced by the `SOMETHINGS` array would also change."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 05. 面向对象 (OBJ)
      - type: DetailClassChin
        value: OBJ13-J：确保可变对象的引用没有被泄露
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 05. Object Orientation (OBJ)
      - type: DetailClassChin
        value: OBJ13-J:Ensure that references to mutable objects are not exposed
    Default: true
    Description: "不要将可变对象的引用暴露给客户端代码。永远不要将这样的字段初始化到客户端提供的对象引用，或者从访问器返回对象引用。暴露一个公共静态final对象允许客户修改对象的内容(尽管它们不能改变对象本身，因为它是final的)。\
      \  \n此规则不处理私有可变对象，请参阅规则OBJ05-J。有关详细信息，请不要返回对私有可变类成员的引用。\n**不规范的代码样例1**：\n假设\
      \ `SomeType` 是不可变的。\n```java\npublic static final SomeType [] SOMETHINGS = {\
      \ ... };\n```\n即使 `SomeType` 是不可变的，此声明也允许 SOMETHINGS 数组也可被代码的不受信任的客户端修改。 可以为数组的任何元素分配一个新值，即对新\
      \  `SomeType`  对象的引用。\n这个不合规的代码示例也违反了 OBJ01-J.限制字段的可访问性。\n**不规范的代码样例2（getter方法）**\n\
      此不合规代码示例符合 OBJ01-J.通过将数组声明为私有来限制字段的可访问性。 但是，在声明数组私有时，此代码示例违反了 OBJ05-J.不要返回对私有可变类成员的引用。\n\
      假设 SomeType 是不可变的。\n```java\nprivate static final SomeType [] SOMETHINGS = {\
      \ ... };\npublic static final getSomethings() {return SOMETHINGS;}\n```\n即使\
      \ `SomeType` 是不可变的，公共 getter 方法也允许不受信任的客户端修改 `SOMETHINGS` 数组。 可以为数组的任何元素分配一个新值，即对新的\
      \ `SomeType` 对象的引用。"
    Description_En: 'Do not expose references to mutable objects to client code. Never
      initialize such a field to a client-provided object reference or return the
      object reference from an accessor. Exposing a public static final object allows
      clients to modify the contents of the object (although they will not be able
      to change the object itself, as it is final).


      This rule does not address private mutable objects, see rule OBJ05-J. Do not
      return references to private mutable class members for more information.


      **Noncompliant Code Example**


      Suppose that `SomeType` is immutable.


      ```java

      public static final SomeType [] SOMETHINGS = { ... };

      ```


      Even though `SomeType` is immutable, this declaration allows the `SOMETHINGS`
      array to be modified by untrusted clients of the code. Any element of the array
      can be assigned a new value, namely a reference to a new `SomeType` object.


      This noncompliant code example also violates OBJ01-J. Limit accessibility of
      fields.


      **Noncompliant Code Example (getter method)**


      This noncompliant code example complies with OBJ01-J. Limit accessibility of
      fields by declaring the array private. But, in declaring the array private,
      this code example violates OBJ05-J. Do not return references to private mutable
      class members.


      Suppose that `SomeType` is immutable.


      ```java

      private static final SomeType [] SOMETHINGS = { ... };

      public static final getSomethings() {return SOMETHINGS;}

      ```


      Even though `SomeType` is immutable, the public getter method enables untrusted
      clients to modify the `SOMETHINGS` array. Any element of the array can be assigned
      a new value, namely a reference to a new `SomeType` object.'
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140613'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nprivate static final SomeType\
      \ [] SOMETHINGS = { ... };  \npublic static final SomeType [] somethings() {\
      \  \n    return SOMETHINGS.clone();  \n}\n```  \n在上面规范的代码中，数组的值不能被客户端修改。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.1 面向对象程序安全
      - type: DetailClassChin
        value: A.3.1.3 确保可变对象的引用没有被泄露
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：不要将可变对象的引用暴露给客户端代码。永远不要可变对象字段初始化到客户端提供的对象引用，或者从访问器返回对象引用。暴露一个公共静态final对象允许客户修改对象的内容(尽管它们不能改变对象本身，因为它是final的)。\
      \  \n对于确保可变对象的引用没有被泄露的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\npublic\
      \ static final SomeType [] SOMETHINGS = { ... };\n``` \n不规范的代码中， `SOMETHINGS`中数组的值可以被客户端的代码修改。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: 0302000010170029
    language: Java
  - Advice: "为了避免存储型XSS攻击，建议采用以下方式进行防御：  \n1. 与预防反射型XSS相同，对用户的输入进行合理验证（如年龄只能是数字），对特殊字符（如`<`、`>`、`'`、`\"\
      `以及`<script>`、`javascript`等进行过滤。  \n2. 与预防反射型XSS相同，根据数据将要置于HTML上下文中的不同位置（HTML标签、HTML属性、JavaScript脚本、CSS、URL），对所有不可信数据进行恰当的输出编码。\
      \  \n3. 与预防反射型XSS相同，设置HttpOnly属性。"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.1 数据清洗
      - type: DetailClassChin
        value: A.2.1.2.4 预防存储型XSS
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：应用程序从数据库或其它后端数据存储获取不可信赖的数据，在未检验数据是否存在恶意代码的情况下，便将其传送给了Web用户，应用程序将易于受到存储型XSS攻击。\
      \  \n对于预防存储型XSS的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**： \n```java \n<%  \nStatement\
      \ stmt = conn.createStatement();  \nResultSet rs = stmt.executeQuery(\"select\
      \ * from users where id =\" + id);  \nString address = null;  \nif (rs != null)\
      \ {  \n    rs.next();  \n    address = rs.getString(\"address\");  \n}%>  \n\
      家庭地址: <%= address %>  \n```\n上面JSP代码片段的功能是根据一个已知雇员ID（eid）从数据库中查询出该雇员的姓名，并显示在JSP页面上。\
      \  \n如果name的值是由用户提供的，且存入数据库时没有进行合理的校验，那么攻击者就可以利用上面的代码进行存储型XSS攻击。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 83:Improper Neutralization of Script in Attributes in a Web Page
      - type: CWE
        value: 82:Improper Neutralization of Script in Attributes of IMG Tags in a
          Web Page
      - type: CWE
        value: 81:Improper Neutralization of Script in an Error Message Web Page
      - type: CWE
        value: 80:Improper Neutralization of Script-Related HTML Tags in a Web Page
          (Basic XSS)
      - type: CWE
        value: 79:Improper Neutralization of Input During Web Page Generation ('Cross-site
          Scripting')
      - type: OWASP TOP 10
        value: 2017:A7 Cross-Site Scripting (XSS)
      - type: OWASP TOP 10
        value: 2013:A3 Cross-Site Scripting (XSS)
      - type: OWASP TOP 10
        value: 2010:A2 Cross-Site Scripting (XSS)
      - type: OWASP TOP 10
        value: 2007:A1 Cross Site Scripting (XSS)
      - type: OWASP TOP 10
        value: 2004:A4 Cross Site Scripting
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 79
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 79
      - type: CWE/SANS TOP 25
        value: 2011:Insecure Interaction - CWE ID 79
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 79
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 79
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Cross-site Scripting [XYT]
      - type: GB/T 34944-2017 Java
        value: Web问题:跨站脚本
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170010'
    language: Java
  - Advice: 标识符中建议不要使用美元符号（$）。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】变量名中不要含有$字符
    Categories_En:
      Category: []
    Default: true
    Description: "根据Java语言规范第三版的3.8节标识符的定义（部分）:  \n>1. 标识符是Java字母和Java数字的不限长序列，必须以Java字母开头。\
      \  \n>2. Java字母包括大写和小写的ASCII拉丁字母`A〜Z (\\u0041〜\\u005a)`和`a〜z (\\u0061〜\\u007a)`，并且出于历史的原因，还包括ASCII下划线（_或\\\
      u005f)和美元符号($或\\0024)。应当仅在机器生成的源代码中使用$字符，或者仅用它来很少地访问遗留系统上预先存在的名称。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Identifier Contains Dollar Symbol ($)
    ENSubClassChin: Poor Style
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: 等级计算
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150421'
    language: Java
  - Advice: "**规范的代码样例1**：\n这个解决方案正确地使用了 `start()` 方法来告诉 Java 运行时启动一个新线程：\n```java\n\
      public final class Foo implements Runnable {\n  @Override\n  public void run()\
      \ {\n    // ...\n  }\n\n  public static void main(String[] args) {\n    Foo\
      \ foo = new Foo();\n    new Thread(foo).start();\n  }\n}\n```\n**例外**\nTHI00-J-EX0：`run()`\
      \ 方法可以在单元测试期间直接调用。 请注意，此方法不能用于测试类以供多线程使用。\n给定一个使用可运行参数构造的 Thread 对象，在调用 Thread.run()\
      \ 方法时，可以将 Thread 对象强制转换为 Runnable 以消除分析器诊断：\n```java\npublic void sampleRunTest()\
      \ {\n\n  Thread thread = new Thread(new Runnable() {\n      @Override public\
      \ void run() {\n        // ...\n      }\n    });\n\n  ((Runnable) thread).run();\
      \  // THI00-J-EX0: Does not start a new thread\n\n}\n```\n在调用 `run()` 方法之前将线程强制转换为\
      \ `Runnable` 表明对 `Thread.run()` 的显式调用是有意的。 强烈建议在调用旁边添加解释性注释。\nTHI00-J-EX1：允许启动新线程所涉及的运行时系统代码直接调用\
      \ `Thread` 对象的 `run()` 方法； 这对于一个正常工作的 Java 运行时系统来说显然是必要的。 请注意，此异常适用于用户编写的代码的可能性非常小。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution correctly uses the\
      \ `start()` method to tell the Java runtime to start a new thread:\n\n```java\n\
      public final class Foo implements Runnable {\n  @Override public void run()\
      \ {\n    // ...\n  }\n\n  public static void main(String[] args) {\n    Foo\
      \ foo = new Foo();\n    new Thread(foo).start();\n  }\n}\n```\n**Exceptions**\n\
      \nTHI00-J-EX0: The `run()` method may be directly invoked during unit testing.\
      \ Note that this method cannot be used to test a class for multithreaded use.\n\
      \nGiven a `Thread` object that was constructed with a runnable argument, when\
      \ invoking the `Thread.run()` method, the `Thread` object may be cast to `Runnable`\
      \ to eliminate analyzer diagnostics:\n\n```java\npublic void sampleRunTest()\
      \ {\n\n  Thread thread = new Thread(new Runnable() {\n      @Override public\
      \ void run() {\n        // ...\n      }\n    });\n\n  ((Runnable) thread).run();\
      \  // THI00-J-EX0: Does not start a new thread\n\n}\n```\n\nCasting a thread\
      \ to `Runnable` before calling the `run()` method documents that the explicit\
      \ call to `Thread.run()` is intentional. Adding an explanatory comment alongside\
      \ the invocation is highly recommended.\n\nTHI00-J-EX1: Runtime system code\
      \ involved in starting new threads is permitted to invoke a `Thread` object's\
      \ `run()` method directly; this is an obvious necessity for a working Java runtime\
      \ system. Note that the likelihood that this exception applies to user-written\
      \ code is vanishingly small."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 10. 线程API (THI)
      - type: DetailClassChin
        value: THI00-J：不要调用Thread.run()
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 10. Thread APIs (THI)
      - type: DetailClassChin
        value: THI00-J:Do not invoke Thread.run()
    Default: true
    Description: "`Thread.start()`是以通知Java虚拟机调用该线程的`run()`方法的方式来启动线程，`start()`方法的结果是两个线程并发运行，当前线程（从调用返回给`start()`方法）和另一个线程（执行其`run()`方法）。\
      \  \n`Thread.run()`方法是在当前线程中执行`run()`方法，而不是创建一个新的线程执行，程序中依然只有主线程这一个线程，其程序执行路径还是只有一条，还是要顺序执行，还是要等待`run()`方法体执行完毕后才可继续执行下面的代码。\
      \  \n**不规范的代码样例1**：\n这个不合规的代码示例在当前线程的上下文中显式调用 `run()`：\n```java\npublic final\
      \ class Foo implements Runnable {\n  @Override\n  public void run() {\n    //\
      \ ...\n  }\n\n  public static void main(String[] args) {\n    Foo foo = new\
      \ Foo();\n    new Thread(foo).run();\n  }\n}\n```\n由于 `run()` 启动新线程的错误假设，新创建的线程永远不会启动。\
      \ 因此，`run()` 方法中的语句由当前线程执行，而不是由新线程执行。"
    Description_En: "Thread startup can be misleading because the code can appear\
      \ to be performing its function correctly when it is actually being executed\
      \ by the wrong thread.\nInvoking the `Thread.start()` method instructs the Java\
      \ runtime to start executing the thread's `run()` method using the started thread.\
      \ Invoking a `Thread` object's `run()` method directly is incorrect. When a\
      \ `Thread` object's `run()` method is invoked directly, the statements in the\
      \ `run()` method are executed by the current thread rather than by the newly\
      \ created thread. Furthermore, if the `Thread` object was constructed by instantiating\
      \ a subclass of `Thread` that fails to override the `run()` method rather than\
      \ constructed from a `Runnable` object, any calls to the subclass's `run()`\
      \ method would invoke `Thread.run()`, which does nothing. Consequently, programs\
      \ must not directly invoke a `Thread` object's `run()` method.\n\n**Noncompliant\
      \ Code Example**\n\nThis noncompliant code example explicitly invokes `run()`\
      \ in the context of the current thread:\n\n```java\npublic final class Foo implements\
      \ Runnable {\n  @Override public void run() {\n    // ...\n  }\n\n  public static\
      \ void main(String[] args) {\n    Foo foo = new Foo();\n    new Thread(foo).run();\n\
      \  }\n}\n```\n\nThe newly created thread is never started because of the incorrect\
      \ assumption that `run()` starts the new thread. Consequently, the statements\
      \ in the `run()` method are executed by the current thread rather than by the\
      \ new thread."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 572:Call to Thread run() instead of start()
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 6.8.3 显式调用Thread.run方法
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141100'
    language: Java
  - Advice: "调用线程的`start()`方法替代`run()`方法。  \n  \n**例如**：下面代码片段中使用`start()`方法来通知Java运行库开始执行新线程。\
      \  \n```java\npublic final class Foo implements Runnable {  \n    @Override\
      \ public void run() {  \n        // ...  \n    }  \n    public static void main(String[]\
      \ args) {  \n        Foo foo = new Foo();  \n        new Thread(foo).start();\
      \  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【强制】不要调用Thread.run()
    Categories_En:
      Category: []
    Default: true
    Description: "程序调用了线程的`run()`方法，而不是`start()`方法。   \n  \n**例如**：下面代码片段中，程序错误地调用了线程的`run()`方法，而不是`start()`方法。\
      \  \n```java\npublic final class Foo implements Runnable {  \n    @Override\
      \ public void run() {  \n        // ...  \n    }  \n    public static void main(String[]\
      \ args) {  \n        Foo foo = new Foo();  \n        new Thread(foo).run();\
      \  \n    }  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Call to Thread.run()
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 572:Call to Thread run() instead of start()
      - type: CERT
        value: THI00-J:Do not invoke Thread.run()
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 6.8.3 显式调用Thread.run方法
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: 0202000010150109
    language: Java
  - Advice: "对应符合规则的解决方案中，正确地使用`start()`方法来通知 Java 运行库开始执行新线程。   \n    ```java\n \
      \   public final class Foo implements Runnable {\n        @Override\n      \
      \  public void run() {\n            // . . .\n        }\n    \n        public\
      \ static void main(String[] args) {\n            Foo foo = new Foo();\n    \
      \        new Thread(foo).start();\n        }\n    }  \n    ```\n4) 特例   \n1、在进行单元测试时，可以直接调用`run()`方法的。注意，这个方法不能用来测试那些在多线程场景中应用的类。\
      \   \n2、在运行系统中，与启动新线程有关的代码是可以直接调用 Thread 对象的`run()`方法的。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.10.1 不要调用 Thread_run()
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序应禁止直接调用 `Thread.run()`方法。   \n2) 原理   \n调用 `Thread.start()`方法标志着\
      \ Java 运行库开始执行线程的`run()`方法，但直接调用一个 Thread 对象的`run()`方法是不正确的。如果直接调用 Thread 对象的`run()`方\
      \ 法，`run()`方法中的语句是由当前线程而不是由新创建的线程来执行的，而且当 Thread 对象是通过实例化一个没有覆写`run()`方法的 Thread\
      \ 子类来创建，而不是用 Runnable 对象来创建时，任何对子类中`run()`方法的调用都会调用 `Thread.run()`，这个方法不会做任何操作。因此，程序禁止直接调用\
      \ `Thread.run()`方法。   \n3) 示例   \n    一个不符合规则的代码示例，该示例在当前线程中直接调用`run()`。   \n\
      \    ```java\n    public final class Foo implements Runnable {\n        @Override\n\
      \        public void run() {\n            // . . .\n        }\n    \n      \
      \  public static void main(String[] args) {\n            Foo foo = new Foo();\n\
      \            new Thread(foo).run();\n        }\n    }\n    ```  \n    这里假设`run()`会启动新线程是错误的，因为这个新创建的线程并不会开始执行。因此，`run()`方法中的语句是由当前线程而不是新线程来执行的。\
      \   \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 572:Call to Thread run() instead of start()
      - type: CERT
        value: THI00-J:Do not invoke Thread.run()
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 6.8.3 显式调用Thread.run方法
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160091
    language: Java
  - Advice: 检查程序逻辑，如果确定程序中的死代码没有作用，应该将其删除。否则，应在该代码块中加入相应的代码。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 并发规范
      - type: DetailClassChin
        value: 【推荐】不要保留空的synchronized代码块
    Categories_En:
      Category: []
    Default: true
    Description: "空的同步代码块在程序中没有起到任何同步的作用，是程序中的死代码或者是被注释掉的调试代码。这些死代码会增加代码的阅读、理解和维护难度。\
      \  \n  \n**例如**：下列程序是一个空的synchronized代码块  \n```java\n...  \nsynchronized(object)\
      \ {       \n}  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Empty Synchronized Block
    ENSubClassChin: Poor Style
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 585:Empty Synchronized Block
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150417'
    language: Java
  - Advice: "**规范的代码样例1（净化用户）**：\n此解决方案在登录 `username` 之前进行净化，以防止注入攻击。\n```java\nif\
      \ (loginSuccessful) {\n  logger.severe(\"User login succeeded for: \" + sanitizeUser(username));\n\
      } else {\n  logger.severe(\"User login failed for: \" + sanitizeUser(username));\n\
      }\n```\n通过专用的用户名净化方法进行净化：\n```java\npublic String sanitizeUser(String username)\
      \ {\n  return Pattern.matches(\"[A-Za-z0-9_]+\", username))\n      ? username\
      \ : \"unauthorized user\";\n}\n```\n**规范的代码样例2（净化日志）**：\n此规范的代码示例使用文本记录器自动净化输入。\n\
      ```java\nLogger sanLogger = new SanitizedTextLogger(logger);\n\nif (loginSuccessful)\
      \ {\n  sanLogger.severe(\"User login succeeded for: \" + username);\n} else\
      \ {\n  sanLogger.severe(\"User login failed for: \" + username);\n}\n```\n经过净化的文本记录器将实际记录器作为代理。我们假设记录器将文本日志消息输出到文件、网络或控制台，并且每条日志消息都没有缩进行。净化文本记录器通过将除第一行之外的每一行缩进两个空格来净化所有要记录的文本。虽然恶意用户可以将文本缩进更多，但恶意用户不能创建假日志条目，因为除了真实的日志输出之外，她的所有输出都将缩进。\n\
      ```java\nclass SanitizedTextLogger extends Logger {\n  Logger delegate;\n\n\
      \  public SanitizedTextLogger(Logger delegate) {\n    super(delegate.getName(),\
      \ delegate.getResourceBundleName());\n    this.delegate = delegate;\n  }\n\n\
      \  public String sanitize(String msg) {\n    Pattern newline = Pattern.compile(\"\
      \\n\");\n    Matcher matcher = newline.matcher(msg);\n    return matcher.replaceAll(\"\
      \\n  \");\n  }\n\n  public void severe(String msg) {\n    delegate.severe(sanitize(msg));\n\
      \  }\n\n  // .. Other Logger methods which must also sanitize their log messages\n\
      }\n```"
    Advice_En: "**Compliant Solution (Sanitized User)**\n\nThis compliant solution\
      \ sanitizes the `username` before logging it, preventing injection attacks.\n\
      \n```java\nif (loginSuccessful) {\n  logger.severe(\"User login succeeded for:\
      \ \" + sanitizeUser(username));\n} else {\n  logger.severe(\"User login failed\
      \ for: \" + sanitizeUser(username));\n}\n```\n\nThe sanitization is done by\
      \ a dedicated method for sanitizing user names:\n\n```java\npublic String sanitizeUser(String\
      \ username) {\n  return Pattern.matches(\"[A-Za-z0-9_]+\", username))\n    \
      \  ? username : \"unauthorized user\";\n}\n```\n\n**Compliant Solution (Sanitized\
      \ Logger)**\n\nThis compliant solution uses a text logger that automatically\
      \ sanitizes its input. A sanitized logger saves the developer from having to\
      \ worry about unsanitized log messages.\n\n```java\nLogger sanLogger = new SanitizedTextLogger(logger);\n\
      \nif (loginSuccessful) {\n  sanLogger.severe(\"User login succeeded for: \"\
      \ + username);\n} else {\n  sanLogger.severe(\"User login failed for: \" + username);\n\
      }\n```\n\nThe sanitized text logger takes as delegate an actual logger. We assume\
      \ the logger outputs text log messages to a file, network, or the console, and\
      \ each log message has no indented lines. The sanitized text logger sanitizes\
      \ all text to be logged by indenting every line except the first by two spaces.\
      \ While a malicious user can indent text by more, a malicious user cannot create\
      \ a fake log entry because all of her output will be indented, except for the\
      \ real log output.\n\n```java\nclass SanitizedTextLogger extends Logger {\n\
      \  Logger delegate;\n\n  public SanitizedTextLogger(Logger delegate) {\n   \
      \ super(delegate.getName(), delegate.getResourceBundleName());\n    this.delegate\
      \ = delegate;\n  }\n\n  public String sanitize(String msg) {\n    Pattern newline\
      \ = Pattern.compile(\"\\n\");\n    Matcher matcher = newline.matcher(msg);\n\
      \    return matcher.replaceAll(\"\\n  \");\n  }\n\n  public void severe(String\
      \ msg) {\n    delegate.severe(sanitize(msg));\n  }\n\n  // .. Other Logger methods\
      \ which must also sanitize their log messages\n}\n```"
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 00. 输入验证和数据处理 (IDS)
      - type: DetailClassChin
        value: IDS03-J：不要将未经验证的用户输入记录日志
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 00. Input Validation and Data Sanitization (IDS)
      - type: DetailClassChin
        value: IDS03-J:Do not log unsanitized user input
    Default: not
    Description: "当日志条目包含未经净化的用户输入时会引发记录注入漏洞。恶意用户会插入伪造的日志数据，从而让系统管理员以为是系统行为 [OWASP\
      \ 2008]。**例如**：用户在将冗长的日志记录拆分成两份时，日志中使用的回车和换行符可能会被误解。记录注入攻击可以通过对任何非受信的发送到日志的输入进行净化和验证来阻止。\
      \  \n记录未初始化的用户输入还可能导致敏感数据跨信任边界泄漏。例如，攻击者可能会向日志文件中注入脚本，以便在使用web浏览器查看文件时，浏览器可以向攻击者提供管理员cookie的副本，以便攻击者可以作为管理员获得访问权限。\n\
      **不规范的代码样例1**：\n此不合规代码示例记录来自未经身份验证用户的不受信任数据，而不进行数据清理。\n  \n```java\nif (loginSuccessful)\
      \ {\n  logger.severe(\"User login succeeded for: \" + username);\n} else {\n\
      \  logger.severe(\"User login failed for: \" + username);\n}\n```\n如果没有净化，那么可能会出现日志注入攻击。用户名为guest时的标准日志消息可能如下所示：\n\
      ```\nMay 15, 2011 2:19:10 PM java.util.logging.LogManager$RootLogger log\nSEVERE:\
      \ User login failed for: guest\n```\n如果日志消息中使用的username不是guest，而是多行字符串，如下所示：\n\
      ```\nguest\nMay 15, 2011 2:25:52 PM java.util.logging.LogManager$RootLogger\
      \ log\nSEVERE: User login succeeded for: administrator\n```\n日志将包含以下误导性数据：\n\
      \  \n```\nMay 15, 2011 2:19:10 PM java.util.logging.LogManager$RootLogger log\n\
      SEVERE: User login failed for: guest\nMay 15, 2011 2:25:52 PM java.util.logging.LogManager\
      \ log\nSEVERE: User login succeeded for: administrator\n```"
    Description_En: "A log injection vulnerability arises when a log entry contains\
      \ unsanitized user input. A malicious user can insert fake log data and consequently\
      \ deceive system administrators as to the system's behavior [OWASP 2008]. For\
      \ example, an attacker might split a legitimate log entry into two log entries\
      \ by entering a carriage return and line feed (CRLF) sequence to mislead an\
      \ auditor. Log injection attacks can be prevented by sanitizing and validating\
      \ any untrusted input sent to a log.\n\nLogging unsanitized user input can also\
      \ result in leaking sensitive data across a trust boundary. For example, an\
      \ attacker might inject a script into a log file such that when the file is\
      \ viewed using a web browser, the browser could provide the attacker with a\
      \ copy of the administrator's cookie so that the attacker might gain access\
      \ as the administrator.\n\n**Noncompliant Code Example**\n\nThis noncompliant\
      \ code example logs untrusted data from an unauthenticated user without data\
      \ sanitization.\n\n```java\nif (loginSuccessful) {\n  logger.severe(\"User login\
      \ succeeded for: \" + username);\n} else {\n  logger.severe(\"User login failed\
      \ for: \" + username);\n}\n```\n\nWithout sanitization, a log injection attack\
      \ is possible. A standard log message when `username` is `guest` might look\
      \ like this:\n\n```\nMay 15, 2011 2:19:10 PM java.util.logging.LogManager$RootLogger\
      \ log\nSEVERE: User login failed for: guest\n```\n\nIf the `username` that is\
      \ used in a log message is not `guest` but rather a multiline string like this:\n\
      \n```\nguest\nMay 15, 2011 2:25:52 PM java.util.logging.LogManager$RootLogger\
      \ log\nSEVERE: User login succeeded for: administrator\n```\n\nthe log would\
      \ contain the following misleading data:\n\n```\nMay 15, 2011 2:19:10 PM java.util.logging.LogManager$RootLogger\
      \ log\nSEVERE: User login failed for: guest\nMay 15, 2011 2:25:52 PM java.util.logging.LogManager\
      \ log\nSEVERE: User login succeeded for: administrator\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 117:Improper Output Neutralization for Logs
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A1 Unvalidated Input
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: cert
    WhiteList: true
    id: '0102000010140203'
    language: Java
  - Advice: "对应的符合规定的方案在登录之前会净化用户名输入，从而防止注入攻击。\n    ```java\n    if (!Pattern.matches(\"\
      [A-Za-z0-9_]+\", username)) {\n        // Unsanitized username\n        logger.severe(\"\
      User login failed for unauthorized user\");\n    } else if (loginSuccessful)\
      \ {\n        logger.severe(\"User login succeeded for: \" + username);\n   \
      \ } else {\n        logger.severe(\"User login failed for: \" + username);\n\
      \    }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 5.2 数据净化
      - type: DetailClassChin
        value: C.1.4 不要记录未经净化的用户输入
    Categories_En:
      Category: []
    Default: not
    Description: "1) 规定   \n   \n不要将未经净化的用户输入写入日志，以防止向受信边界之外泄露敏感数据或违反本地的安全规则。   \n\
      2) 原理：   \n当日志条目包含未经净化的用户输入时会引发记录注入漏洞。恶意用户会插入伪造的日志数据，从而让系统管理员以为是系统行为。例如，用户在将冗长的日志记录拆分成两份时，日志中使用的回车和换行符可能会被误解。记录注入攻击可以通过对任何非受信的发送到日志的输入进行净化和验证来阻止。\
      \   \n将未经净化的用户输入写入日志同样会导致向受信边界之外泄露敏感数据，或者在存储敏感数据的时候，违反了本地的安全规则。   \n3) 示例  \
      \ \n    从这个不符合规则的代码示例中，可以看到在接收到非法请求的时候，会记录用户的用户名。这时没有执行任何输入净化。   \n    ```java\n\
      \    if (loginSuccessful) {   \n        logger.severe(\"User login succeeded\
      \ for: \" + username);   \n    } else {   \n        logger.severe(\"User login\
      \ failed for: \" + username);   \n    }\n    ```   \n    如果没有净化，那么可能会出现日志注入攻击。\
      \   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 117:Improper Output Neutralization for Logs
      - type: CERT
        value: IDS03-J:Do not log unsanitized user input
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A1 Unvalidated Input
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: true
    id: 0902000010160004
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。当出现`username`是david时，标准的日志信息如下所示：  \n```\nMay 15,\
      \ 2011 2:19:10 PM java.util.logging.LogManager$RootLogger log    \nSEVERE: User\
      \ login failed for: david   \n```\n如果日志信息中使用的`username`不是david而是多行字符串，如下所示：\
      \  \n```\ndavid    \nMay 15, 2011 2:25:52 PM java.util.logging.LogManager$RootLogger\
      \ log    \nSEVERE: User login succeeded for: administrator  \n``` \n那么日志中包含了以下可能引起误导的信息：\
      \  \n```\nMay 15, 2011 2:19:10 PM java.util.logging.LogManager$RootLogger log\
      \    \nSEVERE: User login failed for: david    \nMay 15, 2011 2:25:52 PM java.util.logging.LogManager$RootLogger\
      \ log    \nSEVERE: User login succeeded for: administrator\n ``` \n**示例2**：\
      \  \n```java\nif (!Pattern.matches(\"[A-Za-z0-9_]+\", username)) {    \n   \
      \ // Unsanitized username    \n    logger.severe(\"User login failed for unauthorized\
      \ user\");    \n} else if (loginSuccessful) {    \n    logger.severe(\"User\
      \ login succeeded for: \" + username);    \n} else {    \n    logger.severe(\"\
      User login failed for: \" + username);    \n}\n```   \n这个符合规范的代码在登录之前会净化用户名输入，从而防止注入攻击。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.4 日志安全
      - type: DetailClassChin
        value: A.2.4.2 不要将未经验证的用户输入记录日志
    Categories_En:
      Category: []
    Default: not
    Description: "规范说明：当日志条目包含未经净化的用户输入时会引发记录注入漏洞。恶意用户会插入伪造的日志数据，从而让系统管理员以为是系统行为 [OWASP\
      \ 2008]。例如，用户在将冗长的日志记录拆分成两份时，日志中使用的回车和换行符可能会被误解。记录注入攻击可以通过对任何非受信的发送到日志的输入进行净化和验证来阻止。\
      \  \n对于不要将未经验证的用户输入记录日志的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nif\
      \ (loginSuccessful) {    \n    logger.severe(\"User login succeeded for: \"\
      \ + username);    \n} else {    \n    logger.severe(\"User login failed for:\
      \ \" + username);    \n}\n```   \n这个不符合规则的代码示例中，在接收到非法请求的时候，会记录用户的用户名。这时没有执行任何输入净化。\
      \  \n如果没有净化，那么可能会出现日志注入攻击。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 117:Improper Output Neutralization for Logs
      - type: CERT
        value: IDS03-J:Do not log unsanitized user input
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A1 Unvalidated Input
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170027'
    language: Java
  - Advice: "使用EntityResolver方案来防止XML外部实体注入。  \n**规范的代码样例1**：  \n针对不规范的代码样例3的解决方案是，定义一个CustomResolver类，这个类实现了`org.xml.sax.EntityResolver`接口。它可以让SAX应用定制对外部实体的处理。这个定制的处理器使用的是一个为外部实体定义的简单的白名单。当输入不是任何指定的、安全地实体源路径时，`resolverEntity()`方法会返回一个空的InputSource对象。\
      \  \n```java\nimport java.io.IOException;\nimport org.xml.sax.EntityResolver;\n\
      import org.xml.sax.InputSource;\nimport org.xml.sax.SAXException;\n\nclass CustomResolver\
      \ implements EntityResolver {\n    public InputSource resolveEntity(String publicId,\
      \ String systemId) throws SAXException, IOException {\n\n        // Check for\
      \ known good entities  \n        String entityPath = \"file:/Users/onlinestore/good.xml\"\
      ;\n        if (systemId.equals(entityPath)) {\n            System.out.println(\"\
      Resolving entity: \" + publicId + \" \" + systemId);\n            return new\
      \ InputSource(entityPath);\n        } else {\n            // Disallow unknown\
      \ entities by returning a blank path\n            return new InputSource();\n\
      \        }\n    }\n}\n```\n`setEntityResolver()`方法可以将对应的SAX驱动实例注册进来。当解析恶意输入时，这个由自定义的解析器返回的空的InputStream对象会抛出`java.net.MalformedURLException`异常。需要注意的是，必须创建一个XMLReader对象，以便通过这个对象来设置自定义的实体解析器。\n\
      ```java\nclass XXE {\n    private static void receiveXMLStream(InputStream inStream,\
      \ DefaultHandler defaultHandler)\n            throws ParserConfigurationException,\
      \ SAXException, IOException {\n        SAXParserFactory factory = SAXParserFactory.newInstance();\n\
      \        SAXParser saxParser = factory.newSAXParser();\n        // To set the\
      \ Entity Resolver, an XML reader needs to be created  \n        XMLReader reader\
      \ = saxParser.getXMLReader();\n        reader.setEntityResolver(new CustomResolver());\n\
      \        reader.setErrorHandler(defaultHandler);\n        InputSource is = new\
      \ InputSource(inStream);\n        reader.parse(is);\n    }\n\n    public static\
      \ void main(String[] args) throws ParserConfigurationException, SAXException,\
      \ IOException {\n        receiveXMLStream(new FileInputStream(\"evil.xml\"),\
      \ new DefaultHandler());\n    }\n}\n```"
    Advice_En: "**Compliant Solution (`EntityResolver`)**\n\nThis compliant solution\
      \ defines a `CustomResolver` class that implements the interface `org.xml.sax.EntityResolver`.\
      \ This interface enables a SAX application to customize handling of external\
      \ entities. The customized handler uses a simple whitelist for external entities.\
      \ The `resolveEntity()` method returns an empty `InputSource` when an input\
      \ fails to resolve to any of the specified, safe entity source paths.\n\n```java\n\
      import java.io.IOException;\nimport org.xml.sax.EntityResolver;\nimport org.xml.sax.InputSource;\n\
      import org.xml.sax.SAXException;\n\nclass CustomResolver implements EntityResolver\
      \ {\n  public InputSource resolveEntity(String publicId, String systemId)\n\
      \      throws SAXException, IOException {\n\n    // Check for known good entities\n\
      \    String entityPath = \"file:/Users/onlinestore/good.xml\";\n    if (systemId.equals(entityPath))\
      \ {\n      System.out.println(\"Resolving entity: \" + publicId + \" \" + systemId);\n\
      \      return new InputSource(entityPath);\n    } else {\n      // Disallow\
      \ unknown entities by returning a blank path\n      return new InputSource();\n\
      \    }\n  }\n}\n```\n\nThe `setEntityResolver()` method registers the instance\
      \ with the corresponding SAX driver. When parsing malicious input, the empty\
      \ `InputSource` returned by the custom resolver causes a `java.net.MalformedURLException`\
      \ to be thrown. Note that you must create an `XMLReader` object on which to\
      \ set the custom entity resolver.\n\n```java\nimport java.io.FileInputStream;\n\
      import java.io.IOException;\nimport java.io.InputStream;\n\nimport javax.xml.parsers.ParserConfigurationException;\n\
      import javax.xml.parsers.SAXParser;\nimport javax.xml.parsers.SAXParserFactory;\n\
      \nimport org.xml.sax.InputSource;\nimport org.xml.sax.SAXException;\nimport\
      \ org.xml.sax.XMLReader;\nimport org.xml.sax.helpers.DefaultHandler;\n\nclass\
      \ XXE {\n  private static void receiveXMLStream(InputStream inStream,\n    \
      \  DefaultHandler defaultHandler) throws ParserConfigurationException,\n   \
      \   SAXException, IOException {\n    SAXParserFactory factory = SAXParserFactory.newInstance();\n\
      \    SAXParser saxParser = factory.newSAXParser();\n\n    // Create an XML reader\
      \ to set the entity resolver.\n    XMLReader reader = saxParser.getXMLReader();\n\
      \    reader.setEntityResolver(new CustomResolver());\n    reader.setContentHandler(defaultHandler);\n\
      \n    InputSource is = new InputSource(inStream);\n    reader.parse(is);\n \
      \ }\n\n  public static void main(String[] args) throws ParserConfigurationException,\n\
      \      SAXException, IOException {\n    try {\n      receiveXMLStream(new FileInputStream(\"\
      evil.xml\"), new DefaultHandler());\n    } catch (java.net.MalformedURLException\
      \ mue) {\n      System.err.println(\"Malformed URL Exception: \" + mue);\n \
      \   }\n  }\n}\n```"
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 00. 输入验证和数据处理 (IDS)
      - type: DetailClassChin
        value: IDS17-J：预防XML外部实体攻击
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 00. Input Validation and Data Sanitization (IDS)
      - type: DetailClassChin
        value: IDS17-J:Prevent XML External Entity Attacks
    Default: true
    Description: "实体声明定义了常用文本或特殊字符的快捷方式。实体声明可以定义内部实体或外部实体。对于内部实体，实体的内容在声明中给出。对于外部实体，内容由统一资源标识符（URI）指定。\n\
      实体可以是解析的，也可以是未解析的。已解析实体的内容称为其替换文本。未解析实体是一种资源，其内容可能是文本，也可能不是文本，如果是文本，则可能不是XML。已解析的实体通过实体引用来调用；未解析的实体通过名称来调用。\n\
      根据XML W3C 4.4.3小节的建议[W3C 2008]：“包含所需的验证”部分：\n>当一个XML处理器找到一个已经解析过的实例的引用的时候，为了验证这个文档，处理器必须包含它的替代字段。如果该实体是外部的，而且处理器不打算验证这个XML文档，那么处理器可以（但不必）包含该实体的替代字段。\n\
      \n由于包含来自外部实体的替换文本是可选的，因此并非所有XML处理器在验证期间都容易受到外部实体攻击。\n当包含外部实体引用的XML输入由配置不当的XML解析器处理时，就会发生XML外部实体（XXE）攻击。攻击者可能会通过操纵实体的URI来引用本地文件系统中包含密码和私人用户数据等敏感数据的文件，从而使用XXE攻击来访问敏感信息。攻击者可能会发起拒绝服务攻击，例如，将`/dev/random`或`/dev/tty`指定为输入URI，这可能会导致程序崩溃或永久阻塞程序。\n\
      **不规范的代码样例1**：  \n下面的代码尝试对`evil.xml`进行解析，并且报告相关错误，然后退出。然而，SAX或者DOM解析器会尝试访问在SYSTEM属性中标识的URL，这意味着它将读取一个本地的`/dev/tty`文件的内容。在POSIX系统中，读取这个文件会导致程序阻塞，直到可以通过计算机控制台得到输入数据为止。攻击者可以使用这一类的恶意XML文件来导致系统挂起。\n\
      ```java\nimport java.io.FileInputStream;\nimport java.io.IOException;\nimport\
      \ java.io.InputStream;\n\nimport javax.xml.parsers.ParserConfigurationException;\n\
      import javax.xml.parsers.SAXParser;\nimport javax.xml.parsers.SAXParserFactory;\n\
      \nimport org.xml.sax.SAXException;\nimport org.xml.sax.helpers.DefaultHandler;\n\
      \nclass XXE {\n  private static void receiveXMLStream(InputStream inStream,\n\
      \                                       DefaultHandler defaultHandler)\n   \
      \   throws ParserConfigurationException, SAXException, IOException {\n    SAXParserFactory\
      \ factory = SAXParserFactory.newInstance();\n    SAXParser saxParser = factory.newSAXParser();\n\
      \    saxParser.parse(inStream, defaultHandler);\n  }\n\n  public static void\
      \ main(String[] args) throws ParserConfigurationException,\n      SAXException,\
      \ IOException {\n    try {\n      receiveXMLStream(new FileInputStream(\"evil.xml\"\
      ), new DefaultHandler());\n    } catch (java.net.MalformedURLException mue)\
      \ {\n      System.err.println(\"Malformed URL Exception: \" + mue);\n    }\n\
      \  }\n}\n```\n如果`evil.xml`文件中包含以下文本，程序会受到远程XXE攻击。\n```xml\n<?xml version=\"\
      1.0\"?>\n<!DOCTYPE foo SYSTEM \"file:/dev/tty\">\n<foo>bar</foo>\n```"
    Description_En: "*Entity declarations* define shortcuts to commonly used text\
      \ or special characters. An entity declaration may define either an *internal*\
      \ or *external entity*. For internal entities, the content of the entity is\
      \ given in the declaration. For external entities, the content is specified\
      \ by a Uniform Resource Identifier (URI).\n\nEntities may be either *parsed*\
      \ or *unparsed*. The contents of a parsed entity are called its *replacement\
      \ text*. An unparsed entity is a resource whose contents may or may not be text,\
      \ and if text, may be other than XML. Parsed entities are invoked by name using\
      \ an *entity reference*; unparsed entities are invoked by name.\n\nAccording\
      \ to XML W3C Recommendation, section 4.4.3, \"Included If Validating\" [W3C\
      \ 2008]:\n\n> When an XML processor recognizes a reference to a parsed entity,\
      \ to validate the document, the processor MUST include its replacement text.\
      \ If the entity is external, and the processor is not attempting to validate\
      \ the XML document, the processor MAY, but need not, include the entity's replacement\
      \ text.\n\nBecause inclusion of replacement text from an external entity is\
      \ optional, not all XML processors are vulnerable to external entity attacks\
      \ during validation.\n\nAn *XML external entity (XXE)* attack occurs when XML\
      \ input containing a reference to an external entity is processed by an improperly\
      \ configured XML parser. An attacker might use an XXE attack to gain access\
      \ to sensitive information by manipulating the URI of the entity to refer to\
      \ files on the local file system containing sensitive data such as passwords\
      \ and private user data. An attacker might launch a denial-of-service attack,\
      \ for example, by specifying `/dev/random` or `/dev/tty` as input URIs, which\
      \ can crash or indefinitely block a program.\n\n**Noncompliant Code Example**\n\
      \nThis noncompliant code example attempts to parse the file `evil.xml`, report\
      \ any errors, and exit. However, a SAX (Simple API for XML) or a DOM (Document\
      \ Object Model) parser will attempt to access the URI specified by the `SYSTEM`\
      \ attribute, which means it will attempt to read the contents of the local `/dev/tty`\
      \ file. On POSIX systems, reading this file causes the program to block until\
      \ input data is supplied to the machine's console. Consequently, an attacker\
      \ can use this malicious XML file to cause the program to hang.\n\n```java\n\
      import java.io.FileInputStream;\nimport java.io.IOException;\nimport java.io.InputStream;\n\
      \nimport javax.xml.parsers.ParserConfigurationException;\nimport javax.xml.parsers.SAXParser;\n\
      import javax.xml.parsers.SAXParserFactory;\n\nimport org.xml.sax.SAXException;\n\
      import org.xml.sax.helpers.DefaultHandler;\n\nclass XXE {\n  private static\
      \ void receiveXMLStream(InputStream inStream,\n                            \
      \           DefaultHandler defaultHandler)\n      throws ParserConfigurationException,\
      \ SAXException, IOException {\n    SAXParserFactory factory = SAXParserFactory.newInstance();\n\
      \    SAXParser saxParser = factory.newSAXParser();\n    saxParser.parse(inStream,\
      \ defaultHandler);\n  }\n\n  public static void main(String[] args) throws ParserConfigurationException,\n\
      \      SAXException, IOException {\n    try {\n      receiveXMLStream(new FileInputStream(\"\
      evil.xml\"), new DefaultHandler());\n    } catch (java.net.MalformedURLException\
      \ mue) {\n      System.err.println(\"Malformed URL Exception: \" + mue);\n \
      \   }\n  }\n}\n```\n\nThis program is subject to a remote XXE attack if the\
      \ `evil.xml` file contains the following:\n\n```xml\n<?xml version=\"1.0\"?>\n\
      <!DOCTYPE foo SYSTEM \"file:/dev/tty\">\n<foo>bar</foo>\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 611
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 611
      - type: CWE
        value: 611:Improper Restriction of XML External Entity Reference ('XXE')
      - type: OWASP TOP 10
        value: 2017:A4 XML External Entities (XXE)
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: cert
    WhiteList: true
    id: '0102000010140217'
    language: Java
  - Advice: "对于使用Java原生DOM和SAX有以下两种修复方法:   \n1. 不允许DTD（doctypes），这样可以阻止几乎所有的XML实体攻击\
      \  \n    **例1**：下面代码片段SAXParserFactory将禁用DTD  \n    ```java\n    private static\
      \ void receiveXMLStream(InputStream inStream){  \n        SAXParserFactory factory\
      \ = SAXParserFactory.newInstance();  \n        SAXParser saxParser;  \n    \
      \    try {  \n            factory.setFeature(\"http://apache.org/xml/features/disallow-doctype-decl\"\
      ,true);  \n            saxParser = factory.newSAXParser();  \n            saxParser.parse(inStream,new\
      \ DefaultHandler());  \n            ...  \n        } catch (ParserConfigurationException\
      \ e) {  \n            ...  \n        } catch (SAXException e) {  \n        \
      \    ...  \n        } catch (IOException e) {  \n            ...  \n       \
      \ }  \n    }\n    ```  \n    **例2**：下面代码片段DocumentBuilderFactory将禁用DTD\n   \
      \ ```java  \n    private static void receiveXMLStream(InputStream stream){ \
      \ \n        DocumentBuilderFactory documentBuilderFactory = DocumentBuilderFactory.newInstance();\
      \  \n        DocumentBuilder documentBuilder;  \n        try {  \n         \
      \   documentBuilderFactory.setFeature(\"http://apache.org/xml/features/disallow-doctype-decl\"\
      , true);  \n            documentBuilder = documentBuilderFactory.newDocumentBuilder();\
      \  \n            documentBuilder.parse(stream);  \n            ...  \n     \
      \   } catch (ParserConfigurationException e) {  \n            ...  \n      \
      \  } catch (SAXException e) {  \n            ...  \n        } catch (IOException\
      \ e) {  \n            ...  \n        }     \n    }\n    ```  \n2. 如果无法完全禁用DTD，可以采用禁用外部通用实体和参数实体并且禁用外部DTD和禁止实体扩展引用。\
      \   \n    **例1**：下面代码片段SAXParserFactory将禁用DTD\n    ```java  \n    private static\
      \ void receiveXMLStream(InputStream inStream){  \n        SAXParserFactory factory\
      \ = SAXParserFactory.newInstance();  \n        SAXParser saxParser;  \n    \
      \    try {  \n            factory.setFeature(\"http://xml.org/sax/features/external-general-entities\"\
      ,false);  \n            factory.setFeature(\"http://xml.org/sax/features/external-parameter-entities\"\
      , false);  \n            factory.setFeature(\"http://apache.org/xml/features/nonvalidating/load-external-dtd\"\
      ,false);  \n            factory.setXIncludeAware(false);  \n            saxParser\
      \ = factory.newSAXParser();  \n            saxParser.parse(inStream,new DefaultHandler());\
      \  \n            ...  \n        } catch (ParserConfigurationException e) { \
      \ \n            ...  \n        } catch (SAXException e) {  \n            ...\
      \  \n        } catch (IOException e) {  \n            ...  \n        }  \n \
      \   }\n    ```  \n      \n    **例2**：下面代码片段DocumentBuilderFactory将禁用DTD  \n\
      \    ```java\n    private static void receiveXMLStream(InputStream stream){\
      \  \n        DocumentBuilderFactory documentBuilderFactory = DocumentBuilderFactory.newInstance();\
      \  \n        DocumentBuilder documentBuilder;  \n        try {  \n         \
      \   documentBuilderFactory.setFeature(\"http://xml.org/sax/features/external-general-entities\"\
      ,false);  \n            documentBuilderFactory.setFeature(\"http://apache.org/xml/features/nonvalidating/load-external-dtd\"\
      ,false);  \n            documentBuilderFactory.setXIncludeAware(false);  \n\
      \            documentBuilderFactory.setExpandEntityReferences(false);   \n \
      \           documentBuilder = documentBuilderFactory.newDocumentBuilder(); \
      \ \n            documentBuilder.parse(stream);  \n            ...  \n      \
      \  } catch (ParserConfigurationException e) {  \n            ...  \n       \
      \ } catch (SAXException e) {  \n            ...  \n        } catch (IOException\
      \ e) {  \n            ...  \n        }     \n    }\n    ```  \n    上述设置功能的方法setFeature也可以换成setAttribute，setAttribute方法用于用户在底层设置特定属性，使用方法和效果是一样的。\
      \  \n    上述防御需要Java 7 Update 67，Java 8 Update 20或更高版本，因为DocumentBuilderFactory和SAXParserFactory的上述对策在早期Java版本中被破坏。\
      \  \n    也可以从其他类上着手防范xxe漏洞：  \n    如XMLInputFactory、TransformerFactory、Validator、SchemaFactory、SAXTransformerFactory。但这些类都是调用了XMLConstants中的属性，故需要支持JAXP1.5.使用StAX解析xml文件时，以XMLInputFactory为例，可使用下面方法进行防范：\
      \  \n    ```java\n    //禁用DTD   \n    xmlInputFactory.setProperty(XMLInputFactory.SUPPORT_DTD,false);\n\
      \    ```   \n    ```java\n    //禁用外部实体 \n    xmlInputFactory.setProperty(\"\
      javax.xml.stream.isSupportingExternalEntities\",false);\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码注入
      - type: DetailClassChin
        value: 【强制】需要防止XML外部实体注入
    Categories_En:
      Category: []
    Default: true
    Description: "XML外部实体的URI设置不当有可能造成拒绝服务攻击或者程序崩溃。  \n  \n**例1**：下面代码片段使用Java原生的SAX对evil.xml文件进行解析\
      \  \n```java\nprivate static void receiveXMLStream(InputStream inStream){  \n\
      \    SAXParserFactory factory = SAXParserFactory.newInstance();  \n    SAXParser\
      \ saxParser;  \n    try {  \n        saxParser = factory.newSAXParser();  \n\
      \        saxParser.parse(inStream,new DefaultHandler());  \n        ...  \n\
      \    } catch (ParserConfigurationException e) {  \n        ...  \n    } catch\
      \ (SAXException e) {  \n        ...  \n    } catch (IOException e) {  \n   \
      \     ...  \n    }  \n}\n```  \n  \n**例2**：下面代码片段使用Java原生的DOM对evil.xml文件进行解析\
      \  \n```java\nprivate static void receiveXMLStream(InputStream stream){  \n\
      \    DocumentBuilderFactory documentBuilderFactory = DocumentBuilderFactory.newInstance();\
      \  \n    DocumentBuilder documentBuilder;  \n    try {  \n        documentBuilder\
      \ = documentBuilderFactory.newDocumentBuilder();  \n        documentBuilder.parse(stream);\
      \  \n        ...  \n    } catch (ParserConfigurationException e) {  \n     \
      \   ...  \n    } catch (SAXException e) {  \n        ...  \n    } catch (IOException\
      \ e) {  \n        ...  \n    }     \n}\n```  \n  如果evil.xml文件中包含以下文本\n```xml\n\
      <?xml version=\"1.0\"?>  \n<!DOCTYPE foo SYSTEM \"file:/dev/tty\">  \n<foo>bar</foo>\
      \  \n...  \n```  \n  SAX或者DOM解析器会尝试访问在SYSTEM属性中标识的URL，这意味着它将读取本地`/dev/tty`文件的内容。在POSIX系统中，读取这个文件会导致程序阻塞，直到可以通过计算机控制台得到输入数据为止。这样，攻击者可以使用这个恶意的XML文件来导致系统挂起，程序会受到XML外部实体注入攻击。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: XML External Entity Injection
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 611
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 611
      - type: CWE
        value: 611:Improper Restriction of XML External Entity Reference ('XXE')
      - type: CERT
        value: IDS17-J:Prevent XML External Entity Attacks
      - type: OWASP TOP 10
        value: 2017:A4 XML External Entities (XXE)
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: security
    Type: std
    WhiteList: not
    id: '0202000010150001'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nclass CustomResolver implements\
      \ EntityResolver {\n    public InputSource resolveEntity(String publicId, String\
      \ systemId) throws SAXException, IOException {\n\n        // Check for known\
      \ good entities\n        String entityPath = \"file:/Users/onlinestore/good.xml\"\
      ;\n        if (systemId.equals(entityPath)) {\n            System.out.println(\"\
      Resolving entity: \" + publicId + \" \" + systemId);\n            return new\
      \ InputSource(entityPath);\n        } else {\n            // Disallow unknown\
      \ entities by returning a blank path\n            return new InputSource();\n\
      \        }\n    }\n}\n```  \n针对不规范的代码示例的解决方案是，定义一个CustomResolver类，这个类实现了`org.xml.sax.EntityResolver`接口。它可以让SAX应用定制对外部实体的处理。这个定制的处理器使用的是一个为外部实体定义的简单的白名单。当输入不是任何指定的、安全的实体源路径时，`resolverEntity()`方法会返回一个空的InputSource对象。\
      \  \n`setEntityResolver()`方法可以将对应的SAX驱动实例注册进来。当解析恶意输入时，这个由自定义解析器返回的空的InputStream对象会抛出`java.net.MalformedURLException`异常。需要注意的是，必须创建一个XMLReader对象，以便通过这个对象来设置自定义的实体解析器。\
      \  \n```java\nclass XXE {\n    private static void receiveXMLStream(InputStream\
      \ inStream, DefaultHandler defaultHandler)\n            throws ParserConfigurationException,\
      \ SAXException, IOException {\n        SAXParserFactory factory = SAXParserFactory.newInstance();\n\
      \        SAXParser saxParser = factory.newSAXParser();\n        // To set the\
      \ Entity Resolver, an XML reader needs to be created\n        XMLReader reader\
      \ = saxParser.getXMLReader();\n        reader.setEntityResolver(new CustomResolver());\n\
      \        reader.setErrorHandler(defaultHandler);\n        InputSource is = new\
      \ InputSource(inStream);\n        reader.parse(is);\n    }\n\n    public static\
      \ void main(String[] args) throws ParserConfigurationException, SAXException,\
      \ IOException {\n        receiveXMLStream(new FileInputStream(\"evil.xml\"),\
      \ new DefaultHandler());\n    }\n}\n```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.1 数据清洗
      - type: DetailClassChin
        value: A.2.1.1.7 预防XML外部实体攻击
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：XML文档可以从一个很小的逻辑块（实体）开始动态构建。实体可以是内部的、外部的或者基于参数的。外部实体运行是将外部文件中的XML包含进来。攻击者可以通过操作实例的URI，使其指向特定的在当前文件系统中保存的文件，从而造成拒绝服务或程序崩溃，比如：指定`/dev/random`或者`/dev/tty`作为输入的URI，这可能造成永久阻塞程序或者程序崩溃。\
      \  \n对于预防XML外部实体攻击的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nclass XXE\
      \ {\n    private static void receiveXMLStream(InputStream inStream, DefaultHandler\
      \ defaultHandler)\n            throws ParserConfigurationException, SAXException,\
      \ IOException {\n        SAXParserFactory factory = SAXParserFactory.newInstance();\n\
      \        SAXParser saxParser = factory.newSAXParser();\n        saxParser.parse(inStream,\
      \ defaultHandler);\n    }\n\n    public static void main(String[] args) throws\
      \ ParserConfigurationException, SAXException, IOException {\n        try {\n\
      \            receiveXMLStream(new FileInputStream(\"evil.xml\"), new DefaultHandler());\n\
      \        } catch (java.net.MalformedURLException mue) {\n            System.err.println(\"\
      Malformed URL Exception: \" + mue);\n        }\n    }\n}\n```  \n上面的代码尝试对`evil.xml`进行解析，并且报告相关错误后退出。然而，SAX或者DOM解析器会尝试访问在SYSTEM属性中标识的URL，这意味着它将读取一个本地的`/dev/try`文件的内容。在POSIX系统中，读取这个文件会导致程序阻塞，直到可以通过计算机控制台得到输入数据为止。攻击者可以使用这一类的恶意XML文件来导致系统挂起。\
      \  \n如果`evil.xml`文件中包含以下文本，程序会受到远程XXE攻击。  \n```xml\n<?xml version=\"1.0\"?>\
      \  \n<!DOCTYPE foo SYSTEM \"file:/dev/tty\">  \n<foo>bar</foo>  \n```\n使用EntityResolver方案来防止XML外部实体注入。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 611
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 611
      - type: CWE
        value: 611:Improper Restriction of XML External Entity Reference ('XXE')
      - type: CERT
        value: IDS17-J:Prevent XML External Entity Attacks
      - type: OWASP TOP 10
        value: 2017:A4 XML External Entities (XXE)
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170006'
    language: Java
  - Advice: "**规范的代码样例1**：\n这个解决方案在 `while` 循环中调用 `wait()` 方法来检查调用 `wait()` 之前和之后的条件：\n\
      ```java\nsynchronized (object) {\n  while (<condition does not hold>) {\n  \
      \  object.wait();\n  }\n  // Proceed when condition holds\n}\n```\n`java.util.concurrent.locks.Condition.await()`\
      \ 方法的调用也必须包含在类似的循环中。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution calls the `wait()`\
      \ method from within a `while` loop to check the condition both before and after\
      \ the call to `wait()`:\n\n```java\nsynchronized (object) {\n  while (<condition\
      \ does not hold>) {\n    object.wait();\n  }\n  // Proceed when condition holds\n\
      }\n```\n\nInvocations of the `java.util.concurrent.locks.Condition.await()`\
      \ method also must be enclosed in a similar loop."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 10. 线程API (THI)
      - type: DetailClassChin
        value: THI03-J：始终在循环中调用wait()和await()方法
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 10. Thread APIs (THI)
      - type: DetailClassChin
        value: THI03-J:Always invoke wait() and await() methods inside a loop
    Default: true
    Description: "`Object.wait()`方法会暂时放弃所拥有的锁，这让其他线程可以有机会得到锁并继续执行。 `Object.wait()`方法通常会在同步代码块及方法中调用。等待的线程只有在它接到通知后才会恢复执行，这通常是其他线程调用`notify()`或`notifyAll()`方法的结果。`Object.wait()`方法必须在检查等候条件是否为真的循环中进行调用。要注意的是，一个等待条件是和循环中的等待条件语句相反的。例如：从向量中删除一个元素的条件是`!isEmpty()`，而while循环中的条件语句是\
      \ `isEmpty()`。下面是向量empty时调用`wait()`方法的正确方式：  \n```java\nprivate Vector vector;\n\
      // ...\n\npublic void consumeElement() throws InterruptedException {\n  synchronized\
      \ (vector) {\n    while (vector.isEmpty()) {\n      vector.wait();\n    }\n\n\
      \    // Resume when condition holds\n  }\n}\n```\n通知机制会通知等待中的线程并允许线程去检查等候条件。在另一个线程中调用`notify()`或`notifyAll()`方法不能精确的判断哪个线程会恢复执行。当一个线程要求被阻拦直至条件成真时，等候条件也是有用的，例如：在读取前等待输入流提供数据。\
      \  \n在使用wait/notify机制时，安全性和活性是需要考虑的问题。安全性属性要求所有的对象在多线程环境中要维持状态的一致性。活性属性要求每一个操作或方法调用在完成前不会被中断。\
      \  \n为了确保活性属性，程序在调用`wait()`方法之前必须对while条件进行检测。这个提前的测试检查了是否会有另一个线程已经满足了等候条件并发出了一个通知。在通知已经发出后调用`wait()`方法会导致无限制的阻塞。\
      \  \n为了确保安全性，程序必须在调用`wait()`方法返回之后对while条件进行检测。虽然`wait()`的目的是不限时的阻拦线程直至收到通知，还是必须将它放在while循环中来避免以下情况发生：\
      \  \n1）中间的线程----（TOCTOU竞争）在通知发出和收到通知的线程恢复执行的间隙中，第三个线程可能会获得共享对象锁。这个线程可能会改变对象的状态，使其不一致。\
      \  \n2）恶意通知----在等候条件为假时可能会收到一个随机或恶意的通知。这样的通知会取消`wait()`的作用。  \n3）误送的通知----收到`notifyAll()`信号后没有指定线程的执行顺序。因此，一个无关的线程可以开始并执行已经满足了它的等候条件。这造成了这个线程恢复执行，但实际上它是应该处于睡眠状态的。\
      \  \n4）虚假唤醒----某些Java虚拟机实现容易受到虚假唤醒的影响，这会导致等待中的线程在没有通知时也会被唤醒。  \n由于这些原因，程序必须在`wait()`方法返回后检查等候条件，不管是在调用`wait()`之前还是之后，while循环都是检查等候条件的最好方式。\
      \  \n类似Condition接口中的`await()`方法也必须是在循环中调用。  \n风险评估：违反该条准则可能会导致无限期的阻塞，导致拒绝服务漏洞。\
      \  \n**不规范的代码样例1**：\n这个不合规的代码示例在传统的 `if` 块中调用 `wait()` 方法，并且在收到通知后无法检查后置条件。\
      \ 如果通知是意外或恶意的，线程可能会过早唤醒。\n```java\nsynchronized (object) {\n  if (<condition\
      \ does not hold>) {\n    object.wait();\n  }\n  // Proceed when condition holds\n\
      }\n```"
    Description_En: "The `Object.wait()` method temporarily cedes possession of a\
      \ lock so that other threads that may be requesting the lock can proceed. `Object.wait()`\
      \ must always be called from a synchronized block or method. The waiting thread\
      \ resumes execution only after it has been notified, generally as the result\
      \ of the invocation of the `notify()` or `notifyAll()` method by some other\
      \ thread. The `wait()` method must be invoked from a loop that checks whether\
      \ a condition predicate holds. Note that a condition predicate is the negation\
      \ of the condition expression in the loop. For example, the condition predicate\
      \ for removing an element from a vector is `!isEmpty()`, whereas the condition\
      \ expression for the while loop condition is `isEmpty()`. Following is the correct\
      \ way to invoke the `wait()` method when the vector is empty.\n\n```java\nprivate\
      \ Vector vector;\n// ...\n\npublic void consumeElement() throws InterruptedException\
      \ {\n  synchronized (vector) {\n    while (vector.isEmpty()) {\n      vector.wait();\n\
      \    }\n\n    // Resume when condition holds\n  }\n}\n```\n\nThe notification\
      \ mechanism notifies the waiting thread and allows it to check its condition\
      \ predicate. The invocation of `notify()` or `notifyAll()` in another thread\
      \ cannot precisely determine which waiting thread will be resumed. Condition\
      \ predicate statements allow notified threads to determine whether they should\
      \ resume upon receiving the notification. Condition predicates are also useful\
      \ when a thread is required to block until a condition becomes true, for example,\
      \ when waiting for data to arrive on an input stream before reading the data.\n\
      \nBoth safety and liveness  are concerns when using the wait/notify mechanism.\
      \ The safety property requires that all objects maintain consistent states in\
      \ a multithreaded environment [Lea 2000]. The liveness property requires that\
      \ every operation or method invocation execute to completion without interruption.\n\
      \nTo guarantee liveness, programs must test the `while` loop condition before\
      \ invoking the `wait()` method. This early test checks whether another thread\
      \ has already satisfied the condition predicate and sent a notification. Invoking\
      \ the `wait()` method after the notification has been sent results in indefinite\
      \ blocking.\n\nTo guarantee safety, programs must test the `while` loop condition\
      \ after returning from the `wait()` method. Although `wait()` is intended to\
      \ block indefinitely until a notification is received, it still must be encased\
      \ within a loop to prevent the following vulnerabilities [Bloch 2001]:\n\n*\
      \ **Thread in the middle:** A third thread can acquire the lock on the shared\
      \ object during the interval between a notification being sent and the receiving\
      \ thread resuming execution. This third thread can change the state of the object,\
      \ leaving it inconsistent. This is a time-of-check, time-of-use (TOCTOU) race\
      \ condition.\n* **Malicious notification:** A random or malicious notification\
      \ can be received when the condition predicate is false. Such a notification\
      \ would cancel the `wait()` method.\n* **Misdelivered notification:** The order\
      \ in which threads execute after receipt of a `notifyAll()` signal is unspecified.\
      \ Consequently, an unrelated thread could start executing and discover that\
      \ its condition predicate is satisfied. Consequently, it could resume execution\
      \ despite being required to remain dormant.\n* **Spurious wakeups:** Certain\
      \ Java Virtual Machine (JVM) implementations are vulnerable to spurious wakeups\
      \ that result in waiting threads waking up even without a notification [API\
      \ 2014].\n\nFor these reasons, programs must check the condition predicate after\
      \ the `wait()` method returns. A `while` loop is the best choice for checking\
      \ the condition predicate both before and after invoking `wait()`.\n\nSimilarly,\
      \ the `await()` method of the `Condition` interface also must be invoked inside\
      \ a loop. According to the Java API [API 2014], Interface `Condition`\n\n> When\
      \ waiting upon a Condition, a \"spurious wakeup\" is permitted to occur, in\
      \ general, as a concession to the underlying platform semantics. This has little\
      \ practical impact on most application programs as a Condition should always\
      \ be waited upon in a loop, testing the state predicate that is being waited\
      \ for. An implementation is free to remove the possibility of spurious wakeups\
      \ but it is recommended that applications programmers always assume that they\
      \ can occur and so always wait in a loop.\n\nNew code should use the `java.util.concurrent.locks`\
      \ concurrency utilities in place of the wait/notify mechanism. However, legacy\
      \ code that complies with the other requirements of this rule is permitted to\
      \ depend on the wait/notify mechanism.\n\n**Noncompliant Code Example**\n\n\
      This noncompliant code example invokes the `wait()` method inside a traditional\
      \ `if` block and fails to check the postcondition after the notification is\
      \ received. If the notification were accidental or malicious, the thread could\
      \ wake up prematurely.\n\n```java\nsynchronized (object) {\n  if (<condition\
      \ does not hold>) {\n    object.wait();\n  }\n  // Proceed when condition holds\n\
      }\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141103'
    language: Java
  - Advice: "对应的符合规则的解决方案是通过在 while 循环中调用`wait()`方法，从而在`wait()`调用的前后都对条件进行检査。   \n\
      \    ```java\n    synchronized (object) {\n        while (<condition does not\
      \ hold>) {\n            object.wait();\n        }\n        // Proceed when condition\
      \ holds\n    }\n    ```   \n    `java.util.concurrent.locks.Condition.await()`方法的调用也必须放置在类似的循环中。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.10.4 始终在循环中调用wait()和await()方法
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \t\t  \n程序必须在`wait()`方法返回后检查等候条件。不管是在调用`wait()`之前还是之后，while\
      \ 循环都是检查等候条件的最好方式。   \n2) 原理   \n`Object.wait()`方法会暂时地放弃所拥有的锁，以让其他线程有机会可得到锁并继续执行。`Object.wait()`方法通常在同步代码块及方法中被调用。等待的线程只有在它接到通知后才会恢复执行，这通常是其他线程调用\
      \ `notify()`或 `notifyAll()`方法的结果。`wait()`方法必须在检查等候条件是否为真的循环中进行调用。    \n在使用 wait/notify\
      \ 机制时，需要同时考虑安全性和灵活性问题。安全性属性要求所有的对象在多线程环境中要维持状态的一致性。灵活性属性要求每一个操作或方法调用在完成前不会被中断。\
      \   \n为确保活性属性，程序在调用 `wait()`方法之前必须对 while 循环的条件进行检测。这个提前的测试检查了是否会有另一个线程已经满足了等候条件并发出了一个通知。在通知已经发出后调用\
      \ `wait()`方法会导致无限期的阻塞。   \n为保证安全性，程序必须在 `wait()`方法返回之后对 while 循环的条件进行检测。虽然 `wait()`的目的是不限时地拦阻线程直至收到通知，还是必须将它放在\
      \ while 循环中来避免以下的漏洞：   \n• 中间的线程——在通知发出和收到通知的线程恢复执行的间隙中，第三个线程可能会获取共享对象的锁。这个线程可能会改变对象的状态，使其不一致。这就是\
      \ TOCTOU 竞争冒险。   \n• 恶意通知——在等候条件为假时可能会收到一个随机或恶意的通知。这样的通知会取消 `wait()`的作用。   \n\
      • 误送的通知——收到 `notifyAll()`信号后没有指定线程的执行顺序。因此，一个无关的线 程可能可以开始执行并发现已经满足 T 它的等候条件。这造成了这个线程恢复执行，但\
      \ 实际上它是应该处于睡眠状态的。   \n• 虚假唤醒——某些 Java 虚拟机实现容易收到虚假唤醒的影响，这会导致等待中的线程在没有通知时也会被唤醒。\
      \   \n基于上述原因，程序必须在 `wait()`方法返回后检查等候条件。不管是在调用 `wait()`之前还是之后，while 循环都是检查等候条件的最好方式。\
      \   \n3) 示例   \n    如下不符合规则的代码示例，该示例在传统的 if 程序段中调用`wait()`方法，没有检査接到通知后的后置条件。如果通知是意外或恶意的，线程将会过早地被唤醒。\
      \   \n    ```java\n    synchronized (object) {\n        if (<condition does\
      \ not hold>) {\n            object.wait();\n        }\n        // Proceed when\
      \ condition holds\n    }\n    ```\n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: THI03-J:Always invoke wait() and await() methods inside a loop
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160094
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。   \n**示例2**：  \n```java\nprivate Vector vector;\n\
      \npublic void consumeElement() throws InterruptedException {\n    synchronized\
      \ (vector) {\n        while (vector.isEmpty()) {\n            vector.wait();\n\
      \        }\n        // Resume when condition holds\n    }\n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.11 始终在循环中调用wait()和await()方法
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：`Object.wait()`方法会暂时放弃所拥有的锁，这让其他线程可以有机会得到锁并继续执行。 `Object.wait()`方法通常会在同步代码块及方法中调用。等待的线程只有在它接到通知后才会恢复执行，不管是在调用`wait()`之前还是之后，while循环都是检查等候条件的最好方式。类似Condition接口中的`await()`方法也必须是在循环中调用。违反该条准则可能会导致无限期的阻塞，导致拒绝服务漏洞。\
      \  \n对于始终在循环中调用`wait()`和`await()`方法的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n\
      ```java\nprivate Vector vector;\n\npublic void consumeElement() throws InterruptedException\
      \ {\n    synchronized (vector) {\n        if (vector.isEmpty()) {\n        \
      \    vector.wait();\n        }\n        // Resume when condition holds\n   \
      \ }\n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: THI03-J:Always invoke wait() and await() methods inside a loop
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170043'
    language: Java
  - Advice: "可以将其置为非静态成员变量，或在方法内使用时再声明，或者使用ThreadLocal从而使每个线程拥有自己的对象。  \n  \n**例如**：下面代码示例中定义了非static的Calendar。\
      \ \n```java \npublic class DateUtils{  \n    private Calendar calendar = Calendar.getInstance();\
      \   \n    ...  \n}\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 并发规范
      - type: DetailClassChin
        value: 【推荐】不要使用static修饰线程不安全的字段
    Categories_En:
      Category: []
    Default: true
    Description: "Java库中不是所有类都是线程安全的，如Calendar，XPath，或者SchemaFactory都是线程不安全的， 多线程运行时它们可能出现数据问题或异常。\
      \  \n  \n**例如**：下面代码示例中定义了static的Calendar。 \n```java \npublic class DateUtils{\
      \  \n    private static Calendar calendar = Calendar.getInstance();   \n   \
      \ ...  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: concurrency,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150441'
    language: Java
  - Advice: 应用程序应对密码进行恰当的哈希运算（如SHA-224、SHA-256、SHA-384和SHA-512）进行保护。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.3 访问控制
      - type: DetailClassChin
        value: A.2.3.2.5 避免使用弱加密算法保护密码
    Categories_En:
      Category: []
    Default: true
    Description: 规范说明：程序采用简单的编码（如base64）或弱哈希算法（如MD5、SHA-1）对密码进行加密不能有效地保护密码。
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 261:Weak Cryptography for Passwords
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: GB/T 34944-2017 Java
        value: 安全功能:存储可恢复的口令
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170025'
    language: Java
  - Advice: "避免该缺陷的方式如下：  \n1. 验证传递给parseDouble数据的合法性。  \n2. 升级JDK版本到6 Update 24或更高版本。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 8.2 开发环境安全
      - type: DetailClassChin
        value: A.5.2.2 避免解析Double类型数据导致拒绝服务
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：程序调用Double的解析方法时，可能导致线程被挂起。`java.lang.Double.parseDouble()`方法解析位于`[2^(-1022)\
      \ - 2^(-1075) :2^(-1022) - 2^(-1076)]`范围内的任何数字时可能导致线程被挂起，攻击者可以故意触发该漏洞执行拒绝服务攻击。该漏洞在java6\
      \ update24或更高版本中进行了修复。  \n对于避免解析Double类型数据导致拒绝服务的情况，示例1给出了不规范用法（java语言）示例。 \
      \ \n**示例1**： \n```java\nDouble d = Double.parseDouble(request.getParameter(\"\
      d\"));\n```  \n上述代码片段中，使用了易受攻击的方法。攻击者可发送d参数值位于该范围(例如`\"0.0222507385850720119e-00306\"\
      `）内的请求，致使程序在处理该请求时被挂起。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 730:OWASP Top Ten 2004 Category A9 - Denial of Service
      - type: OWASP TOP 10
        value: 2017:A9 Using Components with Known Vulnerabilities
      - type: OWASP TOP 10
        value: 2013:A9 Using Components with Known Vulnerabilities
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170072'
    language: Java
  - Advice: "所有向下的转换必须在转换之前检查数值的范围，来确保转换不会丢失数据。\n**规范的代码样例1**：\n```java\nclass CastAway\
      \ {\n    public static void workWith(int i) {\n        // check if i is within\
      \ byte range\n        if ((i < Byte.MIN_VALUE) || (i > Byte.MAX_VALUE)) {\n\
      \            throw new ArithmeticException(\"Value is out of range\");\n   \
      \     }\n        byte b = (byte) i;\n        // work with b\n    }\n}\n```\n\
      代码在语句`byte b = (byte) i;`之前对i的值进行了是否处于byte类型的有效数值范围的检查，当i的值不处于该范围时会抛出ArithmeticException异常。"
    Advice_En: "**Compliant Solution (Integer Narrowing)**\n\nThis compliant solution\
      \ validates that the value stored in the wider integer type is within the range\
      \ of the narrower type before converting to the narrower type. It throws an\
      \ `ArithmeticException` if the value is out of range.\n\n```java\nclass CastAway\
      \ {\n  public static void workWith(int i) {\n    // Check whether i is within\
      \ byte range\n    if ((i < Byte.MIN_VALUE) || (i > Byte.MAX_VALUE)) {\n    \
      \  throw new ArithmeticException(\"Value is out of range\");\n    }\n\n    byte\
      \ b = (byte) i;\n    // Work with b\n  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 03. 数据类型与运算 (NUM)
      - type: DetailClassChin
        value: NUM12-J：确保将数值转换成较小类型时不会产生数据丢失或曲解
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 03. Numeric Types and Operations (NUM)
      - type: DetailClassChin
        value: NUM12-J:Ensure conversions of numeric types to narrower types do not
          result in lost or misinterpreted data
    Default: true
    Description: "如果较大类型的值超出较小类型的值范围，则将数值类型转换为较小类型可能会导致数据丢失或错误解释。因此，必须在转换前通过范围检查值来保证所有缩小转换的安全性。\n\
      Java提供了22种可能的缩小原语转换。根据Java语言规范（JLS），§5.1.3，“缩小基本数据类型转换”[JLS 2015]：\n\n* `short`\
      \ 到 `byte` 或 `char`\n* `char` 到 `byte` 或 `short`\n* `int` 到 `byte`, `short`,\
      \ 或 `char`\n* `long` 到 `byte`, `short`, `char`, 或 `int`\n* `float` 到 `byte`,\
      \ `short`, `char`, `int`, 或 `long`\n* `double` 到o `byte`, `short`, `char`, `int`,\
      \ `long`, 或 `float`\n\n如果较大类型的值在较小类型的范围内，则允许进行缩小基本数据类型转换。\n\n**整数缩小**\n\n整数类型范围由JLS第4.2.1节“整数类型和值”[JLS\
      \ 2015]定义，NUM00-J中也有描述。\n下表列出了缩小整型原语转换范围的规则。在表中，对于整数类型T，n表示用于表示结果类型T（精度）的位数。\n\
      \n| From | To | Description | Possible Resulting Errors |\n| - | - | - | - |\n\
      | Signed integer | Integral type `T` | Keeps only `n` lower-order bits | Lost\
      \ or misinterpreted data |\n| `char` | Integral type `T` | Keeps only `n` lower-order\
      \ bits | Magnitude error; negative number even though `char` is 16-bit unsigned\
      \ |\n\n当整数被转换为更小的数据类型时，数值的大小和相应的符号可能会受到影响。因此，数据可能会丢失或产生错误。\n**浮点到整数转换**\n浮点转换为整数类型T的过程分为两步：\n\
      \n1. 将浮点值转换为int或long且该值为NaN时，将生成零值。否则，如果该值不是无穷大，则将其向零舍入为整数值V：\n\n* 如果T是 `long`，V可以表示为\
      \ `long`，则产生 `long`值V。\n* 如果V可以表示为`int`，则生成`int`值V。\n  否则\n* 该值为负无穷大或值太负而无法表示，并产生`Integer.MIN_VALUE`或`Long.MIN_VALUE`。\n\
      * 该值是正无穷大或值太正而无法表示，并产生 `Integer.MAX_VALUE` 或 `Long.MAX_VALUE`。\n\n2. 如果T是byte、char或short，则转换的结果是将第一步的结果缩小为T类型的结果\n\
      \   有关更多信息，请参阅 JLS，§5.1.3，“Narrowing Primitive Conversions”[JLS 2005]。\n\n**其他转换**\n\
      更小的原始类型可以转换为更大的类型，而不会影响数值的大小（有关更多信息，请参见 JLS，§5.1.2，“扩大原始转换”[JLS 2005]）。从 int\
      \ 或 long 转换为 float 或 from long to double 会导致精度损失（丢失最低有效位）。尽管有这种损失，但不会发生运行时异常。\n\
      \n请注意，从 float 到 double 或从 double 到 float 的转换也可能会丢失有关转换值的整体大小的信息（请参阅 NUM53-J。使用\
      \ strictfp 修饰符实现跨平台的浮点计算一致性以获取更多信息）。\n\n**不规范的代码示例1（整数缩小）**\n在这个不合规的代码示例中，一个\
      \ int 类型的值被转换为一个 byte 类型的值，而不进行范围检查：\n\n```java\nclass CastAway {\n  public\
      \ static void main(String[] args) {\n    int i = 128;\n    workWith(i);\n  }\n\
      \n  public static void workWith(int i) {\n    byte b = (byte) i;  // b has value\
      \ -128\n    // Work with b\n  }\n}\n```\n\n结果值可能是非预期的，因为初始值 (128) 超出了结果类型的范围。"
    Description_En: "Conversions of numeric types to narrower types can result in\
      \ lost or misinterpreted data if the value of the wider type is outside the\
      \ range of values of the narrower type. Consequently, all narrowing conversions\
      \ must be guaranteed safe by range-checking the value before conversion.\n\n\
      Java provides 22 possible narrowing primitive conversions. According to The\
      \ Java Languagee Specification (JLS), §5.1.3, \"Narrowing Primitive Conversions\"\
      \ [JLS 2015]:\n\n* `short` to `byte` or `char`\n* `char` to `byte` or `short`\n\
      * `int` to `byte`, `short`, or `char`\n* `long` to `byte`, `short`, `char`,\
      \ or `int`\n* `float` to `byte`, `short`, `char`, `int`, or `long`\n* `double`\
      \ to `byte`, `short`, `char`, `int`, `long`, or `float`\n\nNarrowing primitive\
      \ conversions are allowed in cases where the value of the wider type is within\
      \ the range of the narrower type.\n\nInteger Narrowing\n\nInteger type ranges\
      \ are defined by the JLS, §4.2.1, \"Integral Types and Values\" [JLS 2015],\
      \ and are also described in NUM00-J. Detect or prevent integer overflow.\n\n\
      The following table presents the rules for narrowing primitive conversions of\
      \ integer types. In the table, for an integer type `T`, `n` represents the number\
      \ of bits used to represent the resulting type `T` (precision).\n\n| From |\
      \ To | Description | Possible Resulting Errors |\n| - | - | - | - |\n| Signed\
      \ integer | Integral type `T` | Keeps only `n` lower-order bits | Lost or misinterpreted\
      \ data |\n| `char` | Integral type `T` | Keeps only `n` lower-order bits | Magnitude\
      \ error; negative number even though `char` is 16-bit unsigned |\n\nWhen integers\
      \ are cast to narrower data types, the magnitude of the numeric value and the\
      \ corresponding sign can be affected. Consequently, data can be lost or misinterpreted.\n\
      \n**Floating-Point to Integer Conversion**\n\nFloating-point conversion to an\
      \ integral type `T` is a two-step procedure:\n\n1. When converting a floating-point\
      \ value to an `int` or `long` and the value is a `NaN`, a zero value is produced.\
      \ Otherwise, if the value is not infinity, it is rounded toward zero to an integer\
      \ value `V`:\n\n* If `T` is `long` and `V` can be represented as a `long`, the\
      \ `long` value `V` is produced.\n* If `V` can be represented as an `int`, then\
      \ the `int` value `V` is produced.\n\nOtherwise,\n\n* The value is negative\
      \ infinity or a value too negative to be represented, and `Integer.MIN_VALUE`\
      \ or `Long.MIN_VALUE` is produced.\n* The value is positive infinity or a value\
      \ too positive to be represented, and `Integer.MAX_VALUE` or `Long.MAX_VALUE`\
      \ is produced.\n\n2. If `T` is `byte`, `char`, or `short`, the result of the\
      \ conversion is the result of a narrowing conversion to type `T` of the result\
      \ of the first step\n\nSee the JLS, §5.1.3, \"Narrowing Primitive Conversions\"\
      \ [JLS 2005], for more information.\n\n**Other Conversions**\n\nNarrower primitive\
      \ types can be cast to wider types without affecting the magnitude of numeric\
      \ values (see the JLS, §5.1.2, Widening Primitive Conversion)[JLS 2005]), for\
      \ more information). Conversion from `int` or `long` to `float` or from `long`\
      \ to `double` can lead to loss of precision (loss of least significant bits).\
      \ No runtime exception occurs despite this loss.\n\nNote that conversions from\
      \ `float` to `double` or from `double` to `float` can also lose information\
      \ about the overall magnitude of the converted value (see NUM53-J. Use the strictfp\
      \ modifier for floating-point calculation consistency across platforms for additional\
      \ information).\n\n**Noncompliant Code Example (Integer Narrowing)**\n\nIn this\
      \ noncompliant code example, a value of type `int` is converted to a value of\
      \ type `byte` without range checking:\n\n```java\nclass CastAway {\n  public\
      \ static void main(String[] args) {\n    int i = 128;\n    workWith(i);\n  }\n\
      \n  public static void workWith(int i) {\n    byte b = (byte) i;  // b has value\
      \ -128\n    // Work with b\n  }\n}\n```\n\nThe resulting value may be unexpected\
      \ because the initial value (128) is outside of the range of the resulting type."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 681:Incorrect Conversion between Numeric Types
      - type: CWE
        value: 197:Numeric Truncation Error
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140512'
    language: Java
  - Advice: 不要将ThreadPoolExecutor大小设置为0，当不需要线程池时可以将ThreadPoolExecutor置为null。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要创建空的ThreadPoolExecutor
    Categories_En:
      Category: []
    Default: true
    Description: "ThreadPoolExecutor大小设置为0，意味着线程池将没有任何线程，也就不会运行任何程序。  \n  \n**例如**：下列代码中ScheduledThreadPoolExecutor的CorePoolSize设置为0。\
      \  \n```java\npublic static void init() {  \n    ScheduledThreadPoolExecutor\
      \ stpe1 = new ScheduledThreadPoolExecutor(0); //构造方法设置CorePoolSize为0  \n   \
      \ ScheduledThreadPoolExecutor stpe2 = new ScheduledThreadPoolExecutor(10); \
      \ \n    stpe2.setCorePoolSize(0); //setCorePoolSize方法设置CorePoolSize为0  \n} \
      \ \n..."
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: correctness,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150427'
    language: Java
  - Advice: "**规范的代码样例1**\n这个解决方案声明了`helper`字段的易失性。\n```java\n// Works with acquire/release\
      \ semantics for volatile\n// Broken under JDK 1.4 and earlier\nfinal class Foo\
      \ {\n  private volatile Helper helper = null;\n\n  public Helper getHelper()\
      \ {\n    if (helper == null) {\n      synchronized (this) {\n        if (helper\
      \ == null) {\n          helper = new Helper();\n        }\n      }\n    }\n\
      \    return helper;\n  }\n}\n```\n当一个线程初始化`Helper`对象时，在这个线程和任何其他检索和返回实例的线程之间建立了一个发生前的关系[Pugh\
      \ 2004], [Manson 2004]。\n**规范的代码样例2**\n这个解决方案在静态变量的声明中初始化了`helper'字段 [Manson\
      \ 2006]。\n```java\nfinal class Foo {\n  private static final Helper helper =\
      \ new Helper();\n\n  public static Helper getHelper() {\n    return helper;\n\
      \  }\n}\n```\n被声明为静态的变量，在声明时或从静态初始化器中初始化的变量，在被其他线程看到之前，保证被完全构建。然而，这种解决方案放弃了懒惰初始化的好处。\n\
      **规范的代码样例3**\n这个解决方案使用了按需初始化，是程序员的习惯做法，通过在静态`Holder`内类中声明静态变量，隐含了懒惰初始化。\n```java\n\
      final class Foo {\n  // Lazy initialization\n  private static class Holder {\n\
      \    static Helper helper = new Helper();\n  }\n\n  public static Helper getInstance()\
      \ {\n    return Holder.helper;\n  }\n}\n```\n静态的`helper`字段的初始化被推迟到`getInstance()`方法被调用。必要的先发生关系是由类加载器加载和初始化`Holder`实例的动作和Java内存模型（JMM）提供的保证共同创造的。这个成语是一个比双重检查锁定成语更好的选择，用于懒惰地初始化静态字段\
      \ [Bloch 2008]。然而，这个成语不能用于懒惰地初始化实例字段[Bloch 2001]。\n**规范的代码样例4**\n这个解决方案（最初由Alexander\
      \ Terekhov [Pugh 2004]提出）使用一个`ThreadLocal`对象来跟踪每个单独的线程是否参与了创建所需的发生前关系的同步。每个线程只在同步的`createHelper()`方法中向其线程本地`perThreadInstance`存储一个非空值；因此，任何看到空值的线程必须通过调用`createHelper()`建立必要的发生前关系。\n\
      ```java\nfinal class Foo {\n  private final ThreadLocal<Foo> perThreadInstance\
      \ =\n      new ThreadLocal<Foo>();\n  private Helper helper = null;\n\n  public\
      \ Helper getHelper() {\n    if (perThreadInstance.get() == null) {\n      createHelper();\n\
      \    }\n    return helper;\n  }\n\n  private synchronized void createHelper()\
      \ {\n    if (helper == null) {\n      helper = new Helper();\n    }\n    //\
      \ Any non-null value can be used as an argument to set()\n    perThreadInstance.set(this);\n\
      \  }\n}\n```\n**规范的代码样例5**\n这个符合要求的解决方案使用一个局部变量来减少对`helper`字段的非同步读取次数，使之达到1。结果是，如果对`helper`的读取产生了一个非空值，它被缓存在一个其他线程无法访问的局部变量中，并被安全地返回。\n\
      ```java\npublic final class Helper {\n  private final int n;\n\n  public Helper(int\
      \ n) {\n    this.n = n;\n  }\n\n  // Other fields and methods, all fields are\
      \ final\n}\n\nfinal class Foo {\n  private Helper helper = null;\n\n  public\
      \ Helper getHelper() {\n    Helper h = helper;       // Only unsynchronized\
      \ read of helper\n    if (h == null) {\n      synchronized (this) {\n      \
      \  h = helper;          // In synchronized block, so this is safe\n        if\
      \ (h == null) {\n          h = new Helper(42);\n          helper = h;\n    \
      \    }\n      }\n    }\n    return h;\n  }\n}\n```"
    Advice_En: "!**Compliant Solution (Volatile)**\n\nThis compliant solution declares\
      \ the `helper` field volatile:\n\n```java\n// Works with acquire/release semantics\
      \ for volatile\n// Broken under JDK 1.4 and earlier\nfinal class Foo {\n  private\
      \ volatile Helper helper = null;\n\n  public Helper getHelper() {\n    if (helper\
      \ == null) {\n      synchronized (this) {\n        if (helper == null) {\n \
      \         helper = new Helper();\n        }\n      }\n    }\n    return helper;\n\
      \  }\n}\n```\n\nWhen a thread initializes the `Helper` object, a happens-before\
      \ relationship is established between this thread and any other thread that\
      \ retrieves and returns the instance [Pugh 2004], [Manson 2004].\n\n**Compliant\
      \ Solution (Static Initialization)**\n\nThis compliant solution initializes\
      \ the `helper` field in the declaration of the static variable [Manson 2006].\n\
      \n```java\nfinal class Foo {\n  private static final Helper helper = new Helper();\n\
      \n  public static Helper getHelper() {\n    return helper;\n  }\n}\n```\n\n\
      Variables that are declared static and initialized at declaration or from a\
      \ static initializer are guaranteed to be fully constructed before being made\
      \ visible to other threads. However, this solution forgoes the benefits of lazy\
      \ initialization.\n\n**Compliant Solution (Initialize-on-Demand, Holder Class\
      \ Idiom)**\n\nThis compliant solution uses the initialize-on-demand, holder\
      \ class idiom that implicitly incorporates lazy initialization by declaring\
      \ a static variable within a static `Holder` inner class:\n\n```java\nfinal\
      \ class Foo {\n  // Lazy initialization\n  private static class Holder {\n \
      \   static Helper helper = new Helper();\n  }\n\n  public static Helper getInstance()\
      \ {\n    return Holder.helper;\n  }\n}\n```\n\nInitialization of the static\
      \ `helper` field is deferred until the `getInstance()` method is called. The\
      \ necessary happens-before relationships are created by the combination of the\
      \ class loader's actions loading and initializing the `Holder` instance and\
      \ the guarantees provided by the Java memory model (JMM). This idiom is a better\
      \ choice than the double-checked locking idiom for lazily initializing static\
      \ fields [Bloch 2008]. However, this idiom cannot be used to lazily initialize\
      \ instance fields [Bloch 2001].\n\n**Compliant Solution (`ThreadLocal` Storage)**\n\
      \nThis compliant solution (originally suggested by Alexander Terekhov [Pugh\
      \ 2004]) uses a `ThreadLocal` object to track whether each individual thread\
      \ has participated in the synchronization that creates the needed happens-before\
      \ relationships. Each thread stores a non-null value into its thread-local `perThreadInstance`\
      \ only inside the synchronized `createHelper()` method; consequently, any thread\
      \ that sees a null value must establish the necessary happens-before relationships\
      \ by invoking `createHelper()`.\n\n```java\nfinal class Foo {\n  private final\
      \ ThreadLocal<Foo> perThreadInstance =\n      new ThreadLocal<Foo>();\n  private\
      \ Helper helper = null;\n\n  public Helper getHelper() {\n    if (perThreadInstance.get()\
      \ == null) {\n      createHelper();\n    }\n    return helper;\n  }\n\n  private\
      \ synchronized void createHelper() {\n    if (helper == null) {\n      helper\
      \ = new Helper();\n    }\n    // Any non-null value can be used as an argument\
      \ to set()\n    perThreadInstance.set(this);\n  }\n}\n```\n**Compliant Solution\
      \ (Immutable)**\n\nThis compliant solution uses a local variable to reduce the\
      \ number of unsynchronized reads of the `helper` field to 1. As a result, if\
      \ the read of `helper` yields a non-null value, it is cached in a local variable\
      \ that is inaccessible to other threads and is safely returned.\n\n```java\n\
      public final class Helper {\n  private final int n;\n\n  public Helper(int n)\
      \ {\n    this.n = n;\n  }\n\n  // Other fields and methods, all fields are final\n\
      }\n\nfinal class Foo {\n  private Helper helper = null;\n\n  public Helper getHelper()\
      \ {\n    Helper h = helper;       // Only unsynchronized read of helper\n  \
      \  if (h == null) {\n      synchronized (this) {\n        h = helper;      \
      \    // In synchronized block, so this is safe\n        if (h == null) {\n \
      \         h = new Helper(42);\n          helper = h;\n        }\n      }\n \
      \   }\n    return h;\n  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 09. 锁 (LCK)
      - type: DetailClassChin
        value: LCK10-J：使用一个正确的双重检查锁定的成语形式
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 09. Locking (LCK)
      - type: DetailClassChin
        value: LCK10-J:Use a correct form of the double-checked locking idiom
    Default: true
    Description: "*双重检查锁定成语*是一种软件设计模式，用于减少获取锁的开销，首先测试锁定标准而不实际获取锁。双重检查锁通过将同步限制在计算字段值或构建字段引用的新实例的罕见情况下，以及在检索已创建的实例或值的常见情况下放弃同步来提高性能。\n\
      双重检查锁的不正确形式包括那些允许公布未初始化或部分初始化的对象。因此，只有那些正确建立了 \"帮助者 \"引用和 \"帮助者 \"实例的完整构造的双重检查锁定成语的形式才是允许的。\n\
      双重检查锁定成语经常被用来实现单例工厂模式，该模式执行*懒惰初始化*。懒惰初始化将成员字段或成员字段引用的对象的构建推迟到实际需要实例时进行，而不是在类的构造函数中计算字段值或构建引用的对象。懒惰初始化有助于打破类和实例初始化中的有害循环。它还可以实现其他优化[Bloch\
      \ 2005]。\n懒惰初始化使用类或实例方法，这取决于成员对象是否是静态的。该方法检查实例是否已经被创建，如果没有，则创建它。如果实例已经存在，该方法就简单地返回该实例。\n\
      ```java\n// Correct single threaded version using lazy initialization\nfinal\
      \ class Foo {\n  private Helper helper = null;\n\n  public Helper getHelper()\
      \ {\n    if (helper == null) {\n      helper = new Helper();\n    }\n    return\
      \ helper;\n  }\n  // ...\n}\n```\n**不规范的代码样例1**\n双重检查锁定模式使用块同步而不是方法同步，并在尝试同步之前安装了一个额外的空引用检查。这个不符合规定的代码例子使用了不正确的双重检查锁模式的形式。\n\
      ```java\n// Double-checked locking idiom\nfinal class Foo {\n  private Helper\
      \ helper = null;\n  public Helper getHelper() {\n    if (helper == null) {\n\
      \      synchronized (this) {\n        if (helper == null) {\n          helper\
      \ = new Helper();\n        }\n      }\n    }\n    return helper;\n  }\n\n  //\
      \ Other methods and members...\n}\n```\n根据Pugh[Pugh 2004]的说法。\n\n> 初始化`Helper`对象的写入和对`helper`字段的写入可以不按顺序进行或被感知。因此，调用`getHelper()`的线程可以看到对`helper`对象的非空引用，但看到的是`helper`对象的默认值，而不是构造函数中设置的值。\n\
      >\n> 即使编译器没有对这些写入进行重新排序，在多处理器上，处理器或内存系统可能会对这些写入进行重新排序，这一点被运行在另一个处理器上的线程所感知。\n\
      \n这段代码也违反了TSM03-J.不要发布部分初始化的对象。\n**不规范的代码样例2**\n在这个不合规的代码例子中，`Helper'类通过声明其字段为final而变得不可变。JMM保证不可变的对象在被任何其他线程看到之前被完全构建。`getHelper()`方法中的块同步保证了所有能看到帮助器字段的非空值的线程也能看到完全初始化的`Helper`对象。\n\
      ```java\npublic final class Helper {\n  private final int n;\n\n  public Helper(int\
      \ n) {\n    this.n = n;\n  }\n\n  // Other fields and methods, all fields are\
      \ final\n}\n\nfinal class Foo {\n  private Helper helper = null;\n\n  public\
      \ Helper getHelper() {\n    if (helper == null) {            // First read of\
      \ helper\n      synchronized (this) {\n        if (helper == null) {       \
      \ // Second read of helper\n          helper = new Helper(42);\n        }\n\
      \      }\n    }\n    return helper;                   // Third read of helper\n\
      \  }\n}\n```\n然而，这段代码不能保证在所有的Java虚拟机平台上都能成功，因为在第一次读取和第三次读取`helper`之间没有发生之前的关系。因此，对`helper`的第三次读取有可能获得一个陈旧的空值（也许是因为它的值被编译器缓存或重新排序了），导致`getHelper()`方法返回一个空指针。"
    Description_En: "The *double-checked locking idiom* is a software design pattern\
      \ used to reduce the overhead of acquiring a lock by first testing the locking\
      \ criterion without actually acquiring the lock. Double-checked locking improves\
      \ performance by limiting synchronization to the rare case of computing the\
      \ field's value or constructing a new instance for the field to reference and\
      \ by foregoing synchronization during the common case of retrieving an already-created\
      \ instance or value.\n\nIncorrect forms of the double-checked locking idiom\
      \ include those that allow publication of an uninitialized or partially initialized\
      \ object. Consequently, only those forms of the double-checked locking idiom\
      \ that correctly establish a happens-before relationship both for the `helper`\
      \ reference and for the complete construction of the `Helper` instance are permitted.\n\
      \nThe double-checked locking idiom is frequently used to implement a singleton\
      \ factory pattern that performs *lazy initialization*. Lazy initialization defers\
      \ the construction of a member field or an object referred to by a member field\
      \ until an instance is actually required rather than computing the field value\
      \ or constructing the referenced object in the class's constructor. Lazy initialization\
      \ helps to break harmful circularities in class and instance initialization.\
      \ It also enables other optimizations [Bloch 2005].\n\nLazy initialization uses\
      \ either a class or an instance method, depending on whether the member object\
      \ is static. The method checks whether the instance has already been created\
      \ and, if not, creates it. When the instance already exists, the method simply\
      \ returns the instance:\n\n```java\n// Correct single threaded version using\
      \ lazy initialization\nfinal class Foo {\n  private Helper helper = null;\n\n\
      \  public Helper getHelper() {\n    if (helper == null) {\n      helper = new\
      \ Helper();\n    }\n    return helper;\n  }\n  // ...\n}\n```\n\n**Noncompliant\
      \ Code Example**\n\nThe double-checked locking pattern uses block synchronization\
      \ rather than method synchronization and installs an additional null reference\
      \ check before attempting synchronization. This noncompliant code example uses\
      \ an incorrect form of the double-checked locking idiom:\n\n```java\n// Double-checked\
      \ locking idiom\nfinal class Foo {\n  private Helper helper = null;\n  public\
      \ Helper getHelper() {\n    if (helper == null) {\n      synchronized (this)\
      \ {\n        if (helper == null) {\n          helper = new Helper();\n     \
      \   }\n      }\n    }\n    return helper;\n  }\n\n  // Other methods and members...\n\
      }\n```\n\nAccording to Pugh [Pugh 2004],\n\n> Writes that initialize the `Helper`\
      \ object and the write to the `helper` field can be done or perceived out of\
      \ order. As a result, a thread which invokes `getHelper()` could see a non-null\
      \ reference to a `helper` object, but see the default values for fields of the\
      \ `helper` object, rather than the values set in the constructor.\n>\n> Even\
      \ if the compiler does not reorder those writes, on a multiprocessor, the processor\
      \ or the memory system may reorder those writes, as perceived by a thread running\
      \ on another processor.\n\nThis code also violates TSM03-J. Do not publish partially\
      \ initialized objects.\n\n**Noncompliant Code Example (Immutable)**\n\nIn this\
      \ noncompliant code example, the `Helper` class is made immutable by declaring\
      \ its fields final. The JMM guarantees that immutable objects are fully constructed\
      \ before they become visible to any other thread. The block synchronization\
      \ in the `getHelper()` method guarantees that all threads that can see a non-null\
      \ value of the helper field will also see the fully initialized `Helper` object.\n\
      \n```java\npublic final class Helper {\n  private final int n;\n\n  public Helper(int\
      \ n) {\n    this.n = n;\n  }\n\n  // Other fields and methods, all fields are\
      \ final\n}\n\nfinal class Foo {\n  private Helper helper = null;\n\n  public\
      \ Helper getHelper() {\n    if (helper == null) {            // First read of\
      \ helper\n      synchronized (this) {\n        if (helper == null) {       \
      \ // Second read of helper\n          helper = new Helper(42);\n        }\n\
      \      }\n    }\n    return helper;                   // Third read of helper\n\
      \  }\n}\n```\n\nHowever, this code is not guaranteed to succeed on all Java\
      \ Virtual Machine platforms because there is no happens-before relationship\
      \ between the first read and third read of `helper`. Consequently, it is possible\
      \ for the third read of `helper` to obtain a stale null value (perhaps because\
      \ its value was cached or reordered by the compiler), causing the `getHelper()`\
      \ method to return a null pointer."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 557:Concurrency Issues
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 6.8.4 持锁时调用sleep方法
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141010'
    language: Java
  - Advice: "如果需要对实例字段使用线程安全的延迟初始化，请使用基于volatile的延迟初始化的方案。如果需要对静态字段使用线程安全的延迟初始化，基于类初始化的方案。两种方式示例如下。\
      \  \n  \n**例1**：基于volatile的双重检查锁定的解决方案。  \n```java\npublic class SafeDoubleCheckedLocking\
      \ {  \n    private volatile static Instance instance;  \n    public static Instance\
      \ getInstance() {  \n        if (instance == null) {  \n            synchronized\
      \ (SafeDoubleCheckedLocking.class) {  \n                if (instance == null)\
      \  \n                    instance = new Instance();//volatile instance  \n \
      \           }  \n        }  \n        return instance;  \n    }  \n}\n```  \n\
      这个解决方案需要JDK5或更高版本，因为从JDK5开始使用新的JSR-133内存模型规范，这个规范增强了volatile的语义。  \n**例2**：基于类初始化的解决方案。\
      \  \n```java\npublic class InstanceFactory {  \n    private static class InstanceHolder\
      \ {  \n        public static Instance instance = new Instance();  \n    }  \n\
      \    public static Instance getInstance() {  \n        return InstanceHolder.instance\
      \ ;  //InstanceHolder class is initialized  \n    }  \n}\n```  \n  JVM在类的初始化阶段（即在Class被加载后，且被线程使用之前），会执行类的初始化。在执行类的初始化期间，JVM会去获取一个锁。这个锁可以同步多个线程对同一个类的初始化。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 并发规范
      - type: DetailClassChin
        value: 【强制】不要使用双重检查锁定
    Categories_En:
      Category: []
    Default: true
    Description: "在程序开发中，有时需要推迟一些高开销的对象初始化操作，并且只有在使用这些对象时才进行初始化，此时开发者可能会采用延迟初始化。但要正确实现线程安全的延迟初始化需要一些技巧，否则容易出现问题，如下面例子所示。\
      \  \n  \n**例1**：下面代码示例中，延迟初始化对象不是线程安全的。  \n```java\npublic class UnsafeLazyInitialization\
      \ {  \n    private static Instance instance;  \n    public static Instance getInstance()\
      \ {  \n        if (instance == null) {        //1：Thread A executed  \n    \
      \        instance = new Instance(); //2：Thread B executed  \n        }  \n \
      \       return instance;  \n    }  \n}\n```  \n在例1中，假设A线程执行代码1的同时，B线程执行代码2。此时，线程A可能会看到instance引用的对象还没有完成初始化。\
      \  \n对应的解决方式，可以对`getInstance()`做同步处理来实现线程安全的延迟初始化。示例代码如下：  \n**例2**：对`getInstance()`同步处理来实现线程安全的延迟初始化。\
      \        \n```java\npublic class SafeLazyInitialization {  \n    private static\
      \ Instance instance;  \n    public synchronized static Instance getInstance()\
      \ {  \n        if (instance == null) {  \n            instance = new Instance();\
      \  \n        }  \n        return instance;  \n    }  \n}   \n```\n在例2代码示例中，如果`getInstance()`被多个线程频繁的调用，将会导致程序执行性能的下降。对此，开发人员想通过双重检查锁定来降低同步的开销。示例代码如下：\
      \  \n**例3**：下面代码示例中，使用双重校验锁来实现延迟初始化。  \n```java\npublic class DoubleCheckedLocking\
      \ {                        //1  \n    private static Instance instance;    \
      \                  //2  \n    public static Instance getInstance() {       \
      \          //3  \n        if (instance == null) {                          \
      \  //4:first check  \n            synchronized (DoubleCheckedLocking.class)\
      \ {    //5:lock  \n                 if (instance == null)                  \
      \   //6:second check  \n                     instance = new Instance();    \
      \        //7:problem  \n            }                                      \
      \        //8  \n        }                                                  //9\
      \  \n        return instance;                                   //10  \n   \
      \ }                                                      //11  \n}\n``` \n在例3的代码示例中，开发者试图通过检测第一次instance不为null，就不需要执行下面的加锁和初始化操作。希望可以大幅降低例2同步方法带来的性能开销。然而，这是一个错误的优化。在线程执行到第4行代码读取到instance不为null时，instance引用的对象有可能还没有完成初始化。原因如下：\
      \  \n第7行（`instance = new Singleton();`）创建一个对象。这一行代码可以分解为如下的三行伪代码：\n```java\n\
      memory = allocate();   //1：分配对象的内存空间  \nctorInstance(memory);  //2：初始化对象  \n\
      instance = memory;     //3：设置instance指向刚分配的内存地址  \n```  \n上面三行伪代码中的2和3之间，可能会被重排序。2和3之间重排序之后的执行时序如下：\
      \  \n```java\nmemory = allocate();   //1：分配对象的内存空间  \ninstance = memory;   \
      \  //3：设置instance指向刚分配的内存地址  \n                       //注意，此时对象还没有被初始化  \nctorInstance(memory);\
      \  //2：初始化对象  \n```\n根据Java语言规范《The Java Language Specification, Java SE 7 Edition》，所有线程在执行java程序时必须要遵守intra-thread\
      \ semantics。intra-thread semantics保证重排序不会改变单线程内的程序执行结果。但是当多线程执行时，线程可能会看到一个还没有被初始化的对象。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Double-Checked Locking
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 667:Improper Locking
      - type: CWE
        value: 609:Double-Checked Locking
    Security: not
    Tag: concurrency,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150305'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n   ```java\npublic class SafeDoubleCheckedLocking\
      \ {  \n    private volatile static Instance instance;  \n    public static Instance\
      \ getInstance() {  \n        if (instance == null) {  \n            synchronized\
      \ (SafeDoubleCheckedLocking.class) {  \n                if (instance == null)\
      \  \n                    instance = new Instance();//volatile instance  \n \
      \           }  \n        }  \n        return instance;  \n    }  \n}\n```  \n\
      基于volatile的双重检查锁定的解决方案。  \n这个解决方案需要JDK5或更高版本，因为从JDK5开始使用新的JSR-133内存模型规范，这个规范增强了volatile的语义。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.19 不要使用双重检查锁定
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：在程序开发中，有时需要推迟一些高开销的对象初始化操作，并且只有在使用这些对象时才进行初始化，此时开发者可能会采用延迟初始化。但要正确实现线程安全的延迟初始化需要一些技巧，否则容易出现问题，如下面例子所示。\
      \  \n对于不要使用双重检查锁定的情况，示例1给出了不规范用法（java语言）示例。  \n开发人员想通过双重检查锁定来降低同步的开销。示例代码如下：\
      \  \n**示例1**：   \n```java\npublic class DoubleCheckedLocking {             \
      \           //1  \n    private static Instance instance;                   \
      \   //2  \n    public static Instance getInstance() {                 //3  \n\
      \        if (instance == null) {                            //4:first check\
      \  \n            synchronized (DoubleCheckedLocking.class) {    //5:lock  \n\
      \                 if (instance == null) {                   //6:second check\
      \  \n                     instance = new Instance();            //7:problem\
      \  \n                 }                                         //8  \n    \
      \        }                                              //9  \n            return\
      \ instance;                               //10  \n        }                \
      \                                  //11  \n    }  \n}\n```  \n上面代码示例中，使用双重校验锁来实现延迟初始化。在示例3的代码示例中，开发者试图通过检测第一次`instance`不为null，就不需要执行下面的加锁和初始化操作。希望可以大幅降低例2同步方法带来的性能开销。然而，这是一个错误的优化。在线程执行到第4行代码读取到`instance`不为null时，`instance`引用的对象有可能还没有完成初始化。原因如下：\
      \  \n第7行（`instance = new Singleton();`）创建一个对象。这一行代码可以分解为如下的三行伪代码：  \n```java\n\
      memory = allocate();   //1：分配对象的内存空间  \nctorInstance(memory);  //2：初始化对象  \n\
      instance = memory;     //3：设置instance指向刚分配的内存地址  \n```\n上面三行伪代码中的2和3之间，可能会被重排序。2和3之间重排序之后的执行时序如下：\n\
      ```java\nmemory = allocate();   //1：分配对象的内存空间  \ninstance = memory;     //3：设置instance指向刚分配的内存地址\
      \  \n                       //注意，此时对象还没有被初始化  \nctorInstance(memory);  //2：初始化对象\
      \  \n```  \n如果需要对实例字段使用线程安全的延迟初始化，需使用基于volatile的延迟初始化的方案。如果需要对静态字段使用线程安全的延迟初始化，基于类初始化的方案。两种方式示例如下。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 667:Improper Locking
      - type: CWE
        value: 609:Double-Checked Locking
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170051'
    language: Java
  - Advice: "将SearchControls配置为不允许反序列化。  \n  \n**例如**：下面代码示例中SearchControls不允许反序列化。\
      \  \n```java\nDirContext ctx = new InitialDirContext();  \n...  \nctx.search(query,\
      \ filter, new SearchControls(scope, countLimit, timeLimit, attributes,false,deref));\n\
      ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】应该禁用LDAP反序列化
    Categories_En:
      Category: []
    Default: true
    Description: "JNDI支持从LDAP目录中反序列化对象，这是不安全的，并且可能导致远程代码执行。当使用配置为允许反序列化的SearchControls执行LDAP搜索查询时，可能会引发问题。\
      \  \n  \n**例如**：下面代码示例中SearchControls允许反序列化。  \n```java\nDirContext ctx = new\
      \ InitialDirContext();  \n...  \nctx.search(query, filter, new SearchControls(scope,\
      \ countLimit, timeLimit, attributes,true,deref));\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 502
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 502
      - type: CWE
        value: 502:Deserialization of Untrusted Data
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
    Security: not
    Tag: security
    Type: std
    WhiteList: not
    id: 0202000010150438
    language: Java
  - Advice: "**规范的代码样例1（限制文件大小）**：  \n```java\nclass ReadNames {\n    // ... Other\
      \ methods and variables\n\n    public static final int fileSizeLimit = 1000000;\n\
      \n    public ReadNames(String filename) throws IOException {\n        long size\
      \ = Files.size(Paths.get(filename));\n        if (size > fileSizeLimit) {\n\
      \            throw new IOException(\"File too large\");\n        } else if (size\
      \ == 0L) {\n            throw new IOException(\"File size cannot be determined,\
      \ possibly too large\");\n        }\n        this.input = new FileReader(filename);\n\
      \        this.reader = new BufferedReader(input);\n    }\n}\n```    \n这个符合规则的方案对被读入的文件设置了大小的限制，这通过`Files.size()`方法来实现，这个方法是在\
      \ Java SE 7 中新出现的。如果一个文件在这个限制范围内，我们认为标准的`readLine()`方法不会耗尽内存，或者也不会被while循环耗尽内存。\
      \    \n**规范的代码样例2（有限的输入长度）**：\n```java\nclass ReadNames {\n    // ... Other\
      \ methods and variables\n\n    public static String readLimitedLine(Reader reader,\
      \ int limit) throws IOException {\n        StringBuilder sb = new StringBuilder();\n\
      \        for (int i = 0; i < limit; i++) {\n            int c = reader.read();\n\
      \            if (c == -1) {\n                return ((sb.length() > 0) ? sb.toString()\
      \ : null);\n            }\n            if (((char) c == '\\n') || ((char) c\
      \ == '\\r')) {\n                break;\n            }\n            sb.append((char)\
      \ c);\n        }\n        return sb.toString();\n    }\n\n    public static\
      \ final int lineLengthLimit = 1024;\n    public static final int lineCountLimit\
      \ = 1000000;\n\n    public void addNames() throws IOException {\n        try\
      \ {\n            String newName;\n            for (int i = 0; i < lineCountLimit;\
      \ i++) {\n                newName = readLimitedLine(reader, lineLengthLimit);\n\
      \                if (newName == null || newName.equalsIgnoreCase(\"quit\"))\
      \ {\n                    break;\n                }\n                names.addElement(newName);\n\
      \                System.out.println(\"adding \" + newName);\n            }\n\
      \        } finally {\n            input.close();\n        }\n    }\n\n}\n```\
      \  \n这个符合规则的方案对每一行的长度加入vector的总数量都加以限制。`readLimitedLine()`方法使用一个数值限制，用它来表示在一行中可以存在的所有字符数量。如果一行包含了更多的字符，这一行会被截断，并且字符会被返回到下一次调用。这可以防止攻击者通过提供没有行中断的输入来耗尽内存。\
      \  \n**规范的代码样例3**：  \n```java\n// ...  \nint count = 10000000;  \n// ...\n```\
      \  \n这个符合规范的简单方案减少了读入的名称的数量。  \n**规范的代码样例4**：  \n通过保证不使用无限循环、不出现内存泄漏并且不保留不需要的对象，可以避免OutOfMemoryError。当预先知道内存需求时，可以使用下面的方法来对内存进行剪裁，以满足要求。\
      \  \n`java -Xms<initial heap size> -Xmx<maximum heap size>`  \n  \n**例如**： \
      \ \n`java -Xms128m -Xmx512m ReadNames`  \n上面的代码将初始的堆大小设置为128MB，而最大的堆大小设置为512MB。\
      \  \n这些设置可以使用命令行下的Java控制台来改变。它们通过应用自身进行调整。"
    Advice_En: "**Compliant Solution (Limited File Size)**\n\nThis compliant solution\
      \ imposes a limit on the size of the file being read. The limit is set with\
      \ the `Files.size()` method, which was introduced in Java SE 7. If the file\
      \ is within the limit, we can assume the standard `readLine()` method will not\
      \ exhaust memory, nor will memory be exhausted by the `while` loop.\n\n```java\n\
      class ReadNames {\n  // ... Other methods and variables\n\n  public static final\
      \ int fileSizeLimit = 1000000;\n\n  public ReadNames(String filename) throws\
      \ IOException {\n    long size = Files.size( Paths.get( filename));\n    if\
      \ (size > fileSizeLimit) {\n      throw new IOException(\"File too large\");\n\
      \    } else if (size == 0L) {\n      throw new IOException(\"File size cannot\
      \ be determined, possibly too large\");\n    }\n    this.input = new FileReader(filename);\n\
      \    this.reader = new BufferedReader(input);\n  }\n}\n```\n\n**Compliant Solution\
      \ (Limited Length Input)**\n\nThis compliant solution imposes limits both on\
      \ the length of each line and on the total number of items to add to the vector.\
      \ (It does not depend on any Java SE 7 or later features.)\n\n```java\nclass\
      \ ReadNames {\n  // ... Other methods and variables\n\n  public static String\
      \ readLimitedLine(Reader reader, int limit)\n                              \
      \         throws IOException {\n    StringBuilder sb = new StringBuilder();\n\
      \    for (int i = 0; i < limit; i++) {\n      int c = reader.read();\n     \
      \ if (c == -1) {\n        return ((sb.length() > 0) ? sb.toString() : null);\n\
      \      }\n      if (((char) c == '\\n') || ((char) c == '\\r')) {\n        break;\n\
      \      }\n      sb.append((char) c);\n    }\n    return sb.toString();\n  }\n\
      \n  public static final int lineLengthLimit = 1024;\n  public static final int\
      \ lineCountLimit = 1000000;\n\n  public void addNames() throws IOException {\n\
      \    try {\n      String newName;\n      for (int i = 0; i < lineCountLimit;\
      \ i++) {\n        newName = readLimitedLine(reader, lineLengthLimit);\n    \
      \    if (newName == null || newName.equalsIgnoreCase(\"quit\")) {\n        \
      \  break;\n        }\n        names.addElement(newName);\n        System.out.println(\"\
      adding \" + newName);\n      }\n    } finally {\n      input.close();\n    }\n\
      \  }\n\n}\n```\n\nThe `readLimitedLine()` method takes a numeric limit, indicating\
      \ the total number of characters that may exist on one line. If a line contains\
      \ more characters, the line is truncated, and the characters are returned on\
      \ the next invocation. This prevents an attacker from exhausting memory by supplying\
      \ input with no line breaks.\n**Compliant Solution**\n\nA simple compliant solution\
      \ is to reduce the number of names to read:\n\n```java\n// ...\nint count =\
      \ 10000000;\n// ...\n```\n\n**Compliant Solution**\n\nThe `OutOfMemoryError`\
      \ can be avoided by ensuring the absence of infinite loops, memory leaks, and\
      \ unnecessary object retention. When memory requirements are known ahead of\
      \ time, the heap size can be tailored to fit the requirements using the following\
      \ runtime parameters [Java 2006]:\n\n`java -Xms<initial heap size> -Xmx<maximum\
      \ heap size>`\n\nFor example,\n\n`java -Xms128m -Xmx512m ReadNames`\n\nHere\
      \ the initial heap size is set to 128MB and the maximum heap size to 512MB.\n\
      \nThese settings can be changed either using the Java Control Panel or from\
      \ the command line. They cannot be adjusted through the application itself."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 49. 其他 (MSC)
      - type: DetailClassChin
        value: MSC05-J：不要耗尽堆空间
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 49. Miscellaneous (MSC)
      - type: DetailClassChin
        value: MSC05-J:Do not exhaust heap space
    Default: true
    Description: "当程序想要使用更多可获得的堆空间时，Java的 OutofMemoryError 就会发生。这个错误会基于以下原因：  \n内存泄漏（参考规则MSC04-J）\
      \  \n一个无限的循环  \n可获得的有限的默认堆内存  \n对常用数据结构（散列表、vector等）的不正确实现  \n不受限制的序列化  \n在ObjectOutputStream中写入大量的对象（参考规则\
      \ SER10-J）  \n创建大量的线程  \n解压一个文件（参考规则 IDS04-J）  \n\n其中一个原因与平台相关，并且难以预测。其它的比较容易预测，比如从文件中读取数据。因此，如果一种方式会导致内存耗尽，那么程序不能使用这种方法来接收非受信的输入。\
      \  \n**不规范的代码样例1**：  \n```java\nclass ReadNames {\n  private Vector<String>\
      \ names = new Vector<String>();\n  private final InputStreamReader input;\n\
      \  private final BufferedReader reader;\n\n  public ReadNames(String filename)\
      \ throws IOException {\n    this.input = new FileReader(filename);\n    this.reader\
      \ = new BufferedReader(input);\n  }\n\n  public void addNames() throws IOException\
      \ {\n    try {\n      String newName;\n      while (((newName = reader.readLine())\
      \ != null) &&\n             !(newName.equalsIgnoreCase(\"quit\"))) {\n     \
      \   names.addElement(newName);\n        System.out.println(\"adding \" + newName);\n\
      \      }\n    } finally {\n      input.close();\n    }\n  }\n\n  public static\
      \ void main(String[] args) throws IOException {\n    if (args.length != 1) {\n\
      \      System.out.println(\"Arguments: [filename]\");\n      return;\n    }\n\
      \    ReadNames demo = new ReadNames(args[0]);\n    demo.addNames();\n  }\n}\n\
      ```\n不合规则的方案从文件中读入行文本，并且把每一行加入到vector中，直到在这一行中遇到\"quit\"一词。这段代码没有限制需要执行程序的内存空间上限，因此，程序可以通过两种方式很容易的耗尽已有的堆空间。第一种方式是，攻击者可以在文件中提供任意多的行，使得vector一直增长直到内存消耗完为止。第二种方式是，攻击者通过简单的提供一个任意长的行，使得`readLine()`方法将内存消耗完。根据Java\
      \ API 文档的描述[API 2014], BufferedReader.readLine()方法的描述：  \n>读取文本的一行，认为这一行结束，在遇到以下任意一个符号时，如换行符（‘\\\
      n’）、回车符（‘\\r’）或者回车换行符。  \n\n任何使用这个方法的代码都容易受到资源耗尽型攻击，因为一个用户可能输入一个任意长度的字符串。  \n\
      **不规范的代码样例2**：  \n```java\n/*\n * Assuming the heap size as 512 MB (calculated\
      \ as 1/4 of 2GB RAM = 512MB)\n * Considering long values being entered (64 bits\
      \ each, the max number of\n * elements would be 512MB/64 bits = 67108864)\n\
      \ */\npublic class ReadNames {\n    // Accepts unknown number of records\n \
      \   Vector<Long> names = new Vector<Long>();\n    long newID = 0L;\n    int\
      \ count = 67108865;\n    int i = 0;\n    InputStreamReader input = new InputStreamReader(System.in);\n\
      \    Scanner reader = new Scanner(input);\n\n    public void addNames() {\n\
      \        try {\n            do {\n                // Adding unknown number of\
      \ records to a list\n                // The user can enter more IDs than the\
      \ heap can support and,\n                // as a result, exhaust the heap. Assume\
      \ that the record ID\n                // is a 64-bit long value\n          \
      \      System.out.print(\"Enter recordID (To quit, enter -1): \");\n       \
      \         newID = reader.nextLong();\n\n                names.addElement(newID);\n\
      \                i++;\n            } while (i < count || newID != -1);\n   \
      \     } finally {\n            input.close();\n        }\n    }\n\n    public\
      \ static void main(String[] args) {\n        ReadNames demo = new ReadNames();\n\
      \        demo.addNames();\n    }\n}\n```\n在使用并行垃圾回收器的服务器类的机器中，默认的初始化和最大的堆大小遵守Java\
      \ SE 6 [Sun 2006]的要求：  \n初始化堆大小：高于1/64的机器物理内存或合理的最小值  \n最大的堆大小：小于1/4的物理内存或者1GB内存\
      \  \n上面不符合规则的代码示例要求得到比默认值更多的堆内存。"
    Description_En: "A Java `OutofMemoryError` occurs when the program attempts to\
      \ use more heap space than is available. Among other causes, this error may\
      \ result from the following:\n\n* A memory leak (see MSC04-J. Do not leak memory)\n\
      * An infinite loop\n* Limited amounts of default heap memory available\n* Incorrect\
      \ implementation of common data structures (hash tables, vectors, and so on)\n\
      * Unbounded deserialization\n* Writing a large number of objects to an `ObjectOutputStream`\
      \ (see SER10-J. Avoid memory and resource leaks during serialization)\n* Creating\
      \ a large number of threads.\n* Uncompressing a file (see IDS04-J. Safely extract\
      \ files from ZipInputStream)\n\nSome of these causes are platform-dependent\
      \ and difficult to anticipate. Others, such as reading data from a file, are\
      \ fairly easy to anticipate. As a result, programs must not accept untrusted\
      \ input in a manner that can cause the program to exhaust memory.\n\n**Noncompliant\
      \ Code Example (`readLine()`)**\n\nThis noncompliant code example reads lines\
      \ of text from a file and adds each one to a vector until a line with the word\
      \ \"quit\" is encountered:\n\n```java\nclass ReadNames {\n  private Vector<String>\
      \ names = new Vector<String>();\n  private final InputStreamReader input;\n\
      \  private final BufferedReader reader;\n\n  public ReadNames(String filename)\
      \ throws IOException {\n    this.input = new FileReader(filename);\n    this.reader\
      \ = new BufferedReader(input);\n  }\n\n  public void addNames() throws IOException\
      \ {\n    try {\n      String newName;\n      while (((newName = reader.readLine())\
      \ != null) &&\n             !(newName.equalsIgnoreCase(\"quit\"))) {\n     \
      \   names.addElement(newName);\n        System.out.println(\"adding \" + newName);\n\
      \      }\n    } finally {\n      input.close();\n    }\n  }\n\n  public static\
      \ void main(String[] args) throws IOException {\n    if (args.length != 1) {\n\
      \      System.out.println(\"Arguments: [filename]\");\n      return;\n    }\n\
      \    ReadNames demo = new ReadNames(args[0]);\n    demo.addNames();\n  }\n}\n\
      ```\n\nThe code places no upper bounds on the memory space required to execute\
      \ the program. Consequently, the program can easily exhaust the available heap\
      \ space in two ways. First, an attacker can supply arbitrarily many lines in\
      \ the file, causing the vector to grow until memory is exhausted. Second, an\
      \ attacker can simply supply an arbitrarily long line, causing the readLine()\
      \ method to exhaust memory. According to the Java API documentation [API 2014],`BufferedReader.readLine()`\
      \ method\n\n> Reads a line of text. A line is considered to be terminated by\
      \ any one of a line feed ('`\\n`'), a carriage return ('`\\r`'), or a carriage\
      \ return followed immediately by a linefeed.\n\nAny code that uses this method\
      \ is susceptible to a resource exhaustion attack because the user can enter\
      \ a string of any length.\n\n**Noncompliant Code Example**\n\nIn a server-class\
      \ machine using a parallel garbage collector, the default initial and maximum\
      \ heap sizes are as follows for Java SE 6 [Sun 2006]:\n\n* Initial heap size:\
      \ larger of 1/64 of the machine's physical memory or some reasonable minimum.\n\
      * Maximum heap size: smaller of 1/4 of the physical memory or 1GB.\n\nThis noncompliant\
      \ code example requires more memory on the heap than is available by default:\n\
      \n```java\n/* Assuming the heap size as 512 MB\n * (calculated as 1/4 of 2GB\
      \ RAM = 512MB)\n * Considering long values being entered (64 bits each,\n *\
      \ the max number of elements would be 512MB/64 bits =\n * 67108864)\n */\npublic\
      \ class ReadNames {\n  // Accepts unknown number of records\n  Vector<Long>\
      \ names = new Vector<Long>();\n  long newID = 0L;\n  int count = 67108865;\n\
      \  int i = 0;\n  InputStreamReader input = new InputStreamReader(System.in);\n\
      \  Scanner reader = new Scanner(input);\n\n  public void addNames() {\n    try\
      \ {\n      do {\n        // Adding unknown number of records to a list\n   \
      \     // The user can enter more IDs than the heap can support and,\n      \
      \  // as a result, exhaust the heap. Assume that the record ID\n        // is\
      \ a 64-bit long value\n        System.out.print(\"Enter recordID (To quit, enter\
      \ -1): \");\n        newID = reader.nextLong();\n\n        names.addElement(newID);\n\
      \        i++;\n      } while (i < count || newID != -1);\n    } finally {\n\
      \      input.close();\n    }\n  }\n\n  public static void main(String[] args)\
      \ {\n    ReadNames demo = new ReadNames();\n    demo.addNames();\n  }\n}\n```\n\
      \n**Compliant Solution**\n\nA simple compliant solution is to reduce the number\
      \ of names to read:\n\n```java\n// ...\nint count = 10000000;\n// ...\n```\n\
      \n**Compliant Solution**\n\nThe `OutOfMemoryError` can be avoided by ensuring\
      \ the absence of infinite loops, memory leaks, and unnecessary object retention.\
      \ When memory requirements are known ahead of time, the heap size can be tailored\
      \ to fit the requirements using the following runtime parameters [Java 2006]:\n\
      \n`java -Xms<initial heap size> -Xmx<maximum heap size>`\n\nFor example,\n\n\
      `java -Xms128m -Xmx512m ReadNames`\n\nHere the initial heap size is set to 128MB\
      \ and the maximum heap size to 512MB.\n\nThese settings can be changed either\
      \ using the Java Control Panel or from the command line. They cannot be adjusted\
      \ through the application itself."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 400
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 400
      - type: CWE
        value: 770:Allocation of Resources without Limits or Throttling
      - type: CWE
        value: 400:Uncontrolled Resource Consumption ("Resource Exhaustion")
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010141805
    language: Java
  - Advice: "**规范的代码样例1**：\n代码使用一个类型为int的变量来获取`read()`方法的返回值。当`read()`方法返回值不是-1的时候，它会被安全的转换为byte类型。\
      \  \n```java\nFileInputStream in;\n// initialize stream\nint inbuff;\nbyte data;\n\
      while ((inbuff = in.read()) != -1) {\n    data = (byte) inbuff;\n    // ...\n\
      }\n```  \n**规范的代码样例2**：  \n代码使用一个类型为int的变量来获取`read()`方法的返回值。当`read()`方法返回值不是-1的时候，它会被安全的转换为char类型。\
      \  \n```java\nFileReader in;\n// initialize stream\nint inbuff;\nchar data;\n\
      while ((inbuff = in.read()) != -1) {\n    data = (char) inbuff;\n    // ...\n\
      }\n```"
    Advice_En: "**Compliant Solution (`byte`)**\n\nUse a variable of type `int` to\
      \ capture the return value of the `byte` input method. When the value returned\
      \ by `read()` is not −1, it can be safely cast to type `byte`. When `read()`\
      \ returns `0x000000FF`, the comparison will test against `0xFFFFFFFF`, which\
      \ evaluates to false.\n\n```java\nFileInputStream in;\n// Initialize stream\n\
      int inbuff;\nbyte data;\nwhile ((inbuff = in.read()) != -1) {\n  data = (byte)\
      \ inbuff;\n  // ...\n}\n```\n**Compliant Solution (`char`)**\n\nUse a variable\
      \ of type `int` to capture the return value of the character input method. When\
      \ the value returned by `read()` is not −1, it can be safely cast to type `char`.\n\
      \n```java\nFileReader in;\n// Initialize stream\nint inbuff;\nchar data;\nwhile\
      \ ((inbuff = in.read()) != -1) {\n  data = (char) inbuff;\n  // ...\n}\n```"
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO08-J：区分从流中读取的字符（或字节）和-1的不同
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO08-J:Distinguish between characters or bytes read from a stream
          and -1
    Default: true
    Description: "抽象方法`InputStream.read()`方法从一个输入源中读取一个简单的字节，返回一个范围在0~255的整数。仅当在读取到输入流末尾的时候，它会返回-1。类似的`Reader.read()`方法读取一个单独的字符，返回范围在0~65535的整数。仅当在读取到输入流的末尾时，它会返回-1。这两个方法都会被子类覆写。\
      \  \n通常会用这些方法来从一个流中读取一个字节或者字符。遗憾的是，在检查是否已经达到流末尾之前，许多开发者过早的将返回类型转换为byte或者char类型。\
      \  \n风险评估：违反该条准则可能会导致诸多注入攻击：如命令注入。    \n**不规范的代码样例1**：  \n代码将`read()`方法返回的值直接强转为byte类型，并且与-1做比较，通过这种方法来检查是否达到了流的末尾。如果`read()`方法返回的是0xFF的字节值，那么返回的字节值专用来表示以及达到末尾的-1的值是没有差别的，这个字节在与-1进行比较之前会被转换并且使用符号来扩展为一个int类型。\
      \  \n```java\nFileInputStream in;\n// initialize stream\nbyte data;\nwhile ((data\
      \ = (byte) in.read()) != -1) {\n    // ...\n}\n```  \n**不规范的代码样例2**：  \n代码将由`read()`方法返回的类型为int的值直接转换为char类型，它会与-1进行比较，用来检测是否达到了流末尾。它的值最好为0xFFFF而非-1，这个是否达到流末尾的判断永远不会返回true。\
      \  \n```java\nFileReader in;\n// initialize stream\nchar c;\nwhile ((c = (char)\
      \ in.read()) != -1) {\n    // ...\n}\n```"
    Description_En: "The abstract `InputStream.read()` and `Reader.read()` methods\
      \ are used to read a byte or character, respectively, from a stream. The  `InputStream.read()`\
      \ method reads a single byte from an input source and returns its value as an\
      \ `int` in the range 0 to 255 (`0x00`-`0xff`). The `Reader.read()` method reads\
      \ a single character and returns its value as an `int` in the range 0 to 65,535\
      \ (`0x0000`-`0xffff`). Both methods return the 32-bit value `-1` (`0xffffffff`)\
      \ to indicate that the end of the stream has been reached and no data is available.\
      \ The larger `int` size is used by both methods to differentiate between the\
      \ end-of-stream indicator and the maximum byte (`0xff`) or character (`0xffff`)\
      \ value. The end-of-stream indicator is an example of an in-band error indicator.\
      \ In-band error indicators are problematic to work with, and the creation of\
      \ new in-band-error indicators is discouraged.\nPrematurely converting the resulting\
      \ `int` to a `byte` or `char` before testing for the value −1 makes it impossible\
      \ to distinguish between characters read and the end of stream indicator. Programs\
      \ must check for the end of stream before narrowing the return value to a `byte`\
      \ or `char`.\nThis rule applies to any method that returns the value −1 to indicate\
      \ the end of a stream. It includes any `InputStream` or `Reader` subclass that\
      \ provides an implementation of the `read()` method. This rule is a specific\
      \ instance ofNUM12-J. Ensure conversions of numeric types to narrower types\
      \ do not result in lost or misinterpreted data.\n**Noncompliant Code Example\
      \ (`byte`)**\n\n`FileInputStream` is a subclass of `InputStream`. It will return\
      \ −1 only when the end of the input stream has been reached. This noncompliant\
      \ code example casts the value returned by the `read()` method directly to a\
      \ value of type `byte` and then compares this value with −1 in an attempt to\
      \ detect the end of the stream.\n\n```java\nFileInputStream in;\n// Initialize\
      \ stream\nbyte data;\nwhile ((data = (byte) in.read()) != -1) {\n  // ...\n\
      }\n```\n\nIf the `read()` method encounters a `0xFF` byte in the file, this\
      \ value becomes indistinguishable from the −1 value used to indicate the end\
      \ of stream, because the byte value is promoted and sign-extended to an `int`\
      \ before being compared with −1. Consequently, the loop will halt prematurely\
      \ if a `0xFF` byte is read.\n**Noncompliant Code Example (`char`)**\n\n`FileReader`\
      \ is a subclass of `InputStreamReader`, which is in turn a subclass of`<span>\
      \ </span>Reader`. It also returns −1 only when the end of the stream is reached.\
      \ This noncompliant code example casts the value of type `int` returned by the\
      \ `read()` method directly to a value of type `char`, which is then compared\
      \ with −1 in an attempt to detect the end of stream. This conversion leaves\
      \ the value of `data` as `0xFFFF` (e.g., `Character.MAX_VALUE`) instead of −1.\
      \ Consequently, the test for the end of file never evaluates to true.\n\n```java\n\
      FileReader in;\n// Initialize stream\nchar data;\nwhile ((data = (char) in.read())\
      \ != -1) {\n  // ...\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010141408
    language: Java
  - Advice: "\n3) 示例   \n  \n    **例 1** 这个不符合规则的代码示例将由`read()`方法返回的值直接转换为 byte 类型，并且将其与-1\
      \ 进行比较，想通过这种比较来检査是否到达了流的末尾。   \n    ```java\n    FileInputStream in;   \n  \
      \  //initialize stream byte data;   \n    while ((data = (byte) in.read()) !=\
      \ -1) {   \n        // . . .    \n    }\n    ```   \n    在这个不符合规则的代码示例中，如果`read()`方法返回\
      \ 0xFF 的字节值，那么返回的字节值专用来表示以及达到流末尾的-1 的值是没有差别的，这个字节值在与-1 进行比较之前会被转换并且使用符号来扩展成为一个\
      \ int 类型。   \n    使用一个类型为 int 的变量来获取输入方法的 byte 的返回值。当`read()`方法的返回值不是-1 的时候，它会被安全地转换为\
      \ byte 类型。当`read()`方法返回 0xFF 时，会对 0x000000FF 和 0xFFFFFFFF 进行比较测试，并且这个测试会失败。\
      \   \n    ```java\n    FilelnputStream in;   \n    // initialize stream    \n\
      \    int inbuff;    \n    byte data;   \n    while ( (inbuff = in.read()) !=\
      \ -1) {   \n        data = (byte) inbuff;   \n        //…   \n    }\n    ```\
      \   \n      \n    **例 2** 这个不符合规则的代码示例将由`read()`方法返回的类型为 int 的值直接转换为 char 类型，它会与-1\
      \ 进行比较，用来检测是否达到了流末尾。 它 将 c 的值转换为 0xFFFF 而非-1。因此，这个是否达到流末尾的测试总是不会返回 true。\n \
      \   ```java   \n    FileReader in;   \n    // initialize stream    \n    char\
      \ c;   \n    while ((c = (char) in.read()) != -1) {   \n        // . . .   \
      \ \n    }\n    ```   \n    使用一个类型为 int 的变量来获得字符输入方法的返回值。当由`read()`方法返回的值不是-1\
      \ 的时候，它可以安全地转换为 char 类型。   \n    ```java\n    FileReader in;   \n    // initialize\
      \ stream    \n    int inbuff;    \n    char data;   \n    while((inbuff = in.read())\
      \ != -1) {   \n      data = (char) inbuff;   \n        //... .   \n    }\n \
      \   ```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 7.5 文件系统
      - type: DetailClassChin
        value: C.13.9 对读取一个字符或者字节的方法，使用int类型的返回值
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n读取一个字符或字节的方法应使用 int 类型的返回值。   \n2) 原理   \n抽象方法 `InputStream.read()`从一个输入源中读取一个简单字节，返回一个范围在\
      \ 0〜255 的整数。仅当在读取到输入流末尾的时候，它会返回-1。类似地 `Reader.read()`方法读取一个单独的字符，返回范围在 0〜65535\
      \ 的整数。仅当在读取到输入流的末尾时，它会返回-1。这两个方法都会被子类覆写。   \n通常会用这些方法来从一个流中读取一个字节或者字符。然而,在检査是否已经达到流末尾之前（这通过返回-1\
      \ 来表示），许多开发者过早地将返回的 int 类型转换为 byte 或 char 类型。   "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160114
    language: Java
  - Advice: "程序中应使用private、final的Object对象作为锁对象。  \n  \n**例如**：以下代码使用了private、final的Object对象作为锁对象。\
      \  \n```java\nprivate final Object lock = new Object();  \npublic void doSomething()\
      \ {  \n    synchronized (lock) {  \n        // ...  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 并发规范
      - type: DetailClassChin
        value: 【强制】不要基于非final对象进行同步
    Categories_En:
      Category: []
    Default: true
    Description: "非final对象意味着此对象可修改，基于可能被修改的对象进行同步，可能会导致死锁和不确定的行为。  \n  \n**例如**：下面代码片段中锁定了一个非final的对象。\
      \  \n```java\npublic Object lock = new Object();  \npublic void doSomething()\
      \ {  \n    synchronized (lock) {  \n        // ...  \n    }  \n}\n```  \n当程序中创建Object对象，并将此对象赋给`lock`，这将导致这个新的Object对象控制锁对象。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: 规则文件没有
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 413:Improper Resource Locking
      - type: CWE
        value: 412:Unrestricted Externally Accessible Lock
      - type: CERT
        value: LCK00-J:Use private final lock objects to synchronize classes that
          may interact with untrusted code
    Security: not
    Tag: concurrency,bad_practice
    Type: std
    WhiteList: not
    id: 0202000010150309
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n  **示例2**：   \n```java\nprivate final Object\
      \ lock = new Object();  \npublic void doSomething() {  \n    synchronized (lock)\
      \ {  \n        // ...  \n    }  \n}\n```  \n上述代码使用了private、final的Object对象作为锁对象。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.3 不要基于非final对象进行同步
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：非final对象意味着此对象可修改，基于可能被修改的对象进行同步，可能会导致死锁和不确定的行为。  \n对于不要基于非final对象进行同步的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**：   \n```java\npublic Object lock = new Object();  \npublic void\
      \ doSomething() {  \n    synchronized (lock) {  \n        // ...  \n    }  \n\
      }\n```  \n上述代码片段中锁定了一个非final的对象。当程序中创建Object对象，并将此对象赋给`lock`，这将导致这个新的Object对象控制锁对象。\
      \  \n程序中应使用private、final的Object对象作为锁对象。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170035'
    language: Java
  - Advice: "正确的重写`toString`或`clone`方法，避免空指针异常和程序逻辑出错。  \n  \n**例1**：下面代码示例中，重写了`toString`方法，不会返回null。\n\
      ```java  \npublic String toString () {  \n    if (this.collection.isEmpty())\
      \ {  \n        return \"\";   \n    }  \n    ...   \n}\n```  \n\n**例2**：下面代码示例中，重写了`clone`方法，不会返回null。\n\
      ```java  \nprotected Object clone() throws CloneNotSupportedException {  \n\
      \    Object object = super.clone();  \n\tif (this.collection.isEmpty()) {  \n\
      \        return object;   \n    }  \n    ...  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】toString或clone方法不应该返回null
    Categories_En:
      Category: []
    Default: true
    Description: "重写`toString`或`clone`方法应该总是返回一个字符串或者对象，程序员默认不会认为其为null。当返回null时，可能会引起空指针异常，影响程序运行。\
      \  \n  \n**例1**：下面代码示例中，重写了`toString`方法，有可能返回为null。 \n```java \npublic String\
      \ toString () {  \n    if (this.collection.isEmpty()) {  \n        return null;\
      \   \n    }  \n\t...   \n}\n```  \n**例2**：下面代码示例中，重写了`clone`方法，有可能返回为null。\n\
      ```java  \nprotected Object clone() throws CloneNotSupportedException {  \n\
      \    Object object = super.clone();  \n    if (this.collection.isEmpty()) {\
      \  \n        return null;   \n    }  \n    ...  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: 等级计算
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: security
    Type: std
    WhiteList: not
    id: '0202000010150436'
    language: Java
  - Advice: "成对的`getter`方法和`setter`方法当有一个方法进行同步时，都需要进行同步。  \n  \n**例如**：下面代码示例中，`getter`方法和`setter`方法成对同步。\
      \  \n```java\npublic class User {  \n    String name;  \n    int age;  \n  \n\
      \    public synchronized void setName(String name) {  \n        this.name =\
      \ name;  \n    }  \n  \n    public synchronized String getName() {  \n     \
      \   return this.name;  \n    }  \n  \n    public void setAge(int age) {  \n\
      \        synchronized (this) {  \n            this.age = age;  \n        } \
      \ \n    }  \n  \n    public int getAge() {  \n        synchronized (this) {\
      \  \n            return this.age;  \n        }  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 并发规范
      - type: DetailClassChin
        value: 【强制】getter方法和setter方法应该成对同步
    Categories_En:
      Category: []
    Default: true
    Description: "当`getter`/`setter`对的一个方法同步时，相对应的方法也应该同步。当调用者访问不一致的方法状态时，在运行时未能同步可能导致不一致的行为。\
      \  \n  \n**例如**：下面代码示例中，`getter`方法和`setter`方法没有成对同步。  \n```java\npublic class\
      \ User {  \n    String name;  \n    int age;  \n  \n    public synchronized\
      \ void setName(String name) {  \n        this.name = name;  \n    }  \n  \n\
      \    public String getName() { //对应的setName方法使用了synchronized修饰进行同步\n       \
      \ return this.name;  \n    }  \n  \n    public void setAge(int age) { //对应的getAge方法使用了synchronized块进行同步\n\
      \        this.age = age;  \n    }  \n  \n    public int getAge() {  \n     \
      \   synchronized (this) {  \n            return this.age;  \n        }  \n \
      \   }  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Non-Static Inner Class Implements Serializable
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: 0202000010150308
    language: Java
  - Advice: '以下是部分不推荐使用的类（方法）及推荐使用的替代类（方法）：


      |Class or Method|Replacement|

      |:-----:|:-----:|

      |java.lang.Character.isJavaLetter()|java.lang.Character.isJavaIdentifierStart()|

      |java.lang.Character.isJava Letter Or Digit()|java.lang.Character.isJavaIdentifierPart()|

      |java.lang.Character.isSpace()|java.lang.Character.isWhitespace()|

      |java.lang.Class.newInstance()|java.lang.reflect.Constructor.newInstance()|

      |java.util.Date|java.util.Calendar|

      |java.util.Dictionary|java.util.Map<K,V>|

      |java.util.Properties.save()|java.util.Properties.store()|

      |java.lang.Thread.run()|java.lang.Thread.start()|

      |java.lang.Thread.stop()|java.lang.Thread.interrupt()|

      |java.lang.ThreadGroup|java.util.concurrent.Executor|'
    Advice_En: '**Obsolete Methods and Classes**


      The methods and classes listed in the following table must not be used:


      | Class or Method | Replacement | Rule |

      | - | - | - |

      | `java.lang.Character.isJavaLetter()` | `java.lang.Character.isJavaIdentifierStart()`
      |  |

      | `java.lang.Character.isJavaLetterOrDigit()` | `java.lang.Character.isJavaIdentifierPart()`
      |  |

      | `java.lang.Character.isSpace()` | `java.lang.Character.isWhitespace()` |  |

      | `java.lang.Class.newInstance()` | `java.lang.reflect.Constructor.newInstance()`
      | ERR06-J. Do not throw undeclared checked exceptions |

      | `java.util.Date` (many methods) | `java.util.Calendar` |  |

      | `java.util.Dictionary` | `java.util.Map` |  |

      | `java.util.Properties.save()` | `java.util.Properties.store()` |  |

      | `java.lang.Thread.run()` | `java.lang.Thread.start()` | THI00-J. Do not invoke
      Thread.run() |

      | `java.lang.Thread.stop()` | `java.lang.Thread.interrupt()` | THI05-J. Do not
      use Thread.stop() to terminate threads |

      | `java.lang.ThreadGroup` (many methods) | `java.util.concurrent.Executor` |
      THI01-J. Do not invoke ThreadGroup methods |

      | java.util.Date | java.time (since Java 8) |  |


      The Java Virtual Machine Profiler Interface (JVMPI) and JVM Debug Interface
      (JVMDI) are also deprecated and have been replaced by the JVM Tool Interface
      (JVMTI) (see ENV05-J. Do not deploy an application that can be remotely monitored
      for more information).'
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 06. 方法 (MET)
      - type: DetailClassChin
        value: MET02-J：不要使用弃用的或过时的类和方法
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 06. Methods (MET)
      - type: DetailClassChin
        value: MET02-J:Do not use deprecated or obsolete classes or methods
    Default: not
    Description: '切勿在新代码中使用已弃用的字段、方法或类。 Java 提供了一个`@deprecated`注释来指示特定字段、方法和类的弃用。例如，`java.util.Date`
      的许多方法，例如` Date.getYear()`，已被明确弃用。 THI05-J.不要使用 Thread.stop() 终止线程描述了使用已弃用的 Thread.stop()
      方法可能导致的问题。


      Java SE 文档为每个语言版本提供了已弃用的 API 列表：


      * Java SE 6

      * Java SE 7

      * Java SE 8


      程序员应该使用特定于他们正在使用的语言版本的已弃用函数列表，尽管如果有合适的替代方案可用，也可以避免使用在更高版本中已弃用的 API。

      不应使用过时的字段、方法和类。 Java 缺少任何表明过时的注释；然而，有几个类和方法被记录为过时的。例如，`java.util.Dictionary
      `类被标记为过时；新代码应使用` java.util.Map` 代替 [API 2014]。'
    Description_En: 'Never use deprecated fields, methods, or classes in new code.
      Java provides an `@deprecated` annotation to indicate the deprecation of specific
      fields, methods, and classes. For example, many methods of `java.util.Date`,
      such as `Date.getYear()`, have been explicitly deprecated. THI05-J. Do not use
      Thread.stop() to terminate threads describes issues that can result from using
      the deprecated `Thread.stop()` method.


      The Java SE documentation provides a list of deprecated APIs for each version
      of the language:


      * Java SE 6

      * Java SE 7

      * Java SE 8


      Programmers should use the list of deprecated functions specific to the language
      version they are using, although it may also be possible to avoid the use of
      APIs that are deprecated in later versions as well if suitable alternatives
      are available.


      Obsolete fields, methods, and classes should not be used. Java lacks any annotation
      that indicates obsolescence; nevertheless, several classes and methods are documented
      as obsolete. For instance, the `java.util.Dictionary<K,V>` class is marked as
      obsolete; new code should use `java.util.Map<K,V>` instead [API 2014].'
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 477:Use of Obsolete Functions
      - type: ISO/IEC TR 24772
        value: 2013:Programming Language Vulnerabilities Dead and Deactivated Code
          [XYQ]
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140702'
    language: Java
  - Advice: 不要使用`@Deprecated`注释的元素或者废弃的方法，使用JDK和程序中所提供的新方法来代替过时的方法。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要使用已过时的元素
    Categories_En:
      Category: []
    Default: not
    Description: 随着编程语言的发展和程序的不断迭代，往往会因为某些原因弃用部分函数，或使用新函数替代之前的。如果程序中使用了`@Deprecated`注释的元素或者一些以及废弃的方法，这些元素或者方法可能会带来安全隐患。
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: Obsolete
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 477:Use of Obsolete Functions
      - type: CERT
        value: MET02-J:Do not use deprecated or obsolete classes or methods
      - type: ISO/IEC TR 24772
        value: 2013:Programming Language Vulnerabilities Dead and Deactivated Code
          [XYQ]
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150416'
    language: Java
  - Advice: "\n3) 示例   \n    不能使用以下的方法和类：\n     \n    | 类或者方法 | 替换 | 规则|\n    | :------:\
      \ | :------: | :------: |\n    | java.lang.Character.isJaveLetter() | java.lang.Character.isJavaIdentifierStart()\
      \ |  |\n    | java.lang.Character.isJaveLetterOrDigit() | java.lang.Character.isJavaIdentifierPart()\
      \ |  |\n    | java.lang.Character.isSpace() | java.lang.Character.isWhitespace()\
      \ |  |\n    | java.lang.Class.newlnstance() | java.lang.reflect.Constructor.newInstance()\
      \ | ERR06-J |\n    | java.util.Date （多个方法） | java.util.Calendar |  |\n    |\
      \ java.util.Dictionary  | java.util.Map<K,V>  |  |\n    | java.util.Proerties.save()\
      \ | java.util.Properties.store() |  |\n    | java.lang.Thread.run() | java.lang.Thread.start()\
      \ |  |\n    | java.lang.Thread.stop() | java.lang.Thread.interrupt() |  |\n\
      \    | java.lang.ThreadGroup （多个方法） | java.util.concurrent.Executor |  |\n \
      \   Java 虚拟机 Profiler Interface (JVM PI)和 JVM Debug Interface (JVM DI)也是被弃用的，它们已经被\
      \ JVM Tool Interface (JVM TI)所替代。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.6.3 不要使用弃用的或过时的类和方法
    Categories_En:
      Category: []
    Default: not
    Description: "1) 规定   \n不要在新代码中使用弃用的或过时的类和方法。同时必须避免在多线程应用中使用具有特殊限制的类和方法。   \n\
      2) 原理   \nJava SE 6文档提供了一个完整的弃用 API的列表。Java还提供了`@deprecated`注释来表示弃用的成员、方法和类。\
      \ 不应该使用过时的字段、方法和类。Java没有提供注释来说明过时。然而，一些类和方法是被注明为过时的。此外，一些类和方法强调了使用它们时，具有特殊的限制。例如，抽象类`java.text.Format`的所有子类是非线程安全的。必须避免在多线程应用中使用这些类。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 477:Use of Obsolete Functions
      - type: CERT
        value: MET02-J:Do not use deprecated or obsolete classes or methods
      - type: ISO/IEC TR 24772
        value: 2013:Programming Language Vulnerabilities Dead and Deactivated Code
          [XYQ]
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160052
    language: Java
  - Advice: "程序必须避免无意地和恶意地调用`System.exit()`。而且，在被强迫终止时（例如通过Windows任务管理器、POSIX kill命令或者其他机制），程序应该进行必要的清理操作。\n\
      **规范的代码样例1**：  \n以下代码设置了一个自定义的安全管理器PasswordSecurityManager，并重写SecurityManager类中定义的`checkExit()`方法。为了实现退出前启用清除代码的调用，必须重写该方法。SecurityManager类中默认的`checkExit()`方法没有该功能。\
      \  \n```java\nclass PasswordSecurityManager extends SecurityManager {\n  private\
      \ boolean isExitAllowedFlag;\n\n  public PasswordSecurityManager(){\n    super();\n\
      \    isExitAllowedFlag = false;\n  }\n\n  public boolean isExitAllowed(){\n\
      \    return isExitAllowedFlag;\n  }\n\n  @Override\n  public void checkExit(int\
      \ status) {\n    if (!isExitAllowed()) {\n      throw new SecurityException();\n\
      \    }\n    super.checkExit(status);\n  }\n\n  public void setExitAllowed(boolean\
      \ f) {\n    isExitAllowedFlag = f;\n  }\n}\n\npublic class InterceptExit {\n\
      \  public static void main(String[] args) {\n    PasswordSecurityManager secManager\
      \ =\n        new PasswordSecurityManager();\n    System.setSecurityManager(secManager);\n\
      \    try {\n      // ...\n      System.exit(1);  // Abrupt exit call\n    }\
      \ catch (Throwable x) {\n      if (x instanceof SecurityException) {\n     \
      \   System.out.println(\"Intercepted System.exit()\");\n        // Log exception\n\
      \      } else {\n        // Forward to exception handler\n      }\n    }\n\n\
      \    // ...\n    secManager.setExitAllowed(true);  // Permit exit\n    // System.exit()\
      \ will work subsequently\n    // ...\n  }\n}\n```\n此实现使用内部标志来跟踪是否允许退出。`setExitAllowed()`方法设置此标志。当标志未设置（即为false）时，`checkExit()`方法会抛出SecurityException异常。因为这个标志没有初始设置，正常的异常处理会绕过初始调用`System.exit()`。程序捕获SecurityException并执行强制清除操作，包括记录异常。`System.exit()`方法仅在清除完成后才能启用。\
      \  \n**特例**：一个命令行应用程序调用`System.exit()`是允许的，例如：当没有输入足够数量要求的参数时[Bloch 2008], [ESA\
      \ 2005]."
    Advice_En: "**Compliant Solution**\n\nThis compliant solution installs a custom\
      \ security manager `PasswordSecurityManager` that overrides the `checkExit()`\
      \ method defined in the `SecurityManager` class. This override is required to\
      \ enable invocation of cleanup code before allowing the exit. The default `checkExit()`\
      \ method in the `SecurityManager` class lacks this facility.\n\n```java\nclass\
      \ PasswordSecurityManager extends SecurityManager {\n  private boolean isExitAllowedFlag;\n\
      \n  public PasswordSecurityManager(){\n    super();\n    isExitAllowedFlag =\
      \ false;\n  }\n\n  public boolean isExitAllowed(){\n    return isExitAllowedFlag;\n\
      \  }\n\n  @Override\n  public void checkExit(int status) {\n    if (!isExitAllowed())\
      \ {\n      throw new SecurityException();\n    }\n    super.checkExit(status);\n\
      \  }\n\n  public void setExitAllowed(boolean f) {\n    isExitAllowedFlag = f;\n\
      \  }\n}\n\npublic class InterceptExit {\n  public static void main(String[]\
      \ args) {\n    PasswordSecurityManager secManager =\n        new PasswordSecurityManager();\n\
      \    System.setSecurityManager(secManager);\n    try {\n      // ...\n     \
      \ System.exit(1);  // Abrupt exit call\n    } catch (Throwable x) {\n      if\
      \ (x instanceof SecurityException) {\n        System.out.println(\"Intercepted\
      \ System.exit()\");\n        // Log exception\n      } else {\n        // Forward\
      \ to exception handler\n      }\n    }\n\n    // ...\n    secManager.setExitAllowed(true);\
      \  // Permit exit\n    // System.exit() will work subsequently\n    // ...\n\
      \  }\n}\n```\nThis implementation uses an internal flag to track whether the\
      \ exit is permitted. The method `setExitAllowed()` sets this flag. The `checkExit()`\
      \ method throws a `SecurityException` when the flag is unset (that is, false).\
      \ Because this flag is not initially set, normal exception processing bypasses\
      \ the initial call to `System.exit()`. The program catches the `SecurityException`\
      \ and performs mandatory cleanup operations, including logging the exception.\
      \ The `System.exit()` method is enabled only after cleanup is complete.\n**Exceptions**\n\
      ERR09-J-EX0: It is permissible for a command-line utility to call `System.exit()`,\
      \ for example, when the required number of arguments are not input [Bloch 2008],\
      \ [ESA 2005]."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 07. 异常行为 (ERR)
      - type: DetailClassChin
        value: ERR09-J：不要允许不可信的代码终止JVM
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 07. Exceptional Behavior (ERR)
      - type: DetailClassChin
        value: ERR09-J:Do not allow untrusted code to terminate the JVM
    Default: true
    Description: "调用`System.exit()`终止Java虚拟机(JVM)，从而终止所有正在运行的程序和线程。这可能导致拒绝服务(DoS)攻击。例如：嵌入到Java\
      \ Server Pages（JSP）代码中的`System.exit()`的调用可能导致Web服务器终止，从而阻止用户进一步的服务。程序必须防止对`System.exit()`的无意和恶意的调用。此外，程序在强制终止时应执行必要的清除操作（**例如**：使用Windows任务管理器，POSIX\
      \ kill命令或其他机制）。  \n**不规范的代码样例1**：  \n以下代码使用`System.exit()`强制关闭JVM并终止正在运行的进程。该程序缺乏安全管理，同时无法检测调用者是否有权调用`System.exit()`。\
      \  \n```java\npublic class InterceptExit {\n  public static void main(String[]\
      \ args) {\n    // ...\n    System.exit(1);  // Abrupt exit\n    System.out.println(\"\
      This never executes\");\n  }\n}\n```"
    Description_En: "Invocation of `System.exit()` terminates the Java Virtual Machine\
      \ (JVM), consequently terminating all running programs and threads. This can\
      \ result in denial-of-service (DoS) attacks. For example, a call to `System.exit()`\
      \ that is embedded in Java Server Pages (JSP) code can cause a web server to\
      \ terminate, preventing further service for users. Programs must prevent both\
      \ inadvertent and malicious calls to `System.exit()`. Additionally, programs\
      \ should perform necessary cleanup actions when forcibly terminated (for example,\
      \ by using the Windows Task Manager, POSIX `kill` command, or other mechanisms).\n\
      \n**Noncompliant Code Example**\n\nThis noncompliant code example uses `System.exit()`\
      \ to forcefully shut down the JVM and terminate the running process. The program\
      \ lacks a security manager; consequently, it lacks the capability to check whether\
      \ the caller is permitted to invoke `System.exit()`.\n\n```java\npublic class\
      \ InterceptExit {\n  public static void main(String[] args) {\n    // ...\n\
      \    System.exit(1);  // Abrupt exit\n    System.out.println(\"This never executes\"\
      );\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '382:J2EE Bad Practices: Use of System.exit()'
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 调用System.exit方法
    Security: true
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140809
    language: Java
  - Advice: 程序必须避免无意地和恶意地调用`System.exit()`。而且，在被强迫终止时（例如通过Windows任务管理器、POSIX kill命令或者其他机制），程序应该进行必要的清理操作。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要在JavaEE程序通过非受信代码终止JVM
    Categories_En:
      Category: []
    Default: true
    Description: 调用`System.exit()`会终止Java虚拟机，从而终止所有运行中的程序和线程。Web应用程序关闭自身的容器，有可能导致拒绝服务攻击，**例如**：一个嵌入在JSP中的`System.exit()`的调用会停止网络服务器，阻碍了对用户的服务。
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - JVM Termination
    ENSubClassChin: J2EE Bad Practices
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: true
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '382:J2EE Bad Practices: Use of System.exit()'
      - type: CERT
        value: ERR09-J:Do not allow untrusted code to terminate the JVM
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 调用System.exit方法
    Security: true
    Tag: security
    Type: std
    WhiteList: not
    id: '0202000010150404'
    language: Java
  - Advice: "对应的符合规则的解决方案是：代码设置一个自定义的安全管理器PasswordSecurityManager，并覆写在SecurityManager类中定义的`checkExit()`方法。为实现退出前对清理代码的调用，这一覆写是必须的。\
      \  \n    ```java\n    class PasswordSecurityManager extends SecurityManager\
      \ {\n        private boolean isExitAllowedFlag;\n    \n        public PasswordSecurityManager()\
      \ {\n            super();\n            isExitAllowedFlag = false;\n        }\n\
      \    \n        public boolean isExitAllowed() {\n            return isExitAllowedFlag;\n\
      \        }\n    \n        @Override\n        public void checkExit(int status)\
      \ {\n            if (!isExitAllowed()) {\n                throw new SecurityException();\n\
      \            }\n            super.checkExit(status);\n        }\n    \n    \
      \    public void setExitAllowed(boolean f) {\n            isExitAllowedFlag\
      \ = f;\n        }\n    }\n    \n    public class InterceptExit {\n        public\
      \ static void main(String[] args) {\n            PasswordSecurityManager secManager\
      \ = new PasswordSecurityManager();\n            System.setSecurityManager(secManager);\n\
      \            try {\n                // ...\n                System.exit(1);\
      \ // Abrupt exit call\n            } catch (Throwable x) {\n               \
      \ if (x instanceof SecurityException) {\n                    System.out.println(\"\
      Intercepted `System.exit()`\");\n                    // Log exception\n    \
      \            } else {\n                    // Forward to exception handler\n\
      \                }\n            }\n            // ...\n            secManager.setExitAllowed(true);\
      \ // Permit exit\n            // `System.exit()` will work subsequently\n  \
      \          // ...\n        }\n    }\n    ```   \n   \n4) 特例   \n—个命令行应用调用 `System.exit()`是允许的，例如，当没有输入足够数量要求的参数时。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.4 错误处理安全
      - type: DetailClassChin
        value: C.7.10 禁止非受信代码终止JVM
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序应该尽量避免无意地和恶意地调用 `System.exit()`终止 Java 虚拟机的行为，在被迫终止时，程序应该进行必要的清理操作。\
      \   \n2) 原理   \n调用 `system.exit()`会终止 Java 虚拟机（JVM），从而终止所有运行中的程序和线程。这会导致拒绝服务攻击。例如，一个嵌入在\
      \ `Java Server Pages(JSP)`中的 `System.exit()`的调用会停止网络服务器，阻碍了对用户的服务。   \n3) 示例\
      \   \n    如下不符合规则的代码示例使用 `System.exit()`来强制关闭 JVM 并终止运行中的进程。这个程序没有安全管理器，因此它缺乏检査是否允许调用者使用\
      \ `System.exit()`的能力。   \n    ```java\n    public class InterceptExit {   \n\
      \        public static void main(String[] args) {   \n            // . . . \
      \   \n            System.exit(1); // Abrupt exit   \n            System.out.println(\"\
      This never executes\");   \n        }   \n    }\n    ```   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '382:J2EE Bad Practices: Use of System.exit()'
      - type: CERT
        value: ERR09-J:Do not allow untrusted code to terminate the JVM
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 调用System.exit方法
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160072
    language: Java
  - Advice: "**规范的代码样例1**：\n通过在while循环中调用`Thread.sleep()`方法来避免毫无意义的无限循环。这个循环体包含一些有语义的操作，并且不会被优化掉。\n\
      ```java\npublic final int DURATION = 10000; // in milliseconds\n\npublic void\
      \ nop() throws InterruptedException {\n    while (true) {\n        // Useful\
      \ operations\n        Thread.sleep(DURATION);\n    }\n}\n```\n**规范的代码样例2**：\n\
      通过在while循环中调用`Thread.yield()`方法，它会使得这个方法中运行的线程被其他线程延迟。\n```java\npublic void\
      \ nop() {\n    while (true) {\n        Thread.yield();\n    }\n}\n```"
    Advice_En: "**Compliant Solution (`Thread.sleep()`)**\n\nThis compliant solution\
      \ avoids use of a meaningless infinite loop by invoking `Thread.sleep()` within\
      \ the `while` loop. The loop body contains semantically meaningful operations\
      \ and consequently cannot be optimized away.\n\n```java\npublic final int DURATION=10000;\
      \ // In milliseconds\n\npublic void nop() throws InterruptedException {\n  while\
      \ (true) {\n    // Useful operations\n    Thread.sleep(DURATION);\n  }\n}\n\
      ```\n\n**Compliant Solution (`yield()`)**\n\nThis compliant solution invokes\
      \ `Thread.yield()`, which causes the thread running this method to consistently\
      \ defer to other threads:\n\n```java\npublic void nop() {\n  while (true) {\n\
      \    Thread.yield();\n  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 49. 其他 (MSC)
      - type: DetailClassChin
        value: MSC01-J：不要使用空的无限循环
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 49. Miscellaneous (MSC)
      - type: DetailClassChin
        value: MSC01-J:Do not use an empty infinite loop
    Default: true
    Description: "一个空的无限循环会消耗大量的CPU计算资源而不做任何事情。优化编译器和即时系统允许去除这样的循环。\n**不规范的代码样例1**：\n\
      代码实现了一个闲置任务，这个任务会连续的执行一个循环，却在这个循环中不执行任何指令。一个优化编译器或者即时系统会去掉这个样例中的while循环。\n```java\n\
      public int nop() {\n    while (true) {}\n}\n```"
    Description_En: "An infinite loop with an empty body consumes CPU cycles but does\
      \ nothing. Optimizing compilers and just-in-time systems (JITs) are permitted\
      \ to (perhaps unexpectedly) remove such a loop. Consequently, programs must\
      \ not include infinite loops with empty bodies.\n\n**Noncompliant Code Example**\n\
      \nThis noncompliant code example implements an idle task that continuously executes\
      \ a loop without executing any instructions within the loop. An optimizing compiler\
      \ or JIT could remove the `while` loop in this example.\n\n```java\npublic int\
      \ nop() {\n  while (true) {}\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 835:Loop with Unreachable Exit Condition ('Infinite Loop')
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010141801
    language: Java
  - Advice: "检查程序逻辑，去除没有代码的无限循环，或者在循环体中加入有语义的操作。  \n  \n**例如**：下面代码片段中，在while循环中调用`Thread.sleep()`方法来避免毫无意义的无限循环。\
      \  \n```java\npublic final int DURATION = 10000;  \npublic void nop() throws\
      \ InterruptedException{  \n    while(true){  \n        Thread.sleep(DURATION);\
      \  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要使用空的无限循环
    Categories_En:
      Category: []
    Default: true
    Description: "空的无限循环会消耗大量的CPU资源而不做任何事情。  \n  \n**例1**：下面代码片段中，执行了一个循环，循环条件值为true，循环中不执行任何指令。\
      \  \n```java\npublic int nop(){  \n    while(true){}  \n}\n```  \n**例2**：下面代码片段中，执行了一个循环，循环条件变量值为true，循环中不执行任何指令。\
      \  \n```java\nboolean condition = true;  \npublic int nop(){  \n    while(condition){}\
      \  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - (MSC01-J)不要使用空的无限循环
    ENSubClassChin: 其他(MSC)
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 835:Loop with Unreachable Exit Condition ('Infinite Loop')
      - type: CERT
        value: MSC01-J:Do not use an empty infinite loop
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150413'
    language: Java
  - Advice: "相应的符合规则的代码示例：通过在 while 循环中调用 `Thread.sleep()`方法来避免这个无限循环。   \n    ```java\n\
      \    public final int DURATION=10000; // in milliseconds   \n    public void\
      \ nop() throws InterruptedException { \n        while (true) { 4   \n      \
      \      // Useful operations Thread.sleep(DURATION);   \n        }   \n    }\n\
      \    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.17.2 不要使用空的无限循环
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n不要使用空的无限循环。   \n2) 原理   \n一个空的无限循环会消耗大量的 CPU 计算资源，而不起任何作用。优化编译器和即时系统\
      \ (JIT)允许去除这样的循环。   \n3) 示例   \n    1.这个不符合规则的代码示例实现了一个闲置任务，这个任务会连续地执行一个循环，却在这个循环中不执行任何指令。\
      \   \n    ```java\n    public int nop () {   \n        while (true) {\n    \
      \        \n        }   \n    }\n    ```   \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 835:Loop with Unreachable Exit Condition ('Infinite Loop')
      - type: CERT
        value: MSC01-J:Do not use an empty infinite loop
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160149
    language: Java
  - Advice: "当进行SSL连接时，应该进行服务器验证检查。根据所使用的库，验证服务器身份并建立安全的SSL连接。  \n  \n**例如**：下面代码示例中，明确验证服务器证书。\
      \  \n```java\nSimpleEmail email = new SimpleEmail();  \nemail.setHostName(\"\
      smtp.testemail.com\");  \nemail.setCharset(\"UTF-8\");  \nemail.setSmtpPort(25);\
      \  \nemail.setAuthenticator(new DefaultAuthenticator(name, pwd));  \nemail.setSSLCheckServerIdentity(true);\
      \  \nemail.setSSLOnConnect(true);  \nemail.setFrom(\"test@163.com\");  \nemail.setSubject(\"\
      测试主题\");  \nemail.setMsg(\"邮件内容\");  \nemail.addTo(\"admin@163.com\");  \nemail.send();\n\
      ```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】SSL连接时服务器身份验证不应该禁用
    Categories_En:
      Category: []
    Default: true
    Description: "当进行SSL连接时，服务器身份验证处于禁用状态。在某些使用SSL连接的库中，默认情况下不验证服务器证书。这相当于信任所有证书。\
      \  \n  \n**例如**：下面代码片段没有明确地验证服务器证书。  \n```java\nSimpleEmail email = new SimpleEmail();\
      \  \nemail.setHostName(\"smtp.testemail.com\");  \nemail.setCharset(\"UTF-8\"\
      );  \nemail.setSmtpPort(25);  \nemail.setAuthenticator(new DefaultAuthenticator(name,\
      \ pwd));  \nemail.setSSLOnConnect(true);  \nemail.setFrom(\"test@163.com\");\
      \  \nemail.setSubject(\"测试主题\");  \nemail.setMsg(\"邮件内容\");  \nemail.addTo(\"\
      admin@163.com\");  \nemail.send();\n```  \n当尝试连接到`smtp.testemail.com:25`时，此应用程序将随时接受颁发给`\"\
      hackedserver.com\"`的证书。此时，当服务器被黑客攻击发生SSL连接中断时，应用程序可能会泄漏用户敏感信息。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Server Identity Verification Disabled
    ENSubClassChin: Insecure SSL
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 高
    GradesComment: 等级计算
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 297:Improper Validation of Certificate with Host Mismatch
      - type: OWASP TOP 10
        value: 2010:A9 Insufficient Transport Layer Protection
      - type: OWASP TOP 10
        value: 2007:A9 Insecure Communications
      - type: OWASP MOBILE TOP 10
        value: 2014:M3 Insufficient Transport Layer Protection
    Security: true
    Tag: security
    Type: std
    WhiteList: not
    id: '0202000010150435'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n```java\nSimpleEmail email = new\
      \ SimpleEmail();  \nemail.setHostName(\"smtp.testemail.com\");  \nemail.setCharset(\"\
      UTF-8\");  \nemail.setSmtpPort(25);  \nemail.setAuthenticator(new DefaultAuthenticator(name,\
      \ pwd));  \nemail.setSSLCheckServerIdentity(true);  \nemail.setSSLOnConnect(true);\
      \  \nemail.setFrom(\"test@163.com\");  \nemail.setSubject(\"测试主题\");  \nemail.setMsg(\"\
      邮件内容)\");  \nemail.addTo(\"admin@163.com\");  \nemail.send();\n```  \n上述代码示例中，明确验证服务器证书。"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.3 访问控制
      - type: DetailClassChin
        value: A.2.3.1.3 SSL连接时要进行服务器身份验证
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：当进行SSL连接时，服务器身份验证处于禁用状态。在某些使用SSL连接的库中，默认情况下不验证服务器证书。这相当于信任所有证书。\
      \  \n对SSL连接时要进行服务器身份验证的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nSimpleEmail\
      \ email = new SimpleEmail();  \nemail.setHostName(\"smtp.testemail.com\"); \
      \ \nemail.setCharset(\"UTF-8\");  \nemail.setSmtpPort(25);  \nemail.setAuthenticator(new\
      \ DefaultAuthenticator(name, pwd));  \nemail.setSSLOnConnect(true);  \nemail.setFrom(\"\
      test@163.com\");  \nemail.setSubject(\"测试主题\");  \nemail.setMsg(\"邮件内容)\");\
      \  \nemail.addTo(\"admin@163.com\");  \nemail.send();\n```  \n上述代码片段没有明确地验证服务器证书。\
      \  \n当尝试连接到`smtp.testemail.com:25`时，此应用程序将随时接受颁发给`“hackedserver.com”`的证书。此时，当服务器被黑客攻击发生SSL连接中断时，应用程序可能会泄漏用户敏感信息。\
      \  \n当进行SSL连接时，应该进行服务器验证检查。根据所使用的库，验证服务器身份并建立安全的SSL连接。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 297:Improper Validation of Certificate with Host Mismatch
      - type: OWASP TOP 10
        value: 2010:A9 Insufficient Transport Layer Protection
      - type: OWASP TOP 10
        value: 2007:A9 Insecure Communications
      - type: OWASP MOBILE TOP 10
        value: 2014:M3 Insufficient Transport Layer Protection
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170020'
    language: Java
  - Advice: "规范的代码样**例1**：  \n将`readObject()`和`writeObject()`方法声明为private static来限制他们的可访问性。\
      \  \n```java\npublic class Ser implements Serializable {\n    private final\
      \ long serialVersionUID = 123456789;\n\n    private Ser() {\n        // initialize\n\
      \    }\n\n    private static void writeObject(final ObjectOutputStream stream)\
      \ throws IOException {\n        stream.defaultWriteObject();\n    }\n\n    private\
      \ static void readObject(final ObjectInputStream stream) throws IOException,\
      \ ClassNotFoundException {\n        stream.defaultReadObject();\n    }\n}\n\
      ```  \n**规范的代码样例2**：  \n将`readResolve()`和`writeReplace()`方法声明为protected 非 static，子类可以继承这两个方法。\
      \  \n```java\nclass Extendable implements Serializable {\n    protected Object\
      \ readResolve() {\n        // . . .\n    }\n\n    protected Object writeReplace()\
      \ {\n        // . . .\n    }\n}\n```"
    Advice_En: "**Compliant Solution (`readObject()`, `writeObject()`)**\n\nThis compliant\
      \ solution declares the `readObject()` and `writeObject()` methods private and\
      \ nonstatic to limit their accessibility:\n\n```java\nprivate void writeObject(final\
      \ ObjectOutputStream stream)\n    throws IOException {\n  stream.defaultWriteObject();\n\
      }\n\nprivate void readObject(final ObjectInputStream stream)\n    throws IOException,\
      \ ClassNotFoundException {\n  stream.defaultReadObject();\n}\n```\n\nReducing\
      \ the accessibility also prevents malicious overriding of the two methods.\n\
      **Compliant Solution (`readResolve()`, `writeReplace()`)**\n\nThis compliant\
      \ solution declares the two methods protected while eliminating the `static`\
      \ keyword so that subclasses can inherit them:\n\n```java\nclass Extendable\
      \ implements Serializable {\n  protected Object readResolve() {\n    // ...\n\
      \  }\n\n  protected Object writeReplace() {\n    // ...\n  }\n}\n```"
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 14. 序列化 (SER)
      - type: DetailClassChin
        value: SER01-J：不要偏离序列化方法的正确签名
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 14. Serialization (SER)
      - type: DetailClassChin
        value: SER01-J:Do not deviate from the proper signatures of serialization
          methods
    Default: true
    Description: "那些需要在对象序列化和反序列化中进行特殊处理的类必须要使用以下的方法签名来实现特殊的方法：  \n  ```java\nprivate\
      \ void writeObject(java.io.ObjectOutputStream out) throws IOException;  \nprivate\
      \ void readObject(java.io.ObjectInputStream in) throws IOException, ClassNotFoundException;\
      \  \nprivate void readObjectNoData() throws ObjectStreamException;\n```  \n\
      对于任何序列化，这些方法要声明为private。序列化的类同样也要实现`readResolve()`和`writeReplace()`方法。根据序列化规范中对`readResolve()`和`writeReplace()`的方法描述：\
      \  \n1）对序列化和外部化类而言，`readResolve()`方法允许一个类去替换/解析对象，这个对象在其返回给调用者之前从流程中读取。通过实现`readResolve()`方法，一个类可以直接控制被反序列化的实例的类型和实例本身。\
      \  \n2）对序列化和外部化类而言，`writeReplace()`方法允许一个对象的类在对象写入之前在流中提名自身的替代。通过实现`writeReplace()`方法，一个类可以直接控制被序列化的实例的类型和实例本身。\
      \  \n可以为`readResolve()`和`writeReplace()`方法加入任何访问指定符。然而，如果这些方法被声明为private的，那么扩展类就不能调用和覆写它们。同样，如果这些类被声明为静态的，扩展类也不能覆写这些方法，它们可以将其隐藏。偏离这些方法签名，会产生一个方法在序列化或者反序列化的时候不会被调用的现象。这样的方法当其在声明为public的时候会被非受信代码调用。\
      \  \n不像其他接口，Serializable没有定义需要的接口方法签名。接口仅允许public字段和方法，而``readObject()`、readObjectData()`和`writeObject()`方法必须要声明为private。同样，Serializable不能防止`readResolve()`和`writeReplace()`方法被声明为static、public、或private的。因此，Java序列化机制不能让编译器识别出任何这些方法不正确的方法签名。\
      \  \n风险评估：偏离正确的序列化签名会导致不可预期的行为。包括不能限制`readObject()`和`writeObject()`方法的访问，会为非受信的调用提供漏洞。声明`readObject()`和`writeObject()`方法为静态或者私有的会强制子类悄无声息的将他们忽略。\
      \  \n**不规范的代码样例1**：  \n```java\npublic class Ser implements Serializable {\n\
      \    private final long serialVersionUID = 123456789;\n\n    private Ser() {\n\
      \        // initialize\n    }\n\n    public static void writeObject(final ObjectOutputStream\
      \ stream) throws IOException {\n        stream.defaultWriteObject();\n    }\n\
      \n    public static void readObject(final ObjectInputStream stream) throws IOException,\
      \ ClassNotFoundException {\n        stream.defaultReadObject();\n    }\n}\n\
      ```  \n代码定义了一个Ser类，这类使用private的构造函数，意味着这个类的外部代码不能创建这个类的实例。这个类实现了`java.io.Serializable`接口并且定义了公有的`readResolve()`和`writeReplace()`方法。因此，非受信的代码可以通过`readObject()`方法来得到重组对象，并且可以使用`writeObject()`来写入流。\
      \  \n**不规范的代码样例2**：\n```java\nclass Extendable implements Serializable {\n \
      \   private Object readResolve() {\n        // . . .\n    }\n\n    private Object\
      \ writeReplace() {\n        // . . .\n    }\n}\n```  \n代码将`readResolve()`和`writeReplace()`方法声明为private。\
      \  \n**不规范的代码样例3**：  \n```java\nclass Extendable implements Serializable {\n\
      \    protected static Object readResolve() {\n        // . . .\n    }\n\n  protected\
      \ static writeReplace() {  \n        // . . .  \n    }\n}\n``` \n代码将`readResolve()`和`writeReplace()`方法声明为static。"
    Description_En: "Classes that require special handling during object serialization\
      \ and deserialization must implement special methods with exactly the following\
      \ signatures [API 2014]:\n\n```java\nprivate void writeObject(java.io.ObjectOutputStream\
      \ out)\n    throws IOException;\nprivate void readObject(java.io.ObjectInputStream\
      \ in)\n    throws IOException, ClassNotFoundException;\nprivate void readObjectNoData()\n\
      \    throws ObjectStreamException;\n```\n\nNote that these methods must be declared\
      \ private for any serializable class. Serializable classes may also implement\
      \ the `readResolve()` and `writeReplace()` methods.\nAccording to the Serialization\
      \ Specification [Sun 2006], `readResolve()` and `writeReplace()` method documentation:\n\
      \n> For Serializable and Externalizable classes, the `readResolve` method allows\
      \ a class to replace/resolve the object read from the stream before it is returned\
      \ to the caller. By implementing the `readResolve` method, a class can directly\
      \ control the types and instances of its own instances being deserialized.\n\
      >\n> For Serializable and Externalizable classes, the `writeReplace` method\
      \ allows a class of an object to nominate its own replacement in the stream\
      \ before the object is written. By implementing the `writeReplace` method, a\
      \ class can directly control the types and instances of its own instances being\
      \ serialized.\n\nIt is possible to add any access-specifier to the `readResolve()`\
      \ and `writeReplace()` methods. However, if these methods are declared private,\
      \ extending classes cannot invoke or override them. Similarly, if these methods\
      \ are declared static, extending classes cannot override these methods; they\
      \ can only hide them.\n\nDeviating from these method signatures produces a method\
      \ that is not invoked during object serialization or deserialization. Such methods,\
      \ especially if declared public, might be accessible to untrusted code.\n\n\
      Unlike most interfaces, `Serializable` does not define the method signatures\
      \ it requires. Interfaces allow only public fields and methods, whereas `readObject()`,\
      \ `readObjectNoData`, and `writeObject()` must be declared private. Similarly,\
      \ the `Serializable` interface does not prevent `readResolve()` and `writeReplace()`\
      \ methods from being declared static, public, or private. Consequently, the\
      \ Java serialization mechanism fails to let the compiler identify an incorrect\
      \ method signature for any of these methods.\n\n**Noncompliant Code Example\
      \ (`readObject()`, `writeObject()`)**\n\nThis noncompliant code example shows\
      \ a class `Ser` with a private constructor, indicating that code external to\
      \ the class should be unable to create instances of it. The class implements\
      \ `java.io.Serializable` and defines public `readObject()` and `writeObject()`\
      \ methods. Consequently, untrusted code can obtain the reconstituted objects\
      \ by using `readObject()` and can write to the stream by using `writeObject()`.\n\
      \n```java\npublic class Ser implements Serializable {\n  private final long\
      \ serialVersionUID = 123456789;\n  private Ser() {\n    // Initialize\n  }\n\
      \  public static void writeObject(final ObjectOutputStream stream)\n    throws\
      \ IOException {\n    stream.defaultWriteObject();\n  }\n  public static void\
      \ readObject(final ObjectInputStream stream)\n      throws IOException, ClassNotFoundException\
      \ {\n    stream.defaultReadObject();\n  }\n}\n```\n\nNote that there are two\
      \ things wrong with the signatures of `writeObject()` and `readObject()` in\
      \ this Noncompliant Code Example: (1) the method is declared `public` instead\
      \ of `private`, and (2) the method is declared `static` instead of non-`static`.\
      \  Since the method signatures do not exactly match the required signatures,\
      \ the JVM will not detect the two methods, resulting in failure to use the custom\
      \ serialized form.\n\n**Noncompliant Code Example (`readResolve()`, `writeReplace()`)**\n\
      \nThis noncompliant code example declares the `readResolve()` and `writeReplace()`\
      \ methods as private:\n\n```java\nclass Extendable implements Serializable {\n\
      \  private Object readResolve() {\n    // ...\n  }\n\n  private Object writeReplace()\
      \ {\n    // ...\n  }\n}\n```\n\n**Noncompliant Code Example (`readResolve()`,\
      \ `writeReplace()`)**\n\nThis noncompliant code example declares the `readResolve()`\
      \ and `writeReplace()` methods as static:\n\n```java\nclass Extendable implements\
      \ Serializable {\n  protected static Object readResolve() {\n    // ...\n  }\n\
      \n  protected static Object writeReplace() {\n    // ...\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 502
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 502
      - type: CWE
        value: 502:Deserialization of Untrusted Data
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141501'
    language: Java
  - Advice: "对应符合规定的解决方案是：将 `readObject()`和 `writeObject()`声明为私有方法，并且通过将其声明为非静态来限制它们的可访问性。\
      \   \n    ```java\n    private void writeObject(final ObjectOutputStream stream)\
      \ throws IOException {\n        stream.defaultWriteObject();\n    }\n    \n\
      \    private void readObject(final ObjectInputStream stream) throws IOException,\
      \ ClassNotFoundException {\n        stream.defaultReadObject();\n    }\n   \
      \ ```  \n    另一个符合规定的解决方案是在消除 static 关键字的同时，将这两个方法声明为 protected，使其可以被子类继承。 \
      \  \n    ```java\n    class Extendable implements Serializable {\n        protected\
      \ Object readResolve() {\n            // ...\n        }\n        \n        protected\
      \ Object writeReplace() {\n            // ...\n        }\n    }\n    ```  \n\
      \    减少可访问性同样可以防止对这两个方法的恶性覆写。   \n    2、另一个不符合规则的代码示例（`readResolve()`、`writeReplace()`)，该示例将`readResolve()`和`writeReplace()`方法声明为私有。\
      \   \n    ```java\n    class Extendable implements Serializable {\n        private\
      \ Object readResolve() {\n            // ...\n        }\n        \n        private\
      \ Object writeReplace() {\n            // ...\n        }\n    }\n    ```\n \
      \   或者将 `readResolve()`和 `writeReplace()`方法声明为静态的。   \n    ```java\n    class\
      \ Extendable implements Serializable {\n        protected static Object readResolve()\
      \ {\n            // ...\n        }\n        \n        protected static Object\
      \ writeReplace() {\n            // ...\n        }\n    }\n    ```\n    对应符合规则的解决方案是：在消除\
      \ static 关键字的同时，将这两个方法声明为 protected,使其可以被子类继承。   \n    ```java\n    class Extendable\
      \ implements Serializable {\n        protected Object readResolve() {\n    \
      \        // ...\n        }\n        \n        protected Object writeReplace()\
      \ {\n            // ...\n        }\n    }\n    ```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: ''
      - type: DetailClassChin
        value: C.14.2 不要偏离序列化方法的正确签名
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \nJava 的序列化机制不能让编译器识别出方法的不正确的签名，不要偏离序列化方法的正确签名。   \n2) 原理\
      \   \n那些需要在对象序列化和反序列化中进行特殊处理的类必须要使用以下的方法签名来实现特殊的方法:   \n```java\nprivate void\
      \ writeObject(java.io.ObjectOutputStream out) throws IOException;  \nprivate\
      \ void readObject(java.io.ObjectInputStream in) throws IOException, ClassNotFoundException;\
      \  \nprivate void readObjectNoData() throws ObjectStreamException;\n```  \n\
      对于任何序列化类，这些方法要声明为私有的序列化的类同样也会实现 `readResolve()`和`writeReplace()`方法。根据序列化规范中对\
      \ `readResolve()`和 `writeReplace()`的方法描述： (1)对序列化和外部化类而言，`readResolve`方法允许一个类去替换/解析对象，这个对象在其返回给调用者之前从流程中读取。通过实现`readResolve`方法，一个类可以直接控制被反序列化的实例的类型和实例本身。(2)\
      \ 对序列化和外部化类而言，`writeReplace`方法允许一个对象的类在对象写入之前在流中提名自身的替代。通过实现`writeReplace`方法，一个类可以直接控制被序列化的实例的类型和实例本身。\
      \   \n不像其它的接口，Serializable 没有定义需要的接口方法签名。接口仅允许公有的字段和方法，而 `readObject()`、`readObjectNoData()`和\
      \ `writeObject()`必须要声明为私有。同样，Serializable不能防止 `readResolve()`和 `writeReplace()`接口被声明为静态的、公有的或者私有的。\
      \   \n3) 示例   \n    1、一个不符合规则的代码示例（`readObject()`、`writeObject()`)，该示例展示了一个\
      \ Ser 类，该类使用私有的构造函数，意味着该类的外部代码不能创建这个类的实例。这个类实现了 java.io.Serializable接口并且定义了公有的\
      \ `readObject()`和 `writeObject()`方法。因此，非受信代码可以通过使用 `readObject()`来得到重组对象，并且可以使用\
      \ `writeObject()`来写入流。   \n    ```java\n    public class Ser implements Serializable\
      \ {\n        private final long serialVersionUID = 123456789;\n    \n      \
      \  private Ser() {\n            // initialize\n        }\n    \n        public\
      \ static void writeObject(final ObjectOutputStream stream) throws IOException\
      \ {\n            stream.defaultWriteObject();\n        }\n    \n        public\
      \ static void readObject(final ObjectInputStream stream) throws IOException,\
      \ ClassNotFoundException {\n            stream.defaultReadObject();\n      \
      \  }\n    }\n    ```   \n    同样，忽略 static 关键字并不会让这个示例代码安全。JVM 不会发现这两个方法，导致不能使用自定义的序列化格式。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 502
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 502
      - type: CWE
        value: 502:Deserialization of Untrusted Data
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160122
    language: Java
  - Advice: "对方法的返回值进行检查。\n**规范的代码样例1**：  \n```java\npublic void deleteFile() {\n\
      \    File someFile = new File(\"someFileName.txt\");\n    // do something with\
      \ someFile\n    if (!someFile.delete()) {\n        // handle failure to delete\
      \ the f ile\n    }\n}\n```  \n代码对`delete()`方法的boolean返回值进行检查，并且对没有删除成功的情况进行处理。\
      \  \n**规范的代码样例2**：  \n```java\npublic class Replace {\n    public static void\
      \ main(String[] args) {\n        String original = \"insecure\";\n        String\
      \ originalNew = original.replace('i', '9');\n        System.out.println(originalNew);\n\
      \    }\n}\n```  \n代码将`String.replace()`方法的返回值赋值给了一个新的字符串`originalNew`。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution checks the Boolean\
      \ value returned by the `delete()` method and handles any resulting errors:\n\
      \n```java\npublic void deleteFile(){\n\n  File someFile = new File(\"someFileName.txt\"\
      );\n  // Do something with someFile\n  if (!someFile.delete()) {\n    // Handle\
      \ failure to delete the file\n  }\n\n}\n```\n**Compliant Solution**\n\nThis\
      \ compliant solution correctly updates the `String` reference `original` with\
      \ the return value from the `String.replace()` method:\n\n```java\npublic class\
      \ Replace {\n  public static void main(String[] args) {\n    String original\
      \ = \"insecure\";\n    original = original.replace('i', '9');\n    System.out.println(original);\n\
      \  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 02. 表达式 (EXP)
      - type: DetailClassChin
        value: EXP00-J：不要忽略方法返回值
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 02. Expressions (EXP)
      - type: DetailClassChin
        value: EXP00-J:Do not ignore values returned by methods
    Default: true
    Description: "方法返回值通常可以知道方法的执行结果，如果忽略了方法返回值，就难以知道方法的执行结果，特别是调用该方法的代码中没有进行适当处理，可能会导致不可预期的结果，尤其是方法执行类似权限校验的操作，甚至会导致安全风险。所以，程序必须重视方法的返回值。\n\
      当getter方法在一个动作之后定义时，程序员可能无法意识到预期的返回值。例如， `ProcessBuilder.redirectErrorStream()`\
      \ 方法的唯一目的是通过返回值报告流程构建器是否成功合并标准错误和标准输出。 实际执行错误流重定向的方法是重载的单参数方法 `ProcessBuilder.redirectErrorStream(boolean)`\n\
      **不规范的代码样例1**：  \n```java\npublic void deleteFile(){\n\n  File someFile = new\
      \ File(\"someFileName.txt\");\n  // Do something with someFile\n  someFile.delete();\n\
      \n}\n```\n代码中调用了`File.delete()`方法用来删除一个文件，但是不确认文件是否删除成功。\n**不规范的代码样例2（替换字符串）**：\n\
      ```java\npublic class Replace {\n  public static void main(String[] args) {\n\
      \    String original = \"insecure\";\n    original.replace('i', '9');\n    System.out.println(original);\n\
      \  }\n}\n```\n这段代码忽略了`String.replace()`方法的返回值，`String.replace()`方法不会修改当前的字符串，而是返回一个新的字符串引用，新的字符串引用才是修改后的数据。"
    Description_En: "Methods can return values to communicate failure or success or\
      \ to update local objects or fields. Security risks can arise when method return\
      \ values are ignored or when the invoking method fails to take suitable action.\
      \ Consequently, programs must not ignore method return values.\n\nWhen getter\
      \ methods are named after an action, a programmer could fail to realize that\
      \ a return value is expected. For example, the only purpose of the `ProcessBuilder.redirectErrorStream()`\
      \ method is to report via return value whether the process builder successfully\
      \ merged standard error and standard output. The method that actually performs\
      \ redirection of the error stream is the overloaded single-argument method `ProcessBuilder.redirectErrorStream(boolean)`.\n\
      \n**Noncompliant Code Example (File Deletion)**\n\nThis noncompliant code example\
      \ attempts to delete a file but fails to check whether the operation has succeeded:\n\
      \n```java\npublic void deleteFile(){\n\n  File someFile = new File(\"someFileName.txt\"\
      );\n  // Do something with someFile\n  someFile.delete();\n\n}\n```\n\n**Noncompliant\
      \ Code Example (String Replacement)**\n\nThis noncompliant code example ignores\
      \ the return value of the `String.replace()` method, failing to update the original\
      \ string. The `String.replace()` method cannot modify the state of the `String`\
      \ (because `String` objects are immutable); rather, it returns a reference to\
      \ a new `String` object containing the modified string.\n\n```java\npublic class\
      \ Replace {\n  public static void main(String[] args) {\n    String original\
      \ = \"insecure\";\n    original.replace('i', '9');\n    System.out.println(original);\n\
      \  }\n}\n```\n\nIt is especially important to process the return values of immutable\
      \ object methods. Although many methods of mutable objects operate by changing\
      \ some internal state of the object, methods of immutable objects cannot change\
      \ the object and often return a mutated new object, leaving the original object\
      \ unchanged."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 754:Improper Check for Unusual or Exceptional Conditions
      - type: CWE
        value: 252:Unchecked Return Value
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Risky Resource Management - CWE ID 754
      - type: SJ/T 11683-2017
        value: 6.4.1 忽略方法的返回值
      - type: SJ/T 11683-2017
        value: 6.6.12 忽略无副作用方法的返回值
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140400'
    language: Java
  - Advice: "程序应检查方法的返回值，确保方法返回的是期望的数据，再进行下一步操作。  \n  \n**例1**：在下面的代码片段中，程序没有对`read()`返回值做判断\
      \  \n```java\n...  \nString name = request.getParameter(\"name\");  \nname =\
      \ name.replaceAll(\"-\",\"\");  \n...\n```  \n**例2**：在下面的代码片段中，程序没有对`read()`返回值做判断\
      \  \n```java\n...  \nint bytesToRead = 1024;  \nbyte[] byteArray = new byte[bytesToRead];\
      \  \nstreamFileInput = new FileInputStream(\"C:\\\\file.txt\");  \nint i = -1;\
      \  \nwhile((i = streamFileInput.read(byteArray)) !=-1){  \n\t...  \n}  \n...\n\
      ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【推荐】不要忽略返回值
    Categories_En:
      Category: []
    Default: true
    Description: "程序员可以假设某些事件或条件永远不会发生或者不需要担心，比如内存条件低，由于限制性权限而无法访问资源，某个函数调用永远不会失败，或者行为不当的客户端或组件。但是，攻击者可能会故意触发这些异常情况，从而违反了程序员的假设，可能会引入不稳定，不正确的行为或漏洞。或者程序调用函数来删除权限但不检查返回代码以确保成功删除权限，则程序将继续以更高权限运行。\
      \  \n  \n**例1**：在下面的代码片段中，程序在替换完字符串之后没有保存返回值  \n```java\n...  \nString name\
      \ = request.getParameter(\"name\");  \nname.replaceAll(\"-\",\"\");  \n...\n\
      ```  \n**例2**：在下面的代码片段中，程序没有对`read()`返回值做判断  \n```java\n...  \nint bytesToRead\
      \ = 1024;  \nbyte[] byteArray = new byte[bytesToRead];  \nstreamFileInput =\
      \ new FileInputStream(\"C:\\\\file.txt\");  \nstreamFileInput.read(byteArray);\
      \  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: Unchecked Return Value
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 754:Improper Check for Unusual or Exceptional Conditions
      - type: CWE
        value: 252:Unchecked Return Value
      - type: CERT
        value: EXP00-J:Do not ignore values returned by methods
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Risky Resource Management - CWE ID 754
      - type: SJ/T 11683-2017
        value: 6.4.1 忽略方法的返回值
      - type: SJ/T 11683-2017
        value: 6.6.12 忽略无副作用方法的返回值
    Security: not
    Tag: correctness,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150114'
    language: Java
  - Advice: "对应的符合规则的代码示例：对`delete()`方法返回的 boolean 变量进行检查，并且处理了可能出现的错误。   \n    ```java\n\
      \    public void deleteFile()  {   \n        File someFile=new File (\"someFileName.txt\"\
      );   \n           //do something with someFile   \n        if (!someFile.delete())\
      \  {   \n           //handle failure to delete the file   \n        }   \n \
      \   }\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.3.1 不要忽略方法的返回值
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n必须重视方法的返回值。   \n2) 原理   \n通过返回值来判断方法调用成功与否，或者通过返回值来更新本地的对象或数据成员时，若忽略方法的返回值，会产生安全风险，因此必须重视方法调用的返回值。\
      \   \n当获取器(getter)方法是以所做的操作命名时，程序员可能无法确认是否有返回值。   \n3) 示例   \n    1.这个不符合规则的代码示例想要删除一个文件，但没有确认这个删除动作是否成功。\
      \   \n    ```java\n    public void deleteFile()  {   \n        File someFile=new\
      \ File (\"someFileName.txt“);   \n        //do something with someFile   \n\
      \        someFile.delete();   \n    }\n    ```   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 754:Improper Check for Unusual or Exceptional Conditions
      - type: CWE
        value: 252:Unchecked Return Value
      - type: CERT
        value: EXP00-J:Do not ignore values returned by methods
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Risky Resource Management - CWE ID 754
      - type: SJ/T 11683-2017
        value: 6.4.1 忽略方法的返回值
      - type: SJ/T 11683-2017
        value: 6.6.12 忽略无副作用方法的返回值
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160018
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\npublic void deleteFile()\
      \ {\n    File someFile = new File(\"someFileName.txt\");\n    // do something\
      \ with someFile\n    if (!someFile.delete()) {\n        // handle failure to\
      \ delete the f ile\n    }\n}\n```\n代码对`delete()`方法的boolean返回值进行检查，并且对没有删除成功的情况进行处理。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 7.4 文件管理
      - type: DetailClassChin
        value: A.4.4.4 不要忽略方法返回值
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：方法返回值通常可以知道方法的执行结果，如果忽略了方法返回值，就难以知道方法的执行结果，特别是调用该方法的代码中没有进行适当处理，可能会导致不可预期的结果，尤其是方法执行类似权限校验的操作，甚至会导致安全风险。所以，程序必须重视方法的返回值。\
      \  \n对于不要忽略方法返回值的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\npublic void\
      \ deleteFile() {\n    File someFile = new File(\"someFileName.txt\");\n    //\
      \ do something with someFile\n    someFile.delete();\n}\n```  \n代码中调用了`File.delete()`方法用来删除一个文件，但是不确认文件是否删除成功。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 754:Improper Check for Unusual or Exceptional Conditions
      - type: CWE
        value: 252:Unchecked Return Value
      - type: CERT
        value: EXP00-J:Do not ignore values returned by methods
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Risky Resource Management - CWE ID 754
      - type: SJ/T 11683-2017
        value: 6.4.1 忽略方法的返回值
      - type: SJ/T 11683-2017
        value: 6.6.12 忽略无副作用方法的返回值
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170067'
    language: Java
  - Advice: 建议使用框架（如EJB、Spring等）的相同功能的方法，取代直接使用线程管理。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 并发规范
      - type: DetailClassChin
        value: 【推荐】不要在JavaEE程序中直接使用线程
    Categories_En:
      Category: []
    Default: not
    Description: "JAVA EE标准禁止在某些环境下使用Web应用程序中的线程管理，因为此时使用线程管理非常容易出错。线程管理起来很困难，并且可能还会以不可预知的方式干扰应用程序容器。即使容器没有受到干扰，线程管理通常还会导致各种难以发现的错误，如死锁、竞争条件及其他同步错误等。\
      \  \n  \n**例如**：下面代码片段中，在Servlet的`doGet()`方法中，直接创建了一个线程对象。  \n```java\npublic\
      \ void doGet(HttpServletRequest request, HttpServletResponse response) throws\
      \ ServletException, IOException {  \n    // Perform servlet tasks.  \n    ...\
      \  \n    // Create a new thread to handle background processing.  \n    Runnable\
      \ r = new Runnable() {  \n        public void run() {  \n            // Process\
      \ and store request statistics.  \n            ...  \n        }  \n    };  \n\
      \    new Thread(r).start();  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Threads
    ENSubClassChin: J2EE Bad Practices
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: true
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '383:J2EE Bad Practices: Direct Use of Threads'
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150402'
    language: Java
  - Advice: "采用`java.sql.PreparedStatement`来执行参数化查询，正确的使用该类可以避免含有注入代码的SQL语句提交执行。 \
      \ \n**规范的代码样例1**：  \n```java\npublic void doPrivilegedAction(String username,\
      \ char[] password) throws SQLException {\n    Connection connection = getConnection();\n\
      \    if (connection == null) {\n        // Handle error\n    }\n    try {\n\
      \        String pwd = hashPassword(password);\n\n        // Validate username\
      \ length\n        if (username.length() > 8) {\n            // Handle error\n\
      \        }\n\n        String sqlString = \"select * from db_user where username=?\
      \ and password=?\";\n        PreparedStatement stmt = connection.prepareStatement(sqlString);\n\
      \        stmt.setString(1, username);\n        stmt.setString(2, pwd);\n   \
      \     ResultSet rs = stmt.executeQuery();\n        if (!rs.next()) {\n     \
      \       throw new SecurityException(\"User name or password incorrect\");\n\
      \        }\n\n        // Authenticated; proceed\n    } finally {\n        try\
      \ {\n            connection.close();\n        } catch (SQLException x) {\n \
      \           // Forward to handler\n        }\n    }\n}\n```  \n代码使用参数化查询来防止SQL注入，通过使用`prepareStatement`类的`set*()`方法，可以进行强类型检查。这样可以减少SQL注入漏洞，因为程序会正确的转义双引号内的输入数据。需要注意的是，那些将数据插入数据库的查询也必须使用`prepareStatement`。"
    Advice_En: "**Compliant Solution (`PreparedStatement`)**\n\nThis compliant solution\
      \ uses a parametric query with a `?` character as a placeholder for the argument.\
      \ This code also validates the length of the `username` argument, preventing\
      \ an attacker from submitting an arbitrarily long user name.\n\n```java\npublic\
      \ void doPrivilegedAction(\n  String username, char[] password\n) throws SQLException\
      \ {\n  Connection connection = getConnection();\n  if (connection == null) {\n\
      \    // Handle error\n  }\n  try {\n    String pwd = hashPassword(password);\n\
      \n    // Validate username length\n    if (username.length() > 8) {\n      //\
      \ Handle error\n    }\n\n    String sqlString =\n      \"select * from db_user\
      \ where username=? and password=?\";\n    PreparedStatement stmt = connection.prepareStatement(sqlString);\n\
      \    stmt.setString(1, username);\n    stmt.setString(2, pwd);\n    ResultSet\
      \ rs = stmt.executeQuery();\n    if (!rs.next()) {\n      throw new SecurityException(\"\
      User name or password incorrect\");\n    }\n\n    // Authenticated; proceed\n\
      \  } finally {\n    try {\n      connection.close();\n    } catch (SQLException\
      \ x) {\n      // Forward to handler\n    }\n  }\n}\n```\n\nUse the `set*()`\
      \ methods of the `PreparedStatement` class to enforce strong type checking.\
      \ This technique mitigates the SQL injection vulnerability because the input\
      \ is properly escaped by automatic entrapment within double quotes. Note that\
      \ prepared statements must be used even with queries that insert data into the\
      \ database."
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 00. 输入验证和数据处理 (IDS)
      - type: DetailClassChin
        value: IDS00-J：预防SQL注入
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 00. Input Validation and Data Sanitization (IDS)
      - type: DetailClassChin
        value: IDS00-J:Prevent SQL injection
    Default: true
    Description: "当初始化的SQL查询被修改成另外一个完全不同形式的查询的时候，就会出现SQL注入漏洞。执行这个被修改过的查询，可能导致信息泄露或者数据被修改。防止SQL注入的主要方法是：净化并验证非受信输入，并采用参数化查询。\n\
      假设一个数据库的表db_user中存储了用户名和密码，它们用于对系统用户进行认证。用户名使用长度为8的字符串表示，密码使用长度为20的字符串表示。\n\
      一个用来验证用户的SQL命令如下：`SELECT * FROM db_user WHERE username='<USERNAME>' AND password='<PASSWORD>'\
      \ `\n如果它可以返回记录，那么意味着用户名和密码是合法的。\n然而，如果攻击者能够替换`<USERNAME>`和`<PASSWORD>`中的任意字符串，它们就可以使用下面关于`<USERNAME>`的字符串进行SQL注入攻击：`validuser'\
      \ OR '1'='1 `\n当其提交SQL查询时，命令就变成：`SELECT * FROM db_user WHERE username='validuser'\
      \ OR '1'='1' AND password='<PASSWORD>'`\n如果`validuser`确实是一个存在的用户名，那么这条语句就会返回表中存储的`validuser`用户的记录。这个操作就成功的跳过了密码验证。\n\
      同样，如果攻击者为`<PASSWORD>`提交字符串：`' OR '1'='1`\n这样就会产生以下命令：`SELECT * FROM db_user\
      \ WHERE username='' AND password='' OR '1'='1'`\n这一次`'1'='1'`是永远成立的，它使得用户名和密码验证都是无效的，攻击者可以不需要用户名和密码就通过了用户认证。\n\
      **不规范的代码样例1**：  \n```java\nimport java.sql.Connection;\nimport java.sql.DriverManager;\n\
      import java.sql.ResultSet;\nimport java.sql.SQLException;\nimport java.sql.Statement;\n\
      \nclass Login {\n    public Connection getConnection() throws SQLException {\n\
      \        DriverManager.registerDriver(new com.microsoft.sqlserver.jdbc.SQLServerDriver());\n\
      \        String dbConnection = PropertyManager.getProperty(\"db.connection\"\
      );\n        // Can hold some value like\n        // \"jdbc:microsoft:sqlserver://\\\
      <HOST\\>:1433,<UID>,<PWD>\"\n        return DriverManager.getConnection(dbConnection);\n\
      \    }\n\n    String hashPassword(char[] password) {\n        // Create hash\
      \ of password\n    }\n\n    public void doPrivilegedAction(String username,\
      \ char[] password) throws SQLException {\n        Connection connection = getConnection();\n\
      \        if (connection == null) {\n            // Handle error\n        }\n\
      \        try {\n            String pwd = hashPassword(password);\n\n       \
      \     String sqlString = \"SELECT * FROM db_user WHERE username = '\" + username\
      \ + \"' AND password = '\" + pwd + \"'\";\n            Statement stmt = connection.createStatement();\n\
      \            ResultSet rs = stmt.executeQuery(sqlString);\n\n            if\
      \ (!rs.next()) {\n                throw new SecurityException(\"User name or\
      \ password incorrect\");\n            }\n\n            // Authenticated; proceed\n\
      \        } finally {\n            try {\n                connection.close();\n\
      \            } catch (SQLException x) {\n                // Forward to handler\n\
      \            }\n        }\n    }\n}\n```\n代码使用JDBC代码来认证用户，提交`stmt.executeQuery(sqlString)`查询的SQL为`sqlString`，它是通过一个拼接字符串组合而来，并且`sqlString`未经过净化或验证，`Statement.executeQuery(String)`方法不能防止SQL注入，该代码将会引发SQL注入漏洞攻击。\
      \  \n**不规范的代码样例2**：  \n```java\nimport java.sql.Connection;\nimport java.sql.DriverManager;\n\
      import java.sql.ResultSet;\nimport java.sql.SQLException;\nimport java.sql.Statement;\n\
      \nclass Login {\n    public Connection getConnection() throws SQLException {\n\
      \        DriverManager.registerDriver(new com.microsoft.sqlserver.jdbc.SQLServerDriver());\n\
      \        String dbConnection = PropertyManager.getProperty(\"db.connection\"\
      );\n        // Can hold some value like  \n        // \"jdbc:microsoft:sqlserver://\\\
      <HOST\\>:1433,<UID>,<PWD>\"  \n        return DriverManager.getConnection(dbConnection);\n\
      \    }\n\n    String hashPassword(char[] password) {\n        // Create hash\
      \ of password  \n    }\n\n    public void doPrivilegedAction(String username,\
      \ char[] password) throws SQLException {\n        Connection connection = getConnection();\n\
      \        if (connection == null) {\n            // Handle error\n        }\n\
      \        try {\n            String pwd = hashPassword(password);\n         \
      \   String sqlString = \"select * from db_user where username=\" + username\
      \ + \" and password =\" + pwd;\n            PreparedStatement stmt = connection.prepareStatement(sqlString);\n\
      \n            ResultSet rs = stmt.executeQuery();\n            if (!rs.next())\
      \ {\n                throw new SecurityException(\"User name or password incorrect\"\
      );\n            }\n\n            // Authenticated; proceed\n        } finally\
      \ {\n            try {\n                connection.close();\n            } catch\
      \ (SQLException x) {\n                // Forward to handler\n            }\n\
      \        }\n    }\n}\n```\n`java.sql.PreparedStatement`类恰当转义输入字符串。当正确使用时，可以防止SQL注入。这段代码示例修改`doPrivilegedAction()`方法使用`PreparedStatement`代替`java.sql.Statement`。在预处理语句中，当未验证的用户输入，仍然会导致SQL注入。"
    Description_En: "SQL injection vulnerabilities arise in applications where elements\
      \ of a SQL query originate from an untrusted source. Without precautions, the\
      \ untrusted data may maliciously alter the query, resulting in information leaks\
      \ or data modification. The primary means of preventing SQL injection are sanitization\
      \ and validation, which are typically implemented as parameterized queries and\
      \ stored procedures.\n\nSuppose a system authenticates users by issuing the\
      \ following query to a SQL database. If the query returns any results, authentication\
      \ succeeds; otherwise, authentication fails.\n\n```\nSELECT * FROM db_user WHERE\
      \ username='<USERNAME>' AND\n                            password='<PASSWORD>'\n\
      ```\n\nSuppose an attacker can substitute arbitrary strings for `<USERNAME>`\
      \ and `<PASSWORD>`. In that case, the authentication mechanism can be bypassed\
      \ by supplying the following `<USERNAME>` with an arbitrary password:\n\n```\n\
      validuser' OR '1'='1\n```\n\nThe authentication routine dynamically constructs\
      \ the following query:\n\n```\nSELECT * FROM db_user WHERE username='validuser'\
      \ OR '1'='1' AND password='<PASSWORD>'\n```\n\nIf `validuser` is a valid user\
      \ name, this `SELECT` statement yields the `validuser` record in the table.\
      \ The password is never checked because `username='validuser'` is true; consequently,\
      \ the items after the `OR` are not tested. As long as the components after the\
      \ `OR` generate a syntactically correct SQL expression, the attacker is granted\
      \ the access of `validuser`.\n\nSimilarly, an attacker could supply the following\
      \ string for `<PASSWORD>` with an arbitrary username:\n\n```\n' OR '1'='1\n\
      ```\n\nproducing the following query:\n\n```\nSELECT * FROM db_user WHERE username='<USERNAME>'\
      \ AND password='' OR '1'='1'\n```\n\n`'1'='1'` always evaluates to true, causing\
      \ the query to yield every row in the database. In this scenario, the attacker\
      \ would be authenticated without needing a valid username or password.\n\n**Noncompliant\
      \ Code Example**\n\nThis noncompliant code example shows JDBC code to authenticate\
      \ a user to a system. The password is passed as a `char` array, the database\
      \ connection is created, and then the passwords are hashed.\n\nUnfortunately,\
      \ this code example permits a SQL injection attack by incorporating the unsanitized\
      \ input argument `username` into the SQL command, allowing an attacker to inject\
      \ `validuser' OR '1'='1`. The `password` argument cannot be used to attack this\
      \ program because it is passed to the `hashPassword()` function, which also\
      \ sanitizes the input.\n\n```java\nimport java.sql.Connection;\nimport java.sql.DriverManager;\n\
      import java.sql.ResultSet;\nimport java.sql.SQLException;\nimport java.sql.Statement;\n\
      \nclass Login {\n  public Connection getConnection() throws SQLException {\n\
      \    DriverManager.registerDriver(new\n            com.microsoft.sqlserver.jdbc.SQLServerDriver());\n\
      \    String dbConnection =\n      PropertyManager.getProperty(\"db.connection\"\
      );\n    // Can hold some value like\n    // \"jdbc:microsoft:sqlserver://<HOST>:1433,<UID>,<PWD>\"\
      \n    return DriverManager.getConnection(dbConnection);\n  }\n\n  String hashPassword(char[]\
      \ password) {\n    // Create hash of password\n  }\n\n  public void doPrivilegedAction(String\
      \ username, char[] password)\n                                 throws SQLException\
      \ {\n    Connection connection = getConnection();\n    if (connection == null)\
      \ {\n      // Handle error\n    }\n    try {\n      String pwd = hashPassword(password);\n\
      \n      String sqlString = \"SELECT * FROM db_user WHERE username = '\"\n  \
      \                       + username +\n                         \"' AND password\
      \ = '\" + pwd + \"'\";\n      Statement stmt = connection.createStatement();\n\
      \      ResultSet rs = stmt.executeQuery(sqlString);\n\n      if (!rs.next())\
      \ {\n        throw new SecurityException(\n          \"User name or password\
      \ incorrect\"\n        );\n      }\n\n      // Authenticated; proceed\n    }\
      \ finally {\n      try {\n        connection.close();\n      } catch (SQLException\
      \ x) {\n        // Forward to handler\n      }\n    }\n  }\n}\n```\n\n**Noncompliant\
      \ Code Example (`PreparedStatement`)**\n\nThe JDBC library provides an API for\
      \ building SQL commands that sanitize untrusted data. The `java.sql.PreparedStatement`\
      \ class properly escapes input strings, preventing SQL injection when used correctly.\
      \ This code example modifies the `doPrivilegedAction()` method to use a `PreparedStatement`\
      \ instead of `java.sql.Statement`. However, the prepared statement still permits\
      \ a SQL injection attack by incorporating the unsanitized input argument `username`\
      \ into the prepared statement.\n\n```java\nimport java.sql.Connection;\nimport\
      \ java.sql.DriverManager;\nimport java.sql.ResultSet;\nimport java.sql.SQLException;\n\
      import java.sql.Statement;\n\nclass Login {\n  public Connection getConnection()\
      \ throws SQLException {\n    DriverManager.registerDriver(new\n            com.microsoft.sqlserver.jdbc.SQLServerDriver());\n\
      \    String dbConnection =\n      PropertyManager.getProperty(\"db.connection\"\
      );\n    // Can hold some value like\n    // \"jdbc:microsoft:sqlserver://<HOST>:1433,<UID>,<PWD>\"\
      \n    return DriverManager.getConnection(dbConnection);\n  }\n\n  String hashPassword(char[]\
      \ password) {\n    // Create hash of password\n  }\n\n  public void doPrivilegedAction(\n\
      \    String username, char[] password\n  ) throws SQLException {\n    Connection\
      \ connection = getConnection();\n    if (connection == null) {\n      // Handle\
      \ error\n    }\n    try {\n      String pwd = hashPassword(password);\n    \
      \  String sqlString = \"select * from db_user where username=\" +\n        username\
      \ + \" and password =\" + pwd;\n      PreparedStatement stmt = connection.prepareStatement(sqlString);\n\
      \n      ResultSet rs = stmt.executeQuery();\n      if (!rs.next()) {\n     \
      \   throw new SecurityException(\"User name or password incorrect\");\n    \
      \  }\n\n      // Authenticated; proceed\n    } finally {\n      try {\n    \
      \    connection.close();\n      } catch (SQLException x) {\n        // Forward\
      \ to handler\n      }\n    }\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '564:SQL Injection: Hibernate'
      - type: CWE
        value: 89:Improper Neutralization of Special Elements used in an SQL Command
          ('SQL Injection')
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2011:Insecure Interaction - CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 89
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Injection [RST]
      - type: GB/T 34944-2017 Java
        value: 数据处理:SQL注入
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: cert
    WhiteList: true
    id: '0102000010140230'
    language: Java
  - Advice: "这个符合规则的方案使用白名单来净化输入数据。在这个方案中，处理方法要求输入的数字必须为0 ～ 9。   \n    ```java\n \
      \   private void createXMLStream(BufferedOutputStream outStream, String quantity)\
      \ throws IOException {\n        // Write XML string if quantity contains numbers\
      \ only.\n        // Blacklisting of invalid characters can be performed\n  \
      \      // in conjunction.\n        if (!Pattern.matches(\"[0-9]+\", quantity))\
      \ {\n            // Format violation\n        }\n        String xmlString =\
      \ \"<item>\\n<description>Widget</description>\\n\" + \"<price>500</price>\\\
      n\" + \"<quantity>\"\n                + quantity + \"</quantity></item>\";\n\
      \        outStream.write(xmlString.getBytes());\n        outStream.flush();\n\
      \    }\n    ```   \n    一个更为通用的检查XML以防止注入的方法是，可以使用文档类型定义（Document TypeDefinition，DTD）或模板（schema）来进行验证。模板必须严格定义，从而防止通过注入的方式将一个合法的XML\
      \ 文档变成错误的文档。以下是一个用来验证XML 文档片段的合适的模板：   \n    ```xml\n    <xs:schema xmlns:xs=\"\
      http://www.w3.org/2001/XMLSchema\">\n        <xs:element name=\"item\">\n  \
      \          <xs:complexType>\n                <xs:sequence>\n               \
      \     <xs:element name=\"description\" type=\"xs:string\" />\n             \
      \       <xs:element name=\"price\" type=\"xs:decimal\" />\n                \
      \    <xs:element name=\"quantity\" type=\"xs:integer\" />\n                </xs:sequence>\n\
      \            </xs:complexType>\n        </xs:element>\n    </xs:schema>\n  \
      \  ```   \n    在`schema.xsd`文件中可以得到XML 模板。在这个符合规则的方案中，可以采用这个模板来防止XML   \n  \
      \  注入。它同时需要使用CustomResolver 类来防止XXE 攻击。关于这个类和所谓的XXE 攻击，可以参见如下代码示例中的描述。   \n\
      \    ```java\n    private void createXMLStream(BufferedOutputStream outStream,\
      \ String quantity) throws IOException {\n        String xmlString;\n       \
      \ xmlString = \"<item>\\n<description>Widget</description>\\n\" + \"<price>500.0</price>\\\
      n\" + \"<quantity>\" + quantity\n                + \"</quantity></item>\";\n\
      \        InputSource xmlStream = new InputSource(new StringReader(xmlString));\n\
      \        // Build a validating SAX parser using our schema\n        SchemaFactory\
      \ sf = SchemaFactory.newInstance(XMLConstants.W3C_XML_SCHEMA_NS_URI);\n    \
      \    DefaultHandler defHandler = new DefaultHandler() {\n            public\
      \ void warning(SAXParseException s) throws SAXParseException {\n           \
      \     throw s;\n            }\n    \n            public void error(SAXParseException\
      \ s) throws SAXParseException {\n                throw s;\n            }\n \
      \   \n            public void fatalError(SAXParseException s) throws SAXParseException\
      \ {\n                throw s;\n            }\n        };\n        StreamSource\
      \ ss = new StreamSource(new File(\"schema.xsd\"));\n        try {\n        \
      \    Schema schema = sf.newSchema(ss);\n            SAXParserFactory spf = SAXParserFactory.newInstance();\n\
      \            spf.setSchema(schema);\n            SAXParser saxParser = spf.newSAXParser();\n\
      \            // To set the custom entity resolver\n            // an XML reader\
      \ needs to be created\n            XMLReader reader = saxParser.getXMLReader();\n\
      \            reader.setEntityResolver(new CustomResolver());\n            saxParser.parse(xmlStream,\
      \ defHandler);\n        } catch (ParserConfigurationException x) {\n       \
      \     throw new IOException(\"Unable to validate XML\", x);\n        } catch\
      \ (SAXException x) {\n            throw new IOException(\"Invalid quantity\"\
      , x);\n        }\n        // Our XML is valid, proceed\n        outStream.write(xmlString.getBytes());\n\
      \        outStream.flush();\n    }\n    ```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 5.2 数据净化
      - type: DetailClassChin
        value: C.1.1 净化穿越受信边界的非受信数据
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序必须对传递给命令行解释器或者解析器的所有字符串数据进行净化，以保证解析器处理后或者解释器处理后的字符串是无害的。\
      \   \n2) 原理   \n许多程序会接受来自未经验证的用户数据、网络连接以及其它来自于非受信域的不可信数据，然后会将这些不可信数据（经过修改或不经修改）穿过受信边界送到另一个受信域中。通常，这些数据是以字符串的形式出现的，并且会有既定的内部数据结构，这种数据结构必须能够被子系统所解析。同时，必须净化这些数据。因为子系统可能并不具备处理恶意输入信息的能力，而未经净化数据输入很有可能包含注入攻击。\
      \   \n一些命令行解释器和解析器会提供自己的数据净化和数据验证的方法。当存在这样的方法时，应当优先使用这些方法。   \n然而，需要注意的是，自定义的净化方法可能会忽略一些特殊的情况，或者会忽略在解析器中所隐含着的那些复杂性。还要注意一种情况是：当有新的功能添加到命令行解释器或者添加到解析器软件时，自定义的处理方法可能并不能得到很好的维护。\
      \   \n在JDBC 类库中，提供了能够构建SQL 命令并且处理非受信数据的API。`java.sql.PreparedStatement`类中，可以对输入字符串进行转义，如果使用正确的话，可以防止SQL\
      \ 注入。   \n通过使用PreparedStatement 类的`set*()`方法，可以进行强类型检查。这样可以减少SQL 注入漏洞，因为自动化例程会正确地转义双引号内的输入数据。需要注意的是，那些将数据插入数据库的查询也会使用PreparedStatement\
      \ 。   \n3) 示例   \n    **例 1**：在这个不符合规则的代码示例中，系统使用 JDBC 代码来认证用户。密码通过 char 数组传入，建立数据库连接，然后进行哈希编码。\
      \   \n    这段代码会出现SQL 注入问题，因为在SQL 语句中，`sqlString` 允许输入未经净化的输入参数。   \n    ```java\
      \ \n    class Login {\n        public Connection getConnection() throws SQLException\
      \ {\n            DriverManager.registerDriver(new com.microsoft.sqlserver.jdbc.SQLServerDriver());\n\
      \            String dbConnection = PropertyManager.getProperty(\"db.connection\"\
      );\n            // can hold some value like\n            // \"jdbc:microsoft:sqlserver://<HOST>:1433,<UID>,<PWD>\"\
      \n            return DriverManager.getConnection(dbConnection);\n        }\n\
      \    \n        String hashPassword(char[] password) {\n            // create\
      \ hash of password\n        }\n    \n        public void doPrivilegedAction(String\
      \ username, char[] password) throws SQLException {\n            Connection connection\
      \ = getConnection();\n            if (connection == null) {\n              \
      \  // handle error\n            }\n            try {\n                String\
      \ pwd = hashPassword(password);\n                String sqlString = \"SELECT\
      \ * FROM db_user WHERE username = '\" + username + \"’ AND password = '\" +\
      \ pwd\n                        + \"’\";\n                Statement stmt = connection.createStatement();\n\
      \                ResultSet rs = stmt.executeQuery(sqlString);\n            \
      \    if (!rs.next()) {\n                    throw new SecurityException(\"User\
      \ name or password incorrect\");\n                }\n                // Authenticated;\
      \ proceed\n            } finally {\n                try {\n                \
      \    connection.close();\n                } catch (SQLException x) {\n     \
      \               // forward to handler\n                }\n            }\n  \
      \      }\n    }  \n    ```\n    对应的符合规则的方案修改了`doPrivilegedAction()` 方法， 使用PreparedStatement\
      \ 类来代替`java.sql.Statement`。并且这段代码会验证`username`参数的长度，防止如果提交一个长用户名的时候可能会出现的攻击。\
      \   \n    ```java\n    public void doPrivilegedAction(String username, char[]\
      \ password) throws SQLException {\n        Connection connection = getConnection();\n\
      \        if (connection == null) {\n            // Handle error\n        }\n\
      \        try {\n            String pwd = hashPassword(password);\n         \
      \   // Ensure that the length of user name is legitimate\n            if (username.length()\
      \ > 8) {   \n                // Handle error   \n            }  \n         \
      \   String sqlString = \"select * from db_user where username=? and password=?\"\
      ;\n            PreparedStatement stmt = connection.prepareStatement(sqlString);\n\
      \            stmt.setString(1, username);\n            stmt.setString(2, pwd);\n\
      \            ResultSet rs = stmt.executeQuery();\n            if (!rs.next())\
      \ {\n                throw new SecurityException(\"User name or password incorrect\"\
      );\n            }\n            // Authenticated, proceed\n        } finally\
      \ {\n            try {\n                connection.close();\n            } catch\
      \ (SQLException x) {\n                // forward to handler\n            }\n\
      \        }\n    }\n    ```    \n    \n    **例 2**：在下面这段不符合规则的代码示例中，一个客户方法使用简单的字符串链接来创建一个\
      \ XML 查询，然后将其发送到服务器。在这时就有可能出现 XML 注入问题，因为这个方法并没有进行任何输入验证。   \n    ```java\n\
      \    private void createXMLStream(BufferedOutputStream outStream, String quantity)\
      \ throws IOException {\n        String xmlString;\n        xmlString = \"<item>\\\
      n<description>Widget</description>\\n\" + \"<price>500.0</price>\\n\" + \"<quantity>\"\
      \ + quantity\n                + \"</quantity></item>\";\n        outStream.write(xmlString.getBytes());\n\
      \        outStream.flush();\n    }\n    ```   \n    根据特定的数据和接收这些数据的命令解释器或者解析器的情况，必须使用一个适当的方法来净化这些非受信的用户输入。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '564:SQL Injection: Hibernate'
      - type: CWE
        value: 89:Improper Neutralization of Special Elements used in an SQL Command
          ('SQL Injection')
      - type: CERT
        value: IDS00-J:Prevent SQL injection
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2011:Insecure Interaction - CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 89
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Injection [RST]
      - type: GB/T 34944-2017 Java
        value: 数据处理:SQL注入
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: true
    id: 0902000010160001
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。 \n**示例2**：  \n```java\npublic void doPrivilegedAction(String\
      \ username, char[] password) throws SQLException {\n    Connection connection\
      \ = getConnection();\n    try {\n        String pwd = hashPassword(password);\n\
      \        if (username.length() > 8) {\n            // ...  \n        }\n   \
      \     String sqlString = \"select * from db_user where username=? and password=?\"\
      ;\n        PreparedStatement stmt = connection.prepareStatement(sqlString);\n\
      \        stmt.setString(1, username);\n        stmt.setString(2, pwd);\n   \
      \     //...  \n    } finally {\n        try {\n            connection.close();\n\
      \        } catch (SQLException x) {\n            // ...  \n        }\n    }\n\
      }\n```  \n在这个规范的代码示例中，采用`java.sql.PreparedStatement`执行参数化查询，可以避免含有注入代码的SQL语句提交执行。通过使用`prepareStatement`类的`set*()`方法，可以进行强类型检查。程序会正确的转义双引号内的输入数据，以减少SQL注入漏洞。类似的，对数据的增删改操作也必须使用`prepareStatement`。"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.1 数据清洗
      - type: DetailClassChin
        value: A.2.1.1.2 预防SQL注入
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：SQL注入是一种数据库攻击手段。攻击者通过向应用程序提交恶意代码来改变原SQL语句的含义，进而执行任意SQL命令，达到入侵数据库乃至操作系统的目的。防止SQL注入的主要方法有：净化并验证非受信输入，采用参数化查询。\
      \  \n对于预防SQL注入的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\npublic void\
      \ doPrivilegedAction(String username, char[] password) throws SQLException {\n\
      \    Connection connection = getConnection();\n    //...  \n    try {\n    \
      \    String pwd = hashPassword(password);\n        String sqlString = \"SELECT\
      \ * FROM db_user WHERE username = '\" + username + \"' AND password = '\" +\
      \ pwd + \"'\";\n        Statement stmt = connection.createStatement();\n   \
      \     ResultSet rs = stmt.executeQuery(sqlString);\n        // ...  \n    }\
      \ finally {\n        try {\n            connection.close();\n        } catch\
      \ (SQLException x) {\n            // ...  \n        }\n    }\n}\n```  \n在这个不规范的代码示例中，使用JDBC来认证用户，提交`stmt.executeQuery(sqlString)`查询的SQL为`sqlString`，它是通过一个拼接字符串组合而来，并且`sqlString`未经过净化或验证，当`pwd`为`'\
      \ OR '1'='1` 时，`sqlString`就会变成： `SELECT * FROM db_user WHERE username='' AND\
      \ password='' OR '1'='1'。Statement.executeQuery(String)`方法不能防止SQL注入，该代码将会引发SQL注入漏洞攻击。 "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '564:SQL Injection: Hibernate'
      - type: CWE
        value: 89:Improper Neutralization of Special Elements used in an SQL Command
          ('SQL Injection')
      - type: CERT
        value: IDS00-J:Prevent SQL injection
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2011:Insecure Interaction - CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 89
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 89
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Injection [RST]
      - type: GB/T 34944-2017 Java
        value: 数据处理:SQL注入
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170001'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n```java\npublic void processFile(String\
      \ filePath) {\n    FileInputStream fis = null;\n    InputStreamReader isr =\
      \ null;\n    BufferedReader br = null;\n    try {\n        fis = new FileInputStream(filePath);\n\
      \        isr = new InputStreamReader(fis);\n        br = new BufferedReader(isr);\n\
      \        String line = \"\";\n        while ((line = br.readLine()) != null)\
      \ {\n            // processLine(line);\n        }\n    } catch (FileNotFoundException\
      \ e) {\n        // log(e);\n    } catch (IOException e) {\n        // log(e);\n\
      \    } finally {\n        if (br != null) {\n            try {\n           \
      \     br.close();\n            } catch (IOException e) {\n                //\
      \ log(e);\n            }\n        }\n        if (isr != null) {\n          \
      \  try {\n                isr.close();\n            } catch (IOException e)\
      \ {\n                // log(e);\n            }\n        }\n        if (fis !=\
      \ null) {\n            try {\n                fis.close();\n            } catch\
      \ (IOException e) {\n                // log(e);\n            }\n        }\n\
      \    }\n}\n```  \n以上代码片段中，在finally代码块中对流资源进行了合理的释放。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 7.1 资源管理
      - type: DetailClassChin
        value: A.4.1.2 需确保流得到释放
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：程序创建或分配流资源后，不进行合理释放，将会降低系统性能。攻击者可能会通过耗尽资源池的方式发起拒绝服务攻击。  \n\
      对于需确保流得到释放的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：   \n```java\npublic void processFile(String\
      \ filePath) {\n    try {\n        FileInputStream fis = new FileInputStream(filePath);\n\
      \        InputStreamReader isr = new InputStreamReader(fis);\n        BufferedReader\
      \ br = new BufferedReader(isr);\n        String line = \"\";\n        while\
      \ ((line = br.readLine()) != null) {\n            processLine(line);\n     \
      \   }\n    } catch (FileNotFoundException e) {\n        log(e);\n    } catch\
      \ (IOException e) {\n        log(e);\n    }\n}\n```  \n在上面Java方法中，创建I/O流对象后未进行合理释放，程序依靠Java虚拟机的垃圾回收机制释放I/O流资源，事实上，程序不能确定何时调用虚拟机的`finalize()`方法。在繁忙的程序环境下，可能导致Java虚拟机不能有效的使用I/O对象。程序不应依赖于`finalize()`回收流资源，应在finally代码块中手动释放流资源。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 775:Missing Release of File Descriptor or Handle after Effective Lifetime
      - type: CWE
        value: 772:Missing Release of Resource after Effective Lifetime
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 404:Improper Resource Shutdown or Release
      - type: CERT
        value: FIO04-J:Close resources when they are no longer needed
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 772
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 404
      - type: SJ/T 11683-2017
        value: 6.6.1 资源泄漏
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170060'
    language: Java
  - Advice: "使用有效的正确来设置参数值和获取列值。  \n  \n**例如**：下列代码中使用了正确的参数索引和列索引。  \n```java\nPreparedStatement\
      \ ps = connection.prepareStatement(\"select *  from users where name = ? and\
      \ age < ?\");  \nps.setString(1, name);  \nps.setInt(2, age);  \n  \nResultSet\
      \ rs = ps.executeQuery();  \nwhile (rs.next()) {  \n    long id = rs.getLong(1);\
      \  \n    // ...  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要使用无效的索引
    Categories_En:
      Category: []
    Default: true
    Description: "PreparedStatement为参数设值的方法和ResultSet中获取指定列的值的方法中的索引都是从1开始编号的，当使用0或者大于占位符数目或列数目时都会抛出SQLException异常。\
      \  \n  \n**例如**：下列代码中使用了无效的参数索引和列索引。  \n```java\nPreparedStatement ps = connection.prepareStatement(\"\
      select *  from users where name = ? and age < ?\");  \nps.setString(0, name);\
      \  //索引都是从1开始编号的  \nps.setInt(3, age);  //索引大小超过占位符总数  \n  \nResultSet rs =\
      \ ps.executeQuery();  \nwhile (rs.next()) {  \n    long id = rs.getLong(0);\
      \  //索引都是从1开始编号的  \n    // ...  \n}  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: correctness
    Type: std
    WhiteList: not
    id: 0202000010150429
    language: Java
  - Advice: "为了避免反射型XSS攻击，建议采用以下方式进行防御：  \n1.对用户的输入进行合理验证（如年龄只能是数字），对特殊字符（如`<`、`>`、`'`、`\"\
      `以及`<script>`、`javascript`等进行过滤。  \n2.根据数据将要置于HTML上下文中的不同位置（HTML标签、HTML属性、JavaScript脚本、CSS、URL），对所有不可信数据进行恰当的输出编码。\
      \    \n**例如**：采用OWASP ESAPI对数据输出HTML上下文中不同位置，编码方法如下。  \n  ```java\n//HTML encode\
      \  \nESAPI.encoder().encodeForHTML(\"inputData\");  \n//HTML attribute encode\
      \  \nESAPI.encoder().encodeForHTMLAttribute(\"inputData\");   \n//JavaScript\
      \ encode  \nESAPI.encoder().encodeForJavaScript(\"inputData\");  \n//CSS encode\
      \  \nESAPI.encoder().encodeForCSS(\"inputData\");  \n//URL encode  \nESAPI.encoder().encodeForURL(\"\
      inputData\");   \n```\n3.设置HttpOnly属性，避免攻击者利用跨站脚本漏洞进行Cookie劫持攻击。在Java EE中，给Cookie添加HttpOnly的代码如下：\
      \  \n```java\nresponse.setHeader(\"Set-Cookie\",\"cookiename=cookievalue; path=/;\
      \ Domain=domainvaule; Max-age=seconds; HttpOnly\");\n```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.1 数据清洗
      - type: DetailClassChin
        value: A.2.1.2.2 预防反射型XSS
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：应用程序通过Web请求获取不可信赖的数据，在未检验数据是否存在恶意代码的情况下，便将其传送给了Web用户，应用程序将易于受到反射型XSS攻击。\
      \  \n对于预防反射型XSS的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：下面JSP代码片段的功能是从HTTP请求中读取雇员的ID（eid），并将其显示给用户。\n\
      ```java\n<% String name= request.getParameter(\"username\");%>  \n姓名: <%= name%>\
      \  \n```   \n如果name里有包含恶意代码，那么Web浏览器就会像显示HTTP响应那样执行该代码，应用程序将受到反射型XSS攻击。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 83:Improper Neutralization of Script in Attributes in a Web Page
      - type: CWE
        value: 82:Improper Neutralization of Script in Attributes of IMG Tags in a
          Web Page
      - type: CWE
        value: 81:Improper Neutralization of Script in an Error Message Web Page
      - type: CWE
        value: 80:Improper Neutralization of Script-Related HTML Tags in a Web Page
          (Basic XSS)
      - type: CWE
        value: 79:Improper Neutralization of Input During Web Page Generation ('Cross-site
          Scripting')
      - type: OWASP TOP 10
        value: 2017:A7 Cross-Site Scripting (XSS)
      - type: OWASP TOP 10
        value: 2013:A3 Cross-Site Scripting (XSS)
      - type: OWASP TOP 10
        value: 2010:A2 Cross-Site Scripting (XSS)
      - type: OWASP TOP 10
        value: 2007:A1 Cross Site Scripting (XSS)
      - type: OWASP TOP 10
        value: 2004:A4 Cross Site Scripting
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 79
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 79
      - type: CWE/SANS TOP 25
        value: 2011:Insecure Interaction - CWE ID 79
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 79
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 79
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Cross-site Scripting [XYT]
      - type: GB/T 34944-2017 Java
        value: Web问题:跨站脚本
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: 0302000010170008
    language: Java
  - Advice: "不要使用浮点变量作为循环计数器。  \n**规范的代码样例1**：  \n```java\nfor (int count = 1; count\
      \ <= 10; count += 1) {  \n    float x = count/10.0f;  \n    System.out.println(x);\
      \  \n}\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution uses an integer\
      \ loop counter from which the desired floating-point value is derived:\n\n```java\n\
      for (int count = 1; count <= 10; count += 1) {\n  float x = count/10.0f;\n \
      \ System.out.println(x);\n}\n```"
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 03. 数据类型与运算 (NUM)
      - type: DetailClassChin
        value: NUM09-J：不要使用浮点数变量做为循环计数器
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 03. Numeric Types and Operations (NUM)
      - type: DetailClassChin
        value: NUM09-J:Do not use floating-point variables as loop counters
    Default: true
    Description: "有限精度的IEEE 754 浮点类型不能表达：  \n* 所有精确的分数。\n* 精确的小数，即使是用少量数字就可以表达的小数。\n\
      * 大数值的数字，在允许的精度内，对大浮点数值的增量可能并不会改变其实际的数值。\n在本规则中，循环计数器是一个归纳变量，用作比较表达式的操作数，该表达式用作do、while或For循环的控制表达式。归纳变量是一个变量，它在循环的每次迭代中以固定的数量增加或减少[Aho\
      \ 1986]。此外，对变量的更改必须直接发生在循环体中（而不是在循环中执行的函数中）\n此规则是NUM04-J的子集.如果需要精确计算，请不要使用浮点数\n\
      **不规范的代码样例1**：  \n```java\nfor (float x = 0.1f; x <= 1.0f; x += 0.1f) {  \n\
      \    System.out.println(x);  \n}\n```  \n因为0.1f会被处理成可以用float类型表达的最接近的数值，所以在每次虚拟化中，x的增加值是一个略大于0.1的值。从而，循环只会执行9次。\n\
      \n**不规范的代码样例2**：\n此不合规代码示例使用浮点循环计数器，该计数器的增量太小，无法在给定精度的情况下更改其值：\n```java\nfor\
      \ (float x = 100000001.0f; x <= 100000010.0f; x += 1.0f) {\n  /* ... */\n}\n\
      ```"
    Description_En: "Floating-point variables must not be used as loop counters. Limited-precision\
      \ IEEE 754 floating-point types cannot represent\n\n* All simple fractions exactly.\n\
      * All decimals precisely, even when the decimals can be represented in a small\
      \ number of digits.\n* All digits of large values, meaning that incrementing\
      \ a large floating-point value might not change that value within the available\
      \ precision.\n\nFor the purpose of this rule, a *loop counter* is an induction\
      \ variable that is used as an operand of a comparison expression that is used\
      \ as the controlling expression of a do, while or for loop. An *induction variable*\
      \ is a variable that gets increased or decreased by a fixed amount on every\
      \ iteration of a loop [Aho 1986]. Furthermore, the change to the variable must\
      \ occur directly in the loop body (rather than inside a function executed within\
      \ the loop.)\n\nThis rule is a subset of NUM04-J. Do not use floating-point\
      \ numbers if precise computation is required.\n\n**Noncompliant Code Example**\n\
      \nThis noncompliant code example uses a floating-point variable as a loop counter.\
      \ The decimal number 0.1 cannot be precisely represented as a `float` or even\
      \ as a `double`.\n\n```java\nfor (float x = 0.1f; x <= 1.0f; x += 0.1f) {\n\
      \  System.out.println(x);\n}\n```\n\nBecause `0.1f` is rounded to the nearest\
      \ value that can be represented in the value set of the `float` type, the actual\
      \ quantity added to `x` on each iteration is somewhat larger than `0.1`. Consequently,\
      \ the loop executes only nine times and typically fails to produce the expected\
      \ output.\n\n**Noncompliant Code Example**\n\nThis noncompliant code example\
      \ uses a floating-point loop counter that is incremented by an amount that is\
      \ typically too small to change its value given the precision:\n\n```java\n\
      for (float x = 100000001.0f; x <= 100000010.0f; x += 1.0f) {\n  /* ... */\n\
      }\n```\nThe code loops forever on execution."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140509
    language: Java
  - Advice: "使用整数作为循环计数器。  \n  \n**例如**：在下面的代码中，使用整数作为循环计数器，可以得到预期的结果。  \n```java\n\
      for(int count = 1; count <= 10; count += 1) {  \n    float x = count/10.0f;\
      \  \n    System.out.println(x);  \n}\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要使用浮点变量作为循环计数器
    Categories_En:
      Category: []
    Default: true
    Description: "不要使用浮点变量作为循环计数器。  \n  \n**例如**：在下面的代码中，0.1f会被处理成可以用float类型表达的最接近的数值，所以在每次循环中，增加至x的数值是一个略大于0.1的值。从而，循环只会执行9次，最终不能产生预期的结果。\
      \  \n```java\nfor (float x = 0.1f; x <= 1.0f; x +=0.1f) {  \n    System.out.println(x);\
      \  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - (NUM09-J)不要使用浮点变量作为循环计数器
    ENSubClassChin: 数值类型与运算(NUM)
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: NUM09-J:Do not use floating-point variables as loop counters
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150412'
    language: Java
  - Advice: "对应的符合规则的解决方案是：使用整数作为循环计数器，并由此得到期望的浮点数值。   \n    ```java\n    for (int\
      \ count = 1; count <= 10; count += 1) {   \n        float x = count/10.Of; \
      \  \n        System.out.println(x);   \n    }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.4.10 不要使用浮点变量作为循环计数器
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n绝对不能使用浮点变量作为循环计数器。   \n2) 原理   \n有限精度的 IEEE 754 浮点类型不能表达：\
      \   \n•  精确的分数。   \n•  精确的小数，即使是可以用少量数字就可以表达的小数。   \n•  大数值的数字。这意味着在允许的精度内，对大浮点数值的增量可能并不会改变其实际数值。\
      \   \n使用浮点变量作为循环计数器会导致意外的行为。   \n3) 示例   \n    如下不符合规则的代码示例使用一个浮点变量作为循环计数器。小数\
      \ 0.1 不能精确地用 float 甚至double 来表达。   \n    ```java\n    for (float x = 0.1f; x\
      \ <= 1.0f; x += 0.1f) {   \n        System.out.printIn(x);   \n    }\n    ```\
      \   \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: NUM09-J:Do not use floating-point variables as loop counters
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160033
    language: Java
  - Advice: "**规范的代码样例1**：  \nJava 7 的nio2包中的若干方法可以创建临时文件，该样例采用`Files.createTempFile()`方法创建了一个不可预测名称的临时文件，这个方法使用try的构造函数打开，它会自动关闭文件，而不管文件是否出现异常。最后，使用Java\
      \ 7 的DELETE_ON_CLOSE选项打开文件，这选项能让在关闭文件的时候文件被删除。  \n```java\nclass TempFile {\n\
      \    public static void main(String[] args) {\n        Path tempFile = null;\n\
      \        try {\n            tempFile = Files.createTempFile(\"tempnam\", \"\
      .tmp\");\n            try (BufferedWriter writer = Files.newBufferedWriter(tempFile,\
      \ Charset.forName(\"UTF8\"),\n                    StandardOpenOption.DELETE_ON_CLOSE))\
      \ {\n                // write to file\n            }\n            System.out.println(\"\
      Temporary file write done, file erased\");\n        } catch (FileAlreadyExistsException\
      \ x) {\n            System.err.println(\"File exists: \" + tempFile);\n    \
      \    } catch (IOException x) {\n            // Some other sort of failure, such\
      \ as permissions.\n            System.err.println(\"Error creating temporary\
      \ file: \" + x);\n        }\n    }\n}\n```"
    Advice_En: "**Compliant Solution (`DELETE_ON_CLOSE`)**\nThis compliant solution\
      \ creates a temporary file using several methods from Java's NIO.2 package (introduced\
      \ in Java SE 7).It uses the `createTempFile()` method, which creates an unpredictable\
      \ name. (The actual method by which the name is created is implementation-defined\
      \ and undocumented.) The file is opened using the *`try`-with-resources* construct,\
      \ which automatically closes the file regardless of whether an exception occurs.\
      \ Finally, the file is opened with the `DELETE_ON_CLOSE` option, which removes\
      \ the file automatically when it is closed.\n```java\nclass TempFile {\n  public\
      \ static void main(String[] args) {\n    Path tempFile = null;\n    try {\n\
      \      tempFile = Files.createTempFile(\"tempnam\", \".tmp\");\n      try (BufferedWriter\
      \ writer =\n          Files.newBufferedWriter(tempFile, Charset.forName(\"UTF8\"\
      ),\n                                  StandardOpenOption.DELETE_ON_CLOSE)) {\n\
      \        // Write to file\n      }\n      System.out.println(\"Temporary file\
      \ write done, file erased\");\n    } catch (FileAlreadyExistsException x) {\n\
      \      System.err.println(\"File exists: \" + tempFile);\n    } catch (IOException\
      \ x) {\n      // Some other sort of failure, such as permissions.\n      System.err.println(\"\
      Error creating temporary file: \" + x);\n    }\n  }\n}\n```"
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO03-J：在终止前移除临时文件
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO03-J:Remove temporary files before termination
    Default: true
    Description: "临时文件可以用于：  \n1）在进程间共享数据。  \n2）存储附带的程序数据。  \n3）动态创建并且/或者装载类、jar文件以及原生库类。\
      \  \n开发人员通常在任何人都可以进行操作的目录中创建临时文件。如：POSIX中的`/tmp`、`/var/tmp`、Windows中的`c:\\temp`，这些目录中的文件一般会被定期的清理。然而，当这些目录中的文件被不安全的创建或者在使用后仍然保持着可访问状态的时候，可访问本地文件系统的攻击者可以操作共享目录中的文件。\
      \  \n创建临时文件通常需要赋予其独特和不可预测的文件名，不仅如此，还需要在不需要它的时候删除它。每一个程序会负责保证在正常操作的情况下删除临时文件，但却没有万无一失的办法可以保证在异常情况下删除孤立文件，甚至采用finally块也不行。基于这个原因，很多系统使用的临时文件删除工具来清除临时目录并删除旧文件。\
      \  \n风险评估：违反该条准则可能会导致信息泄露和资源耗尽。      \n**不规范的代码样例1**：  \n```java\nclass TempFile\
      \ {\n    public static void main(String[] args) throws IOException {\n     \
      \   File f = File.createTempFile(\"tempnam\", \".tmp\");\n        FileOutputStream\
      \ fop = null;\n        try {\n            fop = new FileOutputStream(f);\n \
      \           String str = \"Data\";\n            fop.write(str.getBytes());\n\
      \            fop.flush();\n        } finally {\n            // Stream/file still\
      \ open; file will\n            // not be deleted on Windows systems\n      \
      \      // Delete the file when the JVM terminates\n            f.deleteOnExit();\n\
      \            if (fop != null) {\n                try {\n                } catch\
      \ (IOException x) {// handle error\n                }\n            }\n     \
      \   }\n    }\n}\n```  \n代码使用了`File.creatTempFile()`生成一个具有独特名称的临时文件，并且使用了`File.deleteOnExit()`方法保证了临时文件在JVM终止时删除。然而，根据Java\
      \ API对File类的描述：仅在JVM正常终止的时候才进行删除操作。如果JVM不正常终止，文件不能删除。"
    Description_En: "Temporary files can be used to\n\n* Share data between processes.\n\
      * Store auxiliary program data (for example, to preserve memory).\n* Construct\
      \ and/or load classes, JAR files, and native libraries dynamically.\nTemporary\
      \ files are files and consequently must conform to the requirements specified\
      \ by other rules governing operations on files, including FIO00-J. Do not operate\
      \ on files in shared directories and FIO01-J. Create files with appropriate\
      \ access permissions. Temporary files have the additional requirement that they\
      \ must be removed before program termination.\nRemoving temporary files when\
      \ they are no longer required allows file names and other resources (such as\
      \ secondary storage) to be recycled. Each program is responsible for ensuring\
      \ that temporary files are removed during normal operation. There is no surefire\
      \ method that can guarantee the removal of orphaned files in the case of abnormal\
      \ termination, even in the presence of a `finally` block, because the `finally`\
      \ block may fail to execute. For this reason, many systems employ temporary\
      \ file cleaner utilities to sweep temporary directories and remove old files.\
      \ Such utilities can be invoked manually by a system administrator or can be\
      \ periodically invoked by a system process. However, these utilities are themselves\
      \ frequently vulnerable to file-based exploits.\n**Noncompliant Code Example\
      \ (`createTempFile()`, `deleteOnExit()`)**\n\nThis noncompliant code example\
      \ invokes the `File.createTempFile()` method, which generates a unique temporary\
      \ file name based on two parameters: a prefix and an extension. This is the\
      \ only method from Java 6 and earlier that is designed to produce unique file\
      \ names, although the names produced can be easily predicted. A random number\
      \ generator can be used to produce the prefix if a random file name is required.\n\
      This example also uses the `deleteOnExit()` method to ensure that the temporary\
      \ file is deleted when the JVM terminates. However, according to the Java API\
      \ [API 2014] Class File, method `deleteOnExit()` documentation,\n\n> Deletion\
      \ will be attempted only for normal termination of the virtual machine, as defined\
      \ by the *Java Language Specification*. Once deletion has been requested, it\
      \ is not possible to cancel the request. This method should therefore be used\
      \ with care.\n> Note: this method should not be used for file-locking, as the\
      \ resulting protocol cannot be made to work reliably.\n\nConsequently, the file\
      \ is not deleted if the JVM terminates unexpectedly.A longstanding bug on Windows-based\
      \ systems, reported as Bug ID: 4171239 [SDN 2008],causes JVMs to fail to delete\
      \ a file when `deleteOnExit()` is invoked before the associated stream or `RandomAccessFile`\
      \ is closed.\n```java\nclass TempFile {\n  public static void main(String[]\
      \ args) throws IOException{\n    File f = File.createTempFile(\"tempnam\",\"\
      .tmp\");\n    FileOutputStream fop = null;\n    try {\n      fop = new FileOutputStream(f);\n\
      \      String str = \"Data\";\n      fop.write(str.getBytes());\n      fop.flush();\n\
      \    } finally {\n      // Stream/file still open; file will\n      // not be\
      \ deleted on Windows systems\n      f.deleteOnExit(); // Delete the file when\
      \ the JVM terminates\n\n      if (fop != null) {\n        try {\n          fop.close();\n\
      \        } catch (IOException x) {\n          // Handle error\n        }\n \
      \     }\n    }\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 377:Insecure Temporary File
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141403'
    language: Java
  - Advice: "对应的符合规则的解决方案是：使用 Java SE 7 的 NIO2 包中的若干方法创建临时文件。它使用   `createTempFile()`方法创建一个不可预测的名字。\
      \   \n    ```java\n        class TempFile {\n            public static void\
      \ main(String[] args) {\n                Path tempFile = null;\n           \
      \     try {\n                    tempFile = Files.createTempFile(\"tempnam\"\
      , \".tmp\");\n                    try (BufferedWriter writer = Files.newBufferedWriter(tempFile,\
      \ Charset.forName(\"UTF8\"),\n                            StandardOpenOption.DELETE_ON_CLOSE))\
      \ {\n                        // write to file\n                    }\n     \
      \               System.out.println(\"Temporary file write done, file erased\"\
      );\n                } catch (FileAlreadyExistsException x) {\n             \
      \       System.err.println(\"File exists: \" + tempFile);\n                }\
      \ catch (IOException x) {\n                    // Some other sort of failure,\
      \ such as permissions.\n                    System.err.println(\"Error creating\
      \ temporary file: \" + x);\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 7.5 文件系统
      - type: DetailClassChin
        value: C.13.4 在终止前移除临时文件
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n确保临时文件在终止前被移除。   \n2) 原理   \n开发人员通常在任何人都可以进行写操作的目录中创建临时文件。例如，POSIX\
      \ 中的`/tap`、`/var/tmp`、Windows 中的 `C:\\TEMP`。这些目录中的文件会被定期地清理，例如在每天晚上或者每次重启动时。然而，当那些文件被不安全地创建或者在使用后仍然保持可访问状态的时候，可访问本地文件系统的攻击者可以操作共享目录中的文件。\
      \   \n3) 示例   \n    以下这段代码示例认为在安全目录中创建的文件是符合规则要求的，并且使用了正确的访问权限来进行创建，符合规则的要求。这两个要求是在\
      \ JVM 之外进行管理的。 这个不符合规则的代码示例没有在结束时删除文件。   \n    ```java\n    class TempFile {\n\
      \        public static void main(String[] args) throws IOException {\n     \
      \       File f = new File(\"tempnam.tmp\");\n            if (f.exists()) {\n\
      \                System.out.printIn(\"This file already exists\");\n       \
      \         return;\n            }\n    \n            FileOutputStream fop = null;\n\
      \            try {\n                fop = new FileOutputStream(f);\n       \
      \         String str = \"Data\";\n                Fop.write(str.getBytes());\n\
      \            } finally {\n                if (fop != null) {\n             \
      \       try {\n                        fop.close();\n                    } catch\
      \ (IOException x) {\n                        // handle error\n             \
      \       }\n                }\n            }\n        }\n    }\n    ```   \n\
      \       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference:
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 377:Insecure Temporary File
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160109
    language: Java
  - Advice: "程序对字符串进行比较，应采用`equals()`方法，而不是`==`或`!=`方法。  \n  \n**例如**：下列代码中使用`equals()`方法进行字符串的对比。\
      \  \n```java\n...  \nString username = (String)request.getAttribute(\"usernamae\"\
      );  \nif(username.equals(\"admin\")){  \n    dosomething();  \n}  \n...\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要使用==或!=比较字符串
    Categories_En:
      Category: []
    Default: true
    Description: "程序中使用==或!=比较两个字符串是否相等，其实比较的是两个对象在内存中的地址值，而不是字符串的值。  \n  \n**例如**：下列代码中将`request`中获取的参数值与`\"\
      admin\"`用`==`做比较，`dosomething()`方法将永远不会被执行。\n```java\n...  \nString username\
      \ = (String)request.getAttribute(\"usernamae\");  \nif(username == \"admin\"\
      ){  \n    dosomething();  \n}  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Erroneous String Compare
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 597:Use of Wrong Operator in String Comparison
      - type: SJ/T 11683-2017
        value: 6.6.11 使用"=="运算符比较字符串
    Security: not
    Tag: correctness,bad_practice
    Type: std
    WhiteList: not
    id: 0202000010150408
    language: Java
  - Advice: "在安全性要求较高的系统中，应采用散列值>=224比特的SHA系列算法（如SHA-224、SHA-256、SHA-384和SHA-512）来保证敏感数据的完整性。\
      \  \n  \n**例如**：下面代码片段中，使用SHA-256算法取代MD5算法保证数据完整性。  \n```java\n...   \nstrSrc\
      \ = upsw+salt;   \nbyte[] bt = strSrc.getBytes();  \ntry {  \n    md = MessageDigest.getInstance(\"\
      SHA-256\");  \n    md.update(bt);  \n    strDes = bytes2Hex(md.digest()); //\
      \ to HexString  \n} catch ( NoSuchAlgorithmException e ) {  \n...  \n}  \n...\n\
      ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 密码规范
      - type: DetailClassChin
        value: 【推荐】不要使用不安全的哈希算法
    Categories_En:
      Category: []
    Default: true
    Description: "在安全性要求较高的系统中，不应使用被业界公认的不安全的哈希算法（如MD2、MD4、MD5、SHA、SHA1等)来保证数据的完整性。\
      \  \n  \n**例如**：下面代码片段中，采用MD5算法来保证数据的完整性。  \n```java\n...   \nbyte[] b = str.getBytes();\
      \   \nMessageDigest md = null;   \ntry {   \n    md = MessageDigest.getInstance(\
      \ \"MD5\" );   \n    md.update( b );   \n}catch ( NoSuchAlgorithmException e\
      \ ) {  \n...  \n}  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: Weak Cryptographic Hash
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 328:Reversible One-Way Hash
      - type: CWE
        value: 327:Use of a Broken or Risky Cryptographic Algorithm
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: GB/T 34944-2017 Java
        value: 安全功能:可逆的散列算法
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
    Security: true
    Tag: security,password
    Type: std
    WhiteList: not
    id: '0202000010150201'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\n...   \nbyte[] b = str.getBytes();\
      \  \nMessageDigest md = null;  \ntry {  \n    md = MessageDigest.getInstance(\"\
      SHA-256\");  \n    md.update(b);  \n    ...  \n} catch (NoSuchAlgorithmException\
      \ e) {   \n...\n```  \n在安全性要求较高的系统中，应采用散列值>=224比特的SHA系列算法（如SHA-224、SHA-256、SHA-384和SHA-512）来保证敏感数据的完整性。以上代码片段中，使用SHA-256算法取代MD5算法保证数据完整性。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.2 数据加密与保护
      - type: DetailClassChin
        value: A.2.2.1.3 避免使用不安全的哈希算法
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：在安全性要求较高的系统中，不应使用被业界公认不安全的哈希算法（如MD2、MD4、MD5、SHA、SHA1等)来保证数据的完整性。\
      \  \n对于避免使用不安全的哈希算法的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\n...  \
      \ \nbyte[] b = str.getBytes();   \nMessageDigest md = null;   \ntry {   \n \
      \   md = MessageDigest.getInstance(\"MD5\");   \n    md.update(b);   \n    ...\
      \  \n}catch (NoSuchAlgorithmException e){  \n    ...  \n}  \n...\n```  \n以上代码片段中，采用MD5算法来保证数据的完整性。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 328:Reversible One-Way Hash
      - type: CWE
        value: 327:Use of a Broken or Risky Cryptographic Algorithm
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: GB/T 34944-2017 Java
        value: 安全功能:可逆的散列算法
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170012'
    language: Java
  - Advice: "当类实现了Externalizable接口，添加无参构造方法时。  \n  \n**例如**：下面代码示例中，类实现了Externalizable接口，定义了无参构造方法。\
      \  \n```java\npublic class User implements Externalizable {  \n    private String\
      \ name;  \n    private int age;  \n    ...  \n    User (){}  \n    User (String\
      \ name,int age) {  \n        this.name = name;  \n        this.age = age;  \n\
      \        ...  \n    }  \n    @Override  \n    public void writeExternal(ObjectOutput\
      \ out) throws IOException {  \n        ...  \n    }  \n  \n    @Override  \n\
      \    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException\
      \ {  \n        ...  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】实现Externalizable的类必须存在无参构造方法
    Categories_En:
      Category: []
    Default: true
    Description: "当类实现了Externalizable接口，但是没有定义无参构造方法时，当此类对象被反序列化时，首先需要通过调用无参构造函数来构造。当这个类没有时，会抛出InvalidClassException并失败。\
      \  \n  \n**例如**：下面代码示例中，类实现了Externalizable接口，没有定义无参构造方法。  \n```java\npublic\
      \ class User implements Externalizable {  \n    private String name;  \n   \
      \ private int age;  \n    ...  \n    User (String name,int age) {  \n      \
      \  this.name = name;  \n        this.age = age;  \n        ...  \n    }  \n\
      \    @Override  \n    public void writeExternal(ObjectOutput out) throws IOException\
      \ {  \n        ...  \n    }  \n  \n    @Override  \n    public void readExternal(ObjectInput\
      \ in) throws IOException, ClassNotFoundException {  \n        ...  \n    } \
      \ \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: 0202000010150449
    language: Java
  - Advice: "程序不能捕获`java.lang.NullPointerException`或者任何它的基类。如果捕获了，这通常说明程序中存在错误。\n\
      **规范的代码样例1**：  \n明确的对参数进行null检查，而不是捕获NullPointerException。\n```java  \nboolean\
      \ isName(String s) {\n    if (s == null) {\n        return false;\n    }\n \
      \   String names[] = s.split(\" \");\n    if (names.length != 2) {\n       \
      \ return false;\n    }\n    return (isCapitalized(names[0]) && isCapitalized(names[1]));\n\
      }\n```  \n**符合规范的使用样例2**：  \n异常封装机制。  \n```java\nclass DoSomethingException\
      \ extends Exception {\n  public DoSomethingException(Throwable cause) {\n  \
      \  super(cause);\n  }\n\n  // Other methods\n\n};\n\nprivate void doSomething()\
      \ throws DoSomethingException {\n  try {\n    // Code that might throw an Exception\n\
      \  } catch (Throwable t) {\n    throw new DoSomethingException(t);\n  }\n}\n\
      ```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution explicitly checks\
      \ the `String` argument for `null` rather than catching `NullPointerException`:\n\
      \n```java\nboolean isName(String s) {\n  if (s == null) {\n    return false;\n\
      \  }\n  String names[] = s.split(\" \");\n  if (names.length != 2) {\n    return\
      \ false;\n  }\n  return (isCapitalized(names[0]) && isCapitalized(names[1]));\n\
      }\n```\n**Compliant Solution**\nThis code sample catches all exceptions and\
      \ wraps them in a custom `DoSomethingException` before rethrowing them:\n```java\n\
      class DoSomethingException extends Exception {\n  public DoSomethingException(Throwable\
      \ cause) {\n    super(cause);\n  }\n\n  // Other methods\n\n};\n\nprivate void\
      \ doSomething() throws DoSomethingException {\n  try {\n    // Code that might\
      \ throw an Exception\n  } catch (Throwable t) {\n    throw new DoSomethingException(t);\n\
      \  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 07. 异常行为 (ERR)
      - type: DetailClassChin
        value: ERR08-J：不要捕获NullPointerException或者任何它的基类
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 07. Exceptional Behavior (ERR)
      - type: DetailClassChin
        value: ERR08-J:Do not catch NullPointerException or any of its ancestors
    Default: true
    Description: "一个运行期间抛出的NullPointerException异常表明存在一个对空引用的解引用，这是必须在应用代码中解决的。  \n\
      1）当程序中有多个语句都有可能抛出NullPointerException时，判断抛出异常的语句是困难甚至不可能的，这是因为catch语句会去处理任何位置抛出的NullPointerException。\
      \  \n2）程序抛出NullPointerException后很少会处于一个正常的可用状态。在捕获和记录（更糟糕的是去抑制）第一次异常后继续尝试去执行程序很少能够成功。\
      \  \n类似，程序不能捕获RuntimeException、Exception、Throwable。如果有捕获这些异常的话，很少有方法能够处理这些潜在的运行异常。当方法捕获了上述异常，它可能是程序设计外的异常，很多时候，运行时异常都是程序中应该改正的错误。\
      \  \n风险评估：捕获NullPointerException可能掩盖潜在的空引用，降低应用性能，并造成难以理解和维护的代码。  \n**不规范的代码样例1**：\
      \  \n```java\nboolean isName(String s) {\n    try {\n        String names[]\
      \ = s.split(\" \");\n        if (names.length != 2) {\n            return false;\n\
      \        }\n        return (isCapitalized(names[0]) && isCapitalized(names[1]));\n\
      \    } catch (NullPointerException e) {\n        return false;\n    }\n}\n```\
      \  \n  \n**例外**：  \n1） 当处理任务的线程(线程池中的工作线程)或Swing事件分配线程（Runable接口的抽象调用代码时），它们是允许捕获RuntimeException。\
      \  \n2） 需要大量容错能力或要求降低性能运行的系统是允许在适当的抽象层捕获并记录类似Throwable的普通异常的。  \n3） 异常封装是一个用于安全的处理未知异常的技巧。此时，一个catch程序捕获所有的异常，并将它们封装在一个自定义的异常类中。"
    Description_En: "Programs must not catch `java.lang.NullPointerException`. A `NullPointerException`\
      \ exception thrown at runtime indicates the existence of an underlying `null`\
      \ pointer dereference that must be fixed in the application code (see EXP01-J.\
      \ Do not use a null in a case where an object is required for more information).\
      \ Handling the underlying null pointer dereference by catching the `NullPointerException`\
      \ rather than fixing the underlying problem is inappropriate for several reasons.\
      \ First, catching `NullPointerException` adds significantly more performance\
      \ overhead than simply adding the necessary null checks [Bloch 2008]. Second,\
      \ when multiple expressions in a `try` block are capable of throwing a `NullPointerException`,\
      \ it is difficult or impossible to determine which expression is responsible\
      \ for the exception because the `NullPointerException` `catch` block handles\
      \ any `NullPointerException` thrown from any location in the `try` block. Third,\
      \ programs rarely remain in an expected and usable state after a `NullPointerException`\
      \ has been thrown. Attempts to continue execution after first catching and logging\
      \ (or worse, suppressing) the exception rarely succeed.\n\nLikewise, programs\
      \ must not catch `RuntimeException`, `Exception`, or `Throwable`. Few, if any,\
      \ methods are capable of handling all possible runtime exceptions. When a method\
      \ catches `RuntimeException`, it may receive exceptions unanticipated by the\
      \ designer, including `NullPointerException` and `ArrayIndexOutOfBoundsException`.\
      \ Many `catch` clauses simply log or ignore the enclosed exceptional condition\
      \ and attempt to resume normal execution; this practice often violates ERR00-J.\
      \ Do not suppress or ignore checked exceptions. Runtime exceptions often indicate\
      \ bugs in the program that should be fixed by the developer and often cause\
      \ control flow vulnerabilities.\n\n**Noncompliant Code Example (`NullPointerException`)**\n\
      \nThis noncompliant code example defines an `isName()` method that takes a `String`\
      \ argument and returns true if the given string is a valid name. A valid name\
      \ is defined as two capitalized words separated by one or more spaces. Rather\
      \ than checking to see whether the given string is null, the method catches\
      \ `NullPointerException` and returns false.\n\n```java\nboolean isName(String\
      \ s) {\n  try {\n    String names[] = s.split(\" \");\n\n    if (names.length\
      \ != 2) {\n      return false;\n    }\n    return (isCapitalized(names[0]) &&\
      \ isCapitalized(names[1]));\n  } catch (NullPointerException e) {\n    return\
      \ false;\n  }\n}\n```\n**Exceptions**\n\nERR08-J-EX1: Task processing threads\
      \ such as worker threads in a thread pool or the Swing event dispatch thread\
      \ are permitted to catch `RuntimeException` when they call untrusted code through\
      \ an abstraction such as the `Runnable` interface [Goetz 2006, p. 161].\n\n\
      ERR08-J-EX2: Systems that require substantial fault tolerance or graceful degradation\
      \ are permitted to catch and log general exceptions such as `Throwable` at appropriate\
      \ levels of abstraction.\n\nException wrapping is a common technique to safely\
      \ handle unknown exceptions. For another example, see ERR06-J. Do not throw\
      \ undeclared checked exceptions."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 395:Use of NullPointerException Catch to Detect NULL Pointer Dereference
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.3 捕捉运行时异常
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140808
    language: Java
  - Advice: 检查程序逻辑，删除捕获NullPointerException的代码。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 异常规范
      - type: DetailClassChin
        value: 【推荐】不要捕获NullPointerException
    Categories_En:
      Category: []
    Default: true
    Description: "应用程序中不应该捕获NullPointerException或者任何它的基类。因为捕获NullPointerException只可能掩盖潜在的空引用，降低应用性能，并造成难以理解和维护的代码。\
      \  \n  \n**例如**：如下代码对空指针异常进行了捕获  \n```java\nString name=null;  \ntry {  \n \
      \   name=request.getParameter(\"name\");  \n    if(name.equals(\"admin\")){\
      \  \n        ...  \n    }  \n}catch(NullPointerException e) { //程序捕获NullPointerException\
      \  \n    ...  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Program Catches NullPointerException
    ENSubClassChin: Poor Error Handling
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 395:Use of NullPointerException Catch to Detect NULL Pointer Dereference
      - type: CERT
        value: ERR08-J:Do not catch NullPointerException or any of its ancestors
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.3 捕捉运行时异常
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: not
    Tag: exception,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150501'
    language: Java
  - Advice: "对应的符合规则的解决方案是：明确检测  String 参数是否为空引用，而不是捕捉NullPointerException。\n    ```java\
      \    \n    boolean isName(String s) {\n        if (s == null) {\n          \
      \  return false;\n        }\n        String names[] = s.split(\"\");\n     \
      \   if (names.length != 2) {\n            return false;\n        }\n       \
      \ return (isCapitalized(names[0]) && isCapitalized(names[1]));\n    }\n    ```\
      \   \n      \n    **例 2**如下不符合规则的代码示例尝试通过捕捉 ArithmeticException 来解决问题。然而，代码仍然捕捉\
      \ Exception，从而同时捕捉了预期外的可检査异常和运行期异常。   \n    ```java\n    try (   \n        division\
      \ (200, 5);   \n        division(200, 0); // Divide by zero    \n    } catch\
      \ (ArithmeticException ae) {    \n        throw new DivideByZeroException();\
      \   \n    } catch (Exception e) {   \n        System.out.println(\"Exception\
      \ occurred :\" + e.getMessage());   \n    }\n    ```   \n       \n    对应的符合规则的解决方案是：只捕捉具体的预期内的异常(ArithmeticException和\
      \ IOException）, 并允许其他异常在调用栈中向上传递。   \n    ```java\n    import java.io.IOException;\n\
      \    \n    public class DivideException {\n        public static void main(String[]\
      \ args) {    \n        try {   \n          division(200, 5);   \n          division(200,\
      \ 0); // Divide by zero    \n        } catch (ArithmeticException ae) {   \n\
      \          // DivideByZeroException extends Exception so is checked    \n  \
      \        throw new DivideByZeroException();   \n        } catch (IOException\
      \ ex) {   \n          ExceptionReporter.report(ex);   \n          }\n    \n\
      \        public static void division(int totalSum,int totalNumber) throws ArithmeticException,\
      \ IOException {\n            int average = totalSum / totalNumber;\n       \
      \     // Additional operations that may throw IOException...\n            System.out.println(\"\
      Average: \" + average);\n        }\n    }\n    ```\n   \n4) 特例   \n**特例 1**：—个\
      \ catch 程序段是可以捕捉所有的异常并进行处理的，随后会再重新抛出。例如，在调用栈离开可信任区域前过滤异常中的敏感信息。   \n**特例 2**：当处理任务的线程如线程池中的工作线程或\
      \ Swing 事件分派线程通过如 Runable 接口的抽象调用不可信任的代码时，它们是允许捕捉 RuntimeException 的。    \n\
      **特例 3**：需要大量容错能力或要求降低性能运行的系统是允许在适当的抽象层捕捉并 记录类似于Throwable 的普通异常的。"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.4 错误处理安全
      - type: DetailClassChin
        value: C.7.9 不要捕捉NullPointerException或任何它的基类
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序不能捕捉 java.lang.NullPointerException 以及任何它的基类。   \n2)\
      \ 原理   \n一个运行期间抛出的 NullPointerException 异常表明存在一个对空引用的解引用，这是必须在应用代码中解决的。通过捕捉\
      \ NullPointerException 来处理这样的问题，而不对根本的原因进行处理是不合适的。   \n类似，程序不能捕捉 RuntimeException、Exception\
      \ 或 Throwable。   \n3) 示例   \n    **例 1** 如下不符合规则的代码示例定义了`isName()`方法，这个方法接受一个\
      \ String 参数并在字符串是有效名字时返回 true。一个有效名字是由空格隔开的两个大写开头的词组成的。这个方法捕捉了NullPointerException\
      \ 并返回 false,而不是去检査给出的字符串是否是空引用。   \n    ```java\n    boolean isName(String s)\
      \ {\n        try {\n            String names[] = s.split(\"\");\n    \n    \
      \        if (names.length != 2) {\n                return false;\n         \
      \   }\n            return (isCapitalized(names[0]) && isCapitalized(names[1]));\n\
      \        } catch (NullPointerException e) {\n            return false;\n   \
      \     }\n    }\n    ```   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 395:Use of NullPointerException Catch to Detect NULL Pointer Dereference
      - type: CERT
        value: ERR08-J:Do not catch NullPointerException or any of its ancestors
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.3 捕捉运行时异常
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160071
    language: Java
  - Advice: "使用URI替换URL进行处理。  \n  \n**例如**：下列代码中,在Set中存放URI。  \n```java\nSet<URI>\
      \ map = new HashSet<>();  \nmap.add(uri);\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要在Map和Set中保存URL
    Categories_En:
      Category: []
    Default: true
    Description: "当Map和Set中保存URL时，会默认调用URL的`equals`和`hashCode`方法。而URL的`equals`和`hashCode`方法都是阻塞操作，URL的`equals`和`hashCode`方法都会执行域名解析，会导致很大的性能损失。\
      \  \n  \n**例如**：下列代码中，在Set中存放URL。 \n```java\nSet<URL> set= new HashSet<>();\
      \  \nmap.add(url);\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: performance,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150453'
    language: Java
  - Advice: "正确定义`serialPersistentFields`的修饰符，将其声明为private static final。  \n  \n**例如**：如下所示代码中将`serialPersistentFields`声明为private\
      \ static final\n```java  \nprivate static final ObjectStreamField[] serialPersistentFields\
      \ =      \n{    \n    new ObjectStreamField(\"name\", String.class),     \n\
      \    new ObjectStreamField(\"a\", Integer.TYPE)     \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要使用不正确的serialPersistentFields修饰符
    Categories_En:
      Category: []
    Default: true
    Description: "Java序列化规范指出：正确的使用`serialPersistentFields`，必须将其声明为private static\
      \ final。  \n  \n**例如**：如下所示代码中将`serialPersistentFields`声明为public \n```java \n\
      public ObjectStreamField[] serialPersistentFields =      \n{    \n    new ObjectStreamField(\"\
      name\", String.class),     \n    new ObjectStreamField(\"a\", Integer.TYPE)\
      \     \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Incorrect serialPersistentFields Modifier
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 485:Insufficient Encapsulation
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150433'
    language: Java
  - Advice: "**规范的代码样例1**：\n在`main()`线程初始化所有的成员，而不是从静态的初始化方法中生成一个后台线程。  \n```java\n\
      public final class ConnectionFactory {\n    private static Connection dbConnection;\n\
      \    // Other fields . . .\n    static {\n        // Initialize a database connection\n\
      \        try {\n            dbConnection = DriverManager.getConnection(\"connection\
      \ string\");\n        } catch (SQLException e) {\n            dbConnection =\
      \ null;\n        }\n        // Other initialization (do not start any threads)\n\
      \    }\n    // . . .\n}\n```\n**例外情况**\nTSM02-J-EX0：允许程序在类初始化期间启动后台线程，前提是线程不能访问任何字段。\
      \ 例如，下面的 ObjectPreserver 类（基于 [Grand 2002]）提供了一种存储对象引用的机制，它可以防止对象被垃圾回收，即使该对象不再被取消引用。\n\
      ```java\npublic final class ObjectPreserver implements Runnable {\n  private\
      \ static final ObjectPreserver lifeLine = new ObjectPreserver();\n\n  private\
      \ ObjectPreserver() {\n    Thread thread = new Thread(this);\n    thread.setDaemon(true);\n\
      \    thread.start(); // Keep this object alive\n  }\n\n  // Neither this class\
      \ nor HashMap will be garbage-collected.\n  // References from HashMap to other\
      \ objects\n  // will also exhibit this property\n  private static final ConcurrentHashMap<Integer,Object>\
      \ protectedMap\n      = new ConcurrentHashMap<Integer,Object>();\n\n  public\
      \ synchronized void run() {\n    try {\n      wait();\n    } catch (InterruptedException\
      \ e) {\n      Thread.currentThread().interrupt(); // Reset interrupted status\n\
      \    }\n  }\n\n  // Objects passed to this method will be preserved until\n\
      \  // the unpreserveObject() method is called\n  public static void preserveObject(Object\
      \ obj) {\n    protectedMap.put(0, obj);\n  }\n\n  // Returns the same instance\
      \ every time\n  public static Object getObject() {\n    return protectedMap.get(0);\n\
      \  }\n\n  // Unprotect the objects so that they can be garbage-collected\n \
      \ public static void unpreserveObject() {\n    protectedMap.remove(0);\n  }\n\
      }\n```\n这是一个单例类（有关如何防御性地编写单例类的更多信息，请参阅 MSC07-J.防止单例对象的多个实例化）。 初始化涉及使用类的当前实例创建后台线程。\
      \ 线程通过调用 `Object.wait()`无限期地等待。 因此，该对象会在 Java 虚拟机 (JVM) 的剩余生命周期内持续存在。 因为对象是由守护线程管理的，所以线程不会干扰JVM的正常关闭。\n\
      尽管初始化涉及后台线程，但该线程既不会访问字段，也不会产生任何活性或安全问题。 因此，此代码是此规则的一个安全且有用的例外。"
    Advice_En: "**Compliant Solution (Static Initializer, No Background Threads)**\n\
      \nThis compliant solution initializes all fields on the main thread rather than\
      \ spawning background threads from the static initializer.\n\n```java\npublic\
      \ final class ConnectionFactory {\n  private static Connection dbConnection;\n\
      \  // Other fields ...\n\n  static {\n    // Initialize a database connection\n\
      \    try {\n      dbConnection = DriverManager.getConnection(\"connection string\"\
      );\n    } catch (SQLException e) {\n      dbConnection = null;\n    }\n    //\
      \ Other initialization (do not start any threads)\n  }\n\n  // ...\n}\n```\n\
      \n**Exceptions**\nTSM02-J-EX0: Programs are permitted to start a background\
      \ thread (or threads) during class initialization, provided the thread cannot\
      \ access any fields. For example, the following ObjectPreserver class (based\
      \ on [Grand 2002]) provides a mechanism for storing object references, which\
      \ prevents an object from being garbage-collected even when the object is never\
      \ again dereferenced.\n```java\npublic final class ObjectPreserver implements\
      \ Runnable {\n  private static final ObjectPreserver lifeLine = new ObjectPreserver();\n\
      \n  private ObjectPreserver() {\n    Thread thread = new Thread(this);\n   \
      \ thread.setDaemon(true);\n    thread.start(); // Keep this object alive\n \
      \ }\n\n  // Neither this class nor HashMap will be garbage-collected.\n  //\
      \ References from HashMap to other objects\n  // will also exhibit this property\n\
      \  private static final ConcurrentHashMap<Integer,Object> protectedMap\n   \
      \   = new ConcurrentHashMap<Integer,Object>();\n\n  public synchronized void\
      \ run() {\n    try {\n      wait();\n    } catch (InterruptedException e) {\n\
      \      Thread.currentThread().interrupt(); // Reset interrupted status\n   \
      \ }\n  }\n\n  // Objects passed to this method will be preserved until\n  //\
      \ the unpreserveObject() method is called\n  public static void preserveObject(Object\
      \ obj) {\n    protectedMap.put(0, obj);\n  }\n\n  // Returns the same instance\
      \ every time\n  public static Object getObject() {\n    return protectedMap.get(0);\n\
      \  }\n\n  // Unprotect the objects so that they can be garbage-collected\n \
      \ public static void unpreserveObject() {\n    protectedMap.remove(0);\n  }\n\
      }\n```\nThis is a singleton class (see MSC07-J. Prevent multiple instantiations\
      \ of singleton objects for more information on how to defensively code singleton\
      \ classes). The initialization involves creating a background thread using the\
      \ current instance of the class. The thread waits indefinitely by invoking `Object.wait()`.\
      \ Consequently, this object persists for the remainder of the Java Virtual Machine's\
      \ (JVM) lifetime. Because the object is managed by a daemon thread, the thread\
      \ cannot interfere with normal shutdown of the JVM.\n\nAlthough the initialization\
      \ involves a background thread, that thread neither accesses fields nor creates\
      \ any liveness or safety issues. Consequently, this code is a safe and useful\
      \ exception to this rule."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 12. 与线程安全相关的其他规则 (TSM)
      - type: DetailClassChin
        value: TSM02-J：不要在初始化类的时候使用后台线程
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 12. Thread-Safety Miscellaneous (TSM)
      - type: DetailClassChin
        value: TSM02-J:Do not use background threads during class initialization
    Default: true
    Description: "在类初始化时启动和使用后台线程，会造成类的初始化循环和死锁。例如：负责类初始化的main线程可能会阻塞并等待后台线程，而后台线程也会等待main线程完成类的初始化。**例如**：在初始化类的过程中后台线程创建数据库连接时。\
      \  \n程序必须确保在启动任何线程之前完成类的初始化。  \n风险评估：在初始化类时启动和使用后台线程会导致死锁。  \n**不规范的代码使用样例1**：\
      \  \n```java\npublic final class ConnectionFactory {\n    private static Connection\
      \ dbConnection;\n    // Other fields . . .\n    static {\n        Thread dbInitializerThread\
      \ = new Thread(new Runnable() {\n            @Override\n            public void\
      \ run() {\n                // Initialize the database connection\n         \
      \       try {\n                    dbConnection = DriverManager.getConnection(\"\
      connection string\");\n                } catch (SQLException e) {\n        \
      \            dbConnection = null;\n                }\n            }\n      \
      \  });\n        // Other initialization, for example, start other threads\n\
      \        dbInitializerThread.start();\n        try {\n            dbInitializerThread.join();\n\
      \        } catch (InterruptedException ie) {\n            throw new AssertionError(ie);\n\
      \        }\n    }\n\n    public static Connection getConnection() {\n      \
      \  if (dbConnection == null) {\n            throw new IllegalStateException(\"\
      Error initializing connection\");\n        }\n        return dbConnection;\n\
      \    }\n\n    public static void main(String[] args) {\n        // ...\n   \
      \     Connection connection = getConnection();\n    }\n}\n```  \n作为类初始化的一部分，静态的初始化方法启动了一个后台线程。这个后台线程尝试创建一个数据库连接，但是它应该等待ConnectionFactory类中所有成员(包括`dbConnection)`完成初始化。后台代码在执行之前必须等待main前台线程完成初始化。然后，样例中ConnectionFactory类的`main()`线程调用了`join()`方法，这会等候后台线程的结束。这种相互依赖的关系会造成类初始化的循环，从而导致死锁。"
    Description_En: "Starting and using background threads during class initialization\
      \ can result in class initialization cycles and deadlock. For example, the main\
      \ thread responsible for performing class initialization can block waiting for\
      \ the background thread, which in turn will wait for the main thread to finish\
      \ class initialization. This issue can arise, for example, when a database connection\
      \ is established in a background thread during class initialization [Bloch 2005b].\
      \ Consequently, programs must ensure that class initialization is complete before\
      \ starting any threads.\n**Noncompliant Code Example (Background Thread)**\n\
      \nIn this noncompliant code example, the static initializer starts a background\
      \ thread as part of class initialization. The background thread attempts to\
      \ initialize a database connection but should wait until all members of the\
      \ `ConnectionFactory` class, including `dbConnection`, are initialized.\n\n\
      ```java\npublic final class ConnectionFactory {\n  private static Connection\
      \ dbConnection;\n  // Other fields ...\n\n  static {\n    Thread dbInitializerThread\
      \ = new Thread(new Runnable() {\n        @Override public void run() {\n   \
      \       // Initialize the database connection\n          try {\n           \
      \ dbConnection = DriverManager.getConnection(\"connection string\");\n     \
      \     } catch (SQLException e) {\n            dbConnection = null;\n       \
      \   }\n        }\n    });\n\n    // Other initialization, for example, start\
      \ other threads\n\n    dbInitializerThread.start();\n    try {\n      dbInitializerThread.join();\n\
      \    } catch (InterruptedException ie) {\n      throw new AssertionError(ie);\n\
      \    }\n  }\n\n  public static Connection getConnection() {\n    if (dbConnection\
      \ == null) {\n      throw new IllegalStateException(\"Error initializing connection\"\
      );\n    }\n    return dbConnection;\n  }\n\n  public static void main(String[]\
      \ args) {\n    // ...\n    Connection connection = getConnection();\n  }\n}\n\
      ```\nStatically initialized fields are guaranteed to be fully constructed before\
      \ they are made visible to other threads (see TSM03-J. Do not publish partially\
      \ initialized objects for more information). Consequently, the background thread\
      \ must wait for the main (or foreground) thread to finish initialization before\
      \ it can proceed. However, the ConnectionFactory class's main thread invokes\
      \ the join() method, which waits for the background thread to finish. This interdependency\
      \ causes a class initialization cycle that results in a deadlock situation [Bloch\
      \ 2005b].\n\nSimilarly, it is inappropriate to start threads from constructors\
      \ (see TSM01-J. Do not let the this reference escape during object construction\
      \ for more information). Creating timers that perform recurring tasks and starting\
      \ those timers from within code responsible for initialization also introduces\
      \ liveness issues."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141302'
    language: Java
  - Advice: "对应的符合规则的解决方案在 main 线程初始化了所有的成员，而不是从静态的初始化方法中生成一个后台线程。   \n    ```java\n\
      \    public final class ConnectionFactory {\n        private static Connection\
      \ dbConnection;\n        // Other fields ...\n    \n        static {\n     \
      \       // Initialize a database connection\n            try {\n           \
      \     dbConnection = DriverManager.getConnection(\"connection string\");\n \
      \           } catch (SQLException e) {\n                dbConnection = null;\n\
      \            }\n            // Other initialization (do not start any threads)\n\
      \        }\n    \n        // ...\n    }\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.12.3 不要在初始化类时使用后台线程
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序必须确保在启动任何线程之前完成类的初始化。   \n2) 原理   \n在类初始化时启动和使用后台线程，会造成类的初始化循环和死锁。例如，负责类初始化的\
      \ main线程可能会阻塞并等待后台线程，而后台线程也会等待 main 线程完成类的初始化。这种问题可能会发生在，例如，在初始化类的过程中后台线程建立数据库连接时。\
      \    \n3) 示例   \n    如下不符合规则的代码示例中，作为类初始化的一部分，静态的初始化方法启动了一个后台线程。这个后台线程尝试建立一个数据库连接，但是它应该等待\
      \ ConnectionFactory 类中所有成员，包括dbConnection,都完成初始化。   \n    ```java\n    public\
      \ final class ConnectionFactory {\n        private static Connection dbConnection;\n\
      \        // Other fields ...\n    \n        static {\n            Thread dbInitializerThread\
      \ = new Thread(new Runnable() {\n                @Override\n               \
      \ public void run() {\n                    // Initialize the database connection\n\
      \                    try {\n                        dbConnection = DriverManager.getConnection(\"\
      connection string\");\n                    } catch (SQLException e) {\n    \
      \                    dbConnection = null;\n                    }\n         \
      \       }\n            });\n    \n            // Other initialization, for example,\
      \ start other threads\n    \n            dbInitializerThread.start();\n    \
      \        try {\n                dbInitializerThread.join();\n            } catch\
      \ (InterruptedException ie) {\n                throw new AssertionError(ie);\n\
      \            }\n        }\n    \n        public static Connection getConnection()\
      \ {\n            if (dbConnection == null) {\n                throw new IllegalStateException(\"\
      Error initializing connection\");\n            }\n            return dbConnection;\n\
      \        }\n    \n        public static void main(String[] args) {\n       \
      \     // ...\n            Connection connection = getConnection();\n       \
      \ }\n    }\n    ```   \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160104
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n在`main()`线程初始化所有的成员，而不是从静态的初始化方法中生成一个后台线程。\
      \  \n```java\npublic final class ConnectionFactory {\n    private static Connection\
      \ dbConnection;\n    // Other fields . . .\n    static {\n        // Initialize\
      \ a database connection\n        try {\n            dbConnection = DriverManager.getConnection(\"\
      connection string\");\n        } catch (SQLException e) {\n            dbConnection\
      \ = null;\n        }\n        // Other initialization (do not start any threads)\n\
      \    }\n    // . . .\n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.14 不要在初始化类的时候使用后台线程
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：在类初始化时启动和使用后台线程，会造成类的初始化循环和死锁。例如，负责类初始化的main线程可能会阻塞并等待后台线程，而后台线程也会等待main线程完成类的初始化。例如，在初始化类的过程中使用后台线程创建数据库连接时。\
      \  \n对于不要在初始化类的时候使用后台线程的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\npublic\
      \ final class ConnectionFactory {\n    private static Connection dbConnection;\n\
      \    // Other fields . . .\n    static {\n        Thread dbInitializerThread\
      \ = new Thread(new Runnable() {\n            @Override\n            public void\
      \ run() {\n                // Initialize the database connection\n         \
      \       try {\n                    dbConnection = DriverManager.getConnection(\"\
      connection string\");\n                } catch (SQLException e) {\n        \
      \            dbConnection = null;\n                }\n            }\n      \
      \  });\n        // Other initialization, for example, start other threads\n\
      \        dbInitializerThread.start();\n        try {\n            dbInitializerThread.join();\n\
      \        } catch (InterruptedException ie) {\n            throw new AssertionError(ie);\n\
      \        }\n    }\n\n    public static Connection getConnection() {\n      \
      \  if (dbConnection == null) {\n            throw new IllegalStateException(\"\
      Error initializing connection\");\n        }\n        return dbConnection;\n\
      \    }\n\n    public static void main(String[] args) {\n        // . . .\n \
      \       Connection connection = getConnection();\n    }\n}\n```  \n作为类初始化的一部分，静态的初始化方法启动了一个后台线程。这个后台线程尝试创建一个数据库连接，但是它应该等待ConnectionFactory类中所有成员(包括`dbConnection)`完成初始化。后台代码在执行之前必须等待`main`前台线程完成初始化。然后，样例中ConnectionFactory类的`main()`线程调用了`join()`方法，这会等候后台线程的结束。这种相互依赖的关系会造成类初始化的循环，从而导致死锁。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170046'
    language: Java
  - Advice: "比较两个类是否相同的时候，应该比较两个类对象。  \n**规范的代码样例1**：  \n```java\n// Determine whether\
      \ object auth has required/expected class name  \nif (auth.getClass() == this.getClass().getClassLoader().loadClass(\"\
      com.application.auth.DefaultAuthenticationHandler\")) {  \n   // . . .访问受保护资源的代码\
      \  \n}\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution compares the class\
      \ object `auth` to the class object for the canonical default authentication\
      \ handler:\n\n```java\n// Determine whether object auth has required/expected\
      \ class name\n if (auth.getClass() == com.application.auth.DefaultAuthenticationHandler.class)\
      \ {\n   // ...\n}\n```\n\nThe right-hand side of the comparison directly names\
      \ the class of the canonical authentication handler. In the event that the canonical\
      \ authentication handler had not yet been loaded, the Java runtime manages the\
      \ process of loading the class. Finally, the comparison is correctly performed\
      \ on the two class objects."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 05. 面向对象 (OBJ)
      - type: DetailClassChin
        value: OBJ09-J：比较类而不是类名
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 05. Object Orientation (OBJ)
      - type: DetailClassChin
        value: OBJ09-J:Compare classes and not class names
    Default: true
    Description: "在JVM中，如果两个类可以被同一个类装载器装载，并且拥有相同的全名，那么这两个类被认为是相同的类（并且具有相同的类型）。  \n\
      有时候需要检测一个指定的对象是否是特定的类型，或者两个对象的类型是否相同，例如：实现`equals()`接口的时候。如果这个比较不能正确完成，代码会认为这两个对象是同样的类，但它们实际上却不相同。\
      \  \n如果一个受保护的资源的访问是授权的，并且该授权是基于类名的比较，如果攻击者提供一个完全与目标类全名相同的恶意类，那么攻击者就能访问到受保护的资源。\
      \  \n风险评估：仅仅使用类的名称来比较类，会允许恶意类绕过安全检查并且获得受保护的资源。  \n**不规范的代码样例1**：\n这个不规范的代码示例将对象\
      \ `auth` 的类的名称与字符串 `\"com.application.auth.DefaultAuthenticationHandler\"` 进行比较，并根据比较结果进行分支：\n\
      ```java\n// Determine whether object auth has required/expected class object\n\
      \ if (auth.getClass().getName().equals(\n      \"com.application.auth.DefaultAuthenticationHandler\"\
      )) {\n   // ...\n}\n```\n比较完全限定的类名是不够的，因为不同的类加载器可以将具有相同的完全限定名的不同类加载到单个 JVM 中。\n\
      \n**不规范的代码样例2**\n这个不规范的代码示例使用`equals()`方法比较`x`和`y`的类对象的名称。同样，如果`x`和`y`来自不同的类加载器，它们可能是同名的不同类。\n\
      ```java\n// Determine whether objects x and y have the same class name\nif (x.getClass().getName().equals(y.getClass().getName()))\
      \ {\n  // Objects have the same class\n}\n```"
    Description_En: "In a Java Virtual Machine (JVM), \"Two classes are the same class\
      \ (and consequently the same type) if they are loaded by the same class loader\
      \ and they have the same fully qualified name\" [JVMSpec 1999]. Two classes\
      \ with the same name but different package names are distinct, as are two classes\
      \ with the same fully qualified name loaded by different class loaders.\n\n\
      It could be necessary to check whether a given object has a specific class type\
      \ or whether two objects have the same class type associated with them, for\
      \ example, when implementing the `equals()` method. If the comparison is performed\
      \ incorrectly, the code could assume that the two objects are of the same class\
      \ when they are not. As a result, class names must not be compared.\n\nDepending\
      \ on the function that the insecure code performs, it could be vulnerable to\
      \ a mix-and-match attack. An attacker could supply a malicious class with the\
      \ same fully qualified name as the target class. If access to a protected resource\
      \ is granted based on the comparison of class names alone, the unprivileged\
      \ class could gain unwarranted access to the resource.\n\nConversely, the assumption\
      \ that two classes deriving from the same codebase are the same is error prone.\
      \ Although this assumption is commonly observed to be true in desktop applications,\
      \ it is typically not the case with J2EE servlet containers. The containers\
      \ can use different class loader instances to deploy and recall applications\
      \ at runtime without having to restart the JVM. In such situations, two objects\
      \ whose classes come from the same codebase could appear to the JVM to be two\
      \ different classes. Also note that the `equals()` method might not return true\
      \ when comparing objects originating from the same codebase.\n\n**Noncompliant\
      \ Code Example**\n\nThis noncompliant code example compares the name of the\
      \ class of object `auth` to the string `\"com.application.auth.DefaultAuthenticationHandler\"\
      ` and branches on the result of the comparison:\n\n```java\n// Determine whether\
      \ object auth has required/expected class object\n if (auth.getClass().getName().equals(\n\
      \      \"com.application.auth.DefaultAuthenticationHandler\")) {\n   // ...\n\
      }\n```\n\nComparing fully qualified class names is insufficient because distinct\
      \ class loaders can load differing classes with identical fully qualified names\
      \ into a single JVM.\n\n**Noncompliant Code Example**\n\nThis noncompliant code\
      \ example compares the names of the class objects of `x` and `y` using the `equals()`\
      \ method. Again, it is possible that `x` and `y` are distinct classes with the\
      \ same name if they come from different class loaders.\n\n```java\n// Determine\
      \ whether objects x and y have the same class name\nif (x.getClass().getName().equals(y.getClass().getName()))\
      \ {\n  // Objects have the same class\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 486:Comparison of Classes by Name
    Security: not
    Tag: ''
    Type: cert
    WhiteList: true
    id: 0102000010140609
    language: Java
  - Advice: "对应符合规则的解决方案是将类对象`auth`与当前类装载器装载的类对象进行了比较，而不仅仅对类名称进行比较。   \n    ```java\n\
      \    //Determine whether object auth has required/expected class name \n   \
      \ if (auth.getClass() ==   this.getClass().getClassLoader().loadClass( \"com.application.auth.DefaultAuthenticationHandler11))\
      \ {   \n        // ...   \n    }\n    ```   \n    2、不符合规则的代码示例，该示例使用`equals()`方法对类对象\
      \ x 和 y 的名称进行比较。如果它们来自不同的类装载器，即使是同样的名称，x 和 y 也可能是不同的类。   \n    ```java\n   \
      \ // Determine whether objects x and y have the same class name \n    if (x.getClass()\
      \ .getName() .equals(y.getClass() .getName())) {   \n        //Objects have\
      \ the same class   \n    }\n    ```\n    对应符合规则的解决方案是：   \n    ```java\n   \
      \ // Determine whether objects x and y have the same class \n    if (x.getClass()\
      \ == y.getClass()) {   \n        // Objects have the same class   \n    }\n\
      \    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.5.10 比较类而不是类名称
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n不能基于类名称进行类比较。   \n2) 原理   \n在 JVM 中，如果两个类被同一个类装载器装载且有相同的全名，则被认为是相同的类（且有相同的类型）。有相同名称但却有不同包名的两个类是不同的类，就像是被不同的类装载器载入的有相同的全名的两个类一样。\
      \   \n有时需要检查一个指定对象是否是特定的类型，或两个对象的类型是否相同。如果这个比较不能正确完成，代码会认为这两个对象是同样的类，但它们实际上并不相同。因此，不能基于类名称进行比较。\
      \   \n一些不安全代码，可能容易受到混合与匹配攻击。攻击者会提供与目标类全限定名相同的恶意类。如果对一个受保护资源的访问是被授权的，并且该授权只是基于对类名称的比较，那么非特权类会未经授权就能够访问资源。\
      \   \n3) 示例   \n    1、不符合规则的代码示例，该示例将对象`auth`的类的名称和字符串“`com.application.autli.DefaultAuthenticationHandler”`进行比较，并且基于该比较结果进行分支。\
      \   \n    ```java\n    //Determine whether object auth has required/expected\
      \ class object \n    if (auth.getClass().getName().equals( \"com.application.auth.DefaultAuthenticationHandler\"\
      )) {   \n        //...   \n    }\n    ```   \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 486:Comparison of Classes by Name
      - type: CERT
        value: OBJ09-J:Compare classes and not class names
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: true
    id: 0902000010160047
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\n// Determine whether object\
      \ auth has required/expected class name  \nif(auth.getClass()==this.getClass().getClassLoader().loadClass(\"\
      com.application.auth. DefaultAuthenticationHandler\")) {  \n   // . . .访问受保护资源的代码\
      \  \n} \n```\n上述示例中，使用`class`来比较两个类是否相同。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.1 面向对象程序安全
      - type: DetailClassChin
        value: A.3.1.2 比较类而不是类名
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：在JVM中，如果两个类可以被同一个类装载器装载，并且拥有相同的全名，那么这两个类被认为是相同的类（并且具有相同的类型）。\
      \  \n如果一个受保护的资源的访问是需要授权的，而该授权是基于类名的比较，如果攻击者提供一个完全与目标类全名相同的恶意类，那么攻击者就能访问到受保护的资源。仅仅使用类的名称来比较类，会允许恶意类绕过安全检查并且获得受保护的资源。\
      \  \n对于比较类而不是类名的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nif (auth.getClass().getName().equals(\"\
      com.application.auth.DefaultAuthenticationHandler\")) {  \n  // . . .访问受保护资源的代码\
      \  \n}  \n```\n比较两个类是否相同的时候，应该比较两个类对象。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 486:Comparison of Classes by Name
      - type: CERT
        value: OBJ09-J:Compare classes and not class names
    Security: not
    Tag: ''
    Type: guide
    WhiteList: true
    id: 0302000010170028
    language: Java
  - Advice: "建议使用日志记录工具代替System.out或System.err记录程序日志。  \n  \n**例如**：下列代码中使用Log4j工具类进行程序日志输出。\
      \  \n```java\n...  \nlogger.info(log);  \n...\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 日志规范
      - type: DetailClassChin
        value: 【推荐】不要使用系统输出流记录日志
    Categories_En:
      Category: []
    Default: true
    Description: "使用System.out或System.err进行程序日志输出，会导致程序的运行状况难以监控。  \n  \n**例如**：下列代码中使用System.out进行程序日志输出。\
      \  \n```java\n...  \nSystem.out.println(log);  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Use of a System Output Stream
    ENSubClassChin: Poor Logging Practice
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.5.6 使用system.err或system.out方法输出日志
    Security: not
    Tag: log,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150406'
    language: Java
  - Advice: "**规范的代码样例1**：\nJava 1.6及更早版本缺少在创建文件时指定默认权限的机制。因此，必须使用Java外部的某种机制来避免或解决此问题，例如使用本地代码和Java\
      \ Native Interface（JNI）。  \n规范的代码样例2(POSIX)：  \nJava SE 7新的I/O类库java.nio提供了用于管理文件访问权限的类。此外，创建文件的许多方法和构造函数都接受一个参数，允许程序指定初始文件权限。\
      \  \n例如：以下代码中`Files.newByteChannel()`方法允许创建具有特定权限的文件。该方法与平台无关，但实际的权限是平台特定的。此兼容解决方案为POSIX平台定义了足够的限制性权限。\
      \  \n```java\nPath file = new File(\"file\").toPath();\n\n// Throw exception\
      \ rather than overwrite existing file\nSet<OpenOption> options = new HashSet<OpenOption>();\n\
      options.add(StandardOpenOption.CREATE_NEW);\noptions.add(StandardOpenOption.APPEND);\n\
      \n// File permissions should be such that only user may read/write file\nSet<PosixFilePermission>\
      \ perms =\n    PosixFilePermissions.fromString(\"rw-------\");\nFileAttribute<Set<PosixFilePermission>>\
      \ attr =\n    PosixFilePermissions.asFileAttribute(perms);\n\ntry (SeekableByteChannel\
      \ sbc =\n         Files.newByteChannel(file, options, attr)) {\n  // Write data\n\
      };\n```\n**特例**：  \nFIO01-EX0:当在一个安全和对非受信用户不可读的目录中创建文件时，可以使用默认的访问权限创建。例如整个文件系统是可信的，或者仅受信用户可以访问。安全目录的定义可参考FIO00-J。\
      \  \nFIO01-EX1:那些不含有特权信息的文件不需要使用指定的访问权限创建。"
    Advice_En: "**Compliant Solution (Java 1.6 and Earlier)**\n\nJava 1.6 and earlier\
      \ lack a mechanism for specifying default permissions upon file creation. Consequently,\
      \ the problem must be avoided or solved using some mechanism external to Java,\
      \ such as by using native code and the Java Native Interface (JNI).\n\n**Compliant\
      \ Solution (POSIX)**\n\nThe  I/O facility `java.nio` provides classes for managing\
      \ file access permissions. Additionally, many of the methods and constructors\
      \ that create files accept an argument allowing the program to specify the initial\
      \ file permissions.\n\nThe `Files.newByteChannel()` method allows a file to\
      \ be created with specific permissions. This method is platform-independent,\
      \ but the actual permissions are platform-specific. This compliant solution\
      \ defines sufficiently restrictive permissions for POSIX platforms:\n\n```java\n\
      Path file = new File(\"file\").toPath();\n\n// Throw exception rather than overwrite\
      \ existing file\nSet<OpenOption> options = new HashSet<OpenOption>();\noptions.add(StandardOpenOption.CREATE_NEW);\n\
      options.add(StandardOpenOption.APPEND);\n\n// File permissions should be such\
      \ that only user may read/write file\nSet<PosixFilePermission> perms =\n   \
      \ PosixFilePermissions.fromString(\"rw-------\");\nFileAttribute<Set<PosixFilePermission>>\
      \ attr =\n    PosixFilePermissions.asFileAttribute(perms);\n\ntry (SeekableByteChannel\
      \ sbc =\n         Files.newByteChannel(file, options, attr)) {\n  // Write data\n\
      };\n```\n\n**Exceptions**\n\nFIO01-J-EX0: When a file is created inside a directory\
      \ that is both secure and unreadable by untrusted users, that file may be created\
      \ with the default access permissions. This could be the case if, for example,\
      \ the entire file system is trusted or is accessible only to trusted users (see\
      \ FIO00-J. Do not operate on files in shared directories for the definition\
      \ of a secure directory).\nFIO01-J-EX1: Files that do not contain privileged\
      \ information need not be created with specific access permissions."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO01-J：使用合适的访问权限创建文件
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO01-J:Do not operate on files in shared directories
    Default: true
    Description: "多用户系统中的文件通常由特定用户拥有，该文件的所有者可以指定系统上哪些其他用户可以访问这些文件的内容。\n这些文件系统使用权限和权限系统来保护文件的访问。创建文件时，文件访问权限决定了谁可以访问或者操作该文件。当一个程序使用不充分的访问权限创建文件时，攻击者可以在程序修改权限之前读取或修改该文件。因此，必须创建具有访问权限的文件，以防止未经授权的文件访问。\n\
      **不规范的代码样例1**：  \n```java\nWriter out = new FileWriter(\"file\");\n```\nFileOutputStream和FileWriter的构造函数不允许程序员明确指定文件访问权限。在以上代码中，创建的任何文件的访问权限是由具体实现决定的，可能不会阻止未经授权的访问。"
    Description_En: 'Files on multiuser systems are generally owned by a particular
      user. The owner of the file can specify which other users on the system should
      be allowed to access the contents of these files.


      These file systems use a privileges and permissions model to protect file access.
      When a file is created, the file access permissions dictate who may access or
      operate on the file. When a program creates a file with insufficiently restrictive
      access permissions, an attacker may read or modify the file before the program
      can modify the permissions. Consequently, files must be created with access
      permissions that prevent unauthorized file access.


      **Noncompliant Code Example**


      The constructors for `FileOutputStream` and `FileWriter` do not allow the programmer
      to explicitly specify file access permissions. In this noncompliant code example,
      the access permissions of any file created are implementation-defined and may
      not prevent unauthorized access:


      ```java

      Writer out = new FileWriter("file");

      ```'
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 732:Incorrect Permission Assignment for Critical Resource
      - type: CWE
        value: 264:Permissions, Privileges, and Access Controls
      - type: OWASP TOP 10
        value: 2004:A2 Broken Access Control
      - type: OWASP MOBILE TOP 10
        value: 2014:M8 Security Decisions Via Untrusted Inputs
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 732
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 732
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 732
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 732
    Security: true
    Tag: ''
    Type: cert
    WhiteList: true
    id: '0102000010141401'
    language: Java
  - Advice: "对应的符合规则的方案为 POSIX 平台定义了足够的权限。   \n    ```java\n    Path file = new File(\"\
      file\").toPath();   \n       \n    // Throw exception rather than overwrite\
      \ existing file    \n    Set<OpenOption> options = new HashSet<OpenOption>();\
      \   \n    options.add(StandardOpenOption.CREATE_NEW);    \n    options.add(StandardOpenOption.APPEND);\
      \   \n    // File permissions should be such that only user may read/write file\
      \    \n    Set<PosixFilePermission> perms =PosixFilePermissions.fromString(\"\
      rw-------\");   \n    FileAttribute<Set<PosixFilePermission>> attr =PosixFilePermissions.asFileAttribute(perms);\
      \   \n    try (SeekableByteChannel sbc =Files.newByteChannel(file, options,\
      \ attr)) {   \n      //write data   \n    }\n    ``` \n4) 特例   \n**特例 1**：当在一个安全和对非受信用户不可读的目录中创建文件的时候，可以使用默认\
      \ 的访问权限创建。例如整个文件系统是可信的，或者仅受信用户可访问。   \n**特例 2**：那些不含有特权信息的文件不需要使用指定的访问权限创建。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 7.5 文件系统
      - type: DetailClassChin
        value: C.13.2 使用合适的访问权限创建文件
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n必须使用能避免越权访问的权限来创建文件。   \n2) 原理   \n多用户系统中的文件通常为一个特定的用户所有。文件的所有者会指定系统中的用户可以访问这些文件的内容。\
      \   \n这些文件系统使用一个特权和权限系统来保护文件的访问。当创建一个文件时，文件的访问权限决定了谁能访问和操作这个文件。当一个程序使用不充分的限制性的访问权限创建文件时，攻击者可以在程序修改权限之前读取或修改文件。\
      \   \n3) 示例   \n    在 FileOutputStream 和 FileWriter 的构造方法中，不允许开发者直接指定文件的访问权限。在下面这个不符合规则的代码示例中，任何创建文件的访问权限都是由具体实现决定的，这可能会允许来授权的访问。\
      \   \n    ```java\n    Writer out = new FileWriter(\"file\");\n    ```   \n\
      \    Java SE 7 新的 I/O 类库提供了管理文件访问权限的类。另外，很多创建文件的方法和构造函数接受一个参数，从而允许程序来指定初始的文件权限。\
      \   \n    方法 `Files.newByteChannel()`允许使用指定的权限创建文件。这个方法和平台无关，但是实际的权限和平台有关。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 732:Incorrect Permission Assignment for Critical Resource
      - type: CWE
        value: 264:Permissions, Privileges, and Access Controls
      - type: CERT
        value: FIO01-J:Create files with appropriate access permissions
      - type: OWASP TOP 10
        value: 2004:A2 Broken Access Control
      - type: OWASP MOBILE TOP 10
        value: 2014:M8 Security Decisions Via Untrusted Inputs
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 732
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 732
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 732
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 732
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: true
    id: 0902000010160107
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**： \n```java\nString permissionMask =\
      \ System.getProperty(\"fileMask\");  \nPath path= testFile.toPath();  \nString[]\
      \ validPermissions = {\"r--------\", \"r--r-----\"};  \nfor (String validPermission\
      \ : validPermissions){  \n    if (permissionMask.equals(validPermission){  \n\
      \        Set<PosixFilePermission> permission = PosixFilePermissions.fromString(validPermission);\
      \  \n        Files.setPosixFilePermissions(path, permission);  \n        ...\
      \  \n    }  \n    ...  \n} \n```  \n上述代码可以通过允许用户设置文件权限来防止此类文件权限操纵。如果必须确保用户拥有能够设置文件的权限，则可以采用一种间接方式，即创建一个允许用户进行指定的合法文件权限列表，并且仅允许用户从列表中进行选择。通过这种方式，用户提供的输入将不会直接更改文件权限。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.3 访问控制
      - type: DetailClassChin
        value: A.2.3.3.2 避免越权访问
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：多用户系统中的文件通常为某一特定用户所有，文件所有者会指定系统中的用户可以访问这些文件的内容。当一个程序使用不充分的限制性的访问权限创建文件时，攻击者可以在程序修改权限之前读取或者修改这些文件。如果允许用户输入直接更改文件权限，则攻击者将能够访问其他受保护的系统资源。\
      \  \n对于避免越权访问的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nString permissionMask\
      \ = System.getProperty(\"fileMask\");  \nPath path= testFile.toPath();  \n...\
      \  \nSet<PosixFilePermission> permission= PosixFilePermissions.fromString(permissionMask);\
      \  \nFiles.setPosixFilePermissions(path, permission);\n```\n以上代码使用来自系统属性的参数作为默认的权限代码。但是系统属性中的值未必是可信的，如果系统属性中的值为攻击者可控，则可以使用该程序获得其所处理文件的访问权限。如果程序还易受路径篡改攻击，那么攻击者可能会利用这一漏洞访问系统中的任意文件。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 732:Incorrect Permission Assignment for Critical Resource
      - type: CWE
        value: 264:Permissions, Privileges, and Access Controls
      - type: CERT
        value: FIO01-J:Create files with appropriate access permissions
      - type: OWASP TOP 10
        value: 2004:A2 Broken Access Control
      - type: OWASP MOBILE TOP 10
        value: 2014:M8 Security Decisions Via Untrusted Inputs
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 732
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 732
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 732
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 732
      - type: PCI DSS
        value: 1.1:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2:Requirement 6.5.10
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.10
      - type: PCI DSS
        value: 2.0:Requirement 6.5.8
      - type: PCI DSS
        value: 3.0:Requirement 6.5.8
      - type: PCI DSS
        value: 3.1:Requirement 6.5.8
      - type: PCI DSS
        value: 3.2:Requirement 6.5.8
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.8
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170026'
    language: Java
  - Advice: "**规范的代码样例1**：\n这个兼容的解决方案使用Java 8中引入的`SecureRandom.getInstanceStrong()`方法，以使用强RNG算法，如果有的话。\n\
      ```java\nimport java.security.SecureRandom;\nimport java.security.NoSuchAlgorithmException;\n\
      // ...\n\npublic static void main (String args[]) {\n   try {\n     SecureRandom\
      \ number = SecureRandom.getInstanceStrong();\n     // Generate 20 integers 0..20\n\
      \     for (int i = 0; i < 20; i++) {\n       System.out.println(number.nextInt(21));\n\
      \     }\n   } catch (NoSuchAlgorithmException nsae) {\n     // Forward to handler\n\
      \   }\n}\n```\n**特例**\nMSC02-J-EX0: 使用`java.util.Random`的默认构造函数会应用一个种子值，该值 \"\
      很可能与该构造函数的任何其他调用不同\"[API 2014]，并可能略微提高安全性。因此，它可能只用于对非敏感数据进行操作的非关键应用程序。Java的默认种子使用系统的时间，单位是毫秒。当使用时，需要明确记录这个异常。\n\
      ```java\nimport java.util.Random;\n// ...\n\nRandom number = new Random(); //\
      \ Used only for demo purposes\nint n;\n//...\nfor (int i = 0; i < 20; i++) {\n\
      \  // Reseed generator\n  number = new Random();\n  // Generate another random\
      \ integer in the range [0, 20]\n  n = number.nextInt(21);\n  System.out.println(n);\n\
      }\n```\n对于非关键的情况，如在游戏或单元测试中添加一些随机性，使用`Random`类是可以接受的。然而，值得重申的是，所产生的低熵随机数的随机性不够，不能用于更多的安全关键应用，如密码学。\n\
      MSC02-J-EX1: 在某些情况下需要可预测的伪随机数序列，如运行程序行为的回归测试时。在这种情况下，允许使用不安全的`java.util.Random`类。然而，与安全有关的应用程序可以调用这个例外，*只用于测试目的；这个例外不能应用于生产环境。"
    Advice_En: "**Compliant Solution (Java 8)**\n\nThis compliant solution uses the\
      \ `SecureRandom.getInstanceStrong()` method, introduced in Java 8, to use a\
      \ strong RNG algorithm, if one is available.\n\n```java\nimport java.security.SecureRandom;\n\
      import java.security.NoSuchAlgorithmException;\n// ...\n\npublic static void\
      \ main (String args[]) {\n   try {\n     SecureRandom number = SecureRandom.getInstanceStrong();\n\
      \     // Generate 20 integers 0..20\n     for (int i = 0; i < 20; i++) {\n \
      \      System.out.println(number.nextInt(21));\n     }\n   } catch (NoSuchAlgorithmException\
      \ nsae) {\n     // Forward to handler\n   }\n}\n```\n\n**Exceptions**\n\nMSC02-J-EX0:\
      \ Using the default constructor for `java.util.Random` applies a seed value\
      \ that is \"very likely to be distinct from any other invocation of this constructor\"\
      \ [API 2014] and may improve security marginally. As a result, it may be used\
      \ only for noncritical applications operating on nonsensitive data. Java's default\
      \ seed uses the system's time in milliseconds. When used, explicit documentation\
      \ of this exception is required.\n\n```java\nimport java.util.Random;\n// ...\n\
      \nRandom number = new Random(); // Used only for demo purposes\nint n;\n//...\n\
      for (int i = 0; i < 20; i++) {\n  // Reseed generator\n  number = new Random();\n\
      \  // Generate another random integer in the range [0, 20]\n  n = number.nextInt(21);\n\
      \  System.out.println(n);\n}\n```\n\nFor noncritical cases, such as adding some\
      \ randomness to a game or unit testing, the use of class `Random` is acceptable.\
      \ However, it is worth reiterating that the resulting low-entropy random numbers\
      \ are insufficiently random to be used for more security-critical applications,\
      \ such as cryptography.\n\nMSC02-J-EX1: Predictable sequences of pseudorandom\
      \ numbers are required in some cases, such as when running regression tests\
      \ of program behavior. Use of the insecure `java.util.Random` class is permitted\
      \ in such cases. However, security-related applications may invoke this exception\
      \ *only* for testing purposes; this exception may not be applied in a production\
      \ context."
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 49. 其他 (MSC)
      - type: DetailClassChin
        value: MSC02-J：生成强随机数
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 49. Miscellaneous (MSC)
      - type: DetailClassChin
        value: MSC02-J:Generate strong random numbers
    Default: true
    Description: "伪随机数生成器(PRNG)使用一个确定的数据算法来生成具有良好统计特性的数字序列。然而，这个生成的数字序列并不具有真正的随机性。PRNG通常起始于一个种子数字，算法使用这个种子来生成输出数值和一个新的种子，这个新种子会用来生成一个数值，然后以此类推。\
      \  \nJavaAPI提供了`java.util.Random`类来实现PRNG。这个PRNG是可移植和可重复的。因此，如果两个`java.util.Random`类的实例使用了相同的种子，会在所有的Java实现中生成相同的数值序列。在应用初始化时，或者在每一次系统重启后，种子常常会被重用。在其他情况下，种子会从系统时钟基于当前的时刻获取。攻击者可以通过对有漏洞的目标系统做探查而获取种子数值，然后可以建立一个查找表来对所有的种子值进行估计。\
      \  \n因此，`java.util.Random`不能在安全应用或者在对敏感数据进行保护的场合进行使用（如：密码学），应该使用更安全的随机数生成器，比如`java.security.SecureRandom`类。\
      \  \n风险评估：可预测的随机数序列在像密码学这样的关键应用中会消弱其安全性。  \n**不规范的代码样例1**：  \n```java\nimport\
      \ java.util.Random;\n// ...\n\nRandom number = new Random(123L);\n//...\nfor\
      \ (int i = 0; i < 20; i++) {\n  // Generate another random integer in the range\
      \ [0, 20]\n  int n = number.nextInt(21);\n  System.out.println(n);\n}\n```\n\
      **例外**：用于非关键应用中的非敏感数据时，可以使用java.util.Random类来生成随机数。比如，一个游戏或者单元测试中添加随机性。"
    Description_En: "Pseudorandom number generators (PRNGs) use deterministic mathematical\
      \ algorithms to produce a sequence of numbers with good statistical properties.\
      \ However, the sequences of numbers produced fail to achieve true randomness.\
      \ PRNGs usually start with an arithmetic seed value. The algorithm uses this\
      \ seed to generate an output value and a new seed, which is used to generate\
      \ the next value, and so on.\n\nThe Java API provides a PRNG, the `java.util.Random`\
      \ class. This PRNG is portable and repeatable. Consequently, two instances of\
      \ the `java.util.Random` class that are created using the same seed will generate\
      \ identical sequences of numbers in all Java implementations. Seed values are\
      \ often reused on application initialization or after every system reboot. In\
      \ other cases, the seed is derived from the current time obtained from the system\
      \ clock. An attacker can learn the value of the seed by performing some reconnaissance\
      \ on the vulnerable target and can then build a lookup table for estimating\
      \ future seed values.\n\nConsequently, the java.util.Random class must not be\
      \ used either for security-critical applications or for protecting sensitive\
      \ data. Use a more secure random number generator, such as the java.security.SecureRandom\
      \ class.\n\n**Noncompliant Code Example**\n\nThis noncompliant code example\
      \ uses the insecure `java.util.Random` class. This class produces an identical\
      \ sequence of numbers for each given seed value; consequently, the sequence\
      \ of numbers is predictable.\n\n```java\nimport java.util.Random;\n// ...\n\n\
      Random number = new Random(123L);\n//...\nfor (int i = 0; i < 20; i++) {\n \
      \ // Generate another random integer in the range [0, 20]\n  int n = number.nextInt(21);\n\
      \  System.out.println(n);\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 338:Use of Cryptographically Weak Pseudo-Random Number Generator (PRNG)
      - type: CWE
        value: 330:Use of Insufficiently Random Values
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 330
      - type: GB/T 34944-2017 Java
        value: 安全功能:不充分的随机数
    Security: true
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010141802
    language: Java
  - Advice: "这个符合规则的代码示例使用 `java.security.SecureRandom `类来生成高质量的随机数。\n    ```java\n\
      \    import java.security.SecureRandom;   \n    import java.security.NoSuchAlgorithmException;\
      \   \n    // . . .    \n    public static void main (String args(1){ \n    \
      \    try {   \n            SecureRandom number = SecureRandom.getlnstance(\"\
      SHA1PRNG\"); // Generate 20 integers 0..20 \n            for (int i =   0; i\
      \ < 20; i++) {   \n                System.out.println(number.nextlnt(21)); \
      \  \n            }   \n        } catch (NoSuchAlgorithmException nsae) {   \n\
      \            // Forward to handler\n    ```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 5.3 数据加密与保护
      - type: DetailClassChin
        value: C.17.3 生成强随机数
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \njava.util.Random 类不能用在安全关键应用或存在敏感数据的场合。   \n2) 原理   \n\
      伪随机数生成器（Pseudorandom Number Generator, PRNG）使用确定的数据算法来生 成具有良好统计特性的数字序列，PRNG\
      \ 通常起始于一个种子数字。算法使用这个种子生成输出数值和一个新种子，新种子用来生成下一个数值，然后依次类推。   \nJava API 提供 `java.util.Random`\
      \ 类来实现 PRNG，这个 PRNG 是可移植和可重复的。如果`java.util.Random `类的两个实例使用相同的种子，会在所有 Java 实现中生成相同的数值序列。\
      \    \n种子有可能泄漏。`java.util.Random` 不能用在安全关键应用或存在敏感数据的场合。在这些场合应该使用更安全的随机数生成器，比如\
      \ `java.security.SecureRandom` 类。   \n3) 示例   \n    1.这个不符合规则的代码示例使用了 `java.util.Random`\
      \ 类，一个指定的种子值生成一个相同的序列，这个数值序列是可被预测的。   \n    ```java\n    import java.util.Random;\
      \   \n    // . . .  *   \n    Random number = new Random(123L);   \n    //...\
      \   \n    for (int i = 0; i < 20; i++) {   \n        // Generate another random\
      \ integer in the range [0, 20] int n = number.nextlnt(21);   \n        System.out.printIn(n);\
      \   \n    }\n    ```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 338:Use of Cryptographically Weak Pseudo-Random Number Generator (PRNG)
      - type: CWE
        value: 330:Use of Insufficiently Random Values
      - type: CERT
        value: MSC02-J:Generate strong random numbers
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 330
      - type: GB/T 34944-2017 Java
        value: 安全功能:不充分的随机数
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160150
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nimport java.security.SecureRandom;\
      \  \nimport java.security.NoSuchAlgorithmException;  \n// ...\npublic static\
      \ void main(String args[]) {\n    try {\n        SecureRandom number = SecureRandom.getInstance(\"\
      SHA1PRNG\");\n        // Generate 20 integers 0..20\n        for (int i = 0;\
      \ i < 20; i++) {\n            System.out.println(number.nextInt(21));\n    \
      \    }\n    } catch (NoSuchAlgorithmException nsae) {\n        // Forward to\
      \ handler\n    }\n}\n```  \n在如上示例中，使用了`java.security.SecureRandom`生成随机数。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.2 数据加密与保护
      - type: DetailClassChin
        value: A.2.2.1.2 生成强随机数
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：JavaAPI提供了`java.util.Random`类来实现PRNG。这个PRNG是可移植和可重复的。因此，如果两个`java.util.Random`类的实例使用了相同的种子，会在所有的Java实现中生成相同的数值序列。在应用初始化时，或者在每一次系统重启后，种子常常会被重用。在其他情况下，种子会从系统时钟基于当前的时刻获取。攻击者可以通过对有漏洞的目标系统做探查而获取种子数值，然后可以建立一个查找表来对所有的种子值进行估计。\
      \  \n因此，`java.util.Random`不能在安全应用或者在需对敏感数据进行保护的场合使用（如：密码学），应该使用更安全的随机数生成器，比如`java.security.SecureRandom`、`java.util.concurrent.ThreadLocalRandom`类。可预测的随机数序列在像密码学这样的关键应用中会消弱其安全性。\
      \  \n对于生成强随机数的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nimport java.util.Random;\
      \  \n// ...  \nRandom number = new Random(123L);\n// ...\nfor (int i = 0; i\
      \ < 20; i++) {\n    // Generate another random integer in the range [0, 20]\n\
      \    int n = number.nextInt(21);\n    System.out.println(n);\n}\n```  \n在如上示例中，使用了`java.util.Random`生成随机数。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 338:Use of Cryptographically Weak Pseudo-Random Number Generator (PRNG)
      - type: CWE
        value: 330:Use of Insufficiently Random Values
      - type: CERT
        value: MSC02-J:Generate strong random numbers
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 330
      - type: GB/T 34944-2017 Java
        value: 安全功能:不充分的随机数
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170011'
    language: Java
  - Advice: "当XML可能已经载入还未处理的输入数据时，一般情况下使用XML模板或者DTD验证XML。如果还没有创建这样的XML字符串，那么在创建XML之前处理输入，这种方式性能较高。\
      \  \n**规范的代码样例1**：输入验证  \n下面代码的解决方案是验证`quantity`是一个无符号整数。 \n```java\nimport\
      \ java.io.BufferedOutputStream;\nimport java.io.ByteArrayOutputStream;\nimport\
      \ java.io.IOException;\n\npublic class OnlineStore {\n    private static void\
      \ createXMLStream(final BufferedOutputStream outStream, final String quantity)\n\
      \            throws IOException, NumberFormatException {\n        // Write XML\
      \ string only if quantity is an unsigned integer (count).  \n        int count\
      \ = Integer.parseUnsignedInt(quantity);\n        String xmlString = \"<item>\\\
      n<description>Widget</description>\\n\" + \"<price>500</price>\\n\" + \"<quantity>\"\
      \ + count\n                + \"</quantity></item>\";\n        outStream.write(xmlString.getBytes());\n\
      \        outStream.flush();\n    }\n}\n``` \n**规范的代码样例2**：XML模板  \n更为通用的验证XML，防止注入的方法是：使用文档类型定义（Document\
      \ Type Definition， DTD）或者schema来验证。schema必须严格定义，从而防止通过注入的方式将一个合法的XML文档变成错误的文档。以下是用来对样例2进行验证的schema模板：\
      \  \n```xml\n<xs:schema xmlns:xs=\"http://www.w3.org/2001/XMLSchema\">\n\t<xs:element\
      \ name=\"item\" >\n\t\t<xs:complexType>\n\t\t\t<xs:sequence>\n\t\t\t\t<xs:element\
      \ name=\"description\" type=\"xs:string\" />\n\t\t\t\t<xs:element name=\"price\"\
      \ type=\"xs:decimal\" />\n\t\t\t\t<xs:element name=\"quantity\" type=\"xs:nonNegativeInteger\"\
      \ />\n\t\t\t</xs:sequence>\n\t\t</xs:complexType>\n\t</xs:element>\n</xs:schema>\
      \ \n``` \n在schema.xsd文件中可以得到XML模板。下面解决方案使用这个模板来防止XML注入。它同时需要使用在IDS17-J中定义的CustomResolver类来防止XXE攻击。\
      \  \n```java\nimport java.io.BufferedOutputStream;\nimport java.io.ByteArrayOutputStream;\n\
      import java.io.File;\nimport java.io.IOException;\nimport java.io.StringReader;\n\
      \nimport javax.xml.XMLConstants;\nimport javax.xml.parsers.ParserConfigurationException;\n\
      import javax.xml.parsers.SAXParser;\nimport javax.xml.parsers.SAXParserFactory;\n\
      import javax.xml.transform.stream.StreamSource;\nimport javax.xml.validation.Schema;\n\
      import javax.xml.validation.SchemaFactory;\n\nimport org.xml.sax.InputSource;\n\
      import org.xml.sax.SAXException;\nimport org.xml.sax.SAXParseException;\nimport\
      \ org.xml.sax.XMLReader;\nimport org.xml.sax.helpers.DefaultHandler;\n\npublic\
      \ class OnlineStore {\n\n    private static void createXMLStream(final BufferedOutputStream\
      \ outStream, final String quantity)\n            throws IOException {\n    \
      \    String xmlString;\n        xmlString = \"<item>\\n<description>Widget</description>\\\
      n\" + \"<price>500.0</price>\\n\" + \"<quantity>\" + quantity\n            \
      \    + \"</quantity></item>\";\n        InputSource xmlStream = new InputSource(new\
      \ StringReader(xmlString));\n// Build a validating SAX parser using our schema\
      \  \n        SchemaFactory sf = SchemaFactory.newInstance(XMLConstants.W3C_XML_SCHEMA_NS_URI);\n\
      \        DefaultHandler defHandler = new DefaultHandler() {\n            public\
      \ void warning(SAXParseException s) throws SAXParseException {\n           \
      \     throw s;\n            }\n\n            public void error(SAXParseException\
      \ s) throws SAXParseException {\n                throw s;\n            }\n\n\
      \            public void fatalError(SAXParseException s) throws SAXParseException\
      \ {\n                throw s;\n            }\n        };\n        StreamSource\
      \ ss = new StreamSource(new File(\"schema.xsd\"));\n        try {\n        \
      \    Schema schema = sf.newSchema(ss);\n            SAXParserFactory spf = SAXParserFactory.newInstance();\n\
      \            spf.setSchema(schema);\n            SAXParser saxParser = spf.newSAXParser();\n\
      \            // To set the custom entity resolver,\n            // an XML reader\
      \ needs to be created\n            XMLReader reader = saxParser.getXMLReader();\n\
      \            reader.setEntityResolver(new CustomResolver());\n            saxParser.parse(xmlStream,\
      \ defHandler);\n        } catch (ParserConfigurationException x) {\n       \
      \     throw new IOException(\"Unable to validate XML\", x);\n        } catch\
      \ (SAXException x) {\n            throw new IOException(\"Invalid quantity\"\
      , x);\n        }\n// Our XML is valid, proceed  \n        outStream.write(xmlString.getBytes());\n\
      \        outStream.flush();\n    }\n}\n```"
    Advice_En: "**Compliant Solution (Input Validation)**\n\nDepending on the specific\
      \ data and command interpreter or parser to which data is being sent, appropriate\
      \ methods must be used to sanitize untrusted user input. This compliant solution\
      \ validates that `quantity` is an unsigned integer:\n\n```java\nimport java.io.BufferedOutputStream;\n\
      import java.io.ByteArrayOutputStream;\nimport java.io.IOException;\n\npublic\
      \ class OnlineStore {\n  private static void createXMLStream(final BufferedOutputStream\
      \ outStream,\n      final String quantity) throws IOException, NumberFormatException\
      \ {\n    // Write XML string only if quantity is an unsigned integer (count).\n\
      \    int count = Integer.parseUnsignedInt(quantity);\n    String xmlString =\
      \ \"<item>\\n<description>Widget</description>\\n\"\n        + \"<price>500</price>\\\
      n\" + \"<quantity>\" + count + \"</quantity></item>\";\n    outStream.write(xmlString.getBytes());\n\
      \    outStream.flush();\n  }\n}\n```\n\n**Compliant Solution (XML Schema)**\n\
      \nA more general mechanism for checking XML for attempted injection is to validate\
      \ it using a Document Type Definition (DTD) or schema. The schema must be rigidly\
      \ defined to prevent injections from being mistaken for valid XML. Here is a\
      \ suitable schema for validating our XML snippet:\n\n```java\n<xs:schema xmlns:xs=\"\
      http://www.w3.org/2001/XMLSchema\">\n<xs:element name=\"item\">\n  <xs:complexType>\n\
      \    <xs:sequence>\n      <xs:element name=\"description\" type=\"xs:string\"\
      />\n      <xs:element name=\"price\" type=\"xs:decimal\"/>\n      <xs:element\
      \ name=\"quantity\" type=\"xs:nonNegativeInteger\"/>\n    </xs:sequence>\n \
      \ </xs:complexType>\n</xs:element>\n</xs:schema>\n```\n\nThe schema is available\
      \ as the file `schema.xsd`. This compliant solution employs this schema to prevent\
      \ XML injection from succeeding. It also relies on the `CustomResolver` class\
      \ defined in IDS17-J. Prevent XML External Entity Attacks to prevent XML external\
      \ entity (XXE) attacks.\n\n```java\nimport java.io.BufferedOutputStream;\nimport\
      \ java.io.ByteArrayOutputStream;\nimport java.io.File;\nimport java.io.IOException;\n\
      import java.io.StringReader;\n\nimport javax.xml.XMLConstants;\nimport javax.xml.parsers.ParserConfigurationException;\n\
      import javax.xml.parsers.SAXParser;\nimport javax.xml.parsers.SAXParserFactory;\n\
      import javax.xml.transform.stream.StreamSource;\nimport javax.xml.validation.Schema;\n\
      import javax.xml.validation.SchemaFactory;\n\nimport org.xml.sax.InputSource;\n\
      import org.xml.sax.SAXException;\nimport org.xml.sax.SAXParseException;\nimport\
      \ org.xml.sax.XMLReader;\nimport org.xml.sax.helpers.DefaultHandler;\n\npublic\
      \ class OnlineStore {\n\n  private static void createXMLStream(final BufferedOutputStream\
      \ outStream,\n      final String quantity) throws IOException {\n    String\
      \ xmlString;\n    xmlString = \"<item>\\n<description>Widget</description>\\\
      n\"\n        + \"<price>500.0</price>\\n\" + \"<quantity>\" + quantity\n   \
      \     + \"</quantity></item>\";\n    InputSource xmlStream = new InputSource(new\
      \ StringReader(xmlString));\n    // Build a validating SAX parser using our\
      \ schema\n    SchemaFactory sf = SchemaFactory\n        .newInstance(XMLConstants.W3C_XML_SCHEMA_NS_URI);\n\
      \    DefaultHandler defHandler = new DefaultHandler() {\n      public void warning(SAXParseException\
      \ s) throws SAXParseException {\n        throw s;\n      }\n      public void\
      \ error(SAXParseException s) throws SAXParseException {\n        throw s;\n\
      \      }\n      public void fatalError(SAXParseException s) throws SAXParseException\
      \ {\n        throw s;\n      }\n    };\n    StreamSource ss = new StreamSource(new\
      \ File(\"schema.xsd\"));\n    try {\n      Schema schema = sf.newSchema(ss);\n\
      \      SAXParserFactory spf = SAXParserFactory.newInstance();\n      spf.setSchema(schema);\n\
      \      SAXParser saxParser = spf.newSAXParser();\n      // To set the custom\
      \ entity resolver,\n      // an XML reader needs to be created\n      XMLReader\
      \ reader = saxParser.getXMLReader();\n      reader.setEntityResolver(new CustomResolver());\n\
      \      saxParser.parse(xmlStream, defHandler);\n    } catch (ParserConfigurationException\
      \ x) {\n      throw new IOException(\"Unable to validate XML\", x);\n    } catch\
      \ (SAXException x) {\n      throw new IOException(\"Invalid quantity\", x);\n\
      \    }\n    // Our XML is valid, proceed\n    outStream.write(xmlString.getBytes());\n\
      \    outStream.flush();\n  }\n}\n```\n\nUsing a schema or DTD to validate XML\
      \ is convenient when receiving XML that may have been loaded with unsanitized\
      \ input. If such an XML string has not yet been built, sanitizing input before\
      \ constructing XML yields better performance."
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 00. 输入验证和数据处理 (IDS)
      - type: DetailClassChin
        value: IDS16-J：预防XML注入
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 00. Input Validation and Data Sanitization (IDS)
      - type: DetailClassChin
        value: IDS16-J:Prevent XML Injection
    Default: true
    Description: "可扩展标记语言（XML）旨在帮助存储、构造和传输数据。由于其平台独立性、灵活性和相对简单性，XML已被广泛应用。然而，由于其多功能性，XML容易受到各种攻击，包括XML注入。\n\
      如果用户有能力使用结构化XML文档做为输入，那么他能够通过在数据字段中插入XML标签来重写这个XML文档的内容。允许用户指定可购买商品数量的在线商店应用程序可能会生成以下XML文档：\n\
      ```xml\n<item>\n  <description>Widget</description>\n  <price>500.0</price>\n\
      \  <quantity>1</quantity>\n</item>\n```\n攻击者可能会输入以下字符串，而不是数量计数：\n```xml\n1</quantity><price>1.0</price><quantity>1\n\
      ```\n在本例中，XML解析为以下内容：\n```java\n<item>\n  <description>Widget</description>\n\
      \  <price>500.0</price>\n  <quantity>1</quantity><price>1.0</price><quantity>1</quantity>\n\
      </item>\n```\nXML解析器可以解释本例中的XML，以便第二个价格字段覆盖第一个，从而将商品的价格更改为1美元。或者，攻击者可能会插入特殊字符，例如注释块和\
      \ `CDATA` 分隔符，这会破坏XML的含义。\n**不规范的代码样例1**：\n在这段代码示例中，客户端方法使用简单的字符串连接来构建XML查询以发送到服务器。在这时就有可能出现XML注入问题，因为这个方法没有进行任何输入验证。\n\
      ```java\nimport java.io.BufferedOutputStream;\nimport java.io.ByteArrayOutputStream;\n\
      import java.io.IOException;\n\npublic class OnlineStore {\n  private static\
      \ void createXMLStreamBad(final BufferedOutputStream outStream,\n      final\
      \ String quantity) throws IOException {\n    String xmlString = \"<item>\\n<description>Widget</description>\\\
      n\"\n        + \"<price>500</price>\\n\" + \"<quantity>\" + quantity\n     \
      \   + \"</quantity></item>\";\n    outStream.write(xmlString.getBytes());\n\
      \    outStream.flush();\n  }\n}\n```\n当XML可能已经载入还未处理的输入数据时，一般情况下使用XML模板或者DTD验证XML。如果还没有创建这样的XML字符串，那么在创建XML之前处理输入，这种方式性能较高。"
    Description_En: "The extensible markup language (XML) is designed to help store,\
      \ structure, and transfer data. Because of its platform independence, flexibility,\
      \ and relative simplicity, XML has found use in a wide range of applications.\
      \ However, because of its versatility, XML is vulnerable to a wide spectrum\
      \ of attacks, including *XML injection*.\n\nA user who has the ability to provide\
      \ input string data that is incorporated into an XML document can inject XML\
      \ tags. These tags are interpreted by the XML parser and may cause data to be\
      \ overridden.\n\nAn online store application that allows the user to specify\
      \ the quantity of an item available for purchase might generate the following\
      \ XML document:\n\n```java\n<item>\n  <description>Widget</description>\n  <price>500.0</price>\n\
      \  <quantity>1</quantity>\n</item>\n```\n\nAn attacker might input the following\
      \ string instead of a count for the quantity:\n\n```java\n1</quantity><price>1.0</price><quantity>1\n\
      ```\n\nIn this case, the XML resolves to the following:\n\n```java\n<item>\n\
      \  <description>Widget</description>\n  <price>500.0</price>\n  <quantity>1</quantity><price>1.0</price><quantity>1</quantity>\n\
      </item>\n```\n\nAn XML parser may interpret the XML in this example such that\
      \ the second price field overrides the first, changing the price of the item\
      \ to $1. Alternatively, the attacker may be able to inject special characters,\
      \ such as comment blocks and `CDATA` delimiters, which corrupt the meaning of\
      \ the XML.\n\n**Noncompliant Code Example**\n\nIn this noncompliant code example,\
      \ a client method uses simple string concatenation to build an XML query to\
      \ send to a server. XML injection is possible because the method performs no\
      \ input validation.\n\n```java\nimport java.io.BufferedOutputStream;\nimport\
      \ java.io.ByteArrayOutputStream;\nimport java.io.IOException;\n\npublic class\
      \ OnlineStore {\n  private static void createXMLStreamBad(final BufferedOutputStream\
      \ outStream,\n      final String quantity) throws IOException {\n    String\
      \ xmlString = \"<item>\\n<description>Widget</description>\\n\"\n        + \"\
      <price>500</price>\\n\" + \"<quantity>\" + quantity\n        + \"</quantity></item>\"\
      ;\n    outStream.write(xmlString.getBytes());\n    outStream.flush();\n  }\n\
      }\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 112:Missing XML Validation
      - type: CWE
        value: 91:XML Injection (aka Blind XPath Injection)
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: OWASP MOBILE TOP 10
        value: 2014:M7 Client Side Injection
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Injection [RST]
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: cert
    WhiteList: true
    id: '0102000010140216'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2（输入验证）**：  \n```java\npublic class OnlineStore\
      \ {\n    private static void createXMLStream(final BufferedOutputStream outStream,\
      \ final String quantity)\n            throws IOException, NumberFormatException\
      \ {\n        // Write XML string only if quantity is an unsigned integer (count).\n\
      \        int count = Integer.parseUnsignedInt(quantity);\n        String xmlString\
      \ = \"<item>\\n<description>Widget</description>\\n\" + \"<price>500</price>\\\
      n\" + \"<quantity>\"\n                + count + \"</quantity></item>\";\n  \
      \      outStream.write(xmlString.getBytes());\n        outStream.flush();\n\
      \    }\n}\n```  \n代码的解决方案是验证`quantity`是一个无符号整数。"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.1 数据清洗
      - type: DetailClassChin
        value: A.2.1.1.6 预防XML注入
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：如果用户有能力使用结构化XML文档作为输入，那么他能够通过在数据字段中插入XML标签来重写这个XML文档的内容。XML解析器会将这些标签按照正常标签进行解析。下面是一段在线商店的XML代码，主要用于查询后台数据库。\
      \  \n```xml\n<item>\n\t<description>Widget</description>\n\t<price>500.0</price>\n\
      \t<quantity>1</quantity>\n</item>\n```\n恶意用户可以在<quantity>元素中输入以下字符串：1</quantity><price>1.0</price><quantity>1\
      \  \n会生成以下的XML文档：  \n```xml\n<item>\n\t<description>Widget</description>\n\t\
      <price>500.0</price>\n\t<quantity>1</quantity><price>1.0</price><quantity>1</quantity>\n\
      </item>  \n```\n通过使用简单的API解析器（`org.xml.sax and javax.xml.parsers.SAXParser`）可以解析该XML文件，如果解析XML的代码获取的是最后一个元素`<price>`的值，那么商品价格就被设置为1.0。\
      \  \n对于预防XML注入的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\npublic class\
      \ OnlineStore {\n    private static void createXMLStreamBad(final BufferedOutputStream\
      \ outStream, final String quantity)\n            throws IOException {\n    \
      \    String xmlString = \"<item>\\n<description>Widget</description>\\n\" +\
      \ \"<price>500</price>\\n\" + \"<quantity>\"\n                + quantity + \"\
      </quantity></item>\";\n        outStream.write(xmlString.getBytes());\n    \
      \    outStream.flush();\n    }\n}\n```  \n上面的代码样例中，一个方法简单的使用了字符串拼接来创建一个XML查询，然后将其发送到服务器。在这时就有可能出现XML注入问题，因为这个方法没有进行任何输入验证。\
      \  \n当XML可能已经载入还未处理的输入数据时，一般情况下使用XML模板或者DTD验证XML。如果还没有创建这样的XML字符串，那么应在创建XML之前处理输入，这种方式性能较高。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 112:Missing XML Validation
      - type: CWE
        value: 91:XML Injection (aka Blind XPath Injection)
      - type: CERT
        value: IDS16-J:Prevent XML Injection
      - type: CERT
        value: IDS16-J:Prevent XML Injection,IDS17-J:Prevent XML External Entity Attacks
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: OWASP MOBILE TOP 10
        value: 2014:M7 Client Side Injection
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Injection [RST]
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170005'
    language: Java
  - Advice: "明确程序逻辑，判断是否使用`“.”`或者`“|”`作为正则表达式。  \n  \n**例如**：下列代码中,使用转义符对`“.”`或者`“|”`进行转义。\
      \  \n```java\npublic static void main(String[] args) {  \n\tString string =\
      \ \"hello. hi\";  \n\tString[] split = string.split(\"\\\\.\");  //返回结果为[hello,\
      \  hi]  \n\tString replaceAll = string.replaceAll(\"\\\\.\", \"a\");   //返回为helloa\
      \ hi  \n\tString abcd = \"ab|cd\".replaceAll(\"\\\\|\", \"/\");   //返回为ab/cd\
      \  \n\tString[] abcds= \"ab|cd\".split(\"\\\\|\");  //返回为[ab, cd]  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要使用“.”或者“|”作为正则表达式
    Categories_En:
      Category: []
    Default: true
    Description: "当使用`“.”`或者`“|”`作为正则表达式进行匹配时，会得到不符合预期的结果。  \n  \n**例如**：下列代码中，使用`“.”`或者`“|”`作为正则表达式进行匹配。\
      \  \n```java\npublic static void main(String[] args) {  \n\tString string =\
      \ \"hello. hi\";  \n\tString[] split = string.split(\".\");  //返回结果为空数组  \n\t\
      String replaceAll = string.replaceAll(\".\", \"a\");   //返回为aaaaaaaaa  \n\t\
      String abcd = \"ab|cd\".replaceAll(\"|\", \"/\");   //返回为/a/b/|/c/d/  \n\tString[]\
      \ abcds= \"ab|cd\".split(\"|\");  //返回为[, a, b, |, c, d]  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150450'
    language: Java
  - Advice: "**规范的代码样例1**：  \n```java\nfinal class Wrap {\n    private char[] dataArray;\n\
      \n    public Wrap() {\n        dataArray = new char[10];\n        // Initialize\n\
      \    }\n\n    public CharBuffer getBufferCopy() {\n        return CharBuffer.wrap(dataArray).asReadOnlyBuffer();\n\
      \    }\n}\n```  \n代码以一个只读形式返回一个字节数组的视图，在标准的类库中，可以保证一个对CharBuffer中元素的修改会导致一个`java.nio.ReadOnlyBufferException`异常。\
      \  \n**规范的代码样例2**：  \n```java\nfinal class Dup {\n    CharBuffer cb;\n\n   \
      \ public Dup() {\n        cb = CharBuffer.allocate(10);\n        // Initialize\n\
      \    }\n\n    public CharBuffer getBufferCopy() {\n        return cb.asReadOnlyBuffer();\n\
      \    }\n}\n```  \n返回一个只读视图。"
    Advice_En: "**Compliant Solution (`asReadOnlyBuffer()`)**\n\nThis compliant solution\
      \ returns a read-only view of the `char` array in the form of a read-only `CharBuffer`.\
      \ The standard library implementation of `CharBuffer` guarantees that attempts\
      \ to modify the elements of a read-only `CharBuffer` will result in a `java.nio.ReadOnlyBufferException`.\n\
      \n```java\nfinal class Wrap {\n  private char[] dataArray;\n\n  public Wrap()\
      \ {\n    dataArray = new char[10];\n    // Initialize\n  }\n\n  public CharBuffer\
      \ getBufferCopy() {\n    return CharBuffer.wrap(dataArray).asReadOnlyBuffer();\n\
      \  }\n}\n```\n\n**Compliant Solution (`asReadOnlyBuffer()`)**\n\nThis compliant\
      \ solution exposes a read-only view of the `CharBuffer` to untrusted code:\n\
      \n```java\nfinal class Dup {\n  CharBuffer cb;\n\n  public Dup() {\n    cb =\
      \ CharBuffer.allocate(10);\n    // Initialize\n  }\n\n  public CharBuffer getBufferCopy()\
      \ {\n    return cb.asReadOnlyBuffer();\n  }\n}\n```"
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO05-J：不要让使用wrap()或duplicate()创建缓存暴露给非受信代码
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO05-J:Do not expose buffers or their backing arrays methods to untrusted
          code
    Default: true
    Description: "Buffer类在java.nio包中定义，如IntBuffer、CharBuffer和ByteBuffer，同时还定义了一系列的`wrap()`方法来封装对应的基础数据类型，将其封装到一个缓冲区中，并返回一个Buffer对象来代表这个缓存区。这些方法创建了一个新的Buffer对象，新的Buffer对象由指定的输入数组支持。根据JavaAPI文档对这些方法的描述：新的缓存区由指定的字符数组来支持，也就是说，对缓冲区的修改会导致数值的修改，反之亦然。\
      \  \n将这些缓冲区暴露给非受信的代码，背后的数组也会暴露，可能会遭到恶意的修改。同样，`duplicate()`创建了额外的缓存区，这个缓存区的背后是支持原始缓冲区的数组。暴露额外的缓冲区给非受信的代码，同样这个缓存区也会暴露。\
      \  \n风险评估：违反该条准则会导致非受信的代码修改原始数据。    \n**不规范的代码样例1**：  \n```java\nfinal class\
      \ Wrap {\n    private char[] dataArray;\n\n    public Wrap() {\n        dataArray\
      \ = new char[10];\n        // Initialize\n    }\n\n    public CharBuffer getBufferCopy()\
      \ {\n        return CharBuffer.wrap(dataArray);\n    }\n}\n```  \n代码声明了一个char数组，将其封装在一个缓存区里，并且将那个缓存区通过`getBufferCopy()`方法暴露给非受信代码。\
      \  \n**不规范的代码样例2**：  \n```java\nfinal class Dup {\n    CharBuffer cb;\n\n  \
      \  public Dup() {\n        cb = CharBuffer.allocate(10);\n        // Initialize\n\
      \    }\n\n    public CharBuffer getBufferCopy() {\n        return cb.duplicate();\n\
      \    }\n}\n```\n代码调用`duplicate()`方法来创建并返回一个对CharBuffer的复制。"
    Description_En: "Buffer classes defined in the java.nio package, such as IntBuffer,\
      \ CharBuffer, and ByteBuffer, define a variety of methods that wrap an array\
      \ (or a portion of the array) of the corresponding primitive data type into\
      \ a buffer and return the buffer as a Buffer object. Although these methods\
      \ create a new Buffer object, the new Buffer is backed by the given input array.\
      \ According to the Java API for these methods [API 2014],\n\n> The new buffer\
      \ will be backed by the given character array; that is, modifications to the\
      \ buffer will cause the array to be modified and vice versa.\n\nExposing these\
      \ buffers to untrusted code exposes the backing array of the original buffer\
      \ to malicious modification. Likewise, the `duplicate()`, `array()`, `slice()`,\
      \ and `subsequence()` methods create additional buffers that are backed by the\
      \ original buffer's backing array; exposing such additional buffers to untrusted\
      \ code affords the same opportunity for malicious modification.\nThis rule is\
      \ an instance of OBJ06-J. Defensively copy mutable inputs and mutable internal\
      \ components.\n**Noncompliant Code Example (`wrap()`)**\n\nThis noncompliant\
      \ code example declares a `char` array, wraps it within a `CharBuffer`, and\
      \ exposes that `CharBuffer` to untrusted code via the `getBufferCopy()` method:\n\
      \n```java\nfinal class Wrap {\n  private char[] dataArray;\n\n  public Wrap()\
      \ {\n    dataArray = new char[10];\n    // Initialize\n  }\n\n  public CharBuffer\
      \ getBufferCopy() {\n    return CharBuffer.wrap(dataArray);\n  }\n}\n```\n**Noncompliant\
      \ Code Example (`duplicate()`)**\n\nThis noncompliant code example invokes the\
      \ `duplicate()` method to create and return a copy of the `CharBuffer`. As stated\
      \ in the contract for the `duplicate()` method, the returned buffer is backed\
      \ by the same array as is the original buffer. Consequently, if a caller were\
      \ to modify the elements of the backing array, these modifications would also\
      \ affect the original buffer.\n\n```java\nfinal class Dup {\n  CharBuffer cb;\n\
      \n  public Dup() {\n    cb = CharBuffer.allocate(10);\n    // Initialize\n \
      \ }\n\n  public CharBuffer getBufferCopy() {\n    return cb.duplicate();\n \
      \ }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141405'
    language: Java
  - Advice: "对应的符合规则的解决方案是：以一个只读的 CharBuffer 形式返回一个字节数组的视图。在标准类库中，可以保证对一个 CharBuffer\
      \ 中元素的修改会导致一个 `java.nio.ReadOnlyBufferException` 异常。   \n    ```java\n    final\
      \ class Wrap {\n        private char[] dataArray;\n    \n        public Wrap()\
      \ {\n            dataArray = new char[10];\n            // Initialize\n    \
      \    }\n    \n        public CharBuffer getBufferCopy() {\n            return\
      \ CharBuffer.wrap(dataArray).asReadOnlyBuffer();\n        }\n    }\n    ```\
      \   \n    **例 2** 这个不符合规则的代码示例调用`duplicate()`方法来创建和返回一个对 CharBuffer 的复制。正如在`duplicate()`方法中描述的一样，返回的缓存区与原始的缓存区有同样数组在背后支持，对其修改同样会影响到原始的缓存区。\
      \   \n    ```java\n    final class Dup {\n        CharBuffer cb;\n    \n   \
      \     public Dup() {\n            cb = CharBuffer.allocate(10);\n          \
      \  // Initialize\n        }\n    \n        public CharBuffer getBufferCopy()\
      \ {\n            return cb.duplicate();\n        }\n    }\n    ```  \n     \
      \  \n    对应的符合规则的解决方案是：将一个 CharBuffer 的只读视图暴露给非受信的代码。   \n    ```java\n    final\
      \ class Dup {\n        CharBuffer cb;\n    \n        public Dup() {\n      \
      \      cb = CharBuffer.allocate(10);\n            // Initialize\n        }\n\
      \    \n        public CharBuffer getBufferCopy() {\n            return cb.asReadOnlyBuffer();\n\
      \        }\n    }\n    ```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 7.5 文件系统
      - type: DetailClassChin
        value: C.13.6 不要使用wrap（）或duplicate（）创建缓存，并将这些缓存暴露给非受信代码
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n不要使用 `wrap()`和 `duplicate()`进行缓存创建，防止将缓存暴露给非受信代码。   \n\
      2) 原理   \nBuffer 类在 java.nio 包中定义，例如 IntBuffer、CharBuffer、和 ByteBuffer，同时还定义了一系列的\
      \ `wrap()`方法来封装对应的基础数据类型的数据，将其封装到一个缓存区中，并返回一个 Buffer 对象来代表这个缓存区。这些方法创建了一个新的\
      \ Buffer 对象，新的 Buffer 其实由指定的输入数组支持。   \n新的缓存区由指定的字符数组来支持，也就是说，对缓存区的修改会导致数值的修改，反之亦然。\
      \   \n将这些数组暴露给非受信的代码，会将其背后的数组也暴露给恶意的修改。同样, `duplicate()`创建了额外的缓存区，这个缓存区背后是支持原始缓存区的数组。暴露这个额外的缓存区给非受信的代码，同样将其暴露给恶意的修改。\
      \   \n3) 示例   \n    **例 1** 这个不符合规则的代码示例声明了一个 char 数组，将其封装在一个缓存区里，并且将那个缓存区通过\
      \ `getBufferCopy()`方法暴露给非受信的代码。   \n    ```java\n    final class Wrap {\n  \
      \      private char[] dataArray;\n    \n        public Wrap() {\n          \
      \  dataArray = new char[10];\n            // Initialize\n        }\n    \n \
      \       public CharBuffer getBufferCopy() {\n            return CharBuffer.wrap(dataArray);\n\
      \        }\n    }\n    ```  \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160111
    language: Java
  - Advice: "内部类声明为非public，并且内部类中的public方法、public构造方法不能存在访问外部类private字段的操作。  \n**规范的代码样例1**：\
      \  \n```java\nclass Coordinates {\n    private int x;\n    private int y;\n\n\
      \    private class Point {\n        private void getPoint() {\n            System.out.println(\"\
      (\" + x + \",\" + y + \")\");\n        }\n    }\n}\n```"
    Advice_En: "**Compliant Solution**\n\nUse the `private` access specifier to hide\
      \ the inner class and all contained methods and constructors.\n\n```java\nclass\
      \ Coordinates {\n  private int x;\n  private int y;\n\n  private class Point\
      \ {\n    private void getPoint() {\n      System.out.println(\"(\" + x + \"\
      ,\" + y + \")\");\n    }\n  }\n}\n\nclass AnotherClass {\n  public static void\
      \ main(String[] args) {\n    Coordinates c = new Coordinates();\n    Coordinates.Point\
      \ p = c.new Point();    // Fails to compile\n    p.getPoint();\n  }\n}\n```\n\
      \nCompilation of `AnotherClass` now results in a compilation error because the\
      \ class attempts to access a private nested class."
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 05. 面向对象 (OBJ)
      - type: DetailClassChin
        value: OBJ08-J：不要在内部类中暴露外部类的私有字段
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 05. Object Orientation (OBJ)
      - type: DetailClassChin
        value: OBJ08-J:Do not expose private members of an outer class from within
          a nested class
    Default: true
    Description: "内部类指那些声明在另一个类或者接口代码块中的任何类。使用内部类容易出错，除非语义很容易理解。一个通用的形式是，仅有内部类可以访问外部类的内容。不仅内部类可以访问外部类的私有字段，而且，当前内部类被声明为public或者它包含public方法或者构造方法时，私有字段也可以通过这些方法而被任何在同一个包里的其他类访问。\
      \  \n根据Java语言规范的8.3节“数据成员声明”的描述：  \n>注意，一个基类的私有字段可以被它的一个子类访问（例如：如果这两个类是同一个类的成员）。然而，私有字段是不能被子类继承的。\
      \  \n\n风险评估：当出现一个内部类的时候，Java语言系统弱化了对外部类的私有成员的访问性，从而会导致信息泄露。  \n**不规范的代码样例1**：\
      \  \n内部类代码通过`getPoint()`方法将外部类的私有的x，y变量，暴露给了与Coordinates同一包下面的其他的类AnotherClass。\n\
      ```java\nclass Coordinates {\n  private int x;\n  private int y;\n\n  public\
      \ class Point {\n    public void getPoint() {\n      System.out.println(\"(\"\
      \ + x + \",\" + y + \")\");\n    }\n  }\n}\n\nclass AnotherClass {\n  public\
      \ static void main(String[] args) {\n    Coordinates c = new Coordinates();\n\
      \    Coordinates.Point p = c.new Point();\n    p.getPoint();\n  }\n}\n```"
    Description_En: "A nested class is any class whose declaration occurs within the\
      \ body of another class or interface [JLS 2015]. The use of a nested class is\
      \ error prone unless the semantics are well understood. A common notion is that\
      \ only the nested class may access the contents of the outer class. Not only\
      \ does the nested class have access to the private fields of the outer class,\
      \ but the same fields can be accessed by any other class within the package\
      \ when the nested class is declared public or if it contains public methods\
      \ or constructors. As a result, the nested class must not expose the private\
      \ members of the outer class to external classes or packages.\n\nAccording to\
      \ The Java Language Specification (JLS), §8.3, \"Field Declarations\" [JLS 2015]:\n\
      \n> Note that a private field of a superclass might be accessible to a subclass\
      \ (for example, if both classes are members of the same class). Nevertheless,\
      \ a private field is never inherited by a subclass.\n\n**Noncompliant Code Example**\n\
      \nThis noncompliant code example exposes the private `(x,y)` coordinates through\
      \ the `getPoint()` method of the inner class. Consequently, the `AnotherClass`\
      \ class that belongs to the same package can also access the coordinates.\n\n\
      ```java\nclass Coordinates {\n  private int x;\n  private int y;\n\n  public\
      \ class Point {\n    public void getPoint() {\n      System.out.println(\"(\"\
      \ + x + \",\" + y + \")\");\n    }\n  }\n}\n\nclass AnotherClass {\n  public\
      \ static void main(String[] args) {\n    Coordinates c = new Coordinates();\n\
      \    Coordinates.Point p = c.new Point();\n    p.getPoint();\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 492:Use of Inner Class Containing Sensitive Data
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140608
    language: Java
  - Advice: "对应的符合规则的解决方案是：使用私有的访问描述符来隐藏内部类以及所有包含的方法和构造函数。   \n    ```java\n    class\
      \ Coordinates {\n        private int x;\n        private int y;\n    \n    \
      \    private class Point {\n            private void getPoint() {\n        \
      \        System.out.println(\"(\" + x + \",\" + y + \")\");\n            }\n\
      \        }\n    }\n    class AnotherClass {\n        public static void main(String[]\
      \ args) {\n            Coordinates c = new Coordinates();\n            Coordinates.Point\
      \ p = c.new Point();\n            p.getPoint();\n        }\n    }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.5.9 不要在嵌套类中暴露外部类的私有字段
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n禁止嵌套类将外部类的私有成员暴露给外部的类或者包。   \n2) 原理   \n嵌套类是指那些声明在另一个类或者接口代码块中的任何类。使用嵌套类易出错，除非其语义易理解。一个通用形式是，仅有嵌套类可以访问外部类的内容。不仅嵌套类可以访问外部类的私有字段，当嵌套类被声明为公有或者当它包含公有的方法或者构造方法时，同样的字段也可以被任何在同一个包里的其他类访问。因此，嵌套类禁止将外部类的私有成员暴露给外部的类或者包。\
      \   \n3) 示例   \n    1、不符合规则的代码示例，该示例将私有的（x, y)坐标通过`getPoint()`方法暴露给内部类。因此，属于同一个包的\
      \ AnotherClass 类同样可以访问这些坐标。   \n    ```java\n    class Coordinates {\n     \
      \   private int x;\n        private int y;\n    \n        public class Point\
      \ {\n            public void getPoint() {\n                System.out.println(\"\
      (\" + x + \",\" + y + \")\");\n            }\n        }\n    }\n    \n    class\
      \ AnotherClass {\n        public static void main(String[] args) {\n       \
      \     Coordinates c = new Coordinates();\n            Coordinates.Point p =\
      \ c.new Point();\n            p.getPoint();\n        }\n    }\n    ```   \n\
      \    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference:
      - type: CWE
        value: 492:Use of Inner Class Containing Sensitive Data
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160046
    language: Java
  - Advice: "**规范的代码样例1**：\n代码通过一个安全目录下的外部文件得到服务器IP地址。通过使用后立即清除内存中的服务器IP，可以防止进一步泄露。\
      \  \n```java\nclass IPaddress {\n    public static void main(String[] args)\
      \ throws IOException {\n        char[] ipAddress = new char[100];\n        BufferedReader\
      \ br = new BufferedReader(\n                new InputStreamReader(new FileInputStream(\"\
      serveripaddress.txt\")));\n        // Reads the server IP address into the char\
      \ array,\n        // returns the number of bytes read\n        int n = br.read(ipAddress);\n\
      \        // Validate server IP address\n        // Manually clear out the server\
      \ IP address\n        // immediately after use\n        for (int i = n - 1;\
      \ i >= 0; i--) {\n            ipAddress[i] = 0;\n        }\n        br.close();\n\
      \    }\n}\n```  \n**规范的代码样例2**：  \n从一个位于安全目录下的配置文件中读取用户名和密码。  \n```java\npublic\
      \ final Connection getConnection() throws SQLException {\n  String username;\n\
      \  String password;\n  // Username and password are read at runtime from a secure\
      \ config file\n  return DriverManager.getConnection(\n      \"jdbc:mysql://localhost/dbName\"\
      , username, password);\n}\n```\n在运行时提示用户输入用户名和密码也是允许的。\n如果可能的话，像密码这样的敏感信息应该存储在字符数组中，而不是字符串中，因为Java虚拟机可能会在不再需要字符串后保留很长时间。然而，这个例子使用了字符串，因为`DriverManager.getConnection()`需要它们。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution retrieves the server\
      \ IP address from an external file located in a secure directory, as recommended\
      \ by FIO00-J. Do not operate on files in shared directories. It reads the file\
      \ in compliance with FIO10-J. Ensure the array is filled when using read() to\
      \ fill an array. Exposure of the IP address is further limited by storing it\
      \ in a char array rather than a java.lang.String, and by clearing the server\
      \ IP address from memory immediately after use.\n\n```java\nclass IPaddress\
      \ {\n  public static void main(String[] args) throws IOException {\n    char[]\
      \ ipAddress = new char[100];\n    int offset = 0;\n    int charsRead = 0;\n\
      \    BufferedReader br = null;\n    try {\n      br = new BufferedReader(new\
      \ InputStreamReader(\n             new FileInputStream(\"serveripaddress.txt\"\
      )));\n      while ((charsRead = br.read(ipAddress, offset, ipAddress.length\
      \ - offset))\n          != -1) {\n        offset += charsRead;\n        if (offset\
      \ >= ipAddress.length) {\n          break;\n        }\n      }\n\n      // ...\
      \ Work with IP address\n\n    } finally {\n      Arrays.fill(ipAddress,  (byte)\
      \ 0);\n      br.close();\n    }\n  }\n}\n```\n\nTo further limit the exposure\
      \ time of the sensitive server IP address, replace `BufferedReader` with a direct\
      \ native input/output (NIO) buffer, which can be cleared immediately after use.\n\
      \n**Compliant Solution**\n\nThis compliant solution reads the user name and\
      \ password from a configuration file located in a secure directory:\n\n```java\n\
      public final Connection getConnection() throws SQLException {\n  String username;\n\
      \  String password;\n  // Username and password are read at runtime from a secure\
      \ config file\n  return DriverManager.getConnection(\n      \"jdbc:mysql://localhost/dbName\"\
      , username, password);\n}\n```\n\nIt is also permissible to prompt the user\
      \ for the user name and password at runtime.\n\nWhen possible, sensitive information\
      \ such as passwords should be stored in character arrays rather than strings\
      \ because the Java Virtual Machine may retain strings long after they are no\
      \ longer needed. However, this example uses strings because `DriverManager.getConnection()`\
      \ requires them."
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 49. 其他 (MSC)
      - type: DetailClassChin
        value: MSC03-J：不要硬编码敏感信息
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 49. Miscellaneous (MSC)
      - type: DetailClassChin
        value: MSC03-J:Never hard code sensitive information
    Default: true
    Description: "硬编码如密码、服务器IP地址、加密密匙这样的敏感信息，会将信息暴露给攻击者。任何一个可以访问类文件的人都可以对其进行反编译，然后得到敏感信息。因此，程序不能对敏感信息进行硬编码。对敏感信息进行硬编码会增加对代码进行管理和接纳变更的需要。**例如**：在一个已部署的程序中，改变其硬编码密码需要发布补丁。\
      \  \n风险评估：对敏感数据进行硬编码会向攻击者泄露信息。  \n**不规范的代码样例1**：  \n代码在一个String常量中对服务器IP地址进行了硬编码。\
      \  \n```java\nclass IPaddress {\n    String ipAddress = new String(\"172.16.254.1\"\
      );\n\n    public static void main(String[] args) {\n        // ...\n    }\n\
      }\n```  \n使用java反编译工具（最简单的：javap -c IPaddress）进行反汇编，从而可以得到硬编码的服务器IP地址。\n```\n\
      Compiled from \"IPaddress.java\"  \nclass IPaddress extends java.lang.Object{\
      \  \njava.lang.String ipAddress;  \nIPaddress();  \nCode:  \n0: aload_0  \n\
      1: invokespecial #1; //Method java/lang/Object.\"<init>\":()V  \n4: aload_0\
      \  \n5: new #2; //class java/lang/String  \n8: dup  \n9: ldc #3; //String 172.16.254.1\
      \  \n11: invokespecial #4; //Method java/lang/String.\"<init>\":(Ljava/lang/String;)V\
      \  \n14: putfield #5; //Field ipAddress:Ljava/lang/String;  \n17: return  \n\
      public static void main(java.lang.String[]);  \nCode:  \n0: return  \n}  \n\
      ```  \n**不规范的代码样例2**：  \n在创建数据库连接的时候，对用户名和密码进了硬编码。  \n\n```java\npublic final\
      \ Connection getConnection() throws SQLException {\n  return DriverManager.getConnection(\n\
      \      \"jdbc:mysql://localhost/dbName\",\n      \"username\", \"password\"\
      );\n}\n```"
    Description_En: "Hard coding sensitive information, such as passwords, server\
      \ IP addresses, and encryption keys can expose the information to attackers.\
      \ Anyone who has access to the class files can decompile them and discover the\
      \ sensitive information. Leaking data protected by International Traffic in\
      \ Arms Regulations (ITAR) or the Health Insurance Portability and Accountability\
      \ Act (HIPAA) can also have legal consequences. Consequently, programs must\
      \ not hard code sensitive information.\n\nHard coding sensitive information\
      \ also increases the need to manage and accommodate changes to the code. For\
      \ example, changing a hard-coded password in a deployed program may require\
      \ distribution of a patch [Chess 2007].\n\n**Noncompliant Code Example**\n\n\
      This noncompliant code example includes a hard-coded server IP address in a\
      \ constant `String`:\n\n```java\nclass IPaddress {\n  String ipAddress = new\
      \ String(\"172.16.254.1\");\n  public static void main(String[] args) {\n  \
      \  //...\n  }\n}\n```\n\nA malicious user can use the `javap -c IPaddress` command\
      \ to disassemble the class and discover the hard-coded server IP address. The\
      \ output of the disassembler reveals the server IP address `172.16.254.1` in\
      \ clear text:\n\n```\nCompiled from \"IPaddress.java\"\nclass IPaddress extends\
      \ java.lang.Object{\njava.lang.String ipAddress;\n\nIPaddress();\n  Code:\n\
      \   0:     aload_0\n   1:     invokespecial     #1; //Method java/lang/Object.\"\
      <init>\":()V\n   4:     aload_0\n   5:     new   #2; //class java/lang/String\n\
      \   8:     dup\n   9:     ldc   #3; //String 172.16.254.1\n   11:    invokespecial\
      \     #4; //Method java/lang/String.\"<init>\":(Ljava/lang/String;)V\n   14:\
      \    putfield    #5; //Field ipAddress:Ljava/lang/String;\n   17:    return\n\
      \npublic static void main(java.lang.String[]);\n  Code:\n   0:     return\n\n\
      }\n```\n\n**Noncompliant Code Example (Hard-Coded Database Password)**\n\nThe\
      \ user name and password fields in the SQL connection request are hard coded\
      \ in this noncompliant code example:\n\n```java\npublic final Connection getConnection()\
      \ throws SQLException {\n  return DriverManager.getConnection(\n      \"jdbc:mysql://localhost/dbName\"\
      ,\n      \"username\", \"password\");\n}\n```\n\nNote that the one- and two-argument\
      \ `java.sql.DriverManager.getConnection()` methods can also be used incorrectly."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 798:Use of Hard-coded Credentials
      - type: CWE
        value: 336:Same Seed in Pseudo-Random Number Generator (PRNG)
      - type: CWE
        value: 330:Use of Insufficiently Random Values
      - type: CWE
        value: 326:Inadequate Encryption Strength
      - type: CWE
        value: 321:Use of Hard-coded Cryptographic Key
      - type: CWE
        value: 259:Use of Hard-coded Password
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: OWASP MOBILE TOP 10
        value: 2014:M6 Broken Cryptography
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2010:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 330
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 259
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Hard-coded Password [XYP]
      - type: GB/T 34944-2017 Java
        value: 安全功能:口令硬编码
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010141803
    language: Java
  - Advice: "对应的符合规则的代码示例：通过位于一个安全目录下的外部文件得到服务器的 IP 地址，使用后立即清除内存中的服务器 IP 地址，以防止泄漏。\
      \   \n    ```java\n    class IPaddress {   \n        public static void main(String[]\
      \ args) throws IOException { \n            char[] ipAddress = new char[100];\
      \   \n            BufferedReader br = new BufferedReader(new InputStreamReader(\
      \ new   \n            FilelnputStreara(\"serveripaddress.txt\")));   \n    \
      \        // Reads the server IP address into the char array,   \n          \
      \  // returns the number of bytes read int n = br.read(ipAddress);   \n    \
      \        // Validate server IP address // Manually clear out the server IP address\
      \ // immediately after use \n            for (int i = n  - 1; i >= 0; i --)\
      \ {   \n                ipAddress[i] = 0;   \n            }   \n           \
      \ br.close();   \n        }   \n    }\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 5.3 数据加密与保护
      - type: DetailClassChin
        value: C.17.4 不要硬编码敏感信息
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n不要硬编码敏感信息。   \n2) 原理   \n对密码、服务器 IP 地址和加密密钥这样的敏感信息进行硬编码，会导致信息泄露。任何一个能够访问类文件的人都可以对其进行反编译，从而得到敏感信息。因此，程序不能对敏感信息进行硬编码。\
      \   \n对敏感信息进行硬编码，会引起代码管理和变更的负担。   \n3) 示例   \n    1.这个不符合规则的代码示例在一个 String 常量中对服务器\
      \ IP 地址进行了硬编码。   \n    ```java\n    class IPaddress {   \n        String ipAddress\
      \ = new String (\"172.15.254.1\"); \n        public static void main(String[]\
      \ args) {\n    ```\n    攻击者使用 javap-cIPaddress 命令将类反汇编，得到服务器 IP 地址。    \n  \
      \  ```java\n    Compiled from \"IPaddress.java\"\n    class IPaddress extends\
      \ java.lang.Object{   \n    java.lang.String ipAddress;   \n    IPaddress();\
      \   \n    Code:   \n    0: aload_0   \n    1: invokespecial #1; //Method java/lang/Object.\"\
      \\<init\\>\":()V 4: aload_0   \n    5: new #2; //class java/lang/String   \n\
      \    8: dup   \n    9: ldc #3; //String 172.15.254.1   \n    11: invokespecial\
      \ #4 ; //Method java/lang/String.u\\<init\\>\": (Ljava/lang/   \n    String;)V\
      \   \n    14: putfield #5; //Field ipAddress:Ljava/lang/String;   \n    17:\
      \ return   \n    public static void main(java.lang.String[]);   \n    Code:\
      \   \n    0: return   \n    }  \n    ```   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 798:Use of Hard-coded Credentials
      - type: CWE
        value: 336:Same Seed in Pseudo-Random Number Generator (PRNG)
      - type: CWE
        value: 330:Use of Insufficiently Random Values
      - type: CWE
        value: 326:Inadequate Encryption Strength
      - type: CWE
        value: 321:Use of Hard-coded Cryptographic Key
      - type: CWE
        value: 259:Use of Hard-coded Password
      - type: CERT
        value: MSC03-J:Never hard code sensitive information
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: OWASP MOBILE TOP 10
        value: 2014:M6 Broken Cryptography
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2010:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 330
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 259
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Hard-coded Password [XYP]
      - type: GB/T 34944-2017 Java
        value: 安全功能:口令硬编码
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160151
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nclass IPaddress {\n   \
      \ public static void main(String[] args) throws IOException {\n        char[]\
      \ ipAddress = new char[100];\n        BufferedReader br = new BufferedReader(new\
      \ InputStreamReader(new FileInputStream(\"serveripaddress.txt\")));\n      \
      \  // Reads the server IP address into the char array,\n        // returns the\
      \ number of bytes read\n        int n = br.read(ipAddress);\n        // Validate\
      \ server IP address\n        // Manually clear out the server IP address\n \
      \       // immediately after use\n        for (int i = n - 1; i >= 0; i--) {\n\
      \            ipAddress[i] = 0;\n        }\n        br.close();\n    }\n}\n```\
      \  \n代码通过一个安全目录下的外部文件得到服务器IP地址。通过使用后立即清除内存中的服务器IP，可以防止进一步泄露。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.2 数据加密与保护
      - type: DetailClassChin
        value: A.2.2.2.3 不要硬编码敏感信息
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：硬编码如密码、服务器IP地址、加密密匙这样的敏感信息，会将信息暴露给攻击者。任何一个可以访问类文件的人都可以对其进行反编译，然后得到敏感信息。因此，程序不能对敏感信息进行硬编码。对敏感信息进行硬编码会使代码管理变得更复杂。例如，在一个已部署的程序中，改变其硬编码密码需要发布补丁。对敏感数据进行硬编码会向攻击者泄露信息。\
      \  \n对于不要硬编码敏感信息的情况，示例1、给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nclass IPaddress\
      \ {  \n    String ipAddress = new String(\"172.16.254.1\");  \n    public static\
      \ void main(String[] args) {  \n        // ...  \n    }  \n}\n```  \n代码在一个String常量中对服务器IP地址进行了硬编码。使用java反编译工具（例如：javap\
      \ -c IPaddress）进行反汇编，从而可以得到硬编码的服务器IP地址。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 798:Use of Hard-coded Credentials
      - type: CWE
        value: 336:Same Seed in Pseudo-Random Number Generator (PRNG)
      - type: CWE
        value: 330:Use of Insufficiently Random Values
      - type: CWE
        value: 326:Inadequate Encryption Strength
      - type: CWE
        value: 259:Use of Hard-coded Password
      - type: CERT
        value: MSC03-J:Never hard code sensitive information
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2010:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 330
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 259
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Hard-coded Password [XYP]
      - type: GB/T 34944-2017 Java
        value: 安全功能:口令硬编码
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: 0302000010170018
    language: Java
  - Advice: "检查代码逻辑，将transient标识符移除或将变量所在类继承Serializable接口。  \n  \n**例如**：下面代码示例中，transient修饰的`name`所在类User可序列化。\
      \  \n```java\npublic class User implements Serializable{  \n    transient private\
      \ String name;  \n    private int age;  \n    ...  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】transient修饰的变量所在类应该可序列化
    Categories_En:
      Category: []
    Default: true
    Description: "用transient关键字标记的成员变量不参与序列化过程，而当变量所在类不可序列化时，transient的修饰毫无意义。  \n\
      \  \n**例如**：下面代码示例中，transient修饰的`name`所在类User不可序列化。  \n```java\npublic class\
      \ User{  \n    transient private String name;  \n    private int age;  \n  \
      \  ...  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: correctness
    Type: std
    WhiteList: not
    id: '0202000010150455'
    language: Java
  - Advice: "检查代码逻辑，判断`HttpSecurity.authorizeRequests()`方法上配置的URL模式执行顺序。  \n  \n**例如**：下面代码示例中`/admin/login`配置在`/admin/**`前面。\
      \  \n```java\nhttp.authorizeRequests()  \n    .antMatchers(\"/resources/**\"\
      , \"/signup\", \"/about\").permitAll()  \n    .antMatchers(\"/admin/login\"\
      ).permitAll()  \n    .antMatchers(\"/admin/**\").hasRole(\"ADMIN\")      \n\
      \    .antMatchers(\"/db/**\").access(\"hasRole('ADMIN') and hasRole('DBA')\"\
      )  \n    .antMatchers(\"/**\").permitAll().and().formLogin()  \n    .loginPage(\"\
      /login\").permitAll().and().logout().permitAll();\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】HttpSecurity配置的URL应该正确排序
    Categories_En:
      Category: []
    Default: true
    Description: "在`HttpSecurity.authorizeRequests()`方法上配置的URL模式将会按照声明的顺序进行匹配，过多的配置容易犯错，导致作用域大的在作用域小的前面。\
      \  \n  \n**例如**：下面代码示例中`/admin/**`配置在`/admin/login`前面。  \n```java\nhttp.authorizeRequests()\
      \  \n    .antMatchers(\"/resources/**\", \"/signup\", \"/about\").permitAll()\
      \  \n    .antMatchers(\"/admin/**\").hasRole(\"ADMIN\")  \n    .antMatchers(\"\
      /admin/login\").permitAll()  \n    .antMatchers(\"/**\").permitAll()  \n   \
      \ .antMatchers(\"/db/**\").access(\"hasRole('ADMIN') and hasRole('DBA')\").and().formLogin()\
      \  \n    .loginPage(\"/login\").permitAll().and().logout().permitAll();\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: OWASP TOP 10
        value: 2017:A6 Security Misconfiguration
      - type: OWASP TOP 10
        value: 2013:A5 Security Misconfiguration
      - type: OWASP TOP 10
        value: 2010:A6 Security Misconfiguration
    Security: not
    Tag: correctness
    Type: std
    WhiteList: not
    id: '0202000010150446'
    language: Java
  - Advice: "可以将所有的for语句中循环变量声明为final。这个final声明可以让Java编译器做一个标志，并且拒绝对这个循环变量的任何赋值。 \
      \ \n**规范的代码样例1**：  \n下面规范的代码将Integer声明为final类型。  \n ```java\n// ...  \n for\
      \ (final Integer i: list) {  \n // ...  \n }  \n // ...\n```   \n**规范的代码样例2**：\
      \  \n下面符合规范的代码处理集合，但是保持实际的集合不变。  \n  \n```java\n// ...    \nfor (final Integer\
      \ i : list) {\n    Integer item = i;\n    if (first) {\n        first = false;\n\
      \        item = new Integer(99);\n    }\n    System.out.println(\" New item:\
      \ \" + item);\n    // Process item\n}    \n// ...\n```"
    Advice_En: "**Compliant Solution**\n\nDeclaring `i` to be final mitigates this\
      \ problem by causing the compiler to fail to permit `i` to be assigned a new\
      \ value:\n\n```java\n// ...\nfor (final Integer i: list) {\n\n// ...\n```\n\n\
      **Compliant Solution**\n\nThis compliant solution processes the \"modified\"\
      \ list but leaves the actual list unchanged:\n\n```java\n// ...\n\nfor (final\
      \ Integer i: list) {\n  Integer item = i;\n  if (first) {\n    first = false;\n\
      \    item = new Integer(99);\n  }\n  System.out.println(\" New item: \" + item);\n\
      \  // Process item\n}\n\n// ...\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 01. 声明和初始化 (DCL)
      - type: DetailClassChin
        value: DCL02-J：当使用增强型for语句时不要修改集合元素
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 01. Declarations and Initialization (DCL)
      - type: DetailClassChin
        value: DCL02-J:Do not modify the collection's elements during an enhanced
          for statement
    Default: true
    Description: "增强的for语句是为通过集合和数组进行迭代而设计的。\nJava语言规范（JLS）在§14.14.2 \"增强型for语句\"\
      [JLS 2014]中提供了增强型for语句的如下示例。\n```java\nfor (I #i = Expression.iterator(); #i.hasNext();\
      \ ) {\n    {VariableModifier} TargetType Identifier =\n        (TargetType)\
      \ #i.next();\n    Statement\n}\n```\n#i是一个自动生成的标识符，它与任何其他标识符（自动生成或其他）不同，这些标识符作用在增强for语句范围内。\n\
      增强的for语句等同于基本for语句的形式。\nJava 5平台引入了增强的for语句，用来对对象集合进行迭代。与基本的for语句不同，在基本的for语句中，给循环变量赋值是不能对循环的迭代次序有所影响，但在增强的for语句中，给循环变量赋值就可以有影响。\n\
      **不规范的代码样例1**：  \n以下代码尝试使用增强型for循环来处理整数集合，并且打算修改集合中的某一条目。  \n```java\nList<Integer>\
      \ list = Arrays.asList(new Integer[] { 13, 14, 15 });\nboolean first = true;\n\
      System.out.println(\"Processing list...\");\nfor (Integer i : list) {\n    if\
      \ (first) {\n        first = false;\n        i = new Integer(99);\n    }\n \
      \   System.out.println(\" New item: \" + i);\n    // Process i\n}\nSystem.out.println(\"\
      Modified list?\");\nfor (Integer i : list) {\n    System.out.println(\"List\
      \ item: \" + i);\n}\n```\n但是，该代码并没有修改列表，其输出如下：  \n```\nProcessing list... \n\
      New item: 99  \nNew item: 14  \nNew item: 15  \nModified list?  \nList item:\
      \ 13  \nList item: 14  \nList item: 15\n```"
    Description_En: "The enhanced for statement is designed for iteration through\
      \ Collections and arrays.\n\nThe Java Language Specification (JLS) provides\
      \ the following example of the enhanced for statement in §14.14.2, \"The Enhanced\
      \ for Statement\" [JLS 2014]:\nThe enhanced for statement is equivalent to a\
      \ basic for statement of the form:\n\n```java\nfor (I #i = Expression.iterator();\
      \ #i.hasNext(); ) {\n    {VariableModifier} TargetType Identifier =\n      \
      \  (TargetType) #i.next();\n    Statement\n}\n```\n\n#i is an automatically\
      \ generated identifier that is distinct from any other identifiers (automatically\
      \ generated or otherwise) that are in scope...at the point where the enhanced\
      \ for statement occurs.\n\nUnlike the basic `for` statement, assignments to\
      \ the loop variable fail to affect the loop's iteration order over the underlying\
      \ set of objects. Consequently, an assignment to the loop variable is equivalent\
      \ to modifying a variable local to the loop body whose initial value is the\
      \ object referenced by the loop iterator. This modification is not necessarily\
      \ erroneous but can obscure the loop functionality or indicate a misunderstanding\
      \ of the underlying implementation of the enhanced `for` statement.\n\nDeclare\
      \ all enhanced `for` statement loop variables final. The `final` declaration\
      \ causes Java compilers to flag and reject any assignments made to the loop\
      \ variable.\n\n**Noncompliant Code Example**\n\nThis noncompliant code example\
      \ attempts to process a collection of integers using an enhanced `for` loop.\
      \ It further intends to modify one item in the collection for processing:\n\n\
      ```java\nList<Integer> list = Arrays.asList(new Integer[] {13, 14, 15});\nboolean\
      \ first = true;\n\nSystem.out.println(\"Processing list...\");\nfor (Integer\
      \ i: list) {\n  if (first) {\n    first = false;\n    i = new Integer(99);\n\
      \  }\n  System.out.println(\" New item: \" + i);\n  // Process i\n}\n\nSystem.out.println(\"\
      Modified list?\");\nfor (Integer i: list) {\n  System.out.println(\"List item:\
      \ \" + i);\n}\n```\n\nHowever, this code does not actually modify the list,\
      \ as shown by the program's output:\n\n> Processing list...\n> New item: 99\n\
      > New item: 14\n> New item: 15\n> Modified list?\n> List item: 13\n> List item:\
      \ 14\n> List item: 15"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140302'
    language: Java
  - Advice: "对应的符合规则的代码示例：能够正确处理集合中的每一个对象，并且每一个对象只被处理一次。\n    ```java\n    Collection<ProcessObj>\
      \ processThese = // ...   \n    for (final ProcessObj processMe: processThese)\
      \ {   \n        if (someCondition) { // found the item to skip   \n        \
      \    someCondition = false;   \n            continue; // skip by continuing\
      \ to next iteration   \n        }   \n        processMe.doTheProcessing(); //\
      \ process the object   \n    }\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.2.3 将所有增强for 语句的循环变量声明为final 类型
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n将所有增强 for 语句的循环变量声明为 final 类型。   \n2) 原理   \n    与基本的\
      \ for语句不同，在增强的 for语句中，给循环变量赋值会影响循环的迭代次序。因此应避免对 for 循环中的循环变量赋值。   \n    增强的 for\
      \ 循环通常采用以下形式：   \n    ```java\n    for (ObjType obj : someIterableItem) {  \
      \ \n        // ...   \n    }\n    ```    \n    等价于以下形式的基本 for 循环：   \n    ```java\n\
      \    for (Iterator myIterator = someIterableItem.iterator(); myIterator.hasNext();)\
      \ {   \n        ObjType obj = myIterator.next();   \n        // ...   \n   \
      \ }\n    ```   \n    给循环变量赋值的动作，等价于修改循环体的局部变量的值。   \n    应将所有 for 语句的循环变量声明为\
      \ final，这个 final声明可让 Java 编译器做一个标志，拒绝对这个循环变量进行任何赋值。   \n3) 示例   \n    1.这个不符合规则的代码示例想使用增强型的\
      \ for循环处理对象集合。此外，它还想跳过对集合中某一个元素的处理。代码执行的结果：需要跳过的元素跳过了，但是它之后的元素被处理了两次。   \n \
      \   ```java\n    Collection<ProcessObj> processThese = // ...   \n    for (ProcessObj\
      \ processMe: processThese) {   \n        if (someCondition) { // found the item\
      \ to skip   \n            someCondition = false;   \n            processMe =\
      \ processMe.getNext(); // attempt to skip to next item   \n        }   \n  \
      \      processMe.doTheProcessing(); // process the object   \n    }\n    ```\
      \  \n     \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160017
    language: Java
  - Advice: "正确重写Iterator接口的next方法，当元素获取不到时，抛出NoSuchElementException。  \n  \n**例如**：下面代码示例中，正确重写Iterator接口的`next`方法。\n\
      ```java\npublic class Users implements Iterable<User> {\n    private User[]\
      \ users = new User[10];...\n\n    @Override\n    public Iterator<User> iterator()\
      \ {\n        return new Itr();\n    }\n\n    class Itr implements Iterator<User>\
      \ {\n        private int cursor;\n\n        @Override\n        public boolean\
      \ hasNext() {\n            return cursor != users.length;\n        }\n\n   \
      \     @Override\n        public User next() {\n            if (cursor >= users.length)\n\
      \                throw new NoSuchElementException();\n            return users[cursor++];\n\
      \        }\n        ...\n    }\n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】重写next方法应该抛出NoSuchElementException
    Categories_En:
      Category: []
    Default: true
    Description: "当类实现Iterator接口时，需重写`next`方法，而`next`方法未抛出NoSuchElementException可能导致程序运行异常。\
      \  \n  \n**例如**：下面代码示例中，类Iter实现了Iterator接口，而`next`方法未抛出NoSuchElementException,当程序为判断是否`hasNext`时，有可能抛出数组越界异常。\
      \  \n```java\npublic class Users implements Iterable<User> {  \n    private\
      \ User[] users = new User[10];  \n    ...  \n   @Override  \n    public Iterator<User>\
      \ iterator() {  \n       return new Itr();  \n   }  \n   class Itr implements\
      \ Iterator<User> {  \n       private int cursor;  \n       @Override  \n   \
      \    public boolean hasNext() {  \n           return cursor != users.length;\
      \  \n       }  \n       @Override  \n       public User next() {  \n       \
      \    return users[cursor++];  \n       }  \n       ...  \n   }  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: 0202000010150448
    language: Java
  - Advice: "**规范的代码示例1（字符串比较）**\n解决方案使用`BigDecimal`类来避免精度损失。它执行一个数字比较，结果符合预期。\n\n\
      ```java\nint i = 1;\nBigDecimal d = new BigDecimal(Double.valueOf(i / 1000.0).toString());\n\
      if (d.compareTo(new BigDecimal(\"0.001\")) == 0) {\n  // ...\n}\n```"
    Advice_En: "**Compliant Solution (String Comparison)**\n\nThis compliant solution\
      \ uses the `BigDecimal` class to avoid precision loss. It then performs a numeric\
      \ comparison, which passes as expected.\n\n```java\nint i = 1;\nBigDecimal d\
      \ = new BigDecimal(Double.valueOf(i / 1000.0).toString());\nif (d.compareTo(new\
      \ BigDecimal(\"0.001\")) == 0) {\n  // ...\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 03. 数据类型与运算 (NUM)
      - type: DetailClassChin
        value: NUM11-J：不要比较或检查浮点值的字符串表示法
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 03. Numeric Types and Operations (NUM)
      - type: DetailClassChin
        value: NUM11-J:Do not compare or inspect the string representation of floating-point
          values
    Default: true
    Description: "不得比较或检查浮点数的字符串表示形式。\n**不规范的代码示例1（字符串比较）**\n此不合规代码示例比较两个浮点值的字符串表示形式：\n\
      \n```java\nint i = 1;\nString s = Double.valueOf(i / 1000.0).toString();\nif\
      \ (s.equals(\"0.001\")) {\n  // ...\n}\n```\n\n比较失败，因为s包含字符串`0.0010`，该字符串不等于`0.001`。\n\
      **不规范的代码示例2（正则表达式）**\n此不合规代码示例试图在比较字符串之前，通过在字符串上使用正则表达式来减少额外的尾随零：\n\n```java\n\
      int i = 1;\nString s = Double.valueOf(i / 1000.0).toString();\ns = s.replaceFirst(\"\
      [.0]*$\", \"\");\nif (s.equals(\"0.001\")) {\n  // ...\n}\n```\n\n虽然在前面的代码中比较成功，但在下面的类似代码中比较失败，该代码使用`1/10000.0`而不是`1/1000.0`。生成的字符串不是`0.0`0010，而是`1.0E-4`。\n\
      \n```java\nint i = 1;\nString s = Double.valueOf(i / 10000.0).toString();\n\
      s = s.replaceFirst(\"[.0]*$\", \"\");\nif (s.equals(\"0.0001\")) {\n  // ...\n\
      }\n```"
    Description_En: "String representations of floating-point numbers must not be\
      \ compared or inspected.\n\n**Noncompliant Code Example (String Comparison)**\n\
      \nThis noncompliant code example compares the string representations of two\
      \ floating-point values:\n\n```java\nint i = 1;\nString s = Double.valueOf(i\
      \ / 1000.0).toString();\nif (s.equals(\"0.001\")) {\n  // ...\n}\n```\n\nThe\
      \ comparison unexpectedly fails because `s` contains the string `\"0.0010\"\
      `, which is not equal to `<code>\"0.001\"</code>`.\n\n**Noncompliant Code Example\
      \ (Regex)**\n\nThis noncompliant code example attempts to mitigate the extra\
      \ trailing zero by using a regular expression on the string before comparing\
      \ it:\n\n```java\nint i = 1;\nString s = Double.valueOf(i / 1000.0).toString();\n\
      s = s.replaceFirst(\"[.0]*$\", \"\");\nif (s.equals(\"0.001\")) {\n  // ...\n\
      }\n```\n\nAlthough the comparison succeeds on the preceding code, it fails on\
      \ the following similar code, which uses `1/10000.0` instead of `1/1000.0`.\
      \ The string produced is not `0.00010` but rather `1.0E-4`.\n\n```java\nint\
      \ i = 1;\nString s = Double.valueOf(i / 10000.0).toString();\ns = s.replaceFirst(\"\
      [.0]*$\", \"\");\nif (s.equals(\"0.0001\")) {\n  // ...\n}\n```"
    DomainType: ''
    DynamicDescription: ''
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 681:Incorrect Conversion between Numeric Types
      - type: CWE
        value: 197:Numeric Truncation Error
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140511'
    language: Java
  - Advice: "**规范的代码样例1**：不序列化内部类。  \n```java\npublic class OuterSer implements Serializable\
      \ {\n    private int rank;\n\n    class InnerSer {\n        protected String\
      \ name;\n        // . . .\n    }\n}\n```  \n**规范的代码样例2**：静态内部类实例可以序列化。  \n```java\n\
      public class OuterSer implements Serializable {\n    private int rank;\n\n \
      \   static class InnerSer implements Serializable {\n        protected String\
      \ name;\n        // . . .\n    }\n}\n```"
    Advice_En: "**Compliant Solution**\n\nThe `InnerSer` class of this compliant solution\
      \ deliberately fails to implement the `Serializable` interface:\n\n```java\n\
      public class OuterSer implements Serializable {\n  private int rank;\n  class\
      \ InnerSer {\n    protected String name;\n    // ...\n  }\n}\n```\n\n**Compliant\
      \ Solution**\n\nIf an inner and outer class must both be `Serializable`, the\
      \ inner class can be declared `static` to prevent a serialized inner class from\
      \ also serializing its outer class.\n\n```java\npublic class OuterSer implements\
      \ Serializable {\n  private int rank;\n  static class InnerSer implements Serializable\
      \ {\n    protected String name;\n    // ...\n  }\n}\n```"
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 14. 序列化 (SER)
      - type: DetailClassChin
        value: SER05-J：不要序列化内部类实例
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 14. Serialization (SER)
      - type: DetailClassChin
        value: SER05-J:Do not serialize instances of inner classes
    Default: true
    Description: "一个内部类是一个没有显式或隐式声明为静态的嵌套类。对内部类的序列化是容易出错的。根据序列化规范的说明：  \n1）当内部类声明在非静态环境中，并隐含对外部类实例非transient的引用时，序列化这样的内部类，会导致将它的外部类实例也进行序列化。\
      \  \n2）通过Java编译器产生的来实现内部类的合成数据成员是与实现相关的，并且会随着编译器的不同而不同，而这些数据成员的不同会破坏兼容性，并且会导致与默认的`serialVersionUID`数值相冲突。服务本地和匿名内部类的名称同样也是与实现相关的，并且在不同的编译器之间它们是不同的。\
      \  \n3）因为除了编译器的常量数据成员，内部类不能声明静态数据成员，所以它们不能使用`serialPersistentFields`机制来设计可序列化的数据成员。\
      \  \n4）因为与外部类相关联的内部类不能存在零参数的构造方法，所以它们不能实现Externalizable接口。而Externalizable接口要求实现的对象需要通过使用`writeExternal()`和`readExternal()`方法来对它的状态进行手动保存或者恢复。\
      \  \n因此，程序不能对内部类进行序列化。上述规则不适用于静态的成员类，所以对静态成员类的序列化是允许的。  \n风险评估：对内部类的序列化会引入平台依赖，并且会导致外部类的实例被序列化。\
      \  \n**不规范的代码样例1**：  \n```java\npublic class OuterSer implements Serializable\
      \ {\n    private int rank;\n\n    class InnerSer implements Serializable {\n\
      \        protected String name;\n        // . . .\n    }\n}\n```"
    Description_En: "\"An inner class is a nested class that is not explicitly or\
      \ implicitly declared static\" [JLS 2015]. Serialization of inner classes (including\
      \ local and anonymous classes) is error prone. According to the Serialization\
      \ Specification [Sun 2006]:\n\n> * Serializing an inner class declared in a\
      \ non-static context that contains implicit non-transient references to enclosing\
      \ class instances results in serialization of its associated outer class instance.\n\
      > * Synthetic fields generated by Java compilers to implement inner classes\
      \ are implementation dependent and may vary between compilers; differences in\
      \ such fields can disrupt compatibility as well as result in conflicting default\
      \ `serialVersionUID` values. The names assigned to local and anonymous inner\
      \ classes are also implementation dependent and may differ between compilers.\n\
      > * Because inner classes cannot declare static members other than compile-time\
      \ constant fields, they cannot use the `serialPersistentFields` mechanism to\
      \ designate serializable fields.\n> * Because inner classes associated with\
      \ outer instances do not have zero-argument constructors (constructors of such\
      \ inner classes implicitly accept the enclosing instance as a prepended parameter),\
      \ they cannot implement `Externalizable`. The `Externalizable` interface requires\
      \ the implementing object to manually save and restore its state using the `writeExternal()`\
      \ and `readExternal()` methods.\n\nConsequently, programs must not serialize\
      \ inner classes.\n\nBecause none of these issues apply to static member classes,\
      \ serialization of static member classes is permitted.\n\n**Noncompliant Code\
      \ Example**\n\nIn this noncompliant code example, the fields contained within\
      \ the outer class are serialized when the inner class is serialized:\n\n```java\n\
      public class OuterSer implements Serializable {\n  private int rank;\n  class\
      \ InnerSer implements Serializable {\n    protected String name;\n    // ...\n\
      \  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141505'
    language: Java
  - Advice: "内部类不要实现或间接实现Serializable接口，或将内部类声明为静态的。  \n  \n**例1**：下面代码示例中，内部类没有实现序列化接口。\
      \  \n```java\npublic class OuterSer implements Serializable {  \n    private\
      \ int rank;  \n    class InnerSer{  \n        protected String name;  \n   \
      \ }  \n}\n```  \n**例2**：下面代码示例中，将内部类声明为静态内部类。  \n```java\npublic class OuterSer\
      \ implements Serializable {  \n    private int rank;  \n    static class InnerSer\
      \ implements Serializable{  \n        protected String name;  \n    }  \n}\n\
      ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要使用非静态内部类实现序列化接口
    Categories_En:
      Category: []
    Default: true
    Description: "非静态内部类（包括普通内部类、Local内部类和匿名内部类）的实例持有一个外部类实例的隐式引用。非静态内部类实现序列化接口，当其被序列化时会出现运行时异常或泄露外部类中的信息。\
      \  \n下面代码示例中，内部类间接实现序列化接口  \n```java\npublic class SuperSer implements Serializable{\
      \  \n    //...  \n}  \npublic class OuterSer{  \n    private int rank;  \n \
      \   class InnerSer extends SuperSer{  \n        protected String name;  \n \
      \   }  \n}\n```     \n**例1**：下面代码示例中，内部类实现或间接实现序列化接口，外部类没有实现序列化，当内部类被序列化时，程序会报出NotSerializableException\
      \  \n```java\npublic class OuterSer{  \n    private int rank;  \n    class InnerSer\
      \ implements Serializable{  \n        protected String name;  \n    }  \n}\n\
      ```   \n**例2**：下面代码示例中，内部类和外部类实现或间接实现序列化接口，当内部类被序列化时，外部类的字段也会被序列化。  \n```java\n\
      public class OuterSer implements Serializable {  \n    private int rank;  \n\
      \    class InnerSer implements Serializable{  \n        protected String name;\
      \  \n    }  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Non-Static Inner Class Implements Serializable
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: CERT
        value: SER05-J:Do not serialize instances of inner classes
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150307'
    language: Java
  - Advice: "对应的符合规定的解决方案一是：OuterSer类特意不实现 Serializable 接口。   \n    ```java\n    public\
      \ class OuterSer implements Serializable {\n        private int rank;\n    \n\
      \        class InnerSer {\n            protected String name;\n            //\
      \ . . .\n        }\n    }\n    ```   \n    解决方案二是：内部类可以被声明为静态从而防止被序列化。一个静态的内部类也可以实现Serializable。\n\
      \    ```java   \n    public class OuterSer implements Serializable {\n     \
      \   private int rank;\n    \n        static class InnerSer implements Serializable\
      \ {\n            protected String name;\n            // . . .\n        }\n \
      \   }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.14.6 不要序列化内部类实例
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序不能对内部类进行序列化，但对静态成员类的序列化是允许的。   \n2) 原理   \n一个内部类是一个没有显式或隐式声明为静态的嵌套类。对内部类的序列化易出错（包括本地和匿名类）。根据序列化规范的说明:\
      \   \n当内部类声明在非静态环境中，并隐含对外部类实例非 transient的引用时，序列化这样的内部类，会导致将它的外部类实例也进行序列化。  \
      \ \n通过 Java编译器产生的来实现内部类的合成数据成员是与实现相关的，并且会随着编译器的不同而不同，而这些数据成员的不同会破坏兼容性，并且会导致与默认的\
      \ serialVersionlHD数值相冲突。赋予本地和匿名内部类的名称同样也是与实现相关的，并且在不同的编辑器之间它们是不同的。   \n因为除了编译期的常量数据成员，内部类不能声明静态数据成员，所以它们不能使用serialPersistentFields机制来设计可序列化的数据成员。\
      \   \n因为与外部类相关联的内部类不能存在零参数的构造方法（这样的内部类的构造 方法隐含的接受外包实例作为参数），所以它们不能实现 Externalizable\
      \ 接口。而   Externalizableinterface 接口要求实现的对象需要通过用 `writeExternal()`和   \n`readExternal()`\
      \ 方法来对它的状态进行手动保存或者恢复。   \n3) 示例   \n    一个不符合规则的代码示例，该示例中，当内部类被序列化时，包含在外部类的字段也会被序列化。\
      \   \n    ```java\n    public class OuterSer implements Serializable {\n   \
      \     private int rank;\n    \n        class InnerSer implements Serializable\
      \ {\n            protected String name;\n            // . . .\n        }\n \
      \   }\n    ```   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: CERT
        value: SER05-J:Do not serialize instances of inner classes
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160126
    language: Java
  - Advice: "在实现序列化接口的类中定义一个`serialVersionUID`，或者采用`serialPersistentFields`方式。  \n\
      **规范的代码样例1**：  \n在实现序列化接口的类中，显示的定义一个`serialVersionUID`，这个`serialVersionUID`包含了一个在这个类的版本中独一无二的数值。对具有相同类名和版本ID的序列化对象，JVM会进行反序列化。\
      \  \n```java\nclass GameWeapon implements Serializable {\n    private static\
      \ final long serialVersionUID = 24L;\n    int numOfWeapons = 10;\n\n    public\
      \ String toString() {\n        return String.valueOf(numOfWeapons);\n    }\n\
      }\n```\n**规范的代码样例2**：\n理想情况下，`Serializable`应该只为稳定的类实现。一种保持原始序列化形式并允许类进化的方法是在`serialPersistentFields`的帮助下使用自定义序列化。`static`和`transient`限定符指定哪些字段不应该被序列化，而`serialPersistentFields`字段则指定哪些字段应该被序列化。它还使类在类的实现中不必定义可序列化的字段，将当前的实现与整体逻辑解耦。新的字段可以很容易地被添加，而不会破坏不同版本的兼容性。\n\
      ```java\nclass WeaponStore implements Serializable {\n  int numOfWeapons = 10;\
      \ // Total number of weapons\n}\n\npublic class GameWeapon implements Serializable\
      \ {\n  WeaponStore ws = new WeaponStore();\n  private static final ObjectStreamField[]\
      \ serialPersistentFields\n      = {new ObjectStreamField(\"ws\", WeaponStore.class)};\n\
      \n  private void readObject(ObjectInputStream ois)\n      throws IOException,\
      \ ClassNotFoundException {\n    ObjectInputStream.GetField gf = ois.readFields();\n\
      \    this.ws = (WeaponStore) gf.get(\"ws\", ws);\n  }\n\n  private void writeObject(ObjectOutputStream\
      \ oos) throws IOException {\n    ObjectOutputStream.PutField pf = oos.putFields();\n\
      \    pf.put(\"ws\", ws);\n    oos.writeFields();\n  }\n\n  public String toString()\
      \ {\n    return String.valueOf(ws);\n  }\n}\n```"
    Advice_En: "**Compliant Solution (`serialVersionUID`)**\n\nIn this solution, the\
      \ class has an explicit `serialVersionUID` that contains a number unique to\
      \ this version of the class. The JVM will make a good-faith effort to deserialize\
      \ any serialized object with the same class name and version ID.\n\n```java\n\
      class GameWeapon implements Serializable {\n  private static final long serialVersionUID\
      \ = 24L;\n\n  int numOfWeapons = 10;\n\n  public String toString() {\n    return\
      \ String.valueOf(numOfWeapons);\n  }\n}\n```\n**Compliant Solution (`serialPersistentFields`)**\n\
      Ideally, `Serializable` should be implemented only for stable classes. One way\
      \ to maintain the original serialized form and allow the class to evolve is\
      \ to use custom serialization with the help of `serialPersistentFields`. The\
      \ `static` and `transient` qualifiers specify which fields should *not* be serialized,\
      \ whereas the `serialPersistentFields` field specifies which fields should be\
      \ serialized. It also relieves the class from defining the serializable field\
      \ within the class implementation, decoupling the current implementation from\
      \ the overall logic. New fields can easily be added without breaking compatibility\
      \ across releases.\n```java\nclass WeaponStore implements Serializable {\n \
      \ int numOfWeapons = 10; // Total number of weapons\n}\n\npublic class GameWeapon\
      \ implements Serializable {\n  WeaponStore ws = new WeaponStore();\n  private\
      \ static final ObjectStreamField[] serialPersistentFields\n      = {new ObjectStreamField(\"\
      ws\", WeaponStore.class)};\n\n  private void readObject(ObjectInputStream ois)\n\
      \      throws IOException, ClassNotFoundException {\n    ObjectInputStream.GetField\
      \ gf = ois.readFields();\n    this.ws = (WeaponStore) gf.get(\"ws\", ws);\n\
      \  }\n\n  private void writeObject(ObjectOutputStream oos) throws IOException\
      \ {\n    ObjectOutputStream.PutField pf = oos.putFields();\n    pf.put(\"ws\"\
      , ws);\n    oos.writeFields();\n  }\n\n  public String toString() {\n    return\
      \ String.valueOf(ws);\n  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 14. 序列化 (SER)
      - type: DetailClassChin
        value: SER00-J：在类的演化过程中维护其序列化的兼容性
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 14. Serialization (SER)
      - type: DetailClassChin
        value: SER00-J:Enable serialization compatibility during class evolution
    Default: true
    Description: "一旦某个特定类的对象被序列化，未来对该类的代码进行重构往往会成为问题。具体来说，现有的序列化形式（编码表示）成为该对象发布的API的一部分，必须无限期地予以支持。从安全的角度来看，这可能是个麻烦；它不仅促进了死代码，而且还迫使提供者在其产品的生命周期内维护一个兼容的代码库。\n\
      实现`Serializable`而不重写其功能的类被称为使用*默认的*序列化形式。如果类发生变化，旧版本的类的用户产生的字节流将与新的实现不兼容。因此，依赖默认序列化形式的可序列化类不能在不影响兼容性的情况下被演化。\n\
      为了实现可序列化类的兼容进化，开发者必须使用*定制的*序列化形式，它比默认形式更灵活。具体来说。\n\n* 使用自定义的形式可以摆脱流格式对实现类的代码的依赖。\n\
      * 为反序列化自定义表单而生成的代码可以处理与序列化表单的兼容偏差，如额外的字段。\n\n因此，开发者既不需要维护早期版本的代码，也不需要明确地支持原始的序列化形式。\n\
      请注意，遵守这一规则虽然是必要的，但并不足以保证可序列化类的兼容进化。关于可序列化类的兼容演进的全面讨论，请参阅《Java 对象序列化规范》（第 6 版）第\
      \ 5 章 \"可序列化对象的版本\"[Sun 2006]。\n**不规范的代码样例1**：  \n类GameWeapon实现了Serializable接口，而且包含了一个可序列化字段`numOfWeapons`，并使用了默认的序列化格式。任何对该类内部表示的改变都会破坏线程的序列化格式。因为这个类不能提供一个`serialVersionUID`，所以JVM会通过默认的实现方法赋予它一个。如果这个类的定义改变了，那么`serialVersionUID`同样也会改变。因此，当版本IDs不同的时候，JVM会拒绝将一个类定义与其对象的序列化格式联系起来。\
      \  \n```java\nclass GameWeapon implements Serializable {\n    int numOfWeapons\
      \ = 10;\n\n    public String toString() {\n        return String.valueOf(numOfWeapons);\n\
      \    }\n}\n```"
    Description_En: "Once an object of a particular class has been serialized, future\
      \ refactoring of the class's code often becomes problematic. Specifically, existing\
      \ serialized forms (encoded representations) become part of the object's published\
      \ API and must be supported for an indefinite period. This can be troublesome\
      \ from a security perspective; not only does it promote dead code, it also forces\
      \ the provider to maintain a compatible codebase for the lifetime of their products.\n\
      \nClasses that implement `Serializable` without overriding its functionality\
      \ are said to be using the *default* serialized form. In the event the class\
      \ changes, byte streams produced by users of old versions of the class become\
      \ incompatible with the new implementation. Consequently, serializable classes\
      \ that rely on the default serialized form cannot be evolved without compromising\
      \ compatibility.\n\nTo enable compatible evolution of a serializable class,\
      \ developers must use a *custom* serialized form, which is more flexible than\
      \ a default form. Specifically,\n\n* Use of a custom form severs the dependence\
      \ of the stream format on the code of the implementing class.\n* The code generated\
      \ for deserializing a custom form can handle compatible deviations from the\
      \ serialized form, like extra fields.\n\nAs a result, developers need neither\
      \ maintain the earlier version of the code nor explicitly support the original\
      \ serialized form.\n\nNote that compliance with this rule, while necessary,\
      \ is not sufficient to guarantee compatible evolution of serializable classes.\
      \ For a full discussion of compatible evolution of serializable classes, see\
      \ the Java Object Serialization Specification (version 6), Chapter 5, \"Versioning\
      \ of Serializable Objects\" [Sun 2006]\n**Noncompliant Code Example**\n\nThis\
      \ noncompliant code example implements a `GameWeapon` class with a serializable\
      \ field called `numOfWeapons` and uses the default serialized form. Any changes\
      \ to the internal representation of the class can break the existing serialized\
      \ form.\n\n```java\nclass GameWeapon implements Serializable {\n  int numOfWeapons\
      \ = 10;\n\n  public String toString() {\n    return String.valueOf(numOfWeapons);\n\
      \  }\n}\n```\n\nBecause this class does not provide a `serialVersionUID`, the\
      \ Java Virtual Machine (JVM) assigns it one using implementation-defined methods.\
      \ If the class definition changes, the `serialVersionUID` is also likely to\
      \ change. Consequently, the JVM will refuse to associate the serialized form\
      \ of an object with the class definition when the version IDs are different."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 589:Call to Non-ubiquitous API
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141500'
    language: Java
  - Advice: "对应的符合规定的解决方案中，类有显式的`serialVersionUID`，这个`serialVersionUID`包含了一个在这个类的版本中独一无二的数值。具有相同类名和版本\
      \ ID 的序列化对象，JVM 会忠实地进行反序列化。   \n    ```java\n    class GameWeapon implements\
      \ Serializable {   \n        private static final long serialVersionUID = 24L;\
      \   \n        int numOfWeapons = 10;   \n        public String toString() {\
      \   \n            return String.valueOf(numOfWeapons);\n    ```   \n    另一个符合规则的解决方案是：维护原始的序列化格式并且允许类进行演化的方法是通过`serialPersistentFields`来使用自定义的序列化。static和\
      \ transient 标识符指定了哪些字段不应当被序列化，而`serialPersistentFields`字段指定了哪些字段应当被序列化。它使得类不需要在类实现中定义序列化字段，将当前实现与整个逻辑解耦。新的字段可以很容易地增加进来，而不会破坏各个版本之间的兼容性。\
      \   \n    ```java\n    class WeaponStore implements Serializable {\n       \
      \ int numOfWeapons = 10; // Total number of weapons\n    }\n    \n    public\
      \ class GameWeapon implements Serializable {\n        WeaponStore ws = new WeaponStore();\n\
      \        private static final ObjectStreamField[] serialPersistentFields = {\n\
      \                new ObjectStreamField(\"ws\", WeaponStore.class) };\n    \n\
      \        private void readObject(ObjectInputStream ois) throws IOException,\
      \ ClassNotFoundException {\n            ObjectInputStream.GetField gf = ois.readFields();\n\
      \            this.ws = (WeaponStore) gf.get(\"ws\", ws);\n        }\n    \n\
      \        private void writeObject(ObjectOutputStream oos) throws IOException\
      \ {\n            ObjectOutputStream.PutField pf = oos.putFields();\n       \
      \     pf.put(\"ws\", ws);\n            oos.writeFields();\n        }\n    \n\
      \        public String toString() {\n            return String.valueOf(ws);\n\
      \        }\n    }\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.14.1 在类的演化过程中维护其序列化的兼容性
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序必须在类演化过程中维护序列化的兼容性。   \n2) 原理   \n将 Serializable 功能覆写的类实现使用的是默认的序列化格式。在类发生变化的时候，由旧版本的类的使用者产生的字节流会与新的不兼容。程序必须在类演化过程中维护序列化的兼容性。一个可以接受的方式是使用自定义的序列化格式，除了新演化版本之外，这种格式还可减轻维护原始序列化格式和对应类的必要性。\
      \   \n3) 示例   \n    1、一个不符合规则的代码示例，该示例实现了一个 Game Weapon 类，类包含了一个可序列化字段`numofWeapons`，并且使用了默认的序列化格式。任何对该类内部表示的改变都会破坏现存的序列化格式。\
      \   \n    ```java\n    class GameWeapon implements Serializable { \n       \
      \ int numOfWeapons = 10;   \n        public String toString () {   \n      \
      \      return String.valueOf(numOfWeapons);\n        }\n    ```   \n    因为这个类不能提供一个`serialVersionUID`，,所以\
      \ Java 虚拟机会通过自定义的实现方法赋予它一个。如果这个类的定义改变了，那么`serialVersionUID`同样也会改变。因此，当版本 IDs\
      \ 不同时，JVM 会拒绝将一个类定义与其对象的序列化格式联系起来。   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 589:Call to Non-ubiquitous API
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160121
    language: Java
  - Advice: "确认代码逻辑，是否使用YYYY来格式化日期。  \n  \n**例如**：下列代码中使用yyyy格式化日期，获取的值为2017/12/31。\n\
      ```java  \nDate date = new SimpleDateFormat(\"yyyy/MM/dd\").parse(\"2017/12/31\"\
      );  \nString result = new SimpleDateFormat(\"yyyy/MM/dd\").format(date);\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要使用WeekYear日期格式化
    Categories_En:
      Category: []
    Default: true
    Description: "在使用SimpleDateFormat格式化和解析日期时，Y表示Week Year，意味着是当天所在的周属于的年份，一周从周日开始，周六结束，只要本周跨年，那么这周就算入下一年。。\
      \  \n  \n**例如**：下列代码中使用YYYY格式化日期，获取的值为2017/12/31。\n```java  \nDate date = new\
      \ SimpleDateFormat(\"yyyy/MM/dd\").parse(\"2017/12/31\");  \nString result =\
      \ new SimpleDateFormat(\"YYYY/MM/dd\").format(date);\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: correctness
    Type: std
    WhiteList: not
    id: '0202000010150430'
    language: Java
  - Advice: "使用集成开发环境一键生成get和set方法是一个很好的选择，或者在写完get或set方法对其进行检查。  \n  \n**例如**：下列代码中正确的提供了get和set方法。\
      \  \n```java\nclass User {  \n    private String username;  \n    private String\
      \ password;  \n    ...  \n      \n    public void setUsername(String username)\
      \ {  \n        this.username = username;  \n    }  \n  \n    public String getUsername()\
      \ {  \n        return this.username;  \n    }  \n      \n    ...  \n}\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要构建错误的getter或setter方法
    Categories_En:
      Category: []
    Default: true
    Description: "在Java程序中，经常使用getter和setter方法来提供对类中私有字段的获取和修改。而当字段太多时，程序员可能会通过复制粘贴构建get和set方法，有可能会生成错误的get或set方法，当程序运行时会产生不可预料的效果。\
      \  \n  \n**例如**：下列代码中`setUsername`方法没有修改相应的字段，而`getUsername`方法没有获取相应的字段。  \n\
      ```java\nclass User {  \n    private String username;  \n    private String\
      \ password;  \n    ...  \n      \n    public void setUsername(String username)\
      \ {  \n        this.password = username;  \n    }  \n  \n    public String getUsername()\
      \ {  \n        return this.password;  \n    }  \n      \n    ...  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: correctness
    Type: std
    WhiteList: not
    id: '0202000010150426'
    language: Java
  - Advice: "应该使用`BigDecimal.valueOf(double)`，它将转化为字符串再进行转换，或使用字符串参数的构造函数。  \n  \n\
      **例1**：在下面的代码片段中，程序使用`valueOf`方法进行转换。  \n```java\n...  \nBigDecimal bigDecimal\
      \ = BigDecimal.valueOf(0.1);  \n...\n```  \n**例2**：在下面的代码片段中，程序使用字符串参数的构造函数进行转换。\
      \  \n```java\n...  \nBigDecimal bigDecimal = new BigDecimal(\"0.1\");  \n...\n\
      ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【强制】不要使用BigDecimal(double)
    Categories_En:
      Category: []
    Default: true
    Description: "使用`BigDecimal(double)`构造BigDecimal，可能获得不是所期望的值。  \n  \n**例如**：在下面的代码片段中，传递给构造函数的值看起来是0.1，但是并不完全等于0.1，实际上最后生成的BigDecimal为`0.1000000000000000000005111512312827021181583404541015625`。\
      \  \n```java\n...  \nBigDecimal bigDecimal = new BigDecimal(0.1);  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150117'
    language: Java
  - Advice: 不要依赖DNS名称进行安全认证。
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.3 访问控制
      - type: DetailClassChin
        value: A.2.3.1.2 避免使用DNS名称作为安全性的依据
    Categories_En:
      Category: []
    Default: not
    Description: "规范说明：程序中采用DNS名称进行安全认证，但DNS名称是容易被攻击者进行欺骗的。  \n对避免使用DNS名称作为安全性的依据的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**：  \n```java\nString ip = request.getRemoteAddr();  \nInetAddress\
      \ inetAddress = InetAddress.getByName(ip);  \nif (inetAddress.getCanonicalHostName().endsWith(\"\
      demo.com\")) {  \n    //Verification passed  \n}\n```  \n以上代码片段中，如果发生DNS欺骗，会绕过安全验证。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 807:Reliance on Untrusted Inputs in a Security Decision
      - type: CWE
        value: 558:Use of getlogin() in Multithreaded Application
      - type: CWE
        value: 292:Trusting Self-reported DNS Name
      - type: CWE
        value: 247:Reliance on DNS Lookups in a Security Decision
      - type: OWASP TOP 10
        value: 2017:A2 Broken Authentication
      - type: OWASP TOP 10
        value: 2013:A2 Broken Authentication and Session Management
      - type: OWASP TOP 10
        value: 2010:A3 Broken Authentication and Session Management
      - type: OWASP TOP 10
        value: 2007:A7 Broken Authentication and Session Management
      - type: OWASP TOP 10
        value: 2004:A3 Broken Authentication and Session Management
      - type: OWASP MOBILE TOP 10
        value: 2014:M5 Poor Authorization and Authentication
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 807
      - type: CWE/SANS TOP 25
        value: 2010:Porous Defenses - CWE ID 807
      - type: GB/T 34944-2017 Java
        value: 安全功能:安全关键的行为依赖反向域名解析
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: 0302000010170019
    language: Java
  - Advice: "方法可以抛出继承自RuntimeException、Exception的明确异常。\n**规范的代码样例1**：  \n抛出明确的NullPointerException()。\n\
      ```java\nboolean isCapitalized(String s) {\n  if (s == null) {\n    throw new\
      \ NullPointerException();\n  }\n  if (s.equals(\"\")) {\n    return true;\n\
      \  }\n  String first = s.substring(0, 1);\n  String rest = s.substring(1);\n\
      \  return (first.equals(first.toUpperCase()) &&\n          rest.equals(rest.toLowerCase()));\n\
      }\n```\n**特例**\nERR07-J-EX0: 允许对异常进行净化以符合安全策略的类将特定的异常转换为更一般的异常。这种翻译有可能导致在某些情况下抛出\
      \ \"RuntimeException\"、\"Exception \"或 \"Throwable\"，这取决于安全策略的要求。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution throws `NullPointerException`\
      \ to denote the specific exceptional condition:\n\n```java\nboolean isCapitalized(String\
      \ s) {\n  if (s == null) {\n    throw new NullPointerException();\n  }\n  if\
      \ (s.equals(\"\")) {\n    return true;\n  }\n  String first = s.substring(0,\
      \ 1);\n  String rest = s.substring(1);\n  return (first.equals(first.toUpperCase())\
      \ &&\n          rest.equals(rest.toLowerCase()));\n}\n```\n\nNote that the null\
      \ check is redundant; if it were removed, the subsequent call to `s.equals(\"\
      \")` would throw a `NullPointerException` when `s` is null. However, the null\
      \ check explicitly indicates the programmer's intent. More complex code may\
      \ require explicit testing of invariants and appropriate `throw` statements.\n\
      \n**Exceptions**\n\nERR07-J-EX0: Classes that sanitize exceptions to comply\
      \ with a security policy are permitted to translate specific exceptions into\
      \ more general exceptions. This translation could potentially result in throwing\
      \ `RuntimeException`, `Exception`, or `Throwable` in some cases, depending on\
      \ the requirements of the security policy."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 07. 异常行为 (ERR)
      - type: DetailClassChin
        value: ERR07-J：不要抛出RuntimeException、Exception、Throwable
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 07. Exceptional Behavior (ERR)
      - type: DetailClassChin
        value: ERR07-J:Do not throw RuntimeException, Exception, or Throwable
    Default: not
    Description: "方法禁止抛出RuntimeException、Exception、Throwable。抛出RuntimeException会产生微妙的错误，很少有方法能处理潜在的运行异常，例如：一个调用者不能通过检查异常来判断异常抛出的原因，从而不能进行有效的恢复操作。\
      \  \n方法可以抛出一个从`Exception`或`RuntimeException`子类化的特定异常。请注意，可以为一个单一的`throw`语句专门构建一个异常类。\n\
      **不规范的代码样例1**：\n```java\nboolean isCapitalized(String s) {\n  if (s == null)\
      \ {\n    throw new RuntimeException(\"Null String\");\n  }\n  if (s.equals(\"\
      \")) {\n    return true;\n  }\n  String first = s.substring(0, 1);\n  String\
      \ rest = s.substring(1);\n  return (first.equals(first.toUpperCase()) &&\n \
      \         rest.equals(rest.toLowerCase()));\n}\n```"
    Description_En: "Methods must not throw `RuntimeException`, `Exception`, or `Throwable`.\
      \ Handling these exceptions requires catching `RuntimeException`, which is disallowed\
      \ by ERR08-J. Do not catch NullPointerException or any of its ancestors. Moreover,\
      \ throwing a `RuntimeException` can lead to subtle errors; for example, a caller\
      \ cannot examine the exception to determine why it was thrown and consequently\
      \ cannot attempt recovery.\n\nMethods can throw a specific exception subclassed\
      \ from `Exception` or `RuntimeException`. Note that it is permissible to construct\
      \ an exception class specifically for a single `throw` statement.\n\n**Noncompliant\
      \ Code Example**\n\nThe `isCapitalized()` method in this noncompliant code example\
      \ accepts a string and returns true when the string consists of a capital letter\
      \ followed by lowercase letters. The method also throws a `RuntimeException`\
      \ when passed a null string argument.\n\n```java\nboolean isCapitalized(String\
      \ s) {\n  if (s == null) {\n    throw new RuntimeException(\"Null String\");\n\
      \  }\n  if (s.equals(\"\")) {\n    return true;\n  }\n  String first = s.substring(0,\
      \ 1);\n  String rest = s.substring(1);\n  return (first.equals(first.toUpperCase())\
      \ &&\n          rest.equals(rest.toLowerCase()));\n}\n```\n\nA calling method\
      \ must also violate ERR08-J. Do not catch NullPointerException or any of its\
      \ ancestors to determine whether the `RuntimeException` was thrown.\n\n**Compliant\
      \ Solution**\n\nThis compliant solution throws `NullPointerException` to denote\
      \ the specific exceptional condition:\n\n```java\nboolean isCapitalized(String\
      \ s) {\n  if (s == null) {\n    throw new NullPointerException();\n  }\n  if\
      \ (s.equals(\"\")) {\n    return true;\n  }\n  String first = s.substring(0,\
      \ 1);\n  String rest = s.substring(1);\n  return (first.equals(first.toUpperCase())\
      \ &&\n          rest.equals(rest.toLowerCase()));\n}\n```\n\nNote that the null\
      \ check is redundant; if it were removed, the subsequent call to `s.equals(\"\
      \")` would throw a `NullPointerException` when `s` is null. However, the null\
      \ check explicitly indicates the programmer's intent. More complex code may\
      \ require explicit testing of invariants and appropriate `throw` statements.\n\
      \n**Noncompliant Code Example**\n\nThis noncompliant code example specifies\
      \ the `Exception` class in the `throws` clause of the method declaration for\
      \ the `doSomething()` method:\n\n```java\nprivate void doSomething() throws\
      \ Exception {\n  //...\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 397:Declaration of Throws for Generic Exception
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.6 方法声明抛出的异常过于宽泛
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140807
    language: Java
  - Advice: "对应的符合规则的解决方案是：在 `doSomething()`方法声明的 throws 语句中指定一个更具体的异常类。\n    ```java\n\
      \    private void doSomething() throws IOException {   \n        // . . .  \
      \  \n    }\n    ```   \n   \n4) 特例   \n通过过滤异常来符合安全策略的类是允许将具体的异常转化成通用异常的。在某些情况下，根据安全策略的需求，这样的转化可能会导致抛出\
      \ RuntimeException、Exception 或 Throwable。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.4 错误处理安全
      - type: DetailClassChin
        value: C.7.8 不要拋出 RuntimeException、Exception 或 Throwable
    Categories_En:
      Category: []
    Default: not
    Description: "1) 规定   \n方法禁止抛出 RuntimeException、Exception 或是 ```java\nThrowable。\
      \   \n2) 原理   \n处理上述异常需要捕捉 RimtimeException，这是不被允许的。而且，抛出 RuntimeException 会产生微妙的错误，例如，一个调用者不能通过检查异常来判断异常抛出的原因，从而不能尝试恢复操作。\
      \   \n方法可以抛出继承 Exception 或 RuntimeException 的明确异常。要注意，对一个单独的 throw 语句创建一个异常类是允许的。\
      \   \n当抛出 RuntimeException、Exception 或 Throwable 时，在要求类捕捉预期的异常的同时，必须捕捉预料外的异常。\
      \   \n3) 示例   \n    如下不符合规则的代码示例在 `doSomething()`方法声明的 throws 语句中指定了 Exception\
      \ 类。\n    ```java\n    private void doSomething() throws Exception {   \n  \
      \      // . . .   \n    } \n    ```   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 397:Declaration of Throws for Generic Exception
      - type: CERT
        value: ERR07-J:Do not throw RuntimeException, Exception, or Throwable
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.6 方法声明抛出的异常过于宽泛
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160070
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n抛出明确的NullPointerException()。 \n\
      ```java \nboolean isCapitalized(String s) {\n    if (s == null) {\n        throw\
      \ new NullPointerException();\n    }\n    if (s.equals(\"\")) {\n        return\
      \ true;\n    }\n    String first = s.substring(0, 1);\n    String rest = s.substring(1);\n\
      \    return (first.equals(first.toUpperCase()) && rest.equals(rest.toLowerCase()));\n\
      }\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.4 异常处理安全
      - type: DetailClassChin
        value: A.3.4.6 不要抛出RuntimeException、Exception、Throwable
    Categories_En:
      Category: []
    Default: not
    Description: "规范说明：方法禁止抛出RuntimeException、Exception、Throwable。抛出RuntimeException会产生微妙的错误，很少有方法能处理潜在的运行异常，例如，一个调用者不能通过检查异常来判断异常抛出的原因，从而不能进行有效的恢复操作。\
      \  \n方法RuntimeException、Exception、Throwable可能掩盖其他异常。  \n对于不要抛出RuntimeException、Exception、Throwable的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**：  \n```java\nboolean isCapitalized(String s) {\n    if (s == null)\
      \ {\n        throw new RuntimeException(\"Null String\");\n    }\n    if (s.equals(\"\
      \")) {\n        return true;\n    }\n    String first = s.substring(0, 1);\n\
      \    String rest = s.substring(1);\n    return (first.equals(first.toUpperCase())\
      \ && rest.equals(rest.toLowerCase()));\n}\n```  \n方法可以抛出继承自RuntimeException、Exception的明确异常。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 397:Declaration of Throws for Generic Exception
      - type: CERT
        value: ERR07-J:Do not throw RuntimeException, Exception, or Throwable
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.6 方法声明抛出的异常过于宽泛
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: 0302000010170058
    language: Java
  - Advice: "不要重用Java标准库中已经公开的可见标识符、公开的类或接口、包。  \n**规范的代码样例1**：  \n针对本条规则的不规范的样例1：将Vector类名改为MyVector，即可以语义清晰的区分`java.util.Vector`。\
      \ \n```java \nclass MyVector {  \n    // Other code\n}\n```"
    Advice_En: "**Compliant Solution (Class Name)**\n\nThis compliant solution uses\
      \ a different name for the class, preventing any potential shadowing of the\
      \ class from the Java Standard Library:\n\n```java\nclass MyVector {\n  //Other\
      \ code\n}\n```\n\nWhen the developer and organization control the original shadowed\
      \ class, it may be preferable to change the design strategy of the original\
      \ in accordance with Bloch's *Effective Java* Bloch 2008, Item 16, \"Prefer\
      \ Interfaces to Abstract Classes.\" Changing the original class into an interface\
      \ would permit class `MyVector` to declare that it implements the hypothetical\
      \ `Vector` interface. With this technique, client code that intended to use\
      \ `MyVector` would remain compatible with code that uses the original implementation\
      \ of `Vector`."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 01. 声明和初始化 (DCL)
      - type: DetailClassChin
        value: DCL01-J：不要重用Java标准库已经公开的标识符
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 01. Declarations and Initialization (DCL)
      - type: DetailClassChin
        value: DCL01-J:Do not reuse public identifiers from the Java Standard Library
    Default: true
    Description: "不要重复使用Java标准库中公开可见的标识符、公共工具类、接口或包的名称。\n当一个程序员使用和Java标准库中公开类中类的名字来定义一个类时：如Vector，后来的维护者可能不知道这个标识符Vector并不是指Java标准库的类`java.util.Vector`，并且可能会无意的使用了这个自定义的Vector而不是Java标准库的，使得这个自定义的Vector会遮蔽`java.util.Vector`，正如JLS\
      \ 6.3.2节中提到的那样。\n良好定义的import语句可以理解这个问题。然而，当重用的命名定义是从其他包中导入时，使用type-import-on-demand\
      \ declaration（JSL 7.5.2节）会使程序员难以理解语义，他很难理解到底哪一个定义是他所需要的。另外，开发过程中程序员通常会使用IDE的自动导入import语句，通常这种操作是在写完引用代码之后才执行，如果存在多个名称与预期的类名称相同，很难判断哪个才是需要的。\
      \  \n**不规范的代码样例1**：  \n```java\nclass Vector {\n    private int val = 1;\n\n\
      \    public boolean isEmpty() {\n        if (val == 1) { // compares with 1\
      \ instead of 0\n            return true;\n        } else {\n            return\
      \ false;\n        }\n    }\n    // other functionality is same as java.util.Vector\n\
      }\n\n// import java.util.Vector;or Vector  \npublic class VectorUser {\n   \
      \ public static void main(String[] args) {\n        Vector v = new Vector();\n\
      \        if (v.isEmpty()) {\n            System.out.println(\"Vector is empty\"\
      );\n        }\n    }\n}\n```    \n代码实现了一个类，这个类重用了`java.util.Vector`的类名称。并且它定义了一个方法`isEmpty()`和`java.util.Vector`的`isEmpty()`方法的返回类型、参数一致，但是重新定义了方法内部的实现。在类VectorUser中引用`Vector.isEmpty()`方法时，如果维护者混淆了这两个方法，那么将会出现不可预期的行为。"
    Description_En: "Do not reuse the names of publicly visible identifiers, public\
      \ utility classes, interfaces, or packages in the Java Standard Library.\n\n\
      When a developer uses an identifier that has the same name as a public class,\
      \ such as `Vector`, a subsequent maintainer might be unaware that this identifier\
      \ does not actually refer to `java.util.Vector` and might unintentionally use\
      \ the custom `Vector` rather than the original `java.util.Vector` class. The\
      \ custom type `Vector` can shadow a class name from `java.util.Vector`, as specified\
      \ by *The Java Language Specification* (JLS), §6.3.2, &#34;Obscured Declarations&#34;\
      \ JLS 2005, and unexpected program behavior can occur.\n\nWell-defined import\
      \ statements can resolve these issues. However, when reused name definitions\
      \ are imported from other packages, use of the *type-import-on-demand declaration*\
      \ (see §7.5.2, &#34;Type-Import-on-Demand Declaration&#34;JLS 2005) can complicate\
      \ a programmer's attempt to determine which specific definition was intended\
      \ to be used. Additionally, a common practice that can lead to errors is to\
      \ produce the import statements *after* writing the code, often via automatic\
      \ inclusion of import statements by an IDE, which creates further ambiguity\
      \ with respect to the names. When a custom type is found earlier than the intended\
      \ type in the Java include path, no further searches are conducted. Consequently,\
      \ the wrong type is silently adopted.\n\n**Noncompliant Code Example (Class\
      \ Name)**\n\nThis noncompliant code example implements a class that reuses the\
      \ name of the class `java.util.Vector`. It attempts to introduce a different\
      \ condition for the `isEmpty()` method for interfacing with native legacy code\
      \ by overriding the corresponding method in `java.util.Vector`. Unexpected behavior\
      \ can arise if a maintainer confuses the `isEmpty()` method with the `java.util.Vector.isEmpty()`\
      \ method.\n\n```java\nclass Vector {\n  private int val = 1;\n\n  public boolean\
      \ isEmpty() {\n    if (val == 1) {   // Compares with 1 instead of 0\n     \
      \ return true;\n    } else {\n      return false;\n    }\n  }\n  // Other functionality\
      \ is same as java.util.Vector\n}\n\n// import java.util.Vector; omitted\npublic\
      \ class VectorUser {\n  public static void main(String[] args) {\n    Vector\
      \ v = new Vector();\n    if (v.isEmpty()) {\n      System.out.println(\"Vector\
      \ is empty\");\n    }\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140301'
    language: Java
  - Advice: "对应的符合规则的代码示例：为定义的类使用不同的名字。   \n    ```java\n    class MyVector {   \n\
      \        // other code   \n    }\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.2.2 不要重用Java 标准库的已经公开的标识
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n不要重用 Java 标准库中已经使用过的公共标识、公共工具类、接口或者包。   \n2) 原理   \n定义一个类，使用和公开类相同的名字，这个自定义的类会遮蔽原来的公开类，从而导致不可预期的程序行为。\
      \   \n使用定义良好的 import 语句可以解决该问题，但容易产生错误。在 Java 包含的 import 引用路径中，如果在预期的类出现之前出现了一个自定义的类，就不会再进一步搜索，这样就会使用错误的类。\
      \   \n3) 示例   \n    1.这个不符合规则的代码示例实现了一个类，这个类重用了`java.util.Vector`的类名称。它在`isEmpty()`中使用了一个不同的条件判断，尝试通过重写`java.util.Vector`中对应的方法来达到与遗留代码接口的目的。如果维护者混淆了这个`isEmpty()`和\
      \ `java.util.Vector. isEmpty()`，就会出现不可预知行为。   \n    ```java\n    class Vector\
      \ {\n        private int val = 1;\n    \n        public boolean isEmpty() {\n\
      \            if (val == 1) { // compares with 1 instead of 0\n             \
      \   return true;\n            } else {\n                return false;\n    \
      \        }\n        }\n        // other functionality is same as java.util.Vector\n\
      \    }\n    \n    // import java.util.Vector; omitted\n    public class VectorUser\
      \ {\n        public static void main(String[] args) {\n            Vector v\
      \ = new Vector();\n            if (v.isEmpty()) {\n                System.out.println(\"\
      Vector is empty\");\n            }\n        }\n    }\n    ```   \n       \n\
      \    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160016
    language: Java
  - Advice: "可以将父类实现Serializable接口或者添加无参构造方法。  \n  \n**例1**：下面代码示例中User的父类Human实现Serializable接口。\
      \  \n```java\npublic class Human implements Serializable{  \n    private int\
      \ age;  \n    public Human(int age) {...}  \n    ...  \n}  \npublic class User\
      \ extends Human implements Serializable{  \n    private String name;  \n   \
      \ public User (String name,int age) {...}  \n    ...  \n}\n```  \n**例2**：下面代码示例中User的父类Human添加了无参构造方法。\
      \  \n```java\npublic class Human{  \n    private int age;  \n    public Human()\
      \ {...}  \n    public Human(int age) {...}  \n    ...  \n}  \npublic class User\
      \ extends Human implements Serializable{  \n    private String name;  \n   \
      \ public User (String name,int age) {...}  \n    ...  \n}\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】可序列化类的不可序列化的父类应拥有无参构造方法
    Categories_En:
      Category: []
    Default: true
    Description: "当可序列化类序列化时，对象反序列化会从父类中第一个非可序列化的类开始，并沿着链继续下去，添加每个后续子类的属性，直到最后一个对象被实例化为止。为了创建非序列化的父类，将会调用了它的无参构造方法。若没有无参构造方法将会报出InvalidClassException异常。\
      \  \n  \n**例如**：下面代码示例中User的父类Human未实现Serializable接口，并没有无参构造方法。  \n```java\n\
      public class Human{  \n    private int age;  \n    public Human(int age) {...}\
      \  \n    ...  \n}  \npublic class User extends Human implements Serializable{\
      \  \n    private String name;  \n    public User (String name,int age) {...}\
      \  \n    ...  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: correctness,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150442'
    language: Java
  - Advice: "**规范的代码样例1**：  \n```java\npublic final class NativeMethodWrapper {\n\n\
      \    // Private native method\n    private native void nativeOperation(byte[]\
      \ data, int offset, int len);\n\n    // Wrapper method performs SecurityManager\
      \ and input validation checks\n    public void doOperation(byte[] data, int\
      \ offset, int len) {\n        // Permission needed to invoke native method\n\
      \        securityManagerCheck();\n\n        if (data == null) {\n          \
      \  throw new NullPointerException();\n        }\n\n        // Copy mutable input\n\
      \        data = data.clone();\n\n        // Validate input\n        if ((offset\
      \ < 0) || (len < 0) || (offset > (data.length - len))) {\n            throw\
      \ new IllegalArgumentException();\n        }\n\n        nativeOperation(data,\
      \ offset, len);\n    }\n\n    static {\n        // Load native library in static\
      \ initializer of class\n        System.loadLibrary(\"NativeMethodLib\");\n \
      \   }\n}\n```\n这个合规的解决方案声明了本地方法私有。`doOperation()`包装器方法检查权限,创建了一个可变数组副本,并检查参数的范围。\
      \  \n  \n**例外**：JN100-J-EX0:不需要安全管理器检查、验证参数或返回值或可变输入的(例如：标准C函数int rand(void))本地方法不需要被包装。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution declares the native\
      \ method private. The `doOperation()` wrapper method checks permissions, creates\
      \ a defensive copy of the mutable input array `data`, and checks the ranges\
      \ of the arguments. The `nativeOperation()` method is consequently called with\
      \ secure inputs. Note that the validation checks must produce outputs that conform\
      \ to the input requirements of the native methods.\n\n```java\npublic final\
      \ class NativeMethodWrapper {\n\n  // Private native method\n  private native\
      \ void nativeOperation(byte[] data, int offset, int len);\n\n  // Wrapper method\
      \ performs SecurityManager and input validation checks\n  public void doOperation(byte[]\
      \ data, int offset, int len) {\n    // Permission needed to invoke native method\n\
      \    securityManagerCheck();\n\n    if (data == null) {\n      throw new NullPointerException();\n\
      \    }\n\n    // Copy mutable input\n    data = data.clone();\n\n    // Validate\
      \ input\n    if ((offset < 0) || (len < 0) || (offset > (data.length - len)))\
      \ {\n      throw new IllegalArgumentException();\n    }\n\n    nativeOperation(data,\
      \ offset, len);\n  }\n\n  static {\n    // Load native library in static initializer\
      \ of class\n    System.loadLibrary(\"NativeMethodLib\");\n  }\n}\n```\n\n**Exceptions**\n\
      \nJN100-J-EX0: Native methods that do not require security manager checks, validation\
      \ of arguments or return values, or defensive copying of mutable inputs (for\
      \ example, the standard C function `int rand(void)`) do not need to be wrapped."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 17. Java本地接口 (JNI)
      - type: DetailClassChin
        value: JNI00-J：为本地代码定义包装器
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 17. Java Native Interface (JNI)
      - type: DetailClassChin
        value: JNI00-J:Define wrappers around native methods
    Default: true
    Description: "本地方法是用Java定义的，用C和C++等语言编写[JNI 2006]。增加的可扩展性是以灵活性和可移植性为代价的，因为代码不再符合Java所强制执行的政策。本地方法被用于执行特定平台的操作、与遗留库代码的对接，以及提高程序性能[Bloch\
      \ 2008]。\n定义一个封装方法有助于安装适当的安全管理器检查，验证传递给本地代码的参数，验证返回值，防御性地复制易变的输入，以及清理不受信任的数据。因此，每个本地方法都必须是私有的，并且只能由包装方法调用。\n\
      **不规范的代码样例1**：  \n```java\npublic final class NativeMethod {\n\n    // Public\
      \ native method\n    public native void nativeOperation(byte[] data, int offset,\
      \ int len);\n\n    // Wrapper method that lacks security checks and input validation\n\
      \    public void doOperation(byte[] data, int offset, int len) {\n        nativeOperation(data,\
      \ offset, len);\n    }\n\n    static {\n        // Load native library in static\
      \ initializer of class\n        System.loadLibrary(\"NativeMethodLib\");\n \
      \   }\n}\n```  \n这个例子包括`doOperation()`包装器方法,该调用`nativeOperation()`本地方法但未能提供输入验证或安全检查。"
    Description_En: "Native methods are defined in Java and written in languages such\
      \ as C and C++ [JNI 2006]. The added extensibility comes at the cost of flexibility\
      \ and portability because the code no longer conforms to the policies enforced\
      \ by Java. Native methods have been used for performing platform-specific operations,\
      \ interfacing with legacy library code, and improving program performance [Bloch\
      \ 2008].\n\nDefining a wrapper method facilitates installing appropriate security\
      \ manager checks, validating arguments passed to native code, validating return\
      \ values, defensively copying mutable inputs, and sanitizing untrusted data.\
      \ Consequently, every native method must be private and must be invoked only\
      \ by a wrapper method.\n\n**Noncompliant Code Example**\n\nIn this noncompliant\
      \ code example, the `nativeOperation()` method is both native and public; consequently,\
      \ untrusted callers may invoke it. Native method invocations bypass security\
      \ manager checks.\n\nThis example includes the `doOperation()` wrapper method,\
      \ which invokes the `nativeOperation()` native method but fails to provide input\
      \ validation or security checks.\n\n```java\npublic final class NativeMethod\
      \ {\n\n  // Public native method\n  public native void nativeOperation(byte[]\
      \ data, int offset, int len);\n\n  // Wrapper method that lacks security checks\
      \ and input validation\n  public void doOperation(byte[] data, int offset, int\
      \ len) {\n    nativeOperation(data, offset, len);\n  }\n\n  static {\n    //\
      \ Load native library in static initializer of class\n    System.loadLibrary(\"\
      NativeMethodLib\");\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010141900
    language: Java
  - Advice: 检查程序逻辑，重置Content-Length的值。
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要使用负的Content-Length
    Categories_En:
      Category: []
    Default: true
    Description: "Content-Length用于描述HTTP消息实体的传输长度。正常情况下该值应该为0或正整数。  \n  \n**例如**：以下代码设置的Content-Length的值是不正确的。\
      \  \n```java\nURL url = new URL(\"http://www.codesafe.com\");  \nHttpURLConnection\
      \ urlconnection= (HttpURLConnection)url.openConnection();  \nurlconnection.setRequestProperty(\"\
      Content-Length\", \"-1024\");\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Negative Content-Length
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: correctness
    Type: std
    WhiteList: not
    id: '0202000010150432'
    language: Java
  - Advice: "不应为`Random.setSeed`设置特定值作为参数。  \n  \n**例如**：下面代码不为`Random.setSeed`设置特定值。\
      \  \n```java\nRandom a = new Random();  \nint i = a.nextInt();  \ndouble x =\
      \ a.nextDouble();  \n...\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 密码规范
      - type: DetailClassChin
        value: 【推荐】不要将Random.setSeed参数设为特定值
    Categories_En:
      Category: []
    Default: true
    Description: "不应为`Random.setSeed`设置特定值作为参数。如果Random对象使用特定值作为种子，则`Random.nextInt()`以及类似方法返回的值是可以预测的。\
      \  \n  \n**例如**：下面代码可以通过Random对象`a`来预测Random对象`b`生成的值。  \n```java\nRandom a\
      \ = new Random();  \na.setSeed(111111);  \nint i = a.nextInt();  \ndouble x\
      \ = a.nextDouble()  \nRandom b = new Random();  \nb.setSeed(111111);  \nint\
      \ j = b.nextInt();  \ndouble y = b.nextDouble()\n```  \n在此例中，Random对象`a`和`b`设置了相同的种子，`i`和`j`以及`x`和`y`的值都是相等的。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Hardcoded Seed
    ENSubClassChin: Insecure Randomness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 336:Same Seed in Pseudo-Random Number Generator (PRNG)
      - type: CWE
        value: 330:Use of Insufficiently Random Values
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 330
    Security: true
    Tag: security,password,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150205'
    language: Java
  - Advice: "在安全性要求较高的系统中，建议应使用安全的加密算法（如AES、RSA)对敏感数据进行加密。  \n  \n**例如**：下面代码片段中，采用DES对数据进行加密。\
      \  \n```java\nBufferedReader bufread2 = null;  \nInputStreamReader inread2 =\
      \ null;  \ntry {  \n    inread2 = new InputStreamReader(System.in);  \n    bufread2\
      \ = new BufferedReader(inread2);  \n    String str = bufread2.readLine();  \n\
      \    /* FIX: Secure cryptographic algorithm (AES) */  \n    Cipher aes = Cipher.getInstance(\"\
      AES\");  \n    KeyGenerator kg = KeyGenerator.getInstance(\"AES\");  \n    kg.init(128);\
      \  \n    SecretKey key = kg.generateKey();  \n    aes.init(Cipher.ENCRYPT_MODE,\
      \ key);  \n    byte[] enc_str = des.doFinal(str.getBytes());  \n    IO.writeLine(IO.toHex(enc_str));\
      \  \n} catch(IOException e) {  \n    log_bsnk.warning(\"Error reading from console\"\
      );  \n} finally{  \n...  \n}  \n...\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 密码规范
      - type: DetailClassChin
        value: 【推荐】不要使用不安全的加密算法
    Categories_En:
      Category: []
    Default: true
    Description: "在安全性要求较高的系统中，使用不安全的加密算法（如DES、3DES、RC4、RC5等），将无法保证敏感数据的保密性。  \n \
      \ \n**例如**：下面代码片段中，采用DES对数据进行加密。  \n```java\nBufferedReader bufread2 = null;\
      \  \nInputStreamReader inread2 = null;  \ntry {  \n    inread2 = new InputStreamReader(System.in);\
      \  \n    bufread2 = new BufferedReader(inread2);  \n    String str = bufread2.readLine();\
      \  \n    /* FLAW: Insecure cryptographic algorithm (DES) */  \n    Cipher des\
      \ = Cipher.getInstance(\"DES\");  \n    SecretKey key = KeyGenerator.getInstance(\"\
      DES\").generateKey();  \n    des.init(Cipher.ENCRYPT_MODE, key);  \n    byte[]\
      \ enc_str = des.doFinal(str.getBytes());  \n    IO.writeLine(IO.toHex(enc_str));\
      \  \n} catch(IOException e) {  \n    log_bsnk.warning(\"Error reading from console\"\
      );  \n} finally{  \n...  \n}  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: Weak Encryption
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 327:Use of a Broken or Risky Cryptographic Algorithm
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 327
      - type: CWE/SANS TOP 25
        value: 2010:Porous Defenses - CWE ID 327
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 327
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Missing Required Cryptographic Step
          [XZS]
      - type: GB/T 34944-2017 Java
        value: 安全功能:使用已破解或危险的加密算法
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
    Security: true
    Tag: security,password
    Type: std
    WhiteList: not
    id: '0202000010150202'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n```java\nBufferedReader bufread2\
      \ = null;\nInputStreamReader inread2 = null;\ntry {\n    inread2 = new InputStreamReader(System.in);\n\
      \    bufread2 = new BufferedReader(inread2);\n    String str = bufread2.readLine();\n\
      \    /* FIX: Secure cryptographic algorithm (AES) */\n    Cipher aes = Cipher.getInstance(\"\
      AES\");\n    KeyGenerator kg = KeyGenerator.getInstance(\"AES\");\n    kg.init(128);\n\
      \    SecretKey key = kg.generateKey();\n    aes.init(Cipher.ENCRYPT_MODE, key);\n\
      \    byte[] enc_str = aes.doFinal(str.getBytes());\n    IO.writeLine(IO.toHex(enc_str));\n\
      } catch (IOException e) {\n    log_gsnk.warning(\"Error reading from console\"\
      );\n} finally {\n    ...\n} \n...\n```  \n以上代码片段中，使用AES取代DES保证数据完整性。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.2 数据加密与保护
      - type: DetailClassChin
        value: A.2.2.1.5 避免使用不安全的加密算法
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：在安全性要求较高的系统中，使用不安全的加密算法（如DES、3DES、RC4、RC5等），将无法保证敏感数据的保密性。\
      \  \n对于避免使用不安全的加密算法的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：   \n```java\nBufferedReader\
      \ bufread2 = null;\nInputStreamReader inread2 = null;\ntry {\n    inread2 =\
      \ new InputStreamReader(System.in);\n    bufread2 = new BufferedReader(inread2);\n\
      \    String str = bufread2.readLine();\n    /* FLAW: Insecure cryptographic\
      \ algorithm (DES) */\n    Cipher des = Cipher.getInstance(\"DES\");\n    SecretKey\
      \ key = KeyGenerator.getInstance(\"DES\").generateKey();\n    des.init(Cipher.ENCRYPT_MODE,\
      \ key);\n    byte[] enc_str = des.doFinal(str.getBytes());\n    IO.writeLine(IO.toHex(enc_str));\n\
      } catch (IOException e) {\n    log_bsnk.warning(\"Error reading from console\"\
      );\n} finally {\n    ...\n} \n...\n```  \n以上代码片段中，采用DES对数据进行加密。  \n在安全性要求较高的系统中，建议使用安全的加密算法（如AES、RSA)对敏感数据进行加密。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 327:Use of a Broken or Risky Cryptographic Algorithm
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 327
      - type: CWE/SANS TOP 25
        value: 2010:Porous Defenses - CWE ID 327
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 327
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Missing Required Cryptographic Step
          [XZS]
      - type: GB/T 34944-2017 Java
        value: 安全功能:使用已破解或危险的加密算法
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170014'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。   \n**示例2**：   \n```java\nuserName = ctx.getAuthenticatedUserName();\
      \  \nid = Integer.decode(request.getParameter(\"invoiceID\"));  \nString query\
      \ =  \"SELECT * FROM invoices WHERE id = ? AND user = ?\";  \nPreparedStatement\
      \ stmt = conn.prepareStatement(query);  \nstmt.setString(1, id);  \nstmt.setString(2,\
      \ userName);  \nResultSet results = stmt.execute();\n```  \n上述代码片段中，通过把当前被授权的用户名作为查询语句的一部分来限制用户对清单的访问。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 7.3 数据库管理
      - type: DetailClassChin
        value: A.4.3.3 避免数据库访问控制
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：程序未进行恰当的访问控制，执行的SQL语句包含用户所控制的主键，可能会导致攻击者访问未经授权的记录。  \n对于避免数据库访问控制的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**：  \n```java\nid = Integer.decode(request.getParameter(\"invoiceID\"\
      ));  \nString query = \"SELECT * FROM invoices WHERE id = ?\";  \nPreparedStatement\
      \ stmt = conn.prepareStatement(query);  \nstmt.setInt(1, id);  \nResultSet results\
      \ = stmt.execute();  \n...\n```  \n上述代码片段中的SQL语句用于查询与指定标识符相匹配的清单。  \n在上面代码中，攻击者可以通过为`invoiceID`设置不同的值，获取所需的任何清单信息。\
      \  \n任何情况下都不应该允许用户在没有取得相应权限的情况下获取或修改数据库中的记录。可以通过把当前被授权的用户名作为查询语句的一部分来实现。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 566:Authorization Bypass Through User-Controlled SQL Primary Key
      - type: OWASP TOP 10
        value: 2017:A5 Broken Access Control
      - type: OWASP TOP 10
        value: 2013:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2010:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2007:A4 Insecure Direct Object Reference
      - type: OWASP TOP 10
        value: 2004:A2 Broken Access Control
      - type: GB/T 34944-2017 Java
        value: 安全功能:通过用户控制的SQL关键字绕过授权
      - type: PCI DSS
        value: 1.1:Requirement 6.5.2
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2:Requirement 6.5.10
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.10
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.8
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.8
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.8
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.8
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.8
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170064'
    language: Java
  - Advice: "**规范的代码样例1**：  \n检查`File.delete()`的返回值。  \n```java\nFile file = new File(\"\
      file\");\nif (!file.delete()) {\n  System.out.println(\"Deletion failed\");\n\
      }\n```\n**规范的代码样例2**：  \nJava 7 以后可以使用`java.nio.file.Files.delete()`方法删除文件。\
      \  \n```java\nPath file = new File(args[0]).toPath();\ntry {\n  Files.delete(file);\n\
      } catch (IOException x) {\n  System.out.println(\"Deletion failed\");\n  //\
      \ Handle error\n}\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution checks the return\
      \ value of `delete()`:\n\n```java\nFile file = new File(\"file\");\nif (!file.delete())\
      \ {\n  System.out.println(\"Deletion failed\");\n}\n```\n\n**Compliant Solution**\n\
      \nThis compliant solution uses the `java.nio.file.Files.delete()` method from\
      \ Java SE 7 to delete the file:\n\n```java\nPath file = new File(args[0]).toPath();\n\
      try {\n  Files.delete(file);\n} catch (IOException x) {\n  System.out.println(\"\
      Deletion failed\");\n  // Handle error\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO02-J：发现并处理文件相关的错误
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO02-J:Detect and handle file-related errors
    Default: not
    Description: "Java的文件操作方法通常使用返回值而不是抛出异常来指示其错误。  \nJava 7 以前的版本，使用java.io.File类进行文件的IO操作，但它存在不少问题。\
      \  \n其中的一个问题是：许多方法在失败时不能抛出异常，所以不可能获得有用的错误信息。比如说，如果一个文件的删除失败了，程序会得到一个“删除失败”的信息，但并不知道造成删除失败的原因。\
      \  \n因此，忽略文件操作返回值的程序常常不能发现文件操作的失败。Java程序必须对执行文件IO方法的返回值进行检查。  \n**不规范的代码样例1（`delete()`）**：\n\
      ```java\nFile file = new File(args[0]);  \nfile.delete();\n```  \n代码想删除一个文件，但却没有对操作是否成功进行判断。Java\
      \ 7之前，只有程序没有足够的权限来删除文件时，才会抛出SecurityException异常，不会抛出其他异常，所以删除操作可能失败而不被觉察。"
    Description_En: 'Java''s file-manipulation methods often indicate failure with
      a return value instead of throwing an exception. Consequently, programs that
      ignore the return values from file operations often fail to detect that those
      operations have failed. Java programs must check the return values of methods
      that perform file I/O. This is a specific instance of EXP00-J. Do not ignore
      values returned by methods.

      **Noncompliant Code Example (`delete()`)**


      This noncompliant code example attempts to delete a specified file but gives
      no indication of its success. The Java platform requires `File.delete()` to
      throw a `SecurityException` only when the program lacks authorization to delete
      the file [API 2014]. No other exceptions are thrown, so the deletion can silently
      fail.

      ```java

      File file = new File(args[0]);

      file.delete();

      ```'
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141402'
    language: Java
  - Advice: "对应的符合规则的方案检査 delete()方法的返回值。   \n    ```java\n    File file = new File(args[0]);\
      \ if (!file.delete()) {   \n    System.out.println(\"Deletion failed\");\n \
      \   ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 7.5 文件系统
      - type: DetailClassChin
        value: C.13.3 发现并处理与文件相关的错误
    Categories_En:
      Category: []
    Default: not
    Description: "1) 规定   \nJava 程序必须要对执行文件 I/O 方法的返回值进行检查。   \n2) 原理   \nJava 的文件操作方法通常使用返回值而不是抛出异常来指示其错误。许多方法在失败时不能抛出异常，所以不可能获得有用的错误信息。比如说，如果一个文件的删除失败了，程序会得到一个“删除失败”信息，但并不知道，是不是因为文件不存在,\
      \ 是用户没有权限，还是其他的问题而造成的。因此，忽略文件操作返回值的程序常常不能发现文件操作的失败。   \n3) 示例   \n    这个不符合规则的代码示例想要删除一个特定的文件，却没有给出操作成功的指示。只有当程序没有足够的权限来删除文件时，`File.delete()`才抛出\
      \ SecurityException 异常。程序不会抛出其他异常，所以删除操作可能会失败而不被察觉。   \n    ```java\n    File\
      \ file = new File(args[0]);    \n    file.delete();\n    ```   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference:
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160108
    language: Java
  - Advice: "对`write()`方法的参数进行数值0~255的检查。或可采用DataOutputStream类中的`writeInt()`方法，该方法可以输出int类型可表示的所有数值。\
      \  \n**规范的代码样例1**：  \n```java\nclass FileWrite {\n    public static void main(String[]\
      \ args) throws NumberFormatException, IOException {\n        // Perform range\
      \ checking\n        int value = Integer.valueOf(args[0]);\n        if (value\
      \ < 0 || value > 255) {\n            throw new ArithmeticException(\"Value is\
      \ out of range\");\n        }\n        System.out.write(value);\n        System.out.flush();\n\
      \    }\n}\n```\n**规范的代码样例2**：  \n```java\nclass FileWrite {\n    public static\
      \ void main(String[] args) throws NumberFormatException, IOException {\n   \
      \     DataOutputStream dos = new DataOutputStream(System.out);\n        dos.writeInt(Integer.valueOf(args[0].toString()));\n\
      \        System.out.flush();\n    }\n}\n```"
    Advice_En: "**Compliant Solution (Range-Check Inputs)**\n\nThis compliant solution\
      \ prints the corresponding character only if the input integer is in the proper\
      \ range. If the input is outside the representable range of an `int`, the `Integer.valueOf()`\
      \ method throws a `NumberFormatException`. If the input can be represented by\
      \ an `int` but is outside the range required by `write()`, this code throws\
      \ an `ArithmeticException`.\n\n```java\nclass FileWrite {\n  public static void\
      \ main(String[] args) throws NumberFormatException, IOException {\n    // Perform\
      \ range checking\n    int value = Integer.valueOf(args[0]);\n    if (value <\
      \ 0 || value > 255) {\n      throw new ArithmeticException(\"Value is out of\
      \ range\");\n    }\n\n    System.out.write(value);\n    System.out.flush();\n\
      \  }\n}\n```\n\n**Compliant Solution (`writeInt()`)**\n\nThis compliant solution\
      \ uses the `writeInt()` method of the `DataOutputStream` class, which can output\
      \ the entire range of values representable as an `int`:\n\n```java\nclass FileWrite\
      \ {\n  public static void main(String[] args)\n                          throws\
      \ NumberFormatException, IOException {\n    DataOutputStream dos = new DataOutputStream(System.out);\n\
      \    dos.writeInt(Integer.valueOf(args[0].toString()));\n    System.out.flush();\n\
      \  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO09-J：不要使用write()方法输出超过0~255的整数
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO09-J:Do not rely on the write() method to output integers outside
          the range 0 to 255
    Default: true
    Description: "在`java.io.OutputStream`中定义的`write()`方法，将一个类型为int的值做为参数，而这个参数必须在0~255之间。因为一个类型为int的数值会超出这个范围，该方法会将数值的高位部分截去。\
      \  \n风险评估：违反该条准则可能会导致数据被截断。    \n**不符合规范的样例1**：  \n```java\nclass ConsoleWrite\
      \ {\n    public static void main(String[] args) {\n        // Any input value\
      \ > 255 will result in unexpected output\n        System.out.write(Integer.valueOf(args[0]));\n\
      \        System.out.flush();\n    }\n}\n```  \n代码没有对参数进行数值的验证，任何不在0~255之间的数值都会被截断。"
    Description_En: "The `write()` method, defined in the class `java.io.OutputStream`,\
      \ takes an argument of type `int` the value of which must be in the range 0\
      \ to 255. Because a value of type `int` could be outside this range, failure\
      \ to range check can result in the truncation of the higher-order bits of the\
      \ argument.\n\nThe general contract for the `write()` method says that it writes\
      \ one byte to the output stream. The byte to be written constitutes the eight\
      \ lower-order bits of the argument `b`, passed to the `write()` method; the\
      \ 24 high-order bits of `b` are ignored (see `<a class=\"external-link\" href=\"\
      https://docs.oracle.com/javase/8/docs/api/java/io/OutputStream.html\" rel=\"\
      nofollow\">java.io.OutputStream.write()</a>` [[API 2014] for more information).\n\
      **Noncompliant Code Example**\n\nThis noncompliant code example accepts a value\
      \ from the user without validating it. Any value that is not in the range of\
      \ 0 to 255 is truncated. For instance, `write(303)` prints `/` on ASCII-based\
      \ systems because the lower-order 8 bits of 303 are used while the 24 high-order\
      \ bits are ignored (303 % 256 = 47, which is the ASCII code for `/`). That is,\
      \ the result is the remainder of the input divided by 256.\n\n```java\nclass\
      \ ConsoleWrite {\n  public static void main(String[] args) {\n    // Any input\
      \ value > 255 will result in unexpected output\n    System.out.write(Integer.valueOf(args[0]));\n\
      \    System.out.flush();\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference:
      - type: CWE
        value: 252:Unchecked Return Value
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010141409
    language: Java
  - Advice: "**规范的代码样例1**：  \n这个代码样例总是基于Base.class进行同步，也可使用`Class.forName(\"…\")`进行同步。\
      \  \n```java\nclass Base {\n    static DateFormat format = DateFormat.getDateInstance(DateFormat.MEDIUM);\n\
      \n    public Date parse(String str) throws ParseException {\n        synchronized\
      \ (Base.class) {\n            return format.parse(str);\n        }\n    }\n\
      }\n```  \n**规范的代码样例2**：  \nBase类和Helper类同时给予Base的内部锁来实现锁定。  \n```java\nclass\
      \ Base {\n    static DateFormat format = DateFormat.getDateInstance(DateFormat.MEDIUM);\n\
      \n    public Date parse(String str) throws ParseException {\n        synchronized\
      \ (Base.class) {\n            return format.parse(str);\n        }\n    }\n\n\
      \    public Date doSomething(String str) throws ParseException {\n        return\
      \ new Helper().doSomethingAndParse(str);\n    }\n\n    private class Helper\
      \ {\n        public Date doSomethingAndParse(String str) throws ParseException\
      \ {\n            synchronized (Base.class) { // Synchronizes on Base.class\n\
      \                return format.parse(str);\n            }\n        }\n    }\n\
      }\n```"
    Advice_En: "**Compliant Solution (Class Name Qualification)**\n\nIn this compliant\
      \ solution, the class name providing the lock (`Base`) is fully qualified:\n\
      \n```java\nclass Base {\n  static DateFormat format =\n      DateFormat.getDateInstance(DateFormat.MEDIUM);\n\
      \n  public Date parse(String str) throws ParseException {\n    synchronized\
      \ (Base.class) {\n      return format.parse(str);\n    }\n  }\n}\n\n// ...\n\
      ```\n\nThis code example always synchronizes on the `Base.class` object, even\
      \ when it is called from a `Derived` object.\n**Compliant Solution (Class Name\
      \ Qualification)**\n\nThis compliant solution synchronizes using a `Base` class\
      \ literal in the `parse()` and `doSomethingAndParse()` methods:\n\n```java\n\
      class Base {\n  // ...\n\n  public Date parse(String str) throws ParseException\
      \ {\n    synchronized (Base.class) {\n      return format.parse(str);\n    }\n\
      \  }\n\n  private class Helper {\n    public Date doSomethingAndParse(String\
      \ str) throws ParseException {\n      synchronized (Base.class) { // Synchronizes\
      \ on Base class literal\n        // ...\n        return format.parse(str);\n\
      \      }\n    }\n  }\n}\n```\n\nConsequently, both `Base` and `Helper` lock\
      \ on `Base`'s intrinsic lock. Similarly, the `Class.forName()` method can be\
      \ used instead of a class literal."
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 09. 锁 (LCK)
      - type: DetailClassChin
        value: LCK02-J：不要基于通过getClass()返回的类对象进行同步
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 09. Locking (LCK)
      - type: DetailClassChin
        value: LCK02-J:Do not synchronize on the class object returned by getClass()
    Default: true
    Description: "基于`Object.getClass()`方法的返回值进行同步，会导致不可预期的行为。当类实现继承时，子类会对基类进行锁定。对于子类的Class对象来说，它与其基类的Class对象是完全不同的。\
      \  \n基于Java语言规范的4.3.2节“Class对象”的描述：  \n> 如果声明一个类方法为同步，那么它会基于这个类的Class对象实现锁。\n\
      \n以下认识是不对的：如果一个子类使用对`getClass()`的同步，就会基于其基类的Class对象进行同步。  \n当子类不能覆写一个基类的方法时，这个方法会被继承，从而可能导致一个错误的结果，即在子类中可以得到基类的内部锁。当基于类进行同步时，对应的锁对象不能被非受信的代码进入。\
      \  \n**不规范的代码样例1**：  \n```java\nclass Base {\n    static DateFormat format =\
      \ DateFormat.getDateInstance(DateFormat.MEDIUM);\n\n    public Date parse(String\
      \ str) throws ParseException {\n        synchronized (getClass()) {\n      \
      \      return format.parse(str);\n        }\n    }\n}\n\nclass Derived extends\
      \ Base {\n    public Date doSomethingAndParse(String str) throws ParseException\
      \ {\n        synchronized (Base.class) {\n            // . . .\n           \
      \ return format.parse(str);\n        }\n    }\n}\n```  \nBase类的方法`parse()`方法会解析一个日期并且禁用那个`getClass()`方法返回的对象进行同步。Derived同样继承了`parse()`方法。然而，这个继承方法基于Derived类实现同步，因为继承的`parse()`方法对`getClass()`进行调用，实际上调用的是`this.getClass()`，这个this参数实际上是指向Derived类实例的一个引用。\
      \  \n**不规范的代码样例2**：  \n```java\nclass Base {\n    static DateFormat format =\
      \ DateFormat.getDateInstance(DateFormat.MEDIUM);\n\n    public Date parse(String\
      \ str) throws ParseException {\n        synchronized (getClass()) {\n      \
      \      return format.parse(str);\n        }\n    }\n\n    public Date doSomething(String\
      \ str) throws ParseException {\n        return new Helper().doSomethingAndParse(str);\n\
      \    }\n\n    private class Helper {\n        public Date doSomethingAndParse(String\
      \ str) throws ParseException {\n            synchronized (getClass()) { // Synchronizes\
      \ on Helper.class\n                return format.parse(str);\n            }\n\
      \        }\n    }\n}\n```\nHerper类中对`getClass()`方法的调用会返回一个Helper类对象而不是Base类对象。因此，一个线程其实是通过调用`Base.parse()`方法来锁定一个不同的对象而不是`Base.doSomething()`类实现。这样很容易忽视内部类中产生的并发错误，因外它们存在于包含它们的外部类的代码块中。"
    Description_En: "Synchronizing on the return value of the `Object.getClass()`\
      \ method can lead to unexpected behavior. Whenever the implementing class is\
      \ subclassed, the subclass locks on the subclass's type. The `Class` object\
      \ of the subclass is entirely distinct from the `Class` object of the parent\
      \ class.\n\nAccording to *The Java Language Specification*, §4.3.2, &#34;The\
      \ Class Object&#34; [JLS 2005]:\n\n> A class method that is declared `synchronized`\
      \ synchronizes on the lock associated with the `Class` object of the class.\n\
      \nProgrammers who interpret this to mean that a subclass using `getClass()`\
      \ will synchronize on the `Class` object of the base class are incorrect. The\
      \ subclass will actually lock on its own `Class` object, which may or may not\
      \ be what the programmer intended. Consequently, programs must not synchronize\
      \ on the class object returned by `getClass()`.\n\nThe programmer's actual intent\
      \ should be clearly documented or annotated. Note that when a subclass fails\
      \ to override an accessible noncompliant superclass's method, it inherits the\
      \ method, which may lead to the false conclusion that the superclass's intrinsic\
      \ lock is available in the subclass.\n\nWhen synchronizing on a class literal,\
      \ the corresponding lock object should be inaccessible to untrusted code. Callers\
      \ from other packages cannot access class objects that are package-private;\
      \ consequently, synchronizing on the intrinsic lock object of such classes is\
      \ permitted (see LCK00-J. Use private final lock objects to synchronize classes\
      \ that may interact with untrusted code for more information).\n\n**Noncompliant\
      \ Code Example (`getClass()` Lock Object)**\nIn this noncompliant code example,\
      \ the `parse()` method of the `Base` class parses a date and synchronizes on\
      \ the class object returned by `getClass()`. The `Derived` class also inherits\
      \ the `parse()` method. However, this inherited method synchronizes on `Derived`'s\
      \ class object because the inherited parse method's invocation of `getClass()`\
      \ is really an invocation of `this.getClass()`, and the `this` argument is a\
      \ reference to the instance of the `Derived` class.\n\nThe `Derived` class also\
      \ adds a `doSomethingAndParse()` method that locks on the class object of the\
      \ `Base` class because the developer misconstrued that the `parse()` method\
      \ in `Base` always obtains a lock on the `Base` class object, and `doSomethingAndParse()`\
      \ must follow the same locking policy. Consequently, the `Derived` class has\
      \ two different locking strategies and fails to be thread-safe.\n\n```java\n\
      class Base {\n  static DateFormat format =\n      DateFormat.getDateInstance(DateFormat.MEDIUM);\n\
      \n  public Date parse(String str) throws ParseException {\n    synchronized\
      \ (getClass()) {\n      return format.parse(str);\n    }\n  }\n}\n\nclass Derived\
      \ extends Base {\n  public Date doSomethingAndParse(String str) throws ParseException\
      \ {\n    synchronized (Base.class) {\n      // ...\n      return format.parse(str);\n\
      \    }\n  }\n}\n```\n**Noncompliant Code Example (`getClass()` Lock Object,\
      \ Inner Class)**\n\nThis noncompliant code example synchronizes on the class\
      \ object returned by `getClass()` in the `parse()` method of class `Base`. The\
      \ `Base` class also has a nested `Helper` class whose `doSomethingAndParse()`\
      \ method incorrectly synchronizes on the value returned by `getClass()`.\n\n\
      ```java\nclass Base {\n  static DateFormat format =\n      DateFormat.getDateInstance(DateFormat.MEDIUM);\n\
      \n  public Date parse(String str) throws ParseException {\n    synchronized\
      \ (getClass()) { // Intend to synchronizes on Base.class\n      return format.parse(str);\n\
      \    }\n  }\n\n  public Date doSomething(String str) throws ParseException {\n\
      \    return new Helper().doSomethingAndParse(str);\n  }\n\n  private class Helper\
      \ {\n    public Date doSomethingAndParse(String str) throws ParseException {\n\
      \      synchronized (getClass()) { // Synchronizes on Helper.class\n       \
      \ // ...\n        return format.parse(str);\n      }\n    }\n  }\n}\n```\n\n\
      The call to `getClass()` in the `Helper` class returns a `Helper` class object\
      \ instead of the `Base` class object. Consequently, a thread that calls `Base.parse()`\
      \ locks on a different object than a thread that calls `Base.doSomething()`.\
      \ It is easy to overlook concurrency errors in inner classes because they exist\
      \ within the body of the containing outer class. A reviewer might incorrectly\
      \ assume that the two classes have the same locking strategy."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141002'
    language: Java
  - Advice: "对应符合规则的解决方案(类名合规)中，通过类名提供的锁（Base)是合规的。   \n    ```java\n    class Base\
      \ {\n        static DateFormat format = DateFormat.getDateInstance(DateFormat.MEDIUM);\n\
      \    \n        public Date parse(String str) throws ParseException {\n     \
      \       synchronized (Base.class) {\n                return format.parse(str);\n\
      \            }\n        }\n    }  \n    //...\n    ```   \n    它总是基于 Base.class\
      \ 对象实现同步，即便是在一个 Derived 对象中进行调用。   \n       \n    另一个符合规则的解决方案使用了 `Class.forName()`方法来基于\
      \ Base 的类对象进行同步。   \n    ```java\n    class Base {   \n    static DateFormat\
      \ format =   \n    DateFormat.getDatelnstance(DateFormat.MEDIUM);   \n    public\
      \ Date parse(String str) throws ParseException { try {   \n    synchronized\
      \ (Class.forName(\"Base\")) {   \n    return format.parse(str);   \n    }  \
      \ \n    } catch (ClassNotFoundException x) {   \n    // \"Base\" not found;\
      \ handle error   \n    }   \n    return null;   \n    // . . .\n    ```    \n\
      \    不要在使用 `Class.forName()`方法载入类时，接受非受信的输入作为参数。   \n       \n    2、不符合规则的代码示例（`getClass()`锁对象，内隐类），该示例中，Base\
      \ 类的 `parse()`方法通过`getClass()`返回的类对象实现同步。这个 Base 类同样是一个 Helper 类，这个类的 `doSomethingAndParse()`基于`getClass()`的返回值实现同步是不正确的。\
      \   \n    ```java\n    class Base {\n        static DateFormat format = DateFormat.getDateInstance(DateFormat.MEDIUM);\n\
      \    \n        public Date parse(String str) throws ParseException {\n     \
      \       synchronized (getClass()) {\n                return format.parse(str);\n\
      \            }\n        }\n    \n        public Date doSomething(String str)\
      \ throws ParseException {\n            return new Helper().doSomethingAndParse(str);\n\
      \        }\n    \n        private class Helper {\n            public Date doSomethingAndParse(String\
      \ str) throws ParseException {\n                synchronized (getClass()) {\
      \ // Synchronizes on Helper.class\n                    return format.parse(str);\n\
      \                }\n            }\n        }\n    }\n    ```   \n    在 Helper\
      \ 类中对`getClass()`方法的调用会返回一个 Helper 类对象而不是 Base 类对象。因此，一个线程其实是通过调用 `Base.parse()`方法来锁定一个不同的对象而不是通过调用\
      \ `Base.doSomething()`来实现。这样容易忽视在内隐类中产生的并发错误，因为它们存在于包含它们的外部类的代码块中。   \n    \
      \   \n    对应符合规则的解决方案（类名合规）中，使用 `parse()`和 `doSomethingAndParse()`方法中的 Base类的字元实现同步。\
      \   \n    ```java\n    class Base {\n        static DateFormat format = DateFormat.getDateInstance(DateFormat.MEDIUM);\n\
      \    \n        public Date parse(String str) throws ParseException {\n     \
      \       synchronized (Base.class) {\n                return format.parse(str);\n\
      \            }\n        }\n    \n        public Date doSomething(String str)\
      \ throws ParseException {\n            return new Helper().doSomethingAndParse(str);\n\
      \        }\n    \n        private class Helper {\n            public Date doSomethingAndParse(String\
      \ str) throws ParseException {\n                synchronized (Base.class) {\
      \ // Synchronizes on Base.class\n                    return format.parse(str);\n\
      \                }\n            }\n        }\n    }\n    ```   \n    Base和Helper类同时基于Base的内置锁来实现锁定。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.9.3 不要基于那些通过getClass()返回的类对象来实现同步
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n当基于类进行同步时，对应的锁对象不应被非受信的代码进入。其他包的调用者不能进入那些包私有的类对象。   \n\
      2) 原理   \n基于 `Object.getClass()`方法的返回值进行同步，会导致不可预期的行为。当类在实现继承时，子类会对基类进行锁定。对于子类的\
      \ Class 对象来说，它与其基类的 Class 对象是完全不同的。   \n如果声明一个类方法为同步，那么它会基于这个类的 Class 对象实现锁。\
      \   \n如果开发人员认为“一个子类使用`getClass()`，就会基于其基类的 Class 对象进行同步”，这是不对的。一个子类会基于它自己的 Class\
      \ 对象实现锁，可能会是或者不是开发人员想要的功能。因此，开发人员不能基于从`getClass()`方法返回的类对象来实现同步。   \n当一个子类不能覆写一个可进入的不合规的基类的方法时，这个方法会被继承，从而可能会导致一个错误的结论，即在子类中可以得到基类的内置锁。\
      \   \n当基于类进行同步时，对应的锁对象不应被非受信的代码进入。其他包的调用者不能进入那些包私有的类对象。因此，基于那些类的内置锁对象进行的同步是允许的。\
      \   \n3) 示例   \n    1、不符合规则的代码示例（`getClass()`锁对象），该例中，Base 类的`parse()`方法会解析一个日期并且基于`getClass()`方法返回的类对象进行同步。Derived\
      \ 类同样继承了`parse()`方法。然而，这个继承的方法基于 Derived 类实现同步，因为继承的 `parse()`方法对 `getClass()`的调用，实际上调用的是`this.getClass()`，因此这个\
      \ this 参数实际上是指向 Derived 类实例的一个引用。   \n    Derived 类同样增加了一个 `doSomethingAndParse()`方法，这个方法基于\
      \ Base 类的类对象实现锁定，因为开发人员会错误地认为在 Base 类中创建的` parse() `方法总会获得一个基于 Base 类对象的锁，并且`doSomethingAndParse()`方法必须遵循同样的锁规则。因此，Dervied\
      \ 类有两个不同的锁策略并且不是线程安全的。   \n    ```java\n    class Base {\n        static DateFormat\
      \ format = DateFormat.getDateInstance(DateFormat.MEDIUM);\n    \n        public\
      \ Date parse(String str) throws ParseException {\n            synchronized (getClass())\
      \ {\n                return format.parse(str);\n            }\n        }\n \
      \   }\n    \n    class Derived extends Base {\n        public Date doSomethingAndParse(String\
      \ str) throws ParseException {\n            synchronized (Base.class) {\n  \
      \              // . . .\n                return format.parse(str);\n       \
      \     }\n        }\n    }\n    ```    \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160081
    language: Java
  - Advice: "示例2 给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nclass Base {  \n    static\
      \ DateFormat format = DateFormat.getDateInstance(DateFormat.MEDIUM);  \n   \
      \ public Date parse(String str) throws ParseException {  \n        synchronized\
      \ (Base.class) {  \n            return format.parse(str);  \n        }  \n \
      \   }  \n}\n```  \n这个代码样例是基于Base.class进行同步，也可使用`Class.forName(\"…\")`进行同步。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.4 不要基于通过getClass()返回的类对象进行同步
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：基于`Object.getClass()`方法的返回值进行同步，会导致不可预期的行为。当类实现继承时，子类会对基类进行锁定。对于子类的Class对象来说，它与其基类的Class对象是完全不同的。\
      \  \n对于不要基于通过`getClass()`返回的类对象进行同步的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n\
      ```java\nclass Base {\n    static DateFormat format = DateFormat.getDateInstance(DateFormat.MEDIUM);\n\
      \n    public Date parse(String str) throws ParseException {\n        synchronized\
      \ (getClass()) {\n            return format.parse(str);\n        }\n    }\n\
      }\n\nclass Derived extends Base {\n    public Date doSomethingAndParse(String\
      \ str) throws ParseException {\n        synchronized (Base.class) {\n      \
      \      // . . .\n            return format.parse(str);\n        }\n    }\n}\n\
      ```  \nBase类的方法`parse()`方法会解析一个日期并且直接使用`getClass()`方法返回的对象进行同步。Derived同样继承了`parse()`方法。然而，这个继承方法基于Derived类实现同步，因为继承的`parse()`方法对`getClass()`进行调用，实际上调用的是`this.getClass()`，这个this参数实际上是指向Derived类实例的一个引用。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170036'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nConnection conn=null; \
      \ \nPreparedStatement stmt=null;  \ntry{  \n    conn = DriverManager.getConnection(some_connection_string);\
      \  \n    stmt = conn.prepareStatement(sqlquery);  \n    ...  \n} catch(SQLException\
      \ e){  \n    log(e);  \n}finally{  \n    if(con!=null){  \n        try {  \n\
      \            conn.close();  \n        } catch (SQLException e) {  \n       \
      \     log(e);  \n        }  \n    }  \n    if(stmt!=null){  \n        try {\
      \  \n            stmt.close();  \n        } catch (SQLException e) {  \n   \
      \         log(e);  \n        }  \n    }  \n}\n```  \n上述代码片段中，在finally代码块中合理释放了数据库资源。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 7.3 数据库管理
      - type: DetailClassChin
        value: A.4.3.2 需确保数据库资源得到释放
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：程序创建或分配数据库资源后，不进行合理释放，将会降低系统性能。攻击者可能会通过耗尽资源池的方式发起拒绝服务攻击。  \n\
      对于需确保数据库资源得到释放的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\ntry {  \n \
      \   Connection conn = DriverManager.getConnection(some_connection_string)  \n\
      \    PreparedStatement stmt = conn.prepareStatement(sqlquery);  \n    ...  \n\
      } catch (Exception e) {  \n    log(e);  \n}\n```  \n在上述Java代码片段中，创建数据库对象资源后未进行合理释放，在繁忙的程序环境下，数据库连接池可能会耗尽，从而导致其他用户不能访问该数据库资源。\
      \  \n程序不应依赖于`finalize()`回收数据库资源，应在finally代码块中手动释放数据库资源。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 772:Missing Release of Resource after Effective Lifetime
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 404:Improper Resource Shutdown or Release
      - type: CERT
        value: FIO04-J:Close resources when they are no longer needed
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 772
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 404
      - type: SJ/T 11683-2017
        value: 6.6.1 资源泄漏
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170063'
    language: Java
  - Advice: "**规范的代码样例1**：\n```java\npublic final class Client {\n  private final\
      \ Lock lock = new ReentrantLock();\n\n  public void doSomething(File file) {\n\
      \    InputStream in = null;\n    lock.lock();\n    try {\n      in = new FileInputStream(file);\n\
      \      // Perform operations on the open file\n    } catch (FileNotFoundException\
      \ fnf) {\n      // Forward to handler\n    } finally {\n      lock.unlock();\n\
      \n      if (in != null) {\n        try {\n          in.close();\n        } catch\
      \ (IOException e) {\n          // Forward to handler\n        }\n      }\n \
      \   }\n  }\n}\n```\n代码使用了ReentrantLock来保护资源，当打开的文件进行操作而发生异常时或不发生异常时，程序都会走到finally代码块，`lock.unlock()`方法执行，可成功释放持有的锁。"
    Advice_En: "**Compliant Solution (`finally` Block)**\n\nThis compliant solution\
      \ encapsulates operations that could throw an exception in a `try` block immediately\
      \ after acquiring the lock (which cannot throw). The lock is acquired just before\
      \ the `try` block, which guarantees that it is held when the `finally` block\
      \ executes.\n\n```java\npublic final class Client {\n  private final Lock lock\
      \ = new ReentrantLock();\n\n  public void doSomething(File file) {\n    InputStream\
      \ in = null;\n    lock.lock();\n    try {\n      in = new FileInputStream(file);\n\
      \      // Perform operations on the open file\n    } catch (FileNotFoundException\
      \ fnf) {\n      // Forward to handler\n    } finally {\n      lock.unlock();\n\
      \n      if (in != null) {\n        try {\n          in.close();\n        } catch\
      \ (IOException e) {\n          // Forward to handler\n        }\n      }\n \
      \   }\n  }\n}\n```"
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 09. 锁 (LCK)
      - type: DetailClassChin
        value: LCK08-J：在异常时保证释放已持有的锁
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 09. Locking (LCK)
      - type: DetailClassChin
        value: LCK08-J:Ensure actively held locks are released on exceptional conditions
    Default: true
    Description: "一个异常条件会阻碍释放锁，从而导致死锁。根据Java API描述：一个ReentrantLock是由一个成功的执行了锁定但是没有执行解锁操作的线程所拥有的，如果没有其他线程持有锁，一个调用Lock的线程会成功的获得锁并返回。\
      \  \n因此，一个在任何线程中没有被释放的锁会阻止其他线程得到这个锁。在发生异常条件的情况下，线程必须释放当前持有的锁。  \n风险评估：如果在异常条件下释放锁失败，会导致线程饥饿或死锁。\
      \  \n**不规范的代码样例1**：  \n```java\npublic final class Client {\n    public void\
      \ doSomething(File file) {\n        final Lock lock = new ReentrantLock();\n\
      \        InputStream in = null;\n        try {\n            lock.lock();\n \
      \           in = new FileInputStream(file);\n            // Perform operations\
      \ on the open file\n            lock.unlock();\n        } catch (FileNotFoundException\
      \ x) {\n            // Handle exception\n        } finally {\n            if\
      \ (in != null) {\n                try {\n                    in.close();\n \
      \               } catch (IOException x) {\n                    // Handle exception\n\
      \                }\n            }\n        }\n    }\n}\n```  \n代码使用了ReentrantLock来保护资源，当打开的文件进行操作而发生异常时，却不能释放持有的锁。当发生异常时，控制权会移交给catch代码块，并且`unlock()`方法不会执行。"
    Description_En: "An exceptional condition can circumvent the release of a lock,\
      \ leading to deadlock. According to the Java API [API 2014]:\n\n> A `ReentrantLock`\
      \ is owned by the thread last successfully locking, but not yet unlocking it.\
      \ A thread invoking `lock` will return, successfully acquiring the lock, when\
      \ the lock is not owned by another thread.\n\nConsequently, an unreleased lock\
      \ in any thread will prevent other threads from acquiring the same lock. Programs\
      \ must release all actively held locks on exceptional conditions. Intrinsic\
      \ locks of class objects used for method and block synchronization are automatically\
      \ released on exceptional conditions (such as abnormal thread termination).\n\
      \nThis guideline is an instance of FIO04-J. Release resources when they are\
      \ no longer needed. However, most Java lock objects are not closeable, so they\
      \ cannot be automatically released using Java 7's `try`-with-resources feature.\n\
      \n**Noncompliant Code Example (Checked Exception)**\n\nThis noncompliant code\
      \ example protects a resource, an open file, by using a `ReentrantLock`. However,\
      \ the method fails to release the lock when an exception occurs while performing\
      \ operations on the open file. When an exception is thrown, control transfers\
      \ to the `catch` block and the call to `unlock()` never executes.\n\n```java\n\
      public final class Client {\n  private final Lock lock = new ReentrantLock();\n\
      \n  public void doSomething(File file) {\n    InputStream in = null;\n    try\
      \ {\n      in = new FileInputStream(file);\n      lock.lock();\n\n      // Perform\
      \ operations on the open file\n\n      lock.unlock();\n    } catch (FileNotFoundException\
      \ x) {\n      // Handle exception\n    } finally {\n      if (in != null) {\n\
      \        try {\n          in.close();\n        } catch (IOException x) {\n \
      \         // Handle exception\n        }\n      }\n    }\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 772
      - type: CWE
        value: 772:Missing Release of Resource after Effective Lifetime
      - type: CWE
        value: 667:Improper Locking
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 411:Resource Locking Problems
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 6.6.1 资源泄漏
      - type: SJ/T 11683-2017
        value: 6.8.2 获取锁后没有释放锁
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010141008
    language: Java
  - Advice: "对应的符合规则的解决方案（finally 代码块）是：在成功地执行锁定操作之后，将可能抛出异常的操作封装在 try 代码块中。这个锁在执行\
      \ try 代码块之前获得，这可以保证当执行 finally 代码块时，可以持有锁。在 finally 代码块中调用 `Lock.unlock()`方法，可以保证不管是否发生一个异常，总会释放锁。\
      \   \n    ```java\n    public final class Client {\n        public void doSomething(File\
      \ file) {\n            final Lock lock = new ReentrantLock();\n            InputStream\
      \ in = null;\n            try {\n                lock.lock();\n            \
      \    in = new FileInputStream(file);\n                // Perform operations\
      \ on the open file\n                lock.unlock();\n            } catch (FileNotFoundException\
      \ x) {\n                // Handle exception\n            } finally {\n     \
      \           lock.unlock();\n                if (in != null) {\n            \
      \        try {\n                        in.close();\n                    } catch\
      \ (IOException x) {\n                        // Handle exception\n         \
      \           }\n                }\n            }\n    ```    \n       \n    另一个符合规则的解决方案是：围绕执行惯用法（`execute-aroimd\
      \ idiom)`提供一个通用的机制来完成资源的分配并且进行清理操作，这样一个客户可以专注于那些必需的功能上。该惯用法减少了客户代码中的碎片，并且为管理资源提供了一个安全机制。\
      \   \n    本示例中，客户的`doSomething()`方法仅提供了必需的功能，是通过实现 LockAction 接口的`doSomethingWithFile()`方法来实现的，并且这个实现不需要管理锁的获取和释放，\
      \ 也不需要管理文件的打开或关闭操作。这个 ReentrantLockAction 类封装了所有的资源管理动作。   \n    ```java\n \
      \       public interface LockAction {\n            void doSomethingWithFile(InputStream\
      \ in);\n        }\n    \n        public final class ReentrantLockAction {\n\
      \            private static final Lock lock = new ReentrantLock();\n    \n \
      \           public static void doSomething(File file, LockAction action) {\n\
      \                InputStream in = null;\n                lock.lock();\n    \
      \            try {\n                    in = new FileInputStream(file);\n  \
      \                  action.doSomethingWithFile(in);\n                } catch\
      \ (FileNotFoundException fnf) {\n                    // Forward to handler\n\
      \                } finally {\n                    lock.unlock();\n    \n   \
      \                 if (in != null) {\n                        try {\n       \
      \                     in.close();\n                        } catch (IOException\
      \ e) {\n                            // Forward to handler\n                \
      \        }\n                    }\n                }\n            }\n      \
      \  }\n    \n        public final class Client {\n            public void doSomething(File\
      \ file) {\n                ReentrantLockAction.doSomething(file, new LockAction()\
      \ {\n                    public void doSomethingWithFile(InputStream in) {\n\
      \                        // Perform operations on the open file\n          \
      \          }\n                });\n    ```   \n       \n    2、不符合规则的代码示例（未经检查的异常），该示例使用了一个\
      \ ReentrantLock 来保护一个`java.util.Date`实例。   \n    ```java\n    final class DateHandler\
      \ {\n    \n        private final Date date = new Date();\n    \n        private\
      \ final Lock lock = new ReentrantLock();\n    \n        // str could be null\n\
      \        public void doSomething(String str) {\n            lock.lock();\n \
      \           String dateString = date.toString();\n            if (str.equals(dateString))\
      \ {\n                // ...\n            }\n            // ...\n    \n     \
      \       lock.unlock();\n        }\n    }\n    ```   \n    发生一个执行期异常，因为 `doSomething()`方法不能检査\
      \ str 是否是一个空引用，从而阻止了锁的释放。   \n       \n    对应符合规则的解决方案（finally 代码块）是：封装所有的操作，这些操作会在\
      \ try 代码块中抛出异常，并且释放那些在关联代码块中的锁。因此，甚至在一个执行期异常事件中，也会释放锁。   \n    ```java\n   \
      \ final class DateHandler {\n    \n        private final Date date = new Date();\n\
      \    \n        private final Lock lock = new ReentrantLock();\n    \n      \
      \  // str could be null\n        public void doSomething(String str) {\n   \
      \         lock.lock();\n            try {\n                String dateString\
      \ = date.toString();\n                if (str != null && str.equals(dateString))\
      \ {\n                    // ...\n                }\n                // ...\n\
      \    \n            } finally {\n                lock.unlock();\n           \
      \ }\n        }\n    }\n    ```   \n    `doSomething()`方法同样避免抛出一个 NullPointerException\
      \ 异常，可以通过保证这个字符串不包含一个空引用来实现。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.9.9 在异常条件时，保证释放已经持有的锁
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n在发生异常条件的情况下，程序必须释放当前持有的锁。   \n2) 原理   \n一个异常条件会阻碍释放锁，从而导致死锁。\
      \   \n根据 Java API的描述: 一个ReentnmtLock是由一个成功地执行了锁定但没有执行解锁操作的线程所拥有的，如果没有其他的线程持有锁，一个调用\
      \ Lock 的线程会成功的获得锁并返回。   \n因此，一个在任何线程中没有被释放的锁会阻止其他线程得到这个锁。在发生异常条件的情况下，程序必须释放当前持有的锁。在异常条件下（例如线程的非正常终结），被方法块和代码块同步使用的类对象的内置锁会自动释放。\
      \   \n3) 示例   \n    1、不符合规则的代码示例（可检查异常），该示例使用 ReentrantLock 来保护资源，当对打开的文件进行操作而发生异常时，却不能释放持有的锁。当抛出一个异常时，控制权会移交给\
      \ catch 代码块，并且 `unlock()`方法的调用不会成功。   \n    ```java\n    public final class\
      \ Client {\n        public void doSomething(File file) {\n            final\
      \ Lock lock = new ReentrantLock();\n            InputStream in = null;\n   \
      \         try {\n                lock.lock();\n                in = new FileInputStream(file);\n\
      \                // Perform operations on the open file\n                lock.unlock();\n\
      \            } catch (FileNotFoundException x) {\n                // Handle\
      \ exception\n            } finally {\n                if (in != null) {\n  \
      \                  try {\n                        in.close();\n            \
      \        } catch (IOException x) {\n                        // Handle exception\n\
      \                    }\n                }\n    ```  \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 772
      - type: CWE
        value: 772:Missing Release of Resource after Effective Lifetime
      - type: CWE
        value: 667:Improper Locking
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 411:Resource Locking Problems
      - type: CERT
        value: LCK08-J:Ensure actively held locks are released on exceptional conditions
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 6.6.1 资源泄漏
      - type: SJ/T 11683-2017
        value: 6.8.2 获取锁后没有释放锁
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160087
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\npublic final class Client\
      \ {  \n    public void doSomething(File file) {\n        final Lock lock = new\
      \ ReentrantLock();\n        InputStream in = null;\n        try {\n        \
      \    lock.lock();\n            in = new FileInputStream(file);\n           \
      \ // Perform operations on the open file\n            lock.unlock();\n     \
      \   } catch (FileNotFoundException x) {\n            // Handle exception\n \
      \       } finally {\n            lock.unlock();\n            if (in != null)\
      \ {\n                try {\n                    in.close();\n              \
      \  } catch (IOException x) {\n                    // Handle exception\n    \
      \            }\n            }\n        }\n    } \n}\n```  \n代码使用了ReentrantLock来保护资源，当打开的文件进行操作而发生异常或不发生异常时，程序都会走到finally代码块，`lock.unlock()`方法执行，可成功释放持有的锁。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.8 在异常时保证释放已持有的锁
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：一个在任何线程中没有被释放的锁会阻止其他线程得到这个锁。在发生异常条件的情况下，线程必须释放当前持有的锁。如果在异常条件下释放锁失败，会导致线程饥饿或死锁。\
      \  \n对于在异常时保证释放已持有的锁的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\npublic\
      \ final class Client {  \n    public void doSomething(File file) {\n       \
      \ final Lock lock = new ReentrantLock();\n        InputStream in = null;\n \
      \       try {\n            lock.lock();\n            in = new FileInputStream(file);\n\
      \            // Perform operations on the open file\n            lock.unlock();\n\
      \        } catch (FileNotFoundException x) {\n            // Handle exception\n\
      \        } finally {\n            if (in != null) {\n                try {\n\
      \                    in.close();\n                } catch (IOException x) {\n\
      \                    // Handle exception\n                }\n            }\n\
      \        }\n    } \n}\n```  \n代码使用了ReentrantLock来保护资源，当打开的文件进行操作而发生异常时，却不能释放持有的锁。当发生异常时，控制权会移交给catch代码块，并且`unlock()`方法不会执行。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 772
      - type: CWE
        value: 772:Missing Release of Resource after Effective Lifetime
      - type: CWE
        value: 667:Improper Locking
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 411:Resource Locking Problems
      - type: CERT
        value: LCK08-J:Ensure actively held locks are released on exceptional conditions
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 6.6.1 资源泄漏
      - type: SJ/T 11683-2017
        value: 6.8.2 获取锁后没有释放锁
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170040'
    language: Java
  - Advice: "在JavaEE程序中不应该使用`deleteOnExit()`回收文件句柄资源，应在finally代码块中调用`delete()`方法释放文件句柄资源。\
      \  \n  \n**例如**：下面代码片段中，使用完之前创建的文件句柄`tempFile`后，在finally代码块中进行了释放。  \n```java\n\
      File tempFile = null;  \ntry {  \n    ...  \n    tempFile = File.createTempFile(\"\
      temp\", \".txt\");  \n    ...  \n} catch (IOException e) {  \n    ...  \n}finally{\
      \  \n    if(tempFile!=null&&tempFile.exists()){  \n\t\tboolean hasDelete = tempFile.delete();\
      \  \n        if(!hasDelete) {  \n              ...      \n        }  \n    }\
      \  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【强制】不要在JavaEE程序中使用deleteOnExit()释放资源
    Categories_En:
      Category: []
    Default: true
    Description: "程序创建或分配文件句柄后，不进行合理释放，将会降低系统性能，攻击者还可能会通过耗尽资源池的方式发起拒绝服务攻击。  \n  \n\
      **例如**：下面代码片段中，创建了一个临时文件句柄`tempFile`，程序使用`deleteOnExit()`方法删除临时文件，程序会等到JVM正常运行结束后才会删除。而在JavaEE中程序将会持续运行很长时间，将会持续生成临时文件并得不到有效删除。\
      \  \n```java\n...  \nFile tempFile = File.createTempFile(\"temp\", \".txt\"\
      );  \n...  \ntempFile.deleteOnExit();  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: true
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 404:Improper Resource Shutdown or Release
      - type: CERT
        value: FIO04-J:Close resources when they are no longer needed
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 404
      - type: SJ/T 11683-2017
        value: 6.6.1 资源泄漏
    Security: not
    Tag: correctness,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150113'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\npublic void getSocket(String\
      \ host, int port) {\n    Socket socket = null;\n    try {\n        socket =\
      \ new Socket(host, port);\n        BufferedReader reader = new BufferedReader(new\
      \ InputStreamReader(socket.getInputStream()));\n        while (reader.readLine()\
      \ != null) {\n            ...\n        }\n    } catch (UnknownHostException\
      \ e) {\n        e.printStackTrace();\n    } catch (IOException e) {\n      \
      \  e.printStackTrace();\n    } finally {\n        if (socket != null) {\n  \
      \          try {\n                socket.close();\n            } catch (IOException\
      \ e) {\n                e.printStackTrace();\n            }\n        }\n   \
      \ }\n}\n```  \n上述代码片段中，使用完之前创建的socket套接字资源后，在finally代码块中进行了释放。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 7.1 资源管理
      - type: DetailClassChin
        value: A.4.1.3 需确保Sockets得到释放
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：程序创建或分配Socket后，不进行合理释放，将会降低系统性能。攻击者可能会通过耗尽资源池的方式发起拒绝服务攻击。 \
      \ \n对于需确保Sockets得到释放的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\npublic\
      \ void getSocket(String host, int port) {\n    try {\n        Socket socket\
      \ = new Socket(host, port);\n        BufferedReader reader = new BufferedReader(new\
      \ InputStreamReader(socket.getInputStream()));\n        while (reader.readLine()\
      \ != null) {\n            ...\n        }\n    } catch (UnknownHostException\
      \ e) {\n        e.printStackTrace();\n    } catch (IOException e) {\n      \
      \  e.printStackTrace();\n    }\n}\n```  \n上述代码片段中，创建了一个套接字socket对象，未进行合理释放。\
      \  \n程序不应依赖于`finalize()`回收Socket资源，应在finally代码块中手动释放Socket资源。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 772:Missing Release of Resource after Effective Lifetime
      - type: CWE
        value: 459:Incomplete Cleanup
      - type: CWE
        value: 404:Improper Resource Shutdown or Release
      - type: CERT
        value: FIO04-J:Close resources when they are no longer needed
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 772
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 404
      - type: SJ/T 11683-2017
        value: 6.6.1 资源泄漏
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170061'
    language: Java
  - Advice: "**规范的代码样例1**：  \n```java\npublic class SampleServlet extends HttpServlet\
      \ {\n\n    public void doGet(HttpServletRequest request, HttpServletResponse\
      \ response)\n            throws IOException, ServletException {\n        response.setContentType(\"\
      text/html\");\n        PrintWriter out = response.getWriter();\n        out.println(\"\
      <html>\");\n\n        String emailAddr = request.getParameter(\"emailAddr\"\
      );\n        HttpSession session = request.getSession();\n        Object attr\
      \ = session.getAttribute(\"lastAddr\");\n        String lastAddr = (attr ==\
      \ null) ? \"null\" : attr.toString();\n\n        if (emailAddr != null) {\n\
      \            out.println(\"Email Address::\");\n            out.println(sanitize(emailAddr));\n\
      \            out.println(\"<br>Previous Email Address::\");\n            out.println(sanitize(lastAddr));\n\
      \        }\n        ;\n\n        out.println(\"<p>\");\n        out.print(\"\
      <form action=\\\"\");\n        out.print(\"SampleServlet\\\" \");\n        out.println(\"\
      method=POST>\");\n        out.println(\"Parameter:\");\n        out.println(\"\
      <input type=text size=20 name=emailAddr>\");\n        out.println(\"<br>\");\n\
      \        out.println(\"<input type=submit>\");\n        out.println(\"</form>\"\
      );\n\n        session.setAttribute(\"lastAddr\", emailAddr);\n    }\n\n    public\
      \ void doPost(HttpServletRequest request, HttpServletResponse response)\n  \
      \          throws IOException, ServletException {\n        doGet(request, response);\n\
      \    }\n\n    // Filter the specified message string for characters\n    //\
      \ that are sensitive in HTML.\n    public static String sanitize(String message)\
      \ {\n        // ...\n    }\n}\n```\n上面符合规范的代码中，在HttpSession对象中存储的`lastAddr`参数。servlet机制跟踪会话，为客户机提供会话ID，它由客户端浏览器作为cookie存储。会话中的其它信息，包括lastAddr属性，由服务器存储。因此，servlet提供了在同一会话中向servlet提交的最后一个电子邮件地址（避免了其他会话请求的竞争条件）。在这个例子中保存数据的临时变量，不会受到竞争条件的危害。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution stores the `lastAddr`\
      \ parameter in the `HttpSession` object, which is provided as part of the `HttpServletRequest`.\
      \ The servlet mechanism keeps track of the session, providing the client with\
      \ the session's ID, which is stored as a cookie by the client's browser. The\
      \ other information in the session, including the `lastAddr` attribute, is stored\
      \ by the server. Consequently, the servlet provides the last email address that\
      \ was presented to the servlet in the same session (avoiding race conditions\
      \ with requests from other sessions). The local variables, which temporarily\
      \ hold data in this example, are not vulnerable to race conditions.\n\n```java\n\
      public class SampleServlet extends HttpServlet {\n\n  public void doGet(HttpServletRequest\
      \ request, HttpServletResponse response)\n    throws IOException, ServletException\
      \ {\n    response.setContentType(\"text/html\");\n    PrintWriter out = response.getWriter();\n\
      \    out.println(\"<html>\");\n\n    String emailAddr = request.getParameter(\"\
      emailAddr\");\n    HttpSession session = request.getSession();\n    Object attr\
      \ = session.getAttribute(\"lastAddr\");\n    String lastAddr = (attr == null)\
      \ ? \"null\" : attr.toString();\n\n    if (emailAddr != null) {\n      out.println(\"\
      Email Address::\");\n      out.println(sanitize(emailAddr));\n      out.println(\"\
      <br>Previous Email Address::\");\n      out.println(sanitize(lastAddr));\n \
      \   };\n\n    out.println(\"<p>\");\n    out.print(\"<form action=\\\"\");\n\
      \    out.print(\"SampleServlet\\\" \");\n    out.println(\"method=POST>\");\n\
      \    out.println(\"Parameter:\");\n    out.println(\"<input type=text size=20\
      \ name=emailAddr>\");\n    out.println(\"<br>\");\n    out.println(\"<input\
      \ type=submit>\");\n    out.println(\"</form>\");\n\n    session.setAttribute(\"\
      lastAddr\", emailAddr);\n  }\n\n  public void doPost(HttpServletRequest request,\
      \ HttpServletResponse response)\n    throws IOException, ServletException {\n\
      \    doGet(request, response);\n  }\n\n  // Filter the specified message string\
      \ for characters\n  // that are sensitive in HTML.\n  public static String sanitize(String\
      \ message) {\n    // ...\n  }\n}\n```"
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 49. 其他 (MSC)
      - type: DetailClassChin
        value: MSC11-J：不要在servlet中泄露会话信息
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 49. Miscellaneous (MSC)
      - type: DetailClassChin
        value: MSC11-J:Do not let session information leak within a servlet
    Default: true
    Description: "Java servlet提供了一个用于存储会话相关数据的HttpSession类，这些数据被编码在每个web请求中。这个类的使用可以防止跨会话信息泄漏和数据竞争。\
      \  \n**不规范的代码样例1**：  \n```java\npublic class SampleServlet extends HttpServlet\
      \ {\n\n    private String lastAddr = \"nobody@nowhere.com\";\n\n    public void\
      \ doGet(HttpServletRequest request, HttpServletResponse response)\n        \
      \    throws IOException, ServletException {\n        response.setContentType(\"\
      text/html\");\n        PrintWriter out = response.getWriter();\n        out.println(\"\
      <html>\");\n\n        String emailAddr = request.getParameter(\"emailAddr\"\
      );\n\n        if (emailAddr != null) {\n            out.println(\"Email Address:\"\
      );\n            out.println(sanitize(emailAddr));\n            out.println(\"\
      <br>Previous Address:\");\n            out.println(sanitize(lastAddr));\n  \
      \      }\n        ;\n\n        out.println(\"<p>\");\n        out.print(\"<form\
      \ action=\\\"\");\n        out.print(\"SampleServlet\\\" \");\n        out.println(\"\
      method=POST>\");\n        out.println(\"Parameter:\");\n        out.println(\"\
      <input type=text size=20 name=emailAddr>\");\n        out.println(\"<br>\");\n\
      \        out.println(\"<input type=submit>\");\n        out.println(\"</form>\"\
      );\n\n        lastAddr = emailAddr;\n    }\n\n    public void doPost(HttpServletRequest\
      \ request, HttpServletResponse response)\n            throws IOException, ServletException\
      \ {\n        doGet(request, response);\n    }\n\n    // Filter the specified\
      \ message string for characters\n    // that are sensitive in HTML.\n    public\
      \ static String sanitize(String message) {\n        // ...\n    }\n}\n```\n\
      这个不合规范的代码示例创建了一个servlet，它会提示用户输入一个电子邮件地址，然后将地址返回给用户。地址存储在lastAddr变量,这是一个实例字段。\
      \  \n因为HttpServlet类是单例的：每一个客户访问servlet共享一个`lastAddr`字段。因此,`lastAddr`字段的内容可以是一个不同的客户端先前设置的字段。另外，由于这个代码示例缺少线程安全性，因此，`lastAddr`字段可以在两个客户端同时请求该参数时使用一个陈旧的值，这违反了VNA01-J.保证对一个不可变对象的共享引用的可见性。\
      \  \n**不规范的代码样例2**：  \n```java\npublic class SampleServlet extends HttpServlet\
      \ {  \n   \n  private static String lastAddr = \"nobody@nowhere.com\";  \n \
      \  \n  // ... Other methods unchanged  \n}\n```  \n这个不合规范的代码示例，`lastAddr`字段是静态的。每一个客户端访问servlet共享一个`lastAddr`字段。违反了VNA01-J。确保对不可变对象的共享引用的可见性。\
      \  \n**不规范的代码样例3**：  \n```java\npublic class SampleServlet extends HttpServlet\
      \ {\n\n    private static String lastAddr = \"nobody@nowhere.com\";\n    private\
      \ static final Object lastAddrLock = new Object();\n\n    public void doGet(HttpServletRequest\
      \ request, HttpServletResponse response)\n            throws IOException, ServletException\
      \ {\n        response.setContentType(\"text/html\");\n        PrintWriter out\
      \ = response.getWriter();\n        out.println(\"<html>\");\n\n        String\
      \ emailAddr = request.getParameter(\"emailAddr\");\n\n        if (emailAddr\
      \ != null) {\n            out.println(\"Email Address::\");\n            out.println(sanitize(emailAddr));\n\
      \            synchronized (lock) {\n                out.println(\"<br>Previous\
      \ Email Address::\");\n                out.println(sanitize(lastAddr));\n  \
      \          }\n        }\n        ;\n\n        out.println(\"<p>\");\n      \
      \  out.print(\"<form action=\\\"\");\n        out.print(\"SampleServlet\\\"\
      \ \");\n        out.println(\"method=POST>\");\n        out.println(\"Parameter:\"\
      );\n        out.println(\"<input type=text size=20 name=emailAddr>\");\n   \
      \     out.println(\"<br>\");\n        out.println(\"<input type=submit>\");\n\
      \        out.println(\"</form>\");\n\n        synchronized (lock) {\n      \
      \      lastAddr = emailAddr;\n        }\n    }\n\n    public void doPost(HttpServletRequest\
      \ request, HttpServletResponse response)\n            throws IOException, ServletException\
      \ {\n        doGet(request, response);\n    }\n\n    // Filter the specified\
      \ message string for characters\n    // that are sensitive in HTML.\n    public\
      \ static String sanitize(String message) {\n        // ...\n    }\n}\n```\n\
      这个不合规范的代码示例，该`lastAddr`字段是静态的，是由一个单独的锁定对象防止并发访问，该加锁方案由LCK00-J. 通过私有final锁对象来同步那些与非受信代码交互的类。这种方法保证了servlet中的线程安全性。但是，servlet仍然可以返回由不同会话提供的电子邮件地址。"
    Description_En: "Java servlets often must store information associated with each\
      \ client that connects to them. Using member fields in the `javax.servlet.http.HttpServlet`\
      \ to store information specific to individual clients is a common, simple practice.\
      \ However, doing so is a mistake for the following reasons:\n\n* In any Java\
      \ servlet container, such as Apache Tomcat, `HttpServlet` is a singleton class\
      \ (see MSC07-J. Prevent multiple instantiations of singleton objects for information\
      \ related to singleton classes). Therefore, there can be only one instance of\
      \ member variables, even if they are not declared static.\n* A servlet container\
      \ is permitted to invoke the servlet from multiple threads. Consequently, accessing\
      \ fields in the servlet can lead to data races.\n* If two clients initiate sessions\
      \ with the servlet, the servlet can leak information from one client to the\
      \ other client.\n\nJava servlets provide an `HttpSession` class for storing\
      \ session-specific data, which is encoded in each web request. Use of this class\
      \ prevents both cross-session information leakage and data races.\n\n**Noncompliant\
      \ Code Example**\n\nThis noncompliant code example creates a servlet that prompts\
      \ the user for an email address, then repeats the address back to the user.\
      \ The previous address is stored in the `lastAddr` variable, which is an instance\
      \ field.\n\n```java\npublic class SampleServlet extends HttpServlet {\n\n  private\
      \ String lastAddr = \"nobody@nowhere.com\";\n\n  public void doGet(HttpServletRequest\
      \ request, HttpServletResponse response)\n    throws IOException, ServletException\
      \ {\n    response.setContentType(\"text/html\");\n    PrintWriter out = response.getWriter();\n\
      \    out.println(\"<html>\");\n\n    String emailAddr = request.getParameter(\"\
      emailAddr\");\n\n    if (emailAddr != null) {\n      out.println(\"Email Address:\"\
      );\n      out.println(sanitize(emailAddr));\n      out.println(\"<br>Previous\
      \ Address:\");\n      out.println(sanitize(lastAddr));\n    };\n\n    out.println(\"\
      <p>\");\n    out.print(\"<form action=\\\"\");\n    out.print(\"SampleServlet\\\
      \" \");\n    out.println(\"method=POST>\");\n    out.println(\"Parameter:\"\
      );\n    out.println(\"<input type=text size=20 name=emailAddr>\");\n    out.println(\"\
      <br>\");\n    out.println(\"<input type=submit>\");\n    out.println(\"</form>\"\
      );\n\n    lastAddr = emailAddr;\n  }\n\n  public void doPost(HttpServletRequest\
      \ request, HttpServletResponse response)\n    throws IOException, ServletException\
      \ {\n    doGet(request, response);\n  }\n\n  // Filter the specified message\
      \ string for characters\n  // that are sensitive in HTML.\n  public static String\
      \ sanitize(String message) {\n    // ...\n  }\n}\n```\n\nBecause the `HttpServlet`\
      \ class is a singleton, there is only one `lastAddr` field shared by every client\
      \ who accesses the servlet. Consequently, the contents of the `lastAddr` field\
      \ can be the previous setting of the field by a different client. Also, because\
      \ this code example lacks thread-safety, it is possible for the `lastAddr` field\
      \ to take on a stale value should two clients request the parameter simultaneously,\
      \ which violates VNA01-J. Ensure visibility of shared references to immutable\
      \ objects.\n\n**Noncompliant Code Example**\n\nIn this noncompliant code example,\
      \ the `lastAddr` field is static. It more accurately reflects the fact that\
      \ there is never more than a single instance of the field. However, this code\
      \ has the same behavior as the previous noncompliant code example and also violates\
      \ VNA01-J. Ensure visibility of shared references to immutable objects.\n\n\
      ```java\npublic class SampleServlet extends HttpServlet {\n\n  private static\
      \ String lastAddr = \"nobody@nowhere.com\";\n\n  // ... Other methods unchanged\n\
      }\n```\n\n**Noncompliant Code Example**\n\nIn this noncompliant code example,\
      \ the `lastAddr` field is static and is protected from concurrent access by\
      \ a separate lock object, as is recommended by LCK00-J. Use private final lock\
      \ objects to synchronize classes that may interact with untrusted code. This\
      \ approach guarantees thread-safety in the servlet. However, the servlet can\
      \ still return the email address provided by a different session.\n\n```java\n\
      public class SampleServlet extends HttpServlet {\n\n  private static String\
      \ lastAddr = \"nobody@nowhere.com\";\n  private static final Object lastAddrLock\
      \ = new Object();\n\n  public void doGet(HttpServletRequest request, HttpServletResponse\
      \ response)\n    throws IOException, ServletException {\n    response.setContentType(\"\
      text/html\");\n    PrintWriter out = response.getWriter();\n    out.println(\"\
      <html>\");\n\n    String emailAddr = request.getParameter(\"emailAddr\");\n\n\
      \    if (emailAddr != null) {\n      out.println(\"Email Address::\");\n   \
      \   out.println(sanitize(emailAddr));\n      synchronized (lock) {\n       \
      \ out.println(\"<br>Previous Email Address::\");\n        out.println(sanitize(lastAddr));\n\
      \      }\n    };\n\n    out.println(\"<p>\");\n    out.print(\"<form action=\\\
      \"\");\n    out.print(\"SampleServlet\\\" \");\n    out.println(\"method=POST>\"\
      );\n    out.println(\"Parameter:\");\n    out.println(\"<input type=text size=20\
      \ name=emailAddr>\");\n    out.println(\"<br>\");\n    out.println(\"<input\
      \ type=submit>\");\n    out.println(\"</form>\");\n\n    synchronized (lock)\
      \ {\n      lastAddr = emailAddr;\n    }\n  }\n\n  public void doPost(HttpServletRequest\
      \ request, HttpServletResponse response)\n    throws IOException, ServletException\
      \ {\n    doGet(request, response);\n  }\n\n  // Filter the specified message\
      \ string for characters\n  // that are sensitive in HTML.\n  public static String\
      \ sanitize(String message) {\n    // ...\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 488:Exposure of Data Element to Wrong Session
      - type: CWE
        value: 362:Concurrent Execution using Shared Resource with Improper Synchronization
          ('Race Condition')
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 362
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 362
      - type: ISO/IEC TR 24772
        value: 2013:New Vulnerabilities Concurrent Data Access [CGX]
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010141811
    language: Java
  - Advice: "使用Java EE的Servlet,struts1的Action或Spring框架写的一些单例类时，必须保证其是线程安全的。修复方式举例如下：\
      \  \n  \n**例1**：下面代码示例，使用局部变量，因为局部变量在每个线程中都有各自的实例，从而保证线程安全。  \n```java\n...\
      \  \nimport javax.servlet.http.HttpServlet;  \npublic class LoginServlet extends\
      \ HttpServlet {  \n    protected void doPost(HttpServletRequest req,HttpServletResponse\
      \ res) {  \n        String username = req.getParameter(\"username\");  \n  \
      \      ...  \n        out.println(\" 欢迎您，\"+username+\" !\");  \n    }  \n}\
      \  \n...\n```  \n**例2**：下面代码示例，可以将User控制器设为多例，每一次请求将会新建一个User对象,从而保证线程安全。  \n\
      ```java\n@Controller  \n@Scope(\"prototype\")  \npublic class User{  \nString\
      \ username;  \n    @RequestMapping(\"/login\", method = GET)  \n    public String\
      \ greet(String username) {  \n        this.username= username;  \n        ...\
      \ \n        return \" 欢迎您，\"+username+\" !\";      \n    }  \n}\n```  \n同样，也可以使用局部变量解决此问题。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 并发规范
      - type: DetailClassChin
        value: 【推荐】不要使用单例对象下的成员变量
    Categories_En:
      Category: []
    Default: true
    Description: "`javax.servlet.http.HttpServlet`和`org.apache.struts.action.Action`的对象以及Spring框架下单例类（比如使用`@Controller`、`@Service`和`@Repository`注解的类默认都是单例类）都不是线程安全的。Web容器默认采用单实例多线程的方式来处理Http请求，单例对象将在一个多线程的环境中并发执行，这将导致单例对象成员变量访问的线程安全问题。\
      \  \n  \n**例1**：下面是一段JAVA EE的Servlet代码片断，变量`username`为成员变量，当LoginServlet的对象处理多个请求时，变量`username`将被多个请求共享，这将导致线程间数据泄露。\
      \  \n```java\n...  \nimport javax.servlet.http.HttpServlet;  \npublic class\
      \ LoginServlet extends HttpServlet {  \n    String username;  \n    protected\
      \ void doPost(HttpServletRequest req,HttpServletResponse res) {  \n        username\
      \ = req.getParameter(\"username\");  \n        ...  \n        out.println( \"\
      \ 欢迎您，\"+username+\" !\");  \n    }  \n}  \n...\n```  \n**例2**：下面是一段使用Spring框架搭建的WEB服务中的Controller代码片断，变量`username`为成员变量，当User的对象处理多个请求时，变量`username`将被多个请求共享，这将导致线程间数据泄露。\
      \  \n```java\n@Controller  \npublic class User{  \n\tString username;  \n\t\
      @RequestMapping(\"/login\", method = GET)  \n\tpublic String greet(String username)\
      \ {  \n\t\tthis.username= username;  \n\t\t...\n        return \" 欢迎您，\"+username+\"\
      \ !\";      \n\t}  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Singleton Member Field
    ENSubClassChin: Race Condition
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: true
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 488:Exposure of Data Element to Wrong Session
      - type: CWE
        value: 362:Concurrent Execution using Shared Resource with Improper Synchronization
          ('Race Condition')
      - type: CERT
        value: MSC07-J:Prevent multiple instantiations of singleton objects
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 362
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 362
      - type: ISO/IEC TR 24772
        value: 2013:New Vulnerabilities Concurrent Data Access [CGX]
    Security: not
    Tag: correctness,concurrency
    Type: std
    WhiteList: not
    id: '0202000010150302'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\npublic class SampleServlet\
      \ extends HttpServlet {\n\n    public void doGet(HttpServletRequest request,\
      \ HttpServletResponse response)\n            throws IOException, ServletException\
      \ {\n        response.setContentType(\"text/html\");\n        PrintWriter out\
      \ = response.getWriter();\n        out.println(\"<html>\");\n\n        String\
      \ emailAddr = request.getParameter(\"emailAddr\");\n        HttpSession session\
      \ = request.getSession();\n        Object attr = session.getAttribute(\"lastAddr\"\
      );\n        String lastAddr = (attr == null) ? \"null\" : attr.toString();\n\
      \n        if (emailAddr != null) {\n            out.println(\"Email Address::\"\
      );\n            out.println(sanitize(emailAddr));\n            out.println(\"\
      <br>Previous Email Address::\");\n            out.println(sanitize(lastAddr));\n\
      \        }\n        ;\n\n        out.println(\"<p>\");\n        out.print(\"\
      <form action=\\\"\");\n        out.print(\"SampleServlet\\\" \");\n        out.println(\"\
      method=POST>\");\n        out.println(\"Parameter:\");\n        out.println(\"\
      <input type=text size=20 name=emailAddr>\");\n        out.println(\"<br>\");\n\
      \        out.println(\"<input type=submit>\");\n        out.println(\"</form>\"\
      );\n        session.setAttribute(\"lastAddr\", emailAddr);\n    }\n\n    public\
      \ void doPost(HttpServletRequest request, HttpServletResponse response)\n  \
      \          throws IOException, ServletException {\n        doGet(request, response);\n\
      \    }\n\n    // Filter the specified message string for characters\n    //\
      \ that are sensitive in HTML.\n    public static String sanitize(String message)\
      \ {\n        // ...\n    }\n}\n```\n上面符合规范的代码中，在HttpSession对象中存储的`lastAddr`参数。servlet机制跟踪会话，为客户机提供会话ID，它由客户端浏览器作为cookie存储。会话中的其它信息，包括`lastAddr`属性，由服务器存储。因此，servlet提供了在同一会话中向servlet提交的最后一个电子邮件地址（避免了其他会话请求的竞争条件）。在这个例子中保存数据的临时变量，不会受到竞争条件的危害。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.15 不要在servlet中泄露会话信息
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：Java servlet提供了一个用于存储会话相关数据的HttpSession类，这些数据被编码在每个web请求中。这个类的使用可以防止跨会话信息泄漏和数据竞争。\
      \  \n对于不要在servlet中泄露会话信息的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\n\
      public class SampleServlet extends HttpServlet {\n\n    private String lastAddr\
      \ = \"nobody@nowhere.com\";\n\n    public void doGet(HttpServletRequest request,\
      \ HttpServletResponse response)\n            throws IOException, ServletException\
      \ {\n        response.setContentType(\"text/html\");\n        PrintWriter out\
      \ = response.getWriter();\n        out.println(\"<html>\");\n\n        String\
      \ emailAddr = request.getParameter(\"emailAddr\");\n\n        if (emailAddr\
      \ != null) {\n            out.println(\"Email Address:\");\n            out.println(sanitize(emailAddr));\n\
      \            out.println(\"<br>Previous Address:\");\n            out.println(sanitize(lastAddr));\n\
      \        }\n        ;\n\n        out.println(\"<p>\");\n        out.print(\"\
      <form action=\\\"\");\n        out.print(\"SampleServlet\\\" \");\n        out.println(\"\
      method=POST>\");\n        out.println(\"Parameter:\");\n        out.println(\"\
      <input type=text size=20 name=emailAddr>\");\n        out.println(\"<br>\");\n\
      \        out.println(\"<input type=submit>\");\n        out.println(\"</form>\"\
      );\n\n        lastAddr = emailAddr;\n    }\n\n    public void doPost(HttpServletRequest\
      \ request, HttpServletResponse response)\n            throws IOException, ServletException\
      \ {\n        doGet(request, response);\n    }\n\n    // Filter the specified\
      \ message string for characters\n    // that are sensitive in HTML.\n    public\
      \ static String sanitize(String message) {\n        // ...\n    }\n}\n``` \n\
      这个不符合规范的代码示例创建了一个servlet，它会提示用户输入一个电子邮件地址，然后将地址返回给用户。地址存储在`lastAddr`变量,这是一个实例字段。\
      \  \n因为HttpServlet类是单例的,每一个客户访问servlet共享一个`lastAddr`字段。因此,`lastAddr`字段的内容可以是一个不同的客户端先前设置的字段。另外，由于这个代码示例缺少线程安全性，因此，`lastAddr`字段可以在两个客户端同时请求该参数时使用一个陈旧的值，保证对一个不可变对象的共享引用的可见性。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 488:Exposure of Data Element to Wrong Session
      - type: CWE
        value: 362:Concurrent Execution using Shared Resource with Improper Synchronization
          ('Race Condition')
      - type: CERT
        value: MSC07-J:Prevent multiple instantiations of singleton objects
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 362
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 362
      - type: ISO/IEC TR 24772
        value: 2013:New Vulnerabilities Concurrent Data Access [CGX]
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170047'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。修复方式如下：  \n**示例2**： \n```java  \n@Controller\n@Scope(\"\
      prototype\")\npublic class User {\n    String username;\n\n    @RequestMapping(\"\
      /login\", method = GET)  \n    public String greet(String username) {  \n  \
      \      this.username= username;  \n        ...\n        return \" 欢迎您，\"+username+\"\
      \ !\";      \n    }\n}\n```  \n上面代码示例，可以将User控制器设为多例，每一次请求将会新建一个User对象,从而保证线程安全。\
      \  \n同样，也可以使用局部变量解决此问题。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.17 不要使用单例对象下的成员变量
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：`org.apache.struts.action.Action`的对象以及Spring框架下单例类（比如使用`@Controller`、`@Service`和`@Repository`注解的类默认都是单例类）都不是线程安全的。Web容器默认采用单实例多线程的方式来处理Http请求，单例对象将在一个多线程的环境中并发执行，这将导致单例对象成员变量访问的线程安全问题。\
      \  \n对于不要使用单例对象下的成员变量的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\n@Controller\
      \  \npublic class User {\n    String username;\n\n    @RequestMapping(\"/login\"\
      , method = GET)  \n    public String greet(String username) {  \n        this.username=\
      \ username;  \n        ...\n        return \" 欢迎您，\"+username+\" !\";      \n\
      \    }\n}\n```  \n上面是一段使用Spring框架搭建的WEB服务中的Controller代码片断，变量`username`为成员变量，当User的对象处理多个请求时，变量`username`将被多个请求共享，这将导致线程间数据泄露。\
      \  \n使用struts1的Action或Spring框架写的一些单例类时，必须保证其是线程安全的。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 488:Exposure of Data Element to Wrong Session
      - type: CWE
        value: 362:Concurrent Execution using Shared Resource with Improper Synchronization
          ('Race Condition')
      - type: CERT
        value: MSC07-J:Prevent multiple instantiations of singleton objects
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 362
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 362
      - type: ISO/IEC TR 24772
        value: 2013:New Vulnerabilities Concurrent Data Access [CGX]
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: 0302000010170049
    language: Java
  - Advice: "正确定义`serialVersionUID`。  \n  \n**例如**：下面代码示例中，类实现了Serializable接口，`serialVersionUID`定义为静态不可变long类型。\
      \  \n```java\npublic class User implements Serializable{  \n    private static\
      \ final long serialVersionUID = 5280862921227291610L;  \n    private String\
      \ name;  \n    private int age;  \n    ...  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】serialVersionUID应为静态不可变的long类型
    Categories_En:
      Category: []
    Default: true
    Description: "可序列化类中的`serialVersionUID`应为由static，final修饰的long数字。  \n  \n**例如**：下面代码示例中，类实现了Serializable接口，`serialVersionUID`没有定义为静态不可变long类型。\
      \  \n```java\npublic class User implements Serializable{  \n    private int\
      \ serialVersionUID = 454335;  \n    private String name;  \n    private int\
      \ age;  \n    ...  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150451'
    language: Java
  - Advice: "不要将数值直接与`NaN`进行比较运算，采用如：`Double.isNaN()`或者`Float.isNaN()`方法来检测数值是否是一个`NaN`。\
      \  \n**规范的代码样例1**：  \n```java\npublic class NaNComparison {  \n    public static\
      \ void main(String[] args) {\n        double x = 0.0;\n        double result\
      \ = Math.cos(1 / x); // returns NaN when input is infinity\n        if (Double.isNaN(result))\
      \ {\n            System.out.println(\"result is NaN\");\n        }\n    }\n\
      }\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution uses the method\
      \ `Double.isNaN()` to check whether the expression corresponds to a `NaN` value:\n\
      \n```java\npublic class NaNComparison {\n  public static void main(String[]\
      \ args) {\n    double x = 0.0;\n    double result = Math.cos(1/x); // Returns\
      \ NaN when input is infinity\n    if (Double.isNaN(result)) {\n      System.out.println(\"\
      result is NaN\");\n    }\n  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 03. 数据类型与运算 (NUM)
      - type: DetailClassChin
        value: NUM07-J：不要尝试与NaN进行比较
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 03. Numeric Types and Operations (NUM)
      - type: DetailClassChin
        value: NUM07-J:o not attempt comparisons with NaN
    Default: true
    Description: "根据Java语言规范4.2.3节“浮点类型、格式和数值”的描述：  \n>因为NaN（Not-a-Number，非数值）是无序的，所以当一个或者两个操作数是NaN时，数值比较<、<=、>、>=或返回false。当任意一个操作数是NaN，相等运算符==会返回false，当任意一个操作数是NaN，不等运算符!=会返回true。\
      \  \n\n因为无序的特性常常导致意外结果，所以不能和`NaN`进行直接比较。当程序员编写比较浮点数值代码，而没有考虑到`NaN`的语义时，会产生问题。\
      \  \n代码静态分析来判断是否对可能无序的结果进行正确的处理是不能实现的，本规则检测提供的警示信息对于程序员分析程序意图是有所帮助的。  \n**不规范的代码样例1**：\n\
      这个不合规的代码示例试图与`NaN`进行直接比较。根据`NaN`的语义，所有与`NaN`的比较都会产生`false`（除了返回`true`的`!=`运算符）。因此，这种比较总是返回`false`，并且永远不会打印`result\
      \ is NaN`信息。\n```java\npublic class NaNComparison {\n  public static void main(String[]\
      \ args) {\n    double x = 0.0;\n    double result = Math.cos(1/x); // Returns\
      \ NaN if input is infinity\n    if (result == Double.NaN) { // Comparison is\
      \ always false!\n      System.out.println(\"result is NaN\");\n    }\n  }\n\
      }\n```"
    Description_En: "According to The Java Language Specification (JLS), §4.2.3, \"\
      Floating-Point Types, Formats, and Values\" [JLS 2015]:\n\n> `NaN` (not-a-number)\
      \ is unordered, so the numerical comparison operators `<`, `<=`, `>`, and `>=`\
      \ return `false` if either or both operands are `NaN`. The equality operator\
      \ `==` returns `false` if either operand is `NaN`, and the inequality operator\
      \ `!=` returns `true` if either operand is `NaN`.\n\nBecause this unordered\
      \ property is often unexpected, direct comparisons with `NaN` must not be performed.\
      \ Problems can arise when programmers write code that compares floating-point\
      \ values without considering the semantics of `NaN`. For example, input validation\
      \ checks that fail to consider the possibility of a `NaN` value as input can\
      \ produce unexpected results (see NUM08-J. Check floating-point inputs for exceptional\
      \ values for additional information).\n\n**Noncompliant Code Example**\n\nThis\
      \ noncompliant code example attempts a direct comparison with `NaN`. In accordance\
      \ with the semantics of `NaN`, all comparisons with `NaN` yield false (with\
      \ the exception of the `!=` operator, which returns true). Consequently, this\
      \ comparison always return `false`, and the `\"result is NaN\"` message is never\
      \ printed.\n\n```java\npublic class NaNComparison {\n  public static void main(String[]\
      \ args) {\n    double x = 0.0;\n    double result = Math.cos(1/x); // Returns\
      \ NaN if input is infinity\n    if (result == Double.NaN) { // Comparison is\
      \ always false!\n      System.out.println(\"result is NaN\");\n    }\n  }\n\
      }\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 486:Comparison of Classes by Name
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140507'
    language: Java
  - Advice: 使用`Double.isNaN()`来检测表达式是否是一个NaN数值。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要与NaN进行比较
    Categories_En:
      Category: []
    Default: true
    Description: "所以它与任何值都不相等，包括它自己，故如果拿变量与NaN进行比较，得出的计算结果将始终为false（`!=`运算符是个例外，会得出true结果）\
      \  \n  \n**例如**：以下代码输出结果为false。\n```java  \nSystem.out.println(Double.NaN ==\
      \ Double.NaN);\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Comparison with NaN
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 486:Comparison of Classes by Name
      - type: CERT
        value: NUM07-J:Do not attempt comparisons with NaN
    Security: not
    Tag: correctness,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150411'
    language: Java
  - Advice: "对应的符合规则的解决方案是：使用方法 `Double.isNaN()`来检测表达式是否是一个 NaN 数值。   \n    ```java\n\
      \    public class NaNComparison {   \n        public static void main(String[]\
      \ args) {    \n            double x = 0.0;   \n            double result = Math.cos(1/x);\
      \ // returns NaN when input is infinity    \n            if (Double.isNaN(result))\
      \ {   \n                System.out.println(\"result is NaN\");   \n        \
      \    }   \n        }   \n    }\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.4.8 不要尝试与NaN进行比较
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n不能和 NaN 进行直接比较，防止无序特性产生意外结果。   \n2) 原理   \n因为 NaN (Not-a-Number,非数值）是无序的，所以当一个或两个操作数是\
      \ NaN 时，数值 比较符 `<`、`<=`、`>=`和 `>=`会返回 false。当任意一个操作数是 NaN,相等运算符`==`会返回 false,当任意一个操作数是NaN，不等运算符`!=`会返回\
      \ true。因为无序的特性常常导致意外结果，所以不能和 NaN 进行直接比较。   \n3) 示例   \n    如下不符合规则的代码示例尝试和 NaN\
      \ 进行直接比较。根据 NaN 的语义，所有和 NaN 的比较会返回 false (除`!=`运算符返回 true 之外）。因此，代码中的比较只会返回\
      \ false,并且不会输出`“result is NaN”`。   \n    ```java\n    public class NaNComparison\
      \ {   \n        public static void main(String[] args) {    \n            double\
      \ x = 0.0;   \n            double result = Math.cos(1/x); // returns NaN if\
      \ input is infinity    \n            if (result == Double.NaN) { // comparison\
      \ is always false!   \n                System.out.printIn(\"result is NaN\"\
      );   \n            }   \n        }   \n    }\n    ```  \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 486:Comparison of Classes by Name
      - type: CERT
        value: NUM07-J:Do not attempt comparisons with NaN
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160031
    language: Java
  - Advice: "初始化向量(IV)应该使用加密伪随机数生成器进行创建。  \n  \n**例如**：下面代码片段创建随机IV。  \n```java\n\
      SecureRandom secureRandom = new SecureRandom();  \nbyte[] iv = secureRandom.generateSeed(16);\
      \  \nIvParameterSpec ivParameterSpec = new IvParameterSpec(iv);  \n...\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 密码规范
      - type: DetailClassChin
        value: 【推荐】不要使用固定的初始化向量
    Categories_En:
      Category: []
    Default: true
    Description: "初始化向量(IV)应该使用加密随机数生成器进行创建。如果不使用随机IV，则结果密码可预测性会高得多，容易受到字典式攻击。  \n\
      \  \n**例如**：下面代码片段使用硬编码字节创建非随机IV。 \n```java \nbyte[] iv = {14, 80, 94, 47, 3,\
      \ 66, 96, 110, 86, 30, 3, 0, 106, 107, 57, 117}  \nIvParameterSpec ivParameterSpec\
      \ = new IvParameterSpec(iv);  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Insecure Initialization Vector
    ENSubClassChin: Weak Encryption
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 329:Not Using a Random IV with CBC Mode
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: OWASP MOBILE TOP 10
        value: 2014:M6 Broken Cryptography
      - type: GB/T 34944-2017 Java
        value: 安全功能:密码分组链接模式未使用随机初始化矢量
    Security: true
    Tag: security,password,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150206'
    language: Java
  - Advice: "需要精确计算的数值，可以改变计量单位，进而可以使用整型进行计算；或采用`java.math.BigDecimal`类型来计算。  \n**规范的代码样例1**：改变计量单位\
      \  \n```java\nlong dollar = 100;  \nlong dime = 10;  \nint number = 7;  \nSystem.out.println(\"\
      A dollar less \" + number + \" dimes is \"+(dollar - number * dime) + \" cents\"\
      \ );\n```  \n采用100美分表示1美元，即可采用整数来进行计算。  \n代码将输出：`A dollar less 7 dimes is 30\
      \ cents ` \n\n**规范的代码样例2**：采用`java.math.BigDecimal`  \n```java\nBigDecimal dollar\
      \ = new BigDecimal(\"1.0\");  \nBigDecimal dime = new BigDecimal(\"0.1\"); \
      \ \nint number = 7;  \nSystem.out.println(\"A dollar less \" + number + \" dimes\
      \ is $\" +(dollar.subtract(new BigDecimal(number).multiply(dime) )) );\n```\
      \  \n代码将输出：`A dollar less 7 dimes is $0.3`"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution uses an integer\
      \ type (such as `int`) and works with cents rather than dollars:\n\n```java\n\
      int dollar = 100;\nint dime = 10;\nint number = 7;\nSystem.out.println(\n  \"\
      A dollar less \" + number + \" dimes is $0.\" + (dollar - number * dime)\n);\n\
      ```\n\nThis code correctly outputs the following:\n\n```java\nA dollar less\
      \ 7 dimes is $0.30\n```\n\n**Compliant Solution**\n\nThis compliant solution\
      \ uses the `BigDecimal` type, which provides exact representation of decimal\
      \ values. Note that on most platforms, computations performed using `BigDecimal`\
      \ are less efficient than those performed using primitive types.\n\n```java\n\
      import java.math.BigDecimal;\n\nBigDecimal dollar = new BigDecimal(\"1.0\");\n\
      BigDecimal dime = new BigDecimal(\"0.1\");\nint number = 7;\nSystem.out.println\
      \ (\"A dollar less \" + number + \" dimes is $\" +\n    (dollar.subtract(new\
      \ BigDecimal(number).multiply(dime) )) );\n```\n\nThis code outputs the following:\n\
      \n```java\nA dollar less 7 dimes is $0.3\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 03. 数据类型与运算 (NUM)
      - type: DetailClassChin
        value: NUM04-J：不要使用浮点数进行精细计算
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 03. Numeric Types and Operations (NUM)
      - type: DetailClassChin
        value: NUM04-J:Do not use floating-point numbers if precise computation is
          required
    Default: not
    Description: "Java语言提供两种基本浮点数据类型：`float`和`double`。它们是根据IEEE754标准确立的，该标准为32位单精度类型和64位双精度类型定义了数值和相关操作。float和double各自有固定的位数来标示尾数。所以，用浮点数来精确的表示无理数是不可能的（**例如**：圆周率π）。此外，这两个类型利用二进制来表示尾数，这意味着这些类型不能精确的表达许多有限的小数，如：0.1。\n\
      当需要进行精确的计算时，如货币兑换、数据统计，不能使用浮点类型，而应该使用其他完整表达数据的方式。如：BigDecimal。\n当然，浮点类型可以运用在对计算精度要求不高的情况中，但是必须顾及最大的累计计算误差并确保误差在可接受的范围内。\
      \  \n在大多数情况下，静态分析来判断代码是否受精度不足的影响是不能的，本条准则提供的警告信息，对于程序员分析程序是有帮助的。  \n**不规范的代码样例1**：\
      \  \n```java\ndouble dollar = 1.00;\ndouble dime = 0.10;\nint number = 7;\n\
      System.out.println(\n  \"A dollar less \" + number + \" dimes is $\" + (dollar\
      \ - number * dime)\n);\n```\n这段代码进行了一些简单的货币计算。因为`0.10`不能用Java的浮点类型来精确表达，所以，大多数的平台上会输出以下信息：\
      \  \n`A dollar less 7 dimes is $0.29999999999999993`"
    Description_En: "The Java language provides two primitive floating-point types,\
      \ `float` and `double`, which are associated with the single-precision 32-bit\
      \ and double-precision 64-bit format values and operations specified by IEEE\
      \ 754 [IEEE 754]. Each of the floating-point types has a fixed, limited number\
      \ of mantissa bits. Consequently, it is impossible to precisely represent any\
      \ irrational number (for example, pi). Further, because these types use a binary\
      \ mantissa, they cannot precisely represent many finite decimal numbers, such\
      \ as 0.1, because these numbers have repeating binary representations.\n\nWhen\
      \ precise computation is necessary, such as when performing currency calculations,\
      \ floating-point types must not be used. Instead, use an alternative representation\
      \ that can completely represent the necessary values.\n\nWhen precise computation\
      \ is unnecessary, floating-point representations may be used. In these cases,\
      \ you must carefully and methodically estimate the maximum cumulative error\
      \ of the computations to ensure that the resulting error is within acceptable\
      \ tolerances. Consider using numerical analysis to properly understand the problem.\
      \ See Goldberg's work for an introduction to this topic [Goldberg 1991].\n\n\
      **Noncompliant Code Example**\n\nThis noncompliant code example performs some\
      \ basic currency calculations:\n\n```java\ndouble dollar = 1.00;\ndouble dime\
      \ = 0.10;\nint number = 7;\nSystem.out.println(\n  \"A dollar less \" + number\
      \ + \" dimes is $\" + (dollar - number * dime)\n);\n```\n\nBecause the value\
      \ 0.10 lacks an exact representation in Java floating-point type (or *any* floating-point\
      \ format that uses a binary mantissa), on most platforms, this program prints\
      \ the following:\n\n```java\nA dollar less 7 dimes is $0.29999999999999993\n\
      ```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: SJ/T 11683-2017
        value: 6.6.6 浮点数的相等或不等比较
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140504'
    language: Java
  - Advice: "对应的符合规则的解决方案是：用一个整数类型（例如 long 型）来进行计算，而且数值的单位是美分而不是上例中的美元。   \n    ```java\n\
      \    long dollar = 100;   \n    long dime = 10;   \n    int number = 7;   \n\
      \    System.out.printIn(\"A dollar less \" + number + \" dimes is \" +(dollar\
      \ - number *  dime) + \" cents\");\n    ```   \n    这段代码正确地输出以下内容：   \n    ```\n\
      \    A dollar less 7 dimes is 30 cents\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.4.5 不要使用浮点数进行精细计算
    Categories_En:
      Category: []
    Default: not
    Description: "1) 规定   \n需要精细计算时，不能使用浮点数进行计算。   \n2) 原理   \nJava 语言提供两种基本浮点数据类型：float\
      \ 型和 double 型。float 型和 double 型各自有固定的位数来表示尾数。所以，用浮点数来精确地表达无理数是不可能的（如 π）。此外，这些类型使用二进制来表示尾数，这意味着这些类型也不能精确地表达许多有限小数，例如\
      \ 0.1，因为这类数据的二进制表达是无限循环的。   \n当需要进行精确计算时，例如货币兑换，不能使用浮点类型，而应该使用其他能完整表达数据的方式。例如，可以使用能够精确表达小数的\
      \ BigDecimal 类型。要注意的是，在大多数的平台上，相对于基础数据类型，用 BigDecimal 来进行计算，效率比较低。程序性能上的损失是否重要需要根据实际应用来衡量。\
      \   \n3) 示例   \n    如下不符合规则的代码示例进行一些简单货币兑换计算。   \n    ```java\n    double dollar\
      \ = 1.00;    \n    double dime = 0.10;    \n    int number =7;   \n    System.out.printIn(\"\
      A dollar less \" + number + \" dimes is $\" +(dollar - number * dime));\n  \
      \  ```   \n    因为 0.10 不能用 Java 的浮点类型（或任何使用二进制尾数的浮点类型）来精确表达，所以在大多数的平台上会有以下输出：\
      \   \n    `A dollar less 7 dimes is $0.29999999999999993 `  \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: NUM04-J:Do not use floating-point numbers if precise computation is
          required
      - type: SJ/T 11683-2017
        value: 6.6.6 浮点数的相等或不等比较
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160028
    language: Java
  - Advice: "使用`equals()`方法比较对象的成员信息，需要重写Object的`equals()`方法，或者直接使用`==`来比较是否是同一个对象。\
      \  \n  \n**例1**：下列代码中PersonTeam类实现`equals()`方法，当使用`equals()`方法时是通过两个对象的`tid`比较。\
      \  \n```java\npublic class PersonTeam{  \n    private int tid;  \n    public\
      \ int getTeamID(){  \n        return tid;  \n    }  \n    public void setTeamID(int\
      \ newTid){  \n        tid = newTid;  \n    }  \n    @Override  \n    public\
      \ boolean equals(Object obj) {  \n        if (this == obj)  \n            return\
      \ true;  \n        if (obj == null)  \n            return false;  \n       \
      \ if (getClass() != obj.getClass())  \n            return false;  \n       \
      \ PersonTeam other = (PersonTeam) obj;  \n        if (tid != other.tid)  \n\
      \            return false;  \n        return true;  \n    }  \n}  \n...  \n\
      public class CompareTeam{  \n    public boolean compareTeams(PersonTeam t1,\
      \ PersonTeam t2){  \n        return t1.equals(t2);   \n    }  \n}\n```  \n \
      \ \n**例2**：下列代码中PersonTeam类未实现`equals()`方法，直接使用`==`比较，判断是否是同一个对象。  \n```java\n\
      public boolean compareTeams(PersonTeam t1, PersonTeam t2){  \n    return t1\
      \ == t2;   \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【推荐】类需要实现equals()
    Categories_En:
      Category: []
    Default: true
    Description: "在没有重写`equals()`方法的类（或接口）上调用`equals()`方法，会导致调用继承自`java.lang.Object`的`equals()`方法，尽管可以合法的使用`Object.equals()`，但是`Object.equals()`将比较两个对象的内存地址，而不是比较对象的成员变量。\
      \  \n  \n**例如**：下列代码中PersonTeam类未实现`equals()`方法，当使用`equals()`方法时是通过两个对象的内存地址比较。\
      \  \n```java\npublic class PersonTeam{  \n    private int tid;  \n    public\
      \ int getTeamID(){  \n        return tid;  \n    }  \n    public void setTeamID(int\
      \ newTid){  \n        tid = newTid;  \n    }  \n }  \n...  \npublic class CompareTeam{\
      \  \n    public boolean compareTeams(PersonTeam t1, PersonTeam t2){  \n    \
      \    return t1.equals(t2);   \n    }  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Class Does Not Implement equals
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: SJ/T 11683-2017
        value: 6.3.4 equals方法的错误使用
    Security: not
    Tag: correctness,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150102'
    language: Java
  - Advice: "**规范的代码样例1**：\n```java\npublic class Exec {\n    public static void main(String\
      \ args[]) throws IOException, InterruptedException {\n        ProcessBuilder\
      \ pb = new ProcessBuilder(\"notemaker\");\n        pb = pb.redirectErrorStream(true);\n\
      \        Process proc = pb.start();\n        InputStream is = proc.getInputStream();\n\
      \        int c;\n        while ((c = is.read()) != -1) {\n            System.out.print((char)\
      \ c);\n        }\n        int exitVal = proc.waitFor();\n    }\n}\n```  \n代码将进程的错误流重定向至它的输出流，因此，程序可以清空单个输出流，而不需要担心产生阻塞。\
      \  \n**规范的代码样例2**：  \n```java\nclass StreamGobbler implements Runnable {\n \
      \ private final InputStream is;\n  private final PrintStream os;\n\n  StreamGobbler(InputStream\
      \ is, PrintStream os) {\n    this.is = is;\n    this.os = os;\n  }\n\n  public\
      \ void run() {\n    try {\n      int c;\n      while ((c = is.read()) != -1)\n\
      \          os.print((char) c);\n    } catch (IOException x) {\n      // Handle\
      \ error\n    }\n  }\n}\n\npublic class Exec {\n  public static void main(String[]\
      \ args)\n    throws IOException, InterruptedException {\n\n    Runtime rt =\
      \ Runtime.getRuntime();\n    Process proc = rt.exec(\"notemaker\");\n\n    //\
      \ Any error message?\n    Thread errorGobbler\n      = new Thread(new StreamGobbler(proc.getErrorStream(),\
      \ System.err));\n\n    // Any output?\n    Thread outputGobbler\n      = new\
      \ Thread(new StreamGobbler(proc.getInputStream(), System.out));\n\n    errorGobbler.start();\n\
      \    outputGobbler.start();\n\n    // Any error?\n    int exitVal = proc.waitFor();\n\
      \    errorGobbler.join();   // Handle condition where the\n    outputGobbler.join();\
      \  // process ends before the threads finish\n  }\n}\n```\n这个样例使用了两个线程来消耗进程的输出流和错误流。因而，进程不会被那些流永久的阻塞。分别处理输出流和输入流的时候，它们必须要独立的清空。\n\
      **特例**\nFIO07-J-EX0：无法向从不从其输入流中读取输入的进程提供输入是无害的，并且可能是有益的。 未能清空从不将输出发送到其输出或错误流的进程的输出或错误流同样是无害的，甚至是有益的。\
      \ 因此，允许程序忽略*保证*不使用这些流的进程的输入、输出或错误流。"
    Advice_En: "**Compliant Solution (`redirectErrorStream()`)**\n\nThis compliant\
      \ solution redirects the process's error stream to its output stream. Consequently,\
      \ the program can empty the single output stream without fear of blockage.\n\
      \n```java\npublic class Exec {\n  public static void main(String args[])\n \
      \                         throws IOException, InterruptedException {\n    ProcessBuilder\
      \ pb = new ProcessBuilder(\"notemaker\");\n    pb = pb.redirectErrorStream(true);\n\
      \    Process proc = pb.start();\n    InputStream is = proc.getInputStream();\n\
      \    int c;\n    while ((c = is.read()) != -1) {\n      System.out.print((char)\
      \ c);\n    }\n    int exitVal = proc.waitFor();\n  }\n}\n```\n\n**Compliant\
      \ Solution (Process Output Stream and Error Stream)**\n\nThis compliant solution\
      \ spawns two threads to consume the process's output stream and error stream.\
      \ Consequently, the process cannot block indefinitely on those streams.\n\n\
      When the output and error streams are handled separately, they must be emptied\
      \ independently. Failure to do so can cause the program to block indefinitely.\n\
      \n```java\nclass StreamGobbler implements Runnable {\n  private final InputStream\
      \ is;\n  private final PrintStream os;\n\n  StreamGobbler(InputStream is, PrintStream\
      \ os) {\n    this.is = is;\n    this.os = os;\n  }\n\n  public void run() {\n\
      \    try {\n      int c;\n      while ((c = is.read()) != -1)\n          os.print((char)\
      \ c);\n    } catch (IOException x) {\n      // Handle error\n    }\n  }\n}\n\
      \npublic class Exec {\n  public static void main(String[] args)\n    throws\
      \ IOException, InterruptedException {\n\n    Runtime rt = Runtime.getRuntime();\n\
      \    Process proc = rt.exec(\"notemaker\");\n\n    // Any error message?\n \
      \   Thread errorGobbler\n      = new Thread(new StreamGobbler(proc.getErrorStream(),\
      \ System.err));\n\n    // Any output?\n    Thread outputGobbler\n      = new\
      \ Thread(new StreamGobbler(proc.getInputStream(), System.out));\n\n    errorGobbler.start();\n\
      \    outputGobbler.start();\n\n    // Any error?\n    int exitVal = proc.waitFor();\n\
      \    errorGobbler.join();   // Handle condition where the\n    outputGobbler.join();\
      \  // process ends before the threads finish\n  }\n}\n```\n\n**Exceptions**\n\
      FIO07-J-EX0: Failure to supply input to a process that never reads input from\
      \ its input stream is harmless and can be beneficial. Failure to empty the output\
      \ or error streams of a process that never sends output to its output or error\
      \ streams is similarly harmless or even beneficial. Consequently, programs are\
      \ permitted to ignore the input, output, or error streams of processes that\
      \ are *guaranteed* not to use those streams."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO07-J：不要让外部进程阻塞输入和输出流
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO07-J:Do not let external processes block on IO buffers
    Default: true
    Description: "对于`java.lang.Runtime`类的`exec()`方法以及相关的`ProcessBuilder.start()`方法来说，它们可以用来调用外部程序。当运行的时候，这些程序会表示成java.lang.Process对象。这个进程包含一个输入流、一个输出流、一个错误流。因为Process对象运行Java程序与外部程序进行通信，这个进程的输入流就是OutputStream对象，`Process.getOutputStream()`方法可以访问它。同样，InputStream对象可以表示进程的输出流和错误流，`Process.getInputStream()`和`Process.getErrorStream()`方法可以访问进程的输出流和错误流。\
      \  \n这些进程会要求发送输入到输入流，并且它们会同样基于它们的输出流、错误流产生输出结果。对这些外部程序的不正确处理会导致意想不到的异常、Dos攻击和其他安全问题。\
      \  \n一个视图在一个空的输入流上读取输入的进程会阻塞，直到输入出现。因而，调用这样的进程时候必须要有输入。  \n一个外部进程的输入会将保留的输出或错误流的可用缓冲区耗尽。当这发生时，Java程序会同样阻塞外部进程，阻止Java程序和外部进程的继续执行。注意：在许多平台上，给输出流使用的缓冲区大小是有限制的。因而，当调用一个外部进程的时候，如果进程将任何数据发送到输出流中，那么数据流就会被清空。同样，如果进程将任何数据发送到错误流中，也必须清空错误流。\
      \  \n风险评估：违反该条准则可能会导致运行异常和DoS漏洞。    \n**不规范的代码样例1**：  \n```java\npublic class\
      \ Exec {\n    public static void main(String args[]) throws IOException {\n\
      \        Runtime rt = Runtime.getRuntime();\n        Process proc = rt.exec(\"\
      notemaker\");\n        int exitVal = proc.exitValue();\n    }\n}\n```  \n代码使用了外部命令notemarker来调用一个记事本应用，这个notemarker不会读取输入流，但会将输出发送至输出流和错误流。\
      \  \n代码使用`exec()`方法来调用notemarker，它会返回一个Process对象。`exitValue()`方法为终止的进程返回退出值。但在调用一个活跃的进程的时候，它会抛出IllegalThreadStateException异常。因为这个程序不会等notemarker进程终止，对`exitValue()`的调用很可能会抛出一个IllegalThreadStateException异常。\
      \  \n**不规范的代码样例2**：  \n```java\npublic class Exec {\n    public static void\
      \ main(String args[]) throws IOException, InterruptedException {\n        Runtime\
      \ rt = Runtime.getRuntime();\n        Process proc = rt.exec(\"notemaker\");\n\
      \        int exitVal = proc.waitFor();\n    }\n}\n```\n`waitFor()`会阻塞调用线程，直到notemarker线程终结。返回在样例1中的IllegalThreadStateException异常。但，这个例子会经历一个不确定的在终止之前的延时。notemarker进程的输出会耗尽为输出和错误流保持的可用缓存，因为没有一个流在等进程结束的时候会被读取。如果一个缓存满载的状态，它同样会堵塞notemarker进程，从而阻止执行notemarker进程和Java程序。\
      \  \n**不规范的代码样例3**：  \n```java\npublic class Exec {\n    public static void\
      \ main(String args[]) throws IOException, InterruptedException {\n        Runtime\
      \ rt = Runtime.getRuntime();\n        Process proc = rt.exec(\"notemaker\");\n\
      \        InputStream is = proc.getInputStream();\n        int c;\n        while\
      \ ((c = is.read()) != -1) {\n            System.out.print((char) c);\n     \
      \   }\n        int exitVal = proc.waitFor();\n    }\n}\n```  \n代码正确的将进程的输出流清空，因此它可以防止输出流满载和阻塞。然而，它忽略了进程的错误流，这个错误流同样会充满并阻塞进程。"
    Description_En: "The `exec()` method of the `java.lang.Runtime` class and the\
      \ related `ProcessBuilder.start()` method can be used to invoke external programs.\
      \ While running, these programs are represented by a `java.lang.Process` object.\
      \ This process contains an input stream, output stream, and error stream. Because\
      \ the `Process` object allows a Java program to communicate with its external\
      \ program, the process's input stream is an `OutputStream` object, accessible\
      \ by the `Process.getOutputStream()` method. Likewise, the process's output\
      \ stream and error streams are both represented by `InputStream` objects, accessible\
      \ by the `Process.getInputStream()` and `Process.getErrorStream()` methods.\n\
      These processes may require input to be sent to their input stream, and they\
      \ may also produce output on their output stream, their error stream, or both.\
      \ Incorrect handling of such external programs can cause unexpected exceptions,\
      \ denial of service (DoS), and other security problems.\nA process that tries\
      \ to read input on an empty input stream will block until input is supplied.\
      \ Consequently, input must be supplied when invoking such a process.\nOutput\
      \ from an external process can exhaust the available buffer reserved for its\
      \ output or error stream. When this occurs, the Java program can block the external\
      \ process as well, preventing any forward progress for both the Java program\
      \ and the external process. Note that many platforms limit the buffer size available\
      \ for output streams. Consequently, when invoking an external process, if the\
      \ process sends any data to its output stream, the output stream must be emptied.\
      \ Similarly, if the process sends any data to its error stream, the error stream\
      \ must also be emptied.\n**Noncompliant Code Example (`exitValue()`)**\n\nThis\
      \ noncompliant code example invokes a hypothetical cross-platform notepad application\
      \ using the external command `notemaker`. The `notemaker` application does not\
      \ read its input stream but sends output to both its output stream and error\
      \ stream.\n\nThis noncompliant code example invokes `notemaker` using the `exec()`\
      \ method, which returns a `Process` object. The `exitValue()` method returns\
      \ the exit value for processes that have terminated, but it throws an `IllegalThreadStateException`\
      \ when invoked on an active process. Because this noncompliant example program\
      \ fails to wait for the `notemaker` process to terminate, the call to `exitValue()`\
      \ is likely to throw an `IllegalThreadStateException`.\n\n```java\npublic class\
      \ Exec {\n  public static void main(String args[]) throws IOException {\n  \
      \  Runtime rt = Runtime.getRuntime();\n    Process proc = rt.exec(\"notemaker\"\
      );\n    int exitVal = proc.exitValue();\n  }\n}\n```\n**Noncompliant Code Example\
      \ (`waitFor()`)**\n\nIn this noncompliant code example, the `waitFor()` method\
      \ blocks the calling thread until the `notemaker` process terminates. This approach\
      \ prevents the `IllegalThreadStateException` from the previous example. However,\
      \ the example program may experience an arbitrary delay before termination.\
      \ Output from the `notemaker` process can exhaust the available buffer for the\
      \ output or error stream because neither stream is read while waiting for the\
      \ process to complete. If either buffer becomes full, it can block the `notemaker`\
      \ process as well, preventing all progress for both the `notemaker` process\
      \ and the Java program.\n\n```java\npublic class Exec {\n  public static void\
      \ main(String args[])\n                          throws IOException, InterruptedException\
      \ {\n    Runtime rt = Runtime.getRuntime();\n    Process proc = rt.exec(\"notemaker\"\
      );\n    int exitVal = proc.waitFor();\n  }\n}\n```\n\n**Noncompliant Code Example\
      \ (Process Output Stream)**\n\nThis noncompliant code example properly empties\
      \ the process's output stream, thereby preventing the output stream buffer from\
      \ becoming full and blocking. However, it ignores the process's error stream,\
      \ which can also fill and cause the process to block.\n\n```java\npublic class\
      \ Exec {\n  public static void main(String args[])\n                     throws\
      \ IOException, InterruptedException {\n    Runtime rt = Runtime.getRuntime();\n\
      \    Process proc = rt.exec(\"notemaker\");\n    InputStream is = proc.getInputStream();\n\
      \    int c;\n    while ((c = is.read()) != -1) {\n      System.out.print((char)\
      \ c);\n    }\n    int exitVal = proc.waitFor();\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141407'
    language: Java
  - Advice: "1.不要启用Struts2-struts1-plugin插件；  \n2.不要使用showcase.war；  \n3.始终使用资源键，而不是将原始消息传递给ActionMessage：\
      \  \n建议采用如下方式：  \n```java\nmessages.add(\"msg\", new ActionMessage(\"struts1.gangsterAdded\"\
      , gform.getName()));\n```\n禁止采用如下方式：  \n```java\nmessages.add(\"msg\", new ActionMessage(\"\
      Gangster \" + gform.getName() + \" was added\"));\n```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 8.1 第三方软件安全
      - type: DetailClassChin
        value: A.5.1.2 避免Struts2的S2-048漏洞
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：Apache Struts2是Apache基金会发布的一款实现了MVC模式的开源框架，广泛应用于 Web开发和大型网站建设。\
      \  \n使用了Apache Struts 1插件的Apache Struts 2.3.X版本中存在远程代码执行漏洞（漏洞编号： CNNVD-201706-928，CVE-2017-9791）。该漏洞出现于Struts2的Struts\
      \ 1插件的`org.apache.struts2.s1.Struts1Action`类中，该类是为了将 Struts1中的Action包装成为Struts2中的\
      \ Action ，以保证Struts2的兼容性。在Struts2中的Struts1 插件启用的情况下，远程攻击者可通过使用恶意字段值，构造特定的输入，发送到\
      \ ActionMessage类中，从而导致任意命令执行，进而获取目标主机系统权限。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170071'
    language: Java
  - Advice: "加密大于块的数据时，应该避免使用ECB模式。由于CBC模式不会对相同的明文块生成相同的密文块，所以CBC模式更好。然而，CBC模式效率较低，并且在和SSL一起使用时会造成严重风险。可以改用`CCM(Counter\
      \ with CBC-MAC)`模式，如果更注重性能，在可用的情况下则使用`GCM(Galois/Counter)`模式。  \n  \n**例如**：以下代码将AES密码用于CBC模式。\
      \  \n```java\n...  \nCipher cipher = Cipher.getInstance(\"AES/CBC/PKCS7Padding\"\
      , \"BC\");  \ncipher.init(Cipher.ENCRYPT_MODE, createSecretKey(seed));  \n...\n\
      ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 密码规范
      - type: DetailClassChin
        value: 【推荐】不要使用不安全的操作模式
    Categories_En:
      Category: []
    Default: true
    Description: "块密码又称为分组加密，一次加密明文中的一个块。将明文按一定的位长分组，明文组经过加密运算得到密文组，密文组经过解密运算（加密运算的逆运算），还原成明文组。这种加密算法共有四种操作模式用于描述如何重复地应用密码的单块操作来安全的转换大于块的数据量，分别是电子代码(ECB)、密码块链(CBC)、密码反馈(CFB)以及输出反馈（OFB）。其中ECB模式下相同的明文块总是会得到相同的密文，故不能抵挡回放攻击，而CBC模式则没有这个缺陷。\
      \  \n  \n**例如**：以下代码将AES密码用于ECB模式。  \n```java\n...  \nCipher cipher = Cipher.getInstance(\"\
      AES/ECB/PKCS7Padding\", \"BC\");  \ncipher.init(Cipher.ENCRYPT_MODE, createSecretKey(seed));\
      \  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Insecure Mode of Operation
    ENSubClassChin: Weak Encryption
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: 等级计算
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 327:Use of a Broken or Risky Cryptographic Algorithm
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: security,password,bad_practice
    Type: std
    WhiteList: not
    id: 0202000010150208
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n```java\n...  \nCipher cipher =\
      \ Cipher.getInstance(\"AES/CBC/PKCS7Padding\", \"BC\");  \ncipher.init(Cipher.ENCRYPT_MODE,\
      \ createSecretKey(seed));  \n...\n```  \n以上代码将AES密码用于CBC模式。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.2 数据加密与保护
      - type: DetailClassChin
        value: A.2.2.1.6 避免使用不安全的操作模式
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：块密码又称为分组加密，一次加密明文中的一个块。将明文按一定的位长分组，明文组经过加密运算得到密文组，密文组经过解密运算（加密运算的逆运算），还原成明文组。这种加密算法共有四种操作模式用于描述如何重复地应用密码的单块操作来安全的转换大于块的数据量，分别是电子代码(ECB)、密码块链(CBC)、密码反馈(CFB)以及输出反馈（OFB）。其中ECB模式下相同的明文块总是会得到相同的密文，故不能抵挡回放攻击，而CBC模式则没有这个缺陷。\
      \  \n对于避免使用不安全的操作模式的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：   \n```java\n... \
      \ \nCipher cipher = Cipher.getInstance(\"AES/ECB/PKCS7Padding\", \"BC\");  \n\
      cipher.init(Cipher.ENCRYPT_MODE, createSecretKey(seed));  \n...\n```  \n以上代码将AES密码用于ECB模式。\
      \  \n加密大于块的数据时，应该避免使用ECB模式。由于CBC模式不会对相同的明文块生成相同的密文块，所以CBC模式更好。然而，CBC模式效率较低，并且在和SSL一起使用时会造成严重风险。可以改用CCM(Counter\
      \ with CBC-MAC)模式，如果更注重性能，在可用的情况下则使用GCM(Galois/Counter)模式。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 327:Use of a Broken or Risky Cryptographic Algorithm
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170015'
    language: Java
  - Advice: "**规范的代码样例1（交互）**：\n这个解决方案通过要求用户指定另一个文件名来处理`FileNotFoundException` 。\n\
      ```java\nboolean validFlag = false;\ndo {\n  try {\n    // ...\n    // If requested\
      \ file does not exist, throws FileNotFoundException\n    // If requested file\
      \ exists, sets validFlag to true\n    validFlag = true;\n  } catch (FileNotFoundException\
      \ e) {\n    // Ask the user for a different file name\n  }\n} while (validFlag\
      \ != true);\n// Use the file\n```\n为了遵守ERR01-J. 不允许异常暴露敏感信息，应该只允许用户访问用户特定目录下的文件。这可以防止任何其他逃过循环的`IOException`泄露敏感的文件系统信息。\n\
      **规范的代码样例2（异常报告）**：\n对异常情况的正确报告是取决于上下文的。例如，GUI应用程序应该以图形化的方式报告异常，例如在一个错误对话框中。大多数库类应该能够客观地决定如何报告异常，以保持模块化；它们不能依赖`System.err`，不能依赖任何特定的记录器，也不能依赖窗口环境的可用性。因此，希望报告异常的库类应该指定它们用来报告异常的API。这个兼容的解决方案指定了一个报告异常的接口，它导出了`report()`方法，以及一个默认的异常报告器类，库可以使用。异常报告器可以被子类重写。\n\
      ```java\npublic interface Reporter {\n  public void report(Throwable t);\n}\n\
      \nclass ExceptionReporterPermission extends Permission {\n  // ...\n}\n\npublic\
      \ class ExceptionReporter {\n\n  // Exception reporter that prints the exception\n\
      \  // to the console (used as default)\n  private static final Reporter PrintException\
      \ = new Reporter() {\n    public void report(Throwable t) {\n      System.err.println(t.toString());\n\
      \    }\n  };\n\n  // Stores the default reporter\n  // The default reporter\
      \ can be changed by the user\n  private static Reporter Default = PrintException;\n\
      \n  // Helps change the default reporter back to\n  // PrintException in the\
      \ future\n  public static Reporter getPrintException() {\n    return PrintException;\n\
      \  }\n\n  public static Reporter getExceptionReporter() {\n    return Default;\n\
      \  }\n\n  // May throw a SecurityException (which is unchecked)\npublic static\
      \ void setExceptionReporter(Reporter reporter) {\n    // Custom permission\n\
      \    ExceptionReporterPermission perm = new\n        ExceptionReporterPermission(\"\
      exc.reporter\");\n    SecurityManager sm = System.getSecurityManager();\n  \
      \  if (sm != null) {\n      // Check whether the caller has appropriate permissions\n\
      \      sm.checkPermission(perm);\n    }\n    // Change the default exception\
      \ reporter\n    Default = reporter;\n  }\n}\n```\n`setExceptionReporter()`方法通过限制试图改变异常报告者的行为，使之只能由拥有自定义权限`ExceptionReporterPermission`的调用者进行，目标是`exc.reporter`，从而防止敌对代码恶意安装一个更粗略的报告者，泄露敏感信息或将异常报告引向不适当的位置，例如攻击者的计算机。\n\
      该库随后可以在`catch`子句中使用异常报告者。\n```java\ntry {\n  // ...\n} catch (IOException warning)\
      \ {\n  ExceptionReporter.getExceptionReporter().report(warning);\n  // Recover\
      \ from the exception...\n}\n```\n任何拥有所需权限的客户端代码都可以用一个处理程序来覆盖`ExceptionReporter`，该处理程序可以记录错误或提供一个对话框，或两者兼有。例如，一个使用Swing的GUI客户端可能需要用对话框来报告异常。\n\
      ```java\nExceptionReporter.setExceptionReporter(new ExceptionReporter() {\n\
      \  public void report(Throwable exception) {\n    JOptionPane.showMessageDialog(frame,\n\
      \                                  exception.toString,\n                   \
      \               exception.getClass().getName(),\n                          \
      \        JOptionPane.ERROR_MESSAGE);\n  }});\n```\n**规范的代码样例3**\n有时，出于安全考虑，必须对用户隐藏异常（参见ERR01-J.不要让异常暴露敏感信息）。在这种情况下，一种可接受的方法是对\
      \ \"ExceptionReporter \"类进行子类化，除了覆盖默认的 \"report() \"方法外，还添加一个 \"filter() \"\
      方法。\n```java\nclass MyExceptionReporter extends ExceptionReporter {\n  private\
      \ static final Logger logger =\n      Logger.getLogger(\"com.organization.Log\"\
      );\n\n  public static void report(Throwable t) {\n    t = filter(t);\n    if\
      \ (t != null) {\n      logger.log(Level.FINEST, \"Loggable exception occurred\"\
      , t);\n    }\n  }\n\n  public static Exception filter(Throwable t) {\n    if\
      \ (t instanceof SensitiveException1) {\n      // Too sensitive, return nothing\
      \ (so that no logging happens)\n      return null;\n    } else if (t instanceof\
      \ SensitiveException2) {\n      // Return a default insensitive exception instead\n\
      \      return new FilteredSensitiveException(t);\n    }\n    // ...\n    //\
      \ Return for reporting to the user\n    return t;\n  }\n}\n\n\n// ...Definitions\
      \ for SensitiveException1, SensitiveException2\n// and FilteredSensitiveException...\n\
      ```\n`report()` 方法接受 `Throwable` 实例并因此处理所有错误、已检查异常和未检查异常。 过滤机制基于*白名单*方法，其中仅将非敏感异常传播给用户。\
      \ 日志文件中禁止出现的异常可以用同样的方式过滤。 这种方法通过报告针对抽象定制的异常，同时还记录低级别原因以供未来故障分析，从而提供异常链接的好处。\n\
      **规范的代码样例4**\n这个兼容的解决方案捕获了 `InterruptedException`，并通过调用当前线程的 `interrupt()`方法恢复中断状态。\n\
      ```java\nclass Foo implements Runnable {\n  public void run() {\n    try {\n\
      \      Thread.sleep(1000);\n    } catch (InterruptedException e) {\n      Thread.currentThread().interrupt();\
      \ // Reset interrupted status\n    }\n  }\n}\n```\n**特例**\nERR00-J-EX0：在释放资源的过程中发生的异常，在释放资源失败不能影响未来程序行为的情况下，可以被抑制。释放资源的例子包括关闭文件、网络套接字、关闭线程，等等。这些资源通常在\
      \ \"catch \"或 \"finally \"块中被释放，在随后的执行过程中不会被重新使用。因此，异常不能通过资源耗尽以外的任何途径影响未来的程序行为。当资源耗尽被充分处理时，只需对异常进行处理并记录，以便将来改进；在这种情况下，没有必要进行额外的错误处理。\n\
      \nERR00-J-EX1.当在一个特定的抽象层不可能从一个异常条件中恢复时，该层的代码必须不处理该异常条件。在这种情况下，必须抛出一个适当的异常，以便更高层次的代码能够捕捉到这个异常条件并尝试恢复。在这种情况下，最常见的实现是省略\
      \ \"catch \"块，允许异常正常传播。\n\n```java\n// When recovery is possible at higher levels\n\
      private void doSomething() throws FileNotFoundException {\n  // Requested file\
      \ does not exist; throws FileNotFoundException\n  // Higher level code can handle\
      \ it by displaying a dialog box and asking\n  // the user for the file name\n\
      }\n```\n一些API可能会限制由特定方法抛出的可允许的异常。在这种情况下，可能有必要捕获一个异常，并将其包裹在一个允许的异常中，或者将其转换为允许的异常之一。\n\
      ```java\npublic void myMethod() throws MyProgramException {\n  // ...\n  try\
      \ {\n    // Requested file does not exist\n    // User is unable to supply the\
      \ file name\n  } catch (FileNotFoundException e) {\n    throw new MyProgramException(e);\n\
      \  }\n  // ...\n}\n```\n另外，当更高层次的代码也无法从一个特定的异常中恢复时，被检查的异常可能被包裹在一个未检查的异常中并被重新抛出。\n\
      \nERR00-J-EX2: 当扩展 \"Thread \"类时，\"InterruptedException \"可以被捕获和抑制 [Goetz 2006]。中断请求也可以被实现线程中断策略的代码所抑制\
      \ [Goetz 2006, p. 143]。"
    Advice_En: "**Compliant Solution (Interactive)**\n\nThis compliant solution handles\
      \ a `FileNotFoundException` by requesting that the user specify another file\
      \ name:\n\n```java\nboolean validFlag = false;\ndo {\n  try {\n    // ...\n\
      \    // If requested file does not exist, throws FileNotFoundException\n   \
      \ // If requested file exists, sets validFlag to true\n    validFlag = true;\n\
      \  } catch (FileNotFoundException e) {\n    // Ask the user for a different\
      \ file name\n  }\n} while (validFlag != true);\n// Use the file\n```\n\nTo comply\
      \ with ERR01-J. Do not allow exceptions to expose sensitive information, the\
      \ user should only be allowed to access files in a user-specific directory.\
      \ This prevents any other `IOException` that escapes the loop from leaking sensitive\
      \ file system information.\n\n**Compliant Solution (Exception Reporter)**\n\n\
      Proper reporting of exceptional conditions is context-dependent. For example,\
      \ GUI applications should report the exception in a graphical manner, such as\
      \ in an error dialog box. Most library classes should be able to objectively\
      \ determine how an exception should be reported to preserve modularity; they\
      \ cannot rely on `System.err`, on any particular logger, or on the availability\
      \ of the windowing environment. As a result, library classes that wish to report\
      \ exceptions should specify the API they use to report exceptions. This compliant\
      \ solution specifies both an interface for reporting exceptions, which exports\
      \ the `report()` method, and a default exception reporter class that the library\
      \ can use. The exception reporter can be overridden by subclasses.\n\n```java\n\
      public interface Reporter {\n  public void report(Throwable t);\n}\n\nclass\
      \ ExceptionReporterPermission extends Permission {\n  // ...\n}\n\npublic class\
      \ ExceptionReporter {\n\n  // Exception reporter that prints the exception\n\
      \  // to the console (used as default)\n  private static final Reporter PrintException\
      \ = new Reporter() {\n    public void report(Throwable t) {\n      System.err.println(t.toString());\n\
      \    }\n  };\n\n  // Stores the default reporter\n  // The default reporter\
      \ can be changed by the user\n  private static Reporter Default = PrintException;\n\
      \n  // Helps change the default reporter back to\n  // PrintException in the\
      \ future\n  public static Reporter getPrintException() {\n    return PrintException;\n\
      \  }\n\n  public static Reporter getExceptionReporter() {\n    return Default;\n\
      \  }\n\n  // May throw a SecurityException (which is unchecked)\npublic static\
      \ void setExceptionReporter(Reporter reporter) {\n    // Custom permission\n\
      \    ExceptionReporterPermission perm = new\n        ExceptionReporterPermission(\"\
      exc.reporter\");\n    SecurityManager sm = System.getSecurityManager();\n  \
      \  if (sm != null) {\n      // Check whether the caller has appropriate permissions\n\
      \      sm.checkPermission(perm);\n    }\n    // Change the default exception\
      \ reporter\n    Default = reporter;\n  }\n}\n```\n\nThe `setExceptionReporter()`\
      \ method prevents hostile code from maliciously installing a more verbose reporter\
      \ that leaks sensitive information or that directs exception reports to an inappropriate\
      \ location, such as the attacker's computer, by limiting attempts to change\
      \ the exception reporter to callers that have the custom permission `ExceptionReporterPermission`\
      \ with target `exc.reporter`.\n\nThe library may subsequently use the exception\
      \ reporter in `catch` clauses:\n\n```java\ntry {\n  // ...\n} catch (IOException\
      \ warning) {\n  ExceptionReporter.getExceptionReporter().report(warning);\n\
      \  // Recover from the exception...\n}\n```\n\nAny client code that possesses\
      \ the required permissions can override the `ExceptionReporter` with a handler\
      \ that logs the error or provides a dialog box, or both. For example, a GUI\
      \ client using Swing may require exceptions to be reported using a dialog box:\n\
      \n```java\nExceptionReporter.setExceptionReporter(new ExceptionReporter() {\n\
      \  public void report(Throwable exception) {\n    JOptionPane.showMessageDialog(frame,\n\
      \                                  exception.toString,\n                   \
      \               exception.getClass().getName(),\n                          \
      \        JOptionPane.ERROR_MESSAGE);\n  }});\n```\n\n**Compliant Solution (Subclass\
      \ Exception Reporter and Filter-Sensitive Exceptions)**\n\nSometimes exceptions\
      \ must be hidden from the user for security reasons (see ERR01-J. Do not allow\
      \ exceptions to expose sensitive information). In such cases, one acceptable\
      \ approach is to subclass the `ExceptionReporter` class and add a `filter()`\
      \ method in addition to overriding the default `report()` method.\n\n```java\n\
      class MyExceptionReporter extends ExceptionReporter {\n  private static final\
      \ Logger logger =\n      Logger.getLogger(\"com.organization.Log\");\n\n  public\
      \ static void report(Throwable t) {\n    t = filter(t);\n    if (t != null)\
      \ {\n      logger.log(Level.FINEST, \"Loggable exception occurred\", t);\n \
      \   }\n  }\n\n  public static Exception filter(Throwable t) {\n    if (t instanceof\
      \ SensitiveException1) {\n      // Too sensitive, return nothing (so that no\
      \ logging happens)\n      return null;\n    } else if (t instanceof SensitiveException2)\
      \ {\n      // Return a default insensitive exception instead\n      return new\
      \ FilteredSensitiveException(t);\n    }\n    // ...\n    // Return for reporting\
      \ to the user\n    return t;\n  }\n}\n\n\n// ...Definitions for SensitiveException1,\
      \ SensitiveException2\n// and FilteredSensitiveException...\n```\n\nThe `report()`\
      \ method accepts a `Throwable` instance and consequently handles all errors,\
      \ checked exceptions, and unchecked exceptions. The filtering mechanism is based\
      \ on a *whitelisting* approach wherein only nonsensitive exceptions are propagated\
      \ to the user. Exceptions that are forbidden to appear in a log file can be\
      \ filtered in the same fashion (see FIO13-J. Do not log sensitive information\
      \ outside a trust boundary). This approach provides the benefits of exception\
      \ chaining by reporting exceptions tailored to the abstraction while also logging\
      \ the low-level cause for future failure analysis [Bloch 2008].\n**Compliant\
      \ Solution**\n\nThis compliant solution catches the `InterruptedException` and\
      \ restores the interrupted status by calling the `interrupt()` method on the\
      \ current thread:\n\n```java\nclass Foo implements Runnable {\n  public void\
      \ run() {\n    try {\n      Thread.sleep(1000);\n    } catch (InterruptedException\
      \ e) {\n      Thread.currentThread().interrupt(); // Reset interrupted status\n\
      \    }\n  }\n}\n```\n\nConsequently, calling methods (or code from a calling\
      \ thread) can determine that an interrupt was issued [Goetz 2006].\n\n**Exceptions**\n\
      \nERR00-J-EX0: Exceptions that occur during the freeing of a resource may be\
      \ suppressed in those cases where failure to free the resource cannot affect\
      \ future program behavior. Examples of freeing resources include closing files,\
      \ network sockets, shutting down threads, and so forth. Such resources are often\
      \ freed in `catch` or `finally` blocks and never reused during subsequent execution.\
      \ Consequently, the exception cannot influence future program behavior through\
      \ any avenue other than resource exhaustion. When resource exhaustion is adequately\
      \ handled, it is sufficient to sanitize and log the exception for future improvement;\
      \ additional error handling is unnecessary in this case.\n\nERR00-J-EX1: When\
      \ recovery from an exceptional condition is impossible at a particular abstraction\
      \ level, code at that level must not handle that exceptional condition. In such\
      \ cases, an appropriate exception must be thrown so that higher level code can\
      \ catch the exceptional condition and attempt recovery. The most common implementation\
      \ for this case is to omit a `catch` block and allow the exception to propagate\
      \ normally:\n\n```java\n// When recovery is possible at higher levels\nprivate\
      \ void doSomething() throws FileNotFoundException {\n  // Requested file does\
      \ not exist; throws FileNotFoundException\n  // Higher level code can handle\
      \ it by displaying a dialog box and asking\n  // the user for the file name\n\
      }\n```\n\nSome APIs may limit the permissible exceptions thrown by particular\
      \ methods. In such cases, it may be necessary to catch an exception and either\
      \ wrap it in a permitted exception or translate it to one of the permitted exceptions:\n\
      \n```java\npublic void myMethod() throws MyProgramException {\n  // ...\n  try\
      \ {\n    // Requested file does not exist\n    // User is unable to supply the\
      \ file name\n  } catch (FileNotFoundException e) {\n    throw new MyProgramException(e);\n\
      \  }\n  // ...\n}\n```\n\nAlternatively, when higher level code is also unable\
      \ to recover from a particular exception, the checked exception may be wrapped\
      \ in an unchecked exception and rethrown.\n\nERR00-J-EX2: An `InterruptedException`\
      \ may be caught and suppressed when extending class `Thread` [Goetz 2006]. An\
      \ interruption request may also be suppressed by code that implements a thread's\
      \ interruption policy [Goetz 2006, p. 143]."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 07. 异常行为 (ERR)
      - type: DetailClassChin
        value: ERR00-J：不要消除或忽略可检查异常
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 07. Exceptional Behavior (ERR)
      - type: DetailClassChin
        value: ERR00-J:Do not suppress or ignore checked exceptions
    Default: true
    Description: "程序员常常使用一个空的或者简单的catch块来捕获异常，并将其消除。每一个catch块都必须保证程序在发生异常的情况下继续运行。因此，catch块要么从异常中恢复，将异常重新抛出，由下一个与try对应的catch块来处理异常，要么根据catch块的上下文抛出一个适合的异常。\
      \  \n异常中断了预期中的应用程序流程。例如：在try程序段中，不会执行在异常抛出之后的表达式或语句。因此，必须恰当的处理异常。  \n**不规范的代码样例1**：\n\
      以下代码会打印异常的堆栈信息:\n```java\ntry {\n  //...\n} catch (IOException ioe) {\n  ioe.printStackTrace();\n\
      }\n```\n打印异常的堆栈信息可用于程序调试，但程序执行等效于抑制异常。打印异常的堆栈信息还有可能将进程的结构和状态信息泄露给攻击者。此代码片段在程序发生异常时会打印异常的堆栈信息来，但程序会继续执行，就像没有抛出异常一样。也就是说，应用程序的行为不受抛出的异常的影响，除非在try块中抛出异常的点之后有其他的一些表达式或语句。\n\
      **不规范的代码样例2**：\n如果线程在睡眠或等待时被中断，则会引发` java.lang.InterruptedException`。但是`Runnable`接口的`run()`方法不能抛出检查异常，必须处理`InterruptedException`。这个不合规的代码示例捕获并抑制了\
      \ InterruptedException：\n```java\nclass Foo implements Runnable {\n  public\
      \ void run() {\n    try {\n      Thread.sleep(1000);\n    } catch (InterruptedException\
      \ e) {\n      // Ignore\n    }\n  }\n}\n```\n此段代码`run()`方法的调用者无法确定是否发生了中断异常。诸如`Thread.start()`之类的调用者方法不应处理[Goetz\
      \ 2006]异常。如果这段代码在它自己的线程中被调用，那么调用当前线程无法知晓其是否被中断。"
    Description_En: "Programmers often suppress checked exceptions by catching exceptions\
      \ with an empty or trivial `catch` block. Each `catch` block must ensure that\
      \ the program continues only with valid invariants. Consequently, the `catch`\
      \ block must either recover from the exceptional condition, rethrow the exception\
      \ to allow the next nearest enclosing `catch` clause of a `try` statement to\
      \ recover, or throw an exception that is appropriate to the context of the `catch`\
      \ block.\n\nExceptions disrupt the expected control flow of the application.\
      \ For example, no part of any expression or statement that occurs in the `try`\
      \ block after the point from which the exception is thrown is evaluated. Consequently,\
      \ exceptions must be handled appropriately. Many reasons for suppressing exceptions\
      \ are invalid. For example, when the client cannot be expected to recover from\
      \ the underlying problem, it is good practice to allow the exception to propagate\
      \ outwards rather than to catch and suppress the exception.\n\n**Noncompliant\
      \ Code Example**\nThis noncompliant code example simply prints the exception's\
      \ stack trace:\n\n```java\ntry {\n  //...\n} catch (IOException ioe) {\n  ioe.printStackTrace();\n\
      }\n```\n\nPrinting the exception's stack trace can be useful for debugging purposes,\
      \ but the resulting program execution is equivalent to suppressing the exception.\
      \ Printing the stack trace can also leak information about the structure and\
      \ state of the process to an attacker (see ERR01-J. Do not allow exceptions\
      \ to expose sensitive information for more information). Note that even though\
      \ this noncompliant code example reacts to the exception by printing out a stack\
      \ trace, it then proceeds as though the exception were not thrown. That is,\
      \ the behavior of the application is unaffected by the exception being thrown\
      \ except that any expressions or statements that occur in the `try` block after\
      \ the point from which the exception is thrown are not evaluated.\n\n**Noncompliant\
      \ Code Example**\n\nIf a thread is interrupted while sleeping or waiting, it\
      \ causes a `java.lang.InterruptedException` to be thrown. However, the `run()`\
      \ method of interface `Runnable` cannot throw a checked exception and must handle\
      \ `InterruptedException`. This noncompliant code example catches and suppresses\
      \ `InterruptedException`:\n\n```java\nclass Foo implements Runnable {\n  public\
      \ void run() {\n    try {\n      Thread.sleep(1000);\n    } catch (InterruptedException\
      \ e) {\n      // Ignore\n    }\n  }\n}\n```\n\nThis code prevents callers of\
      \ the `run()` method from determining that an interrupted exception occurred.\
      \ Consequently, caller methods such as `Thread.start()` cannot act on the exception\
      \ [Goetz 2006]. Likewise, if this code were called in its own thread, it would\
      \ prevent the calling thread from knowing that the thread was interrupted."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 391:Unchecked Error Condition
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.5 catch语句块为空
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140800
    language: Java
  - Advice: "对应的符合规则的解决方案是：要求用户指定另一个文件名，以此来处理 FileNotFoundException异常。   \n    ```java\n\
      \    boolean volatile validFlag = false;   \n    do {   \n        try {   \n\
      \            // If requested file does not exist, throws FileNotFoundException\
      \    \n            // If requested file exists, sets validFlag to true validFlag\
      \ = true;   \n        } catch (FileNotFoundException e) {   \n            //\
      \ Ask the user for a different file name   \n        }   \n    } while (validFlag\
      \ != true);   \n    //Use the file\n    ```   \n    为符合规则的要求，只能允许用户访问特定的用户目录。这防止了其他的IOException跳出循环从而泄漏文件系统的敏感信息。\
      \   \n4) 特例   \n**特例 1**：释放资源时发生的异常可以被消除。在这种情况下，释放资源的失败并不会影响之后的程序行为。释放资源的例子有关闭文件、网络套接字、停止线程等。这样的资源通常是在\
      \ catch 或 finally程序段中释放的，并不会在之后的执行中使用。因此，除了造成资源枯竭外, 异常不会以任何方式影响之后的程序行为。   \n\
      **特例 2**：在特定的抽象层中，如果不可能从异常情况中恢复时，则不能由处于该抽象 层的代码来处理这个异常。这时必须抛出一个适当的异常，让髙层的代码捕获它，并尝试恢复。这种情况下，最常见的实现是忽略一个\
      \ catch 程序段，并允许异常正常地传递。   \n**特例 3**：当继承 Tread 类时，一个 InterruptedException 异常可以捕获并消除。实现了线程中断策略的代码可以消除一个中断请求。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.4 错误处理安全
      - type: DetailClassChin
        value: C.7.1 不要消除或忽略可检查的异常
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n异常出现时，必须恰当地处理异常，不要消除或者忽略可以检查的异常。   \n2) 原理   \n程序员常常使用一个空的或简单的\
      \ catch 程序段来捕获受控制的异常，并将其消除。catch 程序段要么从异常中恢复，将异常重新抛出，由下一个和 try 语句对应的 catch 段来处理异常，要么根据\
      \ catch   \n程序段的上下文抛出另一个适合的异常。异常会中断预期中的应用程序流程。   \n3) 示例   \n    如下不符合规则的代码示例简单地输入异常的栈跟踪。\
      \   \n    ```java\n    try {   \n        //...   \n    } catch (IOException\
      \ ioe) {    \n        ioe.printStacktrace();   \n    }\n    ```   \n       \n\
      \    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 391:Unchecked Error Condition
      - type: CERT
        value: ERR00-J:Do not suppress or ignore checked exceptions
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.5 catch语句块为空
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160063
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n```java\ntry {  \n    //...  \n\
      } catch (IOException ioe) {  \n    Logger.info(e,\"file is not find\")  \n}\n\
      ```  \n采用日志记录工具记录异常。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.4 异常处理安全
      - type: DetailClassChin
        value: A.3.4.2 不要消除或忽略可检查异常
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：异常是为了中断预期外的应用程序流程。例如，在try程序段中，不会执行在异常抛出之后的表达式或语句。因此，必须恰当的处理异常。\
      \  \n对于不要消除或忽略可检查异常的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\ntry {\
      \  \n    //...  \n} catch (IOException ioe) {  \n    //do nothing  \n}\n```\
      \  \n对于捕获的异常，需要进行对应的处理，而不是忽略异常，或者消除异常。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 391:Unchecked Error Condition
      - type: CERT
        value: ERR00-J:Do not suppress or ignore checked exceptions
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.5 catch语句块为空
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170054'
    language: Java
  - Advice: "**规范的代码样例1**：  \n```java\npackage org.apache.xpath.compiler;  \npublic\
      \ class FunctionTable {  \n    public static final FuncLoader m_functions; \
      \ \n}\n```  \n声明FuncLoader类型的对象m_functions为final的。这样对于多线程的使用是安全的。然而，这样也不能防止攻击者在静态变量（对象类型）初始化之前就间接的得到不正确的值（参考规则：DCL00-J）。另外，即使对象本身是不变的，但被引用的对象的每一个成员都会被修改，如：数组对象本身是static\
      \ final 的，但，数组内部的每一元素都可以被修改。所以，将public修改为private，以此来防止类外的代码直接访问静态字段。  \n**规范的代码样例2**：\
      \  \n按照Java API对于Serializable的说明，需要声明`serialVersionUID` 为private static final。\
      \  \n```java\nclass DataSerializer implements Serializable {  \n    private\
      \ static final long serialVersionUID = 1973473122623778747L;  \n}\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution declares the `FuncLoader`\
      \ static field final and treats it as a constant:\n\n```java\npublic static\
      \ final FuncLoader m_functions;\n// Initialize m_functions in a static initialization\
      \ block\n```\n\nFields declared static and final are also safe for multithreaded\
      \ use (see TSM03-J. Do not publish partially initialized objects for more information).\
      \ However, remember that simply changing the modifier to final might not prevent\
      \ attackers from indirectly retrieving an incorrect value from the static final\
      \ variable before its initialization (see DCL00-J. Prevent class initialization\
      \ cycles for more information). Furthermore, individual members of the referenced\
      \ object can also be changed if the object itself is mutable.\n\nIt is also\
      \ permissible to use a wrapper method to retrieve the value of `m_functions`,\
      \ allowing `m_functions` to be declared private (see rule OBJ01-J. Limit accessibility\
      \ of fields for more information).\n**Compliant Solution**\n\nThis compliant\
      \ solution declares the `serialVersionUID` field final and private:\n\n```java\n\
      class DataSerializer implements Serializable {\n  private static final long\
      \ serialVersionUID = 1973473122623778747L;\n}\n```\n\nThe serialization mechanism\
      \ uses the `serialVersionUID` field internally, so accessible wrapper methods\
      \ are unnecessary."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 05. 面向对象 (OBJ)
      - type: DetailClassChin
        value: OBJ10-J：不要使用public static 非final字段
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 05. Object Orientation (OBJ)
      - type: DetailClassChin
        value: OBJ10-J:Do not use public static nonfinal fields
    Default: true
    Description: "客户端代码可以轻易的访问公有静态字段。安全管理器不会对读取或写入这些变量进行检查。此外，在将新值存储到这些字段之前，是不能通过编程的方式进行验证的。\
      \  \n多线程场合中，非final的公有静态字段会被不一致的方法修改。  \n不正确的使用公有静态字段，同样会导致安全性问题。如果，当一个字段为一个更通用的类型`（java.lang.Object）`时，非受信代码可以通过恶意的方法来提供一个非期望的子类型。\
      \  \n风险评估：对公有的静态字段的未授权修改会导致可预期的行为，并且破坏类的不变性。此外，当那些类装载器在同一个代理链中时，因为静态字段对于被不同类装载器装载的代码是可见的，所以那些字段会被用来在不同的应用域中作为一个隐藏的通信通道而存在。\
      \  \n**不规范的代码样例1**：  \n```java\npackage org.apache.xpath.compiler;\n\npublic\
      \ class FunctionTable {\n  public static FuncLoader m_functions;\n}\n```\n这段代码是从JDK\
      \ 1.4.2中摘录出来的。它声明了一个包含公有静态字段m_functions的类FunctionTable。  \n攻击者可以使用以下方式来代替FunctionTable：\n\
      ```java\nFunctionTable.m_functions = new_table;\n```\n替换FunctionTable会给攻击者访问XPathContext的机会，而XPathContext会被用来设置评估Xpath表达式的引用点。操作XPathContext会导致XML字段被不一致的方式修改，从而导致不可预期的行为。同样，因为静态变量对于整个Java运行环境来说是全局的，它们可以被不同应用域之间作为一个隐藏的通信通道。\
      \  \n这个漏洞在JDK 1.4.2_05中修复。  \n**不规范的代码示样例2**：  \n```java\nclass DataSerializer\
      \ implements Serializable {  \n    public static long serialVersionUID = 1973473122623778747L;\
      \  \n    // . . .  \n}\n```  \n代码在一个序列化的类中声明了一个公有静态非final的字段serialVersionUID。\n\
      **不规范的代码样例3**：\n此不规范的代码示例在为序列化而设计的类中使用了一个公共静态非final`serialVersionUID`字段：\n```java\n\
      class DataSerializer implements Serializable {\n  public static long serialVersionUID\
      \ = 1973473122623778747L;\n  // ...\n}\n```"
    Description_En: "Client code can trivially access public static fields because\
      \ access to such fields are not checked by a security manager. Furthermore,\
      \ new values cannot be validated programmatically before they are stored in\
      \ these fields.\n\nIn the presence of multiple threads, nonfinal public static\
      \ fields can be modified in inconsistent ways (see TSM01-J. Do not let the this\
      \ reference escape during object construction for an example).\n\nImproper use\
      \ of public static fields can also result in type-safety issues. For example,\
      \ untrusted code can supply an unexpected subtype with malicious methods when\
      \ the variable is defined to be of a more general type, such as java.lang.Object\
      \ [Gong 2003]. As a result, classes must not contain nonfinal public static\
      \ fields.\n\n**Noncompliant Code Example**\n\nThis noncompliant code example\
      \ is taken from JDK v1.4.2 [FT 2008]. It declares a function table containing\
      \ a public static field.\n\n```java\npackage org.apache.xpath.compiler;\n\n\
      public class FunctionTable {\n  public static FuncLoader m_functions;\n}\n```\n\
      \nAn attacker can replace the function table as follows:\n\n```java\nFunctionTable.m_functions\
      \ = new_table;\n```\n\nReplacing the function table gives the attacker access\
      \ to `XPathContext`, which is used to set the reference node for evaluating\
      \ `XPath` expressions. Manipulating `XPathContext` can cause XML fields to be\
      \ modified in inconsistent ways, resulting in unexpected behavior. Also, because\
      \ static variables are global across the Java Runtime Environment (JRE), they\
      \ can be used as a covert communication channel between different application\
      \ domains (for example, through code loaded by different class loaders).\n\n\
      This vulnerability was repaired in JDK v1.4.2_05.\n\n**Noncompliant Code Example\
      \ (`serialVersionUID`)**\n\nThis noncompliant code example uses a public static\
      \ nonfinal `serialVersionUID` field in a class designed for serialization:\n\
      \n```java\nclass DataSerializer implements Serializable {\n  public static long\
      \ serialVersionUID = 1973473122623778747L;\n  // ...\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 500:Public Static Field Not Marked Final
      - type: CWE
        value: 493:Critical Public Variable Without Final Modifier
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140610'
    language: Java
  - Advice: "对应的符合规则的解决方案是：声明 FuncLoader 静态成员数据为 final 并将其视为一个常数。   \n    ```java\n\
      \    public static final FuncLoader m_functions;   \n    //Initialize m_functions\
      \ in a constructor   \n    ```\n    声明为静态和 final 的数据成员，对多线程使用同样是安全的。   \n  \
      \  2、不符合规则的代码示例（`serialVersionLMD)`，该示例在一个用来进行序列化的类中，使用了一个公有静态的非 final 的 serialVersionUID\
      \ 的字段。   \n    ```java\n    class DataSerializer implements Serializable { \
      \  \n        public static long serialVersionUID = 1973473122623778747L;   \n\
      \        // . . .    \n    }\n    ```   \n       \n    对应的符合规则的解决方案是：声明 serialVersionUID\
      \ 字段为 final 和私有的。   \n    ```java\n    class DataSerializer implements Serializable\
      \ {   \n        private static final long serialVersionUID = 1973473122623778747L;\n\
      \    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.5.11 不要使用公有静态的非final变量
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n类必须不能包含非 final 的公有静态字段。   \n2) 原理   \n通常，客户代码可轻易访问到公有静态数据成员，而且安全管理器也不会对读取或写入这些变量进行检查。\
      \   \n在多线程的场合中，非 final 的公有静态字段会被不一致的方式修改。不正确使用公有静态数据成员，会导致类型安全问题。例如，当定义变量为一个更通用的像\
      \ java.Iang.Object 这样的通用类型时，非受信代码可以通过恶意方法来提供一个非期望的子类型。因此，类必须不能包含非 final 的公有静态字段。\
      \   \n3) 示例   \n    1、不符合规则的代码示例，该示例是声明了一个包含公有静态字段的方法表。   \n    ```java\n  \
      \  package org.apache.xpath.compiler;   \n    public class FunctionTable { \
      \  \n        public static FuncLoader m_functions;   \n    }\n    ```   \n \
      \   攻击者会使用以下的方式来代替函数表：   \n    `FunctionTable.m—functions = new_table;`   \n\
      \    替换函数表会给攻击者访问 XPathContext 提供机会，而 XPathContext 会被用来设置评估 XPath 表达式的引用节点。操作\
      \ XPathContext 会导致 XML 字段被不一致的方式修改，从而导致不可预期的行为。   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 500:Public Static Field Not Marked Final
      - type: CWE
        value: 493:Critical Public Variable Without Final Modifier
      - type: CERT
        value: OBJ10-J:Do not use public static nonfinal variables
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160048
    language: Java
  - Advice: "符合规范的解决方案是将应用于可见的参数的净化处理也应用于隐藏的参数。  \n**规范的代码样例1**：  \n```java\npublic\
      \ class SampleServlet extends HttpServlet {\n    public void doGet(HttpServletRequest\
      \ request, HttpServletResponse response)\n            throws IOException, ServletException\
      \ {\n        response.setContentType(\"text/html\");\n        PrintWriter out\
      \ = response.getWriter();\n        out.println(\"<html>\");\n\n        String\
      \ visible = request.getParameter(\"visible\");\n        String hidden = request.getParameter(\"\
      hidden\");\n\n        if (visible != null || hidden != null) {\n           \
      \ out.println(\"Visible Parameter:\");\n            out.println(sanitize(visible));\n\
      \            out.println(\"<br>Hidden Parameter:\");\n            out.println(sanitize(hidden));\
      \ // Hidden variable sanitized\n        } else {\n            out.println(\"\
      <p>\");\n            out.print(\"<form action=\\\"\");\n            out.print(\"\
      SampleServlet\\\" \");\n            out.println(\"method=POST>\");\n       \
      \     out.println(\"Parameter:\");\n            out.println(\"<input type=text\
      \ size=20 name=visible>\");\n            out.println(\"<br>\");\n\n        \
      \    out.println(\"<input type=hidden name=hidden value=\\'a benign value\\\
      '>\");\n            out.println(\"<input type=submit>\");\n            out.println(\"\
      </form>\");\n        }\n    }\n\n    public void doPost(HttpServletRequest request,\
      \ HttpServletResponse response)\n            throws IOException, ServletException\
      \ {\n        doGet(request, response);\n    }\n\n    // Filter the specified\
      \ message string for characters\n    // that are sensitive in HTML.\n    public\
      \ static String sanitize(String message) {\n        // ...\n    }\n}\n```\n\
      因此，当恶意URL进入浏览器时，servlet产生以下内容:  \n```\nVisible Parameter: dummy   \nHidden Parameter:\
      \ <font color=red>Surprise</font>!!!\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution applies the same\
      \ sanitization to the hidden parameter as is applied to the visible parameter:\n\
      \n```java\npublic class SampleServlet extends HttpServlet {\n  public void doGet(HttpServletRequest\
      \ request, HttpServletResponse response)\n    throws IOException, ServletException\
      \ {\n    response.setContentType(\"text/html\");\n    PrintWriter out = response.getWriter();\n\
      \    out.println(\"<html>\");\n\n    String visible = request.getParameter(\"\
      visible\");\n    String hidden = request.getParameter(\"hidden\");\n\n    if\
      \ (visible != null || hidden != null) {\n      out.println(\"Visible Parameter:\"\
      );\n      out.println( sanitize(visible));\n      out.println(\"<br>Hidden Parameter:\"\
      );\n      out.println( sanitize(hidden));          // Hidden variable sanitized\n\
      \    } else {\n      out.println(\"<p>\");\n      out.print(\"<form action=\\\
      \"\");\n      out.print(\"SampleServlet\\\" \");\n      out.println(\"method=POST>\"\
      );\n      out.println(\"Parameter:\");\n      out.println(\"<input type=text\
      \ size=20 name=visible>\");\n      out.println(\"<br>\");\n\n      out.println(\"\
      <input type=hidden name=hidden value=\\'a benign value\\'>\");\n      out.println(\"\
      <input type=submit>\");\n      out.println(\"</form>\");\n    }\n  }\n\n  public\
      \ void doPost(HttpServletRequest request, HttpServletResponse response)\n  \
      \  throws IOException, ServletException {\n    doGet(request, response);\n \
      \ }\n\n  // Filter the specified message string for characters\n  // that are\
      \ sensitive in HTML.\n  public static String sanitize(String message) {\n  \
      \  // ...\n  }\n}\n```\n\nConsequently, when the malicious URL is entered into\
      \ a browser, the servlet produces the following:\n\n```\nVisible Parameter:\
      \ dummy\nHidden Parameter: <font color=red>Surprise</font>!!!\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 00. 输入验证和数据处理 (IDS)
      - type: DetailClassChin
        value: IDS14-J：不要信任隐藏字段的内容
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 00. Input Validation and Data Sanitization (IDS)
      - type: DetailClassChin
        value: IDS14-J:Do not trust the contents of hidden form fields
    Default: not
    Description: "HTML允许web表单中的字段可见或隐藏。隐藏字段向web服务器提供值，但不为用户提供修改其内容的机制。但是，也有一些技术，攻击者可以用来修改这些内容。使用GET表单获取参数的web\
      \ servlet也可以通过URL接受这些参数。URL允许用户在web请求中指定任何参数名称和值。因此，隐藏表单字段不应该被认为比可见的表单字段更值得信任。\
      \  \n**不规范的代码样例1**：\n```java\npublic class SampleServlet extends HttpServlet\
      \ {\n\n  public void doGet(HttpServletRequest request, HttpServletResponse response)\n\
      \    throws IOException, ServletException {\n    response.setContentType(\"\
      text/html\");\n    PrintWriter out = response.getWriter();\n    out.println(\"\
      <html>\");\n\n    String visible = request.getParameter(\"visible\");\n    String\
      \ hidden = request.getParameter(\"hidden\");\n\n    if (visible != null || hidden\
      \ != null) {\n      out.println(\"Visible Parameter:\");\n      out.println(\
      \ sanitize(visible));\n      out.println(\"<br>Hidden Parameter:\");\n     \
      \ out.println(hidden);\n    } else {\n      out.println(\"<p>\");\n      out.print(\"\
      <form action=\\\"\");\n      out.print(\"SampleServlet\\\" \");\n      out.println(\"\
      method=POST>\");\n      out.println(\"Parameter:\");\n      out.println(\"<input\
      \ type=text size=20 name=visible>\");\n      out.println(\"<br>\");\n\n    \
      \  out.println(\"<input type=hidden name=hidden value=\\'a benign value\\'>\"\
      );\n      out.println(\"<input type=submit>\");\n      out.println(\"</form>\"\
      );\n    }\n  }\n\n  public void doPost(HttpServletRequest request, HttpServletResponse\
      \ response)\n    throws IOException, ServletException {\n    doGet(request,\
      \ response);\n  }\n\n  // Filter the specified message string for characters\n\
      \  // that are sensitive in HTML.\n  public static String sanitize(String message)\
      \ {\n    // ...\n  }\n}\n```\n\n上面代码演示了一个servlet，它接受一个可见的字段和一个隐藏的字段，并将其返回给用户。在传递给浏览器之前，可见的参数是经过验证处理的，但是隐藏的字段没有验证。\
      \  \n当输入参数`param1`时，web页面将显示以下内容:  \n```\nVisible Parameter: param1\nHidden\
      \ Parameter: a benign value\n```\n \n但是，攻击者可以通过在URL中编码来为隐藏参数提供一个值，如下:  \n```java\n\
      http://localhost:8080/sample/SampleServlet?visible=dummy&hidden=%3Cfont%20color=red%3ESurprise%3C/font%3E!!!\n\
      ```\n当这个URL被提供给浏览器时，浏览器会显示:  \n当输入参数`param1`时，web页面将显示以下内容:  \n```\nVisible\
      \ Parameter: dummy\nHidden Parameter: Surprise!!!\n```"
    Description_En: "HTML allows fields in a web form to be visible or hidden. Hidden\
      \ fields supply values to a web server but do not provide the user with a mechanism\
      \ to modify their contents. However, there are techniques that attackers can\
      \ use to modify these contents anyway. A web servlet that uses a `GET` form\
      \ to obtain parameters can also accept these parameters through a URL. URLs\
      \ allow a user to specify any parameter names and values in the web request.\
      \ Consequently, hidden form fields should not be considered any more trustworthy\
      \ than visible form fields.\n\n**Noncompliant Code Example**\n\nThe following\
      \ noncompliant code example demonstrates a servlet that accepts a visible field\
      \ and a hidden field, and echoes them back to the user. The visible parameter\
      \ is sanitized before being passed to the browser, but the hidden field is not.\n\
      \n```java\npublic class SampleServlet extends HttpServlet {\n\n  public void\
      \ doGet(HttpServletRequest request, HttpServletResponse response)\n    throws\
      \ IOException, ServletException {\n    response.setContentType(\"text/html\"\
      );\n    PrintWriter out = response.getWriter();\n    out.println(\"<html>\"\
      );\n\n    String visible = request.getParameter(\"visible\");\n    String hidden\
      \ = request.getParameter(\"hidden\");\n\n    if (visible != null || hidden !=\
      \ null) {\n      out.println(\"Visible Parameter:\");\n      out.println( sanitize(visible));\n\
      \      out.println(\"<br>Hidden Parameter:\");\n      out.println(hidden);\n\
      \    } else {\n      out.println(\"<p>\");\n      out.print(\"<form action=\\\
      \"\");\n      out.print(\"SampleServlet\\\" \");\n      out.println(\"method=POST>\"\
      );\n      out.println(\"Parameter:\");\n      out.println(\"<input type=text\
      \ size=20 name=visible>\");\n      out.println(\"<br>\");\n\n      out.println(\"\
      <input type=hidden name=hidden value=\\'a benign value\\'>\");\n      out.println(\"\
      <input type=submit>\");\n      out.println(\"</form>\");\n    }\n  }\n\n  public\
      \ void doPost(HttpServletRequest request, HttpServletResponse response)\n  \
      \  throws IOException, ServletException {\n    doGet(request, response);\n \
      \ }\n\n  // Filter the specified message string for characters\n  // that are\
      \ sensitive in HTML.\n  public static String sanitize(String message) {\n  \
      \  // ...\n  }\n}\n```\n\nWhen fed the parameter `param1`, the web page displays\
      \ the following:\n\n```\nVisible Parameter: param1\nHidden Parameter: a benign\
      \ value\n```\n\nHowever, an attacker can easily supply a value to the hidden\
      \ parameter by encoding it in the URL as follows:\n\n```\nhttp://localhost:8080/sample/SampleServlet?visible=dummy&hidden=%3Cfont%20color=red%3ESurprise%3C/font%3E!!!\n\
      ```\n\nWhen this URL is provided to the browser, the browser displays:\n\n```\n\
      Visible Parameter: dummy\nHidden Parameter: Surprise!!!\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 642:External Control of Critical State Data
      - type: CWE
        value: 472:External Control of Assumed-Immutable Web Parameter
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 642
    Security: true
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140214'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n当输入参数`param1`时，web页面将显示以下内容: \n```\nVisible\
      \ Parameter: param1  \nHidden Parameter: a benign value \n``` \n但是，攻击者可以通过在URL中编码来为隐藏参数提供一个值，如下:\
      \  \n`http://localhost:8080/sample/SampleServlet?visible=dummy&hidden=%3Cfont%20color=red%3ESurprise%3C/font%3E!!!`\
      \  \n当这个URL被提供给浏览器时，浏览器会显示: \n``` \nVisible Parameter: dummy   \nHidden Parameter:\
      \ Surprise!!!  \n``` \n**示例2**： \n```java \npublic class SampleServlet extends\
      \ HttpServlet {\n    public void doGet(HttpServletRequest request, HttpServletResponse\
      \ response)\n            throws IOException, ServletException {\n        response.setContentType(\"\
      text/html\");\n        PrintWriter out = response.getWriter();\n        out.println(\"\
      <html>\");\n\n        String visible = request.getParameter(\"visible\");\n\
      \        String hidden = request.getParameter(\"hidden\");\n\n        if (visible\
      \ != null || hidden != null) {\n            out.println(\"Visible Parameter:\"\
      );\n            out.println(sanitize(visible));\n            out.println(\"\
      <br>Hidden Parameter:\");\n            out.println(sanitize(hidden)); // Hidden\
      \ variable sanitized\n        } else {\n            out.println(\"<p>\");\n\
      \            out.print(\"<form action=\\\"\");\n            out.print(\"SampleServlet\\\
      \" \");\n            out.println(\"method=POST>\");\n            out.println(\"\
      Parameter:\");\n            out.println(\"<input type=text size=20 name=visible>\"\
      );\n            out.println(\"<br>\");\n\n            out.println(\"<input type=hidden\
      \ name=hidden value=\\'a benign value\\'>\");\n            out.println(\"<input\
      \ type=submit>\");\n            out.println(\"</form>\");\n        }\n    }\n\
      \n    public void doPost(HttpServletRequest request, HttpServletResponse response)\n\
      \            throws IOException, ServletException {\n        doGet(request,\
      \ response);\n    }\n\n    // Filter the specified message string for characters\n\
      \    // that are sensitive in HTML.\n    public static String sanitize(String\
      \ message) {\n        // ...\n    }\n}\n```\n上面代码片段对隐藏字段进行净化，因此，当恶意URL进入浏览器时，servlet产生以下内容:\n\
      ```  \nVisible Parameter: dummy   \nHidden Parameter: <font color=red>Surprise</font>!!!\n\
      ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.1 数据清洗
      - type: DetailClassChin
        value: A.2.1.1.5 不要信任隐藏字段的内容
    Categories_En:
      Category: []
    Default: not
    Description: "规范说明：HTML允许web表单中的字段可见或隐藏。隐藏字段向web服务器提供值，但不应被用户修改其内容。但是，攻击者仍然可以通过特殊方式来修改隐藏字段。\
      \  \n对于不信任隐藏字段的内容的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\npublic class\
      \ SampleServlet extends HttpServlet {\n    public void doGet(HttpServletRequest\
      \ request, HttpServletResponse response)\n            throws IOException, ServletException\
      \ {\n        response.setContentType(\"text/html\");\n        PrintWriter out\
      \ = response.getWriter();\n        out.println(\"<html>\");\n        String\
      \ visible = request.getParameter(\"visible\");\n        String hidden = request.getParameter(\"\
      hidden\");\n        if (visible != null || hidden != null) {\n            out.println(\"\
      Visible Parameter:\");\n            out.println(sanitize(visible));\n      \
      \      out.println(\"<br>Hidden Parameter:\");\n            out.println(hidden);\n\
      \        } else {\n            out.println(\"<p>\");\n            out.print(\"\
      <form action=\\\"\");\n            out.print(\"SampleServlet\\\" \");\n    \
      \        out.println(\"method=POST>\");\n            out.println(\"Parameter:\"\
      );\n            out.println(\"<input type=text size=20 name=visible>\");\n \
      \           out.println(\"<br>\");\n            out.println(\"<input type=hidden\
      \ name=hidden value=\\'a benign value\\'>\");\n            out.println(\"<input\
      \ type=submit>\");\n            out.println(\"</form>\");\n        }\n    }\n\
      \n    public void doPost(HttpServletRequest request, HttpServletResponse response)\n\
      \            throws IOException, ServletException {\n        doGet(request,\
      \ response);\n    }\n\n    // Filter the specified message string for characters\n\
      \    // that are sensitive in HTML.\n    public static String sanitize(String\
      \ message) {\n        // ...\n    }\n}\n```\n上面代码演示了一个servlet，它接受一个可见的字段和一个隐藏的字段，并将其返回给用户。在传递给浏览器之前，可见的参数是经过验证处理的，但是隐藏的字段没有验证。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 642:External Control of Critical State Data
      - type: CWE
        value: 472:External Control of Assumed-Immutable Web Parameter
      - type: CERT
        value: IDS14-J:Do not trust the contents of hidden form fields
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 642
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170004'
    language: Java
  - Advice: "使用`Boolean.valueOf(boolean expression)`或者`Boolean.valueOf(String expression)`或者`Boolean.TRUE`、`Boolean.FALSE`代替`new\
      \ Boolean`。  \n  \n**例1**：下面代码示例中，使用了`Boolean.valueOf(boolean expression)`来创建对象,\
      \ \n```java\nBoolean isEqual = Boolean.valueOf(false);//与Boolean isEqual = false;相同\
      \  \n``` \n**例2**：下面代码示例中，使用了`Boolean.FALSE`直接赋值。\n```java\nBoolean isEqual\
      \ = Boolean.FALSE;\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要创建Boolean对象
    Categories_En:
      Category: []
    Default: true
    Description: "程序中采用`new Boolean(boolean expression)`或`new Boolean(String expression)`创建对象，该方法产生的额外对象将占用更多空间、降低性能。\
      \  \n  \n**例如**：下面代码示例中，使用了`new Boolean(boolean expression)`来创建对象。\n```java\n\
      Boolean isEqual = new Boolean(false);\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: SJ/T 11683-2017
        value: 6.6.20 不必要的对象创建
    Security: not
    Tag: performance,memory,bad_practice
    Type: std
    WhiteList: not
    id: 0202000010150419
    language: Java
  - Advice: "身份验证和访问控制是防止LDAP注入的最后一道防线，不应该禁用它们。  \n  \n**例如**：下面代码示例中将Context.SECURITY_AUTHENTICATION设为`\"\
      simple\"`，将启用LDAP身份验证。\n```java\nHashtable<String, String> env = new Hashtable<String,\
      \ String>();  \nenv.put(Context.INITIAL_CONTEXT_FACTORY, \"com.sun.jndi.ldap.LdapCtxFactory\"\
      );  \nenv.put(Context.PROVIDER_URL, \"ldap://localhost:389/o=JNDITutorial\"\
      );  \nenv.put(Context.SECURITY_AUTHENTICATION, \"simple\");  \nenv.put(Context.SECURITY_PRINCIPAL,\
      \ \"cn=S. User, ou=NewHires, o=JNDITutorial\");  \nenv.put(Context.SECURITY_CREDENTIALS,\
      \ getLDAPPassword());  \nDirContext ctx = new InitialDirContext(env);\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不应该禁用LDAP身份验证
    Categories_En:
      Category: []
    Default: true
    Description: "未经身份验证的LDAP连接可能因为没有访问控制导致LDAP注入。  \n  \n**例如**：下面代码示例中将Context.SECURITY_AUTHENTICATION设为`\"\
      none\"`，将禁用LDAP身份验证。\n```java\nHashtable<String, String> env = new Hashtable<String,\
      \ String>();  \nenv.put(Context.INITIAL_CONTEXT_FACTORY, \"com.sun.jndi.ldap.LdapCtxFactory\"\
      );  \nenv.put(Context.PROVIDER_URL, \"ldap://localhost:389/o=JNDITutorial\"\
      );  \nenv.put(Context.SECURITY_AUTHENTICATION, \"none\");   \nDirContext ctx\
      \ = new InitialDirContext(env);\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: security
    Type: std
    WhiteList: not
    id: '0202000010150443'
    language: Java
  - Advice: "要序列化某一个对象，这个对象的类必须实现Serializable接口。  \n  \n**例如**：下面代码示例中User实现了Serializable接口，可以对User进行序列化。\
      \  \n```java\npublic class User implement Serializable{    \n    ...  \n}  \n\
      public class Test{  \n    public void doSerialize() throws IOException {  \n\
      \        User user;  \n        ...  \n        FileOutputStream fout = new FileOutputStream(user.getName());\
      \  \n        ObjectOutputStream oos = new ObjectOutputStream(fout);  \n    \
      \    oos.writeObject(veg);  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要序列化未实现Serializable接口的类
    Categories_En:
      Category: []
    Default: true
    Description: "序列化未实现Serializable接口的类时将导致抛出NotSerializableException异常，任何内容都不会写入文件，只有实现Serializable接口的类才能序列化和反序列化。\
      \  \n  \n**例如**：下面代码示例中User没有实现Serializable接口，但是仍将其序列化。  \n```java\npublic class\
      \ User{    \n    ...  \n}  \npublic class Test{  \n    public void doSerialize()\
      \ throws IOException {  \n        User user;  \n        ...  \n        FileOutputStream\
      \ fout = new FileOutputStream(user.getName());  \n        ObjectOutputStream\
      \ oos = new ObjectOutputStream(fout);  \n        oos.writeObject(veg);  \n \
      \   }  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: security,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150440'
    language: Java
  - Advice: "在循环语句结构中，需要保证线程安全可以使用StringBuffer代替String进行拼接，不需要时可以使用StringBuilder代替String进行拼接。\
      \  \n  \n**例如**：在下面代码片段中，在循环中使用StringBuilder拼接字符串。  \n```java\nStringBuilder\
      \ stringBuilder = new StringBuilder();  \nfor (int i < 0;i < 10;i++) {  \n \
      \   stringBuilder.append(i);  \n}  \nString string = stringBuilder.toString();\
      \  \n...\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要在循环中拼接字符串
    Categories_En:
      Category: []
    Default: true
    Description: "字符串对象是不可改变的，字符串对象的拼接和修改，最后都会创建一个新的字符串对象。而在循环中拼接字符串将会产生很多的对象，浪费系统运行时间和空间。\
      \  \n  \n**例如**：在下面代码片段中，在循环中拼接字符串。  \n```java\nString string = \"\"  \nfor\
      \ (int i < 0;i < 10;i++) {  \n    string = string + i;  \n}  \n...\n```  \n\
      循环中将产生10个StringBuilder对象，每次都会调用两次`append`方法分别将`string`和`i`的值追加至StringBuilder对象中，最后调用`toStirng`方法将StringBuilder对象内容转化为字符串并赋值给`string`变量。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: SJ/T 11683-2017
        value: 6.6.16 在循环体中使用"+"或"+="运算符拼接字符串
    Security: not
    Tag: performance,bad_practice
    Type: std
    WhiteList: not
    id: 0202000010150418
    language: Java
  - Advice: "**规范的代码样例1**：  \n```java\nstatic final int BUFFER = 512;\nstatic final\
      \ int TOOBIG = 0x6400000; // Max size of unzipped data, 100MB\nstatic final\
      \ int TOOMANY = 1024; // Max number of files\n// ...\n\nprivate String validateFilename(String\
      \ filename, String intendedDir) throws java.io.IOException {\n    File f = new\
      \ File(filename);\n    String canonicalPath = f.getCanonicalPath();\n\n    File\
      \ iD = new File(intendedDir);\n    String canonicalID = iD.getCanonicalPath();\n\
      \n    if (canonicalPath.startsWith(canonicalID)) {\n        return canonicalPath;\n\
      \    } else {\n        throw new IllegalStateException(\"File is outside extraction\
      \ target directory.\");\n    }\n}\n\npublic final void unzip(String filename)\
      \ throws java.io.IOException {\n    FileInputStream fis = new FileInputStream(filename);\n\
      \    ZipInputStream zis = new ZipInputStream(new BufferedInputStream(fis));\n\
      \    ZipEntry entry;\n    int entries = 0;\n    long total = 0;\n    try {\n\
      \        while ((entry = zis.getNextEntry()) != null) {\n            System.out.println(\"\
      Extracting: \" + entry);\n            int count;\n            byte data[] =\
      \ new byte[BUFFER];\n            // Write the files to the disk, but ensure\
      \ that the filename is valid,\n            // and that the file is not insanely\
      \ big\n            String name = validateFilename(entry.getName(), \".\");\n\
      \            if (entry.isDirectory()) {\n                System.out.println(\"\
      Creating directory \" + name);\n                new File(name).mkdir();\n  \
      \              continue;\n            }\n            FileOutputStream fos =\
      \ new FileOutputStream(name);\n            BufferedOutputStream dest = new BufferedOutputStream(fos,\
      \ BUFFER);\n            while (total + BUFFER <= TOOBIG && (count = zis.read(data,\
      \ 0, BUFFER)) != -1) {\n                dest.write(data, 0, count);\n      \
      \          total += count;\n            }\n            dest.flush();\n     \
      \       dest.close();\n            zis.closeEntry();\n            entries++;\n\
      \            if (entries > TOOMANY) {\n                throw new IllegalStateException(\"\
      Too many files to unzip.\");\n            }\n            if (total > TOOBIG)\
      \ {\n                throw new IllegalStateException(\"File being unzipped is\
      \ too big.\");\n            }\n        }\n    } finally {\n        zis.close();\n\
      \    }\n}\n```\n在这个解决方案方案中，代码在提取条目之前验证每个条目的名称。如果名称无效，那么整个提取就会被中止。 另外一个解决方案也可以跳过该条目并继续提取过程，或者甚至可以将条目提取到某个安全的位置。\n\
      此外，while循环中的代码将跟踪zip归档中每个条目的未压缩文件大小，同时提取条目。如果提取的条目太大，在本例中为100MB，则会抛出异常。代码不使用`ZipEntry.getSize()`方法,\
      \ 因为它返回的值是不可靠的。最后，代码还计算存档中文件条目的数量，如果有超过1024个条目，则抛出一个异常。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 00. 输入验证和数据处理 (IDS)
      - type: DetailClassChin
        value: IDS04-J：从ZipInputStream安全解压文件
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 00. Input Validation and Data Sanitization (IDS)
      - type: DetailClassChin
        value: IDS04-J:Safely extract files from ZipInputStream
    Default: true
    Description: "Java提供了 `java.util.zip` 包，可实现与zip兼容的数据压缩。它提供的类使您能够读取、创建和修改ZIP和GZIP文件格式。\n\
      在使用`java.util.zip.ZipInputStream`从ZIP文件提取文件条目时，必须考虑一些安全问题，文件名可能包含路径遍历信息，这些信息可能导致它们被提取到目标目录之外，通常是为了覆盖现有系统文件。*目录遍历*或路径等价漏洞可以通过按照FIO16-J规范化路径名来消除。在验证路径名之前规范化路径名，然后在提取之前验证位置。\n\
      第二个问题是，提取过程可能会导致系统资源的过度消耗，当资源使用量与输入数据相比不成比例时，可能会导致拒绝服务攻击。zip算法可以产生非常大的压缩比[Mahmoud\
      \ 2002]。例如，由a字符和b字符交替组成的文件可以实现200比1以上的压缩比。使用针对压缩算法的输入数据可以获得更高的压缩比。这允许存在压缩炸弹，其中一个小的zip或GZIP文件在解压缩时会消耗过多的资源。zip炸弹的一个例子是：文件42.zip是一种zip文件，由42千字节的压缩数据组成，包含五层嵌套的zip文件，每组16个，每个底层存档包含一个4.3千兆字节（429497295字节；~3.99Gib）的文件，总容量为4.5千兆字节（450359966321920字节；~3.99Pib）的未压缩数据。Zip炸弹通常依靠重复相同的文件来达到极高的压缩比。程序必须限制此类文件的遍历，或者拒绝提取超出一定限制的数据。实际限制取决于平台的功能和预期使用情况。\n\
      **不规范的代码样例1**：\n此不合规代码无法验证正在解压缩的文件的名称。它将名称直接传递给 `FileOutputStream` 的构造方法。它也无法检查正在解压缩的文件的资源消耗。它允许操作运行到完成或直到本地资源耗尽。\n\
      ```java\nstatic final int BUFFER = 512;\n// ...\n\npublic final void unzip(String\
      \ filename) throws java.io.IOException{\n  FileInputStream fis = new FileInputStream(filename);\n\
      \  ZipInputStream zis = new ZipInputStream(new BufferedInputStream(fis));\n\
      \  ZipEntry entry;\n  try {\n    while ((entry = zis.getNextEntry()) != null)\
      \ {\n      System.out.println(\"Extracting: \" + entry);\n      int count;\n\
      \      byte data[] = new byte[BUFFER];\n      // Write the files to the disk\n\
      \      FileOutputStream fos = new FileOutputStream(entry.getName());\n     \
      \ BufferedOutputStream dest = new BufferedOutputStream(fos, BUFFER);\n     \
      \ while ((count = zis.read(data, 0, BUFFER)) != -1) {\n        dest.write(data,\
      \ 0, count);\n      }\n      dest.flush();\n      dest.close();\n      zis.closeEntry();\n\
      \    }\n  } finally {\n    zis.close();\n  }\n}\n```\n\n**不规范的代码样例2(getSize())**：\n\
      这个不符合规则的代码示例，使用`ZipEntry.getSize()`方法在解压之前，判断zip文档中需解压文件的大小。然而，攻击者可以伪造ZIP文档中未压缩的文件的大小，因此通过getSize()返回的值是不可靠的。\n\
      ```java\nstatic final int BUFFER = 512;\nstatic final int TOOBIG = 0x6400000;\
      \ // 100MB\n// ...\n\npublic final void unzip(String filename) throws java.io.IOException{\n\
      \  FileInputStream fis = new FileInputStream(filename);\n  ZipInputStream zis\
      \ = new ZipInputStream(new BufferedInputStream(fis));\n  ZipEntry entry;\n \
      \ try {\n    while ((entry = zis.getNextEntry()) != null) {\n      System.out.println(\"\
      Extracting: \" + entry);\n      int count;\n      byte data[] = new byte[BUFFER];\n\
      \      // Write the files to the disk, but only if the file is not insanely\
      \ big\n      if (entry.getSize() > TOOBIG ) {\n         throw new IllegalStateException(\"\
      File to be unzipped is huge.\");\n      }\n      if (entry.getSize() == -1)\
      \ {\n         throw new IllegalStateException(\"File to be unzipped might be\
      \ huge.\");\n      }\n      FileOutputStream fos = new FileOutputStream(entry.getName());\n\
      \      BufferedOutputStream dest = new BufferedOutputStream(fos, BUFFER);\n\
      \      while ((count = zis.read(data, 0, BUFFER)) != -1) {\n        dest.write(data,\
      \ 0, count);\n      }\n      dest.flush();\n      dest.close();\n      zis.closeEntry();\n\
      \    }\n  } finally {\n    zis.close();\n  }\n}\n```"
    Description_En: "Java provides the `java.util.zip` package for zip-compatible\
      \ data compression. It provides classes that enable you to read, create, and\
      \ modify ZIP and GZIP file formats.\n\nA number of security concerns must be\
      \ considered when extracting file entries from a ZIP file using `java.util.zip.ZipInputStream`.\
      \ File names may contain path traversal information that may cause them to be\
      \ extracted outside of the intended directory, frequently with the purpose of\
      \ overwriting existing system files.Directory traversal or path equivalence\
      \ vulnerabilities can be eliminated by canonicalizing the path name, in accordance\
      \ with FIO16-J. Canonicalize path names before validating them, and then validating\
      \ the location before extraction.\n\nA second issue is that the extraction process\
      \ can cause excessive consumption of system resources, possibly resulting in\
      \ a denial-of-service attack when resource usage is disproportionately large\
      \ compared to the input data. The zip algorithm can produce very large compression\
      \ ratios [Mahmoud 2002]. For example, a file consisting of alternating lines\
      \ of a characters and b characters can achieve a compression ratio of more than\
      \ 200 to 1. Even higher compression ratios can be obtained using input data\
      \ that is targeted to the compression algorithm. This permits the existence\
      \ of zip bombs in which a small ZIP or GZIP file consumes excessive resources\
      \ when uncompressed. An example of a zip bomb is the file 42.zip, which is a\
      \ zip file consisting of 42 kilobytes of compressed data, containing five layers\
      \ of nested zip files in sets of 16, each bottom layer archive containing a\
      \ 4.3 gigabyte (4 294 967 295 bytes; ~ 3.99 GiB) file for a total of 4.5 petabytes\
      \ (4 503 599 626 321 920 bytes; ~ 3.99 PiB) of uncompressed data. Zip bombs\
      \ often rely on repetition of identical files to achieve their extreme compression\
      \ ratios. Programs must either limit the traversal of such files or refuse to\
      \ extract data beyond a certain limit. The actual limit depends on the capabilities\
      \ of the platform and expected usage.\n\n**Noncompliant Code Example**\n\nThis\
      \ noncompliant code fails to validate the name of the file that is being unzipped.\
      \ It passes the name directly to the constructor of `FileOutputStream`. It also\
      \ fails to check the resource consumption of the file that is being unzipped.\
      \ It permits the operation to run to completion or until local resources are\
      \ exhausted.\n\n```java\nstatic final int BUFFER = 512;\n// ...\n\npublic final\
      \ void unzip(String filename) throws java.io.IOException{\n  FileInputStream\
      \ fis = new FileInputStream(filename);\n  ZipInputStream zis = new ZipInputStream(new\
      \ BufferedInputStream(fis));\n  ZipEntry entry;\n  try {\n    while ((entry\
      \ = zis.getNextEntry()) != null) {\n      System.out.println(\"Extracting: \"\
      \ + entry);\n      int count;\n      byte data[] = new byte[BUFFER];\n     \
      \ // Write the files to the disk\n      FileOutputStream fos = new FileOutputStream(entry.getName());\n\
      \      BufferedOutputStream dest = new BufferedOutputStream(fos, BUFFER);\n\
      \      while ((count = zis.read(data, 0, BUFFER)) != -1) {\n        dest.write(data,\
      \ 0, count);\n      }\n      dest.flush();\n      dest.close();\n      zis.closeEntry();\n\
      \    }\n  } finally {\n    zis.close();\n  }\n}\n```\n\n**Noncompliant Code\
      \ Example (`getSize()`)**\n\nThis noncompliant code attempts to overcome the\
      \ problem by calling the method `ZipEntry.getSize()` to check the uncompressed\
      \ file size before uncompressing it. Unfortunately, a malicious attacker can\
      \ forge the field in the ZIP file that purports to show the uncompressed size\
      \ of the file, so the value returned by `getSize()` is unreliable, and local\
      \ resources may still be exhausted.\n\n```java\nstatic final int BUFFER = 512;\n\
      static final int TOOBIG = 0x6400000; // 100MB\n// ...\n\npublic final void unzip(String\
      \ filename) throws java.io.IOException{\n  FileInputStream fis = new FileInputStream(filename);\n\
      \  ZipInputStream zis = new ZipInputStream(new BufferedInputStream(fis));\n\
      \  ZipEntry entry;\n  try {\n    while ((entry = zis.getNextEntry()) != null)\
      \ {\n      System.out.println(\"Extracting: \" + entry);\n      int count;\n\
      \      byte data[] = new byte[BUFFER];\n      // Write the files to the disk,\
      \ but only if the file is not insanely big\n      if (entry.getSize() > TOOBIG\
      \ ) {\n         throw new IllegalStateException(\"File to be unzipped is huge.\"\
      );\n      }\n      if (entry.getSize() == -1) {\n         throw new IllegalStateException(\"\
      File to be unzipped might be huge.\");\n      }\n      FileOutputStream fos\
      \ = new FileOutputStream(entry.getName());\n      BufferedOutputStream dest\
      \ = new BufferedOutputStream(fos, BUFFER);\n      while ((count = zis.read(data,\
      \ 0, BUFFER)) != -1) {\n        dest.write(data, 0, count);\n      }\n     \
      \ dest.flush();\n      dest.close();\n      zis.closeEntry();\n    }\n  } finally\
      \ {\n    zis.close();\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 73:External Control of File Name or Path
      - type: CWE
        value: 23:Relative Path Traversal
      - type: CWE
        value: 22:Improper Limitation of a Pathname to a Restricted Directory ('Path
          Traversal')
      - type: OWASP TOP 10
        value: 2017:A5 Broken Access Control
      - type: OWASP TOP 10
        value: 2013:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2010:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2007:A4 Insecure Direct Object Reference
      - type: OWASP TOP 10
        value: 2004:A1 Unvalidated Input
      - type: OWASP MOBILE TOP 10
        value: 2014:M8 Security Decisions Via Untrusted Inputs
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2011:Risky Resource Management - CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2010:Risky Resource Management - CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 73
    Security: true
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140204'
    language: Java
  - Advice: "在这个符合规则的方案中，while 循环中的代码会通过使用`ZipEntry.getSize()`方法在解压之前，得到zip文档中需解压文件的大小。如果该解压文件过大，比如说超过100MB，那么抛出异常。\
      \   \n    ```java\n    static final int TOOBIG = 0x6400000; // 100MB\n    //\
      \ ...\n    // write the files to the disk, but only if file is not insanely\
      \ big\n    if (entry.getSize() > TOOBIG) {\n        throw new IllegalStateException(\"\
      File to be unzipped is huge.\");\n    }\n    if (entry.getSize() == -1) {\n\
      \        throw new IllegalStateException(\"File to be unzipped might be huge.\"\
      );\n    }\n    \n    dest = new BufferedOutputStream(fos, BUFFER);\n    while\
      \ ((count = zis.read(data, 0, BUFFER)) != -1) {\n        dest.write(data, 0,\
      \ count);\n    }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 5.2 数据净化
      - type: DetailClassChin
        value: C.1.5 限制传递给ZipInputStream 的文件大小
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n应检查`java.util.ZipInputStream`的输入，以防止消耗过多的系统资源。   \n2)\
      \ 原理   \n当资源使用大大多于输入数据所使用的资源的时候，就会出现拒绝服务问题。当需要解压一个小文件，比如zip、gif 或者gzip编码的HTTP内容，会消耗过多的资源时，并且在压缩率极高的情况下，zip算法的实现本身就会导致zip炸弹（zip\
      \ bomb）的出现。   \nzip 算法能产生很高的压缩比率。在zip文件中的任何数据，如果其解压后的文件大小超出了一个特定的限制，那么就不应该将它们解压。而这个限制实际上取决于平台的能力。\
      \   \n3) 示例   \n下面是一个不符合规则的代码示例，它不能检查解压一个文件时所消耗的资源。它会允许操作完成，或者直至本地资源耗尽为止。  \
      \ \n    ```java\n    static final int BUFFER = 512;\n    // ...\n    // external\
      \ data source: filename\n    BufferedOutputStream dest = null;\n    FileInputStream\
      \ fis = new FileInputStream(filename);\n    ZipInputStream zis = new ZipInputStream(new\
      \ BufferedInputStream(fis));\n    ZipEntry entry;\n    while ((entry = zis.getNextEntry())\
      \ != null) {\n        System.out.println(\"Extracting: \" + entry);\n      \
      \  int count;\n        byte data[] = new byte[BUFFER];\n        // write the\
      \ files to the disk\n        FileOutputStream fos = new FileOutputStream(entry.getName());\n\
      \        dest = new BufferedOutputStream(fos, BUFFER);\n        while ((count\
      \ = zis.read(data, 0, BUFFER)) != -1) {\n            dest.write(data, 0, count);\n\
      \        }\n        dest.flush();\n        dest.close();\n    }\n    zis.close();\n\
      \    ```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 73:External Control of File Name or Path
      - type: CWE
        value: 23:Relative Path Traversal
      - type: CWE
        value: 22:Improper Limitation of a Pathname to a Restricted Directory ('Path
          Traversal')
      - type: CERT
        value: IDS04-J:Safely extract files from ZipInputStream
      - type: OWASP TOP 10
        value: 2017:A5 Broken Access Control
      - type: OWASP TOP 10
        value: 2013:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2010:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2007:A4 Insecure Direct Object Reference
      - type: OWASP TOP 10
        value: 2004:A1 Unvalidated Input
      - type: OWASP MOBILE TOP 10
        value: 2014:M8 Security Decisions Via Untrusted Inputs
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2011:Risky Resource Management - CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2010:Risky Resource Management - CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 73
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160005
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。 \n**示例2**：  \n```java\nstatic final int BUFFER\
      \ = 512;\nstatic final int TOOBIG = 0x6400000; // Max size of unzipped data,\
      \ 100MB\nstatic final int TOOMANY = 1024; // Max number of files\n// ...\n\n\
      private String validateFilename(String filename, String intendedDir) throws\
      \ java.io.IOException {\n    File f = new File(filename);\n    String canonicalPath\
      \ = f.getCanonicalPath();\n\n    File iD = new File(intendedDir);\n    String\
      \ canonicalID = iD.getCanonicalPath();\n\n    if (canonicalPath.startsWith(canonicalID))\
      \ {\n        return canonicalPath;\n    } else {\n        throw new IllegalStateException(\"\
      File is outside extraction target directory.\");\n    }\n}\n\npublic final void\
      \ unzip(String filename) throws java.io.IOException {\n    FileInputStream fis\
      \ = new FileInputStream(filename);\n    ZipInputStream zis = new ZipInputStream(new\
      \ BufferedInputStream(fis));\n    ZipEntry entry;\n    int entries = 0;\n  \
      \  long total = 0;\n    try {\n        while ((entry = zis.getNextEntry()) !=\
      \ null) {\n            System.out.println(\"Extracting: \" + entry);\n     \
      \       int count;\n            byte data[] = new byte[BUFFER];\n          \
      \  // Write the files to the disk, but ensure that the filename is valid,\n\
      \            // and that the file is not insanely big\n            String name\
      \ = validateFilename(entry.getName(), \".\");\n            if (entry.isDirectory())\
      \ {\n                System.out.println(\"Creating directory \" + name);\n \
      \               new File(name).mkdir();\n                continue;\n       \
      \     }\n            FileOutputStream fos = new FileOutputStream(name);\n  \
      \          BufferedOutputStream dest = new BufferedOutputStream(fos, BUFFER);\n\
      \            while (total + BUFFER <= TOOBIG && (count = zis.read(data, 0, BUFFER))\
      \ != -1) {\n                dest.write(data, 0, count);\n                total\
      \ += count;\n            }\n            dest.flush();\n            dest.close();\n\
      \            zis.closeEntry();\n            entries++;\n            if (entries\
      \ > TOOMANY) {\n                throw new IllegalStateException(\"Too many files\
      \ to unzip.\");\n            }\n            if (total > TOOBIG) {\n        \
      \        throw new IllegalStateException(\"File being unzipped is too big.\"\
      );\n            }\n        }\n    } finally {\n        zis.close();\n    }\n\
      }\n``` \n在这个规范的代码示例中，代码在提取条目之前验证每个条目的名称。如果名称无效，那么整个提取就会被中止。while循环中的代码将判断zip归档中每个条目的文件大小，同时提取条目。如果提取的条目太大，在本例中为100MB，则会抛出异常。代码不要使用`ZipEntry.getSize()`方法,\
      \ 因为攻击者可以伪造ZIP文档中未压缩的文件的大小。最后，代码还计算压缩包中文件条目的数量，如果超过1024个条目，则抛出异常。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.1 数据清洗
      - type: DetailClassChin
        value: A.2.1.1.3 从ZipInputStream安全解压文件
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：对`java.util.ZipInputStream`的输入进行检查可以防止消耗过多的系统资源。解压一个文件，比如zip、gif或者gzip编码的HTTP内容，可能会消耗过多的资源，并且在压缩率极高的情况下，可能会导致zip炸弹的出现。\
      \  \n对于从ZipInputStream安全解压文件的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\n\
      static final int BUFFER = 512;\n// ...\n\npublic final void unzip(String filename)\
      \ throws java.io.IOException {\n    FileInputStream fis = new FileInputStream(filename);\n\
      \    ZipInputStream zis = new ZipInputStream(new BufferedInputStream(fis));\n\
      \    ZipEntry entry;\n    try {\n        while ((entry = zis.getNextEntry())\
      \ != null) {\n            System.out.println(\"Extracting: \" + entry);\n  \
      \          int count;\n            byte data[] = new byte[BUFFER];\n       \
      \     // Write the files to the disk\n            FileOutputStream fos = new\
      \ FileOutputStream(entry.getName());\n            BufferedOutputStream dest\
      \ = new BufferedOutputStream(fos, BUFFER);\n            while ((count = zis.read(data,\
      \ 0, BUFFER)) != -1) {\n                dest.write(data, 0, count);\n      \
      \      }\n            dest.flush();\n            dest.close();\n           \
      \ zis.closeEntry();\n        }\n    } finally {\n        zis.close();\n    }\n\
      }\n```  \n这个不规范的代码示例没有检查解压一个文件时所消耗的资源。它会允许操作持续进行直至完成，或者直至本地资源耗尽为止。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 73:External Control of File Name or Path
      - type: CWE
        value: 23:Relative Path Traversal
      - type: CWE
        value: 22:Improper Limitation of a Pathname to a Restricted Directory ('Path
          Traversal')
      - type: CERT
        value: IDS04-J:Safely extract files from ZipInputStream
      - type: OWASP TOP 10
        value: 2017:A5 Broken Access Control
      - type: OWASP TOP 10
        value: 2013:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2010:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2007:A4 Insecure Direct Object Reference
      - type: OWASP TOP 10
        value: 2004:A1 Unvalidated Input
      - type: OWASP MOBILE TOP 10
        value: 2014:M8 Security Decisions Via Untrusted Inputs
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2011:Risky Resource Management - CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2010:Risky Resource Management - CWE ID 22
      - type: CWE/SANS TOP 25
        value: 2009:Risky Resource Management - CWE ID 73
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170002'
    language: Java
  - Advice: "**规范的代码样例1**：\n以下示例能保证用户产生的输入会被排除在格式字符串之外。\n```java\nclass Format {\n\
      \  static Calendar c =\n    new GregorianCalendar(1995, GregorianCalendar.MAY,\
      \ 23);\n  public static void main(String[] args) {\n    // args[0] is the credit\
      \ card expiration date\n    // Perform comparison with c,\n    // if it doesn't\
      \ match, print the following line\n    System.out.format(\n      \"%s did not\
      \ match! HINT: It was issued on %terd of some month\",\n      args[0], c\n \
      \   );\n  }\n}\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution excludes untrusted\
      \ user input from the format string. Although `arg[0]` still may contain one\
      \ or more format specifiers, they are now rendered inert.\n\n```java\nclass\
      \ Format {\n  static Calendar c =\n    new GregorianCalendar(1995, GregorianCalendar.MAY,\
      \ 23);\n  public static void main(String[] args) {\n    // args[0] is the credit\
      \ card expiration date\n    // Perform comparison with c,\n    // if it doesn't\
      \ match, print the following line\n    System.out.format(\n      \"%s did not\
      \ match! HINT: It was issued on %terd of some month\",\n      args[0], c\n \
      \   );\n  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 00. 输入验证和数据处理 (IDS)
      - type: DetailClassChin
        value: IDS06-J：从格式化字符串中排除用户输入
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 00. Input Validation and Data Sanitization (IDS)
      - type: DetailClassChin
        value: IDS06-J:Exclude unsanitized user input from format strings
    Default: true
    Description: "java.io包中的PrintStream类包含两个等效的格式化方法：`format()`和`printf()`。`System.out`和`System.err`作为PrintStream对象，允许在标准输出和错误流上调用PrintStream方法。当任何转换参数无法匹配相应的格式符时，标准类库的实现将抛出一个相应的异常。虽然抛出异常能降低被恶意利用的可能，但如果将不受信任的数据包含到格式字符串中，则可能会造成信息泄露或拒绝服务。因此，不能在格式字符串中使用非受信来源的字符串。\
      \  \n**不规范的代码样例1**：  \n以下代码在格式字符串中包含不受信任的数据，可能会出现信息泄露问题。  \n```java\nclass Format\
      \ {\n  static Calendar c = new GregorianCalendar(1995, GregorianCalendar.MAY,\
      \ 23);\n  public static void main(String[] args) {\n    // args[0] should contain\
      \ the credit card expiration date\n    // but might contain %1$tm, %1$te or\
      \ %1$tY format specifiers\n    System.out.format(\n      args[0] + \" did not\
      \ match! HINT: It was issued on %1$terd of some month\", c\n    );\n  }\n}\n\
      ```\n在没有适当的输入验证的情况下，攻击者可以通过提供包含`%1$tm`, `%1$te`,或者`%1$tY`格式符的输入字符串来确定输入被验证的日期。在此**例**中：这些格式符分别打印05（5月），23（日）和1995（年）。"
    Description_En: "The java.io package includes a `PrintStream` class that has two\
      \ equivalent formatting methods: `format()` and `printf()`. `System.out` and\
      \ `System.err` are `PrintStream` objects, allowing `PrintStream` methods to\
      \ be invoked on the standard output and error streams. The risks from using\
      \ these methods are not as high as from using similar functions in C or C++[Seacord\
      \ 2013].The standard library implementations throw an exception when any conversion\
      \ argument fails to match the corresponding format specifier. Although throwing\
      \ an exception helps mitigate against exploits, if untrusted data is incorporated\
      \ into a format string, it can result in an information leak or allow a denial-of-service\
      \ attack. Consequently, unsanitized input from an untrusted source must never\
      \ be incorporated into format strings.\n\n**Noncompliant Code Example**\n\n\
      This noncompliant code example leaks information about a user's credit card.\
      \ It incorporates untrusted data in a format string.\n\n```java\nclass Format\
      \ {\n  static Calendar c = new GregorianCalendar(1995, GregorianCalendar.MAY,\
      \ 23);\n  public static void main(String[] args) {\n    // args[0] should contain\
      \ the credit card expiration date\n    // but might contain %1$tm, %1$te or\
      \ %1$tY format specifiers\n    System.out.format(\n      args[0] + \" did not\
      \ match! HINT: It was issued on %1$terd of some month\", c\n    );\n  }\n}\n\
      ```\n\nIn the absence of proper input validation, an attacker can determine\
      \ the date against which the input is verified by supplying an input string\
      \ that includes the `%1$tm`, `%1$te`, or `%1$tY` format specifiers. In this\
      \ example, these format specifiers print 05 (May), 23 (day), and 1995 (year),\
      \ respectively."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 730:OWASP Top Ten 2004 Category A9 - Denial of Service
      - type: CWE
        value: 134:Use of Externally-Controlled Format String
      - type: OWASP TOP 10
        value: 2017:A9 Using Components with Known Vulnerabilities
      - type: OWASP TOP 10
        value: 2013:A9 Using Components with Known Vulnerabilities
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: OWASP MOBILE TOP 10
        value: 2014:M1 Weak Server Side Controls
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: cert
    WhiteList: true
    id: '0102000010140206'
    language: Java
  - Advice: "该对应的符合规则的方案能够保证用户产生的输入会被排除在格式字符串之外。   \n    ```java\n    class Format\
      \ {\n        static Calendar c = new GregorianCalendar(1995, GregorianCalendar.MAY,\
      \ 23);\n    \n        public static void main(String[] args) {\n           \
      \ // args[0] is the credit card expiration date\n            // Perform comparison\
      \ with c,\n            // if it doesn't match print the following line\n   \
      \         System.out.printf(\"%s did not match! \" + \" HINT: It was issued\
      \ on %1$terd of some month\", args[0], c);\n        }\n    }\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 5.2 数据净化
      - type: DetailClassChin
        value: C.1.7 从格式字符串中排除用户输入
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n不能在格式字符串中使用非受信来源的字符串。   \n2) 原理   \n对Java格式字符串的解释要比对在像C语言这样的语言中更严格。当任何转换参数不能匹配相应的格式符时，标准类库实现会抛出一个相应的异常。这种方法降低了被恶意利用的可能性。然而，恶意用户输入可以利用格式字符串，并且造成信息泄露或者拒绝服务。因此，不能在格式字符串中使用非受信来源的字符串。\
      \   \n3) 示例   \n    这个不符合规则的代码示例展示了可能出现信息泄露的问题。它将信用卡的失效日期作为输入参数并将其用在格式字符串中。\
      \   \n    ```java\n    class Format {\n        static Calendar c = new GregorianCalendar(1995,\
      \ GregorianCalendar.MAY, 23);\n    \n        public static void main(String[]\
      \ args) {\n            // args[0] is the credit card expiration date\n     \
      \       // args[0] can contain either %1$tm, %1$te or %1$tY as malicious\n \
      \           // arguments\n            // First argument prints 05 (May), second\
      \ prints 23 (day)\n            // and third prints 1995 (year)\n           \
      \ // Perform comparison with c, if it doesn’t match print the\n            //\
      \ following line\n            System.out.printf(args[0] + \" did not match!\
      \ HINT: It was issued on %1$terd of some month\", c);\n        }\n    }\n  \
      \  ```   \n    如果没有经过正确输入验证，攻击者通过在输入语句中包含以下参数之一：`%1$tm`、 `%1$te`或 `%1$tY`，就可以判断验证中所用来和输入相对比的日期。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 730:OWASP Top Ten 2004 Category A9 - Denial of Service
      - type: CWE
        value: 134:Use of Externally-Controlled Format String
      - type: CERT
        value: IDS06-J:Exclude unsanitized user input from format strings
      - type: OWASP TOP 10
        value: 2017:A9 Using Components with Known Vulnerabilities
      - type: OWASP TOP 10
        value: 2013:A9 Using Components with Known Vulnerabilities
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: OWASP MOBILE TOP 10
        value: 2014:M1 Weak Server Side Controls
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: true
    id: 0902000010160007
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：      \n```java\nclass Format {\n  \
      \  static Calendar c = new GregorianCalendar(1995, GregorianCalendar.MAY, 23);\n\
      \n    public static void main(String[] args) {\n        // args[0] is the credit\
      \ card expiration date\n        // Perform comparison with c,\n        // if\
      \ it doesn't match, print the following line\n        System.out.format(\"%s\
      \ did not match! HINT: It was issued on %terd of some month\", args[0], c);\n\
      \    }\n}\n```  \n以上示例能保证用户产生的输入会被排除在格式化字符串之外。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.3 函数调用安全
      - type: DetailClassChin
        value: A.3.3.2 从格式化字符串中排除用户输入
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：java.io包中的PrintStream类包含两个等效的格式化方法：`format()`和`printf()`。System.out和System.err作为PrintStream对象，允许在标准输出和错误流上调用PrintStream方法。当任何转换参数无法匹配相应的格式符时，标准类库的实现将抛出一个相应的异常。虽然抛出异常能降低被恶意利用的可能，但如果将不受信任的数据包含到格式字符串中，则可能会造成信息泄露或拒绝服务。因此，不能在格式字符串中使用非受信来源的字符串。\
      \  \n对于从格式化字符串中排除用户输入的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nclass\
      \ Format {\n    static Calendar c = new GregorianCalendar(1995, GregorianCalendar.MAY,\
      \ 23);\n\n    public static void main(String[] args) {\n        // args[0] should\
      \ contain the credit card expiration date\n        // but might contain %1$tm,\
      \ %1$te or %1$tY format specifiers\n        System.out.format(args[0] + \" did\
      \ not match! HINT: It was issued on %1$terd of some month\", c);\n    }\n}\n\
      ```  \n以上代码在格式字符串中包含不受信任的数据，可能会出现信息泄露问题。  \n在没有适当的输入验证的情况下，攻击者可以通过提供包含`%1$tm`,\
      \ `%1$te`,或者`%1$tY`格式符的输入字符串来确定输入被验证的日期。在此例中，这些格式符分别打印05（5月），23（日）和1995（年）。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 730:OWASP Top Ten 2004 Category A9 - Denial of Service
      - type: CWE
        value: 134:Use of Externally-Controlled Format String
      - type: CERT
        value: IDS06-J:Exclude unsanitized user input from format strings
      - type: OWASP TOP 10
        value: 2017:A9 Using Components with Known Vulnerabilities
      - type: OWASP TOP 10
        value: 2013:A9 Using Components with Known Vulnerabilities
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: OWASP MOBILE TOP 10
        value: 2014:M1 Weak Server Side Controls
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170052'
    language: Java
  - Advice: "**规范的代码样例1**\n这个解决方案使用`SSLSocket`来保护使用SSL/TLS安全协议的数据包。\n```java\n// Exception\
      \ handling has been omitted for the sake of brevity\nclass EchoServer {\n  public\
      \ static void main(String[] args) throws IOException {\n    SSLServerSocket\
      \ sslServerSocket = null;\n    try {\n      SSLServerSocketFactory sslServerSocketFactory\
      \ =\n          (SSLServerSocketFactory) SSLServerSocketFactory.getDefault();\n\
      \      sslServerSocket = (SSLServerSocket) sslServerSocketFactory.\n       \
      \                 createServerSocket(9999);\n      SSLSocket sslSocket = (SSLSocket)\
      \ sslServerSocket.accept();\n      PrintWriter out = new PrintWriter(sslSocket.getOutputStream(),true);\n\
      \      BufferedReader in = new BufferedReader(\n          new InputStreamReader(sslSocket.getInputStream()));\n\
      \      String inputLine;\n      while ((inputLine = in.readLine()) != null)\
      \ {\n        System.out.println(inputLine);\n        out.println(inputLine);\n\
      \      }\n    } finally {\n      if (sslServerSocket != null) {\n        try\
      \ {\n          sslServerSocket.close();\n        } catch (IOException x) {\n\
      \          // Handle error\n        }\n      }\n    }\n  }\n}\n\nclass EchoClient\
      \ {\n  public static void main(String[] args) throws IOException {\n    SSLSocket\
      \ sslSocket = null;\n    try {\n      SSLSocketFactory sslSocketFactory =\n\
      \          (SSLSocketFactory) SSLSocketFactory.getDefault();\n      sslSocket\
      \ =\n          (SSLSocket) sslSocketFactory.createSocket(\"localhost\", 9999);\n\
      \      PrintWriter out = new PrintWriter(sslSocket.getOutputStream(), true);\n\
      \      BufferedReader in = new BufferedReader(\n          new InputStreamReader(sslSocket.getInputStream()));\n\
      \      BufferedReader stdIn = new BufferedReader(\n          new InputStreamReader(System.in));\n\
      \      String userInput;\n      while ((userInput = stdIn.readLine()) != null)\
      \ {\n        out.println(userInput);\n        System.out.println(in.readLine());\n\
      \      }\n    } finally {\n      if (sslSocket != null) {\n        try {\n \
      \         sslSocket.close();\n        } catch (IOException x) {\n          //\
      \ Handle error\n        }\n      }\n    }\n  }\n}\n```\n使用`SSLSocket`的程序如果试图连接到一个没有使用SSL的端口，将无限期地被阻止。同样地，不使用`SSLSocket`的程序在试图通过使用SSL的端口建立连接时也会被阻止。\n\
      注意`SSLSocket`不验证主机名，所以向`SSLSocket`提供一个任意的主机名仍然容易受到中间人的攻击。主机名应该被单独验证。`HttpsURLConnection`类可以验证主机名，是安全网络套接字的一个合适的解决方案。\n\
      **特例**\nMSC00-J-EX0: 由于`SSLSocket`提供了确保数据包安全传输的机制，可能会导致显著的性能开销。在以下情况下，普通的套接字是足够的。\n\
      \n* 通过套接字发送的数据不敏感。\n* 数据是敏感的，但经过适当的加密（见 SER02-J. 更多信息请参见SER02-J.Sign then seal\
      \ objects before sending them outside a trust boundary）。\n* 套接字的网络路径从未穿过信任边界。这可能发生在，例如，套接字的两个端点在同一个本地网络中，并且整个网络都是可信的。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution uses `SSLSocket`\
      \ to protect packets using the SSL/TLS security protocols:\n\n```java\n// Exception\
      \ handling has been omitted for the sake of brevity\nclass EchoServer {\n  public\
      \ static void main(String[] args) throws IOException {\n    SSLServerSocket\
      \ sslServerSocket = null;\n    try {\n      SSLServerSocketFactory sslServerSocketFactory\
      \ =\n          (SSLServerSocketFactory) SSLServerSocketFactory.getDefault();\n\
      \      sslServerSocket = (SSLServerSocket) sslServerSocketFactory.\n       \
      \                 createServerSocket(9999);\n      SSLSocket sslSocket = (SSLSocket)\
      \ sslServerSocket.accept();\n      PrintWriter out = new PrintWriter(sslSocket.getOutputStream(),true);\n\
      \      BufferedReader in = new BufferedReader(\n          new InputStreamReader(sslSocket.getInputStream()));\n\
      \      String inputLine;\n      while ((inputLine = in.readLine()) != null)\
      \ {\n        System.out.println(inputLine);\n        out.println(inputLine);\n\
      \      }\n    } finally {\n      if (sslServerSocket != null) {\n        try\
      \ {\n          sslServerSocket.close();\n        } catch (IOException x) {\n\
      \          // Handle error\n        }\n      }\n    }\n  }\n}\n\nclass EchoClient\
      \ {\n  public static void main(String[] args) throws IOException {\n    SSLSocket\
      \ sslSocket = null;\n    try {\n      SSLSocketFactory sslSocketFactory =\n\
      \          (SSLSocketFactory) SSLSocketFactory.getDefault();\n      sslSocket\
      \ =\n          (SSLSocket) sslSocketFactory.createSocket(\"localhost\", 9999);\n\
      \      PrintWriter out = new PrintWriter(sslSocket.getOutputStream(), true);\n\
      \      BufferedReader in = new BufferedReader(\n          new InputStreamReader(sslSocket.getInputStream()));\n\
      \      BufferedReader stdIn = new BufferedReader(\n          new InputStreamReader(System.in));\n\
      \      String userInput;\n      while ((userInput = stdIn.readLine()) != null)\
      \ {\n        out.println(userInput);\n        System.out.println(in.readLine());\n\
      \      }\n    } finally {\n      if (sslSocket != null) {\n        try {\n \
      \         sslSocket.close();\n        } catch (IOException x) {\n          //\
      \ Handle error\n        }\n      }\n    }\n  }\n}\n```\n\nPrograms that use\
      \ `SSLSocket` will block indefinitely if they attempt to connect to a port that\
      \ is not using SSL. Similarly, a program that does not use `SSLSocket` will\
      \ block when attempting to establish a connection through a port that does use\
      \ SSL.\n\nNote that `SSLSocket` does not validate host names, so providing an\
      \ arbitrary host name to an `SSLSocket` is still vulnerable to a man-in-the-middle\
      \ attack. Host names should be validated separately. The `HttpsURLConnection`\
      \ class validates host names and is a suitable solution for secure web sockets.\n\
      \n**Exceptions**\n\nMSC00-J-EX0: Because of the mechanisms that `SSLSocket`\
      \ provides to ensure the secure transfer of packets, significant performance\
      \ overhead may result. Regular sockets are sufficient under the following circumstances:\n\
      \n* The data being sent over the socket is not sensitive.\n* The data is sensitive\
      \ but properly encrypted (see SER02-J. Sign then seal objects before sending\
      \ them outside a trust boundary for more information).\n* The network path of\
      \ the socket never crosses a trust boundary. This could happen when, for example,\
      \ the two endpoints of the socket are within the same local network and the\
      \ entire network is trusted."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 49. 其他 (MSC)
      - type: DetailClassChin
        value: MSC00-J：使用SSLSocket而不是Socket进行安全数据交换
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 49. Miscellaneous (MSC)
      - type: DetailClassChin
        value: MSC00-J:Use SSLSocket rather than Socket for secure data exchange
    Default: true
    Description: "程序在不安全的通信通道上传输敏感数据时，必须使用`javax.net.ssl.SSLSocket`类而不是`java.net.Socket`类。类`SSLSocket`提供安全协议，如安全套接字层/传输层安全（SSL/TLS），以确保通道不容易被窃听和恶意篡改。\n\
      SSLSocket中包含的、Socket类所不提供的主要保护措施是[API 2014]。\n\n> * *完整性保护：* SSL防止主动窃听者对信息进行修改。\n\
      > 认证：在大多数模式下，SSL提供对等认证。服务器通常是经过认证的，而客户可以根据服务器的要求进行认证。\n> 保密性（隐私保护）：* 在大多数模式下，SSL对客户端和服务器之间发送的数据进行加密。这可以保护数据的机密性，这样被动窃听者就不会看到敏感数据，如金融信息或多种个人信息。\n\
      \n在安全的远程方法调用（RMI）通信中使用SSL也很重要，因为RMI依赖于对象的序列化，而序列化的数据在传输中必须得到保护。Gong、Ellison和Dageforde\
      \ [Gong 2003]描述了如何使用SSLSocket保护RMI通信的安全。\n请注意，这个规则缺乏对通过套接字发送的数据的完整性的任何假设。关于确保数据完整性的信息，请参阅\
      \ SER02-J。在向信任边界外发送对象之前，先签署然后密封对象。\n**不规范的代码样例1**\n这个不符合规定的代码例子显示了一个服务器应用程序使用常规套接字的情况，它未能保护传输中的敏感信息。相应的客户端应用程序的不安全代码跟在服务器的代码后面。\n\
      ```java\n// Exception handling has been omitted for the sake of brevity\nclass\
      \ EchoServer {\n  public static void main(String[] args) throws IOException\
      \ {\n    ServerSocket serverSocket = null;\n    try {\n      serverSocket =\
      \ new ServerSocket(9999);\n      Socket socket = serverSocket.accept();\n  \
      \    PrintWriter out = new PrintWriter(socket.getOutputStream(), true);\n  \
      \    BufferedReader in = new BufferedReader(\n          new InputStreamReader(socket.getInputStream()));\n\
      \      String inputLine;\n      while ((inputLine = in.readLine()) != null)\
      \ {\n        System.out.println(inputLine);\n        out.println(inputLine);\n\
      \      }\n    } finally {\n      if (serverSocket != null) {\n        try {\n\
      \          serverSocket.close();\n        } catch (IOException x) {\n      \
      \    // Handle error\n        }\n      }\n    }\n  }\n}\n\nclass EchoClient\
      \ {\n  public static void main(String[] args)\n                          throws\
      \ UnknownHostException, IOException {\n    Socket socket = null;\n    try {\n\
      \      socket = new Socket(\"localhost\", 9999);\n      PrintWriter out = new\
      \ PrintWriter(socket.getOutputStream(), true);\n      BufferedReader in = new\
      \ BufferedReader(\n          new InputStreamReader(socket.getInputStream()));\n\
      \      BufferedReader stdIn = new BufferedReader(\n          new InputStreamReader(System.in));\n\
      \      String userInput;\n      while ((userInput = stdIn.readLine()) != null)\
      \ {\n        out.println(userInput);\n        System.out.println(in.readLine());\n\
      \      }\n    } finally {\n      if (socket != null) {\n        try {\n    \
      \      socket.close();\n        } catch (IOException x) {\n          // Handle\
      \ error\n        }\n      }\n    }\n  }\n}\n```"
    Description_En: "Programs must use the `javax.net.ssl.SSLSocket` class rather\
      \ than the `java.net.Socket` class when transferring sensitive data over insecure\
      \ communication channels. The class `SSLSocket` provides security protocols\
      \ such as Secure Sockets Layer/Transport Layer Security (SSL/TLS) to ensure\
      \ that the channel is not vulnerable to eavesdropping and malicious tampering.\n\
      \nThe principal protections included in SSLSocket that are not provided by the\
      \ Socket class are [API 2014]:\n\n> * *Integrity Protection:* SSL protects against\
      \ modification of messages by an active wiretapper.\n> * *Authentication:* In\
      \ most modes, SSL provides peer authentication. Servers are usually authenticated,\
      \ and clients may be authenticated as requested by servers.\n> * *Confidentiality\
      \ (privacy protection):* In most modes, SSL encrypts data being sent between\
      \ client and server. This protects the confidentiality of data so that passive\
      \ wiretappers won't see sensitive data such as financial information or personal\
      \ information of many kinds.\n\nIt is also important to use SSL for secure remote\
      \ method invocation (RMI) communications because RMI depends on object serialization,\
      \ and serialized data must be safeguarded in transit. Gong, Ellison, and Dageforde\
      \ [Gong 2003] describe how to secure RMI communications using SSLSocket.\n\n\
      Note that this rule lacks any assumptions about the integrity of the data being\
      \ sent down a socket. For information about ensuring data integrity, see SER02-J.\
      \ Sign then seal objects before sending them outside a trust boundary.\n\n**Noncompliant\
      \ Code Example**\n\nThis noncompliant code example shows the use of regular\
      \ sockets for a server application that fails to protect sensitive information\
      \ in transit. The insecure code for the corresponding client application follows\
      \ the server's code.\n\n```java\n// Exception handling has been omitted for\
      \ the sake of brevity\nclass EchoServer {\n  public static void main(String[]\
      \ args) throws IOException {\n    ServerSocket serverSocket = null;\n    try\
      \ {\n      serverSocket = new ServerSocket(9999);\n      Socket socket = serverSocket.accept();\n\
      \      PrintWriter out = new PrintWriter(socket.getOutputStream(), true);\n\
      \      BufferedReader in = new BufferedReader(\n          new InputStreamReader(socket.getInputStream()));\n\
      \      String inputLine;\n      while ((inputLine = in.readLine()) != null)\
      \ {\n        System.out.println(inputLine);\n        out.println(inputLine);\n\
      \      }\n    } finally {\n      if (serverSocket != null) {\n        try {\n\
      \          serverSocket.close();\n        } catch (IOException x) {\n      \
      \    // Handle error\n        }\n      }\n    }\n  }\n}\n\nclass EchoClient\
      \ {\n  public static void main(String[] args)\n                          throws\
      \ UnknownHostException, IOException {\n    Socket socket = null;\n    try {\n\
      \      socket = new Socket(\"localhost\", 9999);\n      PrintWriter out = new\
      \ PrintWriter(socket.getOutputStream(), true);\n      BufferedReader in = new\
      \ BufferedReader(\n          new InputStreamReader(socket.getInputStream()));\n\
      \      BufferedReader stdIn = new BufferedReader(\n          new InputStreamReader(System.in));\n\
      \      String userInput;\n      while ((userInput = stdIn.readLine()) != null)\
      \ {\n        out.println(userInput);\n        System.out.println(in.readLine());\n\
      \      }\n    } finally {\n      if (socket != null) {\n        try {\n    \
      \      socket.close();\n        } catch (IOException x) {\n          // Handle\
      \ error\n        }\n      }\n    }\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: ''
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010141800
    language: Java
  - Advice: "可使用SSL/TLS对通过网络发送的所有数据进行加密，或者将现有的未加密连接升级到SSL/TLS。  \n  \n**例如**：下面代码示例中，AuthenticatingSMTPClient未使用TLS进行数据的发送。\
      \  \n```java\nAuthenticatingSMTPClient client = new AuthenticatingSMTPClient();\
      \   \nclient.connect(url);  \n...  \nclient.execTLS();  \nWriter writer = client.sendMessageData();\
      \  \n...\n```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】邮件服务器不要建立未加密的连接
    Categories_En:
      Category: []
    Default: true
    Description: "邮件服务器使用的是未加密的连接，使用未加密网络发送敏感信息可能会被恶意攻击者通过拦截网络通信读取并修改信息。  \n  \n**例如**：下面代码示例中，AuthenticatingSMTPClient未使用TLS进行数据的发送。\
      \  \n```java\nAuthenticatingSMTPClient client = new AuthenticatingSMTPClient();\
      \   \nclient.connect(url);  \n...  \nWriter writer = client.sendMessageData();\
      \  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Mail Transmission
    ENSubClassChin: Insecure Transport
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 高
    GradesComment: 等级计算
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 200
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 200
      - type: CWE
        value: 200:Information Exposure
      - type: CERT
        value: MSC00-J:Use SSLSocket rather than Socket for secure data exchange
      - type: OWASP TOP 10
        value: 2017:A6 Security Misconfiguration
      - type: OWASP TOP 10
        value: 2013:A5 Security Misconfiguration
      - type: OWASP TOP 10
        value: 2010:A9 Insufficient Transport Layer Protection
      - type: OWASP TOP 10
        value: 2007:A9 Insecure Communications
      - type: OWASP MOBILE TOP 10
        value: 2014:M3 Insufficient Transport Layer Protection
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.9
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.9
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.4
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.4
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.4
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.4
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.4
    Security: true
    Tag: security
    Type: std
    WhiteList: not
    id: '0202000010150422'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```xml\n<session-config>  \n   \
      \ <session-timeout>30</session-timeout>  \n</session-config> \n```\n上述代码片段中，将Session失效时间设置为30分钟。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 7.5 网络传输
      - type: DetailClassChin
        value: A.4.5.2 避免Session失效时间设置过长
    Categories_En:
      Category: []
    Default: not
    Description: "规范说明：Session持续时间越长，攻击者危害用户帐户的机会就越大。  \n对于避免Session失效时间设置过长的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**：   \n```xml\n<session-config>  \n    <session-timeout>-1</session-timeout>\
      \  \n</session-config> \n```\n上述代码片段中，将Session设置为永不会过期。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 613:Insufficient Session Expiration
      - type: OWASP TOP 10
        value: 2017:A2 Broken Authentication
      - type: OWASP TOP 10
        value: 2013:A2 Broken Authentication and Session Management
      - type: OWASP TOP 10
        value: 2010:A3 Broken Authentication and Session Management
      - type: OWASP TOP 10
        value: 2007:A7 Broken Authentication and Session Management
      - type: OWASP TOP 10
        value: 2004:A3 Broken Authentication and Session Management
      - type: GB/T 34944-2017 Java
        value: 时间和状态:会话永不过期
      - type: PCI DSS
        value: 3.0:Requirement 8.1.8
      - type: PCI DSS
        value: 3.1:Requirement 8.1.8
      - type: PCI DSS
        value: 3.2:Requirement 8.1.8
      - type: PCI DSS
        value: 3.2.1:Requirement 8.1.8
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: 0302000010170069
    language: Java
  - Advice: "重写run方法或者不再继承于Thread。  \n  \n**例如**：下面代码示例中，重写了`run`方法。  \n```java\n\
      public class MyRunner extends Thread {  \n    @Override  \n    public void run()\
      \ {  \n        doSometing();  \n        ...  \n    }  \n    public void doSometing()\
      \ {...}  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 并发规范
      - type: DetailClassChin
        value: 【推荐】继承于Thread的类应该重写run方法
    Categories_En:
      Category: []
    Default: not
    Description: "在不重写run方法的情况下扩展线程类是没有意义的，意味着线程即使运行也只是执行父类的`run`方法。  \n  \n**例如**：下面代码示例中，没有重写Thread\
      \ 类中的`run`方法。  \n```java\npublic class MyRunner extends Thread {  \n    public\
      \ void doSometing() {...}  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Threads
    ENSubClassChin: J2EE Bad Practices
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: true
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '383:J2EE Bad Practices: Direct Use of Threads'
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150310'
    language: Java
  - Advice: 程序不应通过系统输出流或程序日志将系统数据或调试信息输出程序。
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 系统信息泄露：外部
    Categories_En:
      Category: []
    Default: true
    Description: "系统数据或调试信息通过网络流向远程机器时，发生外部信息泄露。\n\n**例如**：以下代码泄露了HTTP响应中的异常信息：\n\
      ```java\nprotected void doGet(HttpServletRequest req, HttpServletResponse res)\
      \ throws IOException {\n    PrintWriter out = res.getWriter();\n    try {\n\
      \        //...\n    } catch (Exception e) {\n        out.println(e.getMessage());\n\
      \    }\n}\n```"
    Description_En: ''
    DomainType: security
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行会发生+${categoryName}+的风险。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行会发生+${categoryName}+的风险。"
      th:remove="tag"></p>'
    ENClassChin: Encapsulation
    ENDetailClassChin:
    - External
    ENSubClassChin: System Information Leak
    FormalizationData:
      CodeFragments:
        CodeFragment:
        - id: '1'
          name: 1.从外部数据源返回触发系统信息泄露：外部漏洞的不可信数据
        - id: '2'
          name: 2.过滤函数
        - id: '3'
          name: 3.传递函数
        - id: '4'
          name: 4.中断函数
        - id: '5'
          name: 5.存在系统信息泄露：外部漏洞的爆发函数
        - id: '6'
          name: 6.不存在系统信息泄露：外部漏洞的其他函数
        - id: '7'
          name: 7.触发系统信息泄露：外部漏洞
        - id: '8'
          name: 8.程序执行正常
        - id: '9'
          name: 9.数据验证净化
        - id: '10'
          name: 10.数据传递中断
      Relationships:
        Relationship:
        - fromId: '1'
          name: 不可信数据传给过滤函数
          toId: '2'
        - fromId: '1'
          name: 不可信数据传给传递函数
          toId: '3'
        - fromId: '1'
          name: 不可信数据传给中断函数
          toId: '4'
        - fromId: '1'
          name: 不可信数据直接传给爆发函数
          toId: '5'
        - fromId: '1'
          name: 不可信数据直接传给其他函数
          toId: '6'
        - fromId: '2'
          name: 执行后数据状态
          toId: '9'
        - fromId: '3'
          name: 不可信数据经过传递函数传给爆发函数
          toId: '5'
        - fromId: '3'
          name: 不可信数据经过传递函数传给其他函数
          toId: '6'
        - fromId: '4'
          name: 执行后数据状态
          toId: '10'
        - fromId: '5'
          name: 函数执行后
          toId: '7'
        - fromId: '6'
          name: 函数执行后
          toId: '8'
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 526:Information Exposure Through Environmental Variables
      - type: CWE
        value: 497:Exposure of Sensitive System Information to an Unauthorized Control
          Sphere
      - type: CWE
        value: 209:Information Exposure Through an Error Message
      - type: CERT
        value: ERR01-J:Do not allow exceptions to expose sensitive information
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
      - type: OWASP ASVS
        value: v4.0.3-14.3.2
      - type: GB/T 34944-2017 Java
        value: 6.2.3.数据处理:6.2.3.7.信息通过错误消息泄露
    Security: true
    Tag: security,log,exception
    Type: bug
    WhiteList: true
    id: 0102000010110081
    language: Java
  - Advice: "程序中不应采用null密码，程序所需密码应从配置文件中获取加密的密码值。  \n  \n**例如**：下列代码中采用获取经过加密的密码值来获取数据库连接。\
      \  \n```java\n...  \nString password = getEncryptedPass();  \nconn = DriverManager.getConnection(url,user,\
      \ password);\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 密码规范
      - type: DetailClassChin
        value: 【强制】不要使用null密码
    Categories_En:
      Category: []
    Default: true
    Description: "null密码会削弱系统的安全性，甚至引起系统异常。  \n  \n**例如**：下列代码中采用null密码获取数据库连接。  \n\
      ```java\n...  \nconn = DriverManager.getConnection(url,\"root\",null);\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Empty Password
    ENSubClassChin: Password Management
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 798:Use of Hard-coded Credentials
      - type: CWE
        value: 259:Use of Hard-coded Password
      - type: CERT
        value: MSC03-J:Never hard code sensitive information
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2010:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 259
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Hard-coded Password [XYP]
      - type: GB/T 34944-2017 Java
        value: 安全功能:口令硬编码
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: security,password
    Type: std
    WhiteList: not
    id: '0202000010150211'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n```java\nString password = getEncryptedPass();\
      \  \nconn = DriverManager.getConnection(url,user, password);\n```  \n上述代码中采用经过加密的密码值来获取数据库连接。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.3 访问控制
      - type: DetailClassChin
        value: A.2.3.2.2 避免使用null密码
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：null密码会削弱系统的安全性，甚至引起系统异常。  \n对于避免使用null密码的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**：  \n```java\nconn = DriverManager.getConnection(url,\"root\",null);\n\
      ```  \n上述代码中采用硬编码密码获取数据库连接。  \n程序中不应采用null密码，程序所需密码应从配置文件中获取加密的密码值。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 798:Use of Hard-coded Credentials
      - type: CWE
        value: 259:Use of Hard-coded Password
      - type: CERT
        value: MSC03-J:Never hard code sensitive information
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2010:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 259
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Hard-coded Password [XYP]
      - type: GB/T 34944-2017 Java
        value: 安全功能:口令硬编码
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170022'
    language: Java
  - Advice: "不要在断言中使用会有衍生操作的表达式。  \n**规范的代码样例1**：  \n```java\nprivate ArrayList<String>\
      \ names;\n\nvoid process(int index) {\n    boolean nullsRemoved = names.remove(null);\n\
      \    assert nullsRemoved; // no side-effect\n    // …\n}\n```\n代码中不管断言是否被关闭，`names.remove(null)`方法总能执行。"
    Advice_En: "**Compliant Solution**\n\nThe possibility of side effects in assertions\
      \ can be avoided by decoupling the Boolean expression from the assertion:\n\n\
      ```java\nprivate ArrayList<String> names;\n\nvoid process(int index) {\n  boolean\
      \ nullsRemoved = names.remove(null);\n  assert nullsRemoved; // No side effect\n\
      \  // ...\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 02. 表达式 (EXP)
      - type: DetailClassChin
        value: EXP06-J：不要在断言中使用有衍生操作表达式
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 02. Expressions (EXP)
      - type: DetailClassChin
        value: EXP06-J:Expressions used in assertions must not produce side effects
    Default: true
    Description: "在代码中加入断言来进行测试是一个方便的做法。通常，断言语言的行为依赖于运行属性的状态，当开启断言时，断言语句会用来计算表达式的值，并在表达式的值是false的时候抛出AssertionError异常；当断言关闭时，断言语句会什么都不执行，如果断言表达式是一个有副作用的表达式，那么这个衍生操作会失去作用。\
      \  \n**不规范的代码样例1**：\n代码想要在断言中将列表`names`中的为`null`的元素删除，然而，在断言关闭时，方法`names.remove(null)`不会被执行。\n\
      ```java\nprivate ArrayList<String> names;\n\nvoid process(int index) {\n  assert\
      \ names.remove(null); // Side effect\n  // ...\n}\n```"
    Description_En: "The `assert` statement is a convenient mechanism for incorporating\
      \ diagnostic tests in code. The behavior of the `assert` statement depends on\
      \ the status of a runtime property. When enabled, the `assert` statement evaluates\
      \ its expression argument and throws an `AssertionError` if false. When disabled,\
      \ `assert` is a no-op; any side effects resulting from evaluation of the expression\
      \ in the assertion are lost. Consequently, expressions used with the standard\
      \ `assert` statement must not produce side effects.\n\n**Noncompliant Code Example**\n\
      \nThis noncompliant code attempts to delete all the null names from the list\
      \ in an assertion. However, the Boolean expression is not evaluated when assertions\
      \ are disabled.\n\n```java\nprivate ArrayList<String> names;\n\nvoid process(int\
      \ index) {\n  assert names.remove(null); // Side effect\n  // ...\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140406'
    language: Java
  - Advice: "finally块中只能存在不会异常终止程序的语句。  \n**规范的代码样例1**：   \n```java\nclass TryFinally\
      \ {\n  private static boolean doLogic() {\n    try {\n      throw new IllegalStateException();\n\
      \    } finally {\n      System.out.println(\"logic done\");\n    }\n    // Any\
      \ return statements must go here;\n    // applicable only when exception is\
      \ thrown conditionally\n  }\n}\n```\n在finally块中不返回，仅打印logic done。\n**特例**：程序控制流语句处于finally块中，break、continue语句可以在控制流语句内使用。\n\
      ```java\nclass TryFinally {\n  private static boolean doLogic() {\n    try {\n\
      \      throw new IllegalStateException();\n    } finally {\n      int c;\n \
      \     try {\n        while ((c = input.read()) != -1) {\n          if (c > 128)\
      \ {\n            break;\n          }\n        }\n      } catch (IOException\
      \ x) {\n        // Forward to handler\n      }\n      System.out.println(\"\
      logic done\");\n    }\n    // Any return statements must go here; applicable\
      \ only when exception is thrown conditionally\n  }\n}\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution removes the `return`\
      \ statement from the `finally` block:\n\n```java\nclass TryFinally {\n  private\
      \ static boolean doLogic() {\n    try {\n      throw new IllegalStateException();\n\
      \    } finally {\n      System.out.println(\"logic done\");\n    }\n    // Any\
      \ return statements must go here;\n    // applicable only when exception is\
      \ thrown conditionally\n  }\n}\n```\n\n**Exceptions**\n\nERRO4-J-EX0: Control\
      \ flow statements whose destination is within the `finally` block are perfectly\
      \ acceptable. For example, the following code does not violate this rule because\
      \ the `break` statement exits within the `while` loop but not within the `finally`\
      \ block:\n\n```java\nclass TryFinally {\n  private static boolean doLogic()\
      \ {\n    try {\n      throw new IllegalStateException();\n    } finally {\n\
      \      int c;\n      try {\n        while ((c = input.read()) != -1) {\n   \
      \       if (c > 128) {\n            break;\n          }\n        }\n      }\
      \ catch (IOException x) {\n        // Forward to handler\n      }\n      System.out.println(\"\
      logic done\");\n    }\n    // Any return statements must go here; applicable\
      \ only when exception is thrown conditionally\n  }\n}\n```"
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 07. 异常行为 (ERR)
      - type: DetailClassChin
        value: ERR04-J：不要从finally块中突然完成
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 07. Exceptional Behavior (ERR)
      - type: DetailClassChin
        value: ERR04-J:Do not complete abruptly from a finally block
    Default: true
    Description: "不要在finally块中使用return、break、continue或throw语句。当程序进行带有finally块的try块时，不管try块代码（或catch块代码）是否完成执行，finally程序块总是会执行的。造成finally块程序非正常终止的语句也会导致整个try块非正常终止，从而消除了从try或者catch块抛出的任何异常。\
      \  \n**不规范的代码样例1**：\n在这个不合规的代码示例中，“finally”块由于块中的“return”语句而突然完成：\n```java\n\
      class TryFinally {\n  private static boolean doLogic() {\n    try {\n      throw\
      \ new IllegalStateException();\n    } finally {\n      System.out.println(\"\
      logic done\");\n      return true;\n    }\n  }\n}\n```\n`IllegalStateException`\
      \ 被`return` 语句导致的`finally` 块的突然完成抑制。"
    Description_En: "Never use `return`, `break`, `continue`, or `throw` statements\
      \ within a `finally` block. When program execution enters a `try` block that\
      \ has a `finally` block, the `finally` block always executes regardless of whether\
      \ the `try` block (or any associated `catch` blocks) executes to normal completion.\
      \ Statements that cause the `finally` block to complete abruptly also cause\
      \ the `try` block to complete abruptly and consequently suppress any exception\
      \ thrown from the `try` or `catch` blocks. According to *The Java Language Specification*,\
      \ §14.20.2, &#34;Execution of `try-finally` and `try-catch-finally`&#34; [JLS\
      \ 2015]:\n\n> If execution of the `try` block completes abruptly for any other\
      \ reason `R`, then the `finally` block is executed. Then there is a choice:\n\
      >\n> * If the `finally` block completes normally, then the `try` statement completes\
      \ abruptly for reason `R`.\n> * If the `finally` block completes abruptly for\
      \ reason `S`, then the `try` statement completes abruptly for reason `S` (and\
      \ reason `R` is discarded).\n\n**Noncompliant Code Example**\n\nIn this noncompliant\
      \ code example, the `finally` block completes abruptly because of a `return`\
      \ statement in the block:\n\n```java\nclass TryFinally {\n  private static boolean\
      \ doLogic() {\n    try {\n      throw new IllegalStateException();\n    } finally\
      \ {\n      System.out.println(\"logic done\");\n      return true;\n    }\n\
      \  }\n}\n```\n\nThe `IllegalStateException` is suppressed by the abrupt completion\
      \ of the `finally` block caused by the `return` statement."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 584:Return Inside Finally Block
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.2 finally语句块中包含return语句
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140804
    language: Java
  - Advice: "检查程序逻辑，删除finally语句块中的返回语句。  \n  \n**例如**：下面代码中在finally代码块中没有return语句。\
      \  \n```java\npublic static String doOperation throws IOException{  \n    String\
      \ string = null;  \n    FileInputStream fis = null;  \n    try{  \n        fis\
      \ = new FileInputStream(\"Reader.txt\");  \n        //...  \n        return\
      \ string;  \n    }finally {  \n        if(fis != null) {  \n            try\
      \ {  \n                fis.close();  \n            } catch (IOException e) {\
      \  \n                //...  \n            }  \n                //...  \n   \
      \     }  \n        //...  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 异常规范
      - type: DetailClassChin
        value: 【推荐】finally代码块中不要存在return语句
    Categories_En:
      Category: []
    Default: true
    Description: "finally语句块中的返回指令会导致try块中抛出的异常丢失。  \n  \n**例如**：下面代码中在finally代码块中存在return语句。\
      \  \n```java\npublic static String doOperation throws IOException{  \n    String\
      \ string = null;  \n    FileInputStream fis = null;  \n    try{  \n        fis\
      \ = new FileInputStream(\"Reader.txt\");  \n        //...  \n        return\
      \ string;  \n    }finally {  \n        if(fis !=null) {  \n            try {\
      \  \n                fis.close();  \n            } catch (IOException e) { \
      \ \n            //...  \n            }  \n            //...  \n        }  \n\
      \        //...  \n  \n    return string;  \n    }  \n}\n```  \n当`Reader.txt`不存在，finally块中存在return语句且程序并未对异常进行捕获时，try代码块中产生的FileNotFoundException将不会显示。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Return Inside Finally
    ENSubClassChin: Poor Error Handling
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 584:Return Inside Finally Block
      - type: CERT
        value: ERR04-J:Do not exit abruptly from a finally block
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.2 finally语句块中包含return语句
    Security: not
    Tag: correctness,exception
    Type: std
    WhiteList: not
    id: '0202000010150502'
    language: Java
  - Advice: "对应的符合规则的解决方案是：去除 finally 程序段中的 return 语句。   \n    ```java\n    class\
      \ TryFinally {   \n    private static boolean doLogic() {   \n        try {\
      \   \n            throw new IllegalStateException ();   \n        } finally\
      \ {   \n            System.out.println(\"logic done\");   \n        }   \n \
      \           // Any return statements must go here;   \n            //applicable\
      \ only when exception is thrown conditionally   \n        }   \n    }  \n  \
      \  ```\n4) 特例   \n程序流控制语句的目的地在 finally 程序段内时，是可以接受的。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.4 错误处理安全
      - type: DetailClassChin
        value: C.7.5 不要在finally程序段非正常退出
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n不要在 finally 程序段中使用 return、break、continue 或 throw 语句。 \
      \  \n2) 原理   \n当程序进入带有 finally 程序段的 try 程序段时，不管 try 程序段（或是相应的 catch 程序段）是否完成执行，finally\
      \ 程序段总是会执行的。造成 finally 程序段非正常终止的语句也会导致 try 程序段非正常终止，从而消除了从 try 或 catch 程序段抛出的任何异常。\
      \   \n3) 示例   \n    如下不符合规则的代码示例中，在代码段中的 return 语句造成了 finally 程序段非正常终止。   \n\
      \    ```java\n    class TryFinally {   \n        private static boolean doLogic()\
      \  {    \n            try {   \n                throw new IllegalStateException();\
      \   \n            } finally {   \n                System.out.printIn(\"logic\
      \ done\");    \n                return true;   \n            }   \n        }\
      \   \n    }\n    ```   \n    return 语句引起的 finally 程序段的非正常终止消除了 IllegalStateException\
      \ 异常。   \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 584:Return Inside Finally Block
      - type: CERT
        value: ERR04-J:Do not exit abruptly from a finally block
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.2 finally语句块中包含return语句
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160067
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n```java\nclass TryFinally {\n \
      \   private static boolean doLogic() {\n        try {\n            throw new\
      \ IllegalStateException();\n        } finally {\n            System.out.println(\"\
      logic done\");\n        }\n    }\n}\n```  \n在finally块中不返回，仅打印`logic done`。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.4 异常处理安全
      - type: DetailClassChin
        value: A.3.4.4 不要在finally块中非正常退出
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：不要在finally块中使用return、break、continue或throw语句。当程序进入带有finally块的try块时，不管try块代码（或catch块代码）是否完成执行，finally程序块总是会执行的。造成finally块程序非正常终止的语句也会导致整个try块非正常终止，从而消除了从try或者catch块抛出的任何异常。\
      \  \n对于不要在finally块中非正常退出的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\n\
      class TryFinally {\n    private static boolean doLogic() {\n        try {\n\
      \            throw new IllegalStateException();\n        } finally {\n     \
      \       System.out.println(\"logic done\");\n            return true;\n    \
      \    }\n    }\n}\n```  \nfinally块中只能存在不会异常终止程序的语句。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 584:Return Inside Finally Block
      - type: CERT
        value: ERR04-J:Do not exit abruptly from a finally block
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.2 finally语句块中包含return语句
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170056'
    language: Java
  - Advice: "对于对称加密算法，建议使用长度大于或等于128位的密钥。对于非对称加密算法（如RSA)，建议使用长度大于或等于2048位的密钥。  \n\
      \  \n**例如**：下面代码片段中，KeyPairGenerator使用RSA加密算法，长度为2048位。  \n```java\nKeyPairGenerator\
      \ keyPairGen = KeyPairGenerator.getInstance(\"RSA\");  \nkeyPairGen.initialize(2048);\
      \  \nKeyPair keyPair = keyPairGen.generateKeyPair();  \nPublicKey publicKey\
      \ = (RSAPublicKey) keyPair.getPublic();  \nPrivateKey privateKey = (RSAPrivateKey)\
      \ keyPair.getPrivate();  \nbyte[] publicKeyData = publicKey.getEncoded();  \n\
      byte[] privateKeyData = privateKey.getEncoded();  \n...\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 密码规范
      - type: DetailClassChin
        value: 【强制】密钥长度应该足够长
    Categories_En:
      Category: []
    Default: true
    Description: "加密算法中使用的密钥长度较短，会降低系统安全。  \n  \n**例如**：下面代码片段中，KeyPairGenerator使用RSA加密算法，长度为1024位。\
      \  \n```java\nKeyPairGenerator keyPairGen = KeyPairGenerator.getInstance(\"\
      RSA\");  \nkeyPairGen.initialize(1024);  \nKeyPair keyPair = keyPairGen.generateKeyPair();\
      \  \nPublicKey publicKey = (RSAPublicKey) keyPair.getPublic();  \nPrivateKey\
      \ privateKey = (RSAPrivateKey) keyPair.getPrivate();  \nbyte[] publicKeyData\
      \ = publicKey.getEncoded();  \nbyte[] privateKeyData = privateKey.getEncoded();\
      \  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Insufficient Key Size
    ENSubClassChin: Weak Encryption
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 326:Inadequate Encryption Strength
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
    Security: true
    Tag: security,password
    Type: std
    WhiteList: not
    id: '0202000010150204'
    language: Java
  - Advice: '

      对于对称加密算法，建议使用长度大于或等于128位的密钥。对于非对称加密算法（如RSA)，建议使用长度大于或等于2048位的密钥。'
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.2 数据加密与保护
      - type: DetailClassChin
        value: A.2.2.1.4 密钥长度应该足够长
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：加密算法中使用的密钥长度较短，会降低系统安全。  \n对于密钥长度应足够长的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**：  \n```java\nKeyPairGenerator keyPairGen = KeyPairGenerator.getInstance(\"\
      RSA\");  \nkeyPairGen.initialize(1024);  \nKeyPair keyPair = keyPairGen.generateKeyPair();\
      \  \nPublicKey publicKey = (RSAPublicKey) keyPair.getPublic();  \nPrivateKey\
      \ privateKey = (RSAPrivateKey) keyPair.getPrivate();  \nbyte[] publicKeyData\
      \ = publicKey.getEncoded();  \nbyte[] privateKeyData = privateKey.getEncoded();\
      \  \n...\n```  \n以上代码片段中，KeyPairGenerator使用RSA加密算法，长度为1024位。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 326:Inadequate Encryption Strength
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170013'
    language: Java
  - Advice: "**规范的代码样例1（白名单）**：  \n```java\npublic static void FindLogEntry(String\
      \ search) {\n    // Sanitize search string\n    StringBuilder sb = new StringBuilder(search.length());\n\
      \    for (int i = 0; i < search.length(); ++i) {\n        char ch = search.charAt(i);\n\
      \        if (Character.isLetterOrDigit(ch) || ch == ' ' || ch == '\\'') {\n\
      \            sb.append(ch);\n        }\n    }\n    search = sb.toString();\n\
      \n    // Construct regex dynamically from user string\n    String regex = \"\
      (.*? +public\\\\[\\\\d+\\\\] +.*\" + search + \".*)\";\n    // ...\n}\n``` \
      \ \n这个符合规则的方案过滤搜索字符串中的非字母数字的字符（除了空格和单引号外），这种方案可以阻止正则表达式注入。  \n**规范的代码样例2（`Pattern.quote()`）**：\n\
      ```java  \npublic static void FindLogEntry(String search) {\n    // Sanitize\
      \ search string\n    StringBuilder sb = new StringBuilder(search.length());\n\
      \    for (int i = 0; i < search.length(); ++i) {\n        char ch = search.charAt(i);\n\
      \        if (Character.isLetterOrDigit(ch) || ch == ' ' || ch == '\\'') {\n\
      \            sb.append(ch);\n        }\n    }\n    search = sb.toString();\n\
      \n    // Construct regex dynamically from user string\n    String regex = \"\
      (.*? +public\\\\[\\\\d+\\\\] +.*\" + search + \".*)\";\n    // ...\n}\n``` \
      \ \n这个符合规则的方案使用`Pattern.quote()`来避免恶意字符串。`Matcher.quoteReplacement()` 方法可以被用来在正则表达式中过滤字符串。\
      \  \n**规范的代码样例3**：  \n另一个减少这种漏洞的方法是，在匹配前过滤敏感信息。这样的方案要求每一次日志文件定期更新时都要进行过滤，从而会产生额外的复杂性和性能损失。如果日志格式变化了，但类的设计没有反映这些变化，那么敏感数据依然可能被暴露。"
    Advice_En: "**Compliant Solution (Whitelisting)**\n\nThis compliant solution sanitizes\
      \ the search terms at the beginning of the `FindLogEntry()`, filtering out nonalphanumeric\
      \ characters (except space and single quote):\n\n```java\npublic static void\
      \ FindLogEntry(String search) {\n    // Sanitize search string\n    StringBuilder\
      \ sb = new StringBuilder(search.length());\n    for (int i = 0; i < search.length();\
      \ ++i) {\n        char ch = search.charAt(i);\n        if (Character.isLetterOrDigit(ch)\
      \ || ch == ' ' || ch == '\\'') {\n            sb.append(ch);\n        }\n  \
      \  }\n    search = sb.toString();\n\n    // Construct regex dynamically from\
      \ user string\n    String regex = \"(.*? +public\\\\[\\\\d+\\\\] +.*\" + search\
      \ + \".*)\";\n    // ...\n}\n```\n\nThis solution prevents regex injection but\
      \ also restricts search terms. For example, a user may no longer search for\
      \ \"`name =`\" because nonalphanumeric characters are removed from the search\
      \ term.\n\n**Compliant Solution (`Pattern.quote()`)**\n\nThis compliant solution\
      \ sanitizes the search terms by using `Pattern.quote()` to escape any malicious\
      \ characters in the search string. Unlike the previous compliant solution, a\
      \ search string using punctuation characters, such as \"`name =`\" is permitted.\n\
      \n```java\npublic static void FindLogEntry(String search) {\n    // Sanitize\
      \ search string\n    search = Pattern.quote(search);\n    // Construct regex\
      \ dynamically from user string\n    String regex = \"(.*? +public\\\\[\\\\d+\\\
      \\] +.*\" + search + \".*)\";\n    // ...\n}\n```\n\nThe  `Matcher.quoteReplacement()`\
      \ method can be used to escape strings used when doing regex substitution.\n\
      \n**Compliant Solution**\n\nAnother method of mitigating this vulnerability\
      \ is to filter out the sensitive information prior to matching. Such a solution\
      \ would require the filtering to be done every time the log file is periodically\
      \ refreshed, incurring extra complexity and a performance penalty. Sensitive\
      \ information may still be exposed if the log format changes but the class is\
      \ not also refactored to accommodate these changes."
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 00. 输入验证和数据处理 (IDS)
      - type: DetailClassChin
        value: IDS08-J：净化传递给正则表达式的非受信数据
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 00. Input Validation and Data Sanitization (IDS)
      - type: DetailClassChin
        value: IDS08-J:Sanitize untrusted data included in a regular expression
    Default: true
    Description: "正则表达式（regex）被广泛用于匹配文本字符串。例如，POSIX grep实用程序支持正则表达式来查找指定文本中的模式。 `java.util.regex`\
      \ 包提供了Pattern类，它封装了正则表达式的编译表示形式，也提供了Matcher类，它是一个引擎，使用Pattern对CharSequence进行匹配。\n\
      必须保护Java强大的regex功能不被滥用。攻击者可能会提供恶意输入，修改原始正则表达式，使正则表达式无法符合程序规范。这种称为regex注入的攻击向量可能会影响控制流、导致信息泄漏或导致拒绝服务（DoS）漏洞。\n\
      Java正则表达式的某些构造和属性容易受到攻击：\n* **匹配标志：** 不受信任的输入可能会覆盖已经传递或未传递的 `Pattern.compile()`\
      \ 方法的匹配选项\n* **贪婪：** 不受信任的输入可能会尝试注入一个正则表达式，将原始正则表达式更改为尽可能多的字符串匹配，从而暴露敏感信息。\n\
      * **分组：** 程序员可以将正则表达式的一部分括在括号中，以便对该组执行一些常见操作。攻击者可以通过提供不受信任的输入来更改分组。\n非受信的输入应该在使用前净化，从而防止发生正则表达式注入。当用户必须指定正则表达式作为输入时，必须注意需要保证初始的正则表达式没有被无限制修改。在用户输入字符串提交给正则解析之前，进行白名单字符处理（比如字母和数字）是一个很好的输入净化策略。开发人员必须仅仅提供最有限的正则表达式功能给用户，从而减少被误用的可能。\n\
      **正则表达式注入示例**\n假设一个系统日志文件包含一系列系统过程的输出信息。其中一些过程会产生公开的信息，而另一些会产生被标识为`“private”`的敏感信息。以下是这个日志文件的**例子**：\
      \  \n```\n10:47:03 private[423] Successful logout  name: usr1 ssn: 111223333\n\
      10:47:04 public[48964] Failed to resolve network service\n10:47:04 public[1]\
      \ (public.message[49367]) Exited with exit code: 255\n10:47:43 private[423]\
      \ Successful login  name: usr2 ssn: 444556666\n10:48:08 public[48964] Backup\
      \ failed with error: 19\n```\n用户希望搜索日志文件以寻找感兴趣的信息，然而又必须防止读取私有数据。程序可能会采用以下的方式，它允许用户将搜索文本作为下面所示正则表达式的一部分：\
      \  \n`(.*? +public\\[\\d+\\] +.*<SEARCHTEXT>.*) `  \n然而，攻击者可以用任何字符串替代\\<SEARCHTEXT\\\
      >，这样他就可以通过下面的文本实现正则表达式注入：  \n`.*)|(.* `  \n注入后的正则表达式为：  \n`(.*? +public\\[\\\
      d+\\] +.*.*)|(.*.*)`   \n这个正则表达式会匹配日志文件中的所有信息，包括那些私有的信息。  \n**不规范的代码样例1**：\n\
      这个不规范的代码使用非受信的用户输入查找日志文件，会导致正则表达式注入。\n```java\nimport java.io.FileInputStream;\n\
      import java.io.IOException;\nimport java.nio.CharBuffer;\nimport java.nio.MappedByteBuffer;\n\
      import java.nio.channels.FileChannel;\nimport java.nio.charset.Charset;\nimport\
      \ java.nio.charset.CharsetDecoder;\nimport java.util.regex.Matcher;\nimport\
      \ java.util.regex.Pattern;\n\npublic class LogSearch {\n    public static void\
      \ FindLogEntry(String search) {\n        // Construct regex dynamically from\
      \ user string\n        String regex = \"(.*? +public\\\\[\\\\d+\\\\] +.*\" +\
      \ search + \".*)\";\n        Pattern searchPattern = Pattern.compile(regex);\n\
      \        try (FileInputStream fis = new FileInputStream(\"log.txt\")) {\n  \
      \          FileChannel channel = fis.getChannel();\n            // Get the file's\
      \ size and map it into memory\n            long size = channel.size();\n   \
      \         final MappedByteBuffer mappedBuffer = channel.map(\n             \
      \       FileChannel.MapMode.READ_ONLY, 0, size);\n            Charset charset\
      \ = Charset.forName(\"ISO-8859-15\");\n            final CharsetDecoder decoder\
      \ = charset.newDecoder();\n            // Read file into char buffer\n     \
      \       CharBuffer log = decoder.decode(mappedBuffer);\n            Matcher\
      \ logMatcher = searchPattern.matcher(log);\n            while (logMatcher.find())\
      \ {\n                String match = logMatcher.group();\n                if\
      \ (!match.isEmpty()) {\n                    System.out.println(match);\n   \
      \             }\n            }\n        } catch (IOException ex) {\n       \
      \     System.err.println(\"thrown exception: \" + ex.toString());\n        \
      \    Throwable[] suppressed = ex.getSuppressed();\n            for (int i =\
      \ 0; i < suppressed.length; i++) {\n                System.err.println(\"suppressed\
      \ exception: \"\n                        + suppressed[i].toString());\n    \
      \        }\n        }\n        return;\n    }\n```\n此代码允许攻击者执行正则表达式注入。"
    Description_En: "Regular expressions (regex) are widely used to match strings\
      \ of text. For example, the POSIX `grep` utility supports regular expressions\
      \ for finding patterns in the specified text. For introductory information on\
      \ regular expressions, see the Java Tutorials [Java Tutorials]. The `java.util.regex`\
      \ package provides the `Pattern` class that encapsulates a compiled representation\
      \ of a regular expression and the `Matcher` class, which is an engine that uses\
      \ a `Pattern` to perform matching operations on a `CharSequence`.\n\nJava's\
      \ powerful regex facilities must be protected from misuse. An attacker may supply\
      \ a malicious input that modifies the original regular expression in such a\
      \ way that the regex fails to comply with the program's specification. This\
      \ attack vector, called a *regex injection*, might affect control flow, cause\
      \ information leaks, or result in denial-of-service(DoS) vulnerabilities.\n\n\
      Certain constructs and properties of Java regular expressions are susceptible\
      \ to exploitation:\n\n* **Matching flags:** Untrusted inputs may override matching\
      \ options that may or may not have been passed to the `Pattern.compile()` method.\n\
      * **Greediness:** An untrusted input may attempt to inject a regex that changes\
      \ the original regex to match as much of the string as possible, exposing sensitive\
      \ information.\n* **Grouping:** The programmer can enclose parts of a regular\
      \ expression in parentheses to perform some common action on the group. An attacker\
      \ may be able to change the groupings by supplying untrusted input.\n\nUntrusted\
      \ input should be sanitized before use to prevent regex injection. When the\
      \ user must specify a regex as input, care must be taken to ensure that the\
      \ original regex cannot be modified without restriction. Whitelisting characters\
      \ (such as letters and digits) before delivering the user-supplied string to\
      \ the regex parser is a good input sanitization strategy. A programmer must\
      \ provide only a very limited subset of regular expression functionality to\
      \ the user to minimize any chance of misuse.\n\n**Regex Injection Example**\n\
      \nSuppose a system log file contains messages output by various system processes.\
      \ Some processes produce public messages, and some processes produce sensitive\
      \ messages marked \"private.\" Here is an example log file:\n\n```java\n10:47:03\
      \ private[423] Successful logout  name: usr1 ssn: 111223333\n10:47:04 public[48964]\
      \ Failed to resolve network service\n10:47:04 public[1] (public.message[49367])\
      \ Exited with exit code: 255\n10:47:43 private[423] Successful login  name:\
      \ usr2 ssn: 444556666\n10:48:08 public[48964] Backup failed with error: 19\n\
      ```\n\nA user wishes to search the log file for interesting messages but must\
      \ be prevented from seeing the private messages. A program might accomplish\
      \ this by permitting the user to provide search text that becomes part of the\
      \ following regex:\n\n```java\n(.*? +public\\[\\d+\\] +.*<SEARCHTEXT>.*)\n```\n\
      \nHowever, if an attacker can substitute any string for `<SEARCHTEXT>`, he can\
      \ perform a regex injection with the following text:\n\n```java\n.*)|(.*\n```\n\
      \nWhen injected into the regex, the regex becomes\n\n```java\n(.*? +public\\\
      [\\d+\\] +.*.*)|(.*.*)\n```\n\nThis regex will match any line in the log file,\
      \ including the private ones.\n\n**Noncompliant Code Example**\n\nThis noncompliant\
      \ code example searches a log file using search terms from an untrusted user:\n\
      \n```java\nimport java.io.FileInputStream;\nimport java.io.IOException;\nimport\
      \ java.nio.CharBuffer;\nimport java.nio.MappedByteBuffer;\nimport java.nio.channels.FileChannel;\n\
      import java.nio.charset.Charset;\nimport java.nio.charset.CharsetDecoder;\n\
      import java.util.regex.Matcher;\nimport java.util.regex.Pattern;\n\npublic class\
      \ LogSearch {\n    public static void FindLogEntry(String search) {\n      \
      \  // Construct regex dynamically from user string\n        String regex = \"\
      (.*? +public\\\\[\\\\d+\\\\] +.*\" + search + \".*)\";\n        Pattern searchPattern\
      \ = Pattern.compile(regex);\n        try (FileInputStream fis = new FileInputStream(\"\
      log.txt\")) {\n            FileChannel channel = fis.getChannel();\n       \
      \     // Get the file's size and map it into memory\n            long size =\
      \ channel.size();\n            final MappedByteBuffer mappedBuffer = channel.map(\n\
      \                    FileChannel.MapMode.READ_ONLY, 0, size);\n            Charset\
      \ charset = Charset.forName(\"ISO-8859-15\");\n            final CharsetDecoder\
      \ decoder = charset.newDecoder();\n            // Read file into char buffer\n\
      \            CharBuffer log = decoder.decode(mappedBuffer);\n            Matcher\
      \ logMatcher = searchPattern.matcher(log);\n            while (logMatcher.find())\
      \ {\n                String match = logMatcher.group();\n                if\
      \ (!match.isEmpty()) {\n                    System.out.println(match);\n   \
      \             }\n            }\n        } catch (IOException ex) {\n       \
      \     System.err.println(\"thrown exception: \" + ex.toString());\n        \
      \    Throwable[] suppressed = ex.getSuppressed();\n            for (int i =\
      \ 0; i < suppressed.length; i++) {\n                System.err.println(\"suppressed\
      \ exception: \"\n                        + suppressed[i].toString());\n    \
      \        }\n        }\n        return;\n    }\n```\n\nThis code permits an attacker\
      \ to perform a regex injection."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 730:OWASP Top Ten 2004 Category A9 - Denial of Service
      - type: CWE
        value: 185:Incorrect Regular Expression
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: cert
    WhiteList: true
    id: 0102000010140208
    language: Java
  - Advice: "对应的符合规定的方案过滤搜索字符串中的非字母数字的字符（除了空格和单引号外），这种方案可以阻止正则表达式注入。   \n    ```java\n\
      \    public class Keywords {\n        // ...\n        public static Set<String>\
      \ suggestSearches(String search) {\n            synchronized (lock) {\n    \
      \            Set<String> searches = new HashSet<String>();\n               \
      \ StringBuilder sb = new StringBuilder(search.length());\n                for\
      \ (int i = 0; i < search.length(); ++i) {\n                    char ch = search.charAt(i);\n\
      \                    if (Character.isLetterOrDigit(ch) || ch == ' ' || ch ==\
      \ '\\\\') {\n                        sb.append(ch);\n                    }\n\
      \                }\n                search = sb.toString();\n              \
      \  // Construct regex dynamically from user string\n                String regex\
      \ = \"(.*? +public\\\\[\\\\d+\\\\] +.*\" + search + \".*)\";\n             \
      \   // ...\n            }\n        }\n    }\n    ```  \n    另一个减少这种漏洞的方法是，在匹配前过滤敏感信息。这样的方案要求每一次日志文件定期更新时都要进行过滤，从而会产生额外的复杂性和性能损失。如果日志格式变化了，但类的设计没有反映这些变化，那么敏感数据依然可能被暴露。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 5.2 数据净化
      - type: DetailClassChin
        value: C.1.9 净化传递给正则表达式的非受信数据
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n非受信的输入应该在使用前进行净化，从而防止发生正则表达式注入   \n2) 原理   \n正则表达式在匹配文本字符串时被广泛使用。在\
      \ Java 中，必须注意不能误用强大的正则表达式功能。攻击者也许会通过提供一个恶意输入对初始的正则表达式进行修改，比如使其不符合程序规定的正则表达的要求。这种攻击方式称为正则注入（regex\
      \ injection），它可以影响程序控制流、导致信息泄露，并引起绝服务漏洞。   \n当用户必须指定正则表达式作为输入时，必须注意需要保证初始的正则表达式没有被无限制修改。在用户输入字符串提交给正则解析之前，进行白名单字符处理（比如字母和数字）是一个很好的输入净化策略。开发人员必须仅仅提供最有限的正则表达式功能给用户，从而减少被误用的可能。\
      \   \n3) 示例   \n    这个不符合规则的代码示例将日志文件周期性地载入内存，它通过将关键词作为参数传给`suggestSearches()`\
      \ 方法来获得关键词搜索建议。   \n    ```java\n    public class Keywords {\n        private\
      \ static ScheduledExecutorService scheduler = Executors.newSingleThreadScheduledExecutor();\n\
      \        private static CharBuffer log;\n        private static final Object\
      \ lock = new Object();\n        // Map log file into memory, and periodically\
      \ reload\n        static {\n            try {\n                FileChannel channel\
      \ = new FileInputStream(\"path\").getChannel();\n                // Get the\
      \ file’s size and map it into memory\n                int size = (int) channel.size();\n\
      \                final MappedByteBuffer mappedBuffer = channel.map(FileChannel.MapMode.READ_ONLY,\
      \ 0, size);\n                Charset charset = Charset.forName(\"ISO-8859-15\"\
      );\n                final CharsetDecoder decoder = charset.newDecoder();\n \
      \               log = decoder.decode(mappedBuffer); // Read file into char buffer\n\
      \                Runnable periodicLogRead = new Runnable() {\n             \
      \       @Override\n                    public void run() {\n               \
      \         synchronized (lock) {\n                            try {\n       \
      \                         log = decoder.decode(mappedBuffer);\n            \
      \                } catch (CharacterCodingException e) {\n                  \
      \              // Forward to handler\n                            }\n      \
      \                  }\n                    }\n                };\n          \
      \      scheduler.scheduleAtFixedRate(periodicLogRead, 0, 5, TimeUnit.SECONDS);\n\
      \            } catch (Throwable t) {\n                // Forward to handler\n\
      \            }\n        }\n    }\n    \n    public static Set<String> suggestSearches(String\
      \ search) {\n        synchronized (lock) {\n            Set<String> searches\
      \ = new HashSet<String>();\n            // Construct regex dynamically from\
      \ user string\n            String regex = \"(.*? +public\\\\[\\\\d+\\\\] +.*\"\
      \ + search + \".*)\";\n            Pattern keywordPattern = Pattern.compile(regex);\n\
      \            Matcher logMatcher = keywordPattern.matcher(log);\n           \
      \ while (logMatcher.find()) {\n                String found = logMatcher.group(1);\n\
      \                searches.add(found);\n            }\n            return searches;\n\
      \        }\n    }\n    ```  \n    这段代码允许受信用户在公共日志信息中搜索`“error”`关键词。然而，这样会导致前面提到的通过正则注入进行的恶意攻击。\
      \   \n    这个"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 730:OWASP Top Ten 2004 Category A9 - Denial of Service
      - type: CWE
        value: 185:Incorrect Regular Expression
      - type: CERT
        value: IDS08-J:Sanitize untrusted data included in a regular expression
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: true
    id: 0902000010160009
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n示例2（白名单）：  \n```java\npublic static void FindLogEntry(String\
      \ search) {\n    // Sanitize search string\n    StringBuilder sb = new StringBuilder(search.length());\n\
      \    for (int i = 0; i < search.length(); ++i) {\n        char ch = search.charAt(i);\n\
      \        if (Character.isLetterOrDigit(ch) || ch == ' ' || ch == '\\'') {\n\
      \            sb.append(ch);\n        }\n    }\n    search = sb.toString();\n\
      \n    // Construct regex dynamically from user string\n    String regex = \"\
      (.*? +public\\\\[\\\\d+\\\\] +.*\" + search + \".*)\";\n    // ...\n}\n``` \
      \ \n这个符合规则的方案过滤搜索字符串中的非字母数字的字符（除了空格和单引号外），这种方案可以阻止正则表达式注入。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.3 函数调用安全
      - type: DetailClassChin
        value: A.3.3.3 净化传递给正则表达式的非受信数据
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：非受信的输入应该在使用前净化，从而防止发生正则表达式注入。当用户必须指定正则表达式作为输入时，必须注意需要保证初始的正则表达式没有被无限制修改。在用户输入字符串提交给正则解析之前，进行白名单字符处理（比如字母和数字）是一个很好的输入净化策略。开发人员必须仅仅提供最有限的正则表达式功能给用户，从而减少被误用的可能。\
      \  \n正则表达式注入示例：  \n假设一个系统日志文件包含一系列系统过程的输出信息。其中一些过程会产生公开的信息，而另一些会产生被标识为`“private”`的敏感信息。以下是这个日志文件的例子：\
      \  \n```\n10:47:03 private[423] Successful logout name: usr1 ssn: 111223333\
      \    \n10:47:04 public[48964] Failed to resolve network service    \n10:47:04\
      \ public[1] (public.message[49367]) Exited with exit code: 255    \n10:47:43\
      \ private[423] Successful login name: usr2 ssn: 444556666    \n10:48:08 public[48964]\
      \ Backup failed with error: 19   \n``` \n用户希望搜索日志文件以寻找感兴趣的信息，然而又必须防止读取私有数据。程序可能会采用以下的方式，它允许用户将搜索文本作为下面所示正则表达式的一部分：\
      \  \n```\n(.*? +public\\[\\d+\\] +.*\\<SEARCHTEXT\\>.*)  \n``` \n然而，攻击者可以用任何字符串替代`<SEARCHTEXT>`，这样他就可以通过下面的文本实现正则表达式注入：\
      \  \n```\n.*)|(.*  \n``` \n注入后的正则表达式为： \n``` \n(.*? +public\\[\\d+\\] +.*.*)|(.*.*)\n\
      ```\n这个正则表达式会匹配日志文件中的所有信息，包括那些私有的信息。  \n对于净化传递给正则表达式的非受信数据的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**：  \n```java\npublic static void FindLogEntry(String search) {\n\
      \    String regex = \"(.*? +public\\\\[\\\\d+\\\\] +.*\" + search + \".*)\"\
      ;\n    Pattern searchPattern = Pattern.compile(regex);\n    try (FileInputStream\
      \ fis = new FileInputStream(\"log.txt\")) {\n        FileChannel channel = fis.getChannel();\n\
      \        long size = channel.size();\n        final MappedByteBuffer mappedBuffer\
      \ = channel.map(FileChannel.MapMode.READ_ONLY, 0, size);\n        Charset charset\
      \ = Charset.forName(\"ISO-8859-15\");\n        final CharsetDecoder decoder\
      \ = charset.newDecoder();\n        CharBuffer log = decoder.decode(mappedBuffer);\n\
      \        Matcher logMatcher = searchPattern.matcher(log);\n        while (logMatcher.find())\
      \ {\n            String match = logMatcher.group();\n            if (!match.isEmpty())\
      \ {\n                System.out.println(match);\n            }\n        }\n\
      \    } catch (IOException ex) {\n        System.err.println(\"thrown exception:\
      \ \" + ex.toString());\n        Throwable[] suppressed = ex.getSuppressed();\n\
      \        for (int i = 0; i < suppressed.length; i++) {\n            System.err.println(\"\
      suppressed exception: \" + suppressed[i].toString());\n        }\n    }\n  \
      \  return;\n}\n```  \n这个不规范的代码使用非受信的用户输入查找日志文件，会导致正则表达式注入。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 730:OWASP Top Ten 2004 Category A9 - Denial of Service
      - type: CWE
        value: 185:Incorrect Regular Expression
      - type: CERT
        value: IDS08-J:Sanitize untrusted data included in a regular expression
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170053'
    language: Java
  - Advice: "将请求响应方法修饰符改为public或者将响应注解移除。  \n  \n**例如**：下面代码示例中使用了public 的请求响应方法。\n\
      ```java  \n@RequestMapping(\"/login\")  \npublic String login(User user) { \
      \ \n    ...  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】请求响应方法应该声明为public
    Categories_En:
      Category: []
    Default: true
    Description: "使用`@Controller`注解的类的方法被`@RequestMapping`（还有`@GetMapping`, `@PostMapping`,\
      \ `@PutMapping`, `@DeleteMapping`, `@PatchMapping`）注解进行注释，这个方法即使不是public的，匹配web请求时也会进行处理。\
      \  \n  \n**例如**：下面代码示例中使用了private的请求响应方法。\n```java  \n@RequestMapping(\"/login\"\
      )  \nprivate String login(User user) {   \n...  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: correctness,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150437'
    language: Java
  - Advice: "**规范的使用样例1（同步方法）**：  \n```java\nclass Base {\n    public synchronized\
      \ void doSomething() {\n        // ...\n    }\n}\n\nclass Derived extends Base\
      \ {\n    @Override\n    public synchronized void doSomething() {\n        //\
      \ ...\n    }\n}\n```  \n这个符合规范的方案同步了基类中的`doSomething()`方法。  \n**规范的使用样例2（私有的final锁对象）**：\
      \  \n```java\nclass Base {\n\n    public synchronized void doSomething() {\n\
      \        // ...\n    }\n}\n\nclass Derived extends Base {\n    private final\
      \ Object lock = new Object();\n\n    @Override\n    public void doSomething()\
      \ {\n        synchronized (lock) {\n            // ...\n        }\n    }\n}\n\
      ```   \n通过使用一个基于私有final锁对象来同步的方法覆写Base类中的同步方法`doSomething()`,这个符合规则的方案确保了Derived类是线程安全的。\
      \  \n**规范的使用样例3（私有锁）**：  \n```java\nclass Base {\n    private final Object lock\
      \ = new Object();\n\n    public void doSomething() {\n        synchronized (lock)\
      \ {\n            // ...\n        }\n    }\n}\n\nclass Derived extends Base {\n\
      \    Logger logger = // Initialize  \n\n    private final Object lock = new\
      \ Object();\n\n    @Override\n    public void doSomething() {\n        synchronized\
      \ (lock) {\n            try {\n                super.doSomething();\n      \
      \      } finally {\n                logger.log(Level.FINE, \"Did something\"\
      );\n            }\n        }\n    }\n}\n```\n这个符合规范的方案使用自己的私有final锁对象来同步子类的`doSomething()`方法。Derived\
      \ 和 Base 对象维持着各自的锁，而且不能访问对方的锁。因此，Derived可以提供线程安全保证并和Base相互独立。"
    Advice_En: "**Compliant Solution (Synchronized Method)**\n\nTThis compliant solution\
      \ synchronizes the doSomething() method of the subclass:\n\n```java\nclass Base\
      \ {\n  public synchronized void doSomething() {\n    // ...\n  }\n}\n\nclass\
      \ Derived extends Base {\n  @Override public synchronized void doSomething()\
      \ {\n    // ...\n  }\n}\n```\n\nThis solution also complies with LCK00-J. Use\
      \ private final lock objects to synchronize classes that may interact with untrusted\
      \ code because the accessibility of the class is package-private. Package-private\
      \ accessibility is permitted when untrusted code cannot infiltrate the package.\n\
      **Compliant Solution (Private Final Lock Object)**\n\nThis compliant solution\
      \ ensures that the `Derived` class is thread-safe by overriding the synchronized\
      \ `doSomething()` method of the `Base` class with a method that synchronizes\
      \ on a private final lock object.\n\n```java\nclass Base {\n\n  public synchronized\
      \ void doSomething() {\n    // ...\n  }\n}\n\nclass Derived extends Base {\n\
      \  private final Object lock = new Object();\n\n  @Override public void doSomething()\
      \ {\n    synchronized (lock) {\n      // ...\n    }\n  }\n}\n```\n\nThis is\
      \ an acceptable solution, provided the locking policy of the `Derived` class\
      \ is consistent with that of the `Base` class.\n**Compliant Solution (Private\
      \ Lock)**\n\nThis compliant solution synchronizes the `doSomething()` method\
      \ of the subclass using its own private final lock object:\n\n```java\nclass\
      \ Base {\n  private final Object lock = new Object();\n\n  public void doSomething()\
      \ {\n    synchronized (lock) {\n      // ...\n    }\n  }\n}\n\nclass Derived\
      \ extends Base {\n  Logger logger = // Initialize\n\n  private final Object\
      \ lock = new Object();\n\n  @Override public void doSomething() {\n    synchronized\
      \ (lock) {\n      try {\n        super.doSomething();\n      } finally {\n \
      \       logger.log(Level.FINE, \"Did something\");\n      }\n    }\n  }\n}\n\
      ```\n\nNote that the `Base` and `Derived` objects maintain distinct locks that\
      \ are inaccessible from each other's classes. Consequently, `Derived` can provide\
      \ thread-safety guarantees independent of `Base`."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 12. 与线程安全相关的其他规则 (TSM)
      - type: DetailClassChin
        value: TSM00-J：不要使用非线程安全方法来覆写线程安全方法
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 12. Thread-Safety Miscellaneous (TSM)
      - type: DetailClassChin
        value: TSM00-J:Do not override thread-safe methods with methods that are not
          thread-safe
    Default: true
    Description: "当一个依赖于基类提供线程安全的客户无意中对子类的实例进行操作时，如果使用对并发不安全的方法来覆写线程安全的方法，会导致不恰当的同步。比如，如果子类提供了对并发操作不安全的实现，那么可能会违反一个被覆写的同步方法的合约。这样的覆写很容易造成难以诊断的错误。因此，程序禁止使用非并发安全的方法来覆写线程安全方法。\
      \  \n**不规范的代码样例1（同步方法）**：  \n这个不规范的代码示例在Derived类中使用了一个非同步的方法覆写了Base类中的同步方法doSomething。\n\
      ```java\nclass Base {\n  public synchronized void doSomething() {\n    // ...\n\
      \  }\n}\n\nclass Derived extends Base {\n  @Override public void doSomething()\
      \ {\n    // ...\n  }\n}\n```\nBase类中的`doSomething()`方法可以安全的被多个线程使用，但这并不适用于Derived子类的实例。\
      \  \n因为接受Base实例的线程同时也接受它的子类的实例，所以可能很难诊断这个程序的错误。因此，客户可能不会意识到他们是在对继承了线程安全的类的非线程安全的子类实例进行操作。\
      \  \n**不规范的代码样例2（私有锁）**：  \n根据规则LCK00-J，这个不符合规范的代码示例在Base类中定义的`doSomething()`方法使用了一个私有的final锁对象。\
      \  \n```java\nclass Base {\n  private final Object lock = new Object();\n\n\
      \  public void doSomething() {\n    synchronized (lock) {\n      // ...\n  \
      \  }\n  }\n}\n\nclass Derived extends Base {\n  Logger logger = // Initialize\n\
      \n  @Override public void doSomething() {\n    try {\n      super.doSomething();\n\
      \    } finally {\n      logger.log(Level.FINE, \"Did something\");\n    }\n\
      \  }\n}\n```\n多个线程有可能造成记入日志的条目有着和任务执行不一样的顺序。因此，Derived类的doSomething方法是非线程安全的，不能安全的被多个线程调用。"
    Description_En: "Overriding thread-safe methods with methods that are unsafe for\
      \ concurrent use can result in improper synchronization when a client that depends\
      \ on the thread-safety promised by the parent inadvertently operates on an instance\
      \ of the subclass. For example, an overridden synchronized method's contract\
      \ can be violated when a subclass provides an implementation that is unsafe\
      \ for concurrent use. Such overriding can easily result in errors that are difficult\
      \ to diagnose. Consequently, programs must not override thread-safe methods\
      \ with methods that are unsafe for concurrent use.\n\nThe locking strategy of\
      \ classes designed for inheritance should always be documented. This information\
      \ can subsequently be used to determine an appropriate locking strategy for\
      \ subclasses (see LCK00-J. Use private final lock objects to synchronize classes\
      \ that may interact with untrusted code and LCK11-J. Avoid client-side locking\
      \ when using classes that do not commit to their locking strategy).\n\n**Noncompliant\
      \ Code Example (Synchronized Method)**\n\nThis noncompliant code example overrides\
      \ the synchronized `doSomething()` method in the `Base` class with an unsynchronized\
      \ method in the `Derived` class:\n\n```java\nclass Base {\n  public synchronized\
      \ void doSomething() {\n    // ...\n  }\n}\n\nclass Derived extends Base {\n\
      \  @Override public void doSomething() {\n    // ...\n  }\n}\n```\n\nThe `doSomething()`\
      \ method of the `Base` class can be safely used by multiple threads, but instances\
      \ of the `Derived` subclass cannot.\n\nThis programming error can be difficult\
      \ to diagnose because threads that accept instances of `Base` can also accept\
      \ instances of its subclasses. Consequently, clients could be unaware that they\
      \ are operating on a thread-unsafe instance of a subclass of athread-safe class.\n\
      \n**Noncompliant Code Example (Private Lock)**\n\nThis noncompliant code example\
      \ defines a `doSomething()` method in the `Base` class that uses a private final\
      \ lock in accordance with LCK00-J. Use private final lock objects to synchronize\
      \ classes that may interact with untrusted code.\n\n```java\nclass Base {\n\
      \  private final Object lock = new Object();\n\n  public void doSomething()\
      \ {\n    synchronized (lock) {\n      // ...\n    }\n  }\n}\n\nclass Derived\
      \ extends Base {\n  Logger logger = // Initialize\n\n  @Override public void\
      \ doSomething() {\n    try {\n      super.doSomething();\n    } finally {\n\
      \      logger.log(Level.FINE, \"Did something\");\n    }\n  }\n}\n```\n\nIt\
      \ is possible for multiple threads to cause the entries to be logged in an order\
      \ that differs from the order in which the tasks are performed. Consequently,\
      \ the `doSomething()` method of the `Derived` class cannot be used safely by\
      \ multiple threads because it is not thread-safe."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141300'
    language: Java
  - Advice: "对应的符合规则的解决方案是：同步子类中的 `doSomething()`方法。   \n    ```java\n    class Base\
      \ {\n        public synchronized void doSomething() {\n            // ...\n\
      \        }\n    }\n    \n    class Derived extends Base {\n        @Override\n\
      \        public synchronized void doSomething() {\n            // ...\n    \
      \    }\n    }\n    ```    \n    **例 2** 这个不符合规则的代码示例在 Base 类中定义的 `doSomething()`方法使用了一个私有的\
      \ final锁对象。   \n    ```java\n    class Base {\n        private final Object\
      \ lock = new Object();\n    \n        public void doSomething() {\n        \
      \    synchronized (lock) {\n                // ...\n            }\n        }\n\
      \    }\n    \n    class Derived extends Base {\n        Logger logger = // Initialize\
      \  \n    \n        @Override\n        public void doSomething() {\n        \
      \    try {\n                super.doSomething();\n            } finally {\n\
      \                logger.log(Level.FINE, \"Did something\");\n            }\n\
      \        }\n    }\n    ``` \n    多个线程有可能会造成记入日志的条目有着和任务执行不一样的顺序。因此， Derived\
      \ 类的`doSomething()`方法是非线程安全的，不能安全地被多个线程调用。   \n    对应的符合规则的解决方案是：使用自己的私有 fianl\
      \ 锁对象来同步子类的 `doSomething()`方法。   \n    ```java\n    class Base {\n        private\
      \ final Object lock = new Object();\n    \n        public void doSomething()\
      \ {\n            synchronized (lock) {\n                // ...\n           \
      \ }\n        }\n    }\n    \n    class Derived extends Base {\n        private\
      \ final Object lock = new Object();\n    \n        @Override\n        public\
      \ void doSomething() {\n            synchronized (lock) {\n                try\
      \ {\n                    super.doSomething();\n                } finally {\n\
      \                    logger.log(Level.FINE, \"Did something\");\n          \
      \      }\n            }\n        }\n    }\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.12.1 不要使用非线程安全方法来覆写线程安全方法
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序禁止使用非并发安全的方法来覆写线程安全方法。   \n2) 原理   \n当一个依赖于基类提供线程安全的客户无意中对子类的实例进行操作时，如果使用对并发不安全的方法来覆写线程安全方法，那会导致不恰当的同步。比如，如果子类提供了对并发操作不安全的实现，那么可能会违反一个被覆写的同步方法的合约。这样的覆写很容易造成难以诊断的错误。\
      \   \n如果被设计成可继承的类，那么它们所用的锁策略应该要详细记录。随后这些信息可以用于决定适合子类的锁策略。   \n3) 示例   \n   \
      \ **例 1** 这个不符合规则的代码示例在 Derived 类中使用一个非同步方法覆写了 Base 类中的同步方法   \n    ```java\n\
      \    class Base {\n        public synchronized void doSomething() {\n      \
      \      // ...\n        }\n    }\n    \n    class Derived extends Base {\n  \
      \      @Override\n        public void doSomething() {\n            // ...\n\
      \        }\n    }\n    ```  \n    Base 类中的 `doSomething()`方法可以安全地被多个线程使用，但这并不适用于\
      \ Deiced 子类的实例。   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: TSM00-J:Do not override thread-safe methods with methods that are not
          thread-safe
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160102
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n示例2（同步方法）：  \n```java\nclass Base {\n    public\
      \ synchronized void doSomething() {\n        // ...\n    }\n}\n\nclass Derived\
      \ extends Base {\n    @Override\n    public synchronized void doSomething()\
      \ {\n        // ...\n    }\n}\n``` \n这个符合规范的方案同步了基类中的`doSomething()`方法。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.13 不要使用非线程安全方法来覆写线程安全方法
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：当一个依赖于基类提供线程安全的客户无意中对子类的实例进行操作时，如果使用对并发不安全的方法来覆写线程安全的方法，会导致不恰当的同步。比如，如果子类提供了对并发操作不安全的实现，那么可能会违反一个被覆写的同步方法的合约。这样的覆写很容易造成难以诊断的错误。因此，程序禁止使用非并发安全的方法来覆写线程安全方法。\
      \  \n对于不要使用非线程安全方法来覆写线程安全方法的情况，示例1给出了不规范用法（java语言）示例。  \n示例1（同步方法）：  \n```java\n\
      class Base {\n    public synchronized void doSomething() {\n        // ...\n\
      \    }\n}\n\nclass Derived extends Base {\n    @Override\n    public void doSomething()\
      \ {\n        // ...\n    }\n}\n```  \n这个不符合规范的代码示例在Derived类中使用了一个非同步的方法覆写了Base类中的同步方法`doSomething`。\
      \  \nBase类中的`doSomething()`方法可以安全的被多个线程使用，但这并不适用于Derived子类的实例。  \n因为接受Base实例的线程同时也接受它的子类的实例，所以可能很难诊断这个程序的错误。因此，客户可能不会意识到他们是在对继承了线程安全的类的非线程安全的子类实例进行操作。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: TSM00-J:Do not override thread-safe methods with methods that are not
          thread-safe
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170045'
    language: Java
  - Advice: "**规范的代码样例1**：\n这个解决方案定义了`doSomething()`方法，有一个`timeout`参数而不是`time`值。使用`Object.wait()`而不是`Thread.sleep()`可以设置一个超时时间，在这个时间段内，通知可以唤醒线程。\n\
      ```java\npublic synchronized void doSomething(long timeout)\n              \
      \                       throws InterruptedException {\n// ...\n  while (<condition\
      \ does not hold>) {\n    wait(timeout); // Immediately releases the current\
      \ monitor\n  }\n}\n```\n当前对象的监视器在进入等待状态后立即释放。 当超时时间过去时，线程在重新获取当前对象的监视器后恢复执行。\n\
      根据 Java API Class `Object` 文档 [API 2014]\n\n> 请注意，`wait` 方法将当前线程放入该对象的等待集中，仅解锁该对象；\
      \ 当前线程可能同步的任何其他对象在线程等待时保持锁定。\n>\n> 此方法只能由作为该对象监视器所有者的线程调用。\n\n程序必须确保持有其他对象锁的线程在进入等待状态之前适当地释放这些锁。\
      \ THI03-J 中提供了有关等待和通知的其他指南. 始终在循环和 THI02-J 中调用 wait() 和 await() 方法. 通知所有等待线程而不是单个线程。\n\
      \n**规范的代码样例2**：\n这个符合要求的解决方案将这一过程分成了一连串的步骤。\n\n1. 对需要同步的数据结构进行操作。\n2. 2. 创建要发送的对象的副本。\n\
      3. 在一个单独的非同步方法中执行网络调用。\n\n方法`doSomething`使用了一个内置锁，使用了`Object.wait()`方法而不是`Thead.sleep()`方法，`Object.wait()`方法设置了一个`timeout`期限，当对象的监视器在进入等待状态时，会被立即释放。在`timeout`期限到达时，线程在获得当前对象的监视器之后会继续执行。\n\
      \n```java\n// No synchronization\npublic boolean sendPage(Socket socket, String\
      \ pageName) {\n  Page targetPage = getPage(pageName);\n\n  if (targetPage ==\
      \ null){\n    return false;\n  }\n  return deliverPage(socket, targetPage);\n\
      }\n\n// Requires synchronization\nprivate synchronized Page getPage(String pageName)\
      \ {\n  Page targetPage = null;\n\n  for (Page p : pageBuff) {\n    if (p.getName().equals(pageName))\
      \ {\n      targetPage = p;\n    }\n  }\n  return targetPage;\n}\n\n// Return\
      \ false if an error occurs, true if successful\npublic boolean deliverPage(Socket\
      \ socket, Page page) {\n  ObjectOutputStream out = null;\n  boolean result =\
      \ true;\n  try {\n    // Get the output stream to write the Page to\n    out\
      \ = new ObjectOutputStream(socket.getOutputStream());\n\n    // Send the page\
      \ to the client\n    out.writeObject(page);out.flush();\n  } catch (IOException\
      \ io) {\n    result = false;\n  } finally {\n    if (out != null) {\n      try\
      \ {\n        out.close();\n      } catch (IOException e) {\n        result =\
      \ false;\n      }\n    }\n  }\n  return result;\n}\n```\n**特例**\nLCK09-J-EX0:\
      \ 允许向调用者提供适当终止机制的类违反此规则（见THI04-J. 确保执行阻塞操作的线程可以被终止）。\n\nLCK09-J-EX1: 需要多个锁的方法可以持有几个锁，同时等待其余的锁变得可用。这构成了一个有效的例外，尽管程序员必须遵循其他适用的规则，特别是LCK07-J.通过以相同的顺序请求和释放锁来避免死锁的发生。"
    Advice_En: "!**Compliant Solution (Intrinsic Lock)**\n\nThis compliant solution\
      \ defines the `doSomething()` method with a `timeout` parameter rather than\
      \ the `time` value. Using `Object.wait()` instead of `Thread.sleep()` allows\
      \ setting a timeout period during which a notification may awaken the thread.\n\
      \n```java\npublic synchronized void doSomething(long timeout)\n            \
      \                         throws InterruptedException {\n// ...\n  while (<condition\
      \ does not hold>) {\n    wait(timeout); // Immediately releases the current\
      \ monitor\n  }\n}\n```\n\nThe current object's monitor is immediately released\
      \ upon entering the wait state. When the timeout period elapses, the thread\
      \ resumes execution after reacquiring the current object's monitor.\n\nAccording\
      \ to the Java API Class `Object` documentation [API 2014]\n\n> Note that the\
      \ `wait` method, as it places the current thread into the wait set for this\
      \ object, unlocks only this object; any other objects on which the current thread\
      \ may be synchronized remain locked while the thread waits.\n>\n> This method\
      \ should only be called by a thread that is the owner of this object's monitor.\n\
      \nPrograms must ensure that threads that hold locks on other objects release\
      \ those locks appropriately before entering the wait state. Additional guidance\
      \ on waiting and notification is available in THI03-J. Always invoke wait()\
      \ and await() methods inside a loop and THI02-J. Notify all waiting threads\
      \ rather than a single thread.\n\n**Compliant Solution**\n\nThis compliant solution\
      \ separates the process into a sequence of steps:\n\n1. Perform actions on data\
      \ structures requiring synchronization.\n2. Create copies of the objects to\
      \ be sent.\n3. Perform network calls in a separate unsynchronized method.\n\n\
      In this compliant solution, the unsynchronized `sendPage()` method calls the\
      \ synchronized `getPage()` method to retrieve the requested `Page` in the `pageBuff`\
      \ array. After the `Page` is retrieved, `sendPage()` calls the unsynchronized\
      \ `deliverPage()` method to deliver the `Page` to the client.\n\n```java\n//\
      \ No synchronization\npublic boolean sendPage(Socket socket, String pageName)\
      \ {\n  Page targetPage = getPage(pageName);\n\n  if (targetPage == null){\n\
      \    return false;\n  }\n  return deliverPage(socket, targetPage);\n}\n\n//\
      \ Requires synchronization\nprivate synchronized Page getPage(String pageName)\
      \ {\n  Page targetPage = null;\n\n  for (Page p : pageBuff) {\n    if (p.getName().equals(pageName))\
      \ {\n      targetPage = p;\n    }\n  }\n  return targetPage;\n}\n\n// Return\
      \ false if an error occurs, true if successful\npublic boolean deliverPage(Socket\
      \ socket, Page page) {\n  ObjectOutputStream out = null;\n  boolean result =\
      \ true;\n  try {\n    // Get the output stream to write the Page to\n    out\
      \ = new ObjectOutputStream(socket.getOutputStream());\n\n    // Send the page\
      \ to the client\n    out.writeObject(page);out.flush();\n  } catch (IOException\
      \ io) {\n    result = false;\n  } finally {\n    if (out != null) {\n      try\
      \ {\n        out.close();\n      } catch (IOException e) {\n        result =\
      \ false;\n      }\n    }\n  }\n  return result;\n}\n```\n\n**Exceptions**\n\n\
      LCK09-J-EX0: Classes that provide an appropriate termination mechanism to callers\
      \ are permitted to violate this rule (see THI04-J. Ensure that threads performing\
      \ blocking operations can be terminated).\n\nLCK09-J-EX1: Methods that require\
      \ multiple locks may hold several locks while waiting for the remaining locks\
      \ to become available. This constitutes a valid exception, although the programmer\
      \ must follow other applicable rules, especially LCK07-J. Avoid deadlock by\
      \ requesting and releasing locks in the same order to avoid deadlock."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 09. 锁 (LCK)
      - type: DetailClassChin
        value: LCK09-J：不要执行那些持有锁时会阻塞的操作
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 09. Locking (LCK)
      - type: DetailClassChin
        value: LCK09-J:Do not perform operations that can block while holding a lock
    Default: true
    Description: "当执行耗时或者阻塞的操作时，持有锁会严重的影响系统的性能并且会导致资源不足。此外，如果无限阻塞相互关联的线程，会发生死锁。阻塞操作包括网络、文件、控制台I/O和对象序列化。将一个线程延时同样会形成阻塞操作。因此，程序必须在不能持有锁的时候执行阻塞操作。\
      \  \n当JVM与文件系统进行交互时，如果这些操作基于不可靠的网络，文件IO会导致很严重的性能问题。在这样的情况下，要避免在持有锁时，进行基于网络的文件IO操作。\
      \  \n风险评估：在同步块（同步方法）中执行阻塞或者冗长的操作会导致死锁或者系统无响应。  \n**不规范的代码样例1**：  \n```java\n\
      public synchronized void doSomething(long time) throws InterruptedException\
      \ {\n    // . . .\n    Thread.sleep(time);\n}\n```  \n代码在一个同步方法中执行线程睡眠语句。因为方法是同步的，当线程挂起时，其他线程也不能使用这个类的同步方法，线程将继续持有当前的对象监视器，这是因为`Thread.sleep()`方法缺少同步语义。\n\
      **不规范的代码样例2（Network I/O）**：\n这个不合规的代码例子定义了一个`sendPage()`方法，该方法将一个`Page`对象从服务器发送到客户端。该方法是同步的，以便在多个线程请求并发访问时保护`pageBuff`数组。\n\
      ```java\n// Class Page is defined separately.\n// It stores and returns the\
      \ Page name via getName()\nPage[] pageBuff = new Page[MAX_PAGE_SIZE];\n\npublic\
      \ synchronized boolean sendPage(Socket socket, String pageName)\n          \
      \                           throws IOException {\n  // Get the output stream\
      \ to write the Page to\n  ObjectOutputStream out\n      = new ObjectOutputStream(socket.getOutputStream());\n\
      \n  // Find the Page requested by the client\n  // (this operation requires\
      \ synchronization)\n  Page targetPage = null;\n  for (Page p : pageBuff) {\n\
      \    if (p.getName().compareTo(pageName) == 0) {\n      targetPage = p;\n  \
      \  }\n  }\n\n  // Requested Page does not exist\n  if (targetPage == null) {\n\
      \    return false;\n  }\n\n  // Send the Page to the client\n  // (does not\
      \ require any synchronization)\n  out.writeObject(targetPage);\n\n  out.flush();\n\
      \  out.close();\n  return true;\n}\n```\n在同步的`sendPage()`方法中调用`writeObject()`，在高延迟的网络中或在网络连接本身就有*损失的情况下，会导致延迟和死锁。"
    Description_En: "Holding locks while performing time-consuming or blocking operations\
      \ can severely degrade system performance and can result in starvation. Furthermore,\
      \ deadlock  can result if interdependent threads block indefinitely. Blocking\
      \ operations include network, file, and console I/O (for example, `Console.readLine()`)\
      \ and object serialization. Deferring a thread indefinitely also constitutes\
      \ a blocking operation. Consequently, programs must not perform blocking operations\
      \ while holding a lock.\n\nWhen the Java Virtual Machine (JVM) interacts with\
      \ a file system that operates over an unreliable network, file I/O might incur\
      \ a large performance penalty. In such cases, avoid file I/O over the network\
      \ while holding a lock. File operations (such as logging) that could block while\
      \ waiting for the output stream lock or for I/O to complete could be performed\
      \ in a dedicated thread to speed up task processing. Logging requests can be\
      \ added to a queue, assuming that the queue's `put()` operation incurs little\
      \ overhead as compared to file I/O [Goetz 2006].\n\n**Noncompliant Code Example\
      \ (Deferring a Thread)**\n\nThis noncompliant code example defines a utility\
      \ method that accepts a `time` argument:\n\n```java\npublic synchronized void\
      \ doSomething(long time)\n                         throws InterruptedException\
      \ {\n  // ...\n  Thread.sleep(time);\n}\n```\n\nBecause the method is synchronized,\
      \ when the thread is suspended, other threads cannot use the synchronized methods\
      \ of the class. The current object's monitor continues to be held because the\
      \ `Thread.sleep()` method lacks synchronization semantics.\n\n**Noncompliant\
      \ Code Example (Network I/O)**\n\nThis noncompliant code example defines a `sendPage()`\
      \ method that sends a `Page` object from a server to a client. The method is\
      \ synchronized to protect the `pageBuff` array when multiple threads request\
      \ concurrent access.\n\n```java\n// Class Page is defined separately.\n// It\
      \ stores and returns the Page name via getName()\nPage[] pageBuff = new Page[MAX_PAGE_SIZE];\n\
      \npublic synchronized boolean sendPage(Socket socket, String pageName)\n   \
      \                                  throws IOException {\n  // Get the output\
      \ stream to write the Page to\n  ObjectOutputStream out\n      = new ObjectOutputStream(socket.getOutputStream());\n\
      \n  // Find the Page requested by the client\n  // (this operation requires\
      \ synchronization)\n  Page targetPage = null;\n  for (Page p : pageBuff) {\n\
      \    if (p.getName().compareTo(pageName) == 0) {\n      targetPage = p;\n  \
      \  }\n  }\n\n  // Requested Page does not exist\n  if (targetPage == null) {\n\
      \    return false;\n  }\n\n  // Send the Page to the client\n  // (does not\
      \ require any synchronization)\n  out.writeObject(targetPage);\n\n  out.flush();\n\
      \  out.close();\n  return true;\n}\n```\n\nCalling `writeObject()` within the\
      \ synchronized `sendPage()` method can result in delays and deadlock-like conditions\
      \ in high-latency networks or when network connections are inherently *lossy*."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 557:Concurrency Issues
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 6.8.4 持锁时调用sleep方法
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010141009
    language: Java
  - Advice: "对应的符合规则的解决方案是：   \n    将进程分成以下几个步骤：   \n    1）基于需要同步的数据结构执行动作。   \n \
      \   1） 创建被发送对象的复制。   \n    3）在一个分离的不同步方法中执行网络调用。   \n    本方案中，不同步的`sendPage()`方法调用同步的`sentPage()`方法来\
      \ 在 pageBuff 队列中取得请求的 Page。在取得 Page 之后，`sendPage()`方法会调用不同步的 `deliverPage()`方法来将\
      \ Page 提交到客户端。   \n    ```java\n    // No synchronization\n    public boolean\
      \ sendPage(Socket socket, String pageName) {\n        Page targetPage = getPage(pageName);\n\
      \    \n        if (targetPage == null) {\n            return false;\n      \
      \  }\n        return deliverPage(socket, targetPage);\n    }\n    \n    // Requires\
      \ synchronization\n    private synchronized Page getPage(String pageName) {\n\
      \        Page targetPage = null;\n    \n        for (Page p : pageBuff) {\n\
      \            if (p.getName().equals(pageName)) {\n                targetPage\
      \ = p;\n            }\n        }\n        return targetPage;\n    }\n    \n\
      \    // Return false if an error occurs, true if successful\n    public boolean\
      \ deliverPage(Socket socket, Page page) {\n        ObjectOutputStream out =\
      \ null;\n        boolean result = true;\n        try {\n            // Get the\
      \ output stream to write the Page to\n            out = new ObjectOutputStream(socket.getOutputStream());\n\
      \    \n            // Send the page to the client\n            out.writeObject(page);\n\
      \            out.flush();\n        } catch (IOException io) {\n            result\
      \ = false;\n        } finally {\n            if (out != null) {\n          \
      \      try {\n                    out.close();\n                } catch (IOException\
      \ e) {\n                    result = false;\n                }\n           \
      \ }\n        }\n        return result;\n    }\n    ``` \n4) 特例   \n1、向调用者提供正确终止机制的类可以违反本规则。\
      \   \n2、要求多个锁的方法可以在持有若干锁的同时，等待剩余的锁。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.9.10 不要执行那些持有锁时会阻塞的操作
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序必须不能持有锁的时候执行阻塞操作   \n2) 原理   \n当执行耗时的或者阻塞的操作时，持有锁会严重影响系统的性能并且会导致资源不足。此\
      \ 外，如果无限期阻塞相互关联的线程，会发生死锁。阻塞操作包括网络、文件和控制台 I/O 和对象序列化。将一个线程延时同样会形成阻塞操作。因此，程序必须不能持有锁的时候执行阻塞操作。\
      \   \n当 Java 虚拟机与文件系统进行交互时，如果这些操作基于不可靠的网络、文件 I/O 会导致严重性能问题。在这样的情况下，要避免在持有锁时进行基于网络的文件\
      \ I/O 操作。   \n3) 示例   \n    1、不符合规则的代码示例（延迟一个线程），该示例定义了一个方法来接受一个time参数。   \n\
      \    ```java\n    public synchronized void doSomething(long time)  throws InterruptedException\
      \ {   \n        // . . .    \n        Thread.sleep(time);   \n    }\n    ```\
      \   \n    因为方法是同步的，当挂起线程时，其他线程也不能使用这个类的同步方法。将继续持有当前的对象监视器，因为 `Thread.sleep()`方法缺少同步语义。\
      \   \n    对应符合规则的解决方案（内置锁）是：定义一个 `doSomething()`方法，它使用了一个 timeout参数而不是一个time数值。使用\
      \ `Object.wait()`方法而不是 `Thread.sleep()`方法会设置一个 timeout 期限，从而由一个通知唤醒线程。   \n\
      \    ```java\n    public synchronized void doSomething(long timeout) throws\
      \ InterruptedException {\n        // ...\n        while (<condition does not\
      \ hold>) {\n            wait(timeout); // Immediately releases the current monitor\n\
      \    ```\n    当前对象的监视器在进入等待的状态时，会被立即释放。在 timeout 期限到达时，线程在获得当前对象的监视器之后会继续执行。\
      \   \n    2、不符合规则的代码示例（网络 I/O )，该示例定义了一个`sendPage()`方法，该方法会从服务器发送一个 Page 对象到客户端。当多个线程请求并发访问时，同步这个方法来保护\
      \ pageBuff 队列。   \n    ```java\n    // Class Page is defined separately.\n \
      \   // It stores and returns the Page name via getName()\n    Page[] pageBuff\
      \ = new Page[MAX_PAGE_SIZE];\n    \n    public synchronized boolean sendPage(Socket\
      \ socket, String pageName) throws IOException {\n        // Get the output stream\
      \ to write the Page to\n        ObjectOutputStream out = new ObjectOutputStream(socket.getOutputStream());\n\
      \    \n        // Find the Page requested by the client\n        // (this operation\
      \ requires synchronization)\n        Page targetPage = null;\n        for (Page\
      \ p : pageBuff) {\n            if (p.getName().compareTo(pageName) == 0) {\n\
      \                targetPage = p;\n            }\n        }\n    \n        //\
      \ Requested Page does not exist\n        if (targetPage == null) {\n       \
      \     return false;\n        }\n    \n        // Send the Page to the client\n\
      \        // (does not require any synchronization)\n        out.writeObject(targetPage);\n\
      \    \n        out.flush();\n        out.close();\n        return true;\n  \
      \  }\n    ```   \n    在同步`sendPage()`方法中调用`writeObject()`会导致延时，并且在高延时的网络或者当网络条件本身存在丢包现象时，会产生类似死锁的条件。\
      \   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 557:Concurrency Issues
      - type: CERT
        value: LCK09-J:Do not perform operations that can block while holding a lock
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 6.8.4 持锁时调用sleep方法
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: true
    id: 0902000010160088
    language: Java
  - Advice: "**规范的代码样例1**：  \n1）当需要使用反射时，确认直接的调用者通过声明为private、final和恶意代码是分开的，正如这个符合规则的方案所展示的一样：\
      \             \n```java\nclass FieldExample {\n    // . . .\n    private void\
      \ zeroField(String fieldName) {\n        // . . .\n    }\n}\n```  \n  \n2）当一个类必须要使用反射来对字段进行访问的时候，它同时必须使用一个非反射的接口来进行同样的访问。这个符合规则的方案提供了受限的`setter`方法，从而赋予所有调用者不使用反射就能访问该类字段的能力。\
      \  \n```java\nclass FieldExample {\n    // . . .\n    public void zeroField(String\
      \ fieldName) {\n        // . . .\n    }\n\n    public void zeroI() {\n     \
      \   this.i = 0;\n    }\n\n    public void zeroJ() {\n        this.i = 0;\n \
      \   }\n}\n```  \n**规范的代码样例2**：  \n1）减少访问权限。这个样例中将`create()`方法的访问减少到包私有的程度，可以防止在包外部的调用者使用这个方法来绕过语言检查从而创建出Trusted类的实例。需要创建Trusted类的实例，可以通过调用Trusted的构造方法来实现。\
      \  \n```java\npackage Safe;  \npublic class Trusted {\n    Trusted() {\n   \
      \ } // package private constructor\n\n    static <T> T create(Class<T> c) throws\
      \ InstantiationException, IllegalAccessException {\n        return c.newInstance();\n\
      \    }\n}  \n```\n2）安全管理器检查：  \n这个方案使用了`getConstructors()`方法来检查做为参数的类是否有公有的构造方法。当具有公有构造方法的时候，没有安全问题，因为这个构造方法本来就可以被恶意代码访问。当其没有公有构造方法时，`create()`方法使用安全管理器的`checkPackageAccess()`方法来保证所有在执行链中的调用者有足够的权限来访问定义在Safe包中的类和他们对应的字段。这个方案的缺点在于：这个类必须要赋予反射权限才允许调用`getConstructors()`方法。\
      \  \n```java\npackage Safe;\nimport java.beans.Beans;  \nimport java.io.IOException;\
      \    \npublic class Trusted {\n    Trusted() {\n    }\n\n    public static <T>\
      \ T create(Class<T> c) throws InstantiationException, IllegalAccessException\
      \ {\n        if (c.getConstructors().length == 0) { // No public constructors\n\
      \            SecurityManager sm = System.getSecurityManager();\n           \
      \ if (sm != null) {\n                // throws an exception when access is not\
      \ allowed\n                sm.checkPackageAccess(\"Safe\");\n            }\n\
      \        }\n        return c.newInstance(); // Safe to return\n    }\n}\n```\n\
      3）java.beans.Beans API  \n这个方案使用`java.beans.Beans` API来检查接收到的Class对象是否有公有的构造函数。这个`Beans.instantiate()`方法仅当实例化的类有公有的构造函数时才会成功，否则，它会抛出IllegalAccessException异常。该方法使用了一个类装载器参数和一个需要实例化的类的名称参数。这个方法避免了使用反射权限的需要。\
      \  \n```java\npublic class Trusted {\n    Trusted() {\n    }\n\n    public static\
      \ <T> T create(Class<T> c) throws IOException, ClassNotFoundException {\n  \
      \      // Executes without exception only if there are public constructors\n\
      \        ClassLoader cl = new SafeClassLoader();\n        Object b = Beans.instantiate(cl,\
      \ c.getName());\n        return c.cast(b);\n    }\n}\n```"
    Advice_En: "**Compliant Solution (Private)**\n\nWhen you must use reflection,\
      \ make sure that the immediate caller (method) is isolated from hostile code\
      \ by declaring it private or final, as in this compliant solution:\n\n```java\n\
      class FieldExample {\n  // ...\n\n  private void zeroField(String fieldName)\
      \ {\n    // ...\n  }\n}\n```\n\nNote that when language access checks are overridden\
      \ using `java.lang.reflect.Field.setAccessible`, the immediate caller gains\
      \ access even to the private fields of other classes. To ensure that the security\
      \ manager will block attempts to access private fields of other classes, never\
      \ grant the permission `ReflectPermission` with action `suppressAccessChecks`.\n\
      \n**Compliant Solution (Nonreflection)**\n\nWhen a class must use reflection\
      \ to provide access to fields, it must also provide the same access using a\
      \ nonreflection interface. This compliant solution provides limited setter methods\
      \ that grant every caller the ability to zero out its fields without using reflection.\
      \ If these setter methods comply with all other rules or security policies,\
      \ the use of reflection also complies with this rule.\n\n```java\nclass FieldExample\
      \ {\n  // ...\n\n  public void zeroField(String fieldName) {\n    // ...\n \
      \ }\n\n  public void zeroI() {\n    this.i = 0;\n  }\n  public void zeroJ()\
      \ {\n    this.j = 0;\n  }\n}\n```\n**Compliant Solution (Access Reduction)**\n\
      \nThis compliant solution reduces the access of the `create()` method to package-private,\
      \ preventing a caller from outside the package from using that method to bypass\
      \ the language access checks to create an instance of the `Trusted` class. Any\
      \ caller that can create a `Trusted` class instance using reflection can simply\
      \ call the `Trusted()` constructor instead.\n\n```java\npackage Safe;\npublic\
      \ class Trusted {\n  Trusted() { } // Package-private constructor\n  static\
      \ <T> T create(Class<T> c)\n      throws InstantiationException, IllegalAccessException\
      \ {\n    return c.newInstance();\n  }\n}\n```\n\n**Compliant Solution (Security\
      \ Manager Check)**\n\nThis compliant solution uses the `getConstructors()` method\
      \ to check whether the class provided as an argument has public constructors.\
      \ The security issue is irrelevant when public constructors are present because\
      \ such constructors are already accessible even to malicious code. When public\
      \ constructors are absent, the `create()` method uses the security manager's\
      \ `checkPackageAccess()` method to ensure that all callers in the execution\
      \ chain have sufficient permissions to access classes and their respective members\
      \ defined in package `Safe`.\n\n```java\nimport java.beans.Beans;\nimport java.io.IOException;\n\
      package Safe;\n\npublic class Trusted  {\n  Trusted() { }\n\n  public static\
      \ <T> T create(Class<T> c)\n      throws InstantiationException, IllegalAccessException\
      \ {\n\n    if (c.getConstructors().length == 0) {  // No public constructors\n\
      \      SecurityManager sm = System.getSecurityManager();\n      if (sm != null)\
      \ {\n        // Throws an exception when access is not allowed\n        sm.checkPackageAccess(\"\
      Safe\");\n      }\n    }\n    return c.newInstance(); // Safe to return\n  }\n\
      }\n```\n\nThe disadvantage of this compliant solution is that the class must\
      \ be granted reflection permissions to permit the call to `getConstructors()`.\n\
      \n**Compliant Solution (`java.beans` Package)**\n\nThis compliant solution uses\
      \ the `java.beans.Beans` API to check whether the `Class` object being received\
      \ has any public constructors:\n\n```java\npublic class Trusted {\n  Trusted()\
      \ { }\n\n  public static <T> T create(Class<T> c)\n      throws IOException,\
      \ ClassNotFoundException {\n\n    // Executes without exception only if there\
      \ are public constructors\n    ClassLoader cl = new SafeClassLoader();\n   \
      \ Object b = Beans.instantiate(cl, c.getName());\n    return c.cast(b);\n  }\n\
      }\n```\n\nThe `Beans.instantiate()` method succeeds only when the class being\
      \ instantiated has a public constructor; otherwise, it throws an `IllegalAccessException`.\
      \ The method uses a class loader argument along with the name of the class to\
      \ instantiate. Unlike the previous compliant solution, this approach avoids\
      \ the need for any reflection permissions."
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 15. 平台安全性 (SEC)
      - type: DetailClassChin
        value: SEC05-J：不要使用反射来增加类、方法和字段的可访问性
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 15. Platform Security (SEC)
      - type: DetailClassChin
        value: SEC05-J:Do not use reflection to increase accessibility of classes,
          methods, or fields
    Default: not
    Description: "使用反射可以让Java程序对自身进行分析或修改。特别的，一个程序可以得到字段的值并且修改它们。Java的反射API包括了一个方法，这个方法可以通过反射来访问正常情况下不能够访问的字段。以下的代码输出类SomeClass的SomeObject对象的所有数据成员的名字和数值。\
      \  \n```java\nField fields[] = SomeClass.getDeclaredFields();\nfor (Field field\
      \ : fields) {\n    if (!Modifier.isPublic(field.getModifiers())) {\n       \
      \ field.setAccessible(true);\n    }\n    System.out.print(\"Field: \" + field.getName());\n\
      \    System.out.println(\", value: \" + field.get(someObject));\n}\n```  \n\
      一个字段可以使用以下的方式设置新的数值：  \n```java\nString newValue = reader.readLine();  \nfield.set(someObject,\
      \ returnValue(newValue, field.getType()));\n```  \n当使用了默认的安全管理器时候，它可以防止使用反射来访问那些正常情况下不能访问的字段。在这样的情况下，默认的安全管理器会抛出`java.security.AccessControlException`异常。可以在`java.lang.reflect.ReflectPermission`中使用`suppressAccessChecks`来覆写默认的行为。\
      \  \n  \n**例如**：尽管通常可以防止一个对象对私有字段的访问，也可以防止调用另一个类的私有方法，但是属于java.lang.reflect包的API函数允许一个对象在进行语言定义检查之后，执行这样的操作。然而，重要的是这些访问检查仅仅考虑对直接调用者的语言级别的可见性。因此，不明智的程序会出现非受信调用者提升特权的问题。\
      \  \n下面列出了在使用中需要注意的API列表。  \n镜像语言检查的API（APIs That Mirror Language Checks）：\n\
      ```java\njava.lang.Class.newInstance()  \njava.lang.reflect.Constructor.newInstance()\
      \  \njava.lang.reflect.Field.get*()  \njava.lang.reflect.Field.set*()  \njava.lang.reflect.Method.invoke()\
      \  \njava.util.concurrent.atomic.AtomicIntegerFieldUpdater.newUpdater()  \n\
      java.util.concurrent.atomic.AtomicLongFieldUpdater.newUpdater()  \njava.util.concurrent.atomic.AtomicReferenceFieldUpdater.newUpdater()\n\
      ```  \n`java.lang.reflect.Field`的方法`setAccessible()`和`getAccessible()`会用来指示JVM去覆写语言的访问检查，它们执行标准（或更为严格）的安全管理器检查，因此不会出现在这条规则中讨论的安全漏洞。然而，在使用这些方法时需要非常小心。其余的`get*()`和`set*()`字段反射方法仅仅会执行语言访问检查，并且是有漏洞的。\
      \  \n使用反射会将安全分析弄复杂，并且会比较容易的引入安全漏洞。因此，就算当它们可以使用的时候，开发者也可以避免使用这些反射API。当使用反射的时候，要特别的小心谨慎。特别的，反射不应当用来提供对类、方法和字段的访问，除非这些类、方法和数据成员本身不通过反射就不可以访问。例如：使用反射来访问或修改字段是不允许的，除非那些字段本身就可以被其他的方式访问和修改，例如通过setter和getter方法。\
      \  \n风险评估：错误的使用那些仅对直接调用者进行语音检查的API会破坏数据封装，泄露安全信息。  \n**不规范的代码样例1**：  \n```java\n\
      class FieldExample {\n    private int i = 3;\n    private int j = 4;\n\n   \
      \ public String toString() {\n        return \"FieldExample: i=\" + i + \",\
      \ j=\" + j;\n    }\n\n    public void zeroI() {\n        this.i = 0;\n    }\n\
      \n    public void zeroField(String fieldName) {\n        try {\n           \
      \ Field f = this.getClass().getDeclaredField(fieldName);\n            // Subsequent\
      \ access to field f passes language access checks\n            // because zeroField()\
      \ could have accessed the field via\n            // ordinary field references\n\
      \            f.setInt(this, 0);\n            // log appropriately or throw sanitized\
      \ exception; see EXC06-J\n        } catch (NoSuchFieldException ex) {\n    \
      \        // report to handler\n        } catch (IllegalAccessException ex) {\n\
      \            // report to handler\n        }\n    }\n\n    public static void\
      \ main(String[] args) {\n        FieldExample fe = new FieldExample();\n   \
      \     System.out.println(fe.toString());\n        for (String arg : args) {\n\
      \            fe.zeroField(arg);\n            System.out.println(fe.toString());\n\
      \        }\n    }\n}\n```  \n代码样例中：私有的字段i和j可以使用反射通过一个Field对象来修改。此外，任何类也都可以使用反射来通过`zeroField()`方法来修改这些字段。然而，仅有类FieldExample可以不通过反射来修改这些字段。\
      \  \n允许恶意的代码传递任意的成员字段给`zeroField()`会：  \n1）一个不合法或不可访问的字段名称抛出异常，从而泄露信息。  \n2）访问那些潜在的敏感信息，这些信息对zeroField()方法是可见的，但对攻击方法是隐藏的。\n\
      \n**不规范的代码样例2**：  \n样例2中，开发者认为可以防止在Safe包之外的代码创建一个特定类的新的实例。因此，Trusted类使用了一个包私有的构造方法。然而API是公有的，攻击者会将Trusted.class类自身做为参数传递给`creat()`方法，从而绕过防止包外部的代码调用包私有构造方法的语言检查。最后，这个`create()`方法会返回一个未经授权的Trusted类实例。\
      \  \n```java\npackage Safe;  \npublic class Trusted {\n    Trusted() {\n   \
      \ } // package private constructor\n\n    public static <T> T create(Class<T>\
      \ c) throws InstantiationException, IllegalAccessException {\n        return\
      \ c.newInstance();\n    }\n} \npackage Attacker;  \nimport Safe.Trusted;  \n\
      public class Attack {\n    public static void main(String[] args) throws InstantiationException,\
      \ IllegalAccessException {\n        System.out.println(Trusted.create(Trusted.class));\
      \ // succeeds\n    }\n}\n```\n在使用了安全管理器s的情况下，`Class.newInstance()`方法会抛出安全异常，这是在出现\
      \  \n(a)`s.checkMemberAccess(this, Member.PUBLIC)`拒绝创造这个类的新实例；  \n(b)对于调用者的类装载器ID\
      \ C1和当前的类装载ID C2，如果C1和C2不同，并且C1不是C2的基类，同时`checkMemberAccess(this, Member.PUBLIC)`会拒绝对当前类所在包的访问。`checkMemberAccess()`方法允许访问公有的字段和那些调用者有相同类装载器的类。然而，这种类装载器之间的比较是不够的，例如：所有的applets正确情况下共享相同的类装载器，因而，在这种情况下会允许恶意的applet通过安全检查。"
    Description_En: "Reflection enables a Java program to analyze and modify itself.\
      \ In particular, a program can discover the values of field variables and change\
      \ them [Forman 2005], [Sun 2002]. The Java reflection API includes a method\
      \ that enables fields that are normally inaccessible to be accessed under reflection.\
      \ The following code prints out the names and values of all fields of an object\
      \ `someObject` of class `SomeClass`:\n\n```java\nField fields[] = SomeClass.class.getDeclaredFields();\n\
      for (Field field : fields) {\n  if ( !Modifier.isPublic(field.getModifiers()))\
      \ {\n    field.setAccessible(true);\n  }\n  System.out.print(\"Field: \" + field.getName());\n\
      \  System.out.println(\", value: \" + field.get(someObject));\n}\n```\n\nA field\
      \ could be set to a new value as follows:\n\n```java\nString newValue = reader.readLine();\n\
      field.set(someObject, returnValue(newValue, field.getType()));\n```\n\nWhen\
      \ the default security manager is used, it prevents fields that are normally\
      \ inaccessible from being accessed under reflection. The default security manager\
      \ throws a `java.security.AccessControlException` in these circumstances. However,\
      \ `java.lang.reflect.ReflectPermission` can be granted with action `suppressAccessChecks`\
      \ to override this default behavior.\n\nFor example, the Java Virtual Machine\
      \ (JVM) normally protects private members of a class from being accessed by\
      \ an object of a different class. When a method uses reflection to access class\
      \ members (that is, uses the APIs belonging to the `java.lang.reflect` package),\
      \ the reflection uses the same restrictions. That is, a foreign object that\
      \ cannot access private members of a class normally also cannot use reflection\
      \ to access those members. However, a class with private members but also with\
      \ a public method that uses reflection to indirectly access those members can\
      \ inadvertently enable a foreign object to access those private members using\
      \ the public method, bypassing the intended accessibility restrictions. Consequently,\
      \ unwary programmers can create an opportunity for a privilege escalation attack\
      \ by untrusted callers.\n\nThe following table lists the APIs that should be\
      \ used with care [SCG 2009].\n\n| APIs That Mirror Language Checks |\n| - |\n\
      | `java.lang.Class.newInstance()` |\n| `java.lang.reflect.Constructor.newInstance()`\
      \ |\n| `java.lang.reflect.Field.get*()` |\n| `java.lang.reflect.Field.set*()`\
      \ |\n| `java.lang.reflect.Method.invoke()` |\n| `java.util.concurrent.atomic.AtomicIntegerFieldUpdater.newUpdater()`\
      \ |\n| `java.util.concurrent.atomic.AtomicLongFieldUpdater.newUpdater()` |\n\
      | `java.util.concurrent.atomic.AtomicReferenceFieldUpdater.newUpdater()` |\n\
      \nBecause the `setAccessible()` and `getAccessible()` methods of class `java.lang.reflect.Field`\
      \ are used to instruct the JVM to override the language access checks, they\
      \ perform standard (and more restrictive) security manager checks and consequently\
      \ lack the vulnerability discussed in this rule. Nevertheless, these methods\
      \ should be used only with extreme caution. The remaining `set*()` and `get*()`\
      \ field reflection methods perform only the language access checks and are vulnerable.\n\
      \nUse of reflection complicates security analysis and can easily introduce security\
      \ vulnerabilities. Consequently, programmers should avoid using the reflection\
      \ APIs when it is feasible to do so. Exercise extreme caution when the use of\
      \ reflection is necessary.\n\nIn particular, reflection must not be used to\
      \ provide access to classes, methods, and fields unless those items are already\
      \ accessible without the use of reflection. For example, the use of reflection\
      \ to access or modify fields is not allowed unless those fields are already\
      \ accessible and modifiable by other means, such as through getter and setter\
      \ methods.\n\nThis rule is similar to MET04-J. Do not increase the accessibility\
      \ of overridden or hidden methods, but it warns against using reflection, rather\
      \ than inheritance, to subvert accessibility.\n\n**Noncompliant Code Example**\n\
      \nIn this noncompliant code example, the private fields `i` and `j` can be modified\
      \ using reflection via a `Field` object. Furthermore, any class can modify these\
      \ fields using reflection via the `zeroField()` method. However, only class\
      \ `FieldExample` can modify these fields without the use of reflection.\n\n\
      Allowing hostile code to pass arbitrary field names to the `zeroField()` method\
      \ can\n\n* Leak information about field names by throwing an exception for invalid\
      \ or inaccessible field names (see ERR01-J. Do not allow exceptions to expose\
      \ sensitive information. This example complies with ERR01-J by catching the\
      \ relevant exceptions at the end of the method.\n* Access potentially sensitive\
      \ data that is visible to `zeroField()` but is hidden from the attacking method.\
      \ This privilege escalation attack can be difficult to find during code review\
      \ because the specific field or fields being accessed are controlled by strings\
      \ in the attacker's code rather than by locally visible source code.\n\n```java\n\
      class FieldExample {\n  private int i = 3;\n  private int j = 4;\n\n  public\
      \ String toString() {\n    return \"FieldExample: i=\" + i + \", j=\" + j;\n\
      \  }\n\n  public void zeroI() {\n    this.i = 0;\n  }\n\n  public void zeroField(String\
      \ fieldName) {\n    try {\n      Field f = this.getClass().getDeclaredField(fieldName);\n\
      \      // Subsequent access to field f passes language access checks\n     \
      \ // because zeroField() could have accessed the field via\n      // ordinary\
      \ field references\n      f.setInt(this, 0);\n      // Log appropriately or\
      \ throw sanitized exception; see EXC06-J\n    } catch (NoSuchFieldException\
      \ ex) {\n      // Report to handler\n    } catch (IllegalAccessException ex)\
      \ {\n      // Report to handler\n    }\n  }\n\n  public static void main(String[]\
      \ args) {\n    FieldExample fe = new FieldExample();\n    System.out.println(fe.toString());\n\
      \    for (String arg : args) {\n      fe.zeroField(arg);\n      System.out.println(fe.toString());\n\
      \    }\n  }\n}\n```\n\n**Noncompliant Code Example**\n\nIn this noncompliant\
      \ code example, the programmer intends that code outside the `Safe` package\
      \ should be prevented from creating a new instance of an arbitrary class. Consequently,\
      \ the `Trusted` class uses a package-private constructor. However, because the\
      \ API is public, an attacker can pass `Trusted.class` itself as an argument\
      \ to the `create()` method and bypass the language access checks that prevent\
      \ code outside the package from invoking the package-private constructor. The\
      \ `create()` method returns an unauthorized instance of the `Trusted` class.\n\
      \n```java\npackage Safe;\npublic class Trusted {\n  Trusted() { } // Package-private\
      \ constructor\n  public static <T> T create(Class<T> c)\n      throws InstantiationException,\
      \ IllegalAccessException {\n    return c.newInstance();\n  }\n}\n\npackage Attacker;\n\
      import Safe.Trusted;\n\npublic class Attack {\n  public static void main(String[]\
      \ args)\n      throws InstantiationException, IllegalAccessException {\n   \
      \ System.out.println(Trusted.create(Trusted.class)); // Succeeds\n  }\n}\n```\n\
      \nIn the presence of a security manager `s`, the `Class.newInstance()` method\
      \ throws a security exception when (a) `s.checkMemberAccess(this, Member.PUBLIC)`\
      \ denies creation of new instances of this class or (b) the caller's class loader\
      \ is not the same class loader or an ancestor of the class loader for the current\
      \ class, and invocation of `s.checkPackageAccess()` denies access to the package\
      \ of this class.\n\nThe `checkMemberAccess` method allows access to public members\
      \ and classes that have the same class loader as the caller. However, the class\
      \ loader comparison is often insufficient; for example, all applets share the\
      \ same class loader by convention, consequently allowing a malicious applet\
      \ to pass the security check in this case."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference: []
    Security: true
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141605'
    language: Java
  - Advice: 很多情况下，修复这一问题的最简单方法是让这个违反规则的对象实现Serializable接口。但对于复杂的对象来说，存储在Session中的对象，其传递闭包必须是可序列化的。如果对象A引用对象B，要将A存储到在Session中，那么A和B都必须实现Serializable接口。
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 49. 其他 (MSC)
      - type: DetailClassChin
        value: MSC08-J：不要将不可序列化的对象作为属性存储在HTTP会话中
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 49. Miscellaneous (MSC)
      - type: DetailClassChin
        value: MSC08-J:Do not store nonserializable objects as attributes in an HTTP
          session
    Default: true
    Description: "把一个不可序列化的对象作为HttpSession属性来储存会破坏应用程序的可靠性。一个JAVA EE应用程序可以利用多个JVM，以提高应用程序的可靠性和性能。为了显示给用户一个单个的应用程序，JAVA\
      \ EE容器可能在多个JVM之间复制HttpSession对象，所以当一个JVM不可用时，另一个JVM可以在不中断应用程序流程的情况下继续执行。为了使Session能够正常传递，作为属性存储在Session中的数值必须实现Serializable接口。\n\
      \n**例如**：下面代码片段中，将一个DataGlob对象添加到HttpSession中，但由于它是不可序列化的，因此该Session就再也不能被传递了。\n\
      ```java\npublic class DataGlob {\n    String globName;\n    String globValue;\n\
      \    public void addToSession(HttpSession session) {\n        session.setAttribute(\"\
      glob\", this);\n    }\n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference:
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 400
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 400
      - type: CWE
        value: 770:Allocation of Resources without Limits or Throttling
      - type: CWE
        value: 400:Uncontrolled Resource Consumption ("Resource Exhaustion")
    Security: not
    Tag: ''
    Type: cert
    Update: Description & Advice Copy from 02000010110001
    WhiteList: not
    id: 0102000010141808
    language: Java
  - Advice: "很多情况下，修复这一问题的最简单方法是让这个违反规则的对象实现Serializable接口。但对于复杂的对象来说，存储在Session中的对象，其传递闭包必须是可序列化的。如果对象A引用对象B，且对象A存储在Session中，那么A和B都必须实现Serializable接口。\
      \  \n  \n**例如**：下面代码片段中，DataGlob实现了Serializable接口。  \n```java\npublic class\
      \ DataGlob implements Serializable{  \n    private static final long serialVersionUID\
      \ = 4689763391876440157L;  \n    String globName;  \n    String globValue; \
      \ \n    public void addToSession(HttpSession session) {  \n        session.setAttribute(\"\
      glob\", this);  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】在JavaEE程序中不要在Session中存储非序列化对象
    Categories_En:
      Category: []
    Default: true
    Description: "把一个不可序列化的对象作为HttpSession属性来储存会破坏应用程序的可靠性。一个JAVA EE应用程序可以利用多个JVM，以提高应用程序的可靠性和性能。为了显示给最终用户一个单个的应用程序，JAVA\
      \ EE容器可能在多个JVM之间复制HttpSession对象，所以当一个JVM不可用时，另一个JVM可以在不中断应用程序流程的情况下接替步骤的执行。为了使Session复制能够正常运行，作为应用程序属性存储在Session中的数值必须实现Serializable接口。\
      \   \n  \n**例如**：下面代码片段中，将一个DataGlob对象添加到HttpSession中，但由于它不是可序列化的，因此该Session就再也不能被复制了。\
      \  \n```java\npublic class DataGlob {  \n    String globName;  \n    String\
      \ globValue;  \n    public void addToSession(HttpSession session) {  \n    \
      \    session.setAttribute(\"glob\", this);  \n    }  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Non-Serializable Object Stored in Session
    ENSubClassChin: J2EE Bad Practices
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: true
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '579:J2EE Bad Practices: Non-serializable Object Stored in Session'
      - type: CERT
        value: MSC08-J:Do not store nonserializable objects as attributes in an HTTP
          session
      - type: OWASP TOP 10
        value: 2010:A3 Broken Authentication and Session Management
      - type: OWASP TOP 10
        value: 2007:A7 Broken Authentication and Session Management
      - type: OWASP TOP 10
        value: 2004:A3 Broken Authentication and Session Management
    Security: true
    Tag: security,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150401'
    language: Java
  - Advice: "**规范的代码样例1**：Integer对象  \n当显式创建一个Integer对象时，这个对象只有唯一一个引用，并且它的内置锁不但区别于其他Integer对象，并且区别于具有同一个数值经过封装的整数。\
      \  \n```java\nint lock = 0;\nprivate final Integer Lock = new Integer(lock);\n\
      \npublic void doSomething() {\n    synchronized (Lock) {\n        // . . .\n\
      \    }\n}\n```  \n**规范的使用样例2**：String对象  \n一个字符串实例和一个字符串文本是不同的。一个实例拥有唯一一个引用，并且它自己的内置锁和其他的字符串对象或者文本不同。\
      \  \n```java\nprivate final String lock = new String(\"LOCK\");\n\npublic void\
      \ doSomething() {\n  synchronized (lock) {\n    // ...\n  }\n}\n```\n**规范的使用样例3**：私有不变锁对象\n\
      此解决方案在：私有不变锁对象上同步。 这是 `java.lang.Object` 实例有用的少数情况之一。\n```java\nprivate final\
      \ Object lock = new Object();\n\npublic void doSomething() {\n  synchronized\
      \ (lock) {\n    // ...\n  }\n}\n```"
    Advice_En: "**Compliant Solution (Integer)**\n\nThis compliant solution locks\
      \ on a nonboxed `Integer`, using a variant of the private lock object idiom.\
      \ The `doSomething()` method synchronizes using the intrinsic lock of the `Integer`\
      \ instance, `Lock`.\n\n```java\nprivate int count = 0;\nprivate final Integer\
      \ Lock = new Integer(count);\n\npublic void doSomething() {\n  synchronized\
      \ (Lock) {\n    count++;\n    // ...\n  }\n}\n```\n\nWhen explicitly constructed,\
      \ an `Integer` object has a unique reference and its own intrinsic lock that\
      \ is distinct not only from other `Integer` objects, but also from boxed integers\
      \ that have the same value. While this is an acceptable solution, it can cause\
      \ maintenance problems because developers can incorrectly assume that boxed\
      \ integers are also appropriate lock objects. A more appropriate solution is\
      \ to synchronize on a private final lock object as described in the final compliant\
      \ solution for this rule.\n**Compliant Solution (`String` Instance)**\n\nThis\
      \ compliant solution locks on a noninterned `String` instance.\n\n```java\n\
      private final String lock = new String(\"LOCK\");\n\npublic void doSomething()\
      \ {\n  synchronized (lock) {\n    // ...\n  }\n}\n```\n\nA `String` instance\
      \ differs from a `String` literal. The instance has a unique reference and its\
      \ own intrinsic lock that is distinct from other `String` object instances or\
      \ literals. Nevertheless, a better approach is to synchronize on a private final\
      \ lock object, as shown in the following compliant solution.\n\n**Compliant\
      \ Solution (Private Final Lock `Object`)**\n\nThis compliant solution synchronizes\
      \ on a private final lock object. This is one of the few cases in which a `java.lang.Object`\
      \ instance is useful.\n\n```java\nprivate final Object lock = new Object();\n\
      \npublic void doSomething() {\n  synchronized (lock) {\n    // ...\n  }\n}\n\
      ```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 09. 锁 (LCK)
      - type: DetailClassChin
        value: LCK01-J：不要基于可被重用的对象进行同步
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 09. Locking (LCK)
      - type: DetailClassChin
        value: LCK01-J:Do not synchronize on objects that may be reused
    Default: true
    Description: "错误的使用那些同步的基础数据类型是引发并发问题的一个常见原因。基于那些可被重用的对象进行同步，会导致死锁和不确定的行为。可重用的对象包括但不限于以下几种：Boolean、封装过的基础数据类型、进入字符串池的String对象、String。\
      \  \n**不规范的代码样例1**：Boolean类型。  \nBoolean类型不适合作为锁来使用，因为它只允许有两个值：true和false。包含相同数值的Boolean数据，并且在Java虚拟机中共享一个Boolean类实例。在这个列子中，initialized指向对应于值Boolean.FALSE的实例。如果在其他任何代码中，不经意的使用了具有这个Boolean类型进行同步，这个锁实例会被重用，并且系统会失去响应或者产生死锁。\
      \  \n风险评估：使用错误的对象类型进行锁定，会产生并发漏洞。  \n```java\nprivate final Boolean initialized\
      \ = Boolean.FALSE;\n\npublic void doSomething() {\n  synchronized (initialized)\
      \ {\n    // ...\n  }\n}\n```\n**不规范的代码样例2**： 封装后的基础数据类型。  \n封装过的基础数据类型会使用整型范围内的同一个实例，因此，它们会遇到与Boolean常量同样的重用问题。\
      \  \n```java\nprivate int count = 0;\nprivate final Integer Lock = count; //\
      \ Boxed primitive Lock is shared\n\npublic void doSomething() {\n  synchronized\
      \ (Lock) {\n    count++;\n    // ...\n  }\n}\n```\n**不规范的使用样例3**： intern()String对象。\
      \  \nJava API对类`java.lang.String`的方法`intern()`描述：当调用`intern`方法时，如果在池中已经存在一个等于这个String对象的字符串时，一般使用`equals(Object)`方法进行这个等于判断，然后返回池中的这个字符串。否则，这个字符串对象会被加入池中，并且返回指向这个字符串对象的引用。\
      \  \n因此，一个进入字符串池的String对象在Java虚拟机中的行为类似一个全局变量。当每一个对象的实例都持有自己的锁字段时，这些字段都会指向一个String常量。基于String常量的锁与基于Boolean常量的锁同样具有重用性的问题。\
      \  \n```java\nprivate final String lock = new String(\"LOCK\").intern();\n\n\
      public void doSomething() {\n  synchronized (lock) {\n    // ...\n  }\n}\n```\
      \  \n**不规范的使用样例4**： String。  \nString文本是常量并且会被自动`intern()`。因此，String文本的锁同样具有重用性的问题。\
      \  \n```java\n// This bug was found in jetty-6.1.3 BoundedThreadPool\nprivate\
      \ final String lock = \"LOCK\";\n\npublic void doSomething() {\n  synchronized\
      \ (lock) {\n    // ...\n  }\n}\n```"
    Description_En: "Misuse of synchronization primitives is a common source of concurrency\
      \ issues. Synchronizing on objects that may be reused can result in deadlock\
      \ and nondeterministic behavior. Consequently, programs must never synchronize\
      \ on objects that may be reused.\n\n**Noncompliant Code Example (`Boolean` Lock\
      \ Object)**\n\nThis noncompliant code example synchronizes on a `Boolean` lock\
      \ object.\n\n```java\nprivate final Boolean initialized = Boolean.FALSE;\n\n\
      public void doSomething() {\n  synchronized (initialized) {\n    // ...\n  }\n\
      }\n```\n\nThe `Boolean` type is unsuitable for locking purposes because it allows\
      \ only two values: true and false. Boolean literals containing the same value\
      \ share unique instances of the `Boolean` class in the Java Virtual Machine\
      \ (JVM). In this example, `initialized` refers to the instance corresponding\
      \ to the value `Boolean.FALSE`. If any other code were to inadvertently synchronize\
      \ on a `Boolean` literal with this value, the lock instance would be reused\
      \ and the system could become unresponsive or could deadlock.\n\n**Noncompliant\
      \ Code Example (Boxed Primitive)**\n\nThis noncompliant code example locks on\
      \ a boxed `Integer` object.\n\n```java\nprivate int count = 0;\nprivate final\
      \ Integer Lock = count; // Boxed primitive Lock is shared\n\npublic void doSomething()\
      \ {\n  synchronized (Lock) {\n    count++;\n    // ...\n  }\n}\n```\n\nBoxed\
      \ types may use the same instance for a range of integer values; consequently,\
      \ they suffer from the same reuse problem as `Boolean` constants. The wrapper\
      \ object are reused when the value can be represented as a byte; JVM implementations\
      \ are also permitted to reuse wrapper objects for larger ranges of values. While\
      \ use of the intrinsic lock associated with the boxed `Integer` wrapper object\
      \ is insecure; instances of the `Integer` object constructed using the `new`\
      \ operator (`new Integer(value)`) are unique and not reused. In general, locks\
      \ on any data type that contains a boxed value are insecure.\n\n**Noncompliant\
      \ Code Example (Interned `String` Object)**\n\nThis noncompliant code example\
      \ locks on an interned `String` object.\n\n```java\nprivate final String lock\
      \ = new String(\"LOCK\").intern();\n\npublic void doSomething() {\n  synchronized\
      \ (lock) {\n    // ...\n  }\n}\n```\n\nAccording to the Java API class `java.lang.String`\
      \ documentation [API 2006]:\n\n> When the `intern()` method is invoked, if the\
      \ pool already contains a string equal to this `String` object as determined\
      \ by the `equals(Object)` method, then the string from the pool is returned.\
      \ Otherwise, this `String` object is added to the pool and a reference to this\
      \ `String` object is returned.\n\nConsequently, an interned `String` object\
      \ behaves like a global variable in the JVM. As demonstrated in this noncompliant\
      \ code example, even when every instance of an object maintains its own `lock`\
      \ field, the fields all refer to a common `String` constant. Locking on `String`\
      \ constants has the same reuse problem as locking on `Boolean` constants.\n\n\
      Additionally, hostile code from any other package can exploit this vulnerability,\
      \ if the class is accessible. See rule LCK00-J. Use private final lock objects\
      \ to synchronize classes that may interact with untrusted code for more information.\n\
      \n**Noncompliant Code Example (`String` Literal)**\n\nThis noncompliant code\
      \ example locks on a final `String` literal.\n\n```java\n// This bug was found\
      \ in jetty-6.1.3 BoundedThreadPool\nprivate final String lock = \"LOCK\";\n\n\
      public void doSomething() {\n  synchronized (lock) {\n    // ...\n  }\n}\n```\n\
      \n`String` literals are constant and are automatically interned. Consequently,\
      \ this example suffers from the same pitfalls as the preceding noncompliant\
      \ code example."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141001'
    language: Java
  - Advice: "程序中应使用private、final的Object对象作为锁对象。  \n  \n**例如**：以下代码使用了private、final的Object对象作为锁对象。\
      \  \n```java\nprivate final Object lock = new Object();  \npublic void doSomething()\
      \ {  \n    synchronized (lock) {  \n        // ...  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 并发规范
      - type: DetailClassChin
        value: 【强制】不要基于可重用对象进行同步
    Categories_En:
      Category: []
    Default: true
    Description: "基于可能被重用的对象（如基础数据类型包装类和String）进行同步，会导致死锁和不确定的行为。  \n  \n**例如**：下面代码片段中锁定了一个final的字符串对象。\
      \  \n```java\nprivate static final String lock = \"LOCK\";  \npublic void doSomething()\
      \ {  \n    synchronized (lock) {  \n        // ...  \n    }  \n}\n```  \n当程序中创建一个新的字符串对象为`\"\
      LOCK\"`时，虚拟机会判断字符串常量池中是否存在该值的字符串，如果存在将返回这个字符串的地址给新的字符串对象，这将导致新的字符串对象控制了字符串锁对象。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: 规则文件没有
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: LCK01-J:Do not synchronize on objects than may be reused
    Security: not
    Tag: concurrency,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150304'
    language: Java
  - Advice: "\n3) 示例   \n    1、不符合规则的代码示例（布尔型的锁对象），该示例基于一个布尔型的锁对象实现同步。   \n    ```java\n\
      \    private final Boolean initialized = Boolean.FALSE;   \n    public void\
      \ doSomething() {   \n        synchronized (initialized) {   \n            //\
      \ ...   \n        }   \n    }\n    ```   \n    Boolean 类型不太适合作为锁来使用，因为它只允许两个值：true\
      \ 和 false。包含相同数值的 Boolean数据，并且在 Java 虚拟机中共享一个 Boolean 类实例。如果在其他任何代码中，不经意地使用了具有这个\
      \ Boolean 类型进行同步，这个锁实例会被重用，并且系统会失去响应或者产生死锁。   \n       \n    2、不符合规则的代码示例（封装过的基础数据类型），该示例使用封包的\
      \ Integer 对象实现锁操作。   \n    ```java\n    int lock = 0;   \n    private final\
      \ Integer Lock = lock; // Boxed primitive Lock is shared   \n    public void\
      \ doSomething() {   \n        synchronized (Lock) {\n            // . . .  \
      \  \n        }   \n    }\n    ```   \n    封装过的数据类型会使用整形范围内的同一个实例，因此，它们会遇到与 Boolean\
      \ 常数同样的重用问题。当将这个数据表示成一个字节时，会重用封装器对象。当使用一个和经过封包的 Interger 封装器对象联系在一起的内置锁时，会产生不安全性。\
      \   \n       \n    对应上述情形的符合规则的解决方案（整形）是：基于一个没有封包的整型来使用锁，这是使用私有锁对象技巧的一种变形。在\
      \ `doSomething()`方法中，使用 Integer 实例的内置锁 Lock 进行同步。   \n    ```java\n    int lock\
      \ = 0;   \n    private final Integer Lock = new Integer(lock);   \n    public\
      \ void doSomething() {   \n        synchronized (Lock) {   \n            //\
      \ ...\n        }\n    }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.9.2 不要基于那些可能被重用的对象进行同步
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序不要基于那些会被重用的对象进行同步。   \n2) 原理   \n错误地使用那些同步的基础数据类型是并发问题的一个常见来源。基于那些可能被重用的对象进行同步，会导致死锁和不确定的行为。因此，程序不要基于那些会被重用的对象进行同步。\
      \   "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: LCK01-J:Do not synchronize on objects than may be reused
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160080
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：Integer对象  \n```java\nint lock = 0;\
      \  \nprivate final Integer Lock = new Integer(lock);  \n    public void doSomething()\
      \ {  \n    synchronized (Lock) {  \n    // . . .  \n    }  \n}\n```  \n当显式创建一个Integer对象时，这个对象只有唯一一个引用，并且它的内置锁不但区别于其他Integer对象，并且区别于具有同一个数值经过封装的整数。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.2 不要基于可被重用的对象进行同步
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：错误的使用基础数据类型进行同步是引发并发问题的一个常见原因。基于那些可被重用的对象进行同步，会导致死锁和不确定的行为。可重用的对象包括但不限于以下几种：Boolean、封装过的基础数据类型、进入字符串池的String对象。\
      \  \n对于不要基于可被重用的对象进行同步的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：Boolean类型  \n```java\n\
      private final Boolean initialized = Boolean.FALSE;  \npublic void doSomething()\
      \ {  \n    synchronized (initialized) {  \n    // . . .  \n    }  \n}\n``` \
      \ \nBoolean类型不适合作为锁来使用，因为它只允许有两个值：true和false。包含相同数值的Boolean数据，并且在Java虚拟机中共享一个Boolean类实例。在这个例子中，`initialized`指向对应于值Boolean.FALSE的实例。如果在其他任何代码中，不经意的使用了具有这个Boolean类型进行同步，这个锁实例会被重用，并且系统会失去响应或者产生死锁。使用错误的对象类型进行锁定，会产生并发漏洞。封装过的基础数据类型会使用整型范围内的同一个实例，它们会遇到与Boolean常量同样的重用问题。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: LCK01-J:Do not synchronize on objects than may be reused
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170034'
    language: Java
  - Advice: 程序不应频繁调用`System.gc()`进行垃圾回收，应让Java虚拟机进行内存管理。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【强制】不要调用System.gc()
    Categories_En:
      Category: []
    Default: true
    Description: 程序频繁调用`System.gc()`将会降低系统性能。
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Call to System.gc()
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 730:OWASP Top Ten 2004 Category A9 - Denial of Service
      - type: OWASP TOP 10
        value: 2004:A9 Application Denial of Service
      - type: SJ/T 11683-2017
        value: 6.5.5 调用System.gc方法
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150101'
    language: Java
  - Advice: 符合规范解决方案简单地从Stuff类中删除`main()`方法，使攻击者失去这个入口点。
    Advice_En: A compliant solution simply removes the `main()` method from the Stuff
      class, depriving attackers of this entry point.
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 16. 运行环境 (ENV)
      - type: DetailClassChin
        value: ENV06-J：产品代码不能包含调试入口
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 16. Runtime Environment (ENV)
      - type: DetailClassChin
        value: ENV06-J:Production code must not contain debugging entry points
    Default: true
    Description: "根据J2EE不良实践:遗留的调试代码 [Hewlett-Packard 2015]:\n一种常见的开发实践是添加专门为调试或测试目的而设计的“后门”代码，这些代码不打算被发送或部署到应用程序中。这些后门入口点会造成安全风险，因为在设计或测试过程中，它们不会被考虑，并且在应用程序的预期运行条件之外。\n\
      最常见的被遗忘的调试代码示例是在web应用程序中出现的`main()`方法。\n**不规范的代码样例1**：\n```java\nclass Stuff\
      \ {\n    private static final bool DEBUG = False;\n\n    // Other fields and\
      \ methods\n    public static void main(String args[]) {\n        Stuff.DEBUG\
      \ = True;\n        Stuff stuff = new Stuff();\n        // Test stuff\n    }\n\
      }\n```\n在这个不合规的代码示例中：Stuff类有一个`main()`函数,测试它的方法。虽然用于调试,如果这个函数是在生产环境的代码(**例如**：一个web应用程序),攻击者可以调用`Stuff.main()`直接获得Stuff的测试方法。"
    Description_En: "According to J2EE Bad Practices: Leftover Debug Code [Hewlett-Packard\
      \ 2015]:\nA common development practice is to add \"back door\" code specifically\
      \ designed for debugging or testing purposes that is not intended to be shipped\
      \ or deployed with the application. When this sort of debug code is accidentally\
      \ left in the application, the application is open to unintended modes of interaction.\
      \ These back door entry points create security risks because they are not considered\
      \ during design or testing and fall outside of the expected operating conditions\
      \ of the application.\nThe most common example of forgotten debug code is a\
      \ main() method appearing in a web application. Although this is an acceptable\
      \ practice during product development, classes that are part of a production\
      \ J2EE application should not define a main().\n**Noncompliant Code Example1**：\n\
      ```java\nclass Stuff {\n    private static final bool DEBUG = False;\n\n   \
      \ // Other fields and methods\n    public static void main(String args[]) {\n\
      \        Stuff.DEBUG = True;\n        Stuff stuff = new Stuff();\n        //\
      \ Test stuff\n    }\n}\n```\nIn this noncompliant code example, the Stuff class\
      \ has a main() function that tests its methods. Although useful for debugging,\
      \ if this function is left in production code (for a web application, for example),\
      \ an attacker can invoke Stuff.main() directly, gaining access to Stuff's test\
      \ methods."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 489:Leftover Debug Code
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
    Security: true
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141706'
    language: Java
  - Advice: 系统在发布之前应删除测试代码。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要在JavaEE程序遗留调试代码
    Categories_En:
      Category: []
    Default: true
    Description: "应用程序中的测试代码会建立一些意想不到的入口，这些入口可能会被攻击者作为\"后门\"进行利用  \n  \n**例如**：JAVA\
      \ EE程序中的`main()`方法。  \n```java\npublic static void main(String[] args) {  \n\
      \    ...  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Leftover Debug Code
    ENSubClassChin: J2EE Bad Practices
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 489:Leftover Debug Code
      - type: CERT
        value: ENV06-J:Production code must not contain debugging entry points
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
    Security: true
    Tag: security,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150403'
    language: Java
  - Advice: "程序必须确保从int、long类型转换为float类型，从long类型转换为double类型，不会造成丢失所需精度。\n**规范的代码样例1**：\
      \  \n方法`subFloatFromInt()`对传入的参数进行范围检查。  \n```java\nstrictfp class WideSample\
      \ {\n    public static int subFloatFromInt(int op1, float op2) throws ArithmeticException\
      \ {\n        // The significand can store at most 23 bits\n        if ((op1\
      \ > 0x007FFFFF) || (op1 < -0x800000)) {\n            throw new ArithmeticException(\"\
      Insufficient precision\");\n        }\n        return op1 - (int) op2;\n   \
      \ }\n\n    public static void main(String[] args) {\n        int result = subFloatFromInt(1234567890,\
      \ 1234567890);\n        System.out.println(result);\n    }\n}\n```\n**特例**\n\
      NUM13-J-EX0：当合适的数值分析表明损失最小有效位的精度是可以接受的，允许从积分类型转换到浮点类型而不进行范围检查。"
    Advice_En: "**Compliant Solution (`ArithmeticException`)**\n\nThis compliant solution\
      \ range checks the argument of the integer argument (`op1`) to ensure it can\
      \ be represented as a value of type `float` without a loss of precision:\n\n\
      ```java\nstrictfp class WideSample {\n  public static int subFloatFromInt(int\
      \ op1, float op2)\n                    throws ArithmeticException {\n\n    //\
      \ The significand can store at most 23 bits\n    if ((op2 > 0x007fffff) || (op2\
      \ < -0x800000)) {\n      throw new ArithmeticException(\"Insufficient precision\"\
      );\n    }\n\n    return op1 - (int)op2;\n  }\n\n  public static void main(String[]\
      \ args) {\n    int result = subFloatFromInt(1234567890, 1234567890);\n    System.out.println(result);\n\
      \  }\n}\n```\n\nIn this example, the `subFloatFromInt()` method throws `ArithmeticException`.\
      \ This general approach, with appropriate range checks, can be used for conversions\
      \ from `long` to either `float` or `double`.\n**Exceptions**\nNUM13-J-EX0: Conversion\
      \ from integral types to floating-point types without a range check is permitted\
      \ when suitable numerical analysis demonstrates that the loss of the least significant\
      \ bits of precision is acceptable."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 03. 数据类型与运算 (NUM)
      - type: DetailClassChin
        value: NUM13-J：转换基本整数类型至浮点数类型时应避免精度损失
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 03. Numeric Types and Operations (NUM)
      - type: DetailClassChin
        value: NUM13-J:Avoid loss of precision when converting primitive integers
          to floating-point
    Default: true
    Description: "以下 19 种基本类型的特定转换称为*扩展基本类型转换*：\n\n* `byte` 到 `short`, `int`, `long`,\
      \ `float`, 或`double`\n* `short` 到`int`, `long`, `float`, 或`double`\n* `char`\
      \ to `int`, `long`, `float`, 或`double`\n* `int` 到`long`, `float`, 或`double`\n\
      * `long` 到`float` 或r `double`\n* `float` 到`double`\n\n从 int 或 long 到 float 或从\
      \ long 到 double 的转换会导致精度损失（丢失最低有效位）。在这些情况下，生成的浮点值是整数值的舍入版本，使用 IEEE 754 舍入到最近模式。尽管有这种精度损失，Java\
      \ 语言规范 (JLS) 要求转换和舍入默认发生，也就是说，没有任何运行时异常（参见 JLS，§5.1.2，“Widening Primitive Conversion”[JLS\
      \ 2015]）。从小于 int 的整数类型到浮点类型的转换以及从 int 到 double 的转换永远不会导致精度损失。因此，程序必须确保从 int\
      \ 或 long 到浮点类型或从 long 到 double 的转换不会导致所需精度的损失。\n请注意，从 float 到 double 的转换也可能会丢失有关转换值的整体大小的信息（请参阅\
      \ NUM53-J。使用 strictfp 修饰符实现跨平台的浮点计算一致性以获取更多信息）。\n\n**不规范的代码样例1**\n在这个不规范的代码示例中，两个相同的大整数作为参数传递给\
      \ `subFloatFromInt()` 方法。 第二个参数被强制转换为浮点数，再转换回 `int`，并作为减数减去。 结果作为 `int `类型的值返回。\n\
      \n由于精度损失，这种方法可能会产生意想不到的结果。 在 FP-strict 模式下，`float` 类型的值有 23 个尾数位、一个符号位和一个 8\
      \ 位指数（请参阅 NUM53-J。使用 strictfp 修饰符实现跨平台的浮点计算一致性以了解有关 FP-strict 的更多信息）。 指数允许浮点类型表示比\
      \ int 类型更大的范围。 但是，23 位尾数意味着 `float `仅支持表示适合 23 位的整数的精确表示； `float `仅支持该范围之外的整数的近似表示。\n\
      \n```java\nstrictfp class WideSample {\n  public static int subFloatFromInt(int\
      \ op1, float op2) {\n    return op1 - (int)op2;\n  }\n\n  public static void\
      \ main(String[] args) {\n    int result = subFloatFromInt(1234567890, 1234567890);\n\
      \    // This prints -46, not 0, as may be expected\n    System.out.println(result);\n\
      \  }\n}\n```\n\n请注意，从 `long` 到 `float` 或 `double` 的转换可能会导致类似的精度损失。"
    Description_En: "The following 19 specific conversions on primitive types are\
      \ called the *widening primitive conversions*:\n\n* `byte` to `short`, `int`,\
      \ `long`, `float`, or `double`\n* `short` to `int`, `long`, `float`, or `double`\n\
      * `char` to `int`, `long`, `float`, or `double`\n* `int` to `long`, `float`,\
      \ or `double`\n* `long` to `float` or `double`\n* `float` to `double`\n\nConversion\
      \ from `int` or `long` to `float` or from `long` to `double` can lead to loss\
      \ of precision (loss of least significant bits). In these cases, the resulting\
      \ floating-point value is a rounded version of the integer value, using IEEE\
      \ 754 round-to-nearest mode. Despite this loss of precision, *The Java Language\
      \ Specification* (JLS) requires that the conversion and rounding occur silently,\
      \ that is, without any runtime exception (see the JLS, §5.1.2, &#34;Widening\
      \ Primitive Conversion&#34;) [JLS 2015], for more information). Conversions\
      \ from integral types smaller than `int` to a floating-point type and conversions\
      \ from `int` to `double` can never result in a loss of precision. Consequently,\
      \ programs must ensure that conversions from an `int` or `long` to a floating-point\
      \ type or from `long` to `double` do not result in a loss of required precision.\n\
      \nNote that conversions from `float` to `double` can also lose information about\
      \ the overall magnitude of the converted value (see NUM53-J. Use the strictfp\
      \ modifier for floating-point calculation consistency across platforms for additional\
      \ information).\n\n**Noncompliant Code Example**\n\nIn this noncompliant code\
      \ example, two identical large integer literals are passed as arguments to the\
      \ `subFloatFromInt()` method. The second argument is coerced to `float`, cast\
      \ back to `int`, and subtracted from a value of type `int`. The result is returned\
      \ as a value of type `int`.\n\nThis method could have unexpected results because\
      \ of the loss of precision. In FP-strict mode, values of type float have 23\
      \ mantissa bits, a sign bit, and an 8-bit exponent (see NUM53-J. Use the strictfp\
      \ modifier for floating-point calculation consistency across platforms for more\
      \ information about FP-strict mode). The exponent allows type float to represent\
      \ a larger range than that of type int. However, the 23-bit mantissa means that\
      \ float supports exact representation only of integers whose representation\
      \ fits within 23 bits; float supports only approximate representation of integers\
      \ outside that range.\n\n```java\nstrictfp class WideSample {\n  public static\
      \ int subFloatFromInt(int op1, float op2) {\n    return op1 - (int)op2;\n  }\n\
      \n  public static void main(String[] args) {\n    int result = subFloatFromInt(1234567890,\
      \ 1234567890);\n    // This prints -46, not 0, as may be expected\n    System.out.println(result);\n\
      \  }\n}\n```\n\nNote that conversions from `long` to either `float` or `double`\
      \ can lead to similar loss of precision."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140513'
    language: Java
  - Advice: "**规范的代码样例1**：  \n声明方法为final的。  \n```java\nclass Super {  \n  protected\
      \ final void doLogic() {  \n    System.out.println(\"Super invoked\");  \n \
      \ }  \n}\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution declares the `doLogic()`\
      \ method final to prevent malicious overriding:\n\n```java\nclass Super {\n\
      \  protected final void doLogic() { // Declare as final\n    System.out.println(\"\
      Super invoked\");\n    // Do sensitive operations\n  }\n}\n```\n\n**Exceptions**\n\
      MET04-J-EX0: For classes that implement the `java.lang.Cloneable` interface,\
      \ the accessibility of the `Object.clone()` method should be increased from\
      \ `protected` to `public` [SCG 2009]."
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 06. 方法 (MET)
      - type: DetailClassChin
        value: MET04-J：不要增加可被覆写方法和被隐藏方法的可访问性
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 06. Methods (MET)
      - type: DetailClassChin
        value: MET04-J:Do not increase the accessibility of overridden or hidden methods
    Default: true
    Description: "如果增加了被覆写方法和被隐藏方法的可访问性，恶意子类将拥有这些受限制的方法更大的访问权限。因此，程序只能在必要的时候对方法进行覆写，并且必须尽可能的将方法声明为final以避免恶意扩展。如果不能将方法声明为final的，程序必须防止增加被覆写方法的访问性。\
      \  \n覆盖或隐藏方法的访问修饰符必须至少提供与覆盖或隐藏方法一样多的访问权限（Java* 语言规范*，第 8.4.8.3 节，“覆盖和隐藏的要求”[JLS\
      \ 2015]）。 下表列出了允许的访问。\n\n| Overridden/Hidden Method Modifier | Overriding/Hiding\
      \ Method Modifier |\n| - | - |\n| `public` | `public` |\n| `protected` | `protected`\
      \ or `public` |\n| default | default or `protected` or `public` |\n| `private`\
      \ | Cannot be overridden |\n\n**不规范的代码样例1**：\n这个不规范的代码示例演示了恶意子类`Sub`如何覆盖超类的`doLogic()`方法并增加覆盖方法的可访问性。\
      \ `Sub` 的任何用户都可以调用 `doLogic` 方法，因为基类 `Super` 将其定义为 `protected`，因此允许类 `Sub` 通过声明自己的版本来增加\
      \ `doLogic()` 的可访问性为公开的方法。\n\n```java\nclass Super {\n  protected void doLogic()\
      \ {\n    System.out.println(\"Super invoked\");\n  }\n}\n\npublic class Sub\
      \ extends Super {\n  public void doLogic() {\n    System.out.println(\"Sub invoked\"\
      );\n    // Do sensitive operations\n  }\n}\n```"
    Description_En: "Increasing the accessibility of overridden or hidden methods\
      \ permits a malicious subclass to offer wider access to the restricted method\
      \ than was originally intended. Consequently, programs must override methods\
      \ only when necessary and must declare methods final whenever possible to prevent\
      \ malicious subclassing. When methods cannot be declared final, programs must\
      \ refrain from increasing the accessibility of overridden methods.\n\nThe access\
      \ modifier of an overriding or hiding method must provide at least as much access\
      \ as the overridden or hidden method (The Java* Language Specification*, §8.4.8.3,\
      \ \"Requirements in Overriding and Hiding\" [JLS 2015]). The following table\
      \ lists the allowed accesses.\n\n| Overridden/Hidden Method Modifier | Overriding/Hiding\
      \ Method Modifier |\n| - | - |\n| `public` | `public` |\n| `protected` | `protected`\
      \ or `public` |\n| default | default or `protected` or `public` |\n| `private`\
      \ | Cannot be overridden |\n\n**Noncompliant Code Example**\n\nThis noncompliant\
      \ code example demonstrates how a malicious subclass `Sub` can both override\
      \ the `doLogic()` method of the superclass and increase the accessibility of\
      \ the overriding method. Any user of `Sub` can invoke the `doLogic` method because\
      \ the base class `Super` defines it to be `protected`, consequently allowing\
      \ class `Sub` to increase the accessibility of `doLogic()` by declaring its\
      \ own version of the method to be public.\n\n```java\nclass Super {\n  protected\
      \ void doLogic() {\n    System.out.println(\"Super invoked\");\n  }\n}\n\npublic\
      \ class Sub extends Super {\n  public void doLogic() {\n    System.out.println(\"\
      Sub invoked\");\n    // Do sensitive operations\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 487:Reliance on Package-level Scope
    Security: true
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140704'
    language: Java
  - Advice: "对应的符合规定的解决方案是：将方法`doLogic()`声明为 final，这避免恶意覆写。   \n    ```java\n    class\
      \ Super {   \n        protected final void doLogic() { // declare as final System.out.println(\"\
      Super invoked\");   \n           // Do sensitive operations   \n        }  \
      \ \n    }   \n    ```  \n4) 特例   \n对于实现了`java.lang.Cloneable`接口的类，应该将方法 `Object.clone()`的可访问性从\
      \ protected 增加为 public。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.6.5 不要增加被覆写方法和被隐藏方法的可访问性
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序只能在必要的时候对方法进行覆写，并且必须尽可能地将方法声明为 final以避免恶意扩展。如果不能将方法声明为\
      \ final的，程序必须防止增加被覆写方法的可访问性。   \n2) 原理   \n如果增加了被覆写方法和被隐藏方法的可访问性，恶意子类将拥有这些受限制的方法比预计中更大的访问权限。覆写或隐藏方法的访问修饰符必须提供最少和被覆写或被隐藏方法一样的可访问性。\
      \   \n3) 示例   \n    这个不符合规则的代码示例：演示了恶意子类 Sub 如何覆写基类中的方法`doLogic()`,并增加了覆写方法的可访问性。因为基类\
      \ Super 定义方法`doLogic()`为 protected 的，这允许子类 Sub 定义该 方法为public 的，从而增加了`doLogic()`的可访问性。因此，任何\
      \ Sub 的使用者都可以调用此方法。   \n    ```java\n    class Super {\n        protected void\
      \ doLogic() {\n            System.out.println(\"Super invoked\");\n        }\n\
      \    \n        public class Sub extends Super {\n            public void doLogic()\
      \ {\n                System.out.println(\"Sub invoked\");\n                //\
      \ Do sensitive operations\n            }\n        }\n    }\n    ```    \n  \
      \     \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 487:Reliance on Package-level Scope
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160054
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nclass Super {\n    protected\
      \ final void doLogic() {\n        System.out.println(\"Super invoked\");\n \
      \   }\n}\n```  \n声明方法为final的。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.1 面向对象程序安全
      - type: DetailClassChin
        value: A.3.1.6 不要增加可被覆写方法和被隐藏方法的可访问性
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：如果增加了被覆写方法和被隐藏方法的可访问性，恶意子类将拥有这些受限制的方法更大的访问权限。因此，程序只能在必要的时候对方法进行覆写，并且必须尽可能的将方法声明为final以避免恶意扩展。如果不能将方法声明为final的，程序必须防止增加被覆写方法的访问性。扩展允许弱化可访问性的限制，这会危及Java应用的安全性。\
      \  \n对于不要增加可被覆写方法和被隐藏方法的可访问性的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\n\
      class Super {\n    protected void doLogic() {\n        System.out.println(\"\
      Super invoked\");\n    }\n}\n\npublic class Sub extends Super {\n    public\
      \ void doLogic() {\n        System.out.println(\"Sub invoked\");\n        //\
      \ Do sensitive operations\n    }\n}\n```  \n对于实现了`java.lang.Cloneable`接口的类，应该将方法`Object.clone()`的可访问性增加为public。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 487:Reliance on Package-level Scope
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170032'
    language: Java
  - Advice: "将Session的失效时间设置为30分钟或更少，既能使用户在一段时间内与应用程序互动，又提供了一个限制窗口攻击的合理范围。  \n  \n\
      **例如**：下面代码片段中，将Session失效时间设置为30分钟。  \n```java\nHttpSession sesssion = request.getSession(true);\
      \  \nsesssion.setMaxInactiveInterval(30*60);\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要在JavaEE程序中设置永远不会超时的Session
    Categories_En:
      Category: []
    Default: true
    Description: "在JAVA EE程序中，设置了一个永远都不会过期的会话不仅会给恶意攻击者提供更多的机会，并且这样做意味着`session`对象会永远存在web服务器的内存空间中，当会话量足够大时可能会导致拒绝服务攻击。\
      \  \n  \n**例如**： 下面代码为设置了会话永远不会过期。  \n```java\nHttpSession sesssion = request.getSession(true);\
      \  \nsesssion.setMaxInactiveInterval(-1);\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Insufficient Session Expiration
    ENSubClassChin: J2EE Bad Practices
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: true
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 613:Insufficient Session Expiration
      - type: OWASP TOP 10
        value: 2017:A2 Broken Authentication
      - type: OWASP TOP 10
        value: 2013:A2 Broken Authentication and Session Management
      - type: OWASP TOP 10
        value: 2010:A3 Broken Authentication and Session Management
      - type: OWASP TOP 10
        value: 2007:A7 Broken Authentication and Session Management
      - type: OWASP TOP 10
        value: 2004:A3 Broken Authentication and Session Management
      - type: GB/T 34944-2017 Java
        value: 时间和状态:会话永不过期
      - type: PCI DSS
        value: 3.0:Requirement 8.1.8
      - type: PCI DSS
        value: 3.1:Requirement 8.1.8
      - type: PCI DSS
        value: 3.2:Requirement 8.1.8
      - type: PCI DSS
        value: 3.2.1:Requirement 8.1.8
    Security: true
    Tag: security,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150405'
    language: Java
  - Advice: "**规范的代码样例1（Sanitization）**：\n符合规则的方案会对非受信的用户输入进行净化，这种净化只允许一小组列入白名单的字符出现在参数中，并传给`Runtime.exec()`方法，其他所有的字符都会被排除掉。\
      \  \n ```java\n// ...    \nif (!Pattern.matches(\"[0-9A-Za-z@.]+\", dir)) {\
      \    \n    // Handle error    \n}    \n// ...\n```   \n尽管这是一个符合规则的方案，这个净化方案会拒绝合法的目录。同时，因为命令行解释器调用是依赖于系统的，所以很难有一个方案可以应付所有Java程序可以运行的平台上的命令行注入。\
      \  \n**规范的代码样例2（限制用户选择）**：\n这个符合规则的方案通过只向`Runtime.exec()`方法输入那些受信的字符串来防止命令注入。当用户可以控制使用哪一个字符串时，用户就可以不向`Runtime.exec()`方法直接提供字符串数据。\
      \  \n```java\n// ...    \nString dir = null;\n// only allow integer choices\n\
      int number = Integer.parseInt(System.getproperty(\"dir\"));\nswitch (number)\
      \ {\ncase 1:\n    dir = \"data1\";\n    break; // Option 1\ncase 2:\n    dir\
      \ = \"data2\";\n    break; // Option 2\ndefault: // invalid\n    break;\n}\n\
      if (dir == null) {\n    // handle error\n}\n```   \n这个方案将可能罗列出的目录直接写出来了。  \n\
      如果有许多可用目录这个方案很快会变得不好管理。一个更富可伸缩性的方案是从一个属性文件中读取所有允许的目录至java.util.Properties对象中。\
      \  \n**规范的代码样例3（避免使用Runtime.exec()）**： \n```java\nimport java.io.File;\nclass\
      \ DirList {\n    public static void main(String[] args) throws Exception {\n\
      \        File dir = new File(System.getProperty(\"dir\"));\n        if (!dir.isDirectory())\
      \ {\n            System.out.println(\"Not a directory\");\n        } else {\n\
      \            for (String file : dir.list()) {\n                System.out.println(file);\n\
      \            }\n        }\n    }\n} \n``` \n当通过执行系统命令可以完成的任务也可以用其他方式完成时，最好避免用执行系统命令的方式。这个符合规则的方案使用`File.list()`方法来提供目录列表，从而消除了命令或参数注入攻击发生的可能性。"
    Advice_En: "**Compliant Solution (Sanitization)**\n\nThis compliant solution sanitizes\
      \ the untrusted user input by permitting only a small group of whitelisted characters\
      \ in the argument that will be passed to `Runtime.exec()`; all other characters\
      \ are excluded.\n\n```java\n// ...\nif (!Pattern.matches(\"[0-9A-Za-z@.]+\"\
      , dir)) {\n  // Handle error\n}\n// ...\n```\n\nAlthough it is a compliant solution,\
      \ this sanitization approach rejects valid directories. Also, because the command\
      \ interpreter invoked is system dependent, it is difficult to establish that\
      \ this solution prevents command injections on every platform on which a Java\
      \ program might run.\n\n**Compliant Solution (Restricted User Choice)**\n\n\
      This compliant solution prevents command injection by passing only trusted strings\
      \ to `Runtime.exec()`. The user has control over which string is used but cannot\
      \ provide string data directly to `Runtime.exec()`.\n\n```java\n// ...\nString\
      \ dir = null;\n\nint number = Integer.parseInt(System.getProperty(\"dir\"));\
      \ // Only allow integer choices\nswitch (number) {\n  case 1:\n    dir = \"\
      data1\";\n    break; // Option 1\n  case 2:\n    dir = \"data2\";\n    break;\
      \ // Option 2\n  default: // Invalid\n    break;\n}\nif (dir == null) {\n  //\
      \ Handle error\n}\n```\n\nThis compliant solution hard codes the directories\
      \ that may be listed.\n\nThis solution can quickly become unmanageable if you\
      \ have many available directories. A more scalable solution is to read all the\
      \ permitted directories from a properties file into a `java.util.Properties`\
      \ object.\n\n**Compliant Solution (Avoid `Runtime.exec()`)**\n\nWhen the task\
      \ performed by executing a system command can be accomplished by some other\
      \ means, it is almost always advisable to do so. This compliant solution uses\
      \ the `File.list()` method to provide a directory listing, eliminating the possibility\
      \ of command or argument injection attacks.\n\n```java\nimport java.io.File;\n\
      \nclass DirList {\n  public static void main(String[] args) throws Exception\
      \ {\n    File dir = new File(System.getProperty(\"dir\"));\n    if (!dir.isDirectory())\
      \ {\n      System.out.println(\"Not a directory\");\n    } else {\n      for\
      \ (String file : dir.list()) {\n        System.out.println(file);\n      }\n\
      \    }\n  }\n}\n```"
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 00. 输入验证和数据处理 (IDS)
      - type: DetailClassChin
        value: IDS07-J：净化传递给Runtime.exec()方法的非受信数据
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 00. Input Validation and Data Sanitization (IDS)
      - type: DetailClassChin
        value: IDS07-J:Sanitize untrusted data passed to the Runtime.exec() method
    Default: true
    Description: "外部程序通常被系统调用来完成某种需要的功能。这是一种重用的形式，也被认为是一种简单基于组件的软件工程方法。在应用没有净化非受信的输入并且在执行外部程序时使用这种数据，就会导致产生命令和参数注入漏洞。\
      \  \n每一个Java应用都有一个唯一的Runtime类的实例，通过它可以提供一个应用和应用运行环境的接口。当前的Runtime对象可以通过`Runtime.getRuntime()`方法获得。`Runtime.getRuntime()`的语义定义并不严格，所以最好仅仅使用它的那些必要的行为，然而，在通常情况下，它应当被命令直接调用，而不应通过shell来调用。如果需要使用shell来调用它，可以在POSIX中使用`/bin/sh`或者在Windows平台中使用cmd.exe的方式。作为`exec()`的另一种形式，它会使用StringTokenizer来分隔从命令行读入的字符串。在Windows平台中，在处理这些符号时，它们会被连接成一个单一的参数字符串。\
      \  \n因而，除非显式地调用命令行解释器，否则是不会产生命令行注入攻击的。然而，当参数中包含那些以空格、双引号或者其他以- /开头的用来表示分支的字符时，就可能发生参数注入攻击。\
      \  \n任何源于程序受信边界之外的字符串数据，在当前平台作为命令来执行之前，都必须经过净化。  \n**不规范的代码样例1（Windows）**：\n\
      ```java\nclass DirList {\n  public static void main(String[] args) throws Exception\
      \ {\n    String dir = System.getProperty(\"dir\");\n    Runtime rt = Runtime.getRuntime();\n\
      \    Process proc = rt.exec(\"cmd.exe /C dir \" + dir);\n    int result = proc.waitFor();\n\
      \    if (result != 0) {\n      System.out.println(\"process error: \" + result);\n\
      \    }\n    InputStream in = (result == 0) ? proc.getInputStream() :\n     \
      \                                proc.getErrorStream();\n    int c;\n    while\
      \ ((c = in.read()) != -1) {\n      System.out.print((char) c);\n    }\n  }\n\
      }\n```\n该代码示例使用dir命令列出目录列表。这是通过`Runtime.exec()`方法调用Windows的dir命令来实现的。因为`Runtime.exec()`方法接受源于运行环境的未经净化的数据，所以这些代码会引起命令注入攻击。\
      \  \n攻击者可以通过以下命令利用该程序：  \n`java -Ddir='dummy & echo bad' Java`   \n该命令实际上执行的是两条命令：\
      \  \n`cmd.exe /C dir dummy & echo bad`   \n第一条命令会列出并不存在的dummy文件夹，并且在控制台上输出bad。\
      \  \n**不规范的代码样例2（POSIX）**：  \n```java\nclass DirList {\n  public static void\
      \ main(String[] args) throws Exception {\n    String dir = System.getProperty(\"\
      dir\");\n    Runtime rt = Runtime.getRuntime();\n    Process proc = rt.exec(new\
      \ String[] {\"sh\", \"-c\", \"ls \" + dir});\n    int result = proc.waitFor();\n\
      \    if (result != 0) {\n      System.out.println(\"process error: \" + result);\n\
      \    }\n    InputStream in = (result == 0) ? proc.getInputStream() :\n     \
      \                                proc.getErrorStream();\n    int c;\n    while\
      \ ((c = in.read()) != -1) {\n      System.out.print((char) c);\n    }\n  }\n\
      }\n```\n这一个代码示例只是使用了POSIX中的ls命令。与Windows版本的唯一区别在于传入`Runtime.exec()`的参数。  \n\
      攻击者可以通过使用与上例中一样的命令取得同样的效果。这个命令实际执行的是：  \n`sh -c 'ls dummy & echo bad'`"
    Description_En: "External programs are commonly invoked to perform a function\
      \ required by the overall system. This practice is a form of reuse and might\
      \ even be considered a crude form of component-based software engineering. Command\
      \ and argument injection vulnerabilities occur when an application fails to\
      \ sanitize untrusted input and uses it in the execution of external programs.\n\
      \nEvery Java application has a single instance of class `Runtime` that allows\
      \ the application to interface with the environment in which the application\
      \ is running. The current runtime can be obtained from the `Runtime.getRuntime()`\
      \ method. The semantics of `Runtime.exec()` are poorly defined, so it is best\
      \ not to rely on its behavior any more than necessary, but typically it invokes\
      \ the command directly without a shell. If you want a shell, you can use `/bin/sh\
      \ -c` on POSIX or `cmd.exe` on Windows. The variants of `exec()` that take the\
      \ command line as a single string split it using a `StringTokenizer`. On Windows,\
      \ these tokens are concatenated back into a single argument string before being\
      \ executed.\n\nConsequently, command injection attacks cannot succeed unless\
      \ a command interpreter is explicitly invoked. However, argument injection attacks\
      \ can occur when arguments have spaces, double quotes, and so forth, or when\
      \ they start with a `-` or `/` to indicate a switch.\n\nAny string data that\
      \ originates from outside the program's trust boundary must be sanitized before\
      \ being executed as a command on the current platform.\n\n**Noncompliant Code\
      \ Example (Windows)**\n\nThis noncompliant code example provides a directory\
      \ listing using the `dir` command. It is implemented using `Runtime.exec()`\
      \ to invoke the Windows `dir` command.\n\n```java\nclass DirList {\n  public\
      \ static void main(String[] args) throws Exception {\n    String dir = System.getProperty(\"\
      dir\");\n    Runtime rt = Runtime.getRuntime();\n    Process proc = rt.exec(\"\
      cmd.exe /C dir \" + dir);\n    int result = proc.waitFor();\n    if (result\
      \ != 0) {\n      System.out.println(\"process error: \" + result);\n    }\n\
      \    InputStream in = (result == 0) ? proc.getInputStream() :\n            \
      \                         proc.getErrorStream();\n    int c;\n    while ((c\
      \ = in.read()) != -1) {\n      System.out.print((char) c);\n    }\n  }\n}\n\
      ```\n\nBecause `Runtime.exec()` receives unsanitized data originating from the\
      \ environment, this code is susceptible to a command injection attack.\n\nAn\
      \ attacker can exploit this program using the following command:\n\n```\njava\
      \ -Ddir='dummy & echo bad' Java\n```\n\nThe command executed is actually two\
      \ commands:\n\n```\ncmd.exe /C dir dummy & echo bad\n```\n\nwhich first attempts\
      \ to list a nonexistent `dummy` folder and then prints `bad` to the console.\n\
      \n**Noncompliant Code Example (POSIX)**\n\nThis noncompliant code example provides\
      \ the same functionality but uses the POSIX `ls` command. The only difference\
      \ from the Windows version is the argument passed to `Runtime.exec()`.\n\n```java\n\
      class DirList {\n  public static void main(String[] args) throws Exception {\n\
      \    String dir = System.getProperty(\"dir\");\n    Runtime rt = Runtime.getRuntime();\n\
      \    Process proc = rt.exec(new String[] {\"sh\", \"-c\", \"ls \" + dir});\n\
      \    int result = proc.waitFor();\n    if (result != 0) {\n      System.out.println(\"\
      process error: \" + result);\n    }\n    InputStream in = (result == 0) ? proc.getInputStream()\
      \ :\n                                     proc.getErrorStream();\n    int c;\n\
      \    while ((c = in.read()) != -1) {\n      System.out.print((char) c);\n  \
      \  }\n  }\n}\n```\n\nThe attacker can supply the same command shown in the previous\
      \ noncompliant code example with similar effects. The command executed is actually\n\
      \n```java\nsh -c 'ls dummy & echo bad'\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 78:Improper Neutralization of Special Elements used in an OS Command
          ('OS Command Injection')
      - type: CWE
        value: 77:Improper Neutralization of Special Elements used in a Command ('Command
          Injection')
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 78
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 78
      - type: CWE/SANS TOP 25
        value: 2011:Insecure Interaction - CWE ID 78
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 78
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Injection [RST]
      - type: GB/T 34944-2017 Java
        value: 数据处理:命令注入
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: cert
    WhiteList: true
    id: '0102000010140207'
    language: Java
  - Advice: "对应的符合规定的方案会对非受信的用户输入进行净化，这种净化只允许一小组列入白名单的字符出现在参数中，并传给 `Runtime.exec()`方法，其他所有的字符都会被排除掉。\
      \   \n    ```java\n    // ...   \n    if (!Pattern.matches(\"[0-9A-Za-z@.]+\"\
      , dir)) {   \n        // Handle error   \n    }   \n    // ...\n    ```   \n\
      \    另一个符合规定的方案通过只向 `Runtime.exec()`方法输入那些受信的字符串来防止命令注入。当用户可以控制使用哪一个字符串时，用户就可以不向\
      \ `Runtime.exec()`方法直接提供字符串数据。   \n    ```java\n    // ...   \n    String dir\
      \ = null;\n    // only allow integer choices\n    int number = Integer.parseInt(System.getproperty(\"\
      dir\"));\n    switch (number) {\n    case 1:\n        dir = \"data1\";\n   \
      \     break; // Option 1\n    case 2:\n        dir = \"data2\";\n        break;\
      \ // Option 2\n    default: // invalid\n        break;\n    }\n    if (dir ==\
      \ null) {\n        // handle error\n    }\n    ```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 5.2 数据净化
      - type: DetailClassChin
        value: C.1.8 不要向Runtime.exec()方法传递非受信、未净化的数据
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n   通过只向 `Runtime.exec()`方法输入受信的字符串来防止命令注入。   \n2) 原理 \
      \  \n每一个 Java 应用都有一个唯一的 Runtime类的实例，通过它可以提供一个应用和应用运行环境的接口。当前的 Runtime 对象可以通过\
      \ `Runtime.getRuntime()`方法获得。`Runtime.getRuntime()`的语义定义并不严格，所以最好仅仅使用它的那些必要的行为，然而，在通常情况下，它应当被命令直接调用，而不应通过\
      \ shell来调用。如果需要使用 shell来调用它，可以在 POSIX 中使用`/bin/sh`或者在 Windows平台中使用 cmd.exe 的方式。作为\
      \ `exec()`的另一种形式，它会使用 StringTokenizer 来分隔从命令行读入的字符串。在 Windows 平台中，在处理这些符号时，它们会被连接成一个单一的参数字符串。\
      \   \n因而，除非显式地调用命令行解释器，否则是不会产生命令行注入攻击的。然而，当参数中包含那些以空格、双引号或者其他以- / 开头的用来表示分支的字符时，就可能发生参数注入攻击。\
      \   \n3) 示例   \n    该不符合规则的代码示例使用 dir 命令列出目录列表。这是通过 `Runtime.exec()` 方法调用 Windows的\
      \ dir命令来实现的。   \n    ```java\n    class DirList {\n        public static void\
      \ main(String[] args) throws Exception {\n            String dir = System.getProperty(\"\
      dir\");\n            Runtime rt = Runtime.getRuntime();\n            Process\
      \ proc = rt.exec(\"cmd.exe /C dir \" + dir);\n            int result = proc.waitFor();\n\
      \            if (result != 0) {\n                System.out.println(\"process\
      \ error: \" + result);\n            }\n            InputStream in = (result\
      \ == 0) ? proc.getInputStream() : proc.getErrorStream();\n            int c;\n\
      \            while ((c = in.read()) != -1) {\n                System.out.print((char)\
      \ c);\n            }\n        }\n    }\n    ```  \n    因为 `Runtime.exec()` 方法接受源于运行环境的未经净化的数据，所以这些代码会引起命令注入攻击。\
      \   \n    攻击者可以通过以下命令利用该程序： \n    ```java\n    java -Ddir='dummy & echo bad'\
      \ Java \n    ```  \n    该命令实际上执行的是两条命令： \n    ```java\n    cmd.exe /C dir dummy\
      \ & echo bad   \n    ```  \n    第一条命令会列出并不存在的 dummy 文件夹，并且在控制台上输出 bad。   \n\
      \    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 78:Improper Neutralization of Special Elements used in an OS Command
          ('OS Command Injection')
      - type: CWE
        value: 77:Improper Neutralization of Special Elements used in a Command ('Command
          Injection')
      - type: CERT
        value: IDS07-J:Sanitize untrusted data passed to the Runtime.exec() method
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 78
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 78
      - type: CWE/SANS TOP 25
        value: 2011:Insecure Interaction - CWE ID 78
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 78
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Injection [RST]
      - type: GB/T 34944-2017 Java
        value: 数据处理:命令注入
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: true
    id: 0902000010160008
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2（净化）**：  \n```java\n// ...    \nif (!Pattern.matches(\"\
      [0-9A-Za-z@.]+\", dir)) {    \n // ...   \n}    \n// ...\n```   \n这个符合规范的代码实例会对非受信的用户输入进行净化，只允许白名单中的字符出现在参数中，并传给`Runtime.exec()`方法，其他所有的字符都会被排除掉。"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.1 数据清洗
      - type: DetailClassChin
        value: A.2.1.1.4 净化传递给Runtime.exec()方法的非受信数据
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明： 每个Java应用都有一个Runtime类的实例，一般需要使用shell时调用它，从而可以在POSIX中使用`/bin/sh`或者在Windows平台中使用cmd.exe。当参数中包含以空格、双引号或者其他以-\
      \ /开头的用来表示分支的字符时，就可能发生参数注入攻击。任何源于程序受信边界之外的字符串数据，在当前平台作为命令来执行之前，都必须经过净化。  \n\
      对于净化传递给`Runtime.exec()`方法的非受信数据的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1（Windows）**：\n\
      ```java  \nclass DirList {\n    public static void main(String[] args) throws\
      \ Exception {\n        String dir = System.getProperty(\"dir\");\n        Runtime\
      \ rt = Runtime.getRuntime();\n        Process proc = rt.exec(\"cmd.exe /C dir\
      \ \" + dir);\n        int result = proc.waitFor();\n        if (result != 0)\
      \ {\n            System.out.println(\"process error: \" + result);\n       \
      \ }\n        InputStream in = (result == 0) ? proc.getInputStream() : proc.getErrorStream();\n\
      \        int c;\n        while ((c = in.read()) != -1) {\n            System.out.print((char)\
      \ c);\n        }\n    }\n}\n```  \n该代码示例使用dir命令列出目录列表。这是通过`Runtime.exec()`方法调用Windows的dir命令来实现的。因为`Runtime.exec()`方法接受源于运行环境的未经净化的数据，所以这些代码会引起命令注入攻击。\
      \  \n攻击者可以通过以下命令利用该程序：  \n`java -Ddir='dummy & echo bad' Java `  \n该命令实际上执行的是两条命令：\
      \  \n`cmd.exe /C dir dummy & echo bad`   \n第一条命令会列出并不存在的dummy文件夹，并且在控制台上输出bad。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 78:Improper Neutralization of Special Elements used in an OS Command
          ('OS Command Injection')
      - type: CWE
        value: 77:Improper Neutralization of Special Elements used in a Command ('Command
          Injection')
      - type: CERT
        value: IDS07-J:Sanitize untrusted data passed to the Runtime.exec() method
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 78
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 78
      - type: CWE/SANS TOP 25
        value: 2011:Insecure Interaction - CWE ID 78
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 78
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Injection [RST]
      - type: GB/T 34944-2017 Java
        value: 数据处理:命令注入
    Security: true
    Tag: ''
    Type: guide
    WhiteList: true
    id: '0302000010170003'
    language: Java
  - Advice: "程序不应通过`printStackTrace()`打印错误信息，应使用日志记录工具进行记录。  \n  \n**例如**：下列代码中使用Log4j工具类进行错误信息管理。\
      \  \n```java\n...  \nlogger.error(\"发生异常:\",e);  \n...\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 日志规范
      - type: DetailClassChin
        value: 【推荐】不要使用printStackTrace()打印错误信息
    Categories_En:
      Category: []
    Default: true
    Description: "通过使用`printStackTrace()`打印错误信息时，在一些类似Eclipse的程序，为了让错误信息更加显眼，会将错误信息以红色文本的形式通过System.err输出到控制台上，更容易发生内部信息泄露。而且通过`printStackTrace()`打印错误信息，输出信息难以监控，容易导致错误信息丢失。\
      \  \n  \n**例如**：下面代码片段中，使用`printStackTrace()`打印错误信息。\n```java\ntry{  \n    ...\
      \  \n}catch(Exception e){  \n    e.printStackTrace();  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 535:Information Exposure Through Shell Error Message
      - type: CWE
        value: 526:Information Exposure Through Environmental Variables
      - type: CWE
        value: 497:Exposure of System Data to an Unauthorized Control Sphere
      - type: CWE
        value: 209:Information Exposure Through an Error Message
      - type: CERT
        value: ERR01-J:Do not allow exceptions to expose sensitive information,ENV02-J:Do
          not trust the values of environment variables
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: GB/T 34944-2017 Java
        value: 数据处理:信息通过错误消息泄露
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: true
    Tag: log,exception,bad_practice
    Type: std
    WhiteList: true
    id: 0202000010150428
    language: Java
  - Advice: "**规范的代码样例1**：\n```java\nBigInteger x = new BigInteger(\"530500452766\"\
      );  \nString s = x.toString();  // Valid character data  \nbyte[] byteArray\
      \ = s.getBytes();  \nString ns = new String(byteArray);   \nx = new BigInteger(ns);\n\
      ```  \n这个符合规范的代码首先生成BigInteger对象的字符串表示，然后将字符串对象转换为字节数组。然后，这个过程被逆转。因为字符串对象中的文本表示是由BigInteger类生成的，它包含默认字符集中的有效字符数据。\
      \  \n**规范的代码样例2**：  \n```java\nBigInteger x = new BigInteger(\"530500452766\"\
      );  \nbyte[] byteArray = x.toByteArray();  \nString s = Base64.getEncoder().encodeToString(byteArray);\
      \  \nbyteArray = Base64.getDecoder().decode(s);  \nx = new BigInteger(byteArray);\n\
      ```  \n尽管Java并没有提供一个字符集来保证无损耗的字节数据编码，但是还有许多其他的解决方案可以安全地将一个任意的字节数组转换成字符串和返回。Java\
      \ 8引入了`java.util.Base64`类，为Base64编码方案提供编码器和解码器。这个符合规范的方案使用Base64安全地将一个数字转换为字符串，而不破坏数据。"
    Advice_En: '**Compliant Solution**


      This compliant solution first produces a `String` representation of the `BigInteger`
      object and then converts the `String` object to a byte array. This process is
      then reversed. Because the textual representation in the `String` object is
      generated by the `BigInteger` class, it contains valid character data in the
      default character set.


      ```java

      BigInteger x = new BigInteger("530500452766");

      String s = x.toString();  // Valid character data

      byte[] byteArray = s.getBytes();

      String ns = new String(byteArray);

      x = new BigInteger(ns);

      ```


      **Compliant Solution (Base64)**


      Although Java does not provide a character set that guarantees lossless encoding
      of byte data, many other solutions exist for safely converting an arbitrary
      byte array into a string and back. Java 8 introduced the `java.util.Base64`
      class, which provides encoders and decoders for the Base64 encoding scheme.
      This compliant solution uses Base64 to safely convert a number to a string and
      back without corrupting the data:


      ```java

      BigInteger x = new BigInteger("530500452766");

      byte[] byteArray = x.toByteArray();

      String s = Base64.getEncoder().encodeToString(byteArray);

      byteArray = Base64.getDecoder().decode(s);

      x = new BigInteger(byteArray);

      ```'
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 04. 字符和字符串 (STR)
      - type: DetailClassChin
        value: STR03-J：不要将noncharacter数据编码成字符串
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 04. Characters and Strings (STR)
      - type: DetailClassChin
        value: STR03-J:Do not encode noncharacter data as a string
    Default: true
    Description: '程序员越来越多地将字符串看作是存储和传递任意数据的一种方式。例如：一个真实的系统将加密密码的二进制值存储在数据库中。非字符数据可能不能作为字符串表示，因为在大多数字符集中，不是所有的位模式都表示有效字符。因此，程序员不能将非字符数据转换成字符串。

      **不规范的代码样例1**：

      ```java

      BigInteger x = new BigInteger("530500452766");

      byte[] byteArray = x.toByteArray();

      String s = new String(byteArray);

      byteArray = s.getBytes();

      x = new BigInteger(byteArray);

      ```

      这个不规范的代码示例尝试将BigInteger的值转换为字符串，然后将其恢复为BigInteger值。用`toByteArray()`方法返回这个BigInteger的字节数组。字节数组是大端字节顺序：最重要的字节在零元素中。该程序使用`String(byte[]
      bytes)`构造函数从字节数组创建字符串。当给定的字节在默认字符集中无效时，此构造函数的行为是未指定的。尽管Java API[API 2014]文档声称`String(byte[],
      Charset)`方法总是用这个字符集的默认替换字符串替换不规范的不能映射的字符序列，指定字符集作为字符串也有未指明的行为，无论如何，将字符串转换回BigInteger不太可能重现原始值。'
    Description_En: 'Increasingly, programmers view strings as a portable means of
      storing and communicating arbitrary data, such as numeric values. For example,
      a real-world system stores the binary values of encrypted passwords as strings
      in a database. Noncharacter data may not be representable as a string, because
      not all bit patterns represent valid characters in most character sets. Consequently,
      programmers must not convert noncharacter data to a string.


      **Noncompliant Code Example**


      This noncompliant code example attempts to convert a `BigInteger` value to a
      `String` and then restore it to a `BigInteger` value. The `toByteArray()` method
      used returns a byte array containing the two''s-complement representation of
      this `BigInteger`. The byte array is in [*big-endian*] byte order: the most
      significant byte is in the zeroth element. The program uses the `String(byte[]
      bytes)` constructor to create the string from the byte array. The behavior of
      this constructor when the given bytes are not valid in the default character
      set is unspecified, which is likely to be the case. Specifying the character
      set as a string also has unspecified behavior, although the Java API [API 2014]
      document claims that the `String(byte[], Charset)` method always replaces malformed-input
      and unmappable-character sequences with this character set''s default replacement
      string. In any case, converting the `String` back to a `BigInteger` is unlikely
      to reproduce the original value.


      ```java

      BigInteger x = new BigInteger("530500452766");

      byte[] byteArray = x.toByteArray();

      String s = new String(byteArray);

      byteArray = s.getBytes();

      x = new BigInteger(byteArray);

      ```'
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 486:Comparison of Classes by Name
      - type: CWE
        value: 320:Key Management Errors
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: OWASP MOBILE TOP 10
        value: 2014:M6 Broken Cryptography
    Security: true
    Tag: ''
    Type: cert
    WhiteList: true
    id: '0102000010142003'
    language: Java
  - Advice: "对于在finally块中的会抛出异常的语句，应采用try-catch进行处理。注：Java7以后新增了try-with-resource新功能，它可以在异常发生时自动的关闭特定资源。\
      \  \n**规范的使用样例1**：  \n```java\npublic class Operation {\n  public static void\
      \ doOperation(String some_file) {\n    // ... Code to check or set character\
      \ encoding ...\n    try ( // try-with-resources\n      BufferedReader reader\
      \ =\n          new BufferedReader(new FileReader(some_file))) {\n      // Do\
      \ operations\n    } catch (IOException ex) {\n      System.err.println(\"thrown\
      \ exception: \" + ex.toString());\n      Throwable[] suppressed = ex.getSuppressed();\n\
      \      for (int i = 0; i < suppressed.length; i++) {\n        System.err.println(\"\
      suppressed exception: \"\n            + suppressed[i].toString());\n      }\n\
      \      // Forward to handler\n    }\n  }\n\n  public static void main(String[]\
      \ args) {\n    if (args.length < 1) {\n      System.out.println(\"Please supply\
      \ a path as an argument\");\n      return;\n    }\n    doOperation(args[0]);\n\
      \  }\n}\n```\n当 `doOperation()` 方法的 `try` 块中发生 `IOException` 时，它会被 `catch` 块捕获并打印为*抛出的异常*。\
      \ 包括在创建 `BufferedReader` 时发生的异常。 当关闭 `reader` 时发生 `IOException` 时，该异常也会被 `catch`\
      \ 块捕获并打印为抛出的异常。 如果 `try` 块和关闭 `reader` 都抛出 `IOException`，则 `catch` 子句捕获这两个异常并将\
      \ `try` 块异常打印为抛出的异常。 关闭异常被抑制并打印为 *suppressed exception*。 在所有情况下，“阅读器”都是安全关闭的。"
    Advice_En: "**Compliant Solution (Handle Exceptions in `finally` Block)**\n\n\
      This compliant solution encloses the `close()` method invocation in a `try-catch`\
      \ block of its own within the `finally` block. Consequently, the potential `IOException`\
      \ can be handled without allowing it to propagate further.\n\n```java\npublic\
      \ class Operation {\n  public static void doOperation(String some_file) {\n\
      \    // ... Code to check or set character encoding ...\n    try {\n      BufferedReader\
      \ reader =\n          new BufferedReader(new FileReader(some_file));\n     \
      \ try {\n        // Do operations\n      } finally {\n        try {\n      \
      \    reader.close();\n        } catch (IOException ie) {\n          // Forward\
      \ to handler\n        }\n        // ... Other cleanup code ...\n      }\n  \
      \  } catch (IOException x) {\n      // Forward to handler\n    }\n  }\n}\n```\n\
      \n**Compliant Solution (`try`-with-resources)**\n\nJava SE 7 introduced a feature\
      \ called *`try`-with-resources* that can close certain resources automatically\
      \ in the event of an error. This compliant solution uses `try`-with-resources\
      \ to properly close the file.\n\n```java\npublic class Operation {\n  public\
      \ static void doOperation(String some_file) {\n    // ... Code to check or set\
      \ character encoding ...\n    try ( // try-with-resources\n      BufferedReader\
      \ reader =\n          new BufferedReader(new FileReader(some_file))) {\n   \
      \   // Do operations\n    } catch (IOException ex) {\n      System.err.println(\"\
      thrown exception: \" + ex.toString());\n      Throwable[] suppressed = ex.getSuppressed();\n\
      \      for (int i = 0; i < suppressed.length; i++) {\n        System.err.println(\"\
      suppressed exception: \"\n            + suppressed[i].toString());\n      }\n\
      \      // Forward to handler\n    }\n  }\n\n  public static void main(String[]\
      \ args) {\n    if (args.length < 1) {\n      System.out.println(\"Please supply\
      \ a path as an argument\");\n      return;\n    }\n    doOperation(args[0]);\n\
      \  }\n}\n```\n\nWhen an `IOException` occurs in the `try` block of the `doOperation()`\
      \ method, it is caught by the `catch` block and printed as the *thrown exception*.\
      \ Exceptions that occur while creating the `BufferedReader` are included. When\
      \ an `IOException` occurs while closing the `reader`, that exception is also\
      \ caught by the `catch` block and printed as the thrown exception. If both the\
      \ `try` block and closing the `reader` throw an `IOException`, the `catch` clause\
      \ catches both exceptions and prints the `try` block exception as the thrown\
      \ exception. The close exception is suppressed and printed as the *suppressed\
      \ exception*. In all cases, the `reader` is safely closed."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 07. 异常行为 (ERR)
      - type: DetailClassChin
        value: ERR05-J：不要在finally块中遗漏可检测异常
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 07. Exceptional Behavior (ERR)
      - type: DetailClassChin
        value: ERR05-J:Do not let checked exceptions escape from a finally block
    Default: true
    Description: "在finally块中调用会抛出异常的方法，如果没有捕获这样的异常会造成整个try块的非正常终止。这导致了在try块中抛出的异常丢失，从而阻碍了任何可能的恢复方法去处理这些问题。而且，由于异常改变了程序流程，因此可能不会执行finally程序段中在异常抛出后的任何语句。\
      \  \n**不规范的代码样例1**：  \n```java\npublic class Operation {\n  public static void\
      \ doOperation(String some_file) {\n    // ... Code to check or set character\
      \ encoding ...\n    try {\n      BufferedReader reader =\n          new BufferedReader(new\
      \ FileReader(some_file));\n      try {\n        // Do operations\n      } finally\
      \ {\n        reader.close();\n        // ... Other cleanup code ...\n      }\n\
      \    } catch (IOException x) {\n      // Forward to handler\n    }\n  }\n}\n\
      ```\n`reader.close()`会抛出IOException。如果抛出这个异常，程序将不会执行随后的代码。"
    Description_En: "Methods invoked from within a `finally` block can throw an exception.\
      \ Failure to catch and handle such exceptions results in the abrupt termination\
      \ of the entire `try` block. Abrupt termination causes any exception thrown\
      \ in the `try` block to be lost, preventing any possible recovery method from\
      \ handling that specific problem. Additionally, the transfer of control associated\
      \ with the exception may prevent execution of any expressions or statements\
      \ that occur after the point in the `finally` block from which the exception\
      \ is thrown. Consequently, programs must appropriately handle checked exceptions\
      \ that are thrown from within a `finally` block.\n\nAllowing checked exceptions\
      \ to escape a `finally` block also violates ERR04-J. Do not complete abruptly\
      \ from a finally block.\n\n**Noncompliant Code Example**\n\nThis noncompliant\
      \ code example contains a `finally` block that closes the `reader` object. The\
      \ programmer incorrectly assumes that the statements in the `finally` block\
      \ cannot throw exceptions and consequently fails to appropriately handle any\
      \ exception that may arise.\n\n```java\npublic class Operation {\n  public static\
      \ void doOperation(String some_file) {\n    // ... Code to check or set character\
      \ encoding ...\n    try {\n      BufferedReader reader =\n          new BufferedReader(new\
      \ FileReader(some_file));\n      try {\n        // Do operations\n      } finally\
      \ {\n        reader.close();\n        // ... Other cleanup code ...\n      }\n\
      \    } catch (IOException x) {\n      // Forward to handler\n    }\n  }\n}\n\
      ```\n\nThe `close()` method can throw an `IOException`, which, if thrown, would\
      \ prevent execution of any subsequent cleanup statements. This problem will\
      \ not be diagnosed by the compiler because any `IOException` would be caught\
      \ by the outer `catch` block. Also, an exception thrown from the `close()` operation\
      \ can mask any exception that gets thrown during execution of the `Do operations`\
      \ block, preventing proper recovery."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140805
    language: Java
  - Advice: "不应在finally代码块中抛出异常，应直接处理掉可能发生的异常  \n  \n**例如**：下面代码中在finally代码块中处理了异常,其后续代码的执行不会被影响\
      \  \n```java\npublic static void doOperation throws IOException{  \n    FileInputStream\
      \ fis = null;  \n    try{  \n        fis = new FileInputStream(\"Reader.txt\"\
      );  \n        //...  \n    }finally {  \n        if(fis !=null) {  \n      \
      \      try {  \n                fis.close();  \n            } catch (IOException\
      \ e) {  \n                //...  \n            }  \n            //...  \n  \
      \      }  \n        //...  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 异常规范
      - type: DetailClassChin
        value: 【推荐】finally代码块中不要抛出异常
    Categories_En:
      Category: []
    Default: true
    Description: "在finally代码块中抛出异常时会消除从try或catch程序段抛出的任何异常，并影响后续代码的执行  \n  \n**例如**：下面代码中在finally代码块中抛出了异常\
      \  \n```java\npublic static void doOperation throws IOException{  \n    FileInputStream\
      \ fis = null;  \n    try{  \n        fis = new FileInputStream(\"Reader.txt\"\
      );  \n        //...  \n    }finally {  \n        fis.close();  \n        //...\
      \  \n    }  \n}\n```  \n当`Reader.txt`不存在，且程序并未对异常进行捕获时，程序只会抛出finally代码块中产生的NullPointerException，try代码块中产生的FileNotFoundException将不会显示，后续代码将不会执行。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Throw Inside Finally
    ENSubClassChin: Poor Error Handling
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: CERT
        value: ERR04-J:Do not exit abruptly from a finally block
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: not
    Tag: correctness,exception
    Type: std
    WhiteList: not
    id: '0202000010150504'
    language: Java
  - Advice: "对应的符合规则的解决方案是：将 `close()`放在 finally 程序段中一个独立的 try-catch 程序段里，从而可以处理潜在的\
      \ IOException 并防止它的传递。   \n    ```java\n    public class Operation {\n     \
      \   public static void doOperation(String some_file) {\n            // ... code\
      \ to check or set character encoding\n            try {\n                BufferedReader\
      \ reader = new BufferedReader(new FileReader(some_file));\n                try\
      \ {\n                    // Do operations\n                } finally {\n   \
      \                 try {\n                        reader.close();\n         \
      \           } catch (IOException ie) {\n                        // Forward to\
      \ handler\n                    }\n                    // ... Other clean-up\
      \ code ...\n                }\n            } catch (IOException x) {\n     \
      \           // Forward to handler\n            }\n        }\n    }\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.4 错误处理安全
      - type: DetailClassChin
        value: C.7.6 不要在finally程序段中遗漏可检查异常
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n在 finally 程序段中捕捉并处理可检查异常。   \n2) 原理   \n在 finally 程序段中调用的方法可能会抛出异常。没能捕捉和处理这样的异常会造成整个\
      \ try 程序段的非正常终止。这导致了在 try 程序段中抛出异常的丢失，从而阻碍了任何可能的恢复方法去处理这些问题。而且，由于异常改变了程序流程，因此可能不会执行\
      \ finally 程序段中在异常抛出后的任何表达式或语句。   \n3) 示例   \n    如下不符合规则的代码示例在 finally 程序段中关闭\
      \ reader 对象。程序员错误地假设 finally 程序段中的语句不会抛出异常，因此没能适当地处理可能发生的异常。   \n    ```java\n\
      \    public class Operation {\n        public static void doOperation(String\
      \ some_file) {\n            // ... code to check or set character encoding ...\n\
      \            try {\n                BufferedReader reader = new BufferedReader(new\
      \ FileReader(some_file));\n                try {\n                    // Do\
      \ operations\n                } finally {\n                    reader.close();\n\
      \                    // ... Other cleanup code ...\n                }\n    \
      \        } catch (IOException x) {\n                // Forward to handler\n\
      \            }\n        }\n    }\n    ```   \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: CERT
        value: ERR04-J:Do not exit abruptly from a finally block
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160068
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\npublic class Operation\
      \ {\n    public static void doOperation(String some_file) {\n        // ...\
      \ code to check or set character encoding ...\n        try {\n            BufferedReader\
      \ reader = new BufferedReader(new FileReader(some_file));\n            try {\n\
      \                // Do operations\n            } finally {\n               \
      \ try {\n                    reader.close();\n                } catch (IOException\
      \ ie) {\n                    // log ie\n                }\n                //\
      \ ... Other cleanup code ...\n            }\n        } catch (IOException x)\
      \ {\n            // Forward to handler\n        }\n    }\n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.4 异常处理安全
      - type: DetailClassChin
        value: A.3.4.5 不要在finally块中遗漏可检测异常
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：在finally块中调用会抛出异常的方法，如果没有捕获这样的异常会造成整个try块的非正常终止。这导致了在try块中抛出的异常丢失，从而阻碍了任何可能的恢复方法去处理这些问题。而且，由于异常改变了程序流程，因此可能不会执行finally程序段中在异常抛出后的任何语句。\
      \  \n对于不要在finally块中遗漏可检测异常的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\n\
      public class Operation {\n    public static void doOperation(String some_file)\
      \ {\n        // ... code to check or set character encoding ...\n        try\
      \ {\n            BufferedReader reader = new BufferedReader(new FileReader(some_file));\n\
      \            try {\n                // Do operations\n            } finally\
      \ {\n                reader.close();\n                // ... Other cleanup code\
      \ ...\n            }\n        } catch (IOException x) {\n            // Forward\
      \ to handler\n        }\n    }\n} \n```\n`reader.close()`会抛出IOException。如果抛出这个异常，程序将不会执行随后的代码。\
      \  \n对于在finally块中的会抛出异常的语句，应采用try-catch进行处理。Java7以后新增了try-with-resource新功能，它可以在异常发生时自动的关闭特定资源。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: CERT
        value: ERR04-J:Do not exit abruptly from a finally block
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170057'
    language: Java
  - Advice: 应该在`java.util.concurrent.locks.Condition`对象上调用`await`方法替代`wait`方法。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【强制】不要在Condition对象上调用wait()
    Categories_En:
      Category: []
    Default: true
    Description: '`java.util.concurrent.locks.Condition`是在java 1.5中才出现的，它用来替代传统的Object的`wait()、notify()`实现线程间的协作，相比使用Object的`wait()、notify()`，使用Condition1的`await()、signal()`这种方式实现线程间协作更加安全和高效。在Condition的对象上调用了`wait`方法实际上调用的是Object的`wait`方法。'
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: concurrency,bad_practice
    Type: std
    WhiteList: not
    id: 0202000010150108
    language: Java
  - Advice: "由于初始循环较为复杂，因此解决该问题比较困难。有时，最佳解决方法可能是重新构建某些类，以消除对静态字段的需要。   \n**规范的代码样例1**：\
      \    \n以下代码更改了`Cycle`的初始化次序，所以对数据成员的初始化是不会造成任何依赖循环的。特别是对c的初始化，因为在编码上处于对`deposit`初始化之后发生，因而它会在`deposit`被完全初始化之后进行初始化。\
      \  \n```java\npublic class Cycle {  \n    private final int balance;  \n   \
      \ private static final int deposit = (int) (Math.random() * 100);  \n    private\
      \ static final Cycle c = new Cycle();  \n    public Cycle() {  \n        balance\
      \ = deposit - 10;  \n    }  \n    public static void main(String[] args) { \
      \ \n        System.out.println(\"The account balance is :\" + c.balance);  \n\
      \    }  \n}\n```      \n当涉及许多字段的时候，并不容易发现这样的初始化循环。因此，确保控制流不产生这样的循环是非常重要的。 尽管这个方案可以防止初始化循环，但它也依赖于声明次序，因此也是脆弱的。程序的维护者可能不知道这种声明的次序必须被保持来保证程序的正确性。因而，这种依赖必须在代码的文档中清楚地说明。\
      \  \n**规范的代码样例2**：  \n以下代码通过消除其中一个依赖来打破类间的循环。  \n```java\nclass A {  \n    public\
      \ static final int a = 2;  \n    //...  \n}  \nclass B {  \n    public static\
      \ final int b = A.a + 1;  \n    //...  \n}\n```   \n**规范的代码样例3**：  \n以下代码通过将方法`c()`移到类B中，以打破初始化循环。\
      \   \n ```java\nclass A {  \n public static int a = B.b();  \n }  \n class B\
      \ {  \n public static int b() { return B.c(); }  \n public static int c() {\
      \ return 1; }  \n }\n```"
    Advice_En: "**Compliant Solution (Intraclass Cycle)**\n\nThis compliant solution\
      \ changes the initialization order of the class `Cycle` so that the fields are\
      \ initialized without creating any dependency cycles. Specifically, the initialization\
      \ of `c` is placed lexically *after* the initialization of `deposit` so that\
      \ it occurs temporally after `deposit` is fully initialized.\n\n```java\npublic\
      \ class Cycle {\n  private final int balance;\n  private static final int deposit\
      \ = (int) (Math.random() * 100); // Random deposit\n  private static final Cycle\
      \ c = new Cycle();  // Inserted after initialization of required fields\n  public\
      \ Cycle() {\n    balance = deposit - 10; // Subtract processing fee\n  }\n\n\
      \  public static void main(String[] args) {\n    System.out.println(\"The account\
      \ balance is: \" + c.balance);\n  }\n}\n```\n\nSuch initialization cycles become\
      \ insidious when many fields are involved, so it is important to ensure that\
      \ the control flow lacks such cycles.\n\nAlthough this compliant solution prevents\
      \ the initialization cycle, it depends on declaration order and is consequently\
      \ fragile; later maintainers of the software may be unaware that the declaration\
      \ order must be maintained to preserve correctness. Consequently, such dependencies\
      \ must be clearly documented in the code.\n**Compliant Solution (Interclass\
      \ Cycle)**\n\nThis compliant solution breaks the interclass cycle by eliminating\
      \ the dependency of `A` on `B`:\n\n```java\nclass A {\n  public static final\
      \ int a = 2;\n  // ...\n}\n\nclass B {\n  public static final int b = A.a +\
      \ 1;\n  // ...\n}\n```\n\nWith the cycle broken, the initial values will always\
      \ be `A.a = 2` and `B.b = 3` regardless of initialization order.\n**Compliant\
      \ Solution**\n\nThis compliant solution moves the `c()` method into class `B`,\
      \ breaking the cycle:\n\n```java\nclass A {\n  public static int a = B.b();\n\
      }\n\nclass B {\n  public static int b() { return B.c(); }\n  public static int\
      \ c() { return 1; }\n}\n```"
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 01. 声明和初始化 (DCL)
      - type: DetailClassChin
        value: DCL00-J：防止类的循环初始化
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 01. Declarations and Initialization (DCL)
      - type: DetailClassChin
        value: DCL00-J:Prevent class initialization cycles
    Default: true
    Description: "根据Java语言规范（JLS），§12.4，“类和接口的初始化”[JLS 2005]：\n> 对类进行的初始化包括执行该类的static静态初始化方法和初始化该类中的静态数据成员（类变量）。\n\
      \n换句话说，一个静态数据成员的出现会触发类的初始化，然而，一个静态数据成员可能会依赖于其他类的初始化，这样可能形成一个初始化循环。\nJLS还在§8.3.2.1“类变量的初始值设定项”[JLS\
      \ 2005]中规定：\n> 在运行时，首先初始化最终静态变量，并使用编译时常量值初始化静态变量。\n\n此语句不适用于使用在稍后阶段初始化的静态最终字段值的实例。将字段声明为static\
      \ final不足以保证它在读取之前完全初始化。\n普通程序应该和安全敏感的程序必须消除所有的类初始化周期。\n**不规范的代码样例1（内部类循环）**：\n\
      以下代码存在一个涉及多个类的类初始化循环。  \n```java\npublic class Cycle {\n  private final int\
      \ balance;\n  private static final Cycle c = new Cycle();\n  private static\
      \ final int deposit = (int) (Math.random() * 100); // Random deposit\n\n  public\
      \ Cycle() {\n    balance = deposit - 10; // Subtract processing fee\n  }\n\n\
      \  public static void main(String[] args) {\n    System.out.println(\"The account\
      \ balance is: \" + c.balance);\n  }\n}\n```\nCycle类声明了一个private static final类变量，这个变量会在创建Cycle对象的时候进行初始化。静态的初始化方法可以保证只调用一次，而这次调用是在第一次使用静态类变量或者在第一次调用构造函数之前发生。\n\
      程序员希望通过在`deposit`中减去10来计算balance，然而，对类变量c的初始化在`deposit`变量初始化之前发生，因为在编码上，它在`deposit`域的初始化之前出现。因此，当对变量`c`进行静态初始化时，Cycle类的构造函数会读取`deposit`的数值，这个`deposit`数值会是0而不是一个随机数，因此最后输出结果为-10。\n\
      由于初始循环较为复杂，因此解决该问题比较困难。有时，最佳解决方法可能是重新构建某些类，以消除对静态字段的需要。\n\n**不规范的代码样例2（内部类循环）**：\n\
      以下类声明了相互依赖的静态字段。  \n```java\nclass A {\n  public static final int a = B.b +\
      \ 1;\n  // ...\n}\n\nclass B {\n  public static final int b = A.a + 1;\n  //\
      \ ...\n}\n```\n因为对这些类的初始化次序是可变的，所以导致的结果是，会计算出不同的`A.a`和`B.b`的值。当先初始化A类时，`A.a`的值是2，`B.b`的值是1。当先初始化B类时，这两个值取值相反。\
      \  \n\n**不规范的代码样例3**：\n以下代码尝试使用第二个类中的静态方法初始化一个类中的静态变量，但该方法又依赖于第一个类中的静态方法。  \
      \ \n```java\nclass A {\n  public static int a = B.b();\n  public static int\
      \ c() { return 1; }\n}\n\nclass B {\n  public static int b() { return A.c();\
      \ }\n}\n```\n此代码使用Oracle JVM时，无论A或者B先加载，都会正确的将`A.a`初始化为1。但是，JLS不能保证`A.a`能被正确初始化。而且，初始化循环使得该系统更加难以维护。"
    Description_En: "According to The Java Language Specification (JLS), §12.4, \"\
      Initialization of Classes and Interfaces\" [JLS 2005]:\n\n> Initialization of\
      \ a class consists of executing its `static` initializers and the initializers\
      \ for `static` fields (class variables) declared in the class.\n\nTherefore,\
      \ the presence of a static field triggers the initialization of a class. However,\
      \ the initializer of a static field could depend on the initialization of another\
      \ class, possibly creating an initialization cycle.\n\nThe JLS also states in\
      \ §8.3.2.1, \"Initializers for Class Variables\" [JLS 2005]:\n\n> At run time,\
      \ `static` variables that are `final` and that are initialized with compile-time\
      \ constant values are initialized first.\n\nThis statement does not apply to\
      \ instances that use values of static final fields that are initialized at a\
      \ later stage. Declaring a field to be static final is insufficient to guarantee\
      \ that it is fully initialized before being read.\n\nPrograms in general should—and\
      \ security-sensitive programs must—eliminate all class initialization cycles.\n\
      \n**Noncompliant Code Example (Intraclass Cycle)**\n\nThis noncompliant code\
      \ example contains an intraclass initialization cycle:\n\n```java\npublic class\
      \ Cycle {\n  private final int balance;\n  private static final Cycle c = new\
      \ Cycle();\n  private static final int deposit = (int) (Math.random() * 100);\
      \ // Random deposit\n\n  public Cycle() {\n    balance = deposit - 10; // Subtract\
      \ processing fee\n  }\n\n  public static void main(String[] args) {\n    System.out.println(\"\
      The account balance is: \" + c.balance);\n  }\n}\n```\n\nThe `Cycle` class declares\
      \ a `private static final` class variable, which is initialized to a new instance\
      \ of the `Cycle` class. Static initializers are guaranteed to be invoked once\
      \ before the first use of a static class member or the first invocation of a\
      \ constructor.\n\nThe programmer's intent is to calculate the account balance\
      \ by subtracting the processing fee from the deposited amount. However, the\
      \ initialization of the `c` class variable happens before the runtime initialization\
      \ of the `deposit` field because it appears lexically before the initialization\
      \ of the `deposit` field. Consequently, the value of `deposit` seen by the constructor,\
      \ when invoked during the static initialization of `c`, is the initial value\
      \ of `deposit` (0) rather than the random value. As a result, the balance is\
      \ always computed to be `-10`.\n\nStep 3 of the detailed initialized procedure\
      \ described in JLS §12.4.2 [JLS 2014] permits implementations to ignore the\
      \ possibility of such recursive initialization cycles.\n\n**Noncompliant Code\
      \ Example (Interclass Cycle)**\n\nThis noncompliant code example declares two\
      \ classes with static variables whose values depend on each other. The cycle\
      \ is obvious when the classes are seen together (as here) but is easy to miss\
      \ when viewing the classes separately.\n\n```java\nclass A {\n  public static\
      \ final int a = B.b + 1;\n  // ...\n}\n\nclass B {\n  public static final int\
      \ b = A.a + 1;\n  // ...\n}\n```\n\nThe initialization order of the classes\
      \ can vary, causing computation of different values for `A.a` and `B.b`. When\
      \ class `A` is initialized first, `A.a` will have the value 2, and `B.b` will\
      \ have the value 1. These values will be reversed when class `B` is initialized\
      \ first.\n\n**Noncompliant Code Example**\n\nThe programmer in this noncompliant\
      \ code example attempts to initialize a static variable in one class using a\
      \ static method in a second class, but that method in turn relies on a static\
      \ method in the first class:\n\n```java\nclass A {\n  public static int a =\
      \ B.b();\n  public static int c() { return 1; }\n}\n\nclass B {\n  public static\
      \ int b() { return A.c(); }\n}\n```\n\nThis code correctly initializes `A.a`\
      \ to 1, using the Oracle JVM, regardless of whether `A` or `B` is loaded first.\
      \ However, the JLS does not guarantee `A.a` to be properly initialized. Furthermore,\
      \ the initialization cycle makes this system harder to maintain and more likely\
      \ to break in surprising ways when modified."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 367:Time-of-check Time-of-use (TOCTOU) Race Condition
      - type: CWE
        value: 362:Concurrent Execution using Shared Resource with Improper Synchronization
          ('Race Condition')
      - type: CWE/SANS TOP 25
        value: 2011:Insecure Interaction - CWE ID 362
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 362
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140300'
    language: Java
  - Advice: "对应的符合规则的代码示例：通过改变 Cycle类的初始化次序，使得对数据成员的初始化不会形成依赖循环。对 c 的初始化，在编码上处于对 `deposit`\
      \ 初始化之后，因而它会在 `deposit` 被完全初始化之后初始化。   \n    ```java\n    public class Cycle\
      \ {\n        private final int balance;\n        // Random deposit\n       \
      \ private static final int deposit = (int) (Math.random() * 100);\n        //\
      \ Inserted after initialization of required fields\n        private static final\
      \ Cycle c = new Cycle();\n\n        public Cycle() {\n            balance =\
      \ deposit - 10; // Subtract processing fee\n        }\n\n        public static\
      \ void main(String[] args) {\n            System.out.println(\"The account balance\
      \ is: \" + c.balance);\n        }\n    }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.2.1 防止类的初始化循环
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n对类进行的初始化，必须避免类的初始化循环。   \n2) 原理   \n类初始化包括：执行该类的 static\
      \ 静态初始化方法和初始化该类中的静态数据成员（类变量）。   \n一个静态数据成员的出现会触发类的初始化。一个静态数据成员可能会依赖于其他类的初始化，这样可能形成一个初始化循环。\
      \   \n一般来说，程序，特别是对安全敏感的程序，必须消除所有类的初始化循环。   \n3) 示例   \n    1.这个不符合规则的代码示例，存在一个涉及多个类的类初始化循环。\
      \   \n    Cycle 类声明了一个 private static final 类变量，这个变量在创建 Cycle对象时初始化。静态的初始化方法只调用一次，调用在第一次使用静态类变量或者在第一次调用构造函数前发生。\
      \   \n    ```java\n    public class Cycle {\n        private final int balance;\n\
      \        private static final Cycle c = new Cycle();\n        // Random deposit\n\
      \        private static final int deposit = (int) (Math.random() * 100);\n \
      \   \n        public Cycle() {\n            balance = deposit - 10; // Subtract\
      \ processing fee\n        }\n    \n        public static void main(String[]\
      \ args) {\n            System.out.println(\"The account balance is: \" + c.balance);\n\
      \        }\n    }\n    ```   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 367:Time-of-check Time-of-use (TOCTOU) Race Condition
      - type: CWE
        value: 362:Concurrent Execution using Shared Resource with Improper Synchronization
          ('Race Condition')
      - type: CERT
        value: DCL00-J:Prevent class initialization cycles
      - type: CWE/SANS TOP 25
        value: 2011:Insecure Interaction - CWE ID 362
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 362
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160015
    language: Java
  - Advice: "检查代码逻辑，当客户端不需要读取cookie时，将HttpOnly属性设为true。  \n  \n**例如**：下面代码示例中设置了HttpOnly属性为true。\n\
      ```java  \n...  \nCookie cookie = new Cookie(\"myCookieName\", value);  \ncookie.setHttpOnly(true);\
      \   \n...\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】Cookie应该设置HttpOnly属性
    Categories_En:
      Category: []
    Default: true
    Description: "Cookie设置HttpOnly属性意味着客户端脚本不允许读取cookie，对于防范跨站脚本攻击大有帮助，浏览器也不会将其发送给任何第三方。\
      \  \n  \n**例如**：下面代码示例中Cookie没有设置HttpOnly属性，HttpOnly属性默认为false。\n```java  \n\
      ...  \nCookie cookie = new Cookie(\"myCookieName\", value);  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A6 Security Misconfiguration
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A10 Insecure Configuration Management
      - type: OWASP MOBILE TOP 10
        value: 2014:M4 Unintended Data Leakage
    Security: true
    Tag: security
    Type: std
    WhiteList: not
    id: '0202000010150447'
    language: Java
  - Advice: "使用安全性更高的TLSv1.2或者DTLSv1.2来替代SSL协议。  \n  \n**例如**：下面代码示例中使用了TLSv1.2协议。\
      \  \n```java\n...  \ncontext = SSLContext.getInstance(\"TLSv1.2\");  \n...\n\
      ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不应该使用弱的SSL协议
    Categories_En:
      Category: []
    Default: true
    Description: "在安全性要求较高的系统中，使用弱的SSL协议，将无法保证敏感数据的保密性。  \n  \n**例如**：下面代码示例中使用了SSL协议。\
      \  \n```java\n...  \ncontext = SSLContext.getInstance(\"SSL\");  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 327:Inadequate Encryption Strength
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
    Security: not
    Tag: security
    Type: std
    WhiteList: not
    id: 0202000010150439
    language: Java
  - Advice: "**规范的代码样例1**：  \n```java\nprivate final Map<Integer, String> mapView\
      \ =\n    Collections.synchronizedMap(new HashMap<Integer, String>());\nprivate\
      \ final Set<Integer> setView = mapView.keySet();\n\npublic Map<Integer, String>\
      \ getMap() {\n  return mapView;\n}\n\npublic void doSomething() {\n  synchronized\
      \ (mapView) {  // Synchronize on map, rather than set\n    for (Integer k :\
      \ setView) {\n      // ...\n    }\n  }\n}\n```\n\n这个代码基于mapView字段实现了同步，在map的底层结构中，它在迭代过程中是不能改变的。"
    Advice_En: "**Compliant Solution (Collection Lock Object)**\n\nThis compliant\
      \ solution synchronizes on the `mapView` field rather than on the `setView`\
      \ field:\n\n```java\nprivate final Map<Integer, String> mapView =\n    Collections.synchronizedMap(new\
      \ HashMap<Integer, String>());\nprivate final Set<Integer> setView = mapView.keySet();\n\
      \npublic Map<Integer, String> getMap() {\n  return mapView;\n}\n\npublic void\
      \ doSomething() {\n  synchronized (mapView) {  // Synchronize on map, rather\
      \ than set\n    for (Integer k : setView) {\n      // ...\n    }\n  }\n}\n```\n\
      \nThis code is compliant because the map's underlying structure cannot be changed\
      \ during the iteration."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 09. 锁 (LCK)
      - type: DetailClassChin
        value: LCK04-J：即使集合是可访问的，也不要基于集合视图使用同步
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 09. Locking (LCK)
      - type: DetailClassChin
        value: LCK04-J:Do not synchronize on a collection view if the backing collection
          is accessible
    Default: true
    Description: "`java.util.Collections`的接口文档中提到了在对集合视图进行迭代时，如果没有对可访问的集合对象进行同步会产生以下后果：\
      \  \n当对任何一个集合视图进行迭代的时候，认为用户手动对返回的map进行同步时必要的。  \n任何一个使用集合视图而不是后台集合做为锁对象的类，会面临两种不同的锁策略。当多个线程可以后台访问集合时，基于集合视图实现锁的类违反了线程安全的属性，同时也是不安全的。因此，当程序需要迭代集合视图并访问后台集合时，程序应该基于后台集合进行同步。\
      \  \n**不规范的代码样例1**：  \n```java\nprivate final Map<Integer, String> mapView =\n\
      \    Collections.synchronizedMap(new HashMap<Integer, String>());\nprivate final\
      \ Set<Integer> setView = mapView.keySet();\n\npublic Map<Integer, String> getMap()\
      \ {\n  return mapView;\n}\n\npublic void doSomething() {\n  synchronized (setView)\
      \ {  // Incorrectly synchronizes on setView\n    for (Integer k : setView) {\n\
      \      // ...\n    }\n  }\n}\n```\n代码创建了一个HashMap对象和两个视图对象：一个空的HashMap同步视图，这个空的HashMap视图被`mapView`数据成员封装；另一个是被`setView`数据成员封装的`mapView`的索引视图。这个HashMap对象是不可访问的，但`mapView`可以通过公有的`getMap()`方法访问。因为同步的语句使用了`setView`的内在锁而不是mapView的，另一个线程可以修改这个同步的map且使k迭代器失效。"
    Description_En: "The Java Tutorials, Wrapper Implementations [Java Tutorials],\
      \ warns about the consequences of failing to synchronize on an accessible collection\
      \ object when iterating over its view:\n\n> It is imperative that the user manually\
      \ synchronize on the returned `Map` when iterating over any of its `Collection`\
      \ views rather than synchronizing on the `Collection` view itself.\n\nDisregarding\
      \ this advice may result in nondeterministic behavior.\n\nAny class that uses\
      \ a collection view rather than the backing collection as the lock object may\
      \ end up with two distinct locking strategies. When the backing collection is\
      \ accessible to multiple threads, the class that locked on the collection view\
      \ has violated the thread-safety properties and is unsafe. Consequently, programs\
      \ that both require synchronization while iterating over collection views and\
      \ have accessible backing collections must synchronize on the backing collection;\
      \ synchronization on the view is a violation of this rule.\n\n**Noncompliant\
      \ Code Example (Collection View)**\nThis noncompliant code example creates a\
      \ `HashMap` object and two view objects: a synchronized view of an empty `HashMap`\
      \ encapsulated by the `mapView` field and a set view of the map's keys encapsulated\
      \ by the `setView` field. This example synchronizes on `setView` [Java Tutorials].\n\
      \n```java\nprivate final Map<Integer, String> mapView =\n    Collections.synchronizedMap(new\
      \ HashMap<Integer, String>());\nprivate final Set<Integer> setView = mapView.keySet();\n\
      \npublic Map<Integer, String> getMap() {\n  return mapView;\n}\n\npublic void\
      \ doSomething() {\n  synchronized (setView) {  // Incorrectly synchronizes on\
      \ setView\n    for (Integer k : setView) {\n      // ...\n    }\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141004'
    language: Java
  - Advice: "对应的符合规则的解决方案（集合锁对象）是：基于`mapView`字段而不是基于`setView`字段实现同步。   \n    ```java\n\
      \    private final Map<Integer, String> mapView = Collections.synchronizedMap(new\
      \ HashMap<Integer, String>());\n    private final Set<Integer> setView = mapView.keySet();\n\
      \    \n    public Map<Integer, String> getMap() {\n        return mapView;\n\
      \    }\n    \n    public void doSomething() {\n        synchronized (mapView)\
      \ { // Synchronize on map, rather than set\n            for (Integer k : setView)\
      \ {\n                // . . .\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.9.5 即使集合是可访问的，也不要基于集合视图使用同步
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n同时需要对集合视图进行迭代和可以进入后台集合的程序，应该基于后台的集合实现同步。   \n2) 原理   \n\
      `java.util.Collections` 的接口文档中提到了在对集合视图进行迭代时，如果没有对可访问的集 合对象进行同步会产生以下后果：   \n\
      当对任何一个集合视图进行迭代的时候，认为用户手动对返回的 map 进行同步是必要的。 如果不听从这条建议，会导致不确定的行为。   \n任何一个使用集合视图而不是后台集合作为锁对象的类，会面临两种不同的锁策略。当多个线程可以访问后台集合时可访问多个线程时，基于集合视图实现锁的类违反了线程安全的属性,同时也是不安全的。因此，同时需要对集合视图进行迭代和可以进入后台集合的程序，应该基于后台的集合实现同步，而基于视图进行同步违反了本条规则。\
      \   \n3) 示例   \n    如下不符合规则的代码示例，该示例创建了一个HashMap对象和两个视图对象：一个空的HashMap的同步视图，这个空的HashMap视图被`mapView`数据成员封装；另一个是被`setView`数据成员封装的map的索引的视图。它基于`setView`实现同步。\
      \   \n    ```java\n    private final Map<Integer, String> mapView = Collections.synchronizedMap(new\
      \ HashMap<Integer, String>());\n    \n    private final Set<Integer> setView\
      \ = mapView.keySet();\n    \n    public Map<Integer, String> getMap() {\n  \
      \      return mapView;\n    }\n    \n    public void doSomething() {\n     \
      \   synchronized (setView) { // Incorrectly synchronizes on setView\n      \
      \      for (Integer k : setView) {\n                // . . .\n    ``` \n   \
      \ 同步的语句使用了`setView`的内在锁而不是`mapView`的，另一个线程可以修改这个同步 map 并且使 k 迭代器无效。   \n   \
      \    \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160083
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nprivate final Map<Integer,\
      \ String> mapView = Collections.synchronizedMap(new HashMap<Integer, String>());\n\
      private final Set<Integer> setView = mapView.keySet();\n\npublic Map<Integer,\
      \ String> getMap() {\n    return mapView;\n}\n\npublic void doSomething() {\n\
      \    synchronized (mapView) { // Synchronize on map, rather than set\n     \
      \   for (Integer k : setView) {\n            // . . .\n        }\n    }\n}\n\
      ```\n这个代码基于`mapView`字段实现了同步，在map的底层结构中，它在迭代过程中是不能改变的。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.6 不要基于集合视图使用同步
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：任何一个使用集合视图而不是集合做为锁对象的类时，当多个线程可以后台访问集合时，基于集合视图实现锁的类违反了线程安全的属性，是不安全的。因此，当程序需要迭代集合视图并访问集合时，程序应该基于集合进行同步。\
      \  \n对于不要基于集合视图使用同步的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nprivate\
      \ final Map<Integer, String> mapView = Collections.synchronizedMap(new HashMap<Integer,\
      \ String>());\nprivate final Set<Integer> setView = mapView.keySet();\n\npublic\
      \ Map<Integer, String> getMap() {\n    return mapView;\n}\n\npublic void doSomething()\
      \ {\n    synchronized (setView) { // Incorrectly synchronizes on setView\n \
      \       for (Integer k : setView) {\n            // . . .\n        }\n    }\n\
      }\n``` \n代码创建了一个HashMap对象和两个视图对象：一个空的HashMap同步视图，这个空的HashMap视图被`mapView`数据成员封装；另一个是被`setView`数据成员封装的`mapView`的索引视图。这个HashMap对象是不可访问的，但`mapView`可以通过公有的`getMap()`方法访问。因为同步的语句使用了`setView`的内在锁而不是`mapView`的，另一个线程可以修改这个同步的map且使k迭代器失效。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: 0302000010170038
    language: Java
  - Advice: "使用URI替换URL进行处理。  \n  \n**例如**：下列代码中,使用URI的`equals`来判断是否为同一个URI。\n```java\
      \  \nuri.equals(otherUri);\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要在URL上使用equals或hashCode方法
    Categories_En:
      Category: []
    Default: true
    Description: "因为URL的`equals`和`hashCode`方法都是阻塞操作，当使用URL的`equals`和`hashCode`方法执行域名解析，会导致很大的性能损失。\
      \  \n  \n**例如**：下列代码中，使用URL的`equals`来判断是否为同一个URL。\n```java  \nurl.equals(otherUrl);\n\
      ```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: performance,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150452'
    language: Java
  - Advice: "**规范的代码样例1**：  \n```java\nDataInputStream dis = null;\ntry {\n    dis\
      \ = new DataInputStream(new FileInputStream(\"data\"));\n    byte[] buffer =\
      \ new byte[4];\n    int bytesRead = dis.read(buffer); // Bytes are read into\
      \ buffer\n    if (bytesRead != 4) {\n        throw new IOException(\"Unexpected\
      \ End of Stream\");\n    }\n    int serialNumber = ByteBuffer.wrap(buffer).order(ByteOrder.LITTLE_ENDIAN).getInt();\n\
      } finally {\n    if (dis != null) {\n        try {\n            dis.close();\n\
      \        } catch (IOException x) {\n            // handle error\n        }\n\
      \    }\n}\n```  \n代码通过使用类ByteBuffer提供的方法来正确的从初始的输入数值中提取一个int值。它使用ByteBuffer来封装输入的字节数组，设置字节顺序为小端字节序，并提取出int值，结果存储在serialNumber整数中。类ByteBuffer提供了类似get、put方法来处理其他的数值类型。\
      \  \n**规范的代码样例2**：\n另一种解决方案是定义读取和写入方法，以在读取或写入文件时支持必要的字节交换。 在此示例中，`readLittleEndianInteger()`\
      \ 方法将四个字节读入字节缓冲区，然后以正确的顺序将整数拼凑在一起。 `writeLittleEndianInteger()` 方法通过重复转换整数来获取字节，以便在连续右移时提取最低有效字节。\
      \ `Long` 值可以通过定义大小为 8 的字节缓冲区来处理。\n```java\n// Read method\npublic static int\
      \ readLittleEndianInteger(InputStream ips)\n                               \
      \           throws IOException {\n  byte[] buffer = new byte[4];\n  int check\
      \ = ips.read(buffer);\n\n  if (check != 4) {\n    throw new IOException(\"Unexpected\
      \ End of Stream\");\n  }\n\n  int result = (buffer[3] << 24) | (buffer[2] <<\
      \ 16) |\n               (buffer[1] << 8)  | buffer[0];\n  return result;\n}\n\
      \n// Write method\npublic static void writeLittleEndianInteger(int i, OutputStream\
      \ ops)\n  throws IOException {\n  byte[] buffer = new byte[4];\n  buffer[0]\
      \ = (byte) i;\n  buffer[1] = (byte) (i >> 8);\n  buffer[2] = (byte) (i >> 16);\n\
      \  buffer[3] = (byte) (i >> 24);\n  ops.write(buffer);\n}\n```\n**规范的代码样例3**：\n\
      使用Java 5+进行编程时，使用定义在类Character、Short、Integer、Long的`reverseBytes()`方法转换整型数值的字节顺序。但是在类Float和Bouble中没有提供这个方法。\
      \  \n```java\npublic static int reverse(int i) {  \n  return Integer.reverseBytes(i);\
      \  \n}\n```"
    Advice_En: "**Compliant Solution (`ByteBuffer`)**\nThis compliant solution uses\
      \ methods provided by class `<a class=\"external-link\" href=\"https://docs.oracle.com/javase/8/docs/api/java/nio/ByteBuffer.html\"\
      \ rel=\"nofollow\">ByteBuffer</a>` [[API 2014] to correctly extract an `int`\
      \ from the original input value. It wraps the input byte array with a `ByteBuffer`,\
      \ sets the byte order to little-endian, and extracts the `int`. The result is\
      \ stored in the integer `serialNumber`. Class `ByteBuffer` provides analogous\
      \ get and put methods for other numeric types.\n```java\ntry {\n  DataInputStream\
      \ dis = null;\n  try {\n    dis = new DataInputStream( new FileInputStream(\"\
      data\"));\n    byte[] buffer = new byte[4];\n    int bytesRead = dis.read(buffer);\
      \  // Bytes are read into buffer\n    if (bytesRead != 4) {\n      throw new\
      \ IOException(\"Unexpected End of Stream\");\n    }\n    int serialNumber =\n\
      \        ByteBuffer.wrap(buffer).order(ByteOrder.LITTLE_ENDIAN).getInt();\n\
      \  } finally {\n    if (dis != null) {\n  try {\n    dis.close();\n  } catch\
      \ (IOException x) {\n    // Handle error\n  }\n    }\n  }\n} catch (IOException\
      \ x) {\n  // Handle error\n}\n```\n**Compliant Solution (Define Special-Purpose\
      \ Methods)**\n\nAn alternative compliant solution is to define read and write\
      \ methods that support the necessary byte-swapping while reading from or writing\
      \ to the file. In this example, the `readLittleEndianInteger()` method reads\
      \ four bytes into a byte buffer and then pieces together the integer in the\
      \ correct order. The `writeLittleEndianInteger()` method obtains bytes by repeatedly\
      \ casting the integer so that the least significant byte is extracted on successive\
      \ right shifts. `Long` values can be handled by defining a byte buffer of size\
      \ 8.\n\n```java\n// Read method\npublic static int readLittleEndianInteger(InputStream\
      \ ips)\n                                          throws IOException {\n  byte[]\
      \ buffer = new byte[4];\n  int check = ips.read(buffer);\n\n  if (check != 4)\
      \ {\n    throw new IOException(\"Unexpected End of Stream\");\n  }\n\n  int\
      \ result = (buffer[3] << 24) | (buffer[2] << 16) |\n               (buffer[1]\
      \ << 8)  | buffer[0];\n  return result;\n}\n\n// Write method\npublic static\
      \ void writeLittleEndianInteger(int i, OutputStream ops)\n  throws IOException\
      \ {\n  byte[] buffer = new byte[4];\n  buffer[0] = (byte) i;\n  buffer[1] =\
      \ (byte) (i >> 8);\n  buffer[2] = (byte) (i >> 16);\n  buffer[3] = (byte) (i\
      \ >> 24);\n  ops.write(buffer);\n}\n```\n\n**Compliant Solution (`reverseBytes()`)**\n\
      \nWhen programming for JDK 1.5 and later, use the `reverseBytes()` method defined\
      \ in the classes `Character`, `Short`, `Integer`, and `Long` to reverse the\
      \ order of the integral value's bytes. Note that classes `Float` and `Double`\
      \ lack such a method.\n\n```java\npublic static int reverse(int i) {\n  return\
      \ Integer.reverseBytes(i);\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO12-J：为小端数据的读写提供方法
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO12-J:Provide methods to read and write little-endian data
    Default: true
    Description: "在Java中，数据是按照大端字节序（big-endian，同样被称为network-order）进行存储的。这意味着，线性的从最高位到最低位表示数据。在同时包括了大端字节序和小端字节序（little-endian）主机的网络化环境中，或者与使用JNI的其他语言在一起工作时，正确的处理相关的字节顺序问题是很重要的。不能处理字节顺序问题，会导致不可预期的行为。在Java\
      \ 4之前，要求提供对相反字节顺序进行管理的方法的定义，同时需要维护这些相反字节顺序与采用小端字节编码的系统的兼容性。  \n风险评估：违反该条准则可能会导致错误的解读数据，这种错误的解读包括对数据值、数据符号属性的错误解读。\
      \    \n**不规范的代码样例1**：  \n```java\ntry {\n  DataInputStream dis = null;\n  try\
      \ {\n    dis = new DataInputStream(new FileInputStream(\"data\"));\n    // Little-endian\
      \ data might be read as big-endian\n    int serialNumber = dis.readInt();\n\
      \  } catch (IOException x) {\n    // Handle error\n  } finally {\n    if (dis\
      \ != null) {\n      try {\n       dis.close();\n      } catch (IOException e)\
      \ {\n      // Handle error\n      }\n    }\n  }\n}\n```\n由类`java.io.DataInputStream`和`java.io.DataOutputStream`定义的数据读取的方法（`readByte()、readShort()、readInt()、readLong()、readFloat()、readDouble()`）和对应的写方法仅对大端字节序数据进行操作。使这些方法与C、C++语言进行交互操作是不安全的，这些语言不能保证字节顺序。"
    Description_En: "In Java, data is stored in big-endian format (also called network\
      \ order). That is, all data is represented sequentially starting from the most\
      \ significant bit to the least significant. JDK versions prior to JDK 1.4 required\
      \ definition of custom methods that manage reversing byte order to maintain\
      \ compatibility with little-endian systems. Correct handling of byte order–related\
      \ issues is critical when exchanging data in a networked environment that includes\
      \ both big-endian and little-endian machines or when working with other languages\
      \ using Java Native Interface (JNI). Failure to handle byte-ordering issues\
      \ can cause unexpected program behavior.\n  **Noncompliant Code Example**\n\n\
      The read methods (`readByte()`, `readShort()`, `readInt()`, `readLong()`, `readFloat()`,\
      \ and `readDouble()`) and the corresponding write methods defined by class `java.io.DataInputStream`\
      \ and class `java.io.DataOutputStream` operate only on big-endian data. Use\
      \ of these methods while interoperating with traditional languages, such as\
      \ C and C++, is insecure because such languages lack any guarantees about endianness.\
      \ This noncompliant code example shows such a discrepancy:\n\n```java\ntry {\n\
      \  DataInputStream dis = null;\n  try {\n    dis = new DataInputStream(new FileInputStream(\"\
      data\"));\n    // Little-endian data might be read as big-endian\n    int serialNumber\
      \ = dis.readInt();\n  } catch (IOException x) {\n    // Handle error\n  } finally\
      \ {\n    if (dis != null) {\n      try {\n       dis.close();\n      } catch\
      \ (IOException e) {\n      // Handle error\n      }\n    }\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 198:Use of Incorrect Byte Ordering
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141412'
    language: Java
  - Advice: "对应的符合规则的解决方案是：通过使用类 ByteBuffer 提供的方法来正确地从初始的输入数值中提取一个 int 值。它使用 ByteBuffer\
      \ 来封装输入字节数组，设置字节顺序为小端字节序，并且提取出 int值，结果存储在 serialNumber 整数中。类 ByteBuffer 提供了类似的\
      \ get 和 put 方法来处理其他的数值类型。   \n    ```java\n    try (   \n        DatalnputStream\
      \ dis = null;    \n        try {   \n            dis = new DatalnputStream(\
      \ new FilelnputStream(\"data\"));    \n            byte[] buffer = new byte[4];\
      \   \n            int bytesRead = dis.read(buffer); // Bytes are read into buffer\
      \   \n            if (bytesRead != 4) {   \n                throw new IOException(\"\
      Unexpected End of Stream\");   \n            }   \n            int serialNumber\
      \ =ByteBuffer.wrap(buffer).order(ByteOrder.LITTLE_ENDIAN).getlnt();   \n   \
      \     } finally {   \n            if (dis != null) {   \n                try\
      \ {   \n                    dis.close ();   \n                } catch (IOException\
      \ x) {   \n                    // handle error   \n                }   \n  \
      \          }   \n        }   \n    } catch (IOException x) {   \n        //handle\
      \ error   \n    }\n    ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 7.5 文件系统
      - type: DetailClassChin
        value: C.13.13 为小端数据的读写提供方法
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n与使用 JNI 的其他语言一起工作时，应正确地处理相关的字节顺序问题。   \n2) 原理   \n在 Java\
      \ 中，数据是按照大端字节序进行存储的（同样被称为网络字节编组）。这意味着，线性地从最高位到最低位表示数据。在 JDK1.4 之前的版本中，要求提供对相反的字节顺序进行管理的方法的定义，同时需要维护这些相反的字节顺序与采用小端（little-endian)字节序编码的系统的兼容性。在同时包括了大端字节序和小端字节序主机的网络化的环境中，或者与使用\
      \ JNI 的其他语言一起工作时，正确地处理相关的字节顺序问题是很重要的。不能处理字节顺序问题，会导致不可预期的程序行为。   \n3) 示例   \n\
      \    由类 java.io.DatalnputStream 和 java.io.DataOutputStream 定义的数据读取的方法（`readByte()、readShort()、readlnt()、readLong()、readFloat()`和\
      \ `readDouble()`）和对应的写方法仅对大端字节序数据进行操作。使用这些方法与传统的语言如 C 或者 C++进行互操作是不安全的，这些语言不能保证字节顺序。这个不符合规则的代码示例展示了这样的一个差别：\
      \   \n    ```java\n    try {   \n        DatalnputStream dis = null;    \n \
      \       try {   \n            dis = new DatalnputStream(new FilelnputStream(\"\
      data\"));   \n            //Little-endian data might be read as big-endian \
      \   \n            int serialNumber = dis.readlnt();   \n        } catch (IOException\
      \ x) {   \n            //handle error   \n        } finally {   \n         \
      \   if (dis != null) {   \n                try {   \n                    dis.close\
      \ ();   \n                } catch (IOException e) {   \n                // handle\
      \ error   \n                }   \n            }   \n        }\n    ```   \n\
      \    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 198:Use of Incorrect Byte Ordering
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160118
    language: Java
  - Advice: 确认代码逻辑，判断此代码是否是测试代码，然后将其删除或者替换为更安全的加密类。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【推荐】不要使用NullCipher
    Categories_En:
      Category: []
    Default: true
    Description: "根据约定，NullCipher类提供一个\"身份密码\"，它不会以任何方式转换或加密明文，密文与明文相同，这个类应用于测试，而不是在已经发布的代码中。\
      \  \n  \n**例如**：在下面的代码片段中，程序使用了NullCipher。\n```java  \n...  \nNullCipher nc\
      \ = new NullCipher();  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 327:Use of a Broken or Risky Cryptographic Algorithm
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
    Security: not
    Tag: security,correctness
    Type: std
    WhiteList: not
    id: '0202000010150115'
    language: Java
  - Advice: 不允许用户控制由程序加载库时所需要的传入参数，若难以避免会包含用户输入的话，那么可以为用户提供一个预先设定的、安全的输入集合来进行选择输入。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: GB/T 34944-2017
      - type: SubClassChin
        value: 输入验证
      - type: DetailClassChin
        value: 有风险的动态库加载
    Categories_En:
      Category: []
    Default: true
    Description: "加载动态链接库时没有指定绝对路径，这样可能会导致加载攻击者提供的恶意代码库。\n\n**例如**：下面代码中，如果参数`libraryName`是用户可控的，那么程序可能会加载攻击者提供的恶意代码库。\n\
      ```java\npublic static void loadLibrary(String libraryName){\n    System.load(libraryName);\n\
      \    ...\n}\n```"
    Description_En: ''
    DomainType: security
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行可能加载被用户控制的库。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行可能加载被用户控制的库。"
      th:remove="tag"></p>'
    ENClassChin: Input Validation and Representation
    ENDetailClassChin:
    - ''
    ENSubClassChin: Process Control
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 114:Process Control
      - type: OWASP TOP 10
        value: 2021:A1 Broken Access Control
      - type: OWASP TOP 10
        value: 2017:A5 Broken Access Control
      - type: OWASP TOP 10
        value: 2013:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2010:A4 Insecure Direct Object References
      - type: OWASP TOP 10
        value: 2007:A4 Insecure Direct Object Reference
      - type: OWASP TOP 10
        value: 2004:A1 Unvalidated Input
      - type: PCI DSS
        value: 1.1:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2:Requirement 6.5.10
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.10
      - type: PCI DSS
        value: 2.0:Requirement 6.5.8
      - type: PCI DSS
        value: 3.0:Requirement 6.5.8
      - type: PCI DSS
        value: 3.1:Requirement 6.5.8
      - type: PCI DSS
        value: 3.2:Requirement 6.5.8
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.8
      - type: GB/T 39412-2020
        value: 6.安全功能缺陷审计:6.1.数据清洗:6.1.1.输入验证
      - type: GB/T 34944-2017 Java
        value: 6.2.3.数据处理:6.2.3.6.进程控制
    Security: true
    Tag: security
    Type: bug
    WhiteList: not
    id: '0402000010050025'
    language: Java
  - Advice: "不要为ScheduledThreadPoolExecutor设置最大线程数。  \n  \n**例如**：下列代码中,在ScheduledThreadPoolExecutor初始化时定义更大的线程池。\
      \  \n```java\npublic static void init() {  \n    ScheduledThreadPoolExecutor\
      \ stpe = new ScheduledThreadPoolExecutor(20);   \n    ...  \n}  \n...\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【强制】不要设置ScheduledThreadPoolExecutor最大线程数
    Categories_En:
      Category: []
    Default: true
    Description: "ThreadPoolExecutor使用`corePoolSize`数量的线程和无界队列作为固定大小的线程池，即使设置最大线程数，也没有任何有用的效果。\
      \  \n  \n**例如**：下列代码中使用`setMaximumPoolSize`方法为ScheduledThreadPoolExecutor设置最大线程数。\
      \  \n```java\npublic static void init() {  \n    ScheduledThreadPoolExecutor\
      \ stpe = new ScheduledThreadPoolExecutor(10);   \n    stpe.setMaximumPoolSize(20);\
      \  \n}  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: correctness,bad_practice
    Type: std
    WhiteList: not
    id: 0202000010150118
    language: Java
  - Advice: "对`read()`方法的多次调用、或采用`DataInputStream.readFully()`方法。  \n**规范的代码样例1**：多次调用read()方法\
      \  \n```java\npublic static String readBytes(InputStream in) throws IOException\
      \ {\n    int offset = 0;\n    int bytesRead = 0;\n    byte[] data = new byte[1024];\n\
      \    while ((bytesRead = in.read(data, offset, data.length - offset)) != -1)\
      \ {\n        offset += bytesRead;\n        if (offset >= data.length) {\n  \
      \          break;\n        }\n    }\n    String str = new String(data, \"UTF-8\"\
      );\n    return str;\n}\n```  \n**规范的代码样例2**：DataInputStream.readFully()方法  \n\
      ```java\npublic static String readBytes(FileInputStream fis) throws IOException\
      \ {\n    byte[] data = new byte[1024];\n    DataInputStream dis = new DataInputStream(fis);\n\
      \    dis.readFully(data);\n    String str = new String(data, \"UTF-8\");\n \
      \   return str;\n}\n```"
    Advice_En: "**Compliant Solution (Multiple Calls to `read()`)**\nThis compliant\
      \ solution reads all the desired bytes into its buffer, accounting for the total\
      \ number of bytes read and adjusting the remaining bytes' offset, consequently\
      \ ensuring that the required data is read in full. It also avoids splitting\
      \ multibyte encoded characters across buffers by deferring construction of the\
      \ result string until the data has been fully read. (see IDS10-J. Do not assume\
      \ every character in a string is the same size for more information).\n```java\n\
      public static String readBytes(InputStream in) throws IOException {\n  int offset\
      \ = 0;\n  int bytesRead = 0;\n  byte[] data = new byte[1024];\n  while ((bytesRead\
      \ = in.read(data, offset, data.length - offset))\n    != -1) {\n    offset +=\
      \ bytesRead;\n    if (offset >= data.length) {\n      break;\n    }\n  }\n \
      \ String str = new String(data, 0, offset, \"UTF-8\");\n  return str;\n}\n```\n\
      **Compliant Solution (`readFully()`)**\n\nThe no-argument and one-argument `readFully()`\
      \ methods of the `DataInputStream` class guarantee that either all of the requested\
      \ data is read or an exception is thrown. These methods throw `EOFException`\
      \ if they detect the end of input before the required number of bytes have been\
      \ read; they throw `IOException` if some other I/O error occurs.\n\n```java\n\
      public static String readBytes(FileInputStream fis)\n                      \
      \         throws IOException {\n  byte[] data = new byte[1024];\n  DataInputStream\
      \ dis = new DataInputStream(fis);\n  dis.readFully(data);\n  String str = new\
      \ String(data, \"UTF-8\");\n  return str;\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 13. 输入输出 (FIO)
      - type: DetailClassChin
        value: FIO10-J：使用read()方法保证填充一个数组
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 13. Input Output (FIO)
      - type: DetailClassChin
        value: FIO10-J:Ensure the array is filled when using read() to fill an array
    Default: true
    Description: "对InputStream和Reader类及其子类的`read()`方法，在其设计上考虑到对byte或字符数组进行填充的情况时，是复杂的。\
      \  \n根据JavaAPI对InputStream的read(byte[] b, int off, int len)方法的描述：  \n> 类 InputStream\
      \ 的 read(b, off, len) 方法重复调用方法 read()。如果第一次这样的调用导致 IOException，则从对 read(b, off,\
      \ len)方法的调用中返回该异常。如果对 read() 的任何后续调用导致 IOException，则捕获该异常并将其视为到达文件末尾；到达该点时读取的字节存储在\
      \ b 中，并返回发生异常之前读取的字节数。在已读取输入数据 len 的请求数量、检测到文件结束标记、抛出异常前，此方法的默认实现将一直阻塞。建议子类提供此方法更为有效的实现。\n\
      \n然而，对于read(byte[] b)方法：  \n> 从输入流中读取一定数量的字节，并将其存储在缓冲区数组 b 中。以整数形式返回实际读取的字节数，读取的字节数最多等于\
      \ b 的长度。在输入数据可用、检测到文件末尾或者抛出异常前，此方法一直阻塞。\n\n当发现可获得输入数据时，`read()`方法就会返回结果。作为结果，如果可以获得的数值不是填充数组时，这些方法会在数组完成填充之前停止读取数据。\
      \  \n当使用`read()`方法来填充一个数组时，程序必须对`read()`的返回值进行检查，并且必须对仅仅进行了部分填充的情况进行处理。  \n\
      本规则仅对使用了一个数组做为参数的`read()`方法适用。为了读入一个单独的字节，可以使用不带参数并且返回值为int类型的`InputStream.read()`方法。为了读取一个单独的字符，可以使用不带参数并且将读取字符做为int类型返回的`Reader.read()`方法。\
      \  \n风险评估：违反该条准则会导致读入错误数量的字节，或者导致字节序列被错误的解释。    \n**不规范的代码样例1**：  \n```java\n\
      public static String readBytes(InputStream in) throws IOException {\n    byte[]\
      \ data = new byte[1024];\n    if (in.read(data) == -1) {\n        throw new\
      \ EOFException();\n    }\n    return new String(data, \"UTF-8\");\n}\n```  \n\
      代码希望在InputStream中读入1024个UTF-8编码的字节，并且将其做为一个字符串返回。然而，数据小于1024字节是有可能的，并且在输入流中还有更多的数据存在，这会导致不能完全读入预期的数据。"
    Description_En: "The contracts of the read methods for InputStream and Reader\
      \ classes and their subclasses are complicated with regard to filling byte or\
      \ character arrays. According to the Java API [API 2014] for the class  `<a\
      \ class=\"external-link\" href=\"https://docs.oracle.com/javase/8/docs/api/java/io/InputStream.html\"\
      \ rel=\"nofollow\">InputStream</a>`, the `read(byte[] b)` method and the `read(byte[]\
      \ b, int off, int len)` method provide the following behavior:\n\n> The number\
      \ of bytes actually read is returned as an integer. This method blocks until\
      \ input data is available, end of file is detected, or an exception is thrown.\n\
      \nAccording to the Java API for the `read(byte[] b<span>, int off, int len</span>)`\
      \ method:\n\n> An attempt is made to read as many as `len` bytes, but a smaller\
      \ number may be read, possibly zero.\n\nBoth `read` methods return as soon as\
      \ they find available input data. As a result, these methods can stop reading\
      \ data before the array is filled because the available data may be insufficient\
      \ to fill the array.\n\nThe documentation for the analogous `read` methods in\
      \ `Reader` return the number of characters read, which implies that they also\
      \ need not fill the `char` array provided as an argument.\nIgnoring the result\
      \ returned by the read() methods is a violation of EXP00-J. Do not ignore values\
      \ returned by methods.Security issues can arise even when return values are\
      \ considered because the default behavior of the `read()` methods lacks any\
      \ guarantee that the entire buffer array is filled. Consequently, when using\
      \ `read()` to fill an array, the program must check the return value of `read()`\
      \ and must handle the case where the array is only partially filled. In such\
      \ cases, the program may try to fill the rest of the array, or work only with\
      \ the subset of the array that was filled, or throw an exception.\n\nThis rule\
      \ applies only to `read()` methods that take an array argument. To read a single\
      \ byte, use the `InputStream.read()` method that takes no arguments and returns\
      \ an `int`. To read a single character, use a `Reader.read()` method that takes\
      \ no arguments and returns the character read as an `int`.\n**Noncompliant Code\
      \ Example (1-argument `read()`)**\n\nThis noncompliant code example attempts\
      \ to read 1024 bytes encoded in UTF-8 from an `InputStream` and return them\
      \ as a `String`. It explicitly specifies the character encoding used to build\
      \ the string, in compliance with STR04-J. Use compatible character encodings\
      \ when communicating string data between JVMs.\n```java\npublic static String\
      \ readBytes(InputStream in) throws IOException {\n  byte[] data = new byte[1024];\n\
      \  if (in.read(data) == -1) {\n    throw new EOFException();\n  }\n  return\
      \ new String(data, \"UTF-8\");\n}\n```\nThe programmer's misunderstanding of\
      \ the general contract of the `read()` method can result in failure to read\
      \ the intended data in full. It is possible that less than 1024 bytes exist\
      \ in the stream, perhaps because the stream originates from a file with less\
      \ than 1024 bytes. It is also possible that the stream contains 1024 bytes but\
      \ less than 1024 bytes are immediately available, perhaps because the stream\
      \ originates from a TCP socket that sent more bytes in a subsequent packet that\
      \ has not arrived yet. In either case, `read()` will return less than 1024 bytes.\
      \ It indicates this through its return value, but the program ignores the return\
      \ value and uses the entire array to construct a string, even though any unread\
      \ bytes will fill the string with null characters."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141410'
    language: Java
  - Advice: "使用valueOf方法来代替构造方法。  \n  \n**例如**：下面代码示例中，使用了`Integer.valueOf(int expression)`来创建对象\
      \ \n```java\nInteger integer = Integer.valueOf(100); //与Integer integer = 100相同\n\
      ```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要创建基本数据类型包装类
    Categories_En:
      Category: []
    Default: true
    Description: "Java默认会对整数-128到127之间进行缓存，程序中采用`new Integer(int expression)`等基本数据类型包装类构造函数来创建对象，而参数的值在-128到127之间，该方法产生的额外对象将占用更多空间、降低性能。\
      \  \n  \n**例如**：下面代码示例中，使用了`new Integer(int expression)`来创建对象。  \n```java\n\
      Integer integer = new Integer(100);\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: performance,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150454'
    language: Java
  - Advice: "与事务相关联的资源对象（比如数据库连接）不要存储在静态字段中。  \n  \n**例如**：下面代码片段中Connection为局部变量。\
      \  \n```java\npublic class DBUtils{  \n    ...  \n    public void doSomthing()\
      \ {  \n        Connection conn = DriverManager.getConnection(url,\"root\",null);\
      \  \n        ...  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 并发规范
      - type: DetailClassChin
        value: 【推荐】不要使用静态数据库连接
    Categories_En:
      Category: []
    Default: true
    Description: "存储在静态字段中的数据库连接会被不同的线程共享，因此存储在静态字段中的数据库连接可能会导致线程间数据泄露。  \n  \n**例如**：下面代码片段中Connection被设为静态。\
      \  \n```java\npublic class DBUtils{  \n    public static Connection connection;\
      \  \n    ...  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Static Database Connection
    ENSubClassChin: Race Condition
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 567:Unsynchronized Access to Shared Data in a Multithreaded Context
      - type: CWE
        value: 362:Concurrent Execution using Shared Resource with Improper Synchronization
          ('Race Condition')
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 362
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 362
      - type: ISO/IEC TR 24772
        value: 2013:New Vulnerabilities Concurrent Data Access [CGX]
    Security: not
    Tag: concurrency,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150303'
    language: Java
  - Advice: "比较两个数组的内容可以采用在`java.util.Arrays`中`equals`方法。\n**规范的代码样例1**：  \n```java\n\
      int[] arr1 = new int[20]; // Initialized to 0\nint[] arr2 = new int[20]; //\
      \ Initialized to 0\nSystem.out.println(Arrays.equals(arr1, arr2)); // Prints\
      \ true\n```"
    Advice_En: '**Compliant Solution**


      This compliant solution compares the content of two arrays using the two-argument
      `Arrays.equals()` method:


      ```java

      int[] arr1 = new int[20]; // Initialized to 0

      int[] arr2 = new int[20]; // Initialized to 0

      System.out.println(Arrays.equals(arr1, arr2)); // Prints true

      ```'
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 02. 表达式 (EXP)
      - type: DetailClassChin
        value: EXP02-J：不要使用 Object.equals() 方法来比较两个数组
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 02. Expressions (EXP)
      - type: DetailClassChin
        value: EXP02-J:Do not use the Object.equals() method to compare two arrays
    Default: true
    Description: "数组没有重写`Object.equals()`方法，所以直接在数组上使用一个参数的`equals`方法，比较的是数组的引用而不是数组的内容值，会导致不可预期的后果。并且在判断两个数组的引用是否相等时，也不推荐采用一个参数的equals方法，而是要采用相等操作符`==或!=`来比较。\n\
      在`java.util.Arrays`中实现了两个参数的`equals`方法来比较两个数组的内容。  \n**不规范的代码样例1**：\n以下代码使用\
      \ `Object.equals()` 方法比较两个数组\n```java\nint[] arr1 = new int[20]; // Initialized\
      \ to 0\nint[] arr2 = new int[20]; // Initialized to 0\nSystem.out.println(arr1.equals(arr2));\
      \ // Prints false\n```"
    Description_En: 'In Java, arrays are objects and support object methods such as
      `Object.equals()`. However, arrays do not support any methods besides those
      provided by `Object`. Consequently, using `Object.equals()` on any array compares
      only array *references*, not their *contents*. Programmers who wish to compare
      the contents of two arrays must use the static two-argument `Arrays.equals()`
      method. This method considers two arrays equivalent if both arrays contain the
      same number of elements, and all corresponding pairs of elements in the two
      arrays are equivalent, according to `Object.equals()`. In other words, two arrays
      are equal if they contain equivalent elements in the same order. To test for
      reference equality, use the reference equality operators, `==<span> </span>`and
      `!=`.


      Because the effect of using `Object.equals()` to compare two arrays is often
      misconstrued as content equality, and because a better alternative exists in
      the use of reference equality operators, the use of the `Object.equals()` method
      to compare two arrays is disallowed.


      **Noncompliant Code Example**


      This noncompliant code example uses the `Object.equals()` method to compare
      two arrays:


      ```java

      int[] arr1 = new int[20]; // Initialized to 0

      int[] arr2 = new int[20]; // Initialized to 0

      System.out.println(arr1.equals(arr2)); // Prints false

      ```'
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 754:Improper Check for Unusual or Exceptional Conditions
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: CWE/SANS TOP 25
        value: 2011:Risky Resource Management - CWE ID 754
      - type: SJ/T 11683-2017
        value: 6.3.4 equals方法的错误使用
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140402'
    language: Java
  - Advice: "应使用`java.util.Arrays.equals()`方法比较两个数组的内容。   \n  \n**例如**：下面代码片段中，使用`java.util.Arrays.equals()`方法比较两个数组。\
      \  \n```java\npublic void arrayEqualsGood(){  \n    int[] arra1 = new int[20];\
      \  \n    int[] arra2 = new int[20];  \n    boolean isEqual = Arrays.equals(arra1,\
      \ arra2);  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【强制】不要用Object.equals()比较两个数组
    Categories_En:
      Category: []
    Default: true
    Description: "数组不会重写父类Object的`equals()`方法，Object中`equals()`方法比较的是数组引用也就是数据的内存地址而不是数组中的内容，当需要比较两数组中的内容是否一致时不要使用`Object.equals()`方法。\
      \  \n  \n**例如**：下面代码片段中，不正确的使用`Object.equals()`方法比较两个数组。  \n```java\npublic\
      \ void arrayEqualBad(){  \n    int[] arra1 = new int[20];  \n    int[] arra2\
      \ = new int[20];  \n    boolean isEqual = arra1.equals(arra2);  \n }\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Invalid Call to Object.equals()
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 754:Improper Check for Unusual or Exceptional Conditions
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: CERT
        value: EXP02-J:Do not use the Object.equals() method to compare two arrays
      - type: CWE/SANS TOP 25
        value: 2011:Risky Resource Management - CWE ID 754
      - type: SJ/T 11683-2017
        value: 6.3.4 equals方法的错误使用
    Security: not
    Tag: correctness
    Type: std
    WhiteList: not
    id: '0202000010150112'
    language: Java
  - Advice: "相应的符合规则的代码示例：使用参数的 `Arrays.equals()`方法比较两个数组。   \n    ```java\n    public\
      \ void arrayEqualsExample(){   \n        int[] arr1=new int[20];//initialized\
      \ to 0   \n        int[] arr2=new int[20];//initialized to 0   \n        Arrays.equals(arr1,arr2);//true\
      \   \n    }\n    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.3.3 使用参数的Arrays,equals()方法比较两个数组的内容
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n应使用参数的 `Arrays.equals()`方法比较两个数组的内容。   \n2) 原理   \n数组不会重写\
      \ `Object.equals()`方法，而 equals()方法比较的是数组引用而不是数组的内容值。应使用参数的 `Arrays.equals()`方法比较两个数组的内容。\
      \   \n若要测试引用是否相等，应使用引用相等操作符：`==`和`!=`，而不是使用数组的`equals()`方法，否则会导致不可预期的后果。   \n\
      3) 示例   \n    1.这个不符合规则的代码示例错误的使用了 `Object．equals()`方法比较两个数组。   \n    ```java\n\
      \    public void arrayEqualsExample(){   \n        int[] arr1=new int[20];//initialized\
      \ to 0   \n        int[] arr2=new int[20];//initialized to 0   \n        arr1.equals(arr2);//false\
      \   \n    }\n    ```   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 754:Improper Check for Unusual or Exceptional Conditions
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: CERT
        value: EXP02-J:Do not use the Object.equals() method to compare two arrays
      - type: CWE/SANS TOP 25
        value: 2011:Risky Resource Management - CWE ID 754
      - type: SJ/T 11683-2017
        value: 6.3.4 equals方法的错误使用
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160020
    language: Java
  - Advice: "\n3) 示例   \n    **示例 1**：当需要使用反射时，确认直接的调用者通过声明为 private和 final和恶意代码是分开的，正如这个符合规定的方案所展示的一样：\
      \   \n    ```java\n    class FieldExample {   \n        //...   \n        private\
      \ void zeroField(String fieldName) {   \n            // ...   \n        }  \
      \ \n    }\n    ```   \n    注意，当通过使用 `java.lang.reflect.Field.setAccessible()`来覆写语言访问检查的时候，直\
      \ 接调用者甚至可以访问其他类的私有字段。因此，不要使用`suppressAccessChecks`来赋予ReflectPermission权限，这会确保安全管理器阻塞那些试图访问其他类的私有字段的企图。\
      \   \n    **示例 2**：当一个类必须要使用反射来对字段进行访问的时候，它同时必须使用一个非反射的接口来进行同样访问。这个符合规定的方案提供了受限的\
      \ setter 方法，从而赋予所有的调用者不使用反射也能访问它的字段的能力。如果这些 setter 方法符合其他所有的规则或者安全策略，那么使用反射也同样符合这条规则。\
      \   \n    ```java\n    class FieldExample {\n        // . . .\n        public\
      \ void zeroField(String fieldName) {\n            // . . .\n        }\n    \n\
      \        public void zeroI() {\n            this.i = 0;\n        }\n    \n \
      \       public void zeroJ() {\n            this.i = 0;\n        }\n    }\n \
      \   ```   \n    **示例 3**：这个符合规定的方案将对`create()`的访问减少到包私有的程度，可以防止在包外部的调用者 使用这个方法来绕过语言检查从而创建出\
      \ Trusted 类的实例。使用反射可以创建出 Trusted 类的实例的调用者，这时可以通过简单的调用`Trusted()`构造方法来替代。   \n\
      \    ```java\n    package Safe;   \n    public class Trusted {   \n        Trusted()\
      \ { } // package private constructor   \n        static <T> T create(Class<T>\
      \ c)  throws InstantiationException, IllegalAccessException {\n            return\
      \ c.newlnstance ();\n        }   \n    } \n    ```\n     \n    **示例 4**：这个符合规定的方案使用\
      \ `getConstructors()`方法来检査作为参数的类是否有公有的构 造方法。当具有公有的构造方法的时候，所以没有相关的安全问题，这个构造方法本身已经可以被恶意代码访问。当其没有公有的构造方法时，`create()`方法使用安全管理器的\
      \ `checkPackageAccess()`方法来保证所有在执行链中的调用者有足够的权限来访问定义在 Safe包中的类和它们对应的字段。   \n\
      \    ```java\n    package Safe;\n    import java.beans.Beans;\n    import java.io.IOException;\n\
      \    \n    public class Trusted {\n        Trusted() {\n        }\n    \n  \
      \      public static <T> T create(Class<T> c) throws InstantiationException,\
      \ IllegalAccessException {\n            if (c.getConstructors().length == 0)\
      \ { // No public constructors\n                SecurityManager sm = System.getSecurityManager();\n\
      \                if (sm != null) {\n                    // throws an exception\
      \ when access is not allowed\n                    sm.checkPackageAccess(\"Safe\"\
      );\n                }\n            }\n            return c.newInstance(); //\
      \ Safe to return\n        }\n    }\n    ```  \n    **示例 5**：这个符合规定的方案使用`java.beans.Beans`API\
      \ 来检査接收到的 Class 对象是否有公有的构造函数。   \n    ```java\n    public class Trusted {\n\
      \        Trusted() {\n        }\n    \n        public static <T> T create(Class<T>\
      \ c) throws IOException, ClassNotFoundException {\n            // Executes without\
      \ exception only if there are public constructors\n            ClassLoader cl\
      \ = new SafeClassLoader();\n            Object b = Beans.instantiate(cl, c.getName());\n\
      \            return c.cast(b);\n        }\n    }\n    ```\n    这个 `Beans.instantiate()`方法仅当实例化的类有公有的构造函数时才会成功，否则，它会抛出IllegalAccessException\
      \ 异常。该方法使用了一个类装载器参数和一个需要实例化的类的名称参数。不像前面的符合规定的例子一样，这个方法避免了使用反射权限的需要。"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.15.6 不要使用反射来增加类、方法和字段的可访问性
    Categories_En:
      Category: []
    Default: not
    Description: "1) 规定   \n反射不应用来提供对类、方法和字段的访问，除非这些类、方法和数据成员本身已经不通过反射就不可以访问。   \n\
      2) 原理   \n使用反射会将安全分析弄复杂，并且会比较容易地引入安全漏洞。因此，就算当它们可以用的时候，开发者也应当避免使用这些反射 API。当使用反射的时候，需要特别地小心谨慎。特别地，反射不应当用来提供对类、方法和字段的访问，除非这些类、方法和数据成员本身已经不通过反射就不可以访问。例如，使用反射来访问或修改字段是不允许的，除非那些字段本身就可以被其他的方式访问和修改，例如通过\
      \ getter和 setter方法。   "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference: []
    Security: true
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160138
    language: Java
  - Advice: "**规范的代码样例1**：\n这个兼容的解决方案覆盖了`hashCode()`方法，这样它就能为任何两个被`equals()`方法认为是相等的实例生成相同的值。Bloch详细讨论了生成这种哈希函数的配方[[Bloch\
      \ 2008]\n```java\npublic final class CreditCard {\n  private final int number;\n\
      \n  public CreditCard(int number) {\n    this.number = number;\n  }\n\n  public\
      \ boolean equals(Object o) {\n    if (o == this) {\n      return true;\n   \
      \ }\n    if (!(o instanceof CreditCard)) {\n      return false;\n    }\n   \
      \ CreditCard cc = (CreditCard)o;\n    return cc.number == number;\n  }\n\n \
      \ public int hashCode() {\n    int result = 17;\n    result = 31 * result +\
      \ number;\n    return result;\n  }\n\n  public static void main(String[] args)\
      \ {\n    Map<CreditCard, String> m = new HashMap<CreditCard, String>();\n  \
      \  m.put(new CreditCard(100), \"4111111111111111\");\n    System.out.println(m.get(new\
      \ CreditCard(100)));\n  }\n}\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution overrides the `hashCode()`\
      \ method so that it generates the same value for any two instances that are\
      \ considered to be equal by the `equals()` method. Bloch discusses the recipe\
      \ to generate such a hash function in detail [Bloch 2008].\n\n```java\npublic\
      \ final class CreditCard {\n  private final int number;\n\n  public CreditCard(int\
      \ number) {\n    this.number = number;\n  }\n\n  public boolean equals(Object\
      \ o) {\n    if (o == this) {\n      return true;\n    }\n    if (!(o instanceof\
      \ CreditCard)) {\n      return false;\n    }\n    CreditCard cc = (CreditCard)o;\n\
      \    return cc.number == number;\n  }\n\n  public int hashCode() {\n    int\
      \ result = 17;\n    result = 31 * result + number;\n    return result;\n  }\n\
      \n  public static void main(String[] args) {\n    Map<CreditCard, String> m\
      \ = new HashMap<CreditCard, String>();\n    m.put(new CreditCard(100), \"4111111111111111\"\
      );\n    System.out.println(m.get(new CreditCard(100)));\n  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 06. 方法 (MET)
      - type: DetailClassChin
        value: MET09-J：声明了equals()方法的类必须声明hashCode()方法
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 06. Methods (MET)
      - type: DetailClassChin
        value: MET09-J:Classes that define an equals() method must also define a hashCode()
          method
    Default: true
    Description: "覆盖`Object.equals()`方法的类也必须覆盖`Object.hashCode()`方法。`java.lang.Object`类要求任何两个使用`equals()`方法比较相等的对象在对对象调用`hashCode()`方法时必须产生相同的整数结果[[API\
      \ 2014]。\n`equals()`方法是用来确定对象实例之间的逻辑等价性。因此，`hashCode()`方法必须为所有相等的对象返回相同的值。不遵守这个契约是一个常见的缺陷来源。\n\
      **不规范的代码样例1**：\n这个不符合规定的代码例子使用 \"HashMap \"将信用卡号码与字符串联系起来，随后试图检索与信用卡号码相关的字符串值。预期的检索值是`41111111111111`；实际的检索值是`null`。\n\
      ```java\npublic final class CreditCard {\n  private final int number;\n\n  public\
      \ CreditCard(int number) {\n    this.number = number;\n  }\n\n  public boolean\
      \ equals(Object o) {\n    if (o == this) {\n      return true;\n    }\n    if\
      \ (!(o instanceof CreditCard)) {\n      return false;\n    }\n    CreditCard\
      \ cc = (CreditCard)o;\n    return cc.number == number;\n  }\n\n  public static\
      \ void main(String[] args) {\n    Map<CreditCard, String> m = new HashMap<CreditCard,\
      \ String>();\n    m.put(new CreditCard(100), \"4111111111111111\");\n    System.out.println(m.get(new\
      \ CreditCard(100)));\n  }\n}\n```\n这个错误行为的原因是`CreditCard`类重写了`equals()`方法，但没有重写`hashCode()`方法。因此，默认的`hashCode()`方法为每个对象返回不同的值，即使这些对象在逻辑上是等价的；这些不同的值导致在哈希表中检查不同的桶，这使得`get()`方法无法找到预定的值。\n\
      请注意，通过在`main()`中指定信用卡号码，这些代码例子违反了[MSC03-J. Never hard code sensitive information]。"
    Description_En: "Classes that override the `Object.equals()` method must also\
      \ override the `Object.hashCode()` method. The `java.lang.Object` class requires\
      \ that any two objects that compare equal using the `equals()` method must produce\
      \ the same integer result when the `hashCode()` method is invoked on the objects\
      \ [API 2014].\n\nThe `equals()` method is used to determine logical equivalence\
      \ between object instances. Consequently, the `hashCode()` method must return\
      \ the same value for all equivalent objects. Failure to follow this contract\
      \ is a common source of defects.\n\n**Noncompliant Code Example**\n\nThis noncompliant\
      \ code example associates credit card numbers with strings using a `HashMap`\
      \ and subsequently attempts to retrieve the string value associated with a credit\
      \ card number. The expected retrieved value is `4111111111111111`; the actual\
      \ retrieved value is `null`.\n\n```java\npublic final class CreditCard {\n \
      \ private final int number;\n\n  public CreditCard(int number) {\n    this.number\
      \ = number;\n  }\n\n  public boolean equals(Object o) {\n    if (o == this)\
      \ {\n      return true;\n    }\n    if (!(o instanceof CreditCard)) {\n    \
      \  return false;\n    }\n    CreditCard cc = (CreditCard)o;\n    return cc.number\
      \ == number;\n  }\n\n  public static void main(String[] args) {\n    Map<CreditCard,\
      \ String> m = new HashMap<CreditCard, String>();\n    m.put(new CreditCard(100),\
      \ \"4111111111111111\");\n    System.out.println(m.get(new CreditCard(100)));\n\
      \  }\n}\n```\n\nThe cause of this erroneous behavior is that the `CreditCard`\
      \ class overrides the `equals()` method but fails to override the `hashCode()`\
      \ method. Consequently, the default `hashCode()` method returns a different\
      \ value for each object, even though the objects are logically equivalent; these\
      \ differing values lead to examination of different buckets in the hash table,\
      \ which prevents the `get()` method from finding the intended value.\nNote that\
      \ by specifying the credit card number in `main()`, these code examples violate\
      \ MSC03-J. Never hard code sensitive information for the sake of brevity."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '581:Object Model Violation: Just One of Equals and Hashcode Defined'
      - type: SJ/T 11683-2017
        value: 6.5.8 类定义了equals方法但是没有定义hashCode方法
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140709
    language: Java
  - Advice: "\n3) 示例   \n这个不符合规则的代码示例用 HashMap 将信用卡号和字符串联系在一起，随后尝试取得一个信用卡的卡号字符串。预期的数值是`4111111111111111`，但实际获得的数值是null。\
      \   \n```java\n    public final class CreditCard {\n        private final int\
      \ number;\n\n        public CreditCard(int number) {\n            this.number\
      \ = (short) number;\n        }\n\n        public boolean equals(Object o) {\n\
      \            if (o == this) {\n                return true;\n            }\n\
      \            if (!(o instanceof CreditCard)) {\n                return false;\n\
      \            }\n            CreditCard cc = (CreditCard) o;\n            return\
      \ cc.number == number;\n        }\n        public static void main(String[]\
      \ args) {\n            Map<CreditCard, String> m = new HashMap<CreditCard, String>();\n\
      \            m.put(new CreditCard(100), \"4111111111111111\");\n           \
      \ System.out.println(m.get(new CreditCard(100)));\n        }\n    }\n``` \n\
      造成这一错误行为的原因是 CreditCrad 类覆写了`equlas()`方法，但没有覆写`hashCode()`方法。 因此，默认的`hashCode()`方法对不同的对象返回一个不同的数值，即便这些对象在逻辑上是等同的；这些不同的数值导致了査看不同的散列桶，这阻碍了`get()`方法找到预期的数值。\
      \   \n   \n"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.6.10 定义了 equlas（）方法的类必须定义hashCode（）方法
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n覆写了 `Object.equals()`方法的类必须同时覆写 `Object.hashCode()`方法。\
      \   \n2) 原理   \n`java.lang.Object`类要求当两个对象用`equals()`方法进行比较并得到等同的结果时，对这两个对象调用\
      \ `hashCode()`方法时必须产生一样的整数结果。   \n`equals()`方法用于判定两个对象实例逻辑上的等同性。因此，对于所有等同的对象，`\
      \ hashCode()`方法必须返回相同的数值。没能遵守这一合约是一个常见的产生错误的原因。   "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '581:Object Model Violation: Just One of Equals and Hashcode Defined'
      - type: CERT
        value: MET09-J:Classes that define an equals() method must also define a hashCode()
          method
      - type: SJ/T 11683-2017
        value: 6.5.8 类定义了equals方法但是没有定义hashCode方法
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160059
    language: Java
  - Advice: "**规范的代码样例1**：  \n对于int参数的有效范围校验，采用Integer.MIN_VALUE和Integer.MIN_VALUE与参数进行比较。\
      \  \n```java\npublic static int getAbsAdd(int x, int y) {\n    if (x == Integer.MIN_VALUE\
      \ || y == Integer.MIN_VALUE) {\n        throw new IllegalArgumentException();\n\
      \    }\n    int absX = Math.abs(x);\n    int absY = Math.abs(y);\n    if (absX\
      \ > Integer.MAX_VALUE - absY) {\n        throw new IllegalArgumentException();\n\
      \    }\n    return absX + absY;\n}\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution validates the method\
      \ arguments by ensuring that values passed to `Math.abs()` exclude `Integer.MIN_VALUE`\
      \ and also by checking for integer overflow:\n\n```java\npublic static int getAbsAdd(int\
      \ x, int y) {\n  if (x == Integer.MIN_VALUE || y == Integer.MIN_VALUE) {\n \
      \   throw new IllegalArgumentException();\n  }\n  int absX = Math.abs(x);\n\
      \  int absY = Math.abs(y);\n  if (absX > Integer.MAX_VALUE - absY) {\n    throw\
      \ new IllegalArgumentException();\n  }\n  return absX + absY;\n}\n```\n\nAlternatively,\
      \ the addition could be performed using type `long` and the result of the addition\
      \ stored in a local variable of type `long`. This alternate implementation would\
      \ require a check to ensure that the resulting `long` can be represented in\
      \ the range of the type `int`. Failure of this latter check would indicate that\
      \ an `int` version of the addition would have overflowed."
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 06. 方法 (MET)
      - type: DetailClassChin
        value: MET01-J：不要使用断言验证方法参数
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 06. Methods (MET)
      - type: DetailClassChin
        value: MET01-J:Never use assertions to validate method arguments
    Default: true
    Description: "依据Java语言规范的14.10节“断言”：  \n> 断言不应用于公共方法中的参数检查。 参数检查通常是方法契约的一部分，无论断言是启用还是禁用，都必须维护这个契约。\n\
      >\n> 使用断言进行参数检查的第二个问题是错误的参数应该导致适当的运行时异常（例如`IllegalArgumentException`、`IndexOutOfBoundsException`\
      \ 或`NullPointerException`）。 断言失败不会引发适当的异常。\n**不规范的代码样例1**：\n`getAbsAdd()` 方法计算并返回参数\
      \ `x` 和 `y` 的绝对值之和。 它缺乏论证验证，违反了 MET00-J.验证方法参数。 因此，由于整数溢出或者当它的一个或两个参数都是 `Integer.MIN_VALUE`\
      \ 时，它可能会产生不正确的结果。\n```java\npublic static int getAbsAdd(int x, int y) {\n  return\
      \ Math.abs(x) + Math.abs(y);\n}\ngetAbsAdd(Integer.MIN_VALUE, 1);\n```\n**不规范的代码样例2**：\n\
      这个不规范的代码示例使用断言来验证公共方法的参数：\n```java\npublic static int getAbsAdd(int x, int y)\
      \ {\n  assert x != Integer.MIN_VALUE;\n  assert y != Integer.MIN_VALUE;\n  int\
      \ absX = Math.abs(x);\n  int absY = Math.abs(y);\n  assert (absX <= Integer.MAX_VALUE\
      \ - absY);\n  return absX + absY;\n}\n```\n断言检查的条件是合理的。 但是，禁用断言时不会执行验证代码。"
    Description_En: "Never use assertions to validate arguments of public methods.\
      \ The Java Language Specification, §14.10, \"The assert Statement\" [JLS 2015],\
      \ states that\n\n> assertions should not be used for argument checking in public\
      \ methods. Argument checking is typically part of the contract of a method,\
      \ and this contract must be upheld whether assertions are enabled or disabled.\n\
      >\n> A secondary problem with using assertions for argument checking is that\
      \ erroneous arguments should result in an appropriate run-time exception (such\
      \ as `IllegalArgumentException`, `IndexOutOfBoundsException`, or `NullPointerException`).\
      \ An assertion failure will not throw an appropriate exception.\n\n**Noncompliant\
      \ Code Example**\n\nThe method `getAbsAdd()` computes and returns the sum of\
      \ the absolute value of parameters `x` and `y`. It lacks argument validation,\
      \ in violation of MET00-J. Validate method arguments. Consequently, it can produce\
      \ incorrect results because of integer overflow or when either or both of its\
      \ arguments are `Integer.MIN_VALUE`.\n\n```java\npublic static int getAbsAdd(int\
      \ x, int y) {\n  return Math.abs(x) + Math.abs(y);\n}\ngetAbsAdd(Integer.MIN_VALUE,\
      \ 1);\n```\n\n**Noncompliant Code Example**\n\nThis noncompliant code example\
      \ uses assertions to validate arguments of a public method:\n\n```java\npublic\
      \ static int getAbsAdd(int x, int y) {\n  assert x != Integer.MIN_VALUE;\n \
      \ assert y != Integer.MIN_VALUE;\n  int absX = Math.abs(x);\n  int absY = Math.abs(y);\n\
      \  assert (absX <= Integer.MAX_VALUE - absY);\n  return absX + absY;\n}\n```\n\
      \nThe conditions checked by the assertions are reasonable. However, the validation\
      \ code is not executed when assertions are disabled."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference:
      - type: CWE
        value: 617:Reachable Assertion
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140701'
    language: Java
  - Advice: "使用非对称加密算法RSA时，建议使用`OAEP(Optimal Asymmetric Encryption Padding)`作为填充模式。\
      \  \n  \n**例如**：下面代码片段中，使用OAEP作为填充模式。  \n```java\nCipher rsa = Cipher.getInstance(\"\
      RSA/ECB/OAEPWITHSHA-256ANDMGF1PADDING\");\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 密码规范
      - type: DetailClassChin
        value: 【推荐】RSA不要使用不适当的填充模式
    Categories_En:
      Category: []
    Default: true
    Description: "用程序使用非对称加密算法RSA，未使用`OAEP(Optimal Asymmetric Encryption Padding)`填充模式，将降低加密的安全性。\
      \  \n  \n**例如**：下面代码片段中，没有使用OAEP作为填充模式。 \n```java\nCipher rsa = Cipher.getInstance(\"\
      RSA/NONE/NoPadding\");\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Inadequate RSA Padding
    ENSubClassChin: Weak Encryption
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 325:Missing Required Cryptographic Step
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Missing Required Cryptographic Step
          [XZS]
      - type: GB/T 34944-2017 Java
        value: 安全功能:RSA算法未使用最优非对称加密填充
    Security: true
    Tag: security,password
    Type: std
    WhiteList: not
    id: '0202000010150203'
    language: Java
  - Advice: "确保程序日志记录能正确的执行，建议采用日志记录机制`java.util.logging.Logger`，或者其他符合规则的日志机制（log4j）记录日志。\
      \  \n**规范的代码样例1**：  \n```java\ntry {\n  // ...\n} catch(SecurityException se)\
      \ {\n  logger.log(Level.SEVERE, se);\n  // Recover from exception\n}\n```\n\
      通常，整个程序只需要一个记录器。"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution uses `java.util.logging.Logger`,\
      \ the default logging API provided by JDK 1.4 and later. Use of other compliant\
      \ logging mechanisms, such as log4j, is also permitted.\n\n```java\ntry {\n\
      \  // ...\n} catch(SecurityException se) {\n  logger.log(Level.SEVERE, se);\n\
      \  // Recover from exception\n}\n```\n\nTypically, only one logger is required\
      \ for the entire program."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 07. 异常行为 (ERR)
      - type: DetailClassChin
        value: ERR02-J：记录日志时应避免异常
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 07. Exceptional Behavior (ERR)
      - type: DetailClassChin
        value: ERR02-J:Prevent exceptions while logging data
    Default: true
    Description: "不要直接使用系统输出流来打印异常。除非小心的处理，否则记录日志时，所抛出的异常会妨碍记录的完成。如果没有考虑到写入日志时发生异常的可能性，则可能会造成安全漏洞。\
      \  \n**不规范的代码样例1**：\n这个不合规的代码示例将一个严重的安全异常写入标准错误流：\n```java\ntry {\n  // ...\n\
      } catch (SecurityException se) {\n  System.err.println(se);\n  // Recover from\
      \ exception\n}\n```\n将此类异常写入标准错误流不足以用于记录目的。 首先，标准错误流可能会被耗尽或关闭，从而阻止记录后续异常。 其次，标准错误流的信任级别可能不足以在不泄露敏感信息的情况下记录某些安全关键异常或错误。\
      \ 如果在写入安全异常时发生 I/O 错误，`catch` 块将抛出 `IOException` 并且关键安全异常将丢失。 最后，攻击者可能会伪装异常，使其与其他几个无害的异常一起发生。\n\
      使用 `Console.printf()`、`System.out.print*()` 或 `Throwable.printStackTrace()`\
      \ 输出安全异常也构成违反此规则。"
    Description_En: "Exceptions that are thrown while logging is in progress can prevent\
      \ successful logging unless special care is taken. Failure to account for exceptions\
      \ during the logging process can cause security vulnerabilities, such as allowing\
      \ an attacker to conceal critical security exceptions by preventing them from\
      \ being logged. Consequently, programs must ensure that data logging continues\
      \ to operate correctly even when exceptions are thrown during the logging process.\n\
      \n**Noncompliant Code Example**\n\nThis noncompliant code example writes a critical\
      \ security exception to the standard error stream:\n\n```java\ntry {\n  // ...\n\
      } catch (SecurityException se) {\n  System.err.println(se);\n  // Recover from\
      \ exception\n}\n```\n\nWriting such exceptions to the standard error stream\
      \ is inadequate for logging purposes. First, the standard error stream may be\
      \ exhausted or closed, preventing recording of subsequent exceptions. Second,\
      \ the trust level of the standard error stream may be insufficient for recording\
      \ certain security-critical exceptions or errors without leaking sensitive information.\
      \ If an I/O error were to occur while writing the security exception, the `catch`\
      \ block would throw an `IOException` and the critical security exception would\
      \ be lost. Finally, an attacker may disguise the exception so that it occurs\
      \ with several other innocuous exceptions.\n\nUsing `Console.printf()`, `System.out.print*()`,\
      \ or `Throwable.printStackTrace()` to output a security exception also constitutes\
      \ a violation of this rule."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: true
    References:
      Reference:
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: 0102000010140802
    language: Java
  - Advice: "**规范的代码样例1**：  \n去掉了方法`displayAccountStatus()`的关键字static，`displayAccountStatus()`即成了一个实例方法。因此，调用方法的动态方法扩展使执行得到了预期结果。\
      \  \n从理论上说，一个private方法是不能被隐藏或被覆写的。没有任何要求指定子类和基类中具有相同签名的private方法有任何联系，即使方法具有相同的返回类型和抛出异常，这是隐藏的必要条件。\
      \  \n```java\nclass GrantAccess {\n    public void displayAccountStatus() {\n\
      \        System.out.println(\"Account details for admin: XX\");\n    }\n}\n\n\
      class GrantUserAccess extends GrantAccess {\n    @Override\n    public void\
      \ displayAccountStatus() {\n        System.out.println(\"Account details for\
      \ user: XX\");\n    }\n}\n\npublic class StatMethod {\n    public static void\
      \ choose(String username) {\n        GrantAccess admin = new GrantAccess();\n\
      \        GrantAccess user = new GrantUserAccess();\n        if (username.equals(\"\
      admin\")) {\n            admin.displayAccountStatus();\n        } else {\n \
      \           user.displayAccountStatus();\n        }\n    }\n\n    public static\
      \ void main(String[] args) {\n        choose(\"user\");\n    }\n}\n```\n**例外情况**\n\
      MET07-J-EX0：有时，API 会提供隐藏的方法。 调用这些方法并不违反此规则，前提是所有隐藏方法的调用都使用限定名称或方法调用表达式，*明确*指示调用了哪个特定方法。\
      \ 例如，如果 `displayAccountStatus()` 是隐藏方法，则以下 `choose()` 方法的实现将是可接受的替代方法：\n```java\n\
      public static void choose(String username) {\n  if (username.equals(\"admin\"\
      )) {\n    GrantAccess.displayAccountStatus();\n  } else {\n    GrantUserAccess.displayAccountStatus();\n\
      \  }\n}\n```"
    Advice_En: "**Compliant Solution**\n\nIn this compliant solution, the programmer\
      \ declares the `displayAccountStatus()` methods as instance methods by removing\
      \ the `static` keyword. Consequently, the dynamic dispatch at the call sites\
      \ produces the expected result. The `@Override` annotation indicates intentional\
      \ overriding of the parent method.\n\n```java\nclass GrantAccess {\n  public\
      \ void displayAccountStatus() {\n    System.out.print(\"Account details for\
      \ admin: XX\");\n  }\n}\n\nclass GrantUserAccess extends GrantAccess {\n  @Override\n\
      \  public void displayAccountStatus() {\n    System.out.print(\"Account details\
      \ for user: XX\");\n  }\n}\n\npublic class StatMethod {\n  public static void\
      \ choose(String username) {\n    GrantAccess admin = new GrantAccess();\n  \
      \  GrantAccess user = new GrantUserAccess();\n\n    if (username.equals(\"admin\"\
      )) {\n      admin.displayAccountStatus();\n    } else {\n      user.displayAccountStatus();\n\
      \    }\n  }\n\n  public static void main(String[] args) {\n    choose(\"user\"\
      );\n  }\n}\n```\n\nThe methods inherited from the superclass can also be overloaded\
      \ in a subclass. Overloaded methods are new methods unique to the subclass and\
      \ neither hide nor override the superclass method [Java Tutorials].\n\nTechnically,\
      \ a private method cannot be hidden or overridden. There is no requirement that\
      \ private methods with the same signature in the subclass and the superclass\
      \ bear any relationship in terms of having the same return type or `throws`\
      \ clause, the necessary conditions for hiding [JLS 2015]. Consequently, hiding\
      \ cannot occur when private methods have different return types or `throws`\
      \ clauses.\n\n**Exceptions**\n\nMET07-J-EX0: Occasionally, an API provides hidden\
      \ methods. Invoking those methods is not a violation of this rule provided that\
      \ all invocations of hidden methods use qualified names or method invocation\
      \ expressions that *explicitly* indicate which specific method is invoked. If\
      \ the `displayAccountStatus()` were a hidden method, for example, the following\
      \ implementation of the `choose()` method would be an acceptable alternative:\n\
      \n```java\npublic static void choose(String username) {\n  if (username.equals(\"\
      admin\")) {\n    GrantAccess.displayAccountStatus();\n  } else {\n    GrantUserAccess.displayAccountStatus();\n\
      \  }\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 06. 方法 (MET)
      - type: DetailClassChin
        value: MET07-J：不要定义方法来隐藏基类或基类接口中声明的方法
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 06. Methods (MET)
      - type: DetailClassChin
        value: MET07-J:Never declare a class method that hides a method declared in
          a superclass or superinterface
    Default: true
    Description: "当声明了一个static方法ma时，ma的定义会隐藏任何符合条件的方法mb。这发生在当ma的签名是mb签名的子签名，并且mb是同时在声明类的基类或者基类接口中定义的（Java语言规范8.4.8.2节“隐藏(类方法)”）。\n\
      如果子类中定义的实例方法与父类中的另一个实例方法具有相同的名称、参数数量和类型，以及返回类型，则子类中定义的实例方法将重写该父类中的另一个实例方法。\n\
      隐藏和重写在确定从调用站点调用哪个方法方面有所不同。对于重写，调用的方法在运行时根据手头的特定对象实例确定。对于隐藏，调用的方法在编译时根据调用站点使用的特定限定名或方法调用表达式确定。尽管Java语言为确定调用哪个方法提供了明确的规则，但这些规则的结果通常是意外的。此外，在语言提供方法隐藏的情况下，程序员有时希望方法重写。因此，程序决不能声明隐藏在父类或父类接口中声明的方法的类方法。\n\
      **不规范的代码样例1**：\n代码中程序员隐藏了static方法而不是将其覆写。因此，代码在两个不同的地方调用了基类中的`displayAccountStatus()`方法，而不是在一处调用基类的方法，另一处调用子类的方法。\
      \  \n```java\nclass GrantAccess {\n  public static void displayAccountStatus()\
      \ {\n    System.out.println(\"Account details for admin: XX\");\n  }\n}\n\n\
      class GrantUserAccess extends GrantAccess {\n  public static void displayAccountStatus()\
      \ {\n    System.out.println(\"Account details for user: XX\");\n  }\n}\n\npublic\
      \ class StatMethod {\n  public static void choose(String username) {\n    GrantAccess\
      \ admin = new GrantAccess();\n    GrantAccess user = new GrantUserAccess();\n\
      \    if (username.equals(\"admin\")) {\n      admin.displayAccountStatus();\n\
      \    } else {\n      user.displayAccountStatus();\n    }\n  }\n\n  public static\
      \ void main(String[] args) {\n    choose(\"user\");\n  }\n}\n```"
    Description_En: "When a class declares a static method *m*, the declaration of\
      \ *m* hides any method *m*', where the signature of *m* is a subsignature of\
      \ the signature of *m*' and the declaration of *m*' is both in the superclasses\
      \ and superinterfaces of the declaring class and also would otherwise be accessible\
      \ to code in the declaring class (*The Java Language Specification*, §8.4.8.2\
      \ \"Hiding (by Class Methods)\" [JLS 2015]).\n\nAn instance method defined in\
      \ a subclass overrides another instance method in the superclass when both have\
      \ the same name, number and type of parameters, and return type.\n\nHiding and\
      \ overriding differ in the determination of which method is invoked from a call\
      \ site. For overriding, the method invoked is determined at runtime on the basis\
      \ of the specific object instance in hand. For hiding, the method invoked is\
      \ determined at compile time on the basis of the specific qualified name or\
      \ method invocation expression used at the call site. Although the Java language\
      \ provides unambiguous rules for determining which method is invoked, the results\
      \ of these rules are often unexpected. Additionally, programmers sometimes expect\
      \ method overriding in cases where the language provides method hiding. Consequently,\
      \ programs must never declare a class method that hides a method declared in\
      \ a superclass or superinterface.\n\n**Noncompliant Code Example**\n\nIn this\
      \ noncompliant code example, the programmer hides the static method rather than\
      \ overriding it. Consequently, the code invokes the `displayAccountStatus()`\
      \ method of the superclass, causing it to print  `Account details for admin`\
      \ despite being instructed to choose `user` rather than `admin`.\n\n```java\n\
      class GrantAccess {\n  public static void displayAccountStatus() {\n    System.out.println(\"\
      Account details for admin: XX\");\n  }\n}\n\nclass GrantUserAccess extends GrantAccess\
      \ {\n  public static void displayAccountStatus() {\n    System.out.println(\"\
      Account details for user: XX\");\n  }\n}\n\npublic class StatMethod {\n  public\
      \ static void choose(String username) {\n    GrantAccess admin = new GrantAccess();\n\
      \    GrantAccess user = new GrantUserAccess();\n    if (username.equals(\"admin\"\
      )) {\n      admin.displayAccountStatus();\n    } else {\n      user.displayAccountStatus();\n\
      \    }\n  }\n\n  public static void main(String[] args) {\n    choose(\"user\"\
      );\n  }\n}\n```"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 486:Comparison of Classes by Name
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140707'
    language: Java
  - Advice: "应该尽量避免静态方法的命名与超类中的命名相同，以避免产生混淆。在开发中，可以将静态方法放入单独的final类中, 因为final类不会创建子类，还可以创建私有的构造函数，以防止针对类的实例而非类来调用静态方法。\
      \  \n  \n**例如**：下面代码示例中，去掉了static关键字，将`displayAccountStatus()`方法声明为实例方法，`choose(\"\
      user\")`和`choose(\"admin\")`调用实现了预期的程序逻辑。  \n```java\nclass GrantAccess {  \n\
      \    public void displayAccountStatus() {  \n        System.out.println(\"Account\
      \ details for admin: XX\");  \n    }  \n}  \nclass GrantUserAccess extends GrantAccess\
      \ {  \n    @Override  \n    public void displayAccountStatus() {  \n       \
      \ System.out.println(\"Account details for user: XX\");  \n    }  \n}  \npublic\
      \ class StatMethod {  \n    public static void choose(String username) {  \n\
      \        GrantAccess admin = new GrantAccess();  \n        GrantAccess user\
      \ = new GrantUserAccess();  \n        if (username.equals(\"admin\")) {  \n\
      \            admin.displayAccountStatus();  \n        } else {  \n         \
      \   user.displayAccountStatus();  \n        }  \n    }  \n    public static\
      \ void main(String[] args) {  \n        choose(\"user\"); // Account details\
      \ for user: XX  \n        choose(\"admin\"); // Account details for admin: XX\
      \  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不要试图重写静态方法
    Categories_En:
      Category: []
    Default: true
    Description: "静态方法无法覆盖，在子类中覆盖父类的静态方法，这样容易产生混淆，从而导致错误。  \n  \n**例如**：下面代码示例中，子类不能覆盖父类的方法，`choose(\"\
      user\")`和`choose(\"admin\")`都调用了父类中的方法。  \n```java\nclass GrantAccess {  \n\
      \    public static void displayAccountStatus() {  \n        System.out.println(\"\
      Account details for admin: XX\");  \n    }  \n}  \nclass GrantUserAccess extends\
      \ GrantAccess {  \n    public static void displayAccountStatus() {  \n     \
      \   System.out.println(\"Account details for user: XX\");  \n    }  \n}  \n\
      public class StatMethod {  \n    public static void choose(String username)\
      \ {  \n        GrantAccess admin = new GrantAccess();  \n        GrantAccess\
      \ user = new GrantUserAccess();  \n        if (username.equals(\"admin\")) {\
      \  \n            admin.displayAccountStatus();  \n        } else {  \n     \
      \       user.displayAccountStatus();  \n        }  \n    }  \n    public static\
      \ void main(String[] args) {  \n        choose(\"user\"); // Account details\
      \ for admin: XX  \n        choose(\"admin\"); // Account details for admin:\
      \ XX  \n    }  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Hidden Method
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 486:Comparison of Classes by Name
      - type: CERT
        value: MET07-J:Never declare a class method that hides a method declared in
          a superclass or superinterface
    Security: not
    Tag: correctness,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150423'
    language: Java
  - Advice: "对应的符合规则的解决方案是：程序员去掉关键字 static，将 `displayAccountStatus()`方法声明为实例方法。因此，调用方的动态方法扩展使执行得到了预期的结果。`@Override`注释表明了覆写基类方法的意图。\
      \   \n    ```java\n    class GrantAccess {\n        public void displayAccountStatus()\
      \ {\n            System.out.println(\"Account details for admin: XX\");\n  \
      \      }\n    }\n    \n    class GrantUserAccess extends GrantAccess {\n   \
      \     @Override\n        public void displayAccountStatus() {\n            System.out.println(\"\
      Account details for user: XX\");\n        }\n    }\n    \n    public class StatMethod\
      \ {\n        public static void choose(String username) {\n            GrantAccess\
      \ admin = new GrantAccess();\n            GrantAccess user = new GrantUserAccess();\n\
      \            if (username.equals(\"admin\")) {\n                admin.displayAccountStatus();\n\
      \            } else {\n                user.displayAccountStatus();\n      \
      \      }\n        }\n    \n        public static void main(String[] args) {\n\
      \            choose(\"user\");\n        }\n    }"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.6.8 不要定义类方法来隐藏基类或基类接口中声明的方法
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序必须杜绝声明类方法的方式，会隐藏基类或父接口中的方法。   \n2) 原理   \n当一个类声明一个 static方法m时，m的定义会隐藏任何符合条件的方法\
      \ m'。这发生在当 m的签名是方法 m'签名的子签名，并且 m'是同时在声明类的基类和基类接口中定义的，否则声明类中的代码可以访问该定义。   \n\
      当方法拥有相同的名字、相同数目和类型的参数，以及相同的返回类型时，在子类中定义的实例方法会覆写基类中的实例方法。   \n隐藏和覆写在调用方决定调用哪一个方法时是有区别的。虽然\
      \ Java 语言为决定调用什么方法提供了明确的规则，但是这些规则的结果常常是预期之外的。而且，因为程序员有时期望使用方法覆写，但语言却提供隐藏。因此，程序必须杜绝声明类方法的方式，会隐藏基类或父接口中的方法。\
      \   \n从理论上说，一个 private 方法是不能被隐藏或被覆写的。没有任何要求指定子类和基类中具有相同签名的 private 方法有任何关联，即使方法有相同的返回类型和抛出异常，这是隐藏的\
      \ 必要条件。因此，当 private 方法有不同的返回类型或抛出条款时，不会发生隐藏。   \n3) 示例   \n    在这个不符合规定的方案中，程序员去掉了关键字\
      \ static，将 `displayAccountStatus()`方法声明为实例方法。因此，调用方的动态方法扩展使执行得到了预期的结果。`@Override`注释表明了覆写基类方法的意图。\
      \   \n    ```java\n    class GrantAccess {\n        public static void displayAccountStatus()\
      \ {\n            System.out.println(\"Account details for admin: XX\");\n  \
      \      }\n    }\n    \n    class GrantUserAccess extends GrantAccess {\n   \
      \     public static void displayAccountStatus() {\n            System.out.println(\"\
      Account details for user: XX\");\n        }\n    }\n    \n    public class StatMethod\
      \ {\n        public static void choose(String username) {\n            GrantAccess\
      \ admin = new GrantAccess();\n            GrantAccess user = new GrantUserAccess();\n\
      \            if (username.equals(\"admin\")) {\n                admin.displayAccountStatus();\n\
      \            } else {\n                user.displayAccountStatus();\n      \
      \      }\n        }\n    \n        public static void main(String[] args) {\n\
      \            choose(\"user\");\n        }\n    }\n    ```   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 486:Comparison of Classes by Name
      - type: CERT
        value: MET07-J:Never declare a class method that hides a method declared in
          a superclass or superinterface
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160057
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\nclass GrantAccess {\n \
      \   public void displayAccountStatus() {\n        System.out.println(\"Account\
      \ details for admin: XX\");\n    }\n}\n\nclass GrantUserAccess extends GrantAccess\
      \ {\n    @Override\n    public void displayAccountStatus() {\n        System.out.println(\"\
      Account details for user: XX\");\n    }\n}\n\npublic class StatMethod {\n  \
      \  public static void choose(String username) {\n        GrantAccess admin =\
      \ new GrantAccess();\n        GrantAccess user = new GrantUserAccess();\n  \
      \      if (username.equals(\"admin\")) {\n            admin.displayAccountStatus();\n\
      \        } else {\n            user.displayAccountStatus();\n        }\n   \
      \ }\n\n    public static void main(String[] args) {\n        choose(\"user\"\
      );\n    }\n}\n``` \n去掉了方法`displayAccountStatus()`的关键字static，`displayAccountStatus()`即成了一个实例方法。因此，调用方法的动态方法扩展使执行得到了预期结果。\
      \  \n从理论上说，一个private方法是不能被隐藏或被覆写的。没有任何要求指定子类和基类中具有相同签名的private方法有任何联系，即使方法具有相同的返回类型和抛出异常，这是隐藏的必要条件。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.1 面向对象程序安全
      - type: DetailClassChin
        value: A.3.1.7 不要定义方法来隐藏基类或基类接口中声明的方法
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明： 当方法使用相同的名字、相同数目和类型的参数，以及相同的返回类型时，在子类中定义的实例方法会覆写基类中的实例方法。 \
      \ \n对于不要定义方法来隐藏基类或基类接口中声明的方法的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\n\
      class GrantAccess {\n    public static void displayAccountStatus() {\n     \
      \   System.out.println(\"Account details for admin: XX\");\n    }\n}\n\nclass\
      \ GrantUserAccess extends GrantAccess {\n    public static void displayAccountStatus()\
      \ {\n        System.out.println(\"Account details for user: XX\");\n    }\n\
      }\n\npublic class StatMethod {\n    public static void choose(String username)\
      \ {\n        GrantAccess admin = new GrantAccess();\n        GrantAccess user\
      \ = new GrantUserAccess();\n        if (username.equals(\"admin\")) {\n    \
      \        admin.displayAccountStatus();\n        } else {\n            user.displayAccountStatus();\n\
      \        }\n    }\n\n    public static void main(String[] args) {\n        choose(\"\
      user\");\n    }\n}\n```  \n代码中程序员隐藏了static方法而不是将其覆写。因此，代码在两个不同的地方调用了基类中的`displayAccountStatus()`方法，而不是在一处调用基类的方法，另一处调用子类的方法。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 486:Comparison of Classes by Name
      - type: CERT
        value: MET07-J:Never declare a class method that hides a method declared in
          a superclass or superinterface
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170033'
    language: Java
  - Advice: "程序中所需密码应从配置文件中获取经过加密的密码值。  \n  \n**例如**：下列代码中采用获取经过加密的密码值来获取数据库连接。  \n\
      ```java\n...  \nString password = getEncryptedPass();  \nconn = DriverManager.getConnection(url,user,\
      \ password);\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 密码规范
      - type: DetailClassChin
        value: 【推荐】不要使用硬编码密码
    Categories_En:
      Category: []
    Default: true
    Description: "程序中采用硬编码方式处理密码，一方面会降低系统安全性，另一方面不易于程序维护。  \n  \n**例如**：下列代码中采用硬编码密码获取数据库连接。\
      \  \n```java\n...  \nconn = DriverManager.getConnection(url,\"root\", \"root\"\
      );\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Hardcoded Password
    ENSubClassChin: Password Management
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 798:Use of Hard-coded Credentials
      - type: CWE
        value: 259:Use of Hard-coded Password
      - type: CERT
        value: MSC03-J:Never hard code sensitive information
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2010:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 259
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Hard-coded Password [XYP]
      - type: GB/T 34944-2017 Java
        value: 安全功能:口令硬编码
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: security,password
    Type: std
    WhiteList: not
    id: '0202000010150210'
    language: Java
  - Advice: "方案1：采用比较操作符`<、<=、>、>=`来比较封装后的基础数据类型，因为这些操作符会自动解封封装；\n方案2：采用`equals`来比较两个封装后的基础数据类型。\
      \  \n**规范的代码样例1**：  \n```java\npublic int compare(Integer i, Integer j) {  \n\
      \    return i < j ? -1 : (i > j ? 1 : 0);  \n}  \n```\n**规范的代码样例2**：  \n```java\n\
      public class Wrapper {\n    public static void main(String[] args) {\n     \
      \   Integer i1 = 100;\n        Integer i2 = 100;\n        Integer i3 = 1000;\n\
      \        Integer i4 = 1000;\n        System.out.println(i1.equals(i2));\n  \
      \      System.out.println(!i1.equals(i2));\n        System.out.println(i3.equals(i4));\n\
      \        System.out.println(!i3.equals(i4));\n    }\n}\n```"
    Advice_En: "**Compliant Solution**\n\nThis compliant solution uses the comparison\
      \ operators, `<`, `>`, `<=`, or `>=`, because these cause automatic unboxing\
      \ of the primitive values. The `==` and `!=` operators should not be used to\
      \ compare boxed primitives.\n\n```java\nimport java.util.Comparator;\n\nstatic\
      \ Comparator<Integer> cmp = new Comparator<Integer>() {\n  public int compare(Integer\
      \ i, Integer j) {\n    return i < j ? -1 : (i > j ? 1 : 0) ;\n  }\n};\n```\n\
      **Compliant Solution**\n\nThis compliant solution uses the `equals()` method\
      \ instead of the `==` operator to compare the values of the objects. The program\
      \ now prints `true`, `false`, `true`, `false` on all platforms, as expected.\n\
      \n```java\npublic class Wrapper {\n  public static void main(String[] args)\
      \ {\n    Integer i1 = 100;\n    Integer i2 = 100;\n    Integer i3 = 1000;\n\
      \    Integer i4 = 1000;\n    System.out.println(i1.equals(i2));\n    System.out.println(!i1.equals(i2));\n\
      \    System.out.println(i3.equals(i4));\n    System.out.println(!i3.equals(i4));\n\
      \  }\n}\n```"
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 02. 表达式 (EXP)
      - type: DetailClassChin
        value: EXP03-J：不要使用相等操作符比较封装的基础数据类型
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 02. Expressions (EXP)
      - type: DetailClassChin
        value: EXP03-J:Do not use the equality operators when comparing values of
          boxed primitives
    Default: true
    Description: "两个封装的基础数据类型的值是不能直接使用` == `或者` != `操作符来比较，因为这些操作符比较的是对象的引用而不是对象的值。考虑到自动封装会内存化或存储一些基础数据类型变量的值，程序员往往认为这条规则的要求是难以相信的。但是，引用比较和值的比较，对于那些完全内存化的变量是会产生相同的值，但没有完全内存化的变量则不会。\
      \  \n在Java语言规范5.1.7节“装箱转换”中关于自动封装时，哪些基础结构数据是要被内存化的：  \n>如果要装箱的p值为true、false、一个byte、一个在\\\
      u0000~\\u007f之间的char或者一个位于-128~127之间的int或short数字，设r1和r2是p的任何两个装箱转化的结果，则始终有r1==r2。（设p为100，Integer\
      \ r1 = 100，Integer r2 = 100，则r1一定等于r2）  \n\n在JVM中，对额外的数据的内存化是允许的，但不是必需的。所以，在内存比较宽松的实现中，有可能会对所有的char、short的值，甚至是值在-32K~+32K之间的int、long进行缓存。\n\
      完全内存化的封装后的基础数据类型：Boolean、Byte。  \n没有完全内存化的封装后的基础数据类型：Character、Short、Integer、Long、Float、Double。\
      \  \n在封装后的基础数据类型不是直接来自于其构造函数的情况下，即属于自动封装，可以：  \n1）如果使用 `==` 或者 `!=` 操作符来比较两个完全内存化的封装过的基础数据类型的值是允许的。\
      \  \n2）如果使用 `==` 或者 `!=` 操作符来比较两个没有完全内存化的封装过的基础数据类型的值，必须保证两个值处于对应类型的有效范围之内。\
      \  \n**不规范的代码样例1**：  \n代码定义了一个`compare()`方法，在方法内对传入的两个Integer对象进行了`==`比较，实际上比较的是这两个对象的地址而不是值。\n\
      ```java\nimport java.util.Comparator;\n\nstatic Comparator<Integer> cmp = new\
      \ Comparator<Integer>() {\n  public int compare(Integer i, Integer j) {\n  \
      \  return i < j ? -1 : (i == j ? 0 : 1);\n  }\n};\n```\n注意，此方法也可接受 `int`型数据，在调用时被自动包装。\n\
      **不规范的代码样例2**：\n这个不合规的代码示例使用 `==` 运算符来尝试比较成对的 `Integer` 对象的值。 但是，`==` 运算符比较的是对象引用而不是对象的值。\n\
      ```java\npublic class Wrapper {\n  public static void main(String[] args) {\n\
      \    Integer i1 = 100;\n    Integer i2 = 100;\n    Integer i3 = 1000;\n    Integer\
      \ i4 = 1000;\n    System.out.println(i1 == i2);\n    System.out.println(i1 !=\
      \ i2);\n    System.out.println(i3 == i4);\n    System.out.println(i3 != i4);\n\
      \  }\n}\n```\n`Integer` 类保证只缓存从 `-128` 到 `127` 的整数值，这可能导致在此范围之外的相等值在使用等于运算符进行测试时会不相等。\
      \ 例如，在运行该程序时没有缓存任何其他值的 JVM 将输出\n| `true``false``false``true` |\n| - |"
    Description_En: "The *values* of boxed primitives cannot be directly compared\
      \ using the `==` and `!=` operators because these operators compare object references\
      \ rather than object values. Programmers can find this behavior surprising because\
      \ autoboxing memoizes, or caches, the values of some primitive variables. Consequently,\
      \ reference comparisons and value comparisons produce identical results for\
      \ the subset of values that are memoized.\n\nAutoboxing automatically wraps\
      \ a value of a primitive type with the corresponding wrapper object. *The Java\
      \ Language Specification* (JLS), §5.1.7, &#34;Boxing Conversion&#34; JLS 2015,\
      \ explains which primitive values are memoized during autoboxing:\n\n> If the\
      \ value `p` being boxed is `true`, `false`, a `byte`, a `char` in the range\
      \ `\\u0000` to `\\u007f`, or an `int` or `short` number between `-128` and `127`,\
      \ then let `r1` and `r2` be the results of any two boxing conversions of `p`.\
      \ It is always the case that `r1 == r2`.\n\nUse of the `==` and `!=` operators\
      \ for comparing the values of fully memoized boxed primitive types is permitted.\n\
      \nUse of the `==` and `!=` operators for comparing the *values* of boxed primitive\
      \ types that are not fully memoized is permitted only when the range of values\
      \ represented is guaranteed to be within the ranges specified by the JLS to\
      \ be fully memoized.\n\nUse of the `==` and `!=` operators for comparing the\
      \ *values* of boxed primitive types is not allowed in all other cases.\n\nNote\
      \ that Java Virtual Machine (JVM) implementations are allowed, but not required,\
      \ to memoize additional values [JLS 2015]:\n\n> Less memory-limited implementations\
      \ could, for example, cache all characters and shorts, as well as integers and\
      \ longs in the range of −32K to +32K. (§5.1.7)\n\nCode that depends on implementation-defined\
      \ behavior is nonportable. It is permissible to depend on implementation-specific\
      \ ranges of memoized values provided that all targeted implementations support\
      \ these greater ranges.\n\n**Noncompliant Code Example**\n\nThis noncompliant\
      \ code example defines a `Comparator` with a `compare()` method [Bloch 2009].\
      \ The `compare()` method accepts two boxed primitives as arguments. The `==`\
      \ operator is used to compare the two boxed primitives. In this context, however,\
      \ it compares the *references* to the wrapper objects rather than comparing\
      \ the *values* held in those objects.\n\n```java\nimport java.util.Comparator;\n\
      \nstatic Comparator<Integer> cmp = new Comparator<Integer>() {\n  public int\
      \ compare(Integer i, Integer j) {\n    return i < j ? -1 : (i == j ? 0 : 1);\n\
      \  }\n};\n```\n\nNote that primitive integers are also accepted by this declaration\
      \ because they are autoboxed at the call site.\n\n**Noncompliant Code Example**\n\
      \nThis noncompliant code example uses the `==` operator in an attempt to compare\
      \ the values of pairs of `Integer` objects. However, the `==` operator compares\
      \ object references rather than object values.\n\n```java\npublic class Wrapper\
      \ {\n  public static void main(String[] args) {\n    Integer i1 = 100;\n   \
      \ Integer i2 = 100;\n    Integer i3 = 1000;\n    Integer i4 = 1000;\n    System.out.println(i1\
      \ == i2);\n    System.out.println(i1 != i2);\n    System.out.println(i3 == i4);\n\
      \    System.out.println(i3 != i4);\n  }\n}\n```\n\nThe `Integer` class is guaranteed\
      \ to cache only integer values from `-128` to `127`, which can result in equivalent\
      \ values outside this range comparing as unequal when tested using the equality\
      \ operators. For example, a JVM that did not cache any other values when running\
      \ this program would output\n\n| `true``false``false``true` |\n| - |"
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 754:Improper Check for Unusual or Exceptional Conditions
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010140403'
    language: Java
  - Advice: "不能直接使用==或!=操作符来比较的两个基本数据类型的包装类的值，因为这些操作符比较的是对象的引用而不是对象的值。  \n符合规范的方案使用`equals()`而不是`==`操作符来比较两个对象的值。这个程序在所有的平台运行时都会打印true、false、true、false的结果，这符合预期。\
      \  \n**例如**：以下代码使用`equals()`方法比较两个Integer对象的值。  \n```java\npublic class Wrapper\
      \ {  \n    public static void main(String[] args) {  \n        Integer i1 =\
      \ 100;  \n        Integer i2 = 100;  \n        Integer i3 = 1000;  \n      \
      \  Integer i4 = 1000;  \n        System.out.println(i1.equals(i2));  \n    \
      \    System.out.println(!i1.equals(i2));  \n        System.out.println(i3.equals(i4));\
      \  \n        System.out.println(!i3.equals(i4));  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要使用==或!=比较基本数据类型的包装类
    Categories_En:
      Category: []
    Default: true
    Description: "不能直接使用`==`或`!=`操作符来比较的两个基本数据类型的包装类型的值，因为这些操作符比较的是对象的引用而不是对象的值。 \
      \ \n不过由于Java的缓存机制，所以如果基本类型的包装类是一个整数且在-128和127之间，或是布尔类型true或false，或者是'\\u0000'和'\\\
      u007f'之间的字符文本，可以使用`==`或`!=`进行比较。也就是说，如果使用了基本类型的包装类型（除去Boolean或Byte），则会缓存或记住一个值区间。对于值区间内的值，使用`==`或`!=`会返回正确的值，而对于值区间外的值，将返回对象地址的比较结果。\
      \  \n**例如**：在这个不符合规范的代码示例中，使用`==`操作符来比较两个Integer对象的值。然而，这个`==`操作比较的是对象的引用，而不是对象的值。\
      \  \n```java\npublic class Wrapper {  \n    public static void main(String[]\
      \ args){  \n        Integer i1 = 100;  \n        Integer i2 = 100;  \n     \
      \   Integer i3 = 1000;  \n        Integer i4 = 1000;  \n        System.out.println(i1\
      \ == i2);  \n        System.out.println(i1 != i2);  \n        System.out.println(i3\
      \ == i4);  \n        System.out.println(i3 != i4);  \n    }  \n}\n```   \nInteger类只能保证缓存介入-128~127的整型数值，当使用相等操作符的时候，这会导致在这个范围之外的等价数值的比较是不相等的。比如，在那些不缓存任何值的JVM虚拟机中，运行程序会产生以下结果：\
      \  \ntrue  \nfalse  \nfalse  \ntrue"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Comparison of Boxed Primitive Types
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 754:Improper Check for Unusual or Exceptional Conditions
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: CERT
        value: EXP03-J:Do not use the equality operators when comparing values of
          boxed primitives
    Security: not
    Tag: correctness,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150410'
    language: Java
  - Advice: "对应的符合规则的代码示例：使用比较操作符`<`、`>`、`<=`或`>=`比较两个封装过的基础数据类型，这些操作符会自动封装基础数据类型数据。\
      \   \n    ```java\n    public int compare(Integer i,Integer j) {   \n      \
      \  return i <j ? -1 : ( i > j ? 0 : 1 );   \n    } \n    ```\n4) 特例   \n在特殊情况下，如果程序只在单一实现中运行，依赖于该特定实现的内存化数值范围，使用相等操作符比较两个基础数据类型的值是允许的。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.2 面对对象程序安全
      - type: DetailClassChin
        value: C.3.4 不要使用相等操作符比较两个基础数据类型的值
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n不要使用相等操作符比较两个基础数据类型的值。   \n2) 原理   \n    不能直接使用`==`或`!=`操作符比较两个基础数据类型的值，这些操作符比较的是对象引用而不是对象的值。\
      \   \n    自动封装基础数据类型的数值需要使用对应的封装器。在自动封装时，这些基础结构数据被内存化：   \n    如果封装的 p 的数值是\
      \ true、false、一个字节、一个在\\u0000～\\u007f 范围的 char 数据，或者是一个位于-128～127 的 int 或 short\
      \ 数据，对 p 的任意两次封装的结果是 rl 和 r2，那么 rl==r2。   \n    \n    | 基础数据类型 | 封装类型 | 完全内存化|\n\
      \    | :------: | :------: | :------: |\n    | boolean. byte | Boolean. Byte\
      \ |是|\n    | char. short. int | Char. Short. Integer |否|\n    使用`==`和`!=`操作符比较两个完全内存化的封装过的基础数据类型的值是允许的。\
      \   \n    使用`==`和`!=`操作符比较那些没有完全内存化的封装过的基础结构数据的值，只在这些值在规定的范围内是允许的。   \n    在其他情况下，使用`==`和`!=`操作比较那些封装过的基础数据类型的数值是不允许的。\
      \   \n3) 示例   \n    1.这个不符合规则的代码示例：定义的`compare()`方法接收两个封装过的基础数据类型的参数，使用==操作符比较这两个封装过的基础数据类型。这实际上是对封装对象的引用进行比较，而不是对对象的值进行比较。\
      \   \n    ```java\n    static Comparator<Integer> cmp=new Comparator<Integer>\
      \ () {   \n        public int compare(Integer i,Integer j) {   \n      return\
      \ i <j ? -1 : ( i== j ? 0 : 1 );   \n        }   \n    };\n    ```  \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 754:Improper Check for Unusual or Exceptional Conditions
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: CERT
        value: EXP03-J:Do not use the equality operators when comparing values of
          boxed primitives
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160021
    language: Java
  - Advice: "**规范的代码样例1**：\n```java\npublic final class ProcessStep implements Runnable\
      \ {\n    private static final Object lock = new Object();\n    private static\
      \ int time = 0;\n    private final int step; // Perform operations when field\
      \ time\n    // reaches this value\n\n    public ProcessStep(int step) {\n  \
      \      this.step = step;\n    }\n\n    @Override\n    public void run() {\n\
      \        try {\n            synchronized (lock) {\n                while (time\
      \ != step) {\n                    lock.wait();\n                }\n        \
      \        // Perform operations\n                time++;\n                lock.notifyAll();\
      \ // Use notifyAll() instead of notify()\n            }\n        } catch (InterruptedException\
      \ ie) {\n            Thread.currentThread().interrupt(); // Reset interrupted\
      \ status\n        }\n    }\n}\n```  \n代码中每一个限制在完成它自己的步骤后调用`Object.notifyAll()`方法来通知所有等待中的线程，准备好的线程可以执行它的任务，而那些其他等待条件为假的线程则会继续等待。"
    Advice_En: "**Compliant Solution (`notifyAll()`)**\n\nIn this compliant solution,\
      \ each thread completes its step and then calls `notifyAll()` to notify the\
      \ waiting threads. The thread that is ready can then perform its task while\
      \ all the threads whose condition predicates are false (loop condition expression\
      \ is true) promptly resume waiting.\n\nOnly the `run()` method from the noncompliant\
      \ code example is modified, as follows:\n\n```java\npublic final class ProcessStep\
      \ implements Runnable {\n  private static final Object lock = new Object();\n\
      \  private static int time = 0;\n  private final int step; // Perform operations\
      \ when field time\n                          // reaches this value\n  public\
      \ ProcessStep(int step) {\n    this.step = step;\n  }\n\n  @Override public\
      \ void run() {\n    try {\n      synchronized (lock) {\n        while (time\
      \ != step) {\n          lock.wait();\n        }\n\n        // Perform operations\n\
      \n        time++;\n        lock.notifyAll(); // Use notifyAll() instead of notify()\n\
      \      }\n    } catch (InterruptedException ie) {\n      Thread.currentThread().interrupt();\
      \ // Reset interrupted status\n    }\n  }\n\n}\n```"
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 10. 线程API (THI)
      - type: DetailClassChin
        value: THI02-J：通知所有等待中的线程而不是单一的线程
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 10. Thread APIs (THI)
      - type: DetailClassChin
        value: THI02-J:Notify all waiting threads rather than a single thread
    Default: true
    Description: "`Object.notify()`和`Object.notifyAll()`方法分别是用来唤醒等待中的一个或者多个线程。这些方法必须由那些拥有等待对象锁的线程来调用，而且这些方法在被其他线程调用时会抛出IllegalMonitorStateException。\
      \  \n`notifyAll()`方法会唤醒所有等待同一对象锁的线程，并允许等候条件成真的线程恢复执行。而且，如果那些等候条件成真的所有线程在进入等候状态前只有某一特定的锁的话，在接到通知时只有一个线程可以重新得到这个锁。其他线程将会继续等待。\
      \  \n`notify()`方法只唤醒一个线程，而并不保证哪一个线程会接到通知。如果没有满足等候条件，那么选中的线程会继续等待，这通常违背了通知的目的。只有满足了以下条件时，调用`notify()`方法才是允许的：\
      \  \n1）所有线程拥有一致的等候条件。  \n2）所有线程被唤醒后会进行相同的操作。  \n3）在发出`notify()`通知后，只需要一个线程恢复工作。\
      \  \n另外，`Condition.signal()`和`Condition.signalAll()`方法唤醒在调用`Condition.await()`时受阻的线程的原理也与`Object.notify()`和`Object.notifyAll()`方法相同。\
      \  \n风险评估：通知单一的线程而不是所有等待中的线程违反了程序的活性属性。  \n**不规范的代码样例**：  \n```java\npublic\
      \ final class ProcessStep implements Runnable {\n    private static final Object\
      \ lock = new Object();\n    private static int time = 0;\n    private final\
      \ int step; // Perform operations when field time\n    // reaches this value\n\
      \n    public ProcessStep(int step) {\n        this.step = step;\n    }\n\n \
      \   @Override\n    public void run() {\n        try {\n            synchronized\
      \ (lock) {\n                while (time != step) {\n                    lock.wait();\n\
      \                }\n                // Perform operations\n                time++;\n\
      \                lock.notify();\n            }\n        } catch (InterruptedException\
      \ ie) {\n            Thread.currentThread().interrupt(); // Reset interrupted\
      \ status\n        }\n    }\n\n    public static void main(String[] args) {\n\
      \        for (int i = 4; i >= 0; i--) {\n            new Thread(new ProcessStep(i)).start();\n\
      \        }\n    }\n}\n```  \n这个代码演示了一个由多线程执行的复杂的多步过程。每一个线程的执行由time字段决定的步骤。线程会等待time字段的指示，来确定是否执行对应线程的步骤。在执行任务后，每一个线程先是增加time，然后在通知负责下一个步骤的线程。但是，每一个线程拥有不同的等候条件，`Object.notify()`每次只唤醒一个线程。除非正好唤醒执行下一个步骤的线程，否则这个程序会发生死锁。"
    Description_En: "Threads that invoke `Object.wait()` expect to wake up and resume\
      \ execution when their condition predicate becomes true. To be compliant with\
      \ THI03-J. Always invoke wait() and await() methods inside a loop, waiting threads\
      \ must test their condition predicates upon receiving notifications and must\
      \ resume waiting if the predicates are false.\n\nThe `notify()` and `notifyAll()`\
      \ methods of package `java.lang.Object` are used to wake up a waiting thread\
      \ or threads, respectively. These methods must be invoked from a thread that\
      \ holds the same object lock as the waiting thread(s); these methods throw an\
      \ `IllegalMonitorStateException` when invoked from any other thread. The `notifyAll()`\
      \ method wakes up all threads waiting on an object lock and allows threads whose\
      \ condition predicate is true to resume execution. Furthermore, if all the threads\
      \ whose condition predicate evaluates to true previously held a specific lock\
      \ before going into the wait state, only one of them will reacquire the lock\
      \ upon being notified. Presumably, the other threads will resume waiting. The\
      \ `notify()` method wakes up only one thread, with no guarantee regarding which\
      \ specific thread is notified. The chosen thread is permitted to resume waiting\
      \ if its condition predicate is unsatisfied; this often defeats the purpose\
      \ of the notification.\n\nConsequently, invoking the `notify()` method is permitted\
      \ only when *all* of the following conditions are met:\n\n* All waiting threads\
      \ have identical condition predicates.\n* All threads perform the same set of\
      \ operations after waking up. That is, any one thread can be selected to wake\
      \ up and resume for a single invocation of `notify()`.\n* Only one thread is\
      \ required to wake upon the notification.\n\nThese conditions are satisfied\
      \ by threads that are identical and provide a stateless service or utility.\n\
      \nThe `java.util.concurrent.locks` utilities provide the `Condition.signal()`\
      \ and `Condition.signalAll()` methods to awaken threads that are blocked on\
      \ a `Condition.await()` call. `Condition` objects are required when using `java.util.concurrent.locks.Lock`\
      \ objects. Although `Lock` objects allow the use of `Object.wait()`, `Object.notify()`,\
      \ and `Object.notifyAll()` methods, their use is prohibited by LCK03-J. Do not\
      \ synchronize on the intrinsic locks of high-level concurrency objects. Code\
      \ that synchronizes using a `Lock` object uses one or more `Condition` objects\
      \ associated with the `Lock` object rather than using its own intrinsic lock.\
      \ These objects interact directly with the locking policy enforced by the `Lock`\
      \ object. Consequently, the `await()`, `signal()`, and `signalAll()` methods\
      \ are used in place of the `wait()`, `notify()`, and `notifyAll()` methods.\n\
      \nThe `signal()` method must not be used unless all of these conditions are\
      \ met:\n\n* The `Condition` object is identical for each waiting thread.\n*\
      \ All threads must perform the same set of operations after waking up, which\
      \ means that any one thread can be selected to wake up and resume for a single\
      \ invocation of `signal()`.\n* Only one thread is required to wake upon receiving\
      \ the signal.\n\nor all of these conditions are met:\n\n* Each thread uses a\
      \ unique `Condition` object.\n* Each `Condition` object is associated with the\
      \ same `Lock` object.\n\nWhen used securely, the `signal()` method has better\
      \ performance than `signalAll()`.\n\nWhen `notify()` or `signal()` is used to\
      \ waken a waiting thread, and the thread is not prepared to resume execution,\
      \ it often resumes waiting. Consequently, no thread wakens, which may cause\
      \ the system to hang.\n\n**Noncompliant Code Example (`notify()`)**\n\nThis\
      \ noncompliant code example shows a complex, multistep process being undertaken\
      \ by several threads. Each thread executes the step identified by the `time`\
      \ field. Each thread waits for the `time` field to indicate that it is time\
      \ to perform the corresponding thread's step. After performing the step, each\
      \ thread first increments `time` and then notifies the thread that is responsible\
      \ for the next step.\n\n```java\npublic final class ProcessStep implements Runnable\
      \ {\n  private static final Object lock = new Object();\n  private static int\
      \ time = 0;\n  private final int step; // Do Perform operations when field time\n\
      \                          // reaches this value\n\n  public ProcessStep(int\
      \ step) {\n    this.step = step;\n  }\n\n  @Override public void run() {\n \
      \   try {\n      synchronized (lock) {\n        while (time != step) {\n   \
      \       lock.wait();\n        }\n\n        // Perform operations\n\n       \
      \ time++;\n        lock.notify();\n      }\n    } catch (InterruptedException\
      \ ie) {\n      Thread.currentThread().interrupt(); // Reset interrupted status\n\
      \    }\n  }\n\n  public static void main(String[] args) {\n    for (int i =\
      \ 4; i >= 0; i--) {\n      new Thread(new ProcessStep(i)).start();\n    }\n\
      \  }\n}\n```\n\nThis noncompliant code example violates the liveness property.\
      \ Each thread has a different condition predicate because each requires `step`\
      \ to have a different value before proceeding. The `Object.notify()` method\
      \ wakes only one thread at a time. Unless it happens to wake the thread that\
      \ is required to perform the next step, the program will deadlock."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '373:DEPRECATED: State Synchronization Error'
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141102'
    language: Java
  - Advice: "使用`notifyAll()`方法代替`notify()`方法。  \n  \n**例如**：下列代码片段中，调用`notifyAll()`方法代替了`notify()`方法，将会唤醒所有线程，满足条件的线程将会执行它的任务，不满足条件的线程将会继续等待。\
      \  \n```java\npublic final class ProcessStep implements Runnable {  \n    private\
      \ static final Object lock = new Object();  \n    private static int time =\
      \ 0;  \n    private final int step; // Perform operations when field time  \n\
      \                            // reaches this value  \n    public ProcessStep(int\
      \ step) {  \n        this.step = step;  \n    }  \n    @Override public void\
      \ run() {  \n        try {  \n            synchronized (lock) {  \n        \
      \        while (time != step) {  \n                    lock.wait();  \n    \
      \            }  \n                // Perform operations  \n                time++;\
      \  \n                lock.notifyAll(); // Use notifyAll() instead of notify()\
      \  \n            }  \n        } catch (InterruptedException ie) {  \n      \
      \      Thread.currentThread().interrupt(); // Reset interrupted status  \n \
      \       }  \n    }  \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【强制】不要调用notify()
    Categories_En:
      Category: []
    Default: true
    Description: "程序调用`notify()`方法无法指定将唤醒哪个线程。  \n  \n**例如**：下面代码片段中，线程执行步骤由`time`字段决定，一个线程执行完操作后，将`time`的值加一，然后通知负责下一个步骤的线程。\
      \  \n```java\npublic final class ProcessStep implements Runnable {  \n    private\
      \ static final Object lock = new Object();  \n    private static int time =\
      \ 0;  \n    private final int step; // Perform operations when field time  \n\
      \    // reaches this value  \n    public ProcessStep(int step) {  \n       \
      \ this.step = step;  \n    }  \n    @Override public void run() {  \n      \
      \  try {  \n            synchronized (lock) {  \n                while (time\
      \ != step) {  \n                    lock.wait();  \n                }  \n  \
      \              // Perform operations  \n                time++;  \n        \
      \        lock.notify();  \n            }  \n        } catch (InterruptedException\
      \ ie) {  \n            Thread.currentThread().interrupt(); // Reset interrupted\
      \ status  \n        }  \n    }  \n    public static void main(String[] args)\
      \ {  \n        for (int i = 4; i >= 0; i--) {  \n            new Thread(new\
      \ ProcessStep(i)).start();  \n        }  \n    }  \n}\n```  \n由于每个线程有不同的wait条件，假如存在满足条件的线程，但是因为`notify()`方法只能唤醒一个线程，若被唤醒的线程不满足条件，则必须继续等待，程序有可能因此将会发生死锁。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Call to notify()
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '373:DEPRECATED: State Synchronization Error'
      - type: CERT
        value: THI02-J:Notify all waiting threads rather than a single thread
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150110'
    language: Java
  - Advice: "对应的符合规则的解决方案（`notifyAll())`中，每一个线程都在完成它自己的步骤后调用`notifyAll()`方法来通知等待中的线程。准备好的线程可以执行它的任务，而那些其他等待条件为假的线程（循环条件表\
      \ 达式为 true)则会继续等待。   \n    本代码只修改了上述不符合规则代码示例中的`run()`方法：   \n    ```java\n\
      \    public final class ProcessStep implements Runnable {\n        private static\
      \ final Object lock = new Object();\n        private static int time = 0;\n\
      \        private final int step; // Perform operations when field time\n   \
      \                             // reaches this value\n    \n        public ProcessStep(int\
      \ step) {\n            this.step = step;\n        }\n    \n        @Override\n\
      \        public void run() {\n            try {\n                synchronized\
      \ (lock) {\n                    while (time != step) {\n                   \
      \     lock.wait();\n                    }\n    \n                    // Perform\
      \ operations\n    \n                    time++;\n                    lock.notifyAll();\
      \ // Use notifyAll() instead of notify()\n                }\n            } catch\
      \ (InterruptedException ie) {\n                Thread.currentThread().interrupt();\
      \ // Reset interrupted status\n            }\n        }\n    \n    }\n    ```\n\
      \       \n    2、不符合规则的代码示例（Condition 接口），该示例是和使用`notify()`的例子相类似，本段代码使用的是 Condition\
      \ 接口来进行等待和通知操作。   \n    ```java\n    public class ProcessStep implements Runnable\
      \ {\n        private static final Lock lock = new ReentrantLock();\n       \
      \ private static final Condition condition = lock.newCondition();\n        private\
      \ static int time = 0;\n        private final int step; // Perform operations\
      \ when field time\n                                // reaches this value\n \
      \   \n        public ProcessStep(int step) {\n            this.step = step;\n\
      \        }\n    \n        @Override\n        public void run() {\n         \
      \   lock.lock();\n            try {\n                while (time != step) {\n\
      \                    condition.await();\n                }\n    \n         \
      \       // Perform operations\n    \n                time++;\n             \
      \   condition.signal();\n            } catch (InterruptedException ie) {\n \
      \               Thread.currentThread().interrupt(); // Reset interrupted status\n\
      \            } finally {\n                lock.unlock();\n            }\n  \
      \      }\n    \n        public static void main(String[] args) {\n         \
      \   for (int i = 4; i >= 0; i--) {\n                new Thread(new ProcessStep(i)).start();\n\
      \            }\n        }\n    }\n    ```   \n    和 `Object.notify()` —样，`signal()`方法会唤醒任意一个线程。\
      \   \n       \n    对应的符合规则的方案（`signalAll()`)通过使用 `signalAll()`方法来通知所有等待中的线程。在\
      \ `await()`返回前，当前线程会重新获取和条件相关的锁。线程在返回时，肯定是拥有这个锁的。准备就绪的线程可以执行其任务，而那些等条件为假的线程则会继续等待。\
      \   \n    本代码只修改了上述不符合规则的代码示例中的`run()`方法：   \n    ```java\n    public class\
      \ ProcessStep implements Runnable {\n        private static final Lock lock\
      \ = new ReentrantLock();\n        private static final Condition condition =\
      \ lock.newCondition();\n        private static int time = 0;\n        private\
      \ final int step; // Perform operations when field time\n                  \
      \              // reaches this value\n    \n        public ProcessStep(int step)\
      \ {\n            this.step = step;\n        }\n    \n        @Override\n   \
      \     public void run() {\n            lock.lock();\n            try {\n   \
      \             while (time != step) {\n                    condition.await();\n\
      \                }\n    \n                // Perform operations\n    \n    \
      \            time++;\n                condition.signalAll();\n            }\
      \ catch (InterruptedException ie) {\n                Thread.currentThread().interrupt();\
      \ // Reset interrupted status\n            } finally {\n                lock.unlock();\n\
      \            }\n        }\n    \n    }\n    ```   \n       \n    另一个符合规则的解决方案是赋予每个线程一个自己的条件。所有的线程都可以访问所有的Condition\
      \ 对象。   \n    ```java\n    // Declare class as final because its constructor\
      \ throws an exception\n    public final class ProcessStep implements Runnable\
      \ {\n        private static final Lock lock = new ReentrantLock();\n       \
      \ private static int time = 0;\n        private final int step; // Perform operations\
      \ when field time\n                                // reaches this value\n \
      \       private static final int MAX_STEPS = 5;\n        private static final\
      \ Condition[] conditions = new Condition[MAX_STEPS];\n    \n        public ProcessStep(int\
      \ step) {\n            if (step <= MAX_STEPS) {\n                this.step =\
      \ step;\n                conditions[step] = lock.newCondition();\n         \
      \   } else {\n                throw new IllegalArgumentException(\"Too many\
      \ threads\");\n            }\n        }\n    \n        @Override\n        public\
      \ void run() {\n            lock.lock();\n            try {\n              \
      \  while (time != step) {\n                    conditions[step].await();\n \
      \               }\n    \n                // Perform operations\n    \n     \
      \           time++;\n                if (step + 1 < conditions.length) {\n \
      \                   conditions[step + 1].signal();\n                }\n    \
      \        } catch (InterruptedException ie) {\n                Thread.currentThread().interrupt();\
      \ // Reset interrupted status\n            } finally {\n                lock.unlock();\n\
      \            }\n        }\n    \n        public static void main(String[] args)\
      \ {\n            for (int i = MAX_STEPS - 1; i >= 0; i--) {\n              \
      \  ProcessStep ps = new ProcessStep(i);\n                new Thread(ps).start();\n\
      \            }\n        }\n    }\n    ```  \n    虽然使用了`signal()`方法，但是只有等候条件对应于特定\
      \ Condition 变量的线程才会被唤醒。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.10.3 通知所有等待中的线程而不是单一线程
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n只有在“所有线程拥有一致的等候条件”、“所有线程在被唤醒后会进行相同的操作”、“在发出通知后，只需要一个线程恢复工作”都满足的情况下，才能调用\
      \ notify()方法。   \n2) 原理   \n调用 `Object.wait()`的线程期望在它们的等候条件成真时恢复继续运行。等待中的线程在接到通知时，必须测试它们的等候条件，而且在等候条件为假时，必须继续等待。`java.lang.Object`\
      \ 包中的`notify()`和`notifyAll()`方法分別是用来唤醒等待中的一个或多个线程的。这些方法必须由那些拥有等待线程的对象锁的线程来调用，而且这些方法在被其他线程调用时会抛出illegalMonitorStateException。`notifyAll()`方法会唤醒所有等待同一对象锁的线程，并允许等候条件成真的线程恢复执行。而且，如果那些等候条件成真的所有线程在进入等候状态前持有某一特定的锁的话，在接到通知时只有一个线程可以重新得到这个锁。其他线程将会继续等待。`notify(`)方法只唤醒一个线程，而并不保证哪一个线程会接到通知。并且，只有在“所有线程拥有一致的等候条件”、“所有线程在被唤醒后会进行相同的操作”、“在发出通知后，只需要一个线程恢复工作”都满足的情况下，才能调用`notify()`方法。\
      \   `java.util.concurrent.locks` 工具提供了 `Condition.signal()`和 `Condition.signalAll()`方法来唤醒在调用`Condition.await()`时受阻的线程。使用\
      \ `java.util.concurrent.locks.Lock` 对象时需要 Condition 对象。利用一个 Lock 对象来同步的代码会用到一个或多个和这个\
      \ Lock 对象相关的 Condition 对象，而不只是使用它的隐含锁。这些对象根据 Lock 对象制定的锁定策略来直接进行相互操作。因此，方法 `await()、signal()`和\
      \ `signalAll()`是用来代替方法 `wait()、notify()` 和 `notifyAll()`的。   \n3) 示例   \n  \
      \  1、不符合规则的代码示例（`notify()`），该示例演示了一个由多个线程执行的复杂的多步骤过程。每一个线程执行由 time 字段决定的步骤。线程会等待\
      \ time 字段的指示，来确定是否执行对应线程的步骤。在 执行任务后，每一个线程先是增加 time,然后再通知负责下一个步骤的线程。   \n   \
      \ ```java\n    public final class ProcessStep implements Runnable {\n      \
      \  private static final Object lock = new Object();\n        private static\
      \ int time = 0;\n        private final int step; // Perform operations when\
      \ field time\n        // reaches this value\n    \n        public ProcessStep(int\
      \ step) {\n            this.step = step;\n        }\n    \n        @Override\n\
      \        public void run() {\n            try {\n                synchronized\
      \ (lock) {\n                    while (time != step) {\n                   \
      \     lock.wait();\n                    }\n                    // Perform operations\n\
      \                    time++;\n                    lock.notify();\n         \
      \       }\n            } catch (InterruptedException ie) {\n               \
      \ Thread.currentThread().interrupt(); // Reset interrupted status\n        \
      \    }\n        }\n    \n        public static void main(String[] args) {\n\
      \            for (int i = 4; i >= 0; i--) {\n                new Thread(new\
      \ ProcessStep(i)).start();\n            }\n        }\n    }\n    ```  \n   \
      \ 本代码示例违反了活性属性。因为每一个线程在继续执行前要求的 step 数值是不一样的，所有线程有着不同的等候条件。`Object.notify()`方法一次只会唤醒一个线程。除非正好唤醒执行下一个步骤的线程，否则这个程序会发生死锁。\
      \   \n       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '373:DEPRECATED: State Synchronization Error'
      - type: CERT
        value: THI02-J:Notify all waiting threads rather than a single thread
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160093
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\npublic final class ProcessStep\
      \ implements Runnable {  \n    private static final Object lock = new Object();\n\
      \    private static int time = 0;\n    private final int step; // Perform operations\
      \ when field time\n    // reaches this value\n\n    public ProcessStep(int step)\
      \ {  \n        this.step = step;  \n    }\n\n    @Override\n    public void\
      \ run() {\n        try {\n            synchronized (lock) {\n              \
      \  while (time != step) {\n                    lock.wait();\n              \
      \  }\n                // Perform operations\n                time++;\n     \
      \           lock.notifyAll(); // Use notifyAll() instead of notify()\n     \
      \       }\n        } catch (InterruptedException ie) {\n            Thread.currentThread().interrupt();\
      \ // Reset interrupted status\n        }\n    } \n}\n```  \n代码中每一个限制在完成它自己的步骤后调用`Object.notifyAll()`方法来通知所有等待中的线程，准备好的线程可以执行它的任务，而那些其他等待条件为假的线程则会继续等待。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.9 通知所有等待中的线程而不是单一的线程
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：notify()方法只唤醒一个线程，而并不保证哪一个线程会接到通知。如果没有满足等候条件，那么选中的线程会继续等待，这通常违背了通知的目的。另外，`Condition.signal()`和`Condition.signalAll()`方法唤醒在调用`Condition.await()`时受阻线程的原理也与`Object.notify()`和`Object.notifyAll()`方法相同。\
      \  \n对于通知所有等待中的线程而不是单一的线程的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\n\
      public final class ProcessStep implements Runnable {  \n    private static final\
      \ Object lock = new Object();\n    private static int time = 0;\n    private\
      \ final int step; // Perform operations when field time\n    // reaches this\
      \ value\n\n    public ProcessStep(int step) {  \n        this.step = step; \
      \ \n    }\n\n    @Override\n    public void run() {\n        try {\n       \
      \     synchronized (lock) {\n                while (time != step) {\n      \
      \              lock.wait();\n                }\n                // Perform operations\n\
      \                time++;\n                lock.notify();\n            }\n  \
      \      } catch (InterruptedException ie) {\n            Thread.currentThread().interrupt();\
      \ // Reset interrupted status\n        }\n    }\n\n    public static void main(String[]\
      \ args) {\n        for (int i = 4; i >= 0; i--) {\n            new Thread(new\
      \ ProcessStep(i)).start();\n        }\n    } \n}\n```  \n这个代码演示了一个由多线程执行的复杂的多步过程。每一个线程的执行由`time`字段决定步骤。线程会等待`time`字段的指示，来确定是否执行对应线程的步骤。在执行任务后，每一个线程先是增加`time`，然后再通知负责下一个步骤的线程。但是，每一个线程拥有不同的等候条件，`Object.notify()`每次只唤醒一个线程。除非正好唤醒执行下一个步骤的线程，否则这个程序会发生死锁。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: '373:DEPRECATED: State Synchronization Error'
      - type: CERT
        value: THI02-J:Notify all waiting threads rather than a single thread
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170041'
    language: Java
  - Advice: "使用`Files.createTempDir`或者Guava的类似方法`Files.createTempDir`来创建临时文件夹。  \n\
      \  \n**例如**：下面代码示例中使用了`Files.createTempDir`来创建临时文件夹。  \n```java\n...  \nPath\
      \ tempPath = Files.createTempDirectory(\"\");  \nFile tempDir = tempPath.toFile();\
      \  \n...\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】不应该使用临时文件创建文件夹
    Categories_En:
      Category: []
    Default: true
    Description: "使用文件`File.createTempFile`作为创建临时目录的第一步，会导致竞态条件，并且本质上是不可靠和不安全的。  \n\
      \  \n**例如**：下面代码示例中使用`createTempFile`生成临时文件，将其删除再使用`mkdir`方法创建文件夹。  \n```java\n\
      ...  \nFile tempDir = File.createTempFile(\"\", \".\");  \ntempDir.delete();\
      \  \ntempDir.mkdir();   \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: correctness
    Type: std
    WhiteList: not
    id: '0202000010150445'
    language: Java
  - Advice: "检查程序逻辑，如果需要获取数组中元素的值，应采用循环的方式获取其中的值形成字符串。或者直接使用Java工具类Arrays中的`toString`方法转化数组为字符串。\
      \  \n  \n**例1**：采用循环的方式获取其中的值形成字符串。  \n```java\nString[ ] params = getParams();\
      \  \nString string = null;  \nif (params == null)  \n    string = \"null\";\
      \  \nint iMax = params.length - 1;  \nif (iMax == -1)  \n    string = \"[]\"\
      ;  \nStringBuilder stringBuilder = new StringBuilder();  \nstringBuilder.append('[');\
      \  \nfor (int i = 0;; i++) {  \n    stringBuilder.append(String.valueOf(params[i]));\
      \  \n    if (i == iMax) {  \n        stringBuilder.append(']');  \n        break;\
      \  \n    }  \n    stringBuilder.append(\", \");  \n}  \nstring = stringBuilder.toString();\n\
      ```  \n  **例2**：直接使用Java工具类Arrays中的`toString`方法转化数组为字符串。  \n```java\nString[]\
      \ params = getParams();  \nString string = Arrays.toString(args);\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: API误用
      - type: DetailClassChin
        value: 【强制】不要在数组上调用toString()
    Categories_En:
      Category: []
    Default: true
    Description: "Java中所有的类都继承至Object，Object类中有一个`toString`的方法，那么所有类也都有这个方法。由于Java输出用的函数`print()`不接受对象输出，所以一般都会先用该对象的`toString`方法将其转换为字符串后再输出。\
      \  \n在Object中`toString`方法的源码如下。  \n```java\npublic String toString() {  \n \
      \   return getClass().getName() + \"@\" + Integer.toHexString(hashCode()); \
      \ \n}\n```  \n而数组类中并没有对此方法进行重写，仅仅是重载为类的静态方法。所以数组直接调用`toString`方法的结果并不是数组中元素的值，而是[数组的类型@内存地址哈希码]。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - toString on Array
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: SJ/T 11683-2017
        value: 6.5.3 调用数组的toString方法
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150104'
    language: Java
  - Advice: "准确的使用Optional可以提高代码简洁性，并且可以避免空指针异常的出现。  \n  \n**例如**：下列代码中使用getOptional返回值为`Optional.empty()`，通过`ifPresent`方法来判断值是否为空。\
      \ \n```java\npublic void doSomething () {  \n    Optional<String> optional =\
      \ getOptional();  \n    optional.ifPresent(...);  \n}  \npublic Optional<String>\
      \ getOptional() {  \n    ...  \n    return Optional.empty();   \n}\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】Optional不应该可能为null
    Categories_En:
      Category: []
    Default: true
    Description: "Optional在某种意义上是Java8提供的为了解决null安全问题的一个API，而将Optional与null进行比较，或者返回的Optional可能为null都是不准确的做法。\
      \  \n  \n**例如**：下列代码中使用`getOptional`返回值可能为null，而且与null进行了对比。 \n```java\npublic\
      \ void doSomething () {  \n    Optional<String> optional = getOptional();  \n\
      \    if (optional != null) {    \n        ...  \n    }  \n}  \npublic Optional<String>\
      \ getOptional() {  \n    ...  \n    return null;   \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 398:Indicator of Poor Code Quality
    Security: not
    Tag: correctness
    Type: std
    WhiteList: not
    id: '0202000010150431'
    language: Java
  - Advice: '**规范的代码样例1**：

      这个解决方案使用一个固定的线程池而不是一个 `ThreadGroup` 来对它的三个任务进行分组。 `java.util.concurrent.ExecutorService`
      接口提供了管理线程池的方法。 尽管该接口缺少用于查找正在执行的线程数或用于枚举线程的方法，但逻辑分组可以帮助控制整个组的行为。 例如，调用 `shutdownPool()`
      方法会终止属于特定线程池的所有线程。

      ``java

      public final class NetworkHandler {

      private final ExecutorService executor;


      NetworkHandler(int poolSize) {

      this.executor = Executors.newFixedThreadPool(poolSize);

      }


      public void startThreads() {

      for (int i = 0; i < 3; i++) {

      executor.execute(new HandleRequest());

      }

      }


      public void shutdownPool() {

      executor.shutdown();

      }


      public static void main(String[] args)  {

      NetworkHandler nh = new NetworkHandler(3);

      nh.startThreads();

      nh.shutdownPool();

      }

      }


      ``

      在 Java SE 5.0 之前，需要在单独的线程中捕获未捕获异常的应用程序必须扩展 `ThreadGroup` 类，因为这是提供所需功能的唯一直接方法。
      具体来说，应用程序的 `UncaughtExceptionHandler` 只能通过子类化 `ThreadGroup` 来控制。 在最近的 Java 版本中，`UncaughtExceptionHandler`
      使用由 `Thread` 类封闭的接口在每个线程的基础上进行维护。 因此，`ThreadGroup` 类几乎没有提供独特的功能 [Goetz 2006],
      [Bloch 2008]。

      请参阅 [TPS03-J. 确保在线程池中执行的任务不会静默失败]以获取有关在线程池中使用未捕获异常处理程序的更多信息。'
    Advice_En: "**Compliant Solution**\n\nThis compliant solution uses a fixed thread\
      \ pool rather than a `ThreadGroup` to group its three tasks. The `java.util.concurrent.ExecutorService`\
      \ interface provides methods to manage the thread pool. Although the interface\
      \ lacks methods for finding the number of actively executing threads or for\
      \ enumerating the threads, the logical grouping can help control the behavior\
      \ of the group as a whole. For instance, invoking the `shutdownPool()` method\
      \ terminates all threads belonging to a particular thread pool.\n\n```java\n\
      public final class NetworkHandler {\n  private final ExecutorService executor;\n\
      \n  NetworkHandler(int poolSize) {\n    this.executor = Executors.newFixedThreadPool(poolSize);\n\
      \  }\n\n  public void startThreads() {\n    for (int i = 0; i < 3; i++) {\n\
      \      executor.execute(new HandleRequest());\n    }\n  }\n\n  public void shutdownPool()\
      \ {\n    executor.shutdown();\n  }\n\n  public static void main(String[] args)\
      \  {\n    NetworkHandler nh = new NetworkHandler(3);\n    nh.startThreads();\n\
      \    nh.shutdownPool();\n  }\n}\n```\n\nBefore Java SE 5.0, applications that\
      \ needed to catch an uncaught exception in a separate thread had to extend the\
      \ `ThreadGroup` class because this was the only direct approach to provide the\
      \ required functionality. Specifically, an application's `UncaughtExceptionHandler`\
      \ could only be controlled by subclassing `ThreadGroup`. In more recent versions\
      \ of Java, `UncaughtExceptionHandler` is maintained on a per-thread basis using\
      \ an interface enclosed by the `Thread` class. Consequently, the `ThreadGroup`\
      \ class provides little unique functionality [Goetz 2006], [Bloch 2008].\n\n\
      Refer to TPS03-J. Ensure that tasks executing in a thread pool do not fail silently\
      \ for more information on using uncaught exception handlers in thread pools."
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CERT
      - type: SubClassChin
        value: Rule 10. 线程API (THI)
      - type: DetailClassChin
        value: THI01-J：不要调用ThreadGroup方法
    Categories_En:
      Category:
      - type: ClassChin
        value: SEI CERT Oracle Coding Standard for Java
      - type: SubClassChin
        value: Rule 10. Thread APIs (THI)
      - type: DetailClassChin
        value: THI01-J:Do not invoke ThreadGroup methods
    Default: true
    Description: "Java中每一个线程在创建时都赋予了一个线程组。这些线程组是由java.lang.ThreadGroup类来实现的。如果没有明确的指定线程组的名字，JVM会分配一个默认的主线程组。ThreadGroup类提供了针对属于同一个线程组的线程进行同时操作的便利方法。但是ThreadGroup类中很多方法是不提倡使用的，如：`allowThreadSuspension()、resume()、stop()、suspend()`，而且有些方法不是线程安全的，如：`activeCount()、enumerate()`。\
      \  \n不安全但未弃用的方法包括\n\n* `ThreadGroup.activeCount()`\n  根据 Java API [API 2014]，`activeCount()`\
      \ 方法\n\n  > 返回当前线程的 [线程组] 及其子组中活动线程的估计数。\n\n  这种方法通常被用作线程枚举的先驱。从未启动的线程仍然驻留在线程组中并被认为是活动的。活动计数还受到某些系统线程\
      \ [API 2014] 的影响。因此，*`activeCount()`* 方法可能无法反映线程组中正在运行的任务的实际数量。\n* `ThreadGroup.enumerate()`\n\
      \  根据 Java API [API 2014]，`ThreadGroup` 类文档\n\n  > [`enumerate()` 方法] 将此线程组及其子组中的每个活动线程复制到指定的数组中......\n\
      \  > 应用程序可能会使用 `activeCount` 方法来估计数组应该有多大；但是，*如果数组太短而无法容纳所有线程，多余的线程将被忽略*。\n\n\
      使用 `ThreadGroup` API 来关闭线程也有缺陷。因为 `stop()` 方法已被弃用，程序需要替代方法来停止线程。根据 *Java 编程语言*\
      \ [JPL 2006]：\n\n> 一种方法是让发起终止的线程加入其他线程，从而知道这些线程何时终止。但是，应用程序可能必须维护它自己创建的线程列表，因为简单地检查\
      \ `ThreadGroup` 可能会返回不会终止的库线程，并且不会返回连接。\n\n`Executor` 框架为管理线程的逻辑分组提供了更好的 API，并提供了用于处理关闭和线程异常的安全设施\
      \ [Bloch 2008]。因此，程序不能调用 `ThreadGroup` 方法。\n风险评估：使用ThreadGroup API中的某些方法（如：`allowThreadSuspension()、resume()、stop()、suspend()`、`activeCount()、enumerate()`）可能会导致竞态、内存泄漏，以及不一致的对象状态。\n\
      **不规范的代码样例1**：\n这个不规范的代码示例包含一个维护 `controller` 线程的“NetworkHandler”类。 `controller`\
      \ 线程将每个新请求委托给工作线程。 为了演示本示例中的竞争条件， `controller` 线程通过其`run()`方法连续启动三个线程来服务三个请求。\
      \ 所有线程都被定义为属于 `Chief` 线程组。\n```java\nfinal class HandleRequest implements Runnable\
      \ {\n  public void run() {\n    // Do something\n  }\n}\n\npublic final class\
      \ NetworkHandler implements Runnable {\n  private static ThreadGroup tg = new\
      \ ThreadGroup(\"Chief\");\n\n  @Override public void run() {\n    new Thread(tg,\
      \ new HandleRequest(), \"thread1\").start();\n    new Thread(tg, new HandleRequest(),\
      \ \"thread2\").start();\n    new Thread(tg, new HandleRequest(), \"thread3\"\
      ).start();\n  }\n\n  public static void printActiveCount(int point) {\n    System.out.println(\"\
      Active Threads in Thread Group \" + tg.getName() +\n        \" at point(\" +\
      \ point + \"):\" + \" \" + tg.activeCount());\n  }\n\n  public static void printEnumeratedThreads(Thread[]\
      \ ta, int len) {\n    System.out.println(\"Enumerating all threads...\");\n\
      \    for (int i = 0; i < len; i++) {\n      System.out.println(\"Thread \" +\
      \ i + \" = \" + ta[i].getName());\n    }\n  }\n\n  public static void main(String[]\
      \ args) throws InterruptedException {\n    // Start thread controller\n    Thread\
      \ thread = new Thread(tg, new NetworkHandler(), \"controller\");\n    thread.start();\n\
      \n    // Gets the active count (insecure)\n    Thread[] ta = new Thread[tg.activeCount()];\n\
      \n    printActiveCount(1); // P1\n    // Delay to demonstrate TOCTOU condition\
      \ (race window)\n    Thread.sleep(1000);\n    // P2: the thread count changes\
      \ as new threads are initiated\n    printActiveCount(2);\n    // Incorrectly\
      \ uses the (now stale) thread count obtained at P1\n    int n = tg.enumerate(ta);\n\
      \    // Silently ignores newly initiated threads\n    printEnumeratedThreads(ta,\
      \ n);\n                                   // (between P1 and P2)\n\n    // This\
      \ code destroys the thread group if it does\n    // not have any live threads\n\
      \    for (Thread thr : ta) {\n      thr.interrupt();\n      while(thr.isAlive());\n\
      \    }\n    tg.destroy();\n  }\n}\n```\n此实现包含检查时间、使用时间 (TOCTOU) 漏洞，因为它在不确保原子性的情况下获取计数并枚举列表。如果在调用\
      \ `activeCount()` 之后和调用 `main()` 方法中的 `enumerate()` 之前发生一个或多个新请求，则组中的线程总数会增加，但枚举列表`ta`只包含初始编号，即两个线程引用：`main`和`controller`。因此，程序将无法考虑`Chief`线程组中新启动的线程。\n\
      任何后续使用 `ta` 数组都是不安全的。例如，调用 `destroy()` 方法来销毁线程组及其子组将无法按预期工作。调用 `destroy()` 的前提是线程组必须为空且没有执行线程。代码尝试通过中断线程组中的每个线程来遵守前提条件。但是，调用\
      \ `destroy()` 方法时线程组不会为空，从而导致抛出 `java.lang.IllegalThreadStateException`。"
    Description_En: "Each thread in Java is assigned to a thread group upon the thread's\
      \ creation. These groups are implemented by the `<a class=\"external-link\"\
      \ href=\"https://docs.oracle.com/javase/8/docs/api/java/lang/ThreadGroup.html\"\
      \ rel=\"nofollow\">java.lang.ThreadGroup</a>` class. When the thread group name\
      \ is not specified explicitly, the `main` default group is assigned by the Java\
      \ Virtual Machine (JVM) [Java Tutorials]. The convenience methods of the `ThreadGroup`\
      \ class can be used to operate on all threads belonging to a thread group at\
      \ once. For instance, the `ThreadGroup.interrupt()` method interrupts all threads\
      \ in the thread group. Thread groups also help reinforce layered security by\
      \ confining threads into groups so that they avoid interference with threads\
      \ in other groups [JavaThreads 2004].\n\nEven though thread groups are useful\
      \ for keeping threads organized, programmers seldom benefit from their use because\
      \ many of the methods of the `ThreadGroup` class (for example, `allowThreadSuspension()`,\
      \ `resume()`, `stop()`, and `suspend()`) are deprecated. Furthermore, many nondeprecated\
      \ methods are obsolete in that they offer little desirable functionality. Ironically,\
      \ a few `ThreadGroup` methods are not even thread-safe [Bloch 2001].\n\nInsecure\
      \ yet nondeprecated methods include\n\n* `ThreadGroup.activeCount()`\n  According\
      \ to the Java API [API 2014], the `activeCount()` method\n\n  > returns an estimate\
      \ of the number of active threads in the current thread's thread group and its\
      \ subgroups.\n\n  This method is often used as a precursor to thread enumeration.\
      \ Threads that have never started nevertheless reside in the thread group and\
      \ are considered to be active. The active count is also affected by the presence\
      \ of certain system threads [API 2014]. Consequently, the *`activeCount()`*\
      \ method might fail to reflect the actual number of running tasks in the thread\
      \ group.\n* `ThreadGroup.enumerate()`\n  According to the Java API [API 2014],\
      \ `ThreadGroup` class documentation\n\n  > [The `enumerate()` method] copies\
      \ into the specified array every active thread in this thread group and its\
      \ subgroups....\n  > An application might use the `activeCount` method to get\
      \ an estimate of how big the array should be; however, *if the array is too\
      \ short to hold all the threads, the extra threads are silently ignored*.\n\n\
      Using the `ThreadGroup` APIs to shut down threads also has pitfalls. Because\
      \ the `stop()` method is deprecated, programs require alternative methods to\
      \ stop threads. According to *The Java Programming Language* [JPL 2006]:\n\n\
      > One way is for the thread initiating the termination to join the other threads\
      \ and so know when those threads have terminated. However, an application may\
      \ have to maintain its own list of the threads it creates because simply inspecting\
      \ the `ThreadGroup` may return library threads that do not terminate and for\
      \ which join will not return.\n\nThe `Executor` framework provides a better\
      \ API for managing a logical grouping of threads and offers secure facilities\
      \ for handling shutdown and thread exceptions [Bloch 2008]. Consequently, programs\
      \ must not invoke `ThreadGroup` methods.\n\n**Noncompliant Code Example**\n\n\
      This noncompliant code example contains a `NetworkHandler` class that maintains\
      \ a `controller` thread. The `controller` thread delegates each new request\
      \ to a worker thread. To demonstrate the race condition in this example, the\
      \ `controller` thread serves three requests by starting three threads in succession\
      \ from its `run()` method. All threads are defined to belong to the `Chief`\
      \ thread group.\n\n```java\nfinal class HandleRequest implements Runnable {\n\
      \  public void run() {\n    // Do something\n  }\n}\n\npublic final class NetworkHandler\
      \ implements Runnable {\n  private static ThreadGroup tg = new ThreadGroup(\"\
      Chief\");\n\n  @Override public void run() {\n    new Thread(tg, new HandleRequest(),\
      \ \"thread1\").start();\n    new Thread(tg, new HandleRequest(), \"thread2\"\
      ).start();\n    new Thread(tg, new HandleRequest(), \"thread3\").start();\n\
      \  }\n\n  public static void printActiveCount(int point) {\n    System.out.println(\"\
      Active Threads in Thread Group \" + tg.getName() +\n        \" at point(\" +\
      \ point + \"):\" + \" \" + tg.activeCount());\n  }\n\n  public static void printEnumeratedThreads(Thread[]\
      \ ta, int len) {\n    System.out.println(\"Enumerating all threads...\");\n\
      \    for (int i = 0; i < len; i++) {\n      System.out.println(\"Thread \" +\
      \ i + \" = \" + ta[i].getName());\n    }\n  }\n\n  public static void main(String[]\
      \ args) throws InterruptedException {\n    // Start thread controller\n    Thread\
      \ thread = new Thread(tg, new NetworkHandler(), \"controller\");\n    thread.start();\n\
      \n    // Gets the active count (insecure)\n    Thread[] ta = new Thread[tg.activeCount()];\n\
      \n    printActiveCount(1); // P1\n    // Delay to demonstrate TOCTOU condition\
      \ (race window)\n    Thread.sleep(1000);\n    // P2: the thread count changes\
      \ as new threads are initiated\n    printActiveCount(2);\n    // Incorrectly\
      \ uses the (now stale) thread count obtained at P1\n    int n = tg.enumerate(ta);\n\
      \    // Silently ignores newly initiated threads\n    printEnumeratedThreads(ta,\
      \ n);\n                                   // (between P1 and P2)\n\n    // This\
      \ code destroys the thread group if it does\n    // not have any live threads\n\
      \    for (Thread thr : ta) {\n      thr.interrupt();\n      while(thr.isAlive());\n\
      \    }\n    tg.destroy();\n  }\n}\n```\n\nThis implementation contains a time-of-check,\
      \ time-of-use (TOCTOU) vulnerability because it obtains the count and enumerates\
      \ the list without ensuring atomicity. If one or more new requests were to occur\
      \ after the call to `activeCount()` and before the call to `enumerate()` in\
      \ the `main()` method, the total number of threads in the group would increase,\
      \ but the enumerated list `ta` would contain only the initial number, that is,\
      \ two thread references: `main` and `controller`. Consequently, the program\
      \ would fail to account for the newly started threads in the `Chief` thread\
      \ group.\n\nAny subsequent use of the `ta` array would be insecure. For example,\
      \ calling the `destroy()` method to destroy the thread group and its subgroups\
      \ would not work as expected. The precondition to calling `destroy()` is that\
      \ the thread group must be empty with no executing threads. The code attempts\
      \ to comply with the precondition by interrupting every thread in the thread\
      \ group. However, the thread group would not be empty when the `destroy()` method\
      \ was called, causing a `java.lang.IllegalThreadStateException` to be thrown."
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: cert
    WhiteList: not
    id: '0102000010141101'
    language: Java
  - Advice: Executor框架提供了更好的API来管理逻辑上的线程组，并提供了安全终止线程和处理线程异常的功能。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 危险函数
      - type: DetailClassChin
        value: 【强制】不要调用不安全的ThreadGroup方法
    Categories_En:
      Category: []
    Default: true
    Description: "使用ThreadGroup API有可能导致竞争条件、内存泄漏以及不一致的对象状态。  \nThreadGroup类中很多的方法已经不提倡使用了，其中包括：`allowThreadSuspension()、resume()、stop()、suspend()`。\
      \  \n危险但不反对使用的方法包括：`ThreadGroup.activeCount()、ThreadGroup.enumerate()`。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - (THI01-J)不能调用ThreadGroup方法
    ENSubClassChin: 线程API(THI)
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: THI00-J:Do not invoke Thread.run()
    Security: not
    Tag: concurrency,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150602'
    language: Java
  - Advice: "对应的符合规则的解决方案中，使用一个固定的线程池，而不是 ThreadGroup 来管理它的 3 个任务。接口 `java.util.concurrent.ExecutorService`\
      \ 提供了管理线程池的方法。虽然这个接口没有提供方法来 获取活跃线程的数目或是进行线程列举，但是逻辑上的分组可以有助于全局化管理线程组的行为。例如，调用\
      \ `shutdownPool()`方法可以终止属于同一线程组的所有线程。   \n    ```java\n    public final class\
      \ NetworkHandler {\n        private final ExecutorService executor;\n    \n\
      \        NetworkHandler(int poolSize) {\n            this.executor = Executors.newFixedThreadPool(poolSize);\n\
      \        }\n    \n        public void startThreads() {\n            for (int\
      \ i = 0; i < 3; i++) {\n                executor.execute(new HandleRequest());\n\
      \            }\n        }\n    \n        public void shutdownPool() {\n    \
      \        executor.shutdown();\n        }\n    \n        public static void main(String[]\
      \ args) {\n            NetworkHandler nh = new NetworkHandler(3);\n        \
      \    nh.startThreads();\n            nh.shutdownPool();\n        }\n    }\n\
      \    ```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 6.3 并发程序安全
      - type: DetailClassChin
        value: C.10.2 调用 ThreadGroup 方法
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n程序不应调用 ThreadGroup 方法。   \n2) 原理   \nJava 中每一个线程在创建时都赋予了一个线程组。这些线程组是由`java.lang.ThreadGroup`类来实现的。\
      \ ThreadGroup 类提供了对属于同一个线程组的线程进行同时操作的便利方法。比如，`ThreadGroup.intemipt()`方法可以中断线程组中的所有线程。通过将线程限制在线程组中可避免它们与其他线程组中的线程发生冲突，因此，线程组还可以用于分层安全机制。\
      \   \n虽然线程组有利于线程的组织，但因为 ThreadGroup 类中很多方法是不提倡使用的（例  如   \n`allowThreadSuspension()`、`resume()`、`stop()`和\
      \ `suspend()`），所以程序员很少能从线程组的使用中获得好处。而且，因为没有提供合适的功能，很多方法也是过时的。甚至一些 ThreadGroup\
      \ 的方法不是线程安全的，同时，使用 ThreadGroupAPI 来终止线程也会有问题。因为不提倡使用`stop()`方法，程序需要使用替代的方法来停止线程。\
      \    \n一种方式是让即将终止的线程等待其他线程的完成，并因此知道其他线程是否已停止运行。 然而，一个应用将不得不维持一个它所创建的线程的列表，因为简单地检查\
      \ ThreadGroup 可能会得到不会终止的运行库线程并且对它们的等待不会返回。   \nExcutor 框架提供了好的 API 来管理逻辑上的线程组，并提供了安全终止线程和处理线程异常的功能。因此，程序不应该调用\
      \ ThreadGroup 方法。   \n3) 示例   \n    一个不符合规则的代码示例，该示例使用一个 NetworkHandler 类来维护一个控制线程。这个控制线程将新请求分配给一个工作线程。为说明本例子中的竞态条件，代码中的控制线程在它的`run()`方法中，通过启动\
      \ 3 个线程来处理 3 个请求。所有的线程都属于 Chief 线程组。   \n    ```java\n    final class HandleRequest\
      \ implements Runnable {\n        public void run() {\n            // Do something\n\
      \        }\n    }\n    \n    public final class NetworkHandler implements Runnable\
      \ {\n        private static ThreadGroup tg = new ThreadGroup(\"Chief\");\n \
      \   \n        @Override\n        public void run() {\n            new Thread(tg,\
      \ new HandleRequest(), \"thread1\").start();\n            new Thread(tg, new\
      \ HandleRequest(), \"thread2\").start();\n            new Thread(tg, new HandleRequest(),\
      \ \"thread3\").start();\n        }\n    \n        public static void printActiveCount(int\
      \ point) {\n            System.out.println(\"Active Threads in Thread Group\
      \ \" + tg.getName() + \" at point(\" + point + \"):\" + \" \"\n            \
      \        + tg.activeCount());\n        }\n    \n        public static void printEnumeratedThreads(Thread[]\
      \ ta, int len) {\n            System.out.println(\"Enumerating all threads...\"\
      );\n            for (int i = 0; i < len; i++) {\n                System.out.println(\"\
      Thread \" + i + \" = \" + ta[i].getName());\n            }\n        }\n    \n\
      \        public static void main(String[] args) throws InterruptedException\
      \ {\n            // Start thread controller\n            Thread thread = new\
      \ Thread(tg, new NetworkHandler(), \"controller\");\n            thread.start();\n\
      \    \n            // Gets the active count (insecure)\n            Thread[]\
      \ ta = new Thread[tg.activeCount()];\n    \n            printActiveCount(1);\
      \ // P1\n            // Delay to demonstrate TOCTOU condition (race window)\n\
      \            Thread.sleep(1000);\n            // P2: the thread count changes\
      \ as new threads are initiated\n            printActiveCount(2);\n         \
      \   // Incorrectly uses the (now stale) thread count obtained at P1\n      \
      \      int n = tg.enumerate(ta);\n            // Silently ignores newly initiated\
      \ threads\n            printEnumeratedThreads(ta, n);\n            // (between\
      \ P1 and P2)\n    \n            // This code destroys the thread group if it\
      \ does\n            // not have any live threads\n            for (Thread thr\
      \ : ta) {\n                thr.interrupt();\n                while (thr.isAlive())\n\
      \                    ;\n            }\n            tg.destroy();\n        }\n\
      \    }\n    ```   \n    这个实现中隐藏着一个 `time-of-check,time-of-use (TOCTOU)`漏洞，因为代码在获取线程数并列举它们时，并不能保证操作的原子性。如果一个或多个请求在调用\
      \ `activeCount()`之 后，并且是在 `main()`方法中调用 `enumerate()`之前发生，那么线程组中线程的数目会增加，但是线程的列表\
      \ ta 中只包含之前数量的线程。即，只有两个线程引用：main 和控制线程。因此，程序会因为没有考虑到 Chief线程组中新加入的线程而出错。   \n\
      \       \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: THI00-J:Do not invoke Thread.run()
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160092
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：  \n```java\npublic final class NetworkHandler\
      \ {  \n    private final ExecutorService executor;\n\n    NetworkHandler(int\
      \ poolSize) {  \n        this.executor = Executors.newFixedThreadPool(poolSize);\
      \  \n    }\n\n    public void startThreads() {\n        for (int i = 0; i <\
      \ 3; i++) {\n            executor.execute(new HandleRequest());\n        }\n\
      \    }\n\n    public void shutdownPool() {\n        executor.shutdown();\n \
      \   }\n\n    public static void main(String[] args) {\n        NetworkHandler\
      \ nh = new NetworkHandler(3);\n        nh.startThreads();\n        nh.shutdownPool();\n\
      \    }  \n}\n```  \n代码使用了一个固定的线程池来管理它的3个线程，而不是采用ThreadGroup。接口`java.util.concurrent.ExecutorService`提供了管理线程池的方法。虽然这个接口没有提供方法来获取活跃线程的数量或是进行线程列举，但是逻辑上的分组可以有助于全局化管理线程组。如：代码中`shutdownPool()`方法可以终止属于同一线程组的所有线程。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.10 不要调用ThreadGroup方法
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：Java中每一个线程在创建时都赋予了一个线程组。这些线程组是由`java.lang.ThreadGroup`类来实现的。如果没有明确的指定线程组的名字，JVM会分配一个默认的主线程组。ThreadGroup类提供了针对属于同一个线程组的线程进行同时操作的便利方法。但是ThreadGroup类中很多方法是不提倡使用的，如：`allowThreadSuspension()、resume()、stop()、suspend()`，而且有些方法不是线程安全的，如：`activeCount()、enumerate()`。使用ThreadGroup\
      \ API中的某些方法（如：`allowThreadSuspension()、resume()、stop()、suspend()`、`activeCount()、enumerate()`）可能会导致竞态、内存泄漏，以及不一致的对象状态。\
      \  \n 对于不要调用ThreadGroup方法的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\n\
      public int docreate(ThreadGroup tg) {\n    int alive = tg.activeCount();\n \
      \   return alive;\n}\n```  \n代码调用了`ThreadGroup.activeCount()`方法，该方法在API中描述为返回线程组中活跃线程数的一个估计值，该方法可能不能正确的反应线程组中那些正在运行的线程的数量。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CERT
        value: THI00-J:Do not invoke Thread.run()
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170042'
    language: Java
  - Advice: "`salt`值始终不能为硬编码。通常情况下，应该对密码加以模糊化，并在外部资源中进行管理。在系统中采用明文的形式存储`salt`，会造成任何有充分权限的人读取和误用`salt`。\
      \  \n  \n**例如**：以下代码使用由系统管理员配置的`salt`变量。  \n```java\n...  \nbyte[] salt = getConfigSalt();\
      \  \nPBEParameterSpec defParams = new PBEParameterSpec(salt, iterationCount);\
      \  \n...\n```"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 密码规范
      - type: DetailClassChin
        value: 【推荐】不要使用硬编码PBE Salt
    Categories_En:
      Category: []
    Default: true
    Description: "采用硬编码处理`salt`不仅使所有项目开发人员都可以查看该`salt`，而且一旦将该代码投入到生产中，将无法轻易更改该`salt`。攻击者可以根据该`salt`的值计算出该应用程序的\"\
      彩虹表\"，并能轻松地确定Hash值。  \n  \n**例如**：以下代码使用了硬编码`salt`。\n```java  \n...  \nbyte[]\
      \ salt = new byte[]{50, 33, 64, 36, 40, 53, 35, 64, 53, 51, 50, 64, 37, 35,\
      \ 36, 50, 53, 51, 108, 53, 35, 64, 36};  \nPBEParameterSpec defParams = new\
      \ PBEParameterSpec(salt, iterationCount);  \n...\n```  \n该代码可以正常运行，但是任何有该代码权限的人都能得到这个`salt`。一旦程序发布，将无法更改`salt`，攻击者如果得到`salt`值，将很容易入侵系统。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Hardcoded PBE Salt
    ENSubClassChin: Weak Cryptographic Hash
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 760:Use of a One-Way Hash with a Predictable Salt
      - type: CERT
        value: MSC03-J:Never hard code sensitive information
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: OWASP MOBILE TOP 10
        value: 2014:M6 Broken Cryptography
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
    Security: true
    Tag: security,password,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150207'
    language: Java
  - Advice: "采用SSL连接时，不应放弃主机名验证。可考虑采用StrictHostnameVerifer建立服务器身份和安全SSL连接以及正确的重写`verify`方法。\
      \  \n  \n**例1**：下列代码SSLSocketFactory调用`setHostnameVerifier`方法，将`SSLSocketFactory.STRICT_HOSTNAME_VERIFIER`\
      \ 作为参数，将会开启严格匹配模式。  \n```java\n...  \nKeyStore store = KeyStore.getInstance(KeyStore.getDefaultType());\
      \    \nstore.load(null, null);    \nSSLSocketFactory sf = new SSLSocketFactory(store);\
      \    \nsf.setHostnameVerifier(SSLSocketFactory.STRICT_HOSTNAME_VERIFIER);  \
      \  \nHttpParams params = new BasicHttpParams();    \nHttpProtocolParams.setVersion(params,\
      \ HttpVersion.HTTP_1_1);    \nHttpProtocolParams.setContentCharset(params, HTTP.UTF_8);\
      \    \nSchemeRegistry registry = new SchemeRegistry();    \nregistry.register(new\
      \ Scheme(\"http\", PlainSocketFactory.getSocketFactory(), 80));    \nregistry.register(new\
      \ Scheme(\"https\", sf, 443));    \nClientConnectionManager ccm = new ThreadSafeClientConnManager(params,\
      \ registry);    \n...\n``` \n**例2**：下列代码正确的重写HostnameVerifier接口的`verify`方法，没有忽略主机名验证。\
      \  \n```java\n...  \nHostnameVerifier hostnameVerifier = new HostnameVerifier()\
      \ {  \n    @Override  \n    public boolean verify(String requestedHost, SSLSession\
      \ remoteServerSession) {  \n        return requestedHost.equalsIgnoreCase(remoteServerSession.getPeerHost());\
      \   \n    }  \n};  \n...\n```"
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】SSL连接时主机名验证功能不应该禁用
    Categories_En:
      Category: []
    Default: true
    Description: "采用SSL进行连接时，使用`AllowAllHostnameVerifier()`和`SSLSocketFactory.ALLOW_ALL_HOSTNAME_VERIFIER`将会关闭主机名验证功能。这相当于信任所有证书。HostnameVerifier是用于主机名验证的接口,若重写`verify`方法只返回true，意味着信任所有主机名。\
      \  \n  \n**例1**：下列代码SSLSocketFactory调用`setHostnameVerifier`方法，将`SSLSocketFactory.ALLOW_ALL_HOSTNAME_VERIFIER`作为参数，将会关闭主机名验证功能。\
      \  \n```java\n...  \nKeyStore store = KeyStore.getInstance(KeyStore.getDefaultType());\
      \    \nstore.load(null, null);    \nSSLSocketFactory sf = new SSLSocketFactory(store);\
      \    \nsf.setHostnameVerifier(`SSLSocketFactory.`ALLOW_ALL_HOSTNAME_VERIFIER);\
      \    \nHttpParams params = new BasicHttpParams();    \nHttpProtocolParams.setVersion(params,\
      \ HttpVersion.HTTP_1_1);    \nHttpProtocolParams.setContentCharset(params, HTTP.UTF_8);\
      \    \nSchemeRegistry registry = new SchemeRegistry();    \nregistry.register(new\
      \ Scheme(\"http\", PlainSocketFactory.getSocketFactory(), 80));    \nregistry.register(new\
      \ Scheme(\"https\", sf, 443));    \nClientConnectionManager ccm = new ThreadSafeClientConnManager(params,\
      \ registry);    \n... \n```\n**例2**：下列代码重写HostnameVerifier接口的`verify`方法，方法内只有个一个返回值为true的return语句，意味着主机名验证毫无意义。\
      \  \n```java\n...  \nHostnameVerifier hostnameVerifier = new HostnameVerifier()\
      \ {  \n    @Override  \n    public boolean verify(String requestedHost, SSLSession\
      \ remoteServerSession) {  \n        return true;   \n    }  \n};  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Android Hostname Verification Disabled
    ENSubClassChin: Insecure SSL
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 高
    GradesComment: 等级计算
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 297:Improper Validation of Certificate with Host Mismatch
      - type: CERT
        value: DRD19-J:Properly verify server certificate on SSL/TLS
      - type: OWASP TOP 10
        value: 2010:A9 Insufficient Transport Layer Protection
      - type: OWASP TOP 10
        value: 2007:A9 Insecure Communications
      - type: OWASP TOP 10
        value: 2004:A3 Broken Authentication and Session Management
      - type: OWASP MOBILE TOP 10
        value: 2014:M3 Insufficient Transport Layer Protection
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
    Security: true
    Tag: security,correctness
    Type: std
    WhiteList: not
    id: '0202000010150434'
    language: Java
  - Advice: 检测程序逻辑，判断程序中此try块是否存在作用。如果存在应修改此try块，否则应将其删除。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【强制】不要保留空的try代码块
    Categories_En:
      Category: []
    Default: true
    Description: "空的try块在程序中只有存在finally才会存在作用，而finally中语句可以直接写为普通语句，这种代码将会增加代码的阅读、理解和维护难度\
      \  \n  \n**例如**：如下所示代码中存在空的try块  \n```java\n...  \ntry { //空的try代码块\n} \ncatch\
      \ (Exception e) {  \n    e.printStackTrace();  \n}  \n...\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Empty Try Block
    ENSubClassChin: Dead Code
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 561:Dead Code
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150415'
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n```java\n...  \nKeyGenerator keyGen\
      \ = KeyGenerator.getInstance(\"AES\");    \nkeyGen.init(128, new SecureRandom(password.getBytes()));\
      \    \nSecretKey secretKey = kgen.generateKey();    \nbyte[] keyBytes = secretKey.getEncoded();\
      \    \nSecretKeySpec key = new SecretKeySpec(keyBytes, \"AES\");  \nCipher encryptCipher\
      \ = Cipher.getInstance(\"AES\");  \nencryptCipher.init(Cipher.ENCRYPT_MODE,\
      \ key);  \n...\n```  \n上述代码使用KeyGenerator来生成密匙。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.2 数据加密与保护
      - type: DetailClassChin
        value: A.2.2.1.7 不要使用硬编码密匙
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：当程序中使用硬编码加密密匙时，所有项目开发人员都可以查看该密匙，甚至如果攻击者能够获取程序class文件，可通过反编译得到密匙，硬编码加密密匙会大大降低系统安全性。\
      \  \n对于避免使用硬编码密匙的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：   \n```java\nprivate\
      \ static String encryptionKey = \"dfashsdsdfsdgagascv\";  \n...  \nbyte[] keyBytes\
      \ = encryptionKey.getBytes();  \nSecretKeySpec key = new SecretKeySpec(keyBytes,\
      \ \"AES\");  \nCipher encryptCipher = Cipher.getInstance(\"AES\");  \nencryptCipher.init(Cipher.ENCRYPT_MODE,\
      \ key);  \n...\n```  \n上述代码使用硬编码加密密钥执行AES加密。  \n程序应采用不小于8个字节的随机生成的字符串作为密匙。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 321:Use of Hard-coded Cryptographic Key
      - type: CERT
        value: MSC03-J:Never hard code sensitive information
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: OWASP MOBILE TOP 10
        value: 2014:M6 Broken Cryptography
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170016'
    language: Java
  - Advice: 程序中避免调用`System.runFinalizersOnExit()`方法。
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 危险函数
      - type: DetailClassChin
        value: 【强制】不要调用System.runFinalizersOnExit()
    Categories_En:
      Category: []
    Default: true
    Description: 调用`System.runFinalizersOnExit()`方法可能会导致不正确的行为或死锁，目前已经被废弃。
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - ''
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: SJ/T 11683-2017
        value: 6.5.7 调用runFinalizersOnExit方法
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150601'
    language: Java
  - Advice: "程序中所需密码应从配置文件中获取经过加密的密码值。  \n  \n**例如**：下列代码中采用获取经过加密的密码值来获取数据库连接。  \n\
      ```java\n...  \nString password = getEncryptedPass();  \nconn = DriverManager.getConnection(url,user,\
      \ password);\n```"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 密码规范
      - type: DetailClassChin
        value: 【强制】不要使用空密码
    Categories_En:
      Category: []
    Default: true
    Description: "程序中使用了空的密码值，系统安全性将会受到威胁。  \n  \n**例如**：下列代码中采用空密码获取数据库连接。  \n```java\n\
      ...  \nconn = DriverManager.getConnection(url,\"root\", \"\");\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Null Password
    ENSubClassChin: Password Management
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 798:Use of Hard-coded Credentials
      - type: CWE
        value: 259:Use of Hard-coded Password
      - type: CERT
        value: MSC03-J:Never hard code sensitive information
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2010:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 259
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Hard-coded Password [XYP]
      - type: GB/T 34944-2017 Java
        value: 安全功能:口令硬编码
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: security,password
    Type: std
    WhiteList: not
    id: 0202000010150209
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n```java\nString password = getEncryptedPass();\
      \  \nconn = DriverManager.getConnection(url,user, password);\n```  \n上述代码中采用经过加密的密码值来获取数据库连接。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.3 访问控制
      - type: DetailClassChin
        value: A.2.3.2.3 避免使用空密码
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：程序中使用了空的密码值，系统安全性将会受到威胁。  \n对于避免使用空密码的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**： \n ```java \nconn = DriverManager.getConnection(url,\"root\",\
      \ \"\");\n ```  \n上述代码中采用空密码获取数据库连接。  \n程序中所需密码应从配置文件中获取经过加密的密码值。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 798:Use of Hard-coded Credentials
      - type: CWE
        value: 259:Use of Hard-coded Password
      - type: CERT
        value: MSC03-J:Never hard code sensitive information
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: CWE/SANS TOP 25
        value: 2019:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2020:CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2011:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2010:Porous Defenses - CWE ID 798
      - type: CWE/SANS TOP 25
        value: 2009:Porous Defenses - CWE ID 259
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Hard-coded Password [XYP]
      - type: GB/T 34944-2017 Java
        value: 安全功能:口令硬编码
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.1:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 6.5.8
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.8
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.3
      - type: PCI DSS
        value: 3.0:Requirement 8.2.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.1:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2:Requirement 8.2.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.3
      - type: PCI DSS
        value: 3.2.1:Requirement 8.2.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170023'
    language: Java
  - Advice: 由于在断开SSL连接上进行的通信可能会泄漏用户信息，因此应用程序不应放弃SSL验证检查。采用`createSocket(Socket k,String
      host,int port,boolean close)`方法，以主机和端口号作为参数建立安全SSL连接。若一定要使用`createSocket()`方法、`createSocket(InetAddress
      addr,int port)`方法或`createSocket(InetAddress addr,int port,InetAddress localaddr,int
      localport)`方法，应验证服务器标识，避免受到to-man-in-the-middle攻击。
    Advice_En: ''
    BugServerity: 5
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 7.5 网络传输
      - type: DetailClassChin
        value: A.4.5.3 避免使用不安全的SSLSocket
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：`createSocket(InetAddress addr,int port)`方法和`createSocket(InetAddress\
      \ addr,int port,InetAddress localaddr,int localport)`方法不执行主机名验证，`getInsecure(int\
      \ handshakeTimeoutMillis,SSLSessionCache cache)`方法返回socket工厂实例，其所有的SSL安全检查都会被禁用。应用程序中调用这些方法易受到to-man-in-the-middle攻击。\
      \  \n对于避免使用不安全的SSLSocket，示例1给出了不规范用法（java语言）示例。  \n**示例1**：  \n```java\nSSLCertificateSocketFactory\
      \ sslCertificateSocketFactory = (SSLCertificateSocketFactory)SSLCertificateSocketFactory.getDefault(0);\
      \  \nSSLSocket sslSocket = sslCertificateSocketFactory.createSocket(inetAddress,port);\n\
      ```  \n上述代码中SSLCertificateSocketFactory使用`createSocket (InetAddress addr,int\
      \ port)`方法获取SSLSocket。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 297:Improper Validation of Certificate with Host Mismatch
      - type: CERT
        value: DRD19-J:Properly verify server certificate on SSL/TLS
      - type: OWASP TOP 10
        value: 2010:A9 Insufficient Transport Layer Protection
      - type: OWASP TOP 10
        value: 2007:A9 Insecure Communications
      - type: OWASP TOP 10
        value: 2004:A3 Broken Authentication and Session Management
      - type: OWASP MOBILE TOP 10
        value: 2014:M3 Insufficient Transport Layer Protection
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170070'
    language: Java
  - Advice: 如果程序员想要判断对象是否为null，可以采用`obj==null`或者`obj!=null`来代替`obj.equals(null)`。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 代码质量
      - type: DetailClassChin
        value: 【推荐】equals()参数不要为null
    Categories_En:
      Category: []
    Default: true
    Description: "程序使用equals()方法将一个对象与null作比较（`obj.equals(null)`），如果obj对象为null，程序将抛出NullPointerException异常；如果obj对象不是null，那么该表达式将始终返回false。\
      \  \n  \n**例如**：如下代码片段中将一个string对象和null作比较  \n```java\npublic class Demo { \
      \ \n    public static boolean equalsNull(String a){  \n        String b = null;\
      \  \n        return a.equals(b);  //equals参数为null  \n    }  \n}\n```"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - null Argument to equals()
    ENSubClassChin: Code Correctness
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 754:Improper Check for Unusual or Exceptional Conditions
      - type: CWE
        value: 398:Indicator of Poor Code Quality
      - type: CWE/SANS TOP 25
        value: 2010:Risky Resource Management - CWE ID 754
      - type: SJ/T 11683-2017
        value: 6.6.8 equals方法的参数为null
    Security: not
    Tag: bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150414'
    language: Java
  - Advice: "有如下四种解决方法：  \n 1.将SimpleDateFormat定义成局部变量，但是每调用一次方法意味创建一个SimpleDateFormat对象，浪费内存。\
      \  \n 2.方法加同步锁synchronized，在同一时刻，只有一个线程可以执行类中的某个方法。这样性能较差，每次都要等待锁释放后其他线程才能进入。\
      \  \n 3.使用第三方库joda-time，由第三方考虑线程不安全的问题。  \n 4.使用ThreadLocal：每个线程拥有自己的SimpleDateFormat对象。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 并发规范
      - type: DetailClassChin
        value: 【推荐】避免日期格式化缺陷
    Categories_En:
      Category: []
    Default: true
    Description: "日期格式对象是非线程安全的，`java.text.Format`中的`parse()`和`format()`方法包含一个可导致用户看到其他用户数据的race\
      \ condition。  \n  \n**例如**：下面代码片段中，定义了一个成员变量（静态的日期格式对象）。  \n```java\npublic\
      \ class DateFormat extends Thread{  \n    private static SimpleDateFormat sdf\
      \ = new SimpleDateFormat(\"yyyy-MM-dd\");  \n    private String name;  \n  \
      \  private String dateStr;  \n    public DateFormat(String name, String dateStr)\
      \ {  \n        this.name = name;  \n        this.dateStr = dateStr;  \n    }\
      \  \n    @Override  \n    public void run() {  \n        try {  \n         \
      \   Date date = sdf.parse(dateStr);  \n            System.out.println(\"线程\"\
      +name+\"运行\"+\":date:\"+date);  \n        } catch (ParseException e) {  \n \
      \           e.printStackTrace();  \n        }  \n    }  \n    public static\
      \ void main(String[] args) {  \n        ExecutorService executorService = Executors.newFixedThreadPool(3);\
      \  \n        executorService.execute(new DateFormat(\"A\", \"2017-06-10\"));\
      \  \n        executorService.execute(new DateFormat(\"B\", \"2016-06-06\"));\
      \  \n        executorService.shutdown();  \n    }  \n}\n```  \n如上代码中输出会有两种情况，一种情况是报错，还有一种情况是两个线程输出一致。出现这种情况的原因是因为SimpleDateFormat类内部有一个Calendar对象引用,它用来储存和这个SimpleDateFormat相关的日期信息。这样就会导致一个问题,如果SimpleDateFormat是static的,\
      \ 那么多个thread之间就会共享SimpleDateFormat, 同时也是共享Calendar引用。在高并发的情况下，容易出现幻读成员变量的现象。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Format Flaw
    ENSubClassChin: Race Condition
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 中
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 488:Exposure of Data Element to Wrong Session
      - type: CWE
        value: 362:Concurrent Execution using Shared Resource with Improper Synchronization
          ('Race Condition')
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 362
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 362
      - type: ISO/IEC TR 24772
        value: 2013:New Vulnerabilities Concurrent Data Access [CGX]
    Security: not
    Tag: concurrency,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150301'
    language: Java
  - Advice: "有如下四种解决方法：  \n1.将SimpleDateFormat定义成局部变量，但是每调用一次方法意味创建一个SimpleDateFormat对象，浪费内存。\
      \  \n2.方法加同步锁synchronized，在同一时刻，只有一个线程可以执行类中的某个方法。这样性能较差，每次都要等待锁释放后其他线程才能进入。\
      \  \n3.使用第三方库joda-time，由第三方考虑线程不安全的问题。  \n4.使用ThreadLocal：每个线程拥有自己的SimpleDateFormat对象。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.2 并发程序安全
      - type: DetailClassChin
        value: A.3.2.16 避免日期格式化缺陷
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：日期格式对象是非线程安全的，java.text.Format中的`parse()`和`format()`方法包含一个可导致用户看到其他用户数据的race\
      \ condition。  \n对于避免日期格式化缺陷的情况，示例1给出了不规范用法（java语言）示例。  \n**示例1**：   \n```java\n\
      public class DateFormat extends Thread {\n    private static SimpleDateFormat\
      \ sdf = new SimpleDateFormat(\"yyyy-MM-dd\");\n    private String name;\n  \
      \  private String dateStr;\n\n    public DateFormat(String name, String dateStr)\
      \ {\n        this.name = name;\n        this.dateStr = dateStr;\n    }\n\n \
      \   @Override\n    public void run() {\n        try {\n            Date date\
      \ = sdf.parse(dateStr);\n            System.out.println(\"线程\" + name + \"运行\"\
      \ + \":date:\" + date);\n        } catch (ParseException e) {\n            e.printStackTrace();\n\
      \        }\n    }\n\n    public static void main(String[] args) {\n        ExecutorService\
      \ executorService = Executors.newFixedThreadPool(3);\n        executorService.execute(new\
      \ DateFormat(\"A\", \"2017-06-10\"));\n        executorService.execute(new DateFormat(\"\
      B\", \"2016-06-06\"));\n        executorService.shutdown();\n    }\n}\n``` \
      \ \n上述代码片段中，定义了一个成员变量（静态的日期格式对象）。  \n如上代码中输出会有三种情况，一种情况是正常运行，一种情况是报错，还有一种情况是两个线程输出一致。出现两个线程输出一致的原因是因为SimpleDateFormat类内部有一个Calendar对象引用,它用来储存和这个SimpleDateFormat相关的日期信息。这样就会导致一个问题,如果SimpleDateFormat是static的,\
      \ 那么多个thread之间就会共享SimpleDateFormat, 同时也是共享Calendar引用。在高并发的情况下，容易出现幻读成员变量的现象。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 488:Exposure of Data Element to Wrong Session
      - type: CWE
        value: 362:Concurrent Execution using Shared Resource with Improper Synchronization
          ('Race Condition')
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 362
      - type: CWE/SANS TOP 25
        value: 2009:Insecure Interaction - CWE ID 362
      - type: ISO/IEC TR 24772
        value: 2013:New Vulnerabilities Concurrent Data Access [CGX]
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: 0302000010170048
    language: Java
  - Advice: 检查程序逻辑，SSL异常进行合理处理。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 异常规范
      - type: DetailClassChin
        value: 【推荐】不要忽略SSL异常
    Categories_En:
      Category: []
    Default: true
    Description: SSL相关的异常有`javax.net.ssl.SSLHandshakeException`、`javax.net.ssl.SSLKeyException`和`javax.net.ssl.SSLPeerUnverifiedException`，被用于传达与SSL连接相关的重要错误，如果选择不抛出这些异常或者是没有显式地处理这些异常，连接会被处于一种意想不到的、潜在不安全的状态。
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin:
    - Unhandled SSL Exception
    ENSubClassChin: Poor Error Handling
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 388:Error Handling
      - type: CWE
        value: 248:Uncaught Exception
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.4 未捕获异常
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: not
    Tag: exception,bad_practice
    Type: std
    WhiteList: not
    id: '0202000010150503'
    language: Java
  - Advice: 检查程序逻辑，对SSL异常进行合理处理。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 6.4 异常处理安全
      - type: DetailClassChin
        value: A.3.4.7 不要忽略SSL异常
    Categories_En:
      Category: []
    Default: true
    Description: 规范说明：SSL相关的异常有`javax.net.ssl.SSLHandshakeException`、`javax.net.ssl.
      SSLKeyException`和`javax.net.ssl.SSLPeerUnverifiedException`，被用于传达与SSL连接相关的重要错误，如果选择不抛出这些异常或者是没有显式地处理这些异常，连接会被处于一种意想不到的、潜在不安全的状态。
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 388:Error Handling
      - type: CWE
        value: 248:Uncaught Exception
      - type: OWASP TOP 10
        value: 2007:A6 Information Leakage and Improper Error Handling
      - type: OWASP TOP 10
        value: 2004:A7 Improper Error Handling
      - type: SJ/T 11683-2017
        value: 6.4.4 未捕获异常
      - type: PCI DSS
        value: 1.1:Requirement 6.5.7
      - type: PCI DSS
        value: 1.2:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.6
      - type: PCI DSS
        value: 2.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.0:Requirement 6.5.5
      - type: PCI DSS
        value: 3.1:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2:Requirement 6.5.5
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.5
    Security: not
    Tag: ''
    Type: guide
    WhiteList: not
    id: 0302000010170059
    language: Java
  - Advice: "对应的符合规则的解决方案是：首先将一个 Biglnteger 对象表示成 String 对象，然后将 String 对象转 换为一个字节数组。这个过程会对输入做相反的操作，因为在\
      \ String 对象中，其文本表示是由   \n    Biglnteger 类来生成的，所以它包含了合法的字符。   \n    ```java\n\
      \    Biglnteger x = new Biglnteger(\"530500452766\");   \n    String s = x.toString();\
      \ // valid character data   \n    try {   \n        byte[] byteArray = s.getBytes(\"\
      UTF8\");   \n        // ns prints as n530500452766u   \n        String ns =\
      \ new String(byteArray, \"UTF8\");   \n        // construct the original Biglnteger\
      \    \n        Biglnteger xl = new Biglnteger(ns);   \n    } catch (UnsupportedEncodingException\
      \ ex) {   \n        // handle error   \n    }\n    ```   \n    不要将一个字符串对象转换为一个字节数组来获得初始的\
      \ Biglnteger。字符编码的数据会产生一个字节数组，在转换为一个 Biglnteger 的时候这个字节数据会生成一个完全不同的数值。"
    Advice_En: ''
    BugServerity: 3
    Categories:
      Category:
      - type: ClassChin
        value: YD/T 3464-2019
      - type: SubClassChin
        value: 7.5 文件系统
      - type: DetailClassChin
        value: C.13.12 不要将原始的二进制数据作为字符数据读入
    Categories_En:
      Category: []
    Default: true
    Description: "1) 规定   \n在 Java 中，通常用字节数组来传输原始的二进制数据和字符编码数据，不要将原始的二进制数据作为字符数据读入。\
      \   \n2) 原理   \n以字符编码数据的形式读入原始二进制数据的操作通常会失败，因为一些字节会落入默认或者指定的编码规范之外，而不能表示成合法的字符。例如，将一个包含不可表示字节的加密密钥转换为字符编码数据进行传输，就会产生错误。\
      \   \n3) 示例   \n    这个不符合规则的代码示例想要将一个 Biglnteger 的字节数组转换为一个孛符串。因为一些字节不能表示成合法字符，所以字符串会损失信息。将字符串转换为\
      \ Biglnteger 会产生另一个不同的值。   \n    ```java\n    BigInteger x = new BigInteger(\"\
      530500452766\");\n    // convert x to a String\n    byte[] byteArray = x.toByteArray();\n\
      \    String s = new String(byteArray);\n    // convert s back to a BigInteger\n\
      \    byteArray = s.getBytes();\n    x = new BigInteger(byteArray);\n    ```\
      \   \n    当这个程序在Linux平台上运行并且其默认的编码格式是US-ASCII的时候，这个字符串s会得到{?J??的值，因为一些字符是不可以输出的。当将其转换回一个Biglnteger时，x的值是`149830058370101340468658109`。\
      \   \n    "
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference: []
    Security: not
    Tag: ''
    Type: YD/T 3464-2019
    WhiteList: not
    id: 0902000010160117
    language: Java
  - Advice: "示例2给出了规范用法（java语言）示例。  \n**示例2**：   \n```java\nSignature signetcheck\
      \ = Signature.getInstance(\"SHA256withRSA\");  \nsignetcheck.initSign(key);\
      \    \nsignetcheck.update(message);   \nreturn signetcheck.sign();\n```   \n\
      上述代码片段中，添加update的调用。"
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.3 访问控制
      - type: DetailClassChin
        value: A.2.3.1.4 保证必要加密步骤
    Categories_En:
      Category: []
    Default: true
    Description: "规范说明：在生成加密签名过程中，代码缺少必要的步骤，会削弱所生成签名的强度。  \n对于保证必要加密步骤的情况，示例1给出了不规范用法（java语言）示例。\
      \  \n**示例1**：  \n```java\nSignature signetcheck = Signature.getInstance(\"SHA256withRSA\"\
      );  \nsignetcheck.initSign(key);    \n//signetcheck.update(message);  //lack\
      \ of necessary steps  \nreturn signetcheck.sign();\n```   \n上述代码片段中，缺少`update`的调用，导致创建不基于任何数据的签名。\
      \  \n在生成加密签名过程中，执行所有必需的步骤，确保不会削弱签名的强度。"
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 325:Missing Required Cryptographic Step
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: OWASP MOBILE TOP 10
        value: 2014:M6 Broken Cryptography
      - type: GB/T 34944-2017 Java
        value: 安全功能:未使用盐值计算散列值
      - type: PCI DSS
        value: 1.1:Requirement 4.1
      - type: PCI DSS
        value: 1.2:Requirement 4.1
      - type: PCI DSS
        value: 1.2.1:Requirement 4.1
      - type: PCI DSS
        value: 2.0:Requirement 4.1
      - type: PCI DSS
        value: 3.0:Requirement 4.1
      - type: PCI DSS
        value: 3.1:Requirement 4.1
      - type: PCI DSS
        value: 3.2:Requirement 4.1
      - type: PCI DSS
        value: 3.2.1:Requirement 4.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170021'
    language: Java
  - Advice: '防止命令注入的方法如下：

      1. 程序对非受信的用户输入数据进行净化，删除不安全的字符。

      2. 创建一份安全字符串列表，限定用户的输入只能在该列表的允许范围内。

      3. 不要让用户直接控制eval()、exec()、readObject()等函数的参数。


      **例1**：下面代码片段中，直接向`exec()`方法传入常量。

      ```java

      ...

      String backuptype = "tables";

      String cmd = "cmd.exe /K \"c:\\util\\rmanDB.bat\" " + backuptype;

      Runtime.getRuntime().exec(cmd);

      ...

      ```


      **例2**：下面代码片段中，直接向`exec()`方法传入常量。

      ```java

      ...

      String cmd = new String("cmd.exe /K \"c:\\util\\rmanDB.bat tables\"");

      System.Runtime.getRuntime().exec(cmd);

      ...

      ```'
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: GB/T 34944-2017
      - type: SubClassChin
        value: 代码注入
      - type: DetailClassChin
        value: 有风险的命令执行
    Categories_En:
      Category: []
    Default: true
    Description: "命令注入是指，在应用程序执行的命令中包含来源于不可信数据时，程序本身没有对这些不可信数据做正确、合理的验证和过滤，导致系统执行恶意命令。系统库中的一些方法存在这样的风险，当传入方法的参数包含非可信数据源时，可能导致系统执行注入命令。\n\
      *该分类判断数据来源是否为常量，主要起提醒作用。*\n\n**例1**：以下代码通过`Runtime.exec()`方法执行Windows的dir命令，列出目录列表。\n\
      ```java\nimport java.io.*;\npublic class DirList {\n    public static void main(String[]\
      \ args) throws Exception {\n        Process proc = Runtime.getRuntime().exec(\"\
      cmd.exe /c dir \" + args[0]);\n        int result = proc.waitFor();\n      \
      \  if (result != 0) {\n            System.out.println(\"process error: \" +\
      \ result);\n        }\n        InputStream in = (result == 0) ? proc.getInputStream()\
      \ : proc.getErrorStream();\n        int c;\n        while ((c = in.read()) !=\
      \ -1) {\n            System.out.print((char) c);\n        }\n    }\n}\n```\n\
      攻击者可以通过以下命令利用该程序：\n`java -Ddir='dummy & echo bad' DirList`\n该命令实际上执行了两条命令：\n\
      `cmd.exe /c dir dummy & echo bad`\n\n**例2**：以下代码片段来自一个Web应用程序，该段代码通过运行rmanDB.bat脚本启动Oracle数据库备份，然后运行cleanup.bat脚本删除一些临时文件。脚本文件rmanDB.bat接受一个命令行参数，其中指明需要执行的备份类型。\n\
      ```java\n...\nString cmd = new String(\"cmd.exe /K \\\"c:\\\\util\\\\rmanDB.bat\
      \ \"+backuptype+\"&& c:\\\\utl\\\\cleanup.bat\\\"\");\nSystem.Runtime.getRuntime().exec(cmd);\n\
      ...\n```\n该段代码没有对来自用户请求中的参数`backuptype`做任何校验。\n通常情况下，`Runtime.exec()`函数不会执行多条命令，但在上面的例子里，黑客为了注入多条执行命令，通过`“&&”`分隔符号来拼接多条命令，以达到依次执行指定命令的目的。"
    Description_En: ''
    DomainType: security
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行进行了有风险的命令执行。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行进行了有风险的命令执行。"
      th:remove="tag"></p>'
    ENClassChin: Input Validation and Representation
    ENDetailClassChin:
    - ''
    ENSubClassChin: Command Injection
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: 低
    GradesComment: ''
    J2ee: not
    Obsolete: not
    References:
      Reference:
      - type: CWE TOP 25
        value: 2022:CWE ID 78
      - type: CWE TOP 25
        value: 2022:CWE ID 77
      - type: CWE TOP 25
        value: 2021:CWE ID 78
      - type: CWE TOP 25
        value: 2021:CWE ID 77
      - type: CWE TOP 25
        value: 2020:CWE ID 78
      - type: CWE TOP 25
        value: 2019:CWE ID 78
      - type: CWE/SANS TOP 25
        value: 2011:Insecure Interaction - CWE ID 78
      - type: CWE/SANS TOP 25
        value: 2010:Insecure Interaction - CWE ID 78
      - type: CWE
        value: 78:Improper Neutralization of Special Elements used in an OS Command
          ('OS Command Injection')
      - type: CWE
        value: 77:Improper Neutralization of Special Elements used in a Command ('Command
          Injection')
      - type: CERT
        value: IDS07-J:Sanitize untrusted data passed to the Runtime.exec() method
      - type: OWASP TOP 10
        value: 2021:A3 Injection
      - type: OWASP TOP 10
        value: 2017:A1 Injection
      - type: OWASP TOP 10
        value: 2013:A1 Injection
      - type: OWASP TOP 10
        value: 2010:A1 Injection
      - type: OWASP TOP 10
        value: 2007:A2 Injection Flaws
      - type: OWASP TOP 10
        value: 2004:A6 Injection Flaws
      - type: ISO/IEC TR 24772
        value: 2013:Application Vulnerabilities Injection [RST]
      - type: PCI DSS
        value: 1.1:Requirement 6.5.6
      - type: PCI DSS
        value: 1.2:Requirement 6.5.2
      - type: PCI DSS
        value: 1.2.1:Requirement 6.5.2
      - type: PCI DSS
        value: 2.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.0:Requirement 6.5.1
      - type: PCI DSS
        value: 3.1:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2:Requirement 6.5.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.5.1
      - type: GB/T 39412-2020
        value: 6.安全功能缺陷审计:6.1.数据清洗:6.1.1.输入验证:6.1.1.6.命令行注入
      - type: OWASP ASVS
        value: v4.0.3-5.3.8
      - type: OWASP ASVS
        value: v4.0.3-12.3.5
      - type: GB/T 34944-2017 Java
        value: 6.2.3.数据处理:6.2.3.3.命令注入
    Security: true
    Tag: security
    Type: bug
    WhiteList: not
    id: '0402000010020022'
    language: Java
  - Advice: 应用程序注释中不应保留密码等敏感信息。
    Advice_En: ''
    BugServerity: 1
    Categories:
      Category:
      - type: ClassChin
        value: CWE
      - type: SubClassChin
        value: 5.2 数据加密与保护
      - type: DetailClassChin
        value: A.2.2.2.2 避免在注释中保留密码
    Categories_En:
      Category: []
    Default: true
    Description: 规范说明：应用程序注释中保留密码等敏感信息，将使敏感信息对任何能够获取到该文件的人员可见。
    Description_En: ''
    DomainType: ''
    DynamicDescription: '<p th:if="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的+''`''+${funcName}+''`''+方法中的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>

      <p th:unless="${funcNameisEmpty}" th:text="在+''`''+${fileName}+''`''+的第+''`''+${line}+''`''+行违反+''`''+${categoryName}+''`''+规范。"
      th:remove="tag"></p>'
    ENClassChin: ''
    ENDetailClassChin: []
    ENSubClassChin: ''
    FormalizationData:
      CodeFragments:
        CodeFragment: []
      Relationships:
        Relationship: []
    Grades: ''
    GradesComment: ''
    J2ee: ''
    Obsolete: not
    References:
      Reference:
      - type: CWE
        value: 615:Information Exposure Through Comments
      - type: CERT
        value: MSC03-J:Never hard code sensitive information
      - type: OWASP TOP 10
        value: 2017:A3 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2013:A6 Sensitive Data Exposure
      - type: OWASP TOP 10
        value: 2010:A7 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2007:A8 Insecure Cryptographic Storage
      - type: OWASP TOP 10
        value: 2004:A8 Insecure Storage
      - type: PCI DSS
        value: 1.1:Requirement 6.3.6
      - type: PCI DSS
        value: 1.2:Requirement 6.3.6
      - type: PCI DSS
        value: 1.2.1:Requirement 6.3.6
      - type: PCI DSS
        value: 2.0:Requirement 6.3.1
      - type: PCI DSS
        value: 3.0:Requirement 6.3.1
      - type: PCI DSS
        value: 3.1:Requirement 6.3.1
      - type: PCI DSS
        value: 3.2:Requirement 6.3.1
      - type: PCI DSS
        value: 3.2.1:Requirement 6.3.1
    Security: true
    Tag: ''
    Type: guide
    WhiteList: not
    id: '0302000010170017'
    language: Java
