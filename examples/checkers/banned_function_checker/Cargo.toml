[package]
name = "uniflow-example-banned-function-checker"
version = "1.0.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1.0"
uniflow-checker-api = { path = "../../../crates/checker_api" }

[workspace]
